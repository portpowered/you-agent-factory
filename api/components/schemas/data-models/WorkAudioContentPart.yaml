description: Ordered audio content for one work item.
allOf:
  - $ref: './WorkContentCommonFields.yaml'
  - type: object
    additionalProperties: false
    required:
      - type
      - url
    properties:
      type:
        allOf:
          - $ref: './WorkContentPartType.yaml'
        enum:
          - AUDIO
      url:
        $ref: './WorkContentURLProperty.yaml'
      file:
        $ref: './WorkContentDeprecatedFileProperty.yaml'
