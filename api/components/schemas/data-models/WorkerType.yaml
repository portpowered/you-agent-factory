type: string
description: Worker implementation families supported by the public factory-config contract.
enum:
    - INFERENCE_WORKER
    - AGENT_WORKER
    - SCRIPT_WORKER
    - POLLER_WORKER
    - MODEL_WORKER
    - HOSTED_WORKER
