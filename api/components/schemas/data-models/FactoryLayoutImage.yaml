type: object
description: Inert embedded-raster image content with required alternative text.
additionalProperties: false
required:
  - source
  - alternativeText
properties:
  source:
    $ref: './FactoryLayoutImageSource.yaml'
  alternativeText:
    type: string
    minLength: 1
    maxLength: 500
    description: Literal alternative text for the embedded image.
