type: string
description: String-first parsing and UI hint for one factory invocation parameter.
enum:
    - STRING
    - PATH
    - FILE_PATH
    - DIRECTORY_PATH
    - NUMBER_STRING
    - BOOLEAN_STRING
