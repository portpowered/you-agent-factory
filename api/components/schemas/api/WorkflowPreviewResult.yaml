deprecated: true
description: >-
  Obsolete transitional Batch 001 workflow-preview result shape. Use
  FactoryPreviewResult on POST /factories/preview for JavaScript-orchestrated
  Factory preview semantics.
type: object
additionalProperties: false
required:
  - valid
  - sourceResolution
  - sourceValidationIssues
  - policyPreview
  - resultConstraints
properties:
  valid:
    type: boolean
    description: True when source resolution, validation, policy, and artifact-root checks pass.
  sourceResolution:
    $ref: './WorkflowSourceResolution.yaml'
  sourceValidationIssues:
    type: array
    description: Workflow source, loader, and orchestrator validation diagnostics.
    items:
      $ref: './WorkflowDiagnostic.yaml'
  policyPreview:
    $ref: './WorkflowPolicyPreview.yaml'
  resultConstraints:
    $ref: './WorkflowResultConstraints.yaml'
