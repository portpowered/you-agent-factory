type: object
additionalProperties: false
required:
  - valid
  - sourceResolution
  - sourceValidationIssues
  - policyPreview
  - resultConstraints
properties:
  valid:
    type: boolean
    description: True when source resolution, validation, policy, and artifact-root checks pass for the JavaScript-orchestrated factory preview.
  sourceResolution:
    $ref: './WorkflowSourceResolution.yaml'
  sourceValidationIssues:
    type: array
    description: Factory source, loader, and orchestrator validation diagnostics.
    items:
      $ref: './WorkflowDiagnostic.yaml'
  policyPreview:
    $ref: './WorkflowPolicyPreview.yaml'
  resultConstraints:
    $ref: './WorkflowResultConstraints.yaml'
