type: object
additionalProperties: false
required:
  - providerSession
  - source
  - parse
  - transcript
properties:
  providerSession:
    $ref: './LoadableProviderSessionRef.yaml'
  source:
    $ref: './ProviderSessionSourceMetadata.yaml'
  parse:
    $ref: './ProviderSessionParseSummary.yaml'
  transcript:
    type: array
    description: Ordered transcript entries extracted from the provider-session stream.
    items:
      $ref: './ProviderSessionTranscriptEntry.yaml'
