name: mode
in: query
required: false
description: >-
  Optional durable result retrieval mode. Defaults to FINAL for terminal outputs.
  PARTIAL returns the latest partial workflow output when available.
schema:
  $ref: '../schemas/api/FactorySessionResultMode.yaml'
