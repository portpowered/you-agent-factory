name: logical_session_key_id
in: query
required: false
description: >-
  Optional canonical logical-session key derived from the normalized factory
  session target. When the requested session selector is missing or stale,
  resolution uses backendScopeId plus this key to locate the replacement
  current live Factory Session.
schema:
  type: string
