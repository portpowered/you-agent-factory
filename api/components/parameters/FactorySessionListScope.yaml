name: scope
in: query
required: false
description: >-
  Optional session list scope. Defaults to LIVE for backward-compatible live
  workspace session listing.
schema:
  $ref: '../schemas/api/FactorySessionListScope.yaml'
