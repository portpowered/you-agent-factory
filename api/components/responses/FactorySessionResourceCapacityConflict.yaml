description: >-
  Resource capacity admission was rejected because the request revision or
  Factory Session lifecycle is stale, the requested capacity is below units in
  use, or the request would be an exact no-op.
content:
  application/json:
    schema:
      oneOf:
        - $ref: '../schemas/api/FactorySessionResourceCapacityResponse.yaml'
        - $ref: '../schemas/api/ErrorResponse.yaml'
