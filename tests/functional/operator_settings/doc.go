// Package operator_settings owns functional root.BuildProcess evidence for
// packaged Operator Settings: construction stays inert until runtime lifecycle
// activates operator-config load, defaults resolution, identity/input inventory,
// and HTTP/CLI/MCP transport surfaces through public process composition and
// edges.Edges effect replacement only. Proofs import only published Operator
// Settings contracts plus shared functional support; peer_import_boundary_test.go
// seals forbidden operator_settings/internal and deleted transitional Settings
// package imports (servicewire/, identityinventory/, testlink/, testproviders/).
package operator_settings
