// Package automations owns functional root.BuildProcess evidence for packaged
// Automations cron scheduling, filesystem watcher preseed, and hosted Linear
// polling: construction stays inert until runtime lifecycle starts scheduler
// sidecars, preseeds watched inputs, or admits hosted-source Work through the
// Automations service root.
package automations
