# Portable corpus fixtures

The `clip.mp4` files are small synthetic ISO BMFF metadata fixtures with a
sentinel payload. They exercise index identity, metadata parsing, and the
Runner V2 scripted component tests. They are not playable media and do not
prove decoding or semantic inference. No external corpus media is included.
