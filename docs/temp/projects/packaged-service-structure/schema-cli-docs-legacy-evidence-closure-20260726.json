{
  "requestId": "schema-cli-docs-legacy-evidence-closure-20260726",
  "type": "FACTORY_REQUEST_BATCH",
  "works": [
    {
      "name": "schema-cli-docs-legacy-evidence-closure",
      "workTypeName": "idea",
      "payload": {
        "title": "Close canonical CLI documentation, legacy definitions, and drift evidence",
        "sourcePath": "docs/temp/projects/schema-driven-cli/plan.md",
        "currentProgress": [
          "PR #1330 merged reviewed head 16d407ea0 as ff23390dc after all acceptance criteria and required CI passed; the paired Factory idea and task are terminal complete.",
          "Production root construction already consumes generated manifest families for session, work, run, and submit, so submitting another family schema migration would duplicate accepted construction work.",
          "Secondary writable definitions remain: production help/usage still depends on legacy constructor or handwritten usage projection paths, legacy work/run/submit constructors and CLI-shape compatibility mirrors remain compiled for tests or adapters, and the final canonical docs/deletion/drift evidence checklist is still open.",
          "The accepted review forbids source-, route-, or inventory-only Go tests as behavioral evidence; static source policy may remain in the non-test lint path while executable behavior and semantic parity tests provide runtime proof."
        ],
        "requestedOutcome": "Deliver the smallest CLI-first closure slice after run/server acceptance. Audit the production session, work, submit, run, root, init/config, mock-worker, Factory-authoring, and packaged reference surfaces against the canonical CLI manifest and accepted customer behavior. Remove or narrow remaining legacy Cobra constructors, usage scraping, duplicate help/input inventories, and CLI-shape config mirrors only where generated production families and typed transport adapters already provide parity; do not change customer behavior or remove domain request types. Make help, parser defaults, relationships, completion, stable handler bindings, and published package artifacts derive from the authored/generated contract with no second writable definition. Update only canonical packaged docs and directly related invocation maps that are still stale. Preserve non-test static lint policy, but use observable root.BuildProcess/built-CLI behavior and semantic generated-artifact parity instead of source-, route-, or inventory-only Go tests. Prove clean generation and drift, docs-reference smoke, focused CLI functional behavior for session/work/submit/run/server/init and removed inputs, short tests, lint, and the appropriate PR verification gate. Exclude new session/work/submit features, run/server lifecycle redesign, OpenAPI shape changes unless a real contract gap is first demonstrated, provider/Workers/Models/package restructuring, UI work, and broad functional-test topology changes."
      }
    },
    {
      "name": "schema-cli-docs-legacy-evidence-closure-loopback",
      "workTypeName": "thoughts",
      "payload": {
        "title": "Review canonical CLI closure delivery and choose the next CLI slice",
        "sourcePath": "docs/temp/schema-cli-docs-legacy-evidence-closure-20260726.json",
        "requestedOutcome": "After the CLI docs/legacy/evidence idea is reviewed, merged, and Factory-consumed, inspect exact implementation, review evidence, tests, changed paths, queue/session health, and the remaining Schema-Driven CLI and Clean CLI checklist. Reconcile failed or stranded work, then decide whether both CLI programs are complete, a narrow correction/publication slice remains, or a deliberate hold is required."
      }
    }
  ],
  "relations": [
    {
      "type": "DEPENDS_ON",
      "sourceWorkName": "schema-cli-docs-legacy-evidence-closure-loopback",
      "targetWorkName": "schema-cli-docs-legacy-evidence-closure",
      "requiredState": "complete"
    }
  ]
}
