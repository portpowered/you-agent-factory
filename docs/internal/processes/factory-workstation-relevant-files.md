# Factory Workstation Relevant Files

This file inventories the checked-in guidance surfaces that active repository
workstation prompts should cite when they tell operators where to read or write
work inputs.

## Active prompt guidance inventory

| Surface | Role in the workflow | Current contract |
| --- | --- | --- |
| `factory/README.md` | Checked-in workflow overview | Describes the repository-local workflow and canonical inbox directories under `factory/inputs/`. |
| `factory/internal/{asks,view,progress,meta}.md` | Canonical repository-maintainer control surface | The checked-in maintainer workflow reads and updates these four files as one control surface; `asks.md` owns customer asks, `view.md` owns the current repository view, `progress.md` owns maintainer progress, and `meta.md` owns theory of mind. |
| `docs/internal/development/process-review-loop-contract.md` | Checked-in process/review contract note | Defines the canonical meaning of `<CONTINUE>`, `<COMPLETE>`, and true rejection for the repository-maintainer workflow, records that the old rejection-overload mismatch is historical rather than current, and names the runtime, prompt, config, and replay surfaces that must stay aligned. |
| `docs/internal/development/root-factory-artifact-contract-inventory.md` | Checked-in artifact inventory | Documents which root-level factory artifacts are checked in, generated, or obsolete. |
| `docs/reference/batch-inputs.md` | Canonical batch request guide | Defines when to author `FACTORY_REQUEST_BATCH` JSON and where those files belong. |
| `factory/inputs/idea/default/` | Standalone idea inbox | Checked-in inbox kept present by `.gitkeep`; standalone idea submissions land here as markdown files. |
| `factory/inputs/task/default/` | Standalone task inbox | Checked-in inbox kept present by `.gitkeep`; standalone task submissions land here as markdown files. |
| `factory/inputs/BATCH/default/` | Ordered or mixed-work-type request inbox | Canonical placement for `FACTORY_REQUEST_BATCH` JSON when operators need dependency ordering or mixed work types. |
| `factory/workstations/cleaner/AGENTS.md` | Active cleanup workstation prompt | Should cite only checked-in workflow docs and the live idea or batch inboxes. |
| `factory/workstations/ideafy/AGENTS.md` | Active ideation workstation prompt | Should default to one standalone idea markdown output and reserve batch JSON output for dependency-ordered or mixed-work-type follow-up. |
| `factory/workstations/plan/AGENTS.md` | Active planning workstation prompt | PRD and story authoring should require behavioral acceptance criteria and avoid planning meta tests that only assert implementation structure. |
| `factory/workstations/process/AGENTS.md` | Active execution workstation prompt | Implementation guidance should prefer observable runtime, API, CLI, UI, or emitted-event tests over source, docs, bundle, command, or route inventory assertions. |
| `factory/workstations/review/AGENTS.md` | Active review workstation prompt | Review guidance should treat meta tests as a blocking quality issue when they do not verify real product behavior. |

## Notes for future iterations

- Treat `factory/inputs/idea/default/` as the live standalone idea inbox, not as a checked-in template catalog; clean checkouts may only contain `.gitkeep`.
- Treat `factory/inputs/task/default/` as the live standalone task inbox, not as a checked-in template catalog; clean checkouts may only contain `.gitkeep`.
- Treat `factory/internal/{asks,view,progress,meta}.md` as one control surface; if maintainer guidance mentions one of these files, keep the other three aligned in the same `factory/internal/` family.
- The canonical `factory/internal/{asks,view,progress,meta}.md` control-surface files are gitignored by default, so intentional contract repairs there must be force-added to review commits; otherwise local test fixes can disappear from the PR diff and CI checkout.
- Keep all four `factory/internal/{asks,view,progress,meta}.md` files tracked in clean checkouts whenever the artifact contract classifies them as `checked_in`; prompt and test guidance now relies on their physical presence, not just documentation references.
- Before redispatching a checked-in workflow-input markdown file, verify that the lane is not already landed on `main`; stale inbox residue should be treated as cleanup, not as a fresh request.
- Do not introduce a second checked-in maintainer backlog path or a redirect-only parallel surface; the canonical maintainer control plane lives only under `factory/internal/`.
- When prompt instructions need ordered or multi-item follow-up work, point them to `docs/reference/batch-inputs.md` and `factory/inputs/BATCH/default/` instead of overloading the markdown idea inbox.
- Treat watched file-input directories as a strict three-segment contract: `factory/inputs/<work-type-or-BATCH>/<channel>/<file>`. Direct drops at `factory/inputs/<work-type>/<file>` are invalid and watcher regressions in this area should be proved through submitted or skipped runtime outcomes, not helper-only assertions.
- Keep workstation prompts repository-local and public-surface neutral: cite checked-in docs or `factory/` paths in this repo, never absolute paths to a different checkout or merge-conflict marker text.
- When the checked-in `process` or `review` loop semantics change, update the contract note, runtime/config surfaces, prompt text, and replay or behavioral evidence together; otherwise ordinary iteration and true rejection drift back into ambiguous history.
- If a workstation can make ordinary partial progress, model that loop with a dedicated `onContinue` route and `CONTINUE` outcome instead of reusing rejection; keep `onRejection` reserved for true negative business or review results.
- When the public workstation outcome contract changes, update the live prompts, `pkg/cli/init` scaffold text, and the workstation authoring/reference docs together so generated workflow text does not drift from the checked-in maintainer flow.
- When the public workstation route families (`outputs`, `onContinue`, `onRejection`, `onFailure`) change shape, update both scaffolded `factory.json` examples in `pkg/cli/init/scaffolds.go` and the checked-in CLI reference docs in `pkg/cli/docs/reference/`; otherwise `agent-factory init` teaches a stale authoring contract even when runtime and OpenAPI already accept the new shape.
- When a public workstation route family changes shape, update all four ownership seams together: the source OpenAPI schema/examples, generated Go and TypeScript clients, `pkg/config` boundary normalization/mapping, and replay or projection fixtures that embed generated factory payloads.
- When a public per-input guard type changes, update the same four seams together: source OpenAPI guard schemas, generated Go and TypeScript clients, `pkg/config` boundary normalization and validation, and any split-workstation frontmatter coverage that proves `AGENTS.md` guard loading.
- When a peer-input guard needs boundary-to-runtime regression coverage, prefer one checked-in fixture under `tests/functional_test/testdata/` that carries `factory.json` plus worker and workstation `AGENTS.md`, and reuse that fixture for generated-boundary, runtime-load, and functional dispatch assertions instead of rebuilding the config inline in each test.
- When a functional runtime test scaffolds a split-config factory with `tests/functional/internal/support.ScaffoldFactory(...)`, add worker `workers/<name>/AGENTS.md` files explicitly for any dispatched model workers; the helper seeds workstation `AGENTS.md` files but does not author worker configs or stop tokens.
- The generated OpenAPI factory/load boundary now accepts workstation `onContinue`, `onRejection`, and `onFailure` only as arrays; do not reintroduce single-object wrapping in `pkg/config/openapi_factory.go`, and treat any checked-in single-object authoring as fixture drift to canonicalize.
- When public workstation topology is converted between `WorkstationIO` arrays and internal place-id slices, preserve authored slice order end to end; sorting those routes during replay or projection silently changes first-match routing semantics even when the route count survives.
- When workstation `AGENTS.md` route families such as `onContinue`, `onRejection`, or `onFailure` are arrays, validate them with the same list-walking alias helpers used for `inputs` and `outputs`; otherwise retired-field errors lose their item index and nested route entries drift past authored-boundary checks.
- When the UI imports or exports canonical factory definitions, keep the handwritten `ui/src/api/factory-definition/api.ts` decoder and PNG import/export fixtures aligned with the generated OpenAPI `Factory` shape; stale singular route parsing there silently truncates multi-route `onContinue`, `onRejection`, and `onFailure` arrays even when backend transport is already correct.
- When the dashboard needs an editable current factory definition, load it through `ui/src/api/current-factory-definition/api.ts`, which combines the existing `GET /factory/~current` transport with `ui/src/api/factory-definition/api.ts` normalization; do not build edit flows from display-only dashboard state or from raw `getCurrentFactory()` payloads alone.
- When graph editing needs topology state before the editable-definition query resolves, derive a fallback view from the dashboard projection but keep the editable-definition document as the only canonical draft/save source; dirty drafts should continue to track the latest document version without being overwritten by refreshes.
- Keep current-factory graph editor mode as a thin shell around the existing React Flow viewport: put mode-toggle, dirty-state prompts, and editable-definition save wiring in dedicated editor modules, and keep the monitoring graph layout/rendering path isolated so editor-mode UI can evolve without turning `react-flow-current-activity-card.tsx` into a multi-responsibility file.
- Reusable graph-editor chrome belongs in `ui/src/features/factory-graph-editor/components/factory-graph-editor-controls.tsx`, the local `components/factory-graph-editor-tooltip-button.tsx` seam, and the shared `ui/src/components/ui/popover.tsx` wrapper; future add menus, node-action popovers, and destructive confirmations should extend those seams and prove representative states through focused Storybook stories instead of adding bespoke per-surface overlays.
- Tests that mount `ui/src/features/workflow-activity/components/workflow-activity-bento-card.tsx`, App-level dashboard shells, or similar current-activity graph shells should mirror `components/react-flow-current-activity-card.test.tsx` and stub `useCurrentEditableFactoryDefinitionDocument`, `useSaveCurrentEditableFactoryDefinition`, and `useFactoryGraphDraftState`; the graph-editor hook stack now mounts in observe mode as part of the shared chrome.
- Workflow-activity tests that render localized shells such as `workflow-activity-bento-card.tsx` should derive expected accessible names from the same message catalog helpers the component uses, rather than hardcoding English viewport or toolbar labels when the test passes a non-default `locale`.
- Graph-editor removal browser verification should stay split across focused Storybook seams: destructive confirmation and blocked-removal notice states belong in `ui/src/features/factory-graph-editor/components/factory-graph-editor-controls.stories.tsx`, while pending-removal node and edge rendering belongs in `ui/src/features/factory-graph-editor/components/factory-graph-editor-flow.stories.tsx`.
- Graph-editor add-entity browser verification should stay in focused dialog seams: prove work-type creation, work-state creation, and field-level validation copy in `ui/src/features/factory-graph-editor/components/factory-graph-editor-add-dialog.stories.tsx`, while keeping draft-mutation orchestration in `ui/src/features/workflow-activity/components/react-flow-current-activity-card.test.tsx` instead of widening dashboard-wide browser suites.
- Pending graph removals should render from the union of the latest editable-definition topology and the current draft topology, with explicit removal badges driven by `draft.removals` and `edgeChanges.removals`; otherwise server-backed nodes disappear too early and customers cannot review destructive edits before save or discard.
- When a graph-entity delete attempt is locally ineligible, surface the exact blocking reason through shared editor notices instead of failing silently; customers need actionable feedback before they can fix assignments or references and retry the removal.
- When graph editor add-entity flows need available workers, work types, or duplicate checks, derive those menus and validations from the pending editable-definition draft (`pendingFactoryDefinition`) instead of the projection snapshot so unsaved additions remain selectable and validation stays aligned with the next save payload.
- When graph-editor draft validation needs duplicate identifier detection, count node IDs from the pending editable-definition entity arrays before converting them into keyed maps; `Map`-indexed topology builders collapse duplicates and cannot prove pre-save duplicate-name errors on their own.
- When the dashboard needs selected-workstation edit values, resolve the selected runtime node against the canonical `factory.workstations` list by `transition_id` or workstation name, then join that workstation to `factory.workers` through `workstation.worker`; prompt lives on workstation `body`, template on `promptFile`, and model on the referenced worker.
- When the dashboard opens a workstation edit session, hydrate the draft from canonical editable values once per selected workstation and only rehydrate on later current-factory refreshes if the local draft is still clean; dirty drafts should keep the customer's in-progress values until an explicit save or reset path handles the conflict.
- When the dashboard saves current factory edits, use the typed editable-definition save endpoint (`PUT /factory/~current/editable-definition`) rather than `POST /factory`; keep mutation orchestration in `ui/src/features/current-selection/` and update both seams on success: write the normalized saved definition into the `current-editable-factory-definition` React Query cache and mark the local workstation draft clean in the same success path so the header save action, form status, and saved values stay aligned without a second local shadow copy.
- When editable-definition saves reject topology before persistence, return `apisurface.TopologyValidationError` targets that map graph issues to `node`, `edge`, `field`, or `save` locations; the API handler should preserve those targets in the `INVALID_FACTORY` response instead of collapsing every validation failure to the form root.
- When the current-selection workstation editor narrows to workstation-scoped fields such as `worker` and `prompt`, keep the mutation path anchored on the canonical current-factory definition but only apply the fields that truly belong to the selected workstation; preserve unrelated worker-model and template data from the latest fetched definition instead of carrying stale hidden values forward into the editable-definition save payload.
- When a workstation edit surface exposes worker-backed fields such as `model`, verify whether the referenced worker is shared by sibling workstations before treating the field as workstation-scoped; if workers can be shared, disable or clearly rescope that edit so one workstation save cannot silently rewrite others.
- When a dirty workstation draft survives a newer current-factory refresh, keep the session-start draft and the latest server-backed draft side by side; derive overwrite warnings by comparing the current draft to the latest server draft instead of rehydrating away the customer's pending input.
- When `FACTORY_CHANGE` events can arrive while the graph editor is open, update the normalized current-factory cache and invalidate the editable-definition document query together; stale-save warnings depend on refreshed version metadata, not just refreshed topology payloads.
- Keep explicit graph-editor save/discard chrome in dedicated editor modules such as `components/react-flow-current-activity-card-editor-dialogs.tsx`, `components/react-flow-current-activity-card-editor-chrome.tsx`, and `components/factory-graph-editor-draft-actions.tsx`; that keeps confirmation, stale-warning, and dirty-state behavior reusable without growing the main dashboard card past lint thresholds.
- For coverage-sensitive graph-editor fixes, prefer focused tests at the small controller, dialog, and copy seams (`react-flow-current-activity-card-editor-controllers.test.tsx`, `components/factory-graph-editor-add-dialog.test.tsx`, and edge-removal copy tests), and keep `components/react-flow-current-activity-card.coverage.test.tsx` limited to viewport wiring that needs the React Flow shell mocked.
- When browser replay or export integration fixtures stand up a lightweight dashboard API server, mirror the current editable-definition contract there too: `GET` and `PUT /factory/~current/editable-definition` need the typed document shape plus CORS headers, or graph-editor fetches will fail in replay lanes even when the production dashboard code is correct.
- When a selection-scoped dashboard mutation needs prominent affordance without changing the page toolbar contract, prefer the existing `SelectionDetailLayout` `headerAction` slot; it keeps feature-specific primary actions beside shared undo/redo controls while leaving the global dashboard header focused on page-level controls.
- When current-selection workstation editing needs expand or collapse behavior, keep the disclosure shell in `ui/src/features/current-selection/workstation-editable-configuration-section.tsx` and prove it through `ui/src/features/current-selection/workstation-detail-card.editable-configuration.test.tsx`; that keeps the interaction contract close to the form and avoids scattering disclosure state through the widget container.
- When current-selection workstation editing needs canonical editable factory data, enable the editable definition query whenever a workstation node is selected, including initial widget mount; selection-transition-only gates leave already-selected workstation editors stuck without worker or prompt values.
- When current-selection workstation editing needs prompt-variable help, keep the query boundary in `ui/src/features/current-selection/useCurrentWorkstationPromptTemplateContract.ts`, map its result into explicit `loading`, `empty`, `error`, and `ready` form state inside `use-editable-workstation-configuration-state.ts`, and render the disclosure in `workstation-editable-configuration-section.tsx`; that keeps fetch logic out of the field renderer while preserving one typed help contract per selected workstation.
- When current-selection workstation editing needs inline prompt diagnostics, keep authoritative validation in `useCurrentWorkstationPromptTemplateValidation.ts`, fold its `idle`/`loading`/`error`/`ready` states into `use-editable-workstation-configuration-state.ts`, and render the textarea overlay plus diagnostic list from `workstation-prompt-field.tsx`; that keeps save-blocking, inline squiggles, and supporting copy driven by one validation contract instead of component-local parsing.
- When current-selection tests mount `CurrentSelectionWidget` with the real prompt-help or prompt-validation hooks, render through a `QueryClientProvider` wrapper and keep App-shell fetch assertions scoped to the endpoint under test; selected-workstation shells now issue prompt-template contract queries during normal editor-state setup.
- When browser replay or export fixtures mount the real current-selection workstation editor, stub `GET /factory/~current/workstations/{name}/prompt-template-contract` and `POST /factory/~current/workstations/{name}/prompt-template-validation` alongside the editable-definition endpoints; prompt-template queries are now part of normal selected-workstation setup, and missing fixture routes surface as browser-console 404 failures in replay lanes.
- For browser-backed current-selection workstation evidence, keep the main switch-edit-switch proof in `ui/src/App.stories.tsx` and activate the editable-configuration disclosure from the keyboard at least once; that story seam is the browser-visible guard for disclosure semantics, singular configuration rendering, and workstation rebinding across desktop and narrow layouts.
- When a live factory-definition save must refresh already-open dashboards, emit a canonical `FACTORY_CHANGE` event with the same factory payload shape used by `INITIAL_STRUCTURE_REQUEST`, then update both replay reducers (`pkg/factory/projections/world_state.go`, `ui/src/features/timeline/state/timeline/replayWorldState.ts`) and the editable current-factory cache bridge in `ui/src/features/dashboard/hooks/useDashboardSnapshot.ts` together.
- When UI replay or topology route families change, update all three dashboard seams together: `ui/src/features/timeline/state/timeline/replayFactoryTopology.ts`, `ui/src/features/timeline/state/timeline/projectTopology.ts`, and `ui/src/api/dashboard/types.ts`; replay placement, graph edges, and typed outcome kinds otherwise drift apart.
- When workstation-request current-selection moves inference-owned detail between dispatch summaries and inference attempts, update all four seams together: the authored factory-world projection schemas under `api/components/schemas/factory-world/`, regenerated Go and TypeScript API artifacts, handwritten dashboard request adapters in `ui/src/features/current-selection/` and `ui/src/features/timeline/state/timeline/`, and the selected-work current-selection tests that prove prompts and responses render from attempts rather than dispatch summaries.
- Keep `ui/src/features/current-selection/execution-details.tsx` dispatch-scoped for inference-backed runs; provider, model, prompt, and provider-session diagnostics belong under `Inference attempts`, while `Execution details` should stay limited to dispatch identity, workstation identity, elapsed time, trace navigation, and the thin workstation-request summary.
- The runtime `workstation_requests_by_dispatch_id` timeline projection is intentionally thinner than the selected-work `workstationRequestsByDispatchID` projection; runtime request and response entries must not regain attempt-owned prompt, response, provider-session, or metadata fields, and verification in this area should assert those details through nested `inference_attempts` instead.
- When `useCurrentSelection` falls back from `workstationRequestsByDispatchID` to `snapshot.runtime.workstation_requests_by_dispatch_id`, rehydrate each normalized request from `snapshot.runtime.inference_attempts_by_dispatch_id` in the same pass; otherwise selected-work dispatch history silently loses the attempt-owned prompt, response, and provider-session surface even though the runtime snapshot still has it.
- For selected-work dispatch history cards, derive the primary card title from the selected work item's label on the matching request-owned work item when that label exists, and keep the dispatch ID as secondary metadata; workstation or transition identity belongs in summary rows, not as the dominant heading for work-scanning flows.
- When generated workstation-request projection types drop retired inference-owned fields, downstream regression tests must switch to raw JSON key assertions for omission checks; compile-time field access cannot guard removed keys once the generated structs no longer expose them.
- For inference-backed selected-work dispatch cards, keep dispatch-level navigation in `Request details` plus `Trace details`, and keep prompts, provider-session diagnostics, request metadata, responses, and worktree or working-directory inspection inside `Inference attempts`; adding placeholder response sections back at the dispatch level recreates the duplicate surface this workflow removed.
- For script-backed selected-work dispatch cards, keep `Request details` and `Response details` limited to dispatch-level context such as work-item navigation and trace links, and keep script request or response diagnostics such as command, args, stdout, stderr, exit code, and pending-response copy exclusively under `Script attempts`; mirroring those fields in summary sections recreates the noisy duplicate surface this cleanup removed.
- For selected-work dispatch cards, keep `Inference attempts` and `Script attempts` behind per-card disclosure controls that default to collapsed, keep each disclosure's expanded state local to its own dispatch card, and source the expand or collapse control copy from the feature-local current-selection message catalog so accessibility labels and localized UI copy stay aligned.
- Keep `ui/src/features/current-selection/components/workstation-request-detail.tsx` aligned with the same inference-ownership split as `selected-work-dispatch-history-card.tsx` and `execution-details.tsx`; inference-backed workstation-request detail cards must not keep a second copy of prompt, request metadata, response metadata, provider-session, response text, or worktree inspection outside `Inference attempts`.
- When Storybook dashboard fixtures seed `useFactoryTimelineStore`, populate `worldViewCache` with full `WorldState` snapshots instead of wrapper objects around `DashboardSnapshot`; Storybook build can still compile while browser-backed interaction tests fail on missing `runtime` fields.
- For browser-backed dashboard evidence on current-selection workstation editing, add tagged Storybook interaction stories in `ui/src/App.stories.tsx` with `dashboardApi.fetchMocks` for `GET /factory/~current` and `POST /factory`; this proves disclosure, dropdown, prompt, save confirmation, and responsive behavior through the same mocked API layer the dashboard uses in Storybook.
- When `pkg/cli/init` changes authored scaffold topology or routing semantics, update `pkg/cli/init/init_test.go` in the same pass; that test suite is the checked-in guard against generated workflow contract drift.
- When the Ralph scaffold changes its starter request-inbox wording, update `pkg/cli/init/scaffolds.go`, `pkg/cli/init/init_test.go`, and `tests/functional/workflow/cli_ralph_init_smoke_test.go` together so the generated README copy, direct scaffold assertions, and seeded loop smoke stay aligned.
- When the default starter inbox contract changes, update both `pkg/cli/init/init_test.go` and `tests/functional/bootstrap_portability/cli_init_factory_test.go` together; the package tests guard generated scaffold shape, while the bootstrap-portability tests prove the generated starter remains usable end to end.
- When the default starter inbox contract changes, keep `pkg/cli/run/run_test.go` aligned too; the OOTB bootstrap smoke should prove the seeded file lands in `inputs/<default-work-type>/default` and completes through the canonical starter flow at runtime.
- When the checked-in maintainer process/review contract changes, add or update focused functional coverage against `tests/adhoc/factory`; synthetic repeater fixtures alone do not prove the live workflow routes `process`, `review`, and loop-breaker outcomes correctly.
- When a workstation prompt can emit either ideas or batch requests, state the default as one standalone idea file and name the exact condition that permits `FACTORY_REQUEST_BATCH` output.
- When maintainer prompts need the checked-in control plane, point them to `factory/internal/{asks,view,progress,meta}.md` explicitly and keep asks, view, progress, and meta in that same file family.
- Treat slash-rooted workstation `working_directory` values as portable runtime paths only when they are repo-authored logical locations such as `/repo/...` or `/worktrees/...`; preserve real existing Unix absolute paths as host-absolute instead of rebasing them under the runtime base.
- Keep workstation testing guidance behavioral. Prompt instructions should reject source scans, docs-topology checks, asset-bundle string inspections, and command or route inventory assertions unless those surfaces are the product behavior being validated.
- Normalize retired workstation config aliases at load boundaries and clear the legacy field there; runtime execution helpers should read only the canonical post-normalization field instead of keeping a second live fallback path.
- When transition-topology normalization only needs workstation repeater semantics, pass a narrow workstation-name-to-kind map from `pkg/config` into `pkg/factory/state` instead of building a config-only `interfaces.RuntimeWorkstationLookup` shim; keep runtime lookup ownership in `pkg/factory/workstationconfig/runtime_lookup.go`.
- When canonical workstation execution-timeout behavior changes, prove it at each runtime boundary that consumes `config.WorkstationExecutionTimeout`, especially `pkg/workers` executor deadlines and `pkg/service` cron deadlines, instead of relying on helper-only assertions.
- When workstation prompt-variable help or validation changes, keep the same four seams aligned together: `pkg/workers` owns the authoritative prompt-template contract and validator, `pkg/api/handlers.go` owns the current-factory workstation prompt endpoints, `api/openapi*.yaml` plus generated clients own the boundary types, and `ui/src/api/current-factory-prompt-template/` owns dashboard-side fetch wrappers for the editor.
- When current-selection inference request or response bodies are collapsed by default, update `ui/src/App.current-selection.test.tsx` alongside the feature-level inference-attempt tests; the app-composition seam still asserts real body text for ready or rejected workstation flows and must expand the relevant `Request body` and `Response body` regions before checking their content.
- When current-selection workstation prompt-editor files need class-name composition, import the shared helper from `ui/src/lib/cn.ts`; `origin/main` does not carry the branch-local `cx` shim, so prompt-editor changes that reference `cx` can pass in a worktree but still fail required PR merge-ref typecheck.
- When backend prompt-validation diagnostic kinds change, keep `ui/src/features/current-selection/workstation-prompt-field.tsx` aligned with the emitted enum names and cover the rendered label copy in `ui/src/features/current-selection/workstation-detail-card.editable-configuration.test.tsx`; otherwise syntax errors can silently fall back into the variable-access bucket.
- When backend prompt diagnostics include `startOffset` and `endOffset`, treat those 1-based byte offsets as the primary squiggle-location contract in `workstation-prompt-field.tsx` and keep `sourceText` matching only as a fallback; otherwise execution-backed diagnostics can underline the wrong characters even when validation correctly blocks save.
- When a cron helper already receives the workflow identity explicitly, keep the remaining dependency surface narrowed to `interfaces.RuntimeWorkstationLookup`; only the entrypoint that derives workflow identity should depend on path-aware runtime methods such as `FactoryDir()`.
- Keep effective worker and workstation runtime-definition application for cloned factory configs centralized in `pkg/config`; shared callers such as `FactoryConfigWithRuntimeDefinitions(...)` and `NewLoadedFactoryConfig(...)` should reuse one package-local merge owner and prove regressions through effective config outcomes instead of helper-topology assertions.
- When a config entrypoint needs on-disk worker or workstation runtime definitions, populate a `pkg/config` runtime-definition lookup first and then route the cloned factory config through the canonical runtime-definition apply helper; keeping load and inline flows on the same lookup-plus-apply seam avoids merge-rule drift.
- When a workstation config alias is retired at normalization time, keep any remaining compatibility proof in `pkg/config` load-boundary tests and remove consumer-level runtime fixtures that still author the cleared legacy field directly.
- When public and factory ingestion paths share compatibility semantics, keep the normalization and validation helpers in `pkg/factory` and have API boundaries only translate those shared errors into boundary-specific messages.
- When an API path already depends on a shared `pkg/factory` compatibility seam, call that seam directly instead of adding API-local wrapper helpers that only forward the same arguments.
- When canonical work content changes after submit normalization, keep readback and history surfaces aligned together: `pkg/api/handlers.go` owns `GET /work` and `GET /work/{id}` shaping, `pkg/factory/event_history.go` owns emitted `Work` payloads from `FactoryWorkItem`, and `pkg/replay/event_artifact.go` plus `pkg/factory/projections/world_state.go` own reconstructing those content parts from generated event payloads.
- When canonical workstation cron fields already decode through the generated OpenAPI boundary, keep cron ownership in `pkg/config` limited to that boundary plus `FactoryConfigFromOpenAPI(...)`; do not add a second raw-JSON repair pass in `FactoryConfigMapper.Expand(...)`.
- When workstation cron public contract behavior changes, keep coverage split across `pkg/api/openapi_contract_test.go` for advertised schema keys and `pkg/config/openapi_factory_test.go` for generated-boundary decode and alias rejection; one layer alone will miss either schema drift or boundary drift.
- When workstation cron load behavior changes, keep runtime evidence split between `pkg/config/runtime_config_test.go` for canonical authored `factory.json` loading and `tests/functional/runtime_api/api_runtime_config_alignment_smoke_test.go` for split-authored flatten/replay/runtime alignment; reviewers need both lanes to catch boundary-only and runtime-only drift.
- Malformed cron expressions are rejected during config validation before service-mode startup, so cron watcher regressions should prove invalid-schedule disable behavior at the `pkg/service` watcher-start boundary with an authored `interfaces.FactoryConfig`, while service-mode tests cover validated cron behavior such as non-cron skips, trigger-at-start, and scheduled firing.
- Treat `pkg/factory.WorkRequestFromSubmitRequests(...)` as the canonical submit-ingestion batch builder; do not keep wrapper ownership surfaces for the same shaping behavior in adjacent packages.
- Functional runtime submission helpers should shape canonical `FACTORY_REQUEST_BATCH` payloads through `pkg/factory.WorkRequestFromSubmitRequests(...)`; do not keep a second hand-shaped batch builder under `tests/functional/...`.
- When compatibility work touches shared request aliases, prove it with API or canonical parsing tests plus normalized request-output assertions; helper-only tests are not enough to prevent boundary drift.
- When worker event cleanup changes emitted payload semantics, keep the regression at the package event boundary by decoding the generated payload in `pkg/workers/script_test.go` or `pkg/workers/recording_provider_test.go` and asserting the safe emitted fields, omission rules, and diagnostics projection rather than helper presence or raw internal diagnostics shape.
- When routing mixed work-plus-resource outputs through `pkg/factory/token_transformer`, preserve the consumed resource token identity and keep its chaining-lineage fields empty unless the resource token already carried lineage explicitly; work-token ancestry must not be synthesized onto resource outputs.
- Canonical work `content` belongs on runtime `interfaces.TokenColor` once submit normalization finishes; worker prompt/render paths and later provider-specific dispatch shaping should read per-input content from tokens instead of reparsing legacy payload bytes or flattening multi-input content early.
- When proving dispatch chaining lineage, assert it at the event-history or replay boundary: dispatch request and response payloads should derive predecessor IDs only from non-resource work inputs, canonicalize fan-in predecessors, and apply the same predecessor list to each fan-out work output.
- Canonical chaining-lineage helpers in `pkg/interfaces/chaining_trace.go` must prefer `CurrentChainingTraceID` and fall back to legacy `TraceID` only for compatibility; dispatch construction and worker-emitted generated batches should reuse those helpers instead of recomputing lineage from `TraceID` directly.
- When queue-visible runtime state needs scheduler-ready lineage, propagate `chainingTraceDepth` at the same boundaries that already carry `currentChainingTraceId` and `previousChainingTraceIds`: submit normalization, token transformation, event payload work records, world-state reconstruction, and queue-facing work-item or token refs must stay aligned or depth silently drops before scheduling can see it.
- Worker-emitted `FACTORY_REQUEST_BATCH` enrichment in `pkg/factory/subsystems/subsystem_transitioner.go` must seed `ChainingTraceDepth` before submit normalization runs; otherwise normalized generated work falls back to traced depth `1` even when upstream tokens already carry deeper lineage.
- When `interfaces.WorkResult.RecordedOutputWork` overrides generated token identity, `pkg/factory/subsystems/applyRecordedOutputWorkIdentity(...)` must copy `ChainingTraceDepth` alongside current and previous chaining IDs; otherwise authored recorded lineage looks correct but queue-visible depth silently regresses on the runtime token path.
- Default multi-output work fan-out in `pkg/factory/token_transformer` must preserve the consumed authored input `Name` verbatim on every generated work lane; do not synthesize per-lane suffixes unless the runtime recorded-output identity explicitly overrides the display name.
- When proving multi-output authored-name propagation for downstream fan-out behavior, assert it through `CompletedDispatch`: check the fan-out workstation's generated output mutations and each downstream workstation's consumed input token names so the regression stays visible at the runtime routing layer instead of only the final marking.
- When a shipped factory example only exists as `examples/factories/*.png`, derive the runtime config from the PNG's embedded `portos.agent-factory` metadata in functional tests instead of hand-recreating the topology; this keeps behavior coverage aligned with the same example contract customers import.
- Three-workstation chaining-lineage smoke coverage can stay fully behavioral by scaffolding the factory in-test, using one worker-emitted `FACTORY_REQUEST_BATCH` fan-out to create branch work with source-dispatch tags, and asserting the mixed work-plus-resource fan-in only through canonical events, world-state reconstruction, and final token state.
- Graph-editor connection UX should keep anchor compatibility ownership in `ui/src/features/factory-graph-editor/factory-graph-editor-connections.ts`, render labeled anchor seams through `components/factory-graph-editor-flow.tsx`, and verify keyboard-reachable anchor states through the focused flow Storybook story instead of the broader dashboard surface.
- Graph-editor edge deletion should keep draft edge add/remove mutations in `ui/src/features/factory-graph-editor/factory-graph-editor-connections.ts`, keep removal confirmation copy in `factory-graph-editor-removals.ts`, and use the focused `components/factory-graph-editor-flow.stories.tsx` seam for browser-backed pending-edge styling proof instead of broader React Flow card tests.
- Graph-editor worker runtime badges and dense-lane toggles should stay derived at the editor view-model layer (`ui/src/features/workflow-activity/hooks/react-flow-current-activity-card-editor-graph.ts`) from the canonical editable definition plus live dashboard runtime data; keep badge styling and focused browser verification in `ui/src/features/factory-graph-editor/components/factory-graph-editor-flow.tsx` and `components/factory-graph-editor-flow.stories.tsx` instead of rebuilding worker status logic inside the broader dashboard card.
- Graph-editor draft transformation helpers should keep `ui/src/features/factory-graph-editor/factory-graph-draft-apply.ts` as the canonical owner for named-entity, work-state, pending-definition, and workstation worker-assignment application; validation can reuse that owner, but edge-removal validation must still seed removed endpoints into its node index so deleting a node and its incident edge in one draft does not regress the existing save/validation behavior.
- Graph-editor pending-save and validation regressions around workstation worker assignments should be proved in `ui/src/features/factory-graph-editor/factory-graph-draft.test.tsx` through `buildPendingFactoryDefinition(...)` and `validateFactoryGraphDraft(...)` assertions on the final resolved draft state, including replacement and removal-without-replacement flows, instead of helper-location or UI-copy-only tests.
- Graph-editor draft validation coverage for duplicate identifiers, required-name failures, incompatible edges, and unknown edge nodes should stay at the same `factory-graph-draft.test.tsx` behavioral seam, asserting returned validation errors and `buildPendingFactoryDefinition(...)` nullability rather than private validator helpers.
- When factory-graph chrome needs to appear in the dashboard grid-card header, hoist `useCurrentActivityGraphEditor(...)` into `ui/src/features/workflow-activity/components/workflow-activity-bento-card.tsx`, render the pill and toggle through `AgentBentoCard` `headerAction`, and keep `ReactFlowCurrentActivityCardView` available with `showHeaderActions={false}` so the shipped dashboard does not duplicate inner and outer controls.
- Keep factory-graph header-consolidation evidence split by seam: `ui/src/features/workflow-activity/components/workflow-activity-bento-card.test.tsx` should prove the shipped grid-card header owns the mode badge and editor toggle, while `ui/src/App.stories.tsx` verification stories should cover browser-visible wide and narrow dashboard layouts.
- When current-selection workstation sections need remount-on-switch behavior, give each keyed sibling in `ui/src/features/current-selection/components/workstation-detail-card.tsx` a distinct semantic key per section; reusing the same workstation key for multiple siblings can preserve and duplicate stale configuration UI across workstation switches.
- Keep current-selection workstation edit and save state scoped to the same semantic workstation identity key in `ui/src/features/current-selection/hooks/use-editable-workstation-configuration-state.ts` and `ui/src/features/current-selection/components/current-selection-widget.tsx`; when selection changes to another workstation or a non-workstation detail card, the editable draft and save feedback should rehydrate from the new scope instead of leaking the previous workstation session.
- `ui/src/features/current-selection/hooks/use-save-editable-workstation-configuration.ts` owns workstation-scoped save lifecycle state; when a selection leaves workstation detail (`scopeKey -> null`), clear stored success and error scope markers together with confirmation state so returning to the same workstation does not resurrect stale save feedback from an earlier session.
