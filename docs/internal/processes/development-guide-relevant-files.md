# Development Guide Relevant Files

This inventory records the checked-in files and directories that the maintainer development guide should describe when it gives repository-root workflow instructions.

| Path | Role | Notes |
| --- | --- | --- |
| `go.mod` | Repository-root marker | Maintainer commands and worktree-aware tests should treat the directory containing `go.mod` as the canonical repository root. |
| `Makefile` | Root command surface | The development guide should describe quality and generation commands as root-level invocations instead of teaching a nested package workflow; maintainers also rely on this file to preserve repo-owned `typecheck`, `release`, functional-lane, API generation or verification commands, the direct `make backend-size` and `make pkg-maint` maintainer gates, their inclusion inside `make lint`, and the enforced backend coverage threshold through `GO_COVERAGE_MIN`. |
| `cmd/gocoveragecheck/` | Backend coverage-lane entrypoint | The repository-owned `make test-coverage-go` command should keep backend package discovery, `-coverpkg` wiring, aggregate-threshold enforcement, and the zero-coverage backend package gate here so backend functional execution counts toward backend-owned coverage without hard-coded shell package lists in `Makefile` or CI, while the actual minimum remains a `Makefile` policy value, package-level `0.0%` regressions are read from the real `go test` summary output in both the bare package-summary shape and the canonical `ok ... coverage:` shapes, including the `in <coverpkg package list>` suffix emitted by `go test -coverpkg`, zero-coverage failures stay actionable by naming the affected packages, helper-runtime branches such as temp profile cleanup and `go tool cover` stderr-first or stdout-fallback wrapper detail should stay package-local through the command's `execCommand` seam plus helper subprocesses, malformed total or package percentage tokens that still match the regex shape should keep surfacing through direct package-local parse-error assertions on the returned error text, malformed coverage-profile and import-path branches should be covered with direct package-local assertions on returned totals and stable error strings instead of source-shape or shell-wrapper tests, exact `go test -cover ./cmd/gocoveragecheck` acceptance work should keep `main()` and `failf` assertions in the parent process through the command's existing writer and exit seams because child-process `main()` helpers can improve diagnostic `-coverprofile` totals without moving the package-summary metric, fake child `go test` output in those parent-process tests must be captured through the command's writer seams so raw `coverage:` lines from helper subprocesses do not contaminate the top-level package summary, wrapped filesystem-read failures in those tests should assert stable wrapper prefixes plus `errors.Is(..., os.ErrNotExist)` or the missing path rather than OS-specific tail text, direct `main()`/`failf` exit-path coverage should stay package-local instead of moving those assertions into `Makefile`, CI, or shell wrappers, and oversized repo-owned command test monoliths should be split into sibling `*_test.go` files by concern while shared fake subprocess builders and temp-profile helpers remain package-local to the command. |
| `cmd/backendsizecheck/` and `internal/backendsizecheck/` | Backend file-size and function-size quality gate | Keep the repo-owned backend size scanner, violation formatting, explicit maintained-source scope rules, and the documented inline waiver directives `backendsizecheck:ignore-file` / `backendsizecheck:ignore-function` here so reviewer-runnable size enforcement does not depend on shell-specific filesystem discovery or ad hoc path judgments. Broad backend scans should route owned-root and exclusion policy through the inventory package, report package path plus file or function measurements directly from command output, and keep passing or failing shape verification package-local by exercising temp-repo fixtures through `run(...)` instead of source-inspection tests or CI-only coverage. Use the inline directives only for legacy oversized surfaces with an immediate justification comment, not as a shortcut around routine refactoring, and keep the integrated maintainer and CI path calling this gate through `make backend-size` plus the existing `make lint` or `make verify-build-contracts` surfaces instead of duplicating raw `go run` invocations across workflow YAML and docs. |
| `cmd/pkgmaintcheck/` | `pkg/` maintainability lint boundary | Keep the reviewer-runnable handwritten `pkg/` maintainability gate here so file-line, function-line, and cyclomatic-complexity thresholds stay on one repo-owned command path with package-local fixture tests rather than ad hoc shell scans or CI-only YAML. Preserve the explicit `pkg`-only owned-source scope, reuse the same generated and `testdata` exclusions as the backend size policy, route the normal repo workflow through `make pkg-maint` and `make lint` instead of duplicated raw `go run` calls, keep `pkgmaintcheck:ignore-file-lines` restricted to a true file-scope comment before the `package` clause, and keep any future inline waivers rule-scoped through `pkgmaintcheck:ignore-function-lines` or `pkgmaintcheck:ignore-cyclomatic-complexity` instead of broad package disables. |
| `cmd/releaseprep/` | Release-preparation entrypoint | The maintainer `make release VERSION=vX.Y.Z` flow shells through this command so git guardrails and readiness sequencing stay cross-platform and testable instead of being duplicated in shell-only Makefile logic; when command-owner regressions need direct coverage, keep them package-local with the same thin `main()` seam pattern used by adjacent release commands instead of pushing assertions into `Makefile` or lower-level policy tests, preserve the preexisting CLI parse-failure contract when swapping package-global flag parsing for a local `FlagSet` seam, and cover help or invalid-flag routing by calling `run(...)` directly so tests can assert the emitted `stderr` usage text and verify `runReleasePrep` stays untouched. |
| `cmd/functionallane/` | Default functional-lane entrypoint | The repository-owned `make test-functional` command should stay cross-platform by delegating package discovery and short-lane execution to this Go helper instead of embedding platform-specific shell pipelines in `Makefile`; when direct command-owner or process-wiring coverage is needed, keep it testable with local seams such as the thin execution variable, package-local stderr and exit seams for `main()` or `failf` behavior, and the `execCommand` helper-process pattern instead of moving command ownership into shell-only wrappers or unrelated packages. Because `parseConfig()` still binds the package-global `flag.CommandLine`, package-local tests that exercise parsing should restore both `os.Args` and `flag.CommandLine` around each case so command defaults and later tests do not drift. |
| `cmd/deadcodecheck/` | Deadcode quality-gate entrypoint | The maintainer-facing `make deadcode` and `make lint` surfaces should keep routing through this repo-owned command so deadcode invocation, current-report writeout, baseline loading, and drift failure messaging stay cross-platform and package-owned; when command-entrypoint coverage is needed, follow the same thin `main()` seam pattern used by adjacent repo-owned commands instead of pushing command ownership into `Makefile` or subprocess-only tests, keep baseline or drift assertions package-local by stubbing the deadcode runner while exercising the real relative baseline and current-report paths from a temp repo root, and cover the real deadcode subprocess failure branch through a package-local `exec.Command` seam plus helper-process stub instead of shelling out to the live tool from tests. When the supported path should inherit ambient subprocess environment, capture the returned `*exec.Cmd` through that same seam and assert `Env` remains unset rather than preserving retired compatibility helpers. For deterministic file-boundary failure coverage, prefer blocking the owned `bin/deadcode-current.txt` or `docs/internal/development/deadcode-baseline.txt` file paths with directories inside the temp repo root so tests still assert the real stderr prefixes and write ordering without changing the command contract. For successful subprocess stderr passthrough, capture `os.Stderr` with a temporary pipe around `run(...)` or `runDeadcode()` so tests prove the forwarded bytes unchanged while the command still returns its normal success output. |
| `internal/releaseprep/` | Release-preparation policy logic | Branch, clean-tree, semver-tag, local/remote tag, and readiness-target enforcement lives here and should stay aligned with the maintainer release policy guide. |
| `cmd/releasetagcheck/` | Workflow-facing release-tag entrypoint | GitHub Actions should call this command when validating an explicit release tag or resolving the one semver tag that points at a verified commit, and workflow-visible regressions should stay covered with package-local command-owner tests here instead of moving those assertions into workflow YAML or lower-level packages. When the real `git tag --points-at` wrapper needs direct coverage, keep it package-local by prepending a temp-directory fake `git` executable on `PATH` so tests exercise the real subprocess boundary without changing command ownership or the public `release_tag=` contract. |
| `internal/releasetag/` | Shared release-tag policy helpers | Keep semver tag validation and filtering here so maintainer commands, workflows, and tests enforce one release-tag contract. |
| `cmd/releasesmoke/` | Release smoke entrypoint | Candidate-artifact and released-artifact verification should invoke this repo-owned smoke boundary so packaged-binary checks stay centralized instead of being reimplemented inline in workflow YAML, and wrapper-visible regressions should stay covered with package-local command-owner tests plus a thin local execution seam here instead of shifting that coverage into shell wrappers or the lower-level harness package. |
| `internal/releasesmoke/` | Release artifact smoke logic | Packaged-binary startup, `/events` validation, browser-backed dashboard rendering checks, and completed-work verification live here so scripts and GitHub Actions share one observable runtime contract. |
| `.goreleaser.yml` | Release packaging contract | The tag-driven CLI artifact matrix, archive formats, checksum naming, executable filenames, and snapshot-capable packaging flow should stay centralized here instead of being duplicated inline in release workflow YAML; the repository and Go module path can remain `infinite-you` while the shipped archive and binary contract uses the customer-facing `you` name. |
| `install.sh`, `scripts/install.sh`, and `scripts/install.ps1` | Repo-owned CLI installer boundaries | Consumer fallback installation should expose a stable repo-root `install.sh` entrypoint for release smoke and hosted-installer parity while the shared archive naming, tagged GitHub release resolution, platform selection, checksum verification, and installed `you` or `you.exe` target naming stay owned by the reusable installer scripts instead of being duplicated across docs or workflow YAML. |
| `scripts/release/` | Cross-platform release smoke wrappers | Shell and PowerShell release smoke entrypoints should stay thin wrappers over the repo-owned Go smoke command or hosted-installer smoke helper instead of drifting into separate platform-specific verification logic. |
| `ui/scripts/release-dashboard-smoke.mjs` | Browser-backed release dashboard verifier | Rendered dashboard smoke should stay in this one Playwright entrypoint so packaged-binary verification proves embedded asset loading plus live dashboard state without duplicating browser assertions in Go tests or workflow YAML. |
| `.github/workflows/release-candidate.yml` | Candidate artifact verification workflow | Tag-driven pre-publish candidate packaging, semver-tag validation, and the six-artifact smoke matrix should stay here, while archive construction and runtime checks continue to delegate into `.goreleaser.yml` and `scripts/release/`. |
| `.github/workflows/release.yml` | Release publication and released-artifact verification workflow | The tag-gated publish lane should resolve the already-verified semver tag from the candidate commit, publish the hosted installer assets, then run separate post-publish jobs for release artifacts, installer smoke, and `go install` verification. |
| `api/` | Authored API contract workspace | OpenAPI validation and bundling start from the repository root, then shell into `api/` only where the documented workflow requires it. |
| `api/components/schemas/shared/` | Shared OpenAPI fragment family | Cross-surface helper schemas such as maps, diagnostics helpers, and resource helper types live here and referenced fragments must use file-relative `$ref`s. |
| `api/components/schemas/api/` | API payload OpenAPI fragment family | Request, response, status, token, and pagination contract fragments are authored here one schema per file. |
| `api/components/schemas/api/SubmitWorkRequest.yaml` | Single-work submit contract fragment | Unary `POST /work` request-shape changes should start in this fragment, then refresh `api/openapi.yaml`, generated Go/UI contracts, and the handwritten `/work` validation seam in `pkg/api/handlers.go` together so required-field drift does not split the published schema from runtime behavior. |
| `api/components/schemas/api/FactorySession*`, `pkg/service/runtime_sessions.go`, `pkg/service/factory.go`, `ui/src/api/factory-sessions/`, `ui/src/api/session-routing.ts`, `ui/src/features/dashboard/state/dashboardSessionStore.ts`, `ui/src/features/dashboard/hooks/useDashboardSnapshot.ts`, `ui/src/features/dashboard/components/dashboard-screen.tsx`, and `ui/src/features/header/dashboard-session-tabs.tsx` | Factory-session workspace contract | Dashboard session work should keep the session inventory plus open/close transport on the shared `/factory-sessions` contract, route selected-session ownership through `dashboardSessionStore`, and build dashboard URLs through `ui/src/api/session-routing.ts`, which owns explicit `/factories/<session>/...` paths and normalizes missing or empty session IDs to `~default`. Map service-owned folder-target discovery and close handoff through `FactoryService.OpenFactorySession(...)` and `CloseFactorySession(...)`, and keep per-session runtime isolation in the service-owned runtime bundle from `pkg/service/factory.go` plus `pkg/service/runtime_sessions.go` so each live session owns its own logger fields, runtime log sink, replay recorder, and event history instead of sharing the default-session artifacts. Expose the session strip through one `DashboardSessionTabs` owner with real tab semantics plus roving focus, keep the selected-session store nullable when no sessions remain, and let `useDashboardSnapshot.ts` plus `dashboard-screen.tsx` own the cleared-workspace reset path so closing the final tab removes stale timeline state instead of silently falling back to `~default`. |
| `pkg/logging/runtime_logger.go`, `pkg/logging/runtime_logger_test.go`, and `pkg/service/factory_runtime_log_test.go` | Default runtime-log home-path contract | Keep the customer-default runtime log root plus the one-time legacy-home migration from `~/.agent-factory/logs` to `~/.you-agent-factory/logs` centralized in the logging package, and prove the observable home-directory contract through tests that assert those concrete canonical and legacy paths directly instead of routing expectations back through the helper under test or reimplementing migration policy in callers. |
| `pkg/api/provider_session_details.go` and `api/components/schemas/api/ProviderSession*` / `api/components/schemas/api/LoadableProviderSession*` / `api/components/schemas/api/CodexSession*` | Provider-session detail API boundary | Session-detail loading for dashboard inspection should stay in the API boundary: the browser sends provider-session metadata, the server resolves supported Codex `rollout-<session-id>.jsonl` and timestamp-prefixed `rollout-<timestamp>-<session-id>.jsonl` files under the configured Codex sessions root, and richer parsing should extend the typed `CodexSession*` response schemas with partial parse diagnostics or transcript entries rather than accepting filesystem paths from the UI or parsing raw session streams in the browser. Timestamp-prefixed support must match the full supported basename shape exactly, with the entire segment after `rollout-<timestamp>-` equal to the canonical `session_id`; do not widen matching with looser suffix checks that would accept extra filename segments. When both supported filename layouts exist for the same canonical session identifier, the exact `rollout-<session-id>.jsonl` basename wins; if more than one safe file matches within a single layout, keep the API behavior stable by surfacing an internal error instead of relying on directory walk order. When the detail route supports only one loadable provider-session shape, publish that surface through dedicated `LoadableProviderSession*` API schemas instead of reusing the broader event metadata schema so generated backend and UI contracts own the allowed `provider` and `kind` values. Regenerate both Go and TypeScript OpenAPI artifacts after those schema changes, then keep the handwritten browser response validator in `ui/src/api/provider-session-details/api.ts` aligned too; generated types alone do not prove the runtime JSON boundary. The generated Go query binder preserves those enums as typed aliases but does not enforce enum membership at runtime, so handlers that rely on single-value query enums still need a thin exact-match contract guard at the boundary while leaving deeper loaders responsible only for identifier and filesystem safety. For transcript text extraction, parse structured message `content` arrays before falling back to compacting the raw `content` field, otherwise user or assistant transcript lines can degrade into JSON blobs instead of readable chat text. |
| `api/components/schemas/data-models/` | Data-model OpenAPI fragment family | Public named-factory, topology, work, relation, and other reusable base model schemas are authored here and should stay split from `api/openapi-main.yaml`. |
| `api/components/schemas/factory-world/` | Additive factory-world OpenAPI fragment family | Dashboard-facing read-model schemas belong here one schema per file rather than remaining inline in `api/openapi-main.yaml`. |
| `pkg/interfaces/world_state_clones.go` | Canonical runtime detached-copy helpers | Detached-copy ownership for canonical runtime tokens, token colors, token histories, provider session metadata, provider failure metadata, world-state projection records, and shared `[]interfaces.WorkContentPart` slice cloning should stay here so backend packages reuse one clone tree instead of growing parallel private clone helpers. |
| `cmd/factory/` | CLI entrypoint | Root-level build and smoke commands compile or execute the `factory` binary from this source tree. Keep thin `main` wiring testable through a local function seam here when the repo-owned backend coverage lane needs command-entrypoint coverage without pushing CLI lifecycle assertions down into unrelated package tests. |
| `pkg/cli/init/` | Generated init scaffold contract | Customer-facing scaffold templates, including Ralph scaffold README command snippets, live here and should use the installed executable token (`you`) for commands customers type while preserving repository and module identifiers where those are the actual contract. Keep generated scaffold assertions in `pkg/cli/init/init_test.go`, including negative checks for retired executable tokens when command examples change. |
| `pkg/cli/sessionpath/`, `pkg/cli/submit/`, and `pkg/cli/work/` | CLI session-scoped runtime routing seam | Explicit CLI session targeting should build non-default runtime URLs through `pkg/cli/sessionpath.ScopedPath(...)` and keep command-owned validation plus response rendering in `submit` or `work` packages, so later `--session` support extends one escaping and default-session alias rule instead of scattering handwritten `/factories/{id}` concatenation across commands. |
| `pkg/cli/default/default.go` and `pkg/cli/root.go` | Canonical local CLI default-flow contract | Customer-visible local CLI defaults should source the standard local factory port from `defaultcmd.FactoryPort` so the no-arg run flow, submit or inspection subcommands, and command help/examples cannot silently drift onto a second default port. When changing the standard local runtime port, update the shared constant first and keep root-command tests asserting both default-port and explicit-override behavior. Record/replay user guidance that is meant to be visible from `you run --help`, including default-recording behavior and replay-artifact sensitivity warnings, should stay in `pkg/cli/root.go` and be asserted in `pkg/cli/root_run_test.go` instead of living only in maintainer docs. |
| `README.md` | Consumer install and quickstart contract | Consumer-facing install guidance should keep the exact hosted installer, `go install`, and manual release-archive fallback commands aligned with the repo-owned release surfaces instead of inventing parallel install instructions in one-off docs. |
| `tests/release/install_script_test.go` | Repo-owned installer regression seam | Local installer smoke and fake-release checksum coverage should execute and read the checked-in `scripts/install.sh` source directly, because the hosted release asset is published from that repo-owned script rather than a separate root-level installer file. |
| `tests/release/release_smoke_test.go` | Release install-surface smoke contract | Packaged-binary runtime checks, the clean-`GOBIN` `go install ./cmd/factory` smoke, and the opt-in published-module `go install` smoke should stay together here so release-oriented install verification remains observable and repo-owned. |
| `docs/internal/development/*-closeout.md` | Cleanup verification artifacts | Narrow cleanup lanes record the exact root-level validation bundle here when maintainers need durable proof beyond `progress.txt`. |
| `docs/internal/development/deadcode-baseline.txt` | Checked-in deadcode quality baseline | When `make deadcode` intentionally changes repository-accepted deadcode findings, update this file in the same review so the enforced root-level quality gate stays aligned with the current intentional baseline. Re-check whether each accepted finding is still truly unreachable before refreshing the baseline; the baseline can go stale after tests or supported call sites are added. |
| `docs/internal/development/openapi-schema-standardization-inventory.md` | OpenAPI cleanup inventory | Records the authored fragment layout, remaining inline schemas, and the canonical bundle and generation verification surfaces for schema-standardization work. |
| `docs/internal/development/root-factory-artifact-contract-inventory.md` | Root artifact contract inventory doc | The checked-in root artifact table must stay in lockstep with the enforced entries in `internal/testpath/artifact_contract.go`. |
| `pkg/testutil/artifact_contract_inventory_test.go` | Root artifact contract regression proof | Prove behaviorally here that checked-in artifacts exist, obsolete root paths stay absent, and the canonical maintainer backlog remains singular under `factory/internal/*`; update the inventory doc and `internal/testpath` classification source together when the root contract changes. |
| `docs/internal/development/import-export-standards-alignment-checklist.md` | Import/export standards evidence doc | Import/export standards-alignment stories should update this one checked-in checklist with backend and frontend status, concrete evidence, and precise follow-up notes instead of scattering partial audit notes across PR descriptions or story-local scratch docs. |
| `docs/internal/development/website-localizability-migration-inventory.md` | Website localization migration inventory | Website localizability stories should keep this inventory aligned with migrated and remaining product-authored UI copy surfaces, using it to separate translatable copy from API-owned data values, generated artifacts, fixtures, and developer-only diagnostics. |
| `docs/internal/development/development.md` | Active maintainer guide | Must describe the real repository-root layout used in this checkout and avoid stale `libraries/agent-factory` instructions. |
| `docs/internal/development/cli-release-policy.md` | Maintainer CLI release policy | Documents the tag-on-`main` release model, semver tag trigger policy, and the operator release flow that future release automation must implement. |
| `README.md` | User-facing CLI quickstart and docs command contract | Root command examples live here, and command-surface changes should keep the `infinite-you` examples aligned with the CLI README assertions in `pkg/cli/root_test.go`. |
| `pkg/cli/docs/` | Packaged CLI docs command surface | The embedded `you docs` topics are fixed in `pkg/cli/docs/docs.go` as `config`, `workstation`, `workers`, `resources`, `batch-work`, and `templates`; customer-facing docs entry points should name those exact topics while linking readers to `docs/reference/work.md`, `docs/reference/config.md`, `docs/reference/workstations.md`, `docs/reference/workers.md`, `docs/reference/resources.md`, `docs/reference/batch-inputs.md`, and `docs/reference/templates.md` as the canonical concept owners. Keep each packaged topic page explicitly handing off to those maintained docs, and keep `tests/functional/smoke/cli_docs_smoke_test.go` asserting those user-visible canonical-path markers from outside the repository docs tree. |
| `docs/internal/processes/manual-qa.md` | Repo-local browser verification checklist | Dashboard UI lanes should publish the reproducible Storybook/browser QA flow and latest browser evidence here so reviewers can rerun the exact spot checks. Provider-session detail fixes should record the exact Storybook or preview proof here, including selection of the affected provider session and confirmation that the timestamp-prefixed Codex source path renders in the success panel. |
| `ui/scripts/run-storybook-ci.mjs` | Storybook CI orchestration entrypoint | Browser-backed Storybook coverage should be added through this wrapper so the built static server, tagged story runner, and dedicated responsive checks continue to execute as one reproducible CI lane; when that lane chains Vitest Storybook plays into a separate Playwright responsive pass, keep readiness gated on both `index.json` and at least one built `iframe.html` shell so the second browser phase does not race a half-served static Storybook page, and keep the top-level orchestration behind an injectable function seam so script tests can prove command ordering, early server-exit handling, and cleanup without launching a live Storybook server. |
| `ui/scripts/storybook-responsive-helpers.mjs`, `ui/scripts/verify-import-export-storybook-responsive.mjs`, `ui/scripts/dashboard-shell-storybook-responsive.mjs`, and `ui/scripts/verify-provider-session-storybook-responsive.mjs` | Cross-viewport Storybook browser assertions | Responsive dashboard regressions that need viewport-specific geometry or keyboard checks should extend this Playwright script family instead of relying on untagged stories or manual-only review comments; the same lane now owns launch-locale browser proof for the localized submit-work, trace drilldown, work-outcome, workflow-activity, and current-selection widget stories, plus current-selection prompt-help and inline-diagnostic verification, header language switcher keyboard checks, computed shell-style comparisons between the dashboard header and representative grid cards, and provider-session detail success verification for timestamp-prefixed Codex session files. Keep shared timeout, visibility, story-region, and horizontal-overflow assertions in `storybook-responsive-helpers.mjs`; keep shell-specific computed-style assertions in `dashboard-shell-storybook-responsive.mjs`; and move story-specific browser checks such as provider-session success into focused helper modules once the shared runner approaches Biome file-size limits. Export reusable assertion helpers from this script family, guard the Playwright `main()` entrypoint with `import.meta.main` so Vitest can import verifiers without launching a browser or requiring a live Storybook server, and wait for target locators to become visibly rendered instead of treating `#storybook-root` attachment as a sufficient readiness signal for static Storybook pages. For real Playwright locators, rely on the visible-state `waitFor` result as the visibility contract and reserve direct `isVisible()` checks for minimal fake locator fallbacks. Keep schedule and localized-dialog regression coverage in focused companion tests such as `verify-import-export-storybook-responsive.schedule.test.mjs` instead of growing the broad verifier test file past Biome size limits. Do not treat `bun run storybook:responsive-check` as a standalone lane: it expects a built `storybook-static` bundle to already be served on `127.0.0.1:6008`, so normal CI and reviewer reruns should go through `ui/scripts/run-storybook-ci.mjs` or explicitly pair `storybook:serve-static` plus `storybook:wait-for-index` before invoking the responsive script. |
| `ui/biome.jsonc` | Frontend handwritten size-gate contract | Handwritten UI tests and stories that still need relaxed Biome file-size or function-size thresholds must be called out here with explicit file includes instead of broad `src/**/*.test.*`, `integration/**/*.test.*`, or `src/**/*.stories.tsx` globs; generated API artifacts and dashboard fixture data keep their own dedicated overrides. |
| `ui/scripts/check-hardcoded-ui-copy.ts`, `ui/scripts/check-hardcoded-ui-copy.test.ts`, and `ui/scripts/hardcoded-ui-copy-baseline.txt` | Frontend localization hardcoded-copy gate | New production `ui/src` JSX copy, rendered string expressions, common visible-label component props, localized accessibility labels, rendered logical-expression fallbacks, and assignment-based validation or error strings should go through feature-owned message catalogs and the shared locale seams, so this scanner plus reviewed baseline own the lightweight enforcement path while intentionally excluding tests, stories, generated API code, fixtures, developer-testing seams, and message-catalog source files; reserve the inline `hardcoded-ui-copy-exception: non-product-diagnostic` marker for intentional rendered diagnostics that are not product copy, and when a cleanup localizes an entry that was previously baseline-approved, remove the stale baseline line in the same change so `bun run lint` stays green. If a governance migration only moves a baseline-approved production file without changing the string literal itself, update the baseline path keys to the new governed location in the same change or the localized-copy gate will fail on both stale and new entries. Keep baseline write, stale-entry, and unexpected-finding behavior covered through the exported in-process runner so coverage does not depend on subprocess execution. |
| `ui/package.json`, `ui/scripts/check-deadcode-baseline.ts`, and `docs/internal/development/frontend-deadcode-baseline.json` | Frontend quality-lane dependency, script, and dead-code baseline contract | Keep shared UI verification dependencies, repo-owned scripts, component-level accessibility tooling such as `jest-axe`, Storybook browser runs, lint, typecheck, the localization copy gate, repo-owned Tailwind spacing guards, and the accepted Knip baseline aligned here so UI verification runs through one package-owned surface instead of ad hoc per-feature commands. When `make lint` or `make ui-deadcode` reports only stable tooling files or other intentionally retained UI entries, review the current `bin/frontend-deadcode-current.json` drift and update the checked-in frontend baseline intentionally rather than leaving the dead-code gate to fail after a base-branch refresh; when these checks target portal-backed UI such as Radix dialogs, run axe against `baseElement` or `document.body` so the audited DOM includes the portaled subtree instead of the default React Testing Library `container`. |
| `ui/src/styles.css` | Tailwind theme token owner | Shared Tailwind v4 design tokens belong in the `@theme` block here; add semantic color roles such as `--color-on-foreground` here so utilities like `text-on-foreground` share one CSS-token contract instead of scattering one-off feature colors. |
| `ui/src/i18n/` | Shared frontend locale seam | Locale registries, the app-level locale provider, the session-over-URL-over-browser source-order policy, reusable catalog-resolution helpers, required-locale catalog validation, and locale-aware `Intl` formatting helpers should live here so feature-owned message packages can add supported locales without growing an app-wide monolithic resource file; the website locale policy uses English `en` as the default fallback and canonical Simplified Mandarin Chinese for China `zh-CN`, with aliases such as `zh` and `zh-Hans` resolving through this shared seam rather than feature-local special cases. Feature widgets that accept a `locale` prop should pass that input through `resolveSupportedLocale(...)` (or the app-locale provider) instead of reimplementing lowercase or primary-language parsing locally, or canonical values such as `zh-CN` can silently fall back to English at runtime. |
| `ui/src/App.tsx`, `ui/src/App.toolbar-locale.test.tsx`, and `ui/src/App.stories.tsx` | App-level locale entry and verification seams | App-shell localization should be introduced through the root locale provider here so tests and stories can seed a locale directly without bypassing runtime source-order resolution; keep app-level observable locale-switch coverage in `ui/src/App.toolbar-locale.test.tsx` for header, dialog, and widget assertions that need real runtime state, and keep browser-backed locale-propagation proof in tagged Storybook stories here instead of inventing a second verification harness beside the dashboard shell. |
| `ui/src/features/import/messages/` | Feature-owned import preview catalogs | Import preview locale-varying copy should stay next to the dialog in this feature-owned catalog package, with direct lookup tests proving the supported locale set and fallback behavior instead of hiding the strings inside the component; when copy needs an emphasized dynamic value such as a factory name, keep one locale-owned template with a placeholder token instead of concatenating translated fragments around the highlighted value. |
| `ui/src/features/export/messages/` | Feature-owned export dialog catalogs | Export dialog and trigger copy should stay in this feature-owned catalog package, with the dialog consuming the shared `ui/src/i18n` resolver and focused lookup tests proving supported locales, fallback behavior, and interpolation-based labels instead of leaving user-visible export strings inline in the component tree; when catalogs use function-valued messages for interpolation, cover those helpers in every supported locale that contributes distinct functions so the repo-wide UI coverage lane does not regress on uninvoked locale entries. |
| `ui/src/features/header/messages/` | Feature-owned dashboard shell catalogs | Dashboard shell, header, brand lockup, loading/error panel, and timeline-control copy should stay in this feature-owned catalog package, with the shell consuming the shared `ui/src/i18n` resolver and focused lookup tests proving supported locales, fallback behavior, and placeholder-backed status templates instead of leaving accessible names, loading states, or timeline status copy inline in components. |
| `ui/src/features/current-selection/messages/` and `ui/src/features/current-selection/current-selection-locale.tsx` | Feature-owned current-selection locale seam | Current-selection shell, execution-details, terminal-summary, and selected-work dispatch-history copy should stay in feature-local catalogs that resolve through `ui/src/i18n`, while the shared shell layout and selected-work detail sections read those messages through the feature-local provider so every detail card can localize shared headings, history actions, trace guidance, and failure fallback copy without pushing locale props through each card-specific interface. When a story needs proof for required locale coverage instead of optional extra locales, use rendered assertions against the repo-required `zh-CN` catalog at this seam rather than relying only on lookup tests for `ja` or `ko`. |
| `ui/src/features/current-selection/current-selection-widget.tsx` and `ui/src/features/current-selection/provider-session-attempts.tsx` | Current-selection locale threading seam | Localized current-selection surfaces should accept an optional `locale` prop at the widget seam and forward only the narrow message-derived labels each detail surface needs, so feature-local catalogs can localize workstation or request cards without inventing a dashboard-wide locale store or coupling shared drill-down helpers to one message package. Hooks that must run before `CurrentSelectionLocaleProvider` renders, such as editable workstation state or save-state hooks, should receive that locale directly and resolve the feature-owned catalog at the hook boundary. |
| `ui/src/features/bento/components/dashboard-bento.tsx` and `ui/src/features/terminal-work/` | Terminal-work composition seam | Keep `useCurrentSelection(...)` ownership in the dashboard bento composition layer and treat `TerminalWorkWidget` plus `CompletedFailedWorkstationCard` as prop-driven presentational surfaces that consume feature-local terminal-work types, selected-row state, and passed selection handlers without importing current-selection hooks or types through the widget seam. |
| `ui/src/features/current-selection/components/selected-work-dispatch-history.tsx`, `selected-work-dispatch-history-card.tsx`, `ui/src/features/current-selection/components/work-item-card.test.tsx`, and `ui/src/App.workstation-requests.stories.tsx` | Selected-work dispatch-history presentation seam | Customer-visible cleanup to the `Workstation dispatches` panel should stay in this component pair so the section heading, populated cards, script-vs-inference request details, and explicit empty state remain aligned without pushing presentational copy choices down into execution-detail selectors. Keep observable regression coverage split between `work-item-card.test.tsx` for rendered selected-work behavior and the workstation-request Storybook smoke story for browser-facing proof that removed helper/count copy stays absent while dispatch cards still render. |
| `ui/src/api/provider-session-details/` and `ui/src/features/provider-session-detail/lib/provider-session-ref.ts` / `ui/src/features/current-selection/components/provider-session-attempts.tsx` | Provider-session selection seam | Keep the browser-owned provider-session detail request canonicalization in the typed API module and let the extracted `provider-session-detail` feature own dispatch-aware selection keys on top of that shape, while current-selection only consumes those helpers for selection controls so cards and hooks reuse one generated-contract boundary instead of maintaining separate Codex/session-kind allowlists in feature code. |
| `ui/src/api/provider-session-details/` | Browser-owned provider-session detail API boundary | Browser session inspection should stay behind this typed fetch module so the `provider-session-detail` feature consumes one client-safe contract for `NOT_FOUND`, generic load failures, and structured Codex parse summaries instead of issuing ad hoc `fetch` calls or re-decoding the API shape inside React components. |
| `ui/src/features/provider-session-detail/hooks/use-provider-session-detail.ts`, `components/provider-session-detail-panel.tsx`, `messages/provider-session-detail.ts`, and `public/index.ts` | Provider-session detail sibling-feature seam | Selected provider-session rendering should map query results into explicit `loading`, `not-found`, `empty`, `parse-error`, and `success` states in this sibling feature hook/panel pair, keep the dashboard card import seam at `provider-session-detail/public`, and leave current-selection responsible only for selection ownership plus history affordances so new diagnostics do not leak network or parse branching back into workstation/work-item summary components. |
| `ui/src/features/bento/dashboard-bento.tsx` and `ui/src/features/bento/messages/` | Dashboard widget locale fanout seam | Dashboard-localized widgets and bento-owned board accessibility labels should keep consuming the already-resolved app locale through this composition boundary so the movable board shell, work totals, workflow activity, current selection, submit work, work outcome, trace drilldown, and terminal work all stay aligned with the same session-scoped locale without each widget re-reading URL or browser state. |
| `ui/src/features/current-selection/current-selection-widget.tsx` and `selected-work-dispatch-history-helpers.ts` | Selected-work provider-session visibility seam | When selected-work provider-session state can be driven from both provider-session attempts and nested inference attempts, build the widget’s visible-session set from both sources and reuse the request-history inference-attempt helper so shared selection is not cleared for sessions that only exist on inference attempts. |
| `ui/src/i18n/locales.ts` | Shared supported-locale metadata seam | Canonical supported locale IDs, alias resolution, and native-language display labels should stay owned in this shared registry so locale pickers can render stable option names like `English`, `简体中文`, `한국어`, and `日本語` without coupling those labels to feature-local translated message catalogs or duplicating fallback logic. |
| `ui/src/features/workflow-activity/messages/` | Feature-owned workflow activity catalogs | Workflow-activity localization should keep dashboard flow legend and graph-import copy in this adjacent feature package, using the shared `ui/src/i18n` resolver and direct lookup tests to prove supported locales, fallback behavior, and function-valued accessibility labels instead of hardcoding visible or accessible strings inside the React Flow host or dialog shell; when a catalog owns icon-label patterns, drag/drop status helpers, or error-copy helpers, test at least one non-default locale invocation so untranslated helper branches do not hide in CI. |
| `ui/src/features/workflow-activity/react-flow-current-activity-card.tsx` | Workflow-activity locale boundary | Dashboard graph hosts should accept an optional `locale` prop and derive localized default legend, graph-import overlay, import error, and preview-dialog shell copy at the workflow-activity seam, so future localized routes or focused stories can exercise non-default graph copy without teaching React Flow primitives, topology builders, or unrelated dashboard widgets about the catalog; when that host seam owns locale fallback, add rendered integration coverage there for unsupported-locale fallback instead of relying only on message-catalog lookup tests, and keep translated validation alerts, dismiss/reset behavior, and preview-dialog activation-failure semantics covered at this host seam. |
| `ui/src/features/header/dashboard-export-dialog.tsx` | Dashboard-to-export locale boundary | Dashboard-owned export hosts should accept an optional `locale` prop and thread it into the feature dialog seam so stories, focused tests, and future localized routes can exercise non-default export copy without teaching shared dialog primitives or global app shells about feature-owned catalogs. |
| `ui/src/features/header/dashboard-header.tsx` | Dashboard header locale-switch and export-trigger boundary | Dashboard-owned controls that open a localized feature dialog should resolve their accessible names from the same feature-owned catalog, and the session-scoped language switcher should stay at this header seam so normal app flow can call `useAppLocale().setLocale(...)` without bypassing direct test or Storybook locale overrides. |
| `ui/src/features/header/tick-slider-control.tsx` | Dashboard timeline locale boundary | Timeline slider controls should accept the same optional `locale` prop as the owning header and resolve visible plus accessible tick copy through the header feature catalog, keeping interpolation local to the control instead of recreating hardcoded tick-status strings in parent shells or tests. |
| `ui/src/components/ui/dashboard-typography.ts` | Canonical dashboard typography contract owner | Dashboard headings, body copy, supporting labels, and Storybook or test assertions should import this file directly instead of routing through `ui/src/components/dashboard/` or feature-local re-export shims. |
| `README.md` | Public CLI quickstart and docs-command surface | Root command examples are test-enforced in `pkg/cli/root_test.go`, so CLI surface changes must keep the README aligned with the installed binary contract. |
| `pkg/cli/root.go`, `pkg/cli/submit/submit.go`, `pkg/cli/root_test.go`, `pkg/cli/submit/submit_test.go`, and `tests/functional/runtime_api/api_generated_smoke_test.go` | CLI single-work submit contract seam | The cobra `submit` command, its local validation, the generated unary `SubmitWorkRequest` payload, and the live-runtime CLI smoke should move together here: required unary API fields such as authored `name` need trim-aware local validation, help text and flag mapping updates, outbound request assertions in the submit package tests, and at least one functional lane that proves the first-party CLI still succeeds through the real `/work` boundary instead of only against permissive mocks. |
| `.github/workflows/ci.yml` | Repository CI contract | Contributor docs should name this workflow and mirror its root-level validation commands and stated non-deployment scope; the backend coverage job should continue to call `make test-coverage-go` so CI and local runs share the same repo-owned `cmd/gocoveragecheck` policy surface. |
| `internal/testpath/artifact_contract.go` | Enforced root artifact contract | Root-level factory artifact additions, removals, and redirect stubs are test-enforced here and must stay synchronized with the inventory doc. |
| `factory/` | Maintainer workflow surface | Contains checked-in operator guidance and active inbox directories that the development guide may reference for workflow-related tasks. |
| `pkg/api/handlers.go` plus sibling `pkg/api/handlers_*.go` files | Handwritten API boundary | Keep HTTP handler entrypoints, raw-JSON decode or validation helpers, response projection helpers, and shared API response writers in the same package but split across sibling `handlers_*.go` files when size cleanup is needed; generated request structs still do not reject unknown keys by themselves, so retired work-request alias and trace-conflict policy should continue routing through `pkg/factory.ValidateCanonicalWorkRequestJSON(...)` and only be translated into API-owned error wording rather than reimplemented locally. |
| `pkg/api/server_*_test.go` plus `pkg/api/server_test_helpers_test.go` | API route regression ownership | When API route tests cross the backend size gate, split them into same-package sibling `server_*_test.go` files by boundary concern such as submit, upsert, factory, provider-session, work-query, and dashboard/event coverage, and keep shared request builders or response assertions in `server_test_helpers_test.go` so route behavior stays observable without recreating one giant test owner file. |
| `api/codegen_config/client.yaml` | Generated Go client config | The checked-in Go client for backend functional tests is generated from this config and must stay in lockstep with `api/openapi.yaml`, `pkg/generatedclient/`, and `make api-smoke`. |
| `api/openapi-main.yaml` | Authored OpenAPI source of truth | Public API behavior changes should start here, including the advertised local `servers` URL, then refresh the bundled spec and generated Go/UI artifacts together so handwritten route seams and generated parameter binding stay aligned. |
| `pkg/generatedclient/` | Generated Go API client package | Backend functional tests that need typed client coverage should import this package instead of rebuilding `http.Get` or `http.Post` helpers for OpenAPI-owned routes. |
| `pkg/factory/interfaces.go` | Shared API runtime contract owner | Shared API-facing runtime methods such as work submission, factory event streaming, and engine snapshot reads should stay owned here so stricter boundary seams can embed one canonical base contract instead of re-declaring duplicate method sets. |
| `pkg/apisurface/contract.go` | API-server runtime seam owner | API-server-facing seams that add behavior on top of the shared runtime contract should embed `factory.APIFactory`, keep additive named-factory or explicit session-routing methods here, and avoid route-local duplicate interface ownership across packages. |
| `pkg/service/factory.go`, `pkg/service/factory_build.go`, `pkg/service/factory_runtime_support.go`, `pkg/service/factory_runtime_boundary.go`, `pkg/service/factory_runtime_state.go`, `pkg/service/factory_dashboard.go`, `pkg/service/factory_workers.go`, `pkg/service/factory_named_factory.go`, `pkg/service/factory_editable_definition.go`, and `pkg/service/factory_editable_definition_validation.go` | Named-factory and editable-definition service seam | Keep persisted named-factory read and serialization ownership in `pkg/service/factory_named_factory.go`, keep editable-definition read or save orchestration plus optimistic-version and topology helpers in the dedicated editable-definition sibling files, keep runtime construction plus replay or recording support in the focused runtime helper siblings, and keep `pkg/service/factory.go` focused on runtime activation, replacement-runtime startup, and long-lived sidecar lifecycle. Backend changes and tests should extend this same-package service seam instead of duplicating activation, current-pointer, editable-validation, or worker-loading rules in handlers or ad hoc helpers. When maintainability cleanup touches service startup, activation, editable saves, or session compatibility, keep default-runtime and replacement-runtime construction flowing through shared same-package helpers so logger setup, replay recorder wiring, worker loading, draft-save validation, dashboard rendering, runtime-state swapping, and inputs watcher behavior stay aligned across both default-session and session-scoped paths. Service-owned long-lived sidecars such as file watchers, cron schedulers, and poller supervisors should also stay wired through `startLiveRuntimeSidecars(...)` and `stopLiveRuntimeSidecars(...)` here so lifecycle start, cancellation, replacement shutdown, and test harness setup continue to share one authoritative runtime-sidecar seam. Pollers that submit ingress work on behalf of one runtime, including hosted and script pollers, should receive the runtime-scoped submitter from this seam instead of rediscovering the current factory through global service state. Default replay-recording path templates that need different per-session artifact ownership should keep their session-ID substitution in `sessionScopedRecordPath(...)` within this service seam so the default runtime and replacement sessions keep one shared filename contract. |
| `pkg/service/runtime_sessions.go` | Live multi-session runtime lifecycle seam | Concurrent factory-session ownership, the reserved `~default` compatibility alias, folder-target discovery for new sessions, background runtime startup, and per-session stop behavior should stay centralized here so later session-scoped API, CLI, and UI work can reuse one live-session manager instead of rebuilding parallel runtime maps. Keep session-open startup on the split-context contract already shared with replacement runtime startup: the long-lived service context owns the background runtime and sidecars, but the original request context must still bound `waitForLiveRuntimeStart(...)` so canceled open-session requests do not leave behind newly registered live sessions. |
| `pkg/service/factory_cron_scheduler_test.go` | Service-mode cron runtime harness owner | Service-mode cron ingress helpers should not return immediately after `go svc.Run(...)`; wait until `currentLiveRuntime()` exists and `waitForLiveRuntimeStart(...)` succeeds before driving submissions or shutdown, otherwise cancellation-oriented runtime fixes can surface as pre-start test races instead of real cron behavior regressions. |
| `pkg/testutil/mock_factory.go` | Shared API/service seam test double | API and service seam tests should reuse this mock and keep its direct `factory.APIFactory`, `apisurface.APISurface`, and `apisurface.SessionAPISurface` compile-time assertions aligned with the live runtime contracts instead of building route-local stubs that can drift. |
| `pkg/workcontent/generated.go` | Canonical generated work-content translation owner | Supported generated `factoryapi.WorkContent` to canonical `[]interfaces.WorkContentPart` translation should stay centralized here; API handlers, projection reconstruction, replay hydration, and future model-operation surfaces may keep boundary-local validation or error wording, but they should not fork the text, image, audio, JSON, or binary success-path translation logic again. |
| `tests/functional/internal/support/api_server.go` | Shared live functional API-server lifecycle owner | Functional suites that need the same live API server, readiness wait, engine snapshot access, canonical event reads, or cancel-and-wait shutdown path should compose this helper instead of cloning `BuildFactoryService` plus `httptest.Server` startup in package-local `helpers_test.go` or `functional_server_test.go` wrappers; use the capture callbacks to preserve suite-local request or assertion conveniences, call the shared `GetFactoryEvents`/`Done`/`Stop` helpers instead of mirroring lifecycle fields locally, keep service-mode observability assertions behavioral by checking `Done()` stays open before explicit cancellation and closes after `Stop(t)`, and keep replay-only dashboard logic or runtime-only direct submission surfaces local to the owning suite. |
| `tests/functional/internal/support/harness.go` | Shared functional harness-state helper owner | Functional suites that need reused marking token lookup, place token counts, or token-identity derivation against live harness snapshots should promote that logic here instead of keeping package-local token traversal helpers in runtime API or neighboring functional suites. |
| `tests/functional/internal/support/events.go` | Shared functional event-history helper owner | Functional suites that reconstruct dispatch inputs or related work records from canonical factory-event history, or need shared nil-safe event payload unwrappers such as work, relation, and string helpers, should promote that logic here once a second behavior package needs it instead of keeping parallel `*_test.go` copies in behavior-local smoke suites. |
| `tests/functional/internal/support/command_runner.go` | Shared functional command-runner helper owner | Functional suites that only need deterministic successful stdout from script execution, or the same reusable request-recording success runner shape used unchanged across multiple suites, should reuse the shared helpers here; keep stderr, non-zero exit-code, returned-error, timeout/retry, or package-specific request-capture semantics local to the behavior package that asserts those distinct outcomes. |
| `api/components/schemas/data-models/FactoryName.yaml` | Reserved current-factory identifier contract | The public `FactoryName` schema owns the reserved `UNDEFINED` default-runtime identifier and must stay aligned with `/factory/~current` docs, generated artifacts, and handler semantics. |
| `pkg/api/openapi_contract_*_test.go`, `pkg/api/generated_contract_*_test.go`, and `pkg/api/workstation_request_projection_test.go` | API contract and projection guard surfaces | OpenAPI authored-versus-bundled assertions, generated-contract round-trip coverage, and workstation-request projection regression tests should stay split into same-package concern files with shared helper owners so the backend size gate can keep applying to maintained API contract tests without moving them into another package. When those tests trip cyclomatic-complexity limits, keep the full observable contract assertion in the owning file and extract same-package phase helpers for payload shape, round-trip decoding, and transcript or route assertions instead of replacing the test with narrower helper-only checks. |
| `pkg/api/workstation_request_projection_test.go` | Workstation dispatch projection regression surface | Dispatch-summary ownership changes should prove observable workstation-request projection behavior here, including the split between inference-attempt-owned detail and script-backed request or response fields. |
| `pkg/factory/projections/world_state.go`, sibling `world_state_*_generated.go` production owners, `world_state_test.go`, and sibling `world_*_support_test.go` owners | Event-first world-state projection ownership seam | Projection-visible replay or diagnostics ownership changes should prove canonical event reconstruction here by asserting `InferenceAttemptsByDispatchID`, `CompletedDispatches`, and `ProviderSessions` on reconstructed world state rather than helper-presence or package-local conversion seams; keep `world_state.go` focused on reconstruction flow, reducer state, mutation methods, and reducer-state lookup helpers, and move only pure generated-to-domain topology, work-item, work-content, relation, or payload adapters into same-package sibling `world_state_*_generated.go` owners when the projection package approaches the backend size gate instead of widening a separate helper package. When projection lineage or replay tests in `pkg/factory/projections` hit file-size or cyclomatic-complexity limits, keep the observable scenario assertions in the behavior-owned test file and extract package-local event builders plus snapshot-assertion helpers there rather than weakening the replay-visible checks or moving the tests out of package. |
| `pkg/api/testdata/canonical-event-vocabulary-stream.json` | OpenAPI vocabulary fixture | Canonical bundled-contract fixtures for event payload validation live here and must be updated alongside public schema field renames. |
| `pkg/factory/event_history.go` | Canonical event fanout owner | Live factory event subscriptions are owned here; cancellation-safe stream shutdown and non-blocking fanout behavior should stay centralized instead of being reimplemented in runtime or API layers. Model-execution evidence events should also be admitted here through the same generated-event append path instead of creating a parallel replay/event sink. |
| `pkg/factory/event_history_test.go` plus sibling `event_history_*.go` test owners | Event-history generated payload and canonical stream regression seam | Keep initial-structure payload assertions, script-boundary stream coverage, and chaining-trace lineage regressions in same-package sibling test owners so the emitted event contract stays reviewable by concern without moving helpers into a separate test package. |
| `pkg/interfaces/public_factory_enums.go` | Shared public factory enum normalizers | Shared worker and workstation alias ownership lives here; exported callers must choose explicit strict or permissive helpers instead of duplicating alias tables. |
| `pkg/interfaces/factory_runtime.go` | Handwritten public work-request boundary structs | Watched-file batch ingestion, generated worker output parsing, and fixture helpers still marshal these structs directly, so their JSON tags must stay aligned with the camelCase OpenAPI contract. |
| `pkg/cli/dashboard/dashboard.go` plus sibling `pkg/cli/dashboard/dashboard_*.go` files | CLI dashboard session-summary boundary | Keep dashboard render entrypoints, render-data shaping, dispatch-history fallback traversal, and formatting helpers in the same package, but split them into sibling `dashboard_*.go` files when size cleanup is needed so CLI/dashboard work stays behavior-preserving without duplicating traversal or formatting policy in neighboring packages. |
| `pkg/cli/dashboard/dashboard_test.go` | CLI dashboard behavioral regression surface | Package tests for dashboard rendering and session-summary mapping should assert observable labels and failed-work detail output, especially when `PlaceCategoriesByID` is absent and the CLI must fall back to dispatch history. |
| `pkg/config/factory_config_mapping.go` | Generated factory-config boundary alias rejection owner | Mirrored legacy alias guards for top-level objects and nested `definition` objects live here; keep one field inventory per boundary type, vary only the path passed into the shared rejection helper, and stop the helper at the documented generated-boundary depth so cleanup work does not broaden alias rejection into deeper nested `definition` chains. |
| `pkg/config/openapi_factory.go` | Public factory-config normalization and root-guard boundary owner | Path-specific enum normalization and mixed-field rejection for authored `factory.json` root guards belong here so malformed factory guards fail before generated decode with explicit `guards[i].field` messages. |
| `pkg/config/config_validator.go` | Semantic factory-config validation owner | Cross-field and duration semantics for authored factory guards belong in this validator after boundary normalization, matching the repository split between raw JSON shape rejection and internal config rule validation. |
| `pkg/config/named_factory.go` | Named-factory bootstrap pointer owner | Current-factory pointer reads and writes plus the precedence rule between a persisted named-factory root and a legacy root `factory.json` stay here so bootstrap resolution and malformed-pointer failures remain consistent across config load, CLI startup, and service activation paths. When maintainability cleanup touches named-factory persistence, keep target validation, payload normalization, and staged commit or replace steps as same-package helpers here instead of re-spreading those checks across service callers. |
| `pkg/factory/projections/simple_dashboard_projection.go` | Simple-dashboard runtime projection owner | Customer-visible runtime counts and place/work-item maps for the lightweight dashboard seam should stay derived here from reconstructed world state so active, non-terminal, terminal, and failed transition views remain aligned across backend projection consumers. |
| `pkg/config/layout.go` | Canonical factory clone owner | Root-level `FactoryConfig` fields such as authored `factory.guards` must be copied through `CloneFactoryConfig` so `LoadRuntimeConfig` and other layout flows do not silently drop supported customer policy; portable expand and named-factory persistence should also keep thin imported `factory.json` generation behind `authoredFactoryConfigForExpandedLayout(...)` before flattening so regression tests assert the same authored-layout contract users actually get on disk. |
| `pkg/config/factory_config_mapping_test.go` | Generated factory-config boundary regression surface | Boundary rejection regressions should stay focused here by asserting `generatedFactoryBoundaryErrorPrefix` plus the full path-specific retirement guidance string for top-level, nested `definition`, and nested `definition.cron` aliases. |
| `pkg/config/openapi_factory_test.go` | Generated OpenAPI JSON boundary regression surface | When cleanup work must preserve generated-boundary error strings or prove the generated `Factory` JSON shape itself, keep the proof here with `GeneratedFactoryFromOpenAPIJSON(...)` assertions over `generatedFactoryBoundaryErrorPrefix`, exact field paths, replacement guidance, and marshaled `factoryapi.Factory` workstation keys. |
| `pkg/replay/event_stream_artifact.go` | Replay event-stream import and adjacent-factory reconstruction owner | SSE replay import, truncated-tail recovery, legacy cron compatibility normalization, and adjacent authored-factory hydration plus payload rewriting should stay centralized here so saved event streams rebuild the same backend-visible factory shape as canonical replay artifacts; tests that need file-based conversion should call `ArtifactFromEventStreamFile` or `SaveArtifactFromEventStreamFile` instead of copying that hydration path into suite-local helpers. |
| `pkg/config/portable_bundled_files.go` | Supported portable bundled-file collection and materialization seam | Export/read-back portability changes should extend this seam so disk-backed `scripts/`, `docs/`, and allowlisted helper files stay mapped onto the canonical `factory/...` bundled-file contract for portable `factory/` layouts and named persisted factories without broadening generic runtime copy semantics; thin authored manifests should rehydrate existing supported entries from disk here before any broader default collection runs, portable script entries that omit inline content still need load-time mode normalization because they bypass the write path that already calls `os.Chmod(...)`, and overwrite detection for differing bundled-file materialization should stay centralized here so higher layers consume one replacement report instead of re-diffing on their own through `ExpandFactoryConfigLayoutWithReport(...)` or `LoadedFactoryConfig.PortableBundledFileReplacements()`. Maintainability cleanup in this seam should keep entry filtering, relative-path validation, and file-materialization helpers local so portable path policy and observable filesystem errors stay shared across export, expand, and starter-work collection flows. |
| `pkg/replay/event_artifact.go` | Replay run-started boundary normalization owner | Historical replay event streams may carry retired generated-factory workstation shapes even though current authored config rejects them; normalize only the replay run-started boundary here so checked-in artifacts remain replayable without relaxing general `pkg/config` generated-boundary validation. |
| `pkg/replay/event_artifact_test.go` | Replay generated-payload merge regression surface | Direct worker and workstation replace-or-append assertions belong here when the contract under change is generated replay payload content and deterministic merge order, so the tests can seed `factoryapi.Factory` values without filesystem-heavy runtime-config fixtures. |
| `tests/functional/replay_contracts/replay_event_stream_artifact_smoke_long_test.go` | Replay event-stream file conversion smoke owner | File-path opening, adjacent-factory hydration, and checked-in sample conversion helpers for the long replay smoke should stay test-owned here rather than expanding `pkg/replay` with production-facing file-wrapper convenience API. |
| `tests/functional/replay_contracts/replay_regression_harness_long_test.go` | Replay divergence regression harness owner | Intentional replay divergence assertions for the long replay contract lane should stay owned here on top of `testutil.NewReplayHarness(...)` so shared `pkg/testutil` keeps only broadly reused harness construction and success-path helpers. |
| `tests/functional/replay_contracts/replay_harness_customization_test.go` | Replay-lane harness customization owner | Copied-factory directory wiring, execution-base overrides, and replay scheduler-specific harness composition should stay shared only inside the replay contract lane on top of `testutil.NewServiceTestHarness(...)`, rather than remaining exported from repository-wide `pkg/testutil` helpers. |
| `tests/functional/smoke/service_pipeline_config_helpers_test.go` | Cross-lane service smoke pipeline-config helper owner | Shared smoke scaffolding for `simpleServicePipelineConfig()` and `twoStageServicePipelineConfig()` should stay in one untagged package-local test helper so both the default lane and the `functionallong` lane compile against the same observable service scenarios instead of reintroducing build-tag-local duplicates. |
| `tests/functional/runtime_api/api_generated_schema_deserialization_smoke_test.go` | Generated-schema runtime payload regression surface | Public workstation export or replay-contract retirements should keep serialized `factoryapi.Factory` assertions here so HTTP initial-structure, replay run-started, and runtime reconstruction lanes fail if emitted workstation JSON reintroduces retired keys like `promptTemplate`. |
| `tests/functional/runtime_api/api_inference_events_test.go` | Cross-boundary inference and dispatch-history regression surface | Event-first runtime changes that rebalance inference-attempt versus dispatch-summary ownership should keep functional proof here so canonical events, reconstructed world state, dashboard projections, and workstation-request views stay aligned. |
| `pkg/factory/runtime/dispatch_result_hook_test.go` | Dispatch-hook regression surface | Planner-present synchronous execution, planner-absent async worker-pool submission, missing-runner failures, and replay-tick validation should stay covered here when the behavior being changed is local to `workerPoolDispatchResultHook`, instead of forcing every dispatch-hook regression through the broader factory integration suite. |
| `pkg/factory/subsystems/subsystem_dispatcher.go` | Dispatcher snapshot-enrichment owner | Dispatcher execution must reattach `d.state` onto raw runtime snapshots before handing them to scheduler/enablement flows, because runtime-only snapshots intentionally omit topology while runtime guards still need normalized transition-to-worker mappings. |
| `pkg/workers/event_exit_code.go` | Shared worker event exit-code emission policy | Script and inference boundary-event payloads should route their final exit-code inclusion decision through this package-private seam so zero-vs-nonzero emission rules stay centralized even when diagnostic extraction differs by caller. |
| `pkg/workers/template_fields.go` | Workstation parameterized field-resolution owner | Working-directory, environment-variable, and worktree template resolution for workstation execution should stay centralized here so runtime cleanup work updates one explicit helper contract instead of reintroducing call-shape shims or duplicating template evaluation in `workstation_executor.go` or tests. |
| `pkg/workers/workstation_executor_test.go` | Workstation execution-context regression owner | When worker runtime-field cleanup changes how workstation templates are applied, keep the observable regression proof here by asserting the dispatched runtime context or `resolveWorkstationExecutionContext` output values rather than adding signature-only or helper-ownership tests elsewhere. |
| `pkg/workers/provider_behavior.go` and `pkg/workers/content_capability.go` | Provider CLI behavior and content-capability owner | Provider-specific argument construction and command-request shaping live in `provider_behavior.go`; worker inference paths should call this seam instead of reintroducing forwarding wrappers in `inference_provider.go`. Codex canonical image content should become ordered `-i <file>` arguments here after validating file readability against the subprocess working directory, while non-Codex text-only execution paths should reuse the shared content-capability helper to fail image-bearing inputs before subprocess invocation. |
| `pkg/` | Go implementation surface | Package-specific test commands in the guide should reference the real package paths under this root. |
| `tests/` | Smoke and fixture surface | Functional and release-facing checks run from the repository root against these checked-in fixtures. |
| `tests/functional_test/` | Legacy functional fixture store | Keep this directory focused on checked-in fixture data under `testdata/`; new observable functional coverage belongs in `tests/functional/...` and new shared helpers belong in `tests/functional/internal/support`. |
| `tests/release/testdata/cli_smoke_factory/` | Canonical packaged-binary fixture | Release verification should reuse this small self-contained factory fixture with checked-in seed input so every artifact smoke lane exercises the same runtime contract, including the required top-level canonical `factory.name` field. |
| `ui/` | Embedded dashboard workspace | UI build, test, and Storybook commands remain part of the same repository-root workflow. |
| `ui/package.json` | Frontend command surface | Targeted frontend test runs should normally go through the repo-owned Vitest scripts here such as `bun run test:unit -- --run <specs>` so jsdom setup, integration exclusions, and coverage behavior stay aligned with package expectations; keep repo-owned coverage scripts responsible for preparing any expected temp output directories such as `coverage/.tmp` before invoking Vitest so clean worktrees and CI runners do not fail on missing-path writes. When a work item explicitly requires direct `bun test <specs>` execution, keep those specs compatible with Bun's runner by preferring explicit dependency injection seams such as per-call `fetch` options instead of Vitest-only global helpers like `vi.stubGlobal(...)`. |
| `ui/vite.config.ts` | Frontend Vitest and coverage threshold owner | Frontend coverage floors and baseline ratchets should stay centralized in the package Vitest config here so `bun run test:coverage` remains the one reviewer-visible command for both execution and enforcement; keep the percentage gate scoped to app/runtime code and exclude non-executable assets, barrel exports, CSS, and Node tooling scripts from the V8 denominator while continuing to run the script tests themselves through Vitest. |
| `ui/src/testing/vitest.setup.ts` | Shared frontend test-environment shim | Package-wide jsdom fixes for repo-owned UI test commands belong here so `bun run test:coverage` and related frontend checks keep one stable browser-compatibility setup instead of scattering per-test patches. |
| `ui/fallback_dist/` | Embedded dashboard fallback shell contract | This directory is the compile-safe placeholder shell served when generated `ui/dist/` assets are unavailable; shell branding changes must update both `index.html` metadata and the tiny runtime placeholder assets here so backend-only or fresh-checkout dashboard responses do not teach stale copy. |
| `ui/.storybook/dashboard-story-runtime.tsx` | Dashboard Storybook runtime seam | Dashboard stories that exercise live selection or timeline behavior should seed the current `features/current-selection` and `features/timeline` stores through this decorator instead of mocking widget-local state, so Storybook browser checks stay aligned with the production dashboard store shape. When a dashboard story needs per-session event-stream behavior, prefer the decorator-owned `eventSourceMocks` seam over ad hoc `window.EventSource` overrides so session switches can replay session-specific snapshots through the same route and store lifecycle the app uses in production. |
| `ui/src/features/export/export-factory-dialog.stories.tsx` | Export dialog browser verification seam | Dashboard export-dialog keyboard and validation smoke should live in this Storybook story so browser-backed UI proof stays close to the shared dialog implementation instead of being recreated in ad hoc scripts. |
| `ui/scripts/verify-import-export-storybook-responsive.mjs` | Storybook responsive verification seam | Breakpoint-sensitive dialog and widget evidence should stay in this Playwright script so the documented `390x844`, `768x1024`, and `1440x900` checks are executable against built Storybook `iframe.html` stories instead of remaining doc-only claims; when a story requires launch-locale browser proof, keep paired English and `zh-CN` assertions here rather than relying on a generic non-default locale story. Cross-surface graph consolidation checks should compare representative factory and trace Storybook graph controls here, using computed control-style assertions instead of screenshot-only review notes so shared graph chrome drift stays behaviorally testable, and text anchors inside graph stories should be scoped to a unique region-local locator rather than page-wide duplicated labels so Playwright strict mode exercises the real runtime surface instead of failing on ambiguous matches. Current-selection provider-session success proof should stay here as the browser-backed check that selects the affected session and confirms the timestamp-prefixed source path renders in the success panel. When one responsive Storybook story can mutate browser storage or other origin-scoped state, run each check in a fresh Playwright browser context rather than only opening a new page so later stories cannot inherit hidden state from earlier verification steps. |
| `ui/src/features/trace-drilldown/trace-grid-card.tsx`, `ui/src/features/trace-drilldown/trace-workstation-path.tsx`, `ui/src/features/trace-drilldown/trace-relation-flow.tsx` | Trace drilldown overflow containment seam | Trace drilldown mixes intentionally wide tables with React Flow graphs. Keep page-level mobile overflow contained by giving the table its own horizontal scroll region and clipping graph shells at the card boundary so localized labels or graph transforms do not widen the dashboard canvas. |
| `ui/src/features/import/dashboard-import-preview-dialog.stories.tsx` | Import preview browser verification seam | Import-preview keyboard and dialog-surface browser checks should stay in this focused Storybook story beside the dashboard-owned import dialog so reviewers can rerun the import dialog proof without booting the full graph flow. |
| `ui/src/features/work-outcome/components/d3-information-card.tsx` and `ui/src/features/work-outcome/components/work-chart.tsx` | Resizable work-outcome height chain seam | Dashboard charts that live inside resizable bento cards need one uninterrupted `min-h-0` and flex-height chain from the shared card body through the feature region into the chart surface, the chart container itself must not keep a fixed ready-state height if the widget is expected to grow with grid resizing, and both ready and non-ready card states need explicit `min-w-0`/`w-full` plus full-height status-panel sizing so browser-measured chart surfaces and empty/loading/error panels stay readable in resized cards. |
| `ui/src/features/work-outcome/components/d3-information-card.stories.tsx` and `ui/src/features/work-outcome/components/work-chart.stories.tsx` | Work-outcome chart Storybook verification seams | Card-level resize regressions should prove chart growth through the real bento resize handles in `d3-information-card.stories.tsx`, while chart-only spacing, zoom-toolbar visibility, removed zoom-copy assertions, and constrained-width no-overflow checks stay in `work-chart.stories.tsx` by asserting feature-owned `data-work-chart-*` hooks instead of Recharts wrapper internals. |
| `ui/src/features/work-outcome/hooks/useWorkOutcomeChart.ts` and `ui/src/features/work-outcome/hooks/useWorkOutcomeChart.test.ts` | Work-outcome timeline contract seam | The work-outcome timeline projection should consume the canonical camelCase generated event contract directly instead of carrying local snake_case fallback shims, and focused hook fixtures should mirror that same `workId`/`traceId`/`workTypeName` payload shape so throughput regressions stay proven at the hook output layer rather than by compatibility helpers. |
| `ui/src/features/workflow-activity/components/mutation-dialog.test.tsx` | Shared mutation-dialog verification seam | Import/export dialog cleanup that depends on shared overlay semantics should keep the direct accessible-title and description proof here so future dialog work does not fall back to architecture-doc reasoning alone. |
| `ui/src/components/ui/dialog.tsx` | Shared shadcn dialog primitive seam | Dashboard mutation overlays that need repository-standard dialog chrome should compose this shared Radix-backed primitive, and flows that must block dismissal during in-flight mutations should use its labeled and disableable close control instead of reintroducing bespoke overlay wrappers. |
| `ui/src/features/bento/dashboard-bento.tsx` | Dashboard-level overlay composition seam | Dashboard overlays that are triggered from bento widgets but should not be clipped or owned by a widget body should keep their controller state here and render dialog siblings beside `AgentBentoLayout`. |
| `ui/src/features/header/dashboard-header.tsx` | Dashboard header branding and action seam | Dashboard-shell branding, live-stream status semantics, and icon-only export/current actions should stay centralized here, while reusable visual identity pieces live beside the header instead of being recreated in app-level layout files. |
| `ui/src/features/header/dashboard-session-tabs.stories.tsx` | Session-tab browser verification seam | Workspace-session tab-strip and open/close-session browser proof should stay in this focused Storybook story so later tab-switching or close-tab work can extend the same browser-backed seam instead of recreating dashboard-shell setup in ad hoc scripts. |
| `ui/src/App.session-switching.stories.tsx` | Session-switch browser verification seam | Dashboard session-switch regressions that must prove session-scoped selection, active-work visibility, and submit-draft reset should stay in this focused full-app story, using `eventSourceMocks` from the shared Storybook runtime so reviewers can exercise real session-route switches without hand-wiring widget-local state. |
| `ui/src/features/header/tick-slider-control.tsx` | Dashboard timeline slider seam | Timeline tick label, status text, disabled-state messaging, return-to-current behavior, and responsive slider-cluster layout should stay owned here so header composition work can move the control group without reimplementing slider semantics in page shells or stories. |
| `ui/src/features/header/dashboard-status-panel.tsx` | Dashboard fallback status shell seam | Empty and error dashboard-shell states should reuse the same Infinite You brand lockup and `DASHBOARD_PANEL_SHELL_CLASS` shell contract here so fallback headings stay aligned with the live toolbar and grid-card framing. |
| `ui/src/features/header/dashboard-export-dialog.tsx` | Dashboard export host seam | Dashboard-owned export overlays should stay as thin host components that bridge the dialog open-store, current-factory export query, and shared `ExportFactoryDialog` surface rather than moving fetch or mutation ownership into header buttons. |
| `ui/src/features/export/export-factory-dialog.tsx` | Shared export-dialog reset seam | Async current-factory refreshes may update the seeded factory name while the dialog is already open; reset the form on open and close, but preserve a user-edited export name unless the input is still mirroring the previous seed so background refreshes do not clobber an in-progress export filename. |
| `ui/src/features/workflow-activity/current-activity-import-controller.ts` | Shared workflow-import controller seam | Workflow graph drag-drop, preview, and activation state should stay reusable through this controller so dashboard-owned dialogs can reuse the same import flow without remounting feature-local widget state. |
| `ui/src/App.stories.tsx` and `ui/src/App.timeline-regression.stories.tsx` | Dashboard Storybook smoke surface | Current-selection and dashboard workflow stories should assert observable headings, supporting timestamps, and the absence of retired work-ID or trace-ID row metadata alongside dispatch markers, mixed inference/script selected-work history output, pending or unavailable inference copy, localized dashboard-header states, and prompt-editor help or validation flows here so browser-backed regression coverage follows the customer-visible contract instead of helper structure. |
| `ui/src/App.layout-graph.test.tsx`, `ui/src/App.follow-up-submit.test.tsx`, `ui/src/App.follow-up-trace.test.tsx`, `ui/src/App.timeline.test.tsx`, `ui/src/App.replay-stream.test.tsx`, `ui/src/App.replay-workstation-requests.test.tsx`, and `ui/src/App.replay-dispatch-selection.test.tsx` | Dashboard app-level regression surfaces | Keep app-shell regressions split by observable surface area here: `App.layout-graph.test.tsx` for dashboard layout and graph behavior, `App.follow-up-submit.test.tsx` for submit-work, export coexistence, workstation-run, and live-total shell flows, `App.follow-up-trace.test.tsx` for trace follow-up routing, `App.timeline.test.tsx` for fixed-tick reconstruction behavior, `App.replay-stream.test.tsx` for event-replay rendering and graph-state transitions, and the replay workstation files for request-history detail routing. For React Flow-heavy app-shell assertions, wait for stable shell readiness such as the dashboard heading before asserting large node counts, and prefer role queries with explicit timeouts over short generic `waitFor` loops so CI coverage runs do not race graph hydration. When app-level coverage grows, extract the next focused shell surface instead of reintroducing file-size suppressions or folding everything back into one monolithic harness. |
| `ui/src/features/submit-work/use-submit-work-widget.ts`, `ui/src/features/submit-work/submit-work-card.tsx`, `ui/src/features/submit-work/submit-work-card.stories.tsx`, `ui/src/api/work/api.ts`, `ui/src/api/work/api.test.ts`, and `ui/src/api/work/api.additional.test.ts` | Dashboard submit-work mutation seam | Submit-work field rules, ready/guidance status copy, post-success draft clearing, and the stable action-row layout contract should stay normalized across the widget hook, card, API wrapper, and feature story here: the hook owns trim-aware validation plus draft reset behavior, the card owns accessible field wiring and inline validation, configured work-type selection stays preserved only while still valid, unary submit must require a non-blank request name while keeping request text optional, and browser-backed story regressions should prove the submit button keeps the same measured right edge across ready, submitting, success, error, and wrapped validation states. The API wrapper should route non-success responses through the shared transport helpers in `ui/src/api/transport.ts` instead of reintroducing local JSON or content-type parsers. Wrapper regressions for explicit empty payloads or structured errors should assert the generated camelCase OpenAPI contract (`workTypeName`, `traceId`) instead of ad hoc snake_case request or response shapes. |
| `ui/src/features/timeline/state/timeline/replayWorldState.ts` | Timeline replay reducer seam | Live timeline replay should reconstruct active and completed dispatch state here by reading canonical event context first and falling back to supported legacy payload fields only at the request and response consumption sites. |
| `ui/src/features/timeline/state/timeline/replayCompletion.ts` | Timeline replay completion projection seam | Dispatch-response replay should keep `WorldCompletion -> DashboardTraceDispatch` shaping and any supported legacy completion fallback reads centralized here instead of reintroducing alias-only wrapper helpers. |
| `ui/src/features/timeline/state/factoryTimelineStore.test.ts` | Focused timeline replay contract surface | Replay cleanup work should prove canonical and supported legacy dispatch request or response behavior here by asserting reconstructed workstation requests, trace dispatches, runtime sessions, and world state rather than source-layout details. |
| `ui/src/App.current-selection.test.tsx` | Dedicated current-selection app regression surface | App-level selected-work, trace-card, responsive-layout, terminal-state, and mixed inference/script dispatch-history regressions should live here once a focused current-selection flow would otherwise outgrow the standard handwritten file or function size limits; when inference attempts or nested request/response bodies are collapsed by default, this seam must prove the app-level flow by expanding the relevant attempt and body disclosures before asserting payload or diagnostic text. |
| `ui/src/App.import.test.tsx`, `ui/src/App.export-dialog.test.tsx`, `ui/src/App.export-submit.test.tsx`, `ui/src/App.layout-graph.test.tsx`, `ui/src/App.follow-up-submit.test.tsx`, `ui/src/App.follow-up-trace.test.tsx`, `ui/src/App.toolbar-locale.test.tsx`, and `ui/src/testing/app-shell-*.ts*` | Focused app-shell regression seams | When app-shell coverage grows past the repo size gates, split shell behaviors into focused app-level files by surface area, keep shared render, lifecycle, browser-stub, and default hook-mock builders local to `ui/src/testing/app-shell-*.ts*`, route shared `useCurrentEditableFactoryDefinition` setup through that harness instead of per-file duplicate mocks, keep only feature-specific seeding in the owning `App.*.test.tsx` file, and prefer named local scenario fixtures over new wrapper helpers when that seeding repeats. Prefer keeping both the focused files and their local helpers under the standard handwritten size limit before adding any new explicit Biome allowlist entries. |
| `ui/src/features/current-selection/workstation-request-detail.tsx` | Workstation-request detail fallback seam | Request-detail rendering, script-versus-inference alternate panels, sparse projected field fallbacks, and the omission of low-value request-count summary rows should stay centralized here so current-selection request diagnostics keep one observable detail contract instead of splitting fallback copy across summary cards or app shells. |
| `ui/src/features/current-selection/workstation-request-detail.test.tsx` | Workstation-request detail regression surface | Customer-visible request-detail regressions should assert rendered markdown/plain-text prompt handling, hidden request-count summary labels, plus sparse request, trace, script-request, and script-response fallback copy here rather than relying on helper-only tests or broader app smoke coverage. |
| `ui/src/features/current-selection/inference-attempt.tsx` and `ui/src/features/current-selection/execution-details.test.tsx` | Inference-attempt disclosure seam | Current-selection inference attempts should own their own collapsed-by-default disclosure state here, keep attempt status and concise timing visible in the collapsed summary, and prove per-attempt `aria-expanded` behavior plus independent expansion through the focused execution-details regression surface instead of broader widget smoke tests. |
| `ui/src/features/current-selection/detail-card-shared.tsx` and `ui/src/features/current-selection/request-authored-text.test.tsx` | Shared authored-body formatting seam | Markdown-safe authored body rendering for current-selection detail cards should stay centralized here through the neutral `AuthoredBodyText` formatter, while request-specific wrappers and tests continue proving headings, lists, code, plain-text fallback, and inert raw-HTML handling without duplicating formatter logic in request or inference card components. |
| `ui/src/features/current-selection/detail-card-shared.tsx`, `provider-session-attempts.tsx`, and `selected-work-dispatch-history-card.tsx` | Current-selection emphasis color seam | Shared current-selection action/link foreground emphasis belongs in `detail-card-shared.tsx`, while current-dispatch card and badge emphasis belongs in the two dispatch-list owners; use semantic tokens such as `on-foreground` for comparable selected/current states instead of reintroducing local accent or signal color utilities. |
| `ui/src/features/current-selection/work-item-card.tsx` | Selected-work summary ownership seam | The selected-work card summary should stay limited to stable work metadata, relationships, and dispatch count here; request, response, provider-session, and failure diagnostics for specific dispatches should not be reintroduced into this top-level summary surface. |
| `ui/src/features/current-selection/work-item-card.test.tsx` | Selected-work component regression surface | Selected-work tests should prove the relationship-graph surface plus dispatch-history-owned request, response, nested attempt, related-work selection, and trace-link behavior here so simplification regressions fail at the component seam before broader app flows are involved. |
| `ui/src/features/current-selection/selected-work-dispatch-history-card.tsx` | Selected-work dispatch-diagnostic ownership seam | Dispatch-specific request, response, and nested inference/script attempt inspection for selected work items should stay centralized in this `Workstation dispatches` surface so dispatch-backed diagnostics share one path instead of splitting across top-level summary panels. |
| `ui/src/App.workstation-requests.stories.tsx` | Selected-work dispatch Storybook browser seam | Browser-backed proof for selected-work dispatch cleanup should live in the focused dashboard story that selects the active work item, asserts the compact summary fields, and expands both inference and script attempt disclosures so the Storybook interaction runner stays aligned with the real current-selection composition. |
| `ui/src/features/bento/agent-bento.stories.tsx` | Shared bento-layout Storybook seam | Shared layout or accessibility-label cleanup work should keep at least one real multi-card dashboard state here, using actual feature cards rather than placeholder children, so Storybook evidence proves board rendering and the customer-visible region name through the owning bento seam. |
| `ui/src/components/ui/widget-frame.stories.tsx` | Shared widget-frame Storybook seam | Widget-frame ownership changes should keep a real feature framing state here, such as current-selection empty guidance, so Storybook evidence continues to cover observable header controls, card titles, and empty/detail body treatment through the shared frame owner. |
| `ui/src/components/ui/formatters.ts` | Shared display formatter seam | Cross-feature duration, work-item, provider-session, trace, and customer-visible current-selection timestamp display helpers belong here; route both nested inference-attempt times and higher-level dispatch or execution summary timestamps through this seam instead of reformatting ISO values inline, and do not reintroduce the removed legacy `components/dashboard/formatters.ts` path as a second owner. |
| `ui/src/components/ui/formatters.test.ts` | Shared formatter regression seam | Helper-focused coverage for duration, provider-session, workstation-outcome, and trace-adjacent display text should live with the shared formatter owner so future seam moves preserve observable text without routing tests back through dashboard compatibility shims. |
| `ui/src/components/ui/place-labels.ts` | Shared place-label seam | Cross-feature place and place-state label helpers belong here; do not reintroduce the removed legacy `components/dashboard/place-labels.ts` path as a second implementation owner. |
| `ui/src/components/ui/place-labels.test.ts` | Shared place-label regression seam | Place label split and fallback coverage should stay adjacent to the shared helper owner, while rendered current-selection or flowchart tests continue proving those labels appear correctly in customer-visible surfaces. |
| `ui/integration/event-stream-replay.integration.test.mjs` | Browser-backed dashboard integration harness | Keep browser-visible dashboard smoke, adjacent API stubs such as explicit default-session routes for `/factories/~default/events`, `/factories/~default/factory/~current`, and `/factories/~default/factory/~current/editable-definition`, plus the retained unscoped prompt-template compatibility aliases, cross-origin `POST /factory`, drag/drop `DataTransfer` file setup, blob-download artifact capture, and dashboard-header assertions against accessible `dashboard summary` status semantics in this one preview-backed Playwright/Vitest lane so UI integration coverage extends the built app without adding a second browser harness or reintroducing retired visible toolbar copy. |
| `ui/scripts/release-dashboard-smoke.mjs` | Packaged dashboard branding smoke | Release smoke for the embedded dashboard should assert the rendered Infinite You heading plus the accessible live stream `role="status"` name so packaged artifacts catch shell-branding drift before PR review or release. |
| `ui/src/components/ui/` | Shared website primitive layer | New reusable button, field, dialog, chart, table, skeleton, calendar, collapsible, resizable, and shared layout primitives such as bento boards or widget frames belong here; dashboard consumers should import these canonical owners directly instead of restoring deleted `components/dashboard/*` compatibility shims. |
| `ui/src/components/ui/dashboard-shell.tsx` | Shared dashboard panel shell primitive | Panel-style dashboard surfaces that must share border, radius, background, text color, or shadow should use `DashboardPanelShell` or compose `DASHBOARD_PANEL_SHELL_CLASS` here and keep only surface-specific layout, padding, and content styling in feature-owned components. |
| `ui/src/features/bento/hooks/useDashboardLayout.ts` | Dashboard layout persistence seam | The persisted layout contract should import `AgentBentoLayoutItem` from `ui/src/components/ui/`, keep default card registration plus saved-layout migrations here, and add new dashboard widgets by updating both `DASHBOARD_WIDGET_IDS` and `DEFAULT_DASHBOARD_LAYOUT` so local-storage migrations, default card positions, and the live bento grid stay aligned with the shared layout owner instead of a compatibility shim. Dashboard compaction or card-order changes should update both the baseline layout and any exact-match local-storage migrations instead of leaving returning users on retired geometry. |
| `ui/src/lib/cn.ts` | Shared class-composition utility | Shared website primitives should compose token-based Tailwind class strings through this utility instead of duplicating local class join helpers in each new primitive family. |
| `ui/src/testing/replay-fixtures.ts` | Canonical replay fixture loader seam | Replay regression tests that need typed `FactoryEvent[]` or projected timeline snapshots should load checked-in `ui/integration/fixtures/*.jsonl` data through this helper instead of hand-parsing duplicate fixture maps in each test file. |
| `ui/src/testing/replay-fixtures.test.ts` | Maintained replay fixture contract seam | Assertions about checked-in UI replay fixture event content, such as script-request commands or dispatch output-work handoffs, belong here when the fixture itself is the contract under review; rendered dashboard behavior should continue to use feature or integration tests. |
| `ui/src/testing/replay-fixture-catalog.ts` | Replay and browser-workflow coverage contract | Browser-backed dashboard smoke coverage, including supplemental browser-only workflows that reuse replay fixtures such as PNG export/import roundtrips, should register scenario metadata here so coverage reporting and integration assertions stay on one source of truth. |
| `ui/scripts/write-replay-coverage-report.ts` | Replay coverage reporter | Package scripts should use this repository-owned reporter to validate replay metadata instead of embedding ad hoc fixture maps in tests or CI. |
| `ui/scripts/normalize-dist-output.mjs` | Embedded asset normalizer | The documented UI build path ends by normalizing Vite entry asset names, preserving required companion assets such as Monaco editor chunks or workers, pruning empty build directories that Go cannot embed, removing legacy stamp files, and refreshing the ignored Go embed-registration file so local `ui/dist/` output stays stable for Go builds and CI verification without creating PR merge churn. |
| `ui/src/features/import/factory-png-import.ts` | PNG import envelope boundary | PNG metadata now uses the direct canonical `Factory` fields plus `schemaVersion`; the importer should reject retired wrapper keys like nested `factory` or `factoryName` and hand the same canonical `Factory` object to `/factory`. |
| `ui/src/App.timeline-regression.stories.tsx` and `ui/src/stories/dashboardStoryTestUtils.ts` | Dashboard top-section responsive regressions | Dashboard header compaction and top-card density regressions should be verified through the Storybook dashboard seam at concrete desktop, tablet, and mobile widths, with shared helpers asserting visible keyboard-reachable controls and no page-level horizontal overflow. |
| `docs/internal/development/website-feature-module-contract.md`, `ui/scripts/check-feature-root-files.mjs`, `ui/scripts/feature-root-file-allowlist.mjs`, and `ui/src/features/` | Feature-directory governance contract | Frontend feature-structure work should treat `website-feature-module-contract.md` as the repository-owned contract for `ui/src/features`: feature roots may contain directories only, root `index.ts` files are prohibited, feature public exports belong under one canonical `public/` directory with `public/index.ts` as the default entrypoint, approved ownership buckets include `components/`, `hooks/`, `messages/`, `state/`, `selectors/`, and `lib/`, better-named domain folders should win when they explain ownership more clearly, shared code should move into shared UI modules instead of being copied between features, and oversized features should default toward sibling-feature splits when that produces clearer behavior ownership than adding more internal buckets. The same contract now owns the split-versus-bucket decision rules and the representative `current-selection` migration path for provider-session detail, dispatch-history behavior, workstation editing, terminal-work detail, and shared selection state, so future migrations should extend that example instead of inventing a second policy source. Enforce the current repository contract through `check-feature-root-files.mjs` in the normal UI lint/check workflow, treat `feature-root-file-allowlist.mjs` as the one temporary debt inventory that must shrink in the same change that relocates a root-level file, fail the guard if an allowlist entry becomes stale after a migration, and when a root-level feature file is pure helper or type surface, move it under `lib/` while keeping maintained cross-feature imports on the feature's `public/` seam, including hook-driven feature APIs that need to re-export both hooks and supporting helper types from `public/index.ts`. Keep `public/` focused on stable seams only: if a feature-local dialog or component depends back on a sibling feature, import it directly from its governed internal directory instead of re-exporting it through `public/`, which avoids cross-feature cycle risks during large migrations. When a governance move puts Storybook stories under `components/`, update repo-root-relative imports such as `styles.css`, shared story support modules, and sibling-feature public barrels to the deeper relative path in the same change, because `make typecheck` and `make ui-lint` can still pass while `bun run build-storybook` fails on stale story import depth. Apply the same rule to replay or fixture-backed tests moved under deeper feature subdirectories: update raw `ui/integration/fixtures/*?raw`, `ui/src/testing/*`, and sibling-feature helper imports in the same change, because `make ui-lint` and most targeted stories can stay green while the repository-wide `make test` lane later fails on stale relative paths. |
| `ui/src/features/provider-session-detail/components/provider-session-widget.tsx`, `provider-session-detail-panel.tsx`, `provider-session-transcript.tsx`, `hooks/use-provider-session-detail.ts`, and `ui/src/features/current-selection/hooks/useSelectedProviderSessionState.ts` | Provider-session dashboard card seam | The dedicated dashboard provider-session card now lives in the extracted `provider-session-detail` sibling feature, keeps explicit `loading`, `not-found`, `error`, `empty`, `empty-transcript`, `parse-error`, and `success` states in the detail panel, and shares one lifted selected-session state hook from current-selection so transcript or metadata work does not fork selection ownership per card. The detail panel should keep selected-session identity visible even while fetch-backed status copy changes, and reserve the fetched source-file metrics for states that actually returned session detail data. Keep chronological transcript rendering in `provider-session-transcript.tsx` so the shell file stays under the repo size gate, and treat long tool arguments or outputs as inline transcript disclosures rather than separate widgets or cards. Empty-transcript sessions can still carry malformed-line or unknown-event diagnostics, so keep diagnostics accessible from the same detail panel even when no transcript-visible entries render. That shared hook also owns visibility-scoped invalidation: when the active work-item or workstation context no longer exposes the previously selected session, clear it there instead of layering per-card cleanup rules. |
| `ui/src/features/current-selection/current-selection-widget.provider-session-selection.test.tsx`, `ui/src/features/current-selection/hooks/useSelectedProviderSessionState.test.tsx`, and `ui/src/features/bento/components/dashboard-bento.test.tsx` | Provider-session selection regression coverage | Provider-session selection regressions should be proved at the real widget and dashboard seams: use the current-selection widget tests for selectable work-item inference history, workstation run-history behavior, and timestamp-prefixed Codex session filenames, the shared hook tests for stale-selection clearing when visible sessions change, and the bento test for cross-card state sharing instead of source-shape-only assertions. |

## Reusable Rules

- When maintainer docs describe command execution, anchor the instructions to the repository root that contains `go.mod` and `Makefile`.
- When a customer-facing reference topic grows into a stable packaged `you docs <topic>` surface, update both `docs/reference/*.md` and `pkg/cli/docs/reference/*.md` together, then keep `pkg/cli/docs/docs.go` plus `pkg/cli/docs/docs_test.go` aligned so the packaged topic list and embedded markdown stay in sync.
- Keep cross-platform maintainer command logic in repo-owned Go entrypoints such as `cmd/functionallane/` or `cmd/releaseprep/` instead of embedding platform-specific shell discovery pipelines directly in `Makefile`.
- If a workflow temporarily changes directories, state that it starts from the repository root and why the subdirectory hop is required.
- When GitHub Actions or other automation is added, prefer repository-owned root commands or package scripts that the maintainer guide already documents instead of inventing CI-only command sequences.
- When PR scope feedback cites unrelated files, fetch the current `origin/main` and compare `gh pr diff --name-only` with `git diff origin/main...HEAD` before doing rollback work; merged base-branch changes can make an earlier scope blocker stale, but the maintainer workflow should still post a PR conversation reply that points to the current diff evidence.
- For dashboard API routing regressions, keep coverage at the real caller tests in `ui/src/api/*.test.ts` and assert the exact default-session and non-default request URLs instead of helper-only or substring-only expectations.
- When a typed UI API refactor is intended to preserve behavior, keep the regression proof at the feature API test boundary in `ui/src/api/*.test.ts` by asserting the exact request method and route together with the full typed success payload, rather than only helper-level parsing details.
- When user-visible work payload history needs replay-safe semantics, keep the canonical lineage model on the selected-tick backend world-state seam (`pkg/factory/projections` + `pkg/interfaces`) and have API or UI projections resolve initial-submission, consumed-input, selected-work, and output-work snapshots from that shared model instead of reconstructing payload precedence independently at each surface.
- When lineage regressions need realistic replay coverage, keep the checked-in event stream fixture next to the backend projection owner under `pkg/factory/projections/testdata/` and have both projection and API request-projection tests decode that same JSONL history instead of duplicating divergent in-memory event builders per surface.
- When default-session dashboard routing changes, update `ui/integration/event-stream-replay.integration.test.mjs` to mirror the canonical `/factories/~default/...` browser-visible endpoints for the targeted APIs while preserving backend compatibility aliases that still remain intentionally unscoped in adjacent modules.
- When dashboard Storybook verification uses `ui/.storybook/dashboard-story-runtime.tsx`, keep its `fetchMocks` and `eventSourceMocks` on the exact request paths the live callers emit; default-session work, events, and current-factory-definition stories should mock `/factories/~default/...` endpoints, while untouched compatibility-only callers can stay on their existing unscoped paths, because the Storybook runtime matches paths exactly.
- For dashboard `/events` replay regressions in `ui/src/App.replay-*.test.tsx`, assert stable projected outcomes such as timeline slider state, totals, trace cards, and current-selection surfaces rather than exact workstation-node labels unless the replay fixture is explicitly meant to prove graph-node naming.
- When browser-backed Storybook checks target icon-only controls or compact header chrome, keep the assertions aligned with the current accessible `aria-label` surface and the current toolbar row structure rather than retired visible-text labels or pre-refactor geometry assumptions; otherwise `bun run test-storybook` can fail even when the shipped UI remains correct.
- When feature work touches the root `Makefile`, preserve the existing maintainer command surface unless the same change intentionally updates the corresponding docs and workflows; `typecheck`, `release`, and `api-smoke` still own UI typecheck, release prep, and generated client or server verification at the repository boundary.
- When one exported backend seam is a strict superset of another, keep the shared runtime methods owned by the base interface and embed that contract from the wider seam instead of re-declaring duplicate method sets across packages.
- When a wider backend seam embeds a narrower runtime contract, keep direct compile-time assertions for both interfaces on the live service and the shared seam test double so interface drift fails at compile time before route or service smoke tests run.
- When a backend cleanup narrows an exported seam without changing caller behavior, keep regression coverage at the owning package's public helper boundary and assert observable forwarding or no-op behavior instead of symbol-name, reflection, or source-inventory checks.
- Keep canonical detached-copy ownership for runtime tokens, token histories, token colors, provider-session metadata, and provider-failure metadata in `pkg/interfaces/world_state_clones.go`; backend packages that need those shapes should call the shared helpers instead of maintaining package-local clone trees.
- When runtime snapshot, transitioner, token-transformer, worker, or replay code needs detached copies of canonical runtime tokens or provider metadata, route those copies through `pkg/interfaces/world_state_clones.go` helpers such as `interfaces.CloneToken(...)`, `interfaces.CloneTokens(...)`, `interfaces.CloneTokenColor(...)`, `interfaces.CloneTokenHistory(...)`, `interfaces.CloneProviderSessionMetadata(...)`, and `interfaces.CloneProviderFailureMetadata(...)` instead of reintroducing package-local clones in `pkg/factory/engine`, `pkg/factory/subsystems`, `pkg/workers`, or `pkg/replay`.
- When backend size cleanup touches runtime orchestration packages such as `pkg/factory/runtime`, `pkg/factory/engine`, `pkg/factory/scheduler`, `pkg/factory/subsystems`, `pkg/replay`, or `pkg/config`, prefer splitting long constructors or execution paths into same-package phase helpers that keep the public seam and dependency direction unchanged instead of introducing a new helper package just to satisfy the file or function limit.
- When `pkgmaintcheck` flags backend test complexity in owning seams such as `pkg/config`, keep the observable assertions in the same package and reduce cyclomatic complexity by extracting same-package fixture, JSON-shape, and runtime-shape assertion helpers instead of weakening coverage or moving the test seam into a shared helper package.
- When the remaining `pkgmaintcheck` debt is only cyclomatic complexity in contract-heavy backend tests or dense assertion helpers, prefer a symbol-scoped `pkgmaintcheck:ignore-cyclomatic-complexity` comment with a concrete reviewer-facing justification over splitting one readable end-to-end seam into opaque helper layers.
- When maintainability cleanup touches event-driven runtime reducers or schedulers such as `pkg/factory/engine`, `pkg/factory/projections`, or `pkg/factory/scheduler`, split large loops into same-package phase helpers for signal wait, normalization, route selection, and candidate ordering so the runtime contract stays stable while the control flow becomes reviewer-readable.
- When backend size cleanup touches schema-heavy config or projection packages such as `pkg/config` or `pkg/factory/projections`, prefer same-package sibling files that separate decode, validation, expansion, dispatch, or generated-conversion helpers so the public seam stays stable while file-size ownership becomes reviewable by concern.
- When factory-graph editor availability depends on authored route families, derive the unsupported reason from already-loaded dashboard topology metadata before any lazy editable-definition query is enabled; entry guards must hold on the first click, not only after editor-mode data finishes loading.
- When factory authoring adds a new workstation route dimension, update the editable graph round-trip owners under `ui/src/features/factory-graph-editor/` to preserve that route identity end to end, or block editor entry with an explicit unavailable reason until the graph edge model can represent the new route family without silently rewriting saved topology.
- When factory-graph editor edge presentation changes, keep the customer-facing route copy in `ui/src/features/factory-graph-editor/messages/editor.ts` and drive reveal state through the custom edge seam in `ui/src/features/factory-graph-editor/components/factory-graph-editor-edge.tsx` so inline labels can stay contextual without dropping accessible edge names.
- When factory-graph editor edge labels are visually hidden by default, keep `edgesFocusable` enabled on the integrated current-activity viewport in `ui/src/features/workflow-activity/components/react-flow-current-activity-card-viewport.tsx` so the accessible edge name still has a keyboard interaction path outside delete mode.
- When factory-graph editor layout work needs observer parity, route editor node positioning through `ui/src/features/factory-graph-editor/factory-graph-editor-layout.ts` and the shared layered-layout helper in `ui/src/features/flowchart/layout.ts` instead of reintroducing fixed kind-column placement inside `factory-graph-editor-flow.tsx`.
- When multiple workflow-activity graph consumers need topology-keyed async layout reuse, keep the cache and in-flight coordination in `ui/src/features/workflow-activity/hooks/workflow-topology-async-cache.ts` and let each caller keep its own topology key, layout builder, and result shaping so observer and editor flows share concurrency behavior without growing an app-wide cache subsystem.
- When `ui/src/features/factory-graph-editor/factory-graph-draft-apply.ts` empties an optional workstation route or resource field, delete the property instead of storing explicit `undefined` so pending factory definitions stay in canonical API shape and equality checks match serialized output.
- When factory-graph editor visibility changes are presentation-only, keep the preset or filter state in `ui/src/features/workflow-activity/hooks/react-flow-current-activity-card-editor-graph.ts` and filter the rendered topology there so switching views never mutates or discards pending draft graph edits.
- When Storybook responsive verifiers target graph-editor toolbar or preset controls, scope Playwright role queries to the owning labeled region and use exact accessible-name matching so node-handle controls with overlapping substrings cannot trip strict-mode locator failures.
- When backend size cleanup touches oversized `pkg/config` test owners such as `config_mapper_test.go` or `runtime_config_test.go`, split them into same-package sibling files by authored-mapping, runtime-load, named-factory, or workstation-kind concern, and keep shared JSON fixture writers plus lookup/assert helpers in dedicated package-local `*_test_helpers_test.go` files instead of duplicating them across the new test owners.
- When backend size cleanup touches `pkg/config` boundary-heavy test owners such as `config_validator_test.go`, `factory_config_mapping_test.go`, or `openapi_factory_test.go`, split semantic-validator, generated-boundary, and public-OpenAPI assertions into same-package sibling files while keeping shared finding, payload, and alias-assertion helpers package-local instead of scattering them across unrelated packages.
- When `MODEL_INVOKE` runtime work adds worker or workstation contract fields, merge those runtime definitions through both `pkg/config/layout.go` and `pkg/config/runtime_config.go`, then prove the fields survive all the way to the provider-facing request in `pkg/workers/agent.go` with both package-level and `pkg/service/loadWorkersFromConfig(...)` coverage.
- Keep production local-model backends behind the service-owned managed runtime seam in `pkg/service/`: resolve cache assets through `modelAssetPuller.ResolveModelCache(...)`, adapt one backend in a focused file such as `pkg/service/omnivoice_local_runtime.go`, and communicate with the backend through the shared subprocess request contract so direct `/models` invocation and factory-level `MODEL_INVOKE` reuse the same load and invoke path.
- Package-level `pkg/service` tests that load local `MODEL_WORKER` definitions only to exercise the managed local-model seam should set `SkipBuiltInRunnerPrerequisiteValidation`, otherwise hosted CI can fail on unrelated built-in runner PATH checks such as `codex` before the local-runtime path under test is even constructed.
- When model-backed execution needs reviewer-usable runtime evidence, keep the canonical request/response event shaping in one service-owned runner wrapper such as `pkg/service/model_execution_events.go`, then let helper seams like `pkg/service/model_resource_limiter.go` and `pkg/service/local_model_runtime.go` contribute wait/load timing into that wrapper through context rather than emitting competing event payloads of their own.
- When `MODEL_INVOKE` authoring grows new slot-binding fields, wire them through authored `api/components/schemas/data-models/Workstation*.yaml`, `pkg/config/openapi_factory.go` boundary normalization, both `pkg/config/factory_config_mapping*.go` directions, and `pkg/workcontent/generated.go` if selectors or config content depend on `WorkContentPart` fields such as `slot`.
- When model-operation work adds typed factory resources, update authored `api/components/schemas/data-models/Resource*.yaml`, root-resource normalization in `pkg/config/openapi_factory.go`, both `pkg/config/factory_config_mapping*.go` directions, and `pkg/interfaces/public_factory_enums.go` together so uppercase resource-type contracts stay aligned across authored JSON, generated artifacts, and runtime config.
- When worker-declared model resources must become real scheduler limits, merge worker `resources` into transition requirements inside `pkg/config/config_mapper.go` instead of treating them as metadata only, and keep process-wide local-model throttling in `pkg/service` keyed by canonical model resource metadata rather than by factory-local resource names.
- When backend size cleanup touches oversized package tests such as `pkg/service/factory_test.go`, split the test owner into same-package sibling `*_test.go` files by concern and keep shared fixtures or test doubles package-local instead of moving them into a separate helper package just to satisfy the size gate.
- When `pkg/workers` tests cross the backend size gate, split agent stop-token/runtime-selection coverage, workstation timeout coverage, and inference-provider error-normalization coverage into sibling same-package `*_test.go` owners, and keep shared fake provider runners, git setup helpers, and fixture loaders in a package-local `inference_provider_test_helpers_test.go` file so command-assembly assertions stay separate from failure-corpus behavior.
- When `pkg/factory/runtime` test cleanup is needed, separate constructor or lifecycle coverage, event-history or replay coverage, and runtime-mode or worker-pool coverage into sibling files such as `factory_constructor_test.go`, `factory_event_history_test.go`, and `factory_modes_test.go`, while keeping shared executors and projection helpers in `factory_runtime_test_helpers_test.go`.
- When `pkg/factory/engine` test cleanup is needed, split the monolithic owner into same-package sibling files for runtime snapshots, tick flow, submit validation, submission hooks, and dispatch tracking, while keeping `mockSubsystem`, generated-net builders, and hook doubles package-local in `engine_test_helpers_test.go`.
- When `pkg/factory/scheduler` test cleanup is needed, split `enablement` coverage into same-package logging or guard owners plus dedicated match-field and runtime-ordering siblings, split `work_queue` coverage into batching and priority siblings, and keep shared topology fixtures or logger helpers in package-local `*_test_helpers_test.go` files.
- When `pkg/factory/subsystems/dispatcher_test.go` crosses the backend size gate, move throttle-pause coverage and cron or determinism coverage into sibling same-package owners such as `dispatcher_throttle_test.go` and `dispatcher_cron_test.go`, while keeping scheduler doubles and shared dispatch fixtures package-local in `dispatcher_test.go`.
- When backend size cleanup only needs to shorten large `pkg/factory/subsystems` or `pkg/replay` test functions before a whole-file split is warranted, move fixture builders and assertion helpers into sibling same-package `*_test.go` owners so the behavior-focused test stays under the function limit without changing the package seam.
- When a same-package backend test split still needs heavy concern-specific helpers, move those helpers with the scenarios they support into the new sibling `*_test.go` owner file instead of leaving a giant catch-all helper file behind in the original package test owner.
- When maintainability cleanup touches session-scoped backend integration tests in packages like `pkg/service` or `pkg/api`, keep the end-to-end route or runtime assertions in the owning package test, but split large files by concern and extract same-package harness helpers for setup, HTTP assertions, and per-session checks instead of introducing a cross-package test utility layer.
- When `pkg/service.FactoryService` is handed to `APIServerStarter` as an `apisurface.APISurface`, prove seam-preservation through the callback-owned `runtime` parameter by exercising submit, subscribe, snapshot, or current-factory behavior there instead of adding reflection or interface-inventory assertions.
- In `pkg/service/factory.go`, start `APIServerStarter` only after `waitForLiveRuntimeStart(...)` succeeds for the default runtime; service-mode API callbacks must not observe a partially initialized runtime or race startup cancellation.
- In `pkg/service/factory.go`, treat top-level `Run(...)` context cancellation during default runtime startup as a graceful shutdown after `stopLiveRuntime(...)` cleanup, but keep `waitForLiveRuntimeStart(...)` cancellation errors intact for narrower request-scoped flows such as session open or replacement runtime activation.
- Keep explicit session-scoped API routing on the shared `apisurface.SessionAPISurface` seam plus `pkg/testutil.MockFactory` session maps; do not introduce handler-local runtime interfaces or one-off route stubs for `/factories/{factory_id}/...` behavior, and keep session-scoped current-factory plus editable-definition routes on that same seam so the UI cannot grow scoped URLs that the generated server or service layer does not actually implement.
- When rebasing or extending typed UI API refactors in `ui/src/api/`, preserve `ui/src/api/session-routing.ts` as the single owner for explicit `/factories/<session>/...` path building, including `~default` normalization for missing or empty session IDs, and keep session-route regression tests on explicit injected `fetch` seams so the documented direct `bun test` workflows remain runner-compatible.
- Keep workspace tab management on the shared `/factory-sessions` API plus `ui/src/api/factory-sessions/`; later dashboard stories should reuse that query-and-dialog seam instead of issuing direct `fetch` calls from header or app-shell components.
- When runtime-state cleanup changes token or place bookkeeping, prove the contract through caller-facing mutation or reconstruction tests that assert owned state surfaces such as `Marking.PlaceTokens` or reconstructed `PlaceOccupancyByID`; do not add helper-topology or source-layout assertions.
- When work-request dependency cleanup touches event history or replay, keep request-level behavior owned by canonical request `Relations` plus emitted `relationship-change` events; retire dead `relation_context` passthrough fields instead of preserving them in runtime records, event payloads, or replay fixtures.
- When generated-batch dependency cleanup needs regression proof, assert the same canonical relation on all three customer-visible surfaces: the recorded request payload `Relations`, the emitted `relationship-change` event, and replayed `RelationsByWorkID` world state.
- Keep provider-session detail ownership in the dedicated `provider-session` dashboard widget; work-item and workstation current-selection cards should keep only the history selection controls plus pressed-state wiring, not a second embedded `ProviderSessionDetailPanel`.
- When the CLI docs command surface changes, update `README.md` in the same pass and keep its command examples aligned with the guard assertions in `pkg/cli/root_test.go`.
- When customer docs mention packaged `infinite-you docs` topics, use the exact embedded topic names from `pkg/cli/docs/docs.go`; the singular topic is `workstation`, while the customer-facing canonical guide is `docs/reference/workstations.md`.
- Keep `docs/reference/batch-inputs.md` as the single maintained customer-facing owner for batch request shape, watched-file placement, and submitted `DEPENDS_ON` or `PARENT_CHILD` semantics; the packaged `you docs batch-work` topic is only the stable quick-reference surface.
- Keep script poller stdout batch-ingress rules in `pkg/service/poller_watcher.go` aligned with `docs/reference/batch-inputs.md`; when the poller submit contract changes, update both the parser and the batch-input guide in the same pass so service-mode pollers do not drift from canonical batch semantics.
- When `pkg/service/poller_watcher_test.go` adds or revises live script-poller scenarios, keep the canonical worker and loaded-runtime-config wiring on the local `newCanonicalScriptPollerWorker(...)` and `newScriptPollerLoadedRuntimeConfigForServiceTest(...)` helpers, and pass explicit workstation names or overrides at the test call site so replacement-order assertions stay readable.
- Keep hosted poller secret and checkpoint runtime behavior centralized in `pkg/service/hosted_linear_poller.go`; `auth.secretRef` currently resolves through the `INFINITE_YOU_SECRET_<REF>` environment override before a runtime-base-dir-relative file lookup, and hosted poller checkpoints persist under `.infinite-you/poller-checkpoints/` so restart replay stays bounded without leaking secret material into logs.
- When hosted Linear pollers use `teamIds` or `stateIds`, push those filters into the GraphQL `issues(...)` query in `pkg/service/hosted_linear_poller.go` instead of relying on client-side filtering alone; bounded checkpoint resume is only correct when the provider page order already reflects the configured candidate set.
- Keep customer-facing poller docs split by canonical owner: `docs/reference/workstations.md` owns `behavior: "POLLER"` lifecycle semantics, `docs/reference/workers.md` owns hosted poller worker fields such as `provider`, `auth.secretRef`, and `linear`, and `docs/reference/batch-inputs.md` owns the script poller stdout submission contract. Workflow pages and packaged CLI quick references should summarize and link back instead of copying those field-level rules.
- Keep provider-error functional harnesses in `pkg/testutil/provider_error_smoke_harness.go` and `pkg/testutil/provider_error_smoke_pause_isolation.go` focused on provider normalization and throttle behavior, not runner-capability rejection; dedicated worktree passthrough coverage owns the `worktree` CLI seam, so provider-error smoke fixtures should not inject workstation `worktree` templates that can short-circuit Codex-specific retry and observability expectations.
- Keep dashboard workstation-kind visuals centralized in `ui/src/features/flowchart/workstation-icon-metadata.ts`, `ui/src/features/flowchart/components/graph-semantic-icon.tsx`, and `ui/src/features/workflow-activity/messages/dashboard-flow-axis-legend.ts`; when a new workstation kind reaches topology or runtime projections, update the shared semantic kind mapping, SVG icon vocabulary, and legend copy together so read-only graph views and editor-adjacent parity fixtures stay aligned.
- When contributor docs mention the repository CI workflow, mirror the exact root-level command sequence and its stated scope from `.github/workflows/ci.yml` so local reproduction and review expectations do not drift.
- When selected-work `Workstation dispatches` falls back to provider-session attempts because request-history rows are missing, keep the customer-visible heading owned by `ui/src/features/current-selection/components/selected-work-dispatch-history.tsx` and prove in `ui/src/features/current-selection/components/work-item-card.test.tsx` that the fallback branch still hides internal `transition_id` values.
- When the repository adds or changes release automation, keep the workflow trigger model and maintainer commands aligned with `docs/internal/development/cli-release-policy.md` so tags, documentation, and GitHub Actions do not describe conflicting release paths.
- Keep maintainer install-surface responsibilities in `docs/internal/development/cli-release-policy.md` aligned with `.goreleaser.yml` and `.github/workflows/release.yml`, including the hosted GitHub release installer asset paths and the tagged archive/checksum contract.
- Keep maintainer release-preparation git checks in `internal/releaseprep/` and call them through `cmd/releaseprep/` from `Makefile` rather than rebuilding the same branch, clean-tree, or tag-existence logic inline in shell recipes.
- Keep `ui-deps` ahead of `typecheck` in the release-preparation readiness order because the documented repository baseline installs dashboard dependencies before running the Bun-backed UI typecheck on a clean checkout.
- Keep the CLI release target matrix, archive naming, checksum contract, and shipped executable filename in `.goreleaser.yml`; release workflow YAML should call that shared packaging file rather than re-declaring per-platform artifact details inline.
- Keep consumer install-surface archive resolution and checksum verification in the repo-owned `scripts/install.sh` and `scripts/install.ps1`; future release automation should host or invoke those scripts rather than reimplementing per-platform asset selection in docs or GitHub workflow steps.
- When a workflow needs published GoReleaser asset filenames, derive them from the semver version string without the leading `v` because `.goreleaser.yml` archive and checksum names use `{{ .Version }}` rather than the raw tag text.
- Keep packaged-binary release smoke behavior in `internal/releasesmoke/` and invoke it through `cmd/releasesmoke/` plus the thin wrappers in `scripts/release/`; candidate and post-publish workflows should share the same runtime verification contract instead of growing platform-specific shell logic.
- Keep hosted installer smoke in `scripts/release/smoke-install.sh` and `scripts/release/smoke-install.ps1`, and run it from its own release-workflow job, so installer failures stay classified separately from packaged release assets and `go install` verification.
- When hosted installer smoke needs repo-local or fake-release coverage, pass installer override environment variables through the repo-owned smoke wrappers instead of forking `scripts/install.sh` or `scripts/install.ps1` or hardcoding GitHub-only URLs into behavioral tests.
- When repo-local release tests need the installer contents or direct execution, read or invoke `scripts/install.sh` and `scripts/install.ps1` from those source-of-truth locations rather than assuming a duplicate installer file exists at the repository root.
- Keep release-oriented `go install` verification in `tests/release/release_smoke_test.go` with a clean temp `GOBIN`; keep the fast repo-owned `./cmd/factory` smoke there for local regression coverage and let the release workflow opt into the published-module `@latest` smoke after tag publication.
- Keep the supported public `go install` command aligned across `go.mod`, `README.md`, `docs/internal/development/cli-release-policy.md`, and `tests/release/release_smoke_test.go`; if the module path changes in one place and not the others, the documented install surface stops being real.
- Keep browser-backed release dashboard assertions in `ui/scripts/release-dashboard-smoke.mjs` and let `internal/releasesmoke/` call that one script rather than duplicating DOM selectors or asset-request checks across Go tests and GitHub workflow steps.
- Keep GitHub Actions Playwright setup on the hosted-runner browser download path (`bunx playwright install chromium`) instead of `--with-deps` unless the workflow truly owns OS package installation; live `apt` dependency resolution adds mirror/DNS flake outside the product behavior under test.
- When release automation needs cross-platform artifact smoke, package the full `dist/` once and fan out verification by downloading that shared artifact on each runner OS instead of rebuilding or re-describing the packaging contract in every matrix job.
- When tag-driven publication must wait for candidate smoke, keep the actual GitHub Release publish step in a follow-on workflow that resolves the same `v*` tag from the verified commit instead of racing a second tag-triggered publish workflow in parallel.
- Keep release-tag semver validation in one repository-owned helper and call it from both `make release` and GitHub Actions so malformed `v*` tags cannot slip into candidate or publish lanes through inconsistent regex snippets.
- Keep consumer install commands in `README.md` aligned with the repo-owned release surfaces: hosted installer commands should point at the GitHub release assets, `go install` should match the supported module path, and manual archive fallback should remain available for constrained environments.
- When UI assets are regenerated for Go embedding, keep the build pipeline responsible for normalizing output filenames and refreshing the generated embed-registration file instead of hand-editing `ui/dist/`.
- When Vite or another UI build step leaves empty directory shells under `ui/dist/`, prune them in the normalization pipeline before refreshing Go embed registration; `//go:embed dist dist/*` rejects empty directories and will break `go test` or `make test`.
- If repository Go builds fail because a legacy `ui/dist_stamp.go` is still present locally, rerun the documented UI normalization flow so it removes the retired file and refreshes the generated embed registration instead of hand-merging stale stamp contents.
- When browser-backed UI replay tests and replay coverage reports share the same scenarios, keep that metadata in one repository-owned catalog so the tests, scripts, and docs cannot silently drift.
- When a UI lane requires browser evidence, record the exact Storybook/browser commands and the dashboard spot-check checklist in `docs/internal/processes/manual-qa.md` so reviewers can reproduce the same local flow instead of relying on PR-only notes.
- When customer-facing concept docs have both canonical pages and combined overview pages, keep field tables, validation rules, runtime details, and provider values on the canonical owner page; overview pages should explain how the concepts cooperate and link to the owner pages for exact contracts.
- Keep workstation-request current-selection browser verification anchored in `ui/src/App.workstation-requests.stories.tsx`; the `WorkstationRequestSelection`, `WorkstationRequestSelectionNoResponse`, `WorkstationRequestSelectionRejected`, and `WorkstationRequestSelectionErrored` stories are the reusable Storybook seams for markdown request or response rendering, collapsed-by-default inference attempt and body disclosures, failure-state readability, and pending-state readability across mobile, tablet, and desktop widths.
- When a Storybook interaction story opens shared Zustand-backed UI state such as dashboard dialogs, close that surface through visible UI interactions and reset the store in a `finally` block before the story exits; browser-backed Storybook runs reuse module state across stories.
- When a resizable dashboard widget needs browser-backed regression proof, exercise the real `.react-resizable-handle-*` bento handles in a Storybook interaction story and assert post-drag geometry from customer-visible card or chart regions instead of relying on jsdom-only size shims.
- When dashboard chart tests need layout-contract assertions, pass feature-owned `data-*` hooks through shared containers such as `ui/src/components/ui/chart.tsx` and assert them from the rendered card or story shell; avoid coupling regression proof to Recharts-generated wrapper structure when a feature can expose a narrower DOM seam.
- When a dashboard chart needs a clickable toolbar action during drag or hover interactions, render that control outside the `ChartContainer` overlay and prove it is not a descendant of the chart `role="img"` surface; the overlay uses pointer-event suppression for chart readability, so interactive controls inside it can become effectively blocked even when jsdom interaction tests still pass.
- When dashboard spacing contracts change, keep regression proof in the focused owners `ui/src/features/dashboard/dashboard-screen.test.tsx` and `ui/src/features/workflow-activity/react-flow-current-activity-card.test.tsx`; assert the rendered shell or card class contract and preserved region-heading semantics there instead of widening into full-app smoke or source-shape checks.
- When dashboard status-card styling changes, keep the border or tone assertions in the smallest feature-owned test such as `ui/src/features/work-totals/work-totals-card.test.tsx`, and prove the user-visible labels plus accessible value labels there instead of relying only on broader app smoke coverage.
- Keep `ui/scripts/run-storybook-ci.mjs` testable through injectable orchestration dependencies so Storybook CI order, readiness handoff, early server-exit failure, and cleanup behavior can be covered without starting a real static server in unit tests.
- Build `ui/storybook-static/` with `bun run build-storybook` before invoking `bun run test-storybook` in a fresh worktree; the repo-owned Storybook CI wrapper serves the prebuilt static bundle and will time out on `index.json` if that artifact has not been generated locally.
- Keep the Storybook smoke-story id in `ui/scripts/run-storybook-ci.mjs` aligned with the actual export-dialog story title in `ui/src/features/export/components/export-factory-dialog.stories.tsx`; if those drift, both `bun run test-storybook` and `bun run storybook:responsive-check` can fail during readiness before any browser assertions run.
- Keep frontend V8 coverage thresholds in `ui/vite.config.ts` scoped to app/runtime source; exclude replay fixture files, CSS, barrel exports, and Node tooling scripts from the denominator while preserving separate Vitest coverage for script behavior.
- When dashboard UI coverage must prove a shared button treatment, prefer Storybook assertions over rendered computed styles against an existing customer-visible dashboard action in `ui/src/App.stories.tsx` instead of adding test-only DOM metadata to shared primitives.
- When dashboard locale-switcher or header-formatting behavior changes, update the focused header coverage in `ui/src/features/header/components/dashboard-header.test.tsx`, the app-shell contract assertions in `ui/src/App.toolbar-locale.test.tsx`, and the browser-backed checks in `ui/src/App.stories.tsx` plus `ui/scripts/verify-import-export-storybook-responsive.mjs` so control ordering, compact tick copy, hidden accessible labels, and cross-surface locale propagation stay aligned.
- When the shared Storybook smoke harness in `ui/src/App.stories.tsx` grows, extract dead or feature-specific fixtures before it crosses the repository line-limit gate; broad dashboard browser coverage belongs there only while the file remains maintainable and lint-clean.
- When Storybook-only dashboard harness controls need local labels outside the product localization path, keep the harness component in `ui/src/stories/` and mark the exact diagnostic-only JSX text with `hardcoded-ui-copy-exception: non-product-diagnostic` so the hardcoded-copy guard does not treat test controls as shippable UI copy.
- When dashboard header copy should stay available to assistive tech but disappear visually, keep the localized text in `ui/src/features/header/messages/header-controls.ts`, hide the rendered label with `sr-only` in the feature owner, and prove both the slider accessible name and the screen-reader-only class through `ui/src/features/header/*` tests plus `ui/scripts/verify-import-export-storybook-responsive.mjs`.
- When dashboard header tick-summary copy changes, update `ui/integration/event-stream-replay.integration.test.mjs` alongside the focused header tests and Storybook verifiers; `make verify-tests` treats the replay browser integration as part of the repository-owned visible tick-status contract and will keep waiting for the rendered summary text, not just the slider value.
- When submit-work success behavior changes in the dashboard shell, update the browser-backed `DashboardSubmitWorkIntegrationSmoke` story in `ui/src/App.stories.tsx` alongside the feature hook or card so post-submit field resets and button-enablement rules stay aligned with the real widget contract.
- When submit-work layout changes in the dashboard shell, keep the browser-backed geometry assertion in `ui/src/features/submit-work/submit-work-card.stories.tsx` aligned with the action row so state-copy changes cannot reintroduce horizontal button drift.
- When dashboard UI work needs reusable controls or states, add or extend primitives under `ui/src/components/ui/` first and keep feature folders focused on composition, data shaping, and product-specific behavior rather than bespoke chrome.
- When current-selection UI copy spans shell, dispatch history, request or attempt detail cards, or state-node empty-state helpers, route it through `ui/src/features/current-selection/current-selection-locale.tsx` plus feature-owned catalogs under `ui/src/features/current-selection/messages/`; shared helpers such as `detail-card-shared.tsx` should accept localized messages from card owners instead of introducing inline fallback strings in rendering components, and selected-work fallback provider-session attempt cards should receive the current locale before reusing workstation-detail attempt messages.
- When current-selection request or response timestamps need customer-visible rendering, keep the parsing and locale-aware local-time formatting in `ui/src/components/ui/formatters.ts` and pass only the localized unavailable fallback from `ui/src/features/current-selection/messages/current-selection-detail.ts`; do not localize timestamps at the API boundary or inline ad hoc `Intl.DateTimeFormat` calls inside detail cards.
- When current-selection state-node cards need visible work-row metadata, keep the customer-facing row layout owned by `ui/src/features/current-selection/state-node-detail.tsx`, derive optional `started_at` display data in `ui/src/features/current-selection/useCurrentSelection.selection-helpers.ts`, and prove both the enriched and omitted timestamp paths in `state-node-detail.test.tsx` plus `App.current-selection.test.tsx`; that keeps list formatting changes out of deeper selected-work detail cards and avoids leaking work IDs, trace IDs, or raw type labels back into the compact state summary.
- When docs cleanup work needs regression coverage, prefer user-visible `you docs <topic>` smoke or command-output assertions in `tests/functional/smoke/cli_docs_smoke_test.go` or `pkg/cli/root_test.go` rather than source-scanning tests that read checked-in markdown files to enforce link topology, retired filenames, or docs inventories.
- When current-selection or adjacent dashboard detail cards need compact label/value metadata rows, reuse `INFERENCE_ATTEMPT_DETAIL_CLASS` from `ui/src/features/current-selection/detail-card-shared.tsx` so the single-line two-column layout stays consistent across summary, response, error, and metadata sections instead of reintroducing stacked ad hoc grids.
- When current-selection inference attempts expose large request or response bodies, keep those payload sections behind their own collapsed-by-default disclosure controls in `ui/src/features/current-selection/inference-attempt.tsx`; only render the toggles when normalized body text exists, and leave pending or unavailable states as direct explanatory copy instead of empty expandable shells.
- When current-selection needs quick failure debugging at the top level, normalize failure reason and message once in `ui/src/features/current-selection/workstation-request-detail-view.ts` and render them inline in the summary outcome row from `ui/src/features/current-selection/workstation-request-detail.tsx`; keep `Error details` as the deeper diagnostic section instead of duplicating failure parsing across summary and detail sections.
- Keep dashboard browser-proof for copy-trimming work in Storybook `play` assertions close to the affected stories, using feature-owned selectors such as `Submit work`, `Trace drill-down`, terminal summary buttons, and the current-selection `Outcome` row instead of source-shape checks or ad hoc test-only markers.
- When dashboard UI needs expandable sections with selection actions, pair `ui/src/components/ui/button.tsx` with `ui/src/components/ui/collapsible.tsx` and keep feature files responsible only for status-specific copy and token overrides rather than reintroducing raw disclosure button markup.
- When migrating dashboard charts away from bespoke SVG renderers, keep feature-owned sample shaping inside the feature module and route the actual rendering through `ui/src/components/ui/chart.tsx` plus Recharts so legend, tooltip, and loading treatment stay on the shared primitive path.
- When a dashboard surface needs a reusable read-only grid, build it on `ui/src/components/ui/table.tsx` or `ui/src/components/ui/data-table.tsx` and keep feature code responsible only for row shaping, captions, and product-specific cell content instead of hand-authoring new table chrome.
- When dashboard dialogs or forms render validation text, wire the affected controls with `aria-invalid` plus `aria-describedby` pointing at the active validation message so shared primitives satisfy the repository accessibility contract instead of relying on visual-only error copy.
- When selected-work dispatch history cards change summary density, keep the visible summary limited to the stable scan fields already owned by `ui/src/features/current-selection/selected-work-dispatch-history-card.tsx` such as workstation, transition, start time, and duration; prove removed noise fields with rendered absence assertions in both default-locale and localized `work-item-card` tests instead of helper-only checks.
- When selected-work `Script attempts` cards gain or move customer-visible fields, route every label, status token, and empty-state string through `ui/src/features/current-selection/messages/current-selection-dispatch-history.ts` and prove at least one expanded non-default-locale script-attempt path in `ui/src/features/current-selection/work-item-card.test.tsx`; section-title-only locale checks are not enough to catch mixed-language regressions inside the expanded diagnostics surface.
- When dashboard copy cleanup removes visible section headings or helper text, preserve the accessibility contract with stable `aria-label` regions and conditional `aria-describedby` links instead of leaving hidden orphan labels or empty hint containers behind.
- When top-of-dashboard density work needs tighter panel chrome, add the shared spacing recipe to `ui/src/features/bento/components/agent-bento.tsx` and opt specific cards such as work totals or factory graph into that `AgentBentoCard` variant instead of layering page-only padding overrides around individual dashboard panels.
- When the default dashboard bento geometry changes, update `ui/src/features/bento/hooks/useDashboardLayout.ts` at both seams: lower the baseline layout and add an exact-match migration for stored legacy positions, then prove the rendered layout signature in `ui/src/App.layout-graph.test.tsx` so returning users do not stay on retired card heights or offsets.
- When GitHub Actions uses `actions/setup-go` against a module that declares a newer `toolchain` than its base `go` version, prefer uncached setup unless you have verified that cache restore does not collide with the auto-downloaded toolchain files in later jobs.
- When a cleanup lane closes with path or contract-alignment work, record the exact root-level verification commands in a `docs/internal/development/*-closeout.md` artifact so the proof survives beyond `progress.txt`.
- When functional tests need `os.Chdir`, route the directory swap through the shared helper and serialize it with a package-level lock; process working directory is global state and will race any `t.Parallel()` coverage otherwise. If the same test changes directories more than once or does so inside subtests, use the scoped helper form so the lock is released between command executions instead of being held until parent-test cleanup.
- Keep `tests/functional_test/` focused on checked-in fixture data under `testdata/`; new observable functional coverage belongs in `tests/functional/...`, and source-layout or docs-topology checks belong in narrower guard surfaces rather than in the functional suite.
- Keep generated replay payload merge regressions in `pkg/replay/event_artifact_test.go` when the behavior under change is deterministic worker or workstation replacement and append ordering; seed `factoryapi.Factory` payloads directly there instead of rebuilding the same coverage through loaded-config filesystem fixtures.
- When `pkg/replay` loaded-config or event-stream hydration tests grow toward the backend size gate, move generated-factory fixture writers and assertion bundles into sibling same-package helper owners such as `effective_config_generated_test.go` instead of expanding the original behavior-focused test file.
- When `pkg/listeners/filewatcher_test.go` grows toward the backend size gate, split same-package watcher coverage into sibling files by watched-input concern such as batch normalization, multi-channel directory behavior, and filename-derivation rules while keeping the shared mock factory and submission polling helpers package-local.
- When helper-heavy runtime files such as `pkg/factory/event_history.go` cross the backend size gate without changing the exported seam, move generated-conversion and projection helpers into sibling same-package owners such as `event_history_generated.go` so the runtime contract stays stable while the maintained file boundary becomes reviewable by concern.
- Keep safe-diagnostics rehydration ownership in `pkg/interfaces/safe_diagnostics.go`; replay, projection, or worker-facing runtime code should call `interfaces.WorkDiagnosticsFromSafeWorkDiagnostics(...)` instead of rebuilding `WorkDiagnostics` from `SafeWorkDiagnostics` package-locally.
- When generated safe-diagnostics `StringMap` values need canonical `map[string]string` copies, route the rehydration through `cloneStringMap` inside `pkg/interfaces/safe_diagnostics.go` and normalize zero-length results back to `nil` where that boundary expects nil-for-empty semantics instead of keeping handwritten map-copy loops beside the conversion call sites.
- Keep worker-facing `interfaces.WorkDiagnostics` deep-clone ownership in `pkg/interfaces`; runtime and replay packages should call the canonical clone helper there instead of maintaining package-local clone trees for nested diagnostics maps, slices, or panic payloads.
- Keep canonical selected-tick consumed-input cloning in `pkg/interfaces/world_state_clones.go`; world-state reducers and adjacent runtime projections should call `interfaces.CloneWorkstationInputs(...)` instead of maintaining package-local workstation-input clone helpers that can drift on nested lineage fields.
- Keep projection-visible safe-diagnostics regressions in `pkg/factory/projections/world_state_test.go`; assert reconstructed `InferenceAttemptsByDispatchID`, `CompletedDispatches`, and `ProviderSessions` from canonical events instead of testing helper topology or package-local conversion details.
- When workstation dispatch diagnostics shift ownership between dispatch summaries and nested attempts, keep the regression split across `pkg/api/workstation_request_projection_test.go`, `tests/functional/runtime_api/api_inference_events_test.go`, `ui/src/App.current-selection.test.tsx`, `ui/src/App.follow-up-submit.test.tsx`, `ui/src/features/current-selection/work-item-card.test.tsx`, and the browser-backed `ui/src/App.stories.tsx` smoke lane; one layer alone will miss projection-only drift, and broader UI or Storybook suites can keep stale dispatch-level expectations even after the focused current-selection lane is green.
- When workstation request details need historically correct consumed payload content, keep `pkg/api/workstation_request_projection.go` as the owner that resolves `request.inputWorkItems` from `FactoryWorldState.PayloadLineage.ResolveConsumedInputSnapshot(...)` and marks unavailable payload states explicitly; do not fall forward to latest `WorkItemsByID` or retained dispatch input refs for payload bodies, because those can drift after downstream work updates.
- When current-selection needs lineage-derived payload bodies in both workstation-request and selected-work dispatch flows, keep the payload-status rendering and copy centralized in `ui/src/features/current-selection/components/work-item-payload-details.tsx` so loading, empty, unavailable, error, and resolved states stay aligned across both cards instead of drifting in parallel implementations.
- When dashboard request or selection surfaces need to tell same-work continuation from newly generated downstream work, keep `pkg/api/workstation_request_projection.go` resolving output refs from `FactoryWorldState.PayloadLineage.ResolveOutputWorkSnapshot(...)` and carry the resulting logical-work, continuity, source-kind, and parent-work metadata on `FactoryWorldWorkItemRef` instead of inferring create-vs-continue solely from token mutation shape or latest work-item state.
- When current-selection disclosure copy or formatter keys change, update `ui/src/features/current-selection/messages/current-selection-message-catalogs.coverage.test.ts` in the same pass and keep app-level or component regressions opening the exact attempt and nested request/response disclosures they assert against; hidden payload or provider-session content should not be treated as always-visible in `ui/src/App.follow-up-submit.test.tsx`, `ui/src/App.current-selection.test.tsx`, `ui/src/features/current-selection/work-item-card.test.tsx`, or provider-session selection widget tests.
- When current-selection or provider-session-detail disclosure copy or formatter keys change, update the owning message-catalog coverage test in the same pass, using `ui/src/features/current-selection/messages/current-selection-message-catalogs.coverage.test.ts` for selection-owned copy and `ui/src/features/provider-session-detail/messages/provider-session-message-catalogs.coverage.test.ts` for the extracted session-detail card. Keep app-level or component regressions opening the exact attempt and nested request/response disclosures they assert against; hidden payload or provider-session content should not be treated as always-visible in `ui/src/App.follow-up-submit.test.tsx`, `ui/src/App.current-selection.test.tsx`, `ui/src/features/current-selection/work-item-card.test.tsx`, or provider-session selection widget tests.
- Terminal-work selection regressions should be proved at `ui/src/App.current-selection.test.tsx`: assert that clicking a terminal row keeps the Current Selection panel on the `work-item` detail path, leaves request-oriented headings absent, and updates the terminal-row `data-selected` state to match the selected work item.
- Keep current-selection naming aligned with explicit user intent: `selectWorkByID(...)` and `resolveWorkItemSelectionByWorkID(...)` own `clicking work selects work`, while `selectWorkstationRequest(...)` stays reserved for explicit request-oriented affordances. Prove both paths together in `ui/src/App.current-selection.test.tsx` so future cleanup does not reintroduce request-preference fallbacks behind work clicks.
- Workflow-activity work clicks should stay on the explicit work-ID intent path: wire `ui/src/features/bento/components/dashboard-bento.tsx` and `ui/src/features/workflow-activity/components/*` through `onSelectWorkID` into `currentSelection.selectWorkByID`, pass dispatch/node hints for active graph rows so duplicate work IDs resolve to the clicked execution, keep workstation or request affordances on their own explicit callbacks, and prove the behavior in `ui/src/features/workflow-activity/components/react-flow-current-activity-card.test.tsx` plus the broader current-selection smoke in `ui/src/App.current-selection.test.tsx`.
- Factory graph editor toolbar changes should stay covered at both the shared control seam and the embedded card seam: keep button-label, tooltip, pressed-state, and keyboard assertions in `ui/src/features/factory-graph-editor/components/factory-graph-editor-controls.test.tsx`, then mirror the customer-visible toolbar presence and disabled-state expectations in `ui/src/features/workflow-activity/components/react-flow-current-activity-card.coverage.test.tsx` and `ui/src/features/workflow-activity/components/react-flow-current-activity-card.test.tsx` so compact editor-control changes do not drift from the in-card editor experience.
- When factory graph editor node presentation needs observer parity, share badge and title chrome from `ui/src/features/flowchart/current-activity-node-chrome.tsx` and keep the focused render proof in `ui/src/features/factory-graph-editor/components/factory-graph-editor-flow.test.tsx` so draft overlays and editor-specific handles layer onto the observer-style base instead of forking a second node presentation system.
- When factory graph editor route-anchor work changes workstation outcome edges, derive saved-edge `sourceHandle` and `targetHandle` from `ui/src/features/factory-graph-editor/factory-graph-editor-connections.ts` inside `ui/src/features/factory-graph-editor/components/factory-graph-editor-flow.tsx` so rendered routes and connect-mode draft gestures both use the same semantic anchor registry.
- When factory graph observer/editor parity changes, keep regression proof split across the focused editor-flow model tests in `ui/src/features/factory-graph-editor/components/factory-graph-editor-flow.test.tsx`, the draft-connection controller lane in `ui/src/features/workflow-activity/react-flow-current-activity-card-editor-controllers.test.tsx`, and the responsive Storybook verifier in `ui/scripts/verify-import-export-storybook-responsive.mjs` so saved anchors, draft gestures, and narrow/desktop usability do not drift independently.
- When a functional test exercises `WithRunAsync()` through `pkg/testutil/service_harness.go`, seed any required work before `RunInBackground()` and poll for the observable runtime state you need; fixed sleeps after starting an empty async loop can race a clean engine shutdown in CI.
- When `pkg/testutil/service_harness.go` or `pkg/testutil/replay_harness.go` waits for `WaitToComplete()`, first poll `GetEngineStateSnapshot(...)` until the runtime is actually available; `FactoryService.WaitToComplete()` returns a closed placeholder channel before startup, so completion waits that skip the readiness gate can cancel the run before the runtime ever comes up.
- When post-run assertions in `pkg/testutil/service_harness.go` need the terminal marking, capture or reuse the latest successful `GetEngineStateSnapshot(...)` result instead of assuming the live runtime will still be queryable after `WaitToComplete()` closes; teardown timing can make direct end-of-run snapshot reads flaky in CI.
- Keep user-facing CLI command examples in `README.md` aligned with `pkg/cli/root.go` and `pkg/cli/root_test.go`; the root command centralizes the installed executable token as `you`, and the tests guard rendered help examples directly rather than inferring them from packaged docs internals.
- When live-run recording defaults or replay-artifact handling change, update the customer run guide in `docs/reference/authoring-factories.md` together with the maintainer contract in `docs/internal/development/record-replay.md` so users and maintainers keep one shared artifact model, path contract, and control-surface explanation.
- When a functional test covers `you docs`, run the command from a temp working directory without a local docs tree and assert user-visible headings, stable content markers, and installed-executable command examples rather than byte-for-byte parity with packaged markdown files; the embedded reference source lives under `pkg/cli/docs/reference/`.
- For current-factory functional coverage, keep the observable test in the owning `tests/functional/...` behavior package and reuse the authored `service_simple` root fixture plus the generated Go client helpers there instead of restoring `tests/functional_test` as a behavior-owner.
- When `make lint` fails in `cmd/deadcodecheck`, remove orphaned `_test.go` helper files and unused test wrappers before touching `docs/internal/development/deadcode-baseline.txt`; the baseline is for accepted remaining debt, not abandoned local helpers.
- Use the split `pkg/api/openapi_contract_*_test.go` and `pkg/api/generated_contract_*_test.go` owners for narrow schema, generated-contract, and event-vocabulary guards when the work is about authored OpenAPI structure, bundled route/schema alignment, or generated round-trip behavior rather than handler runtime behavior.
- When `pkg/factory/event_history_generated.go` needs generated API `Work.Content` payloads from canonical `[]interfaces.WorkContentPart`, route that reverse translation through `pkg/workcontent.GeneratedPtrFromParts(...)` instead of rebuilding work-content union assembly in `pkg/factory`.
- Keep `GET /work` list responses work-shaped rather than token-shaped: `ListWorkResponse.results` should use generated `Work` objects with structured `Work.state`, while token-only fields such as token ID, place ID, timestamps, and visit history stay on token-specific detail/status surfaces.
- Keep `GET /work` explicit sort fields narrow and contract-owned: add public sort options to `api/openapi-main.yaml`, regenerate API artifacts, validate unsupported `sortBy` values in `pkg/api/handlers.go`, and route CLI flags such as `--sort-by` to the exact API query value.
- Keep `GET /work` pagination cursor-based with `maxResults`, `nextToken`, and an always-present `paginationContext`; omit only `paginationContext.nextToken` when the current page is final or trailing-empty so CLI JSON automation can distinguish page metadata from continuation. When the public list has semantic ordering, resume by locating the exact token-ID cursor in that ordered list rather than comparing token IDs lexically.
- Keep `pkg/cli/work/list.go` as the owner of `work list` presentation: default output is a human-readable tabular view with work identity and state columns, while `--json` should continue to encode the generated `ListWorkResponse` directly for automation.
- Keep the reserved default-runtime current-factory identifier documented in `api/components/schemas/data-models/FactoryName.yaml` and `/factory/~current`; regenerate bundled artifacts after authored OpenAPI edits instead of hand-patching `api/openapi.yaml` or generated clients.
- Keep Go OpenAPI client generation in `api/codegen_config/client.yaml` and `pkg/generatedclient/`; current-factory and named-factory functional helpers should call that generated client surface instead of maintaining handwritten HTTP request wrappers for those routes.
- When a generated Go client exists only to drive a bounded functional-test flow, constrain `api/codegen_config/client.yaml` with `include-operation-ids` for the exercised routes so the checked-in client does not duplicate unrelated contract models or require new handwritten-source guard exceptions.
- Keep named-factory activation semantics in `pkg/service/factory.go`; API handlers and higher-level tests should call that service seam so default-runtime fallback, durable-pointer writes, and runtime swaps stay under one owner.
- When multi-session runtime behavior changes, extend `pkg/service/runtime_sessions.go` for live-session ownership and let `pkg/service/factory.go` keep serving the `~default` compatibility surface for unscoped callers; avoid introducing second session registries in handlers, listeners, or tests.
- When dashboard tab switching needs to retarget runtime reads or writes, keep the selected session in `ui/src/features/dashboard/state/dashboardSessionStore.ts` and build all dashboard API paths through `ui/src/api/session-routing.ts`; header tabs should only update that shared store, while stream or submit-work callers keep their own feature logic, read the selected session, and rely on the helper to normalize blank selections to `~default` instead of hand-concatenating `/factories/{id}` paths.
- When dashboard current-factory or editable-definition reads need to follow the active tab, keep the session-aware transport in `ui/src/api/named-factory/api.ts` and `ui/src/api/current-factory-definition/api.ts`, then key React Query state by session in `ui/src/features/current-factory-definition/useCurrentEditableFactoryDefinition.ts`; non-default tabs must not reuse the legacy `/factory/~current...` aliases or a cross-session cache entry.
- When current-factory reads need live runtime fallback data from `pkg/service/factory.go`, snapshot `runtimeCfg` through `currentRuntimeConfig()` before comparing directories or serializing it; `swapActiveRuntime` mutates that state under `runtimeMu`.
- Keep writable named-factory name validation centralized in `pkg/apisurface/`; create or import paths must reject the reserved `UNDEFINED` current-factory identifier there so handlers, services, and generated-client functional tests enforce the same public contract.
- Keep `docs/internal/development/root-factory-artifact-contract-inventory.md` and `internal/testpath/artifact_contract.go` synchronized; the doc is not descriptive-only, it is a checked-in contract surface with order-sensitive tests.
- When public OpenAPI field names change, update `pkg/api/testdata/canonical-event-vocabulary-stream.json` together with the contract guards so fixture validation keeps exercising the current bundled vocabulary.
- When public `Factory` fields are renamed or retired, sweep the event payload fragments under `api/components/schemas/events/payloads/` as well as `api/components/schemas/data-models/Factory.yaml`; bundled replay/event schemas can silently keep stale top-level factory fields even after the primary `Factory` schema is cleaned up.
- Treat checked-in replay artifacts under `factory/logs/` as canonical serialized contract fixtures. After public factory-contract or replay-artifact writer changes, regenerate the paired `.replay.json` recording from the checked-in event stream before trusting replay smoke coverage.
- Keep `make artifact-contract-closeout` pointed at the migrated behavior-owned package tests that still prove the root artifact contract; do not reintroduce `tests/functional_test` as a Go package dependency once that directory is fixture-only.
- Treat packaged-binary release fixtures under `tests/release/testdata/` as canonical authored factory surfaces. If the public factory contract gains a new required field like top-level `factory.name`, update those release fixtures with the same explicit authored value instead of relying on init-time defaults or test-only backfills.
- When `/factory` contract shapes change, update `api/openapi-main.yaml`, run `make generate-api`, and then reconcile the handwritten wrappers in `pkg/api/handlers.go`, `pkg/apisurface/contract.go`, `pkg/service/factory.go`, and `ui/src/api/named-factory/api.ts`; those seams still own request decoding and current-factory adapter behavior after generation.
- When a new runtime discovery route such as `/models` is added, treat `api/openapi-main.yaml`, generated artifacts, `pkg/apisurface/contract.go`, the owning read-model logic in `pkg/service/`, and the paired CLI transport under `pkg/cli/` as one contract seam; generation adds the route types, but the handwritten handler, runtime adapter, and CLI read path still need explicit updates together.
- When a `/models/{model_name}/...` mutation route is added, reuse the canonical `WorkContent` plus workstation-binding schemas instead of inventing a parallel payload shape, and keep the generated invocation request or response types aligned across request validation in `pkg/api/handlers.go`, direct worker selection or execution in `pkg/service/`, and the matching `pkg/cli/models` transport path.
- Keep local model execution behind one service-owned manager in `pkg/service/`: resolve cache paths through `modelAssetPuller.ResolveModelCache(...)`, key loaded handles by canonical MODEL resource metadata (`model|backend|loadPolicy`), and wrap the shared `workers.Runner` path so direct `/models/.../invocations` and factory-level `MODEL_INVOKE` executions reuse the same local load/invoke seam.
- When `BuildFactoryService(...)` or runtime replacement in `pkg/service/factory.go` creates local-model managers, copy `modelAssets`, `modelResources`, and `localModels` onto the live `FactoryService` as well as the runtime bundle; direct `/models/.../invocations` consult those service-owned fields outside the factory-runner startup path.
- When managed local-model pull work changes `pkg/service/model_pull.go`, persist enough cache metadata at pull time and make `ResolveModelCache(...)` prefer disk-backed metadata before remote manifest fetches so already-pulled local models still load and invoke offline.
- Keep real OMNIVOICE long coverage on the documented `make long-tests` lane: package-level local-model manager coverage should stay in `pkg/service`, the real-runtime API smoke should stay under `tests/functional/runtime_api` behind `//go:build functionallong`, and the opt-in environment contract (`INFINITE_YOU_RUN_OMNIVOICE_LONG_TESTS`, optional command override, optional cache-dir override) should be updated together with that lane so maintainers can reproduce local inference before CI automation expands it.
- On GitHub-hosted macOS long-local-inference runners, force `GGML_BACKEND=CPU` in the workflow environment instead of auto-selecting Metal; the hosted Apple-paravirtual Metal device can abort inside `omnivoice-tts` before the real inference lane completes even when the same cached assets and command succeed on Linux and Windows.
- On GitHub-hosted Windows long-local-inference runners, pin the real OMNIVOICE lane to `windows-2022` instead of `windows-latest` while the redirected Windows 2025/VS2026 image is raising `0xc000001d` from `omnivoice-tts.exe`; treat that runner label as part of the mergeability contract for the real-runtime workflow until the upstream runtime image is proven stable again.
- Keep GitHub Actions long local-inference automation in `.github/workflows/long-local-inference.yml` and the paired `scripts/ci/install-omnivoice-command.{sh,ps1}` helpers: hosted runners should restore `.cache/managed-models`, reuse a cached or preinstalled real `omnivoice-tts` backend when available, install it from per-platform repository variables when configured, and otherwise build the pinned `ServeurpersoCom/omnivoice.cpp` source plus the repo-owned `cmd/omnivoice-llamacpp` adapter so every matrix job still runs `make long-tests` against real model execution. On Windows, treat sibling runtime DLLs as part of the cached backend contract by copying them beside `omnivoice-tts.exe` and rebuilding or re-copying when a restored cache only contains the executable. Keep that adapter on the same subprocess contract and maintainer lane instead of inventing a CI-only test command, fake audio backend, or a second model-cache policy.
- Treat `api/openapi.yaml` as a bundled artifact, not an authored source file: schema changes belong in `api/openapi-main.yaml` plus the referenced `api/components/schemas/**` files, and work-content or worker-contract additions must update those component files before `make generate-api` or `make api-smoke` can stay stable.
- When several typed UI API modules repeat the same `Response` body parsing or API error-field extraction, centralize only that transport concern in a small helper under `ui/src/api/`; keep endpoint-specific request flow, success-payload validation, error-code normalization, and user-facing copy in the owning API modules instead of hiding them in a generic fetch client.
- Treat split `workers/*/AGENTS.md` and `workstations/*/AGENTS.md` files as part of the public authored contract. When worker or workstation enum vocabularies change, update the split-layout expansion writer in `pkg/config/layout.go`, the init scaffolds under `pkg/cli/init/`, and the checked-in docs/examples/fixtures together so generated split files do not drift back to legacy values.
- When public factory-config field names change, sweep maintained customer and fixture docs together with code and examples. Entry-point docs such as `docs/reference/config.md`, `docs/reference/workstations.md`, `docs/reference/workers.md`, `docs/reference/authoring-factories.md`, and fixture READMEs can otherwise keep teaching retired contract names after the generated/API surfaces are already canonical.
- When customer-facing concept docs are consolidated, keep `docs/README.md`, `docs/reference/README.md`, and the canonical owner page aligned in the same change so readers can identify one source of truth from either entry point.
- When a customer-facing guide stops owning a field-level contract, replace duplicated tables or validation lists with a scope note and links to the canonical owner; keep workflow pages focused on sequencing, examples, and command order.
- Keep `docs/reference/authoring-agents-md.md` focused on split file shape, prompt bodies, prompt files, and prompt-authoring examples; detailed worker fields, workstation fields, routing, provider values, and timeout behavior belong in the canonical worker and workstation guides.
- Treat top-level `factory.name` as a required authored-contract field across every entry path: update generated-boundary mappers, init scaffolds, named-factory payload helpers, replay artifacts, and test fixture writers together so config loading, regeneration, and replay all preserve the same factory identity.
- Keep maintained test payloads and fixture writers explicit about canonical `factory.name`. If a regression lane authors `factory.json` or generated `Factory` payloads, write the top-level `name` in the fixture itself instead of using helper shims that backfill it from `id`, directory names, or defaults.
- When a root-level `Factory` contract field is added, update the authored schema fragment under `api/components/schemas/data-models/`, the `api/openapi-main.yaml` component ref, generated Go/UI artifacts via `make generate-api`, and the handwritten UI canonical decoder in `ui/src/api/factory-definition/api.ts` together; generation alone does not update the handwritten browser boundary.
- When the public guard contract changes, keep the shared schema family and both authored attachment points aligned together: update `api/components/schemas/data-models/Guard*.yaml`, the `guards` references under both `Workstation` and `WorkstationIO`, the shared guard enum normalizers in `pkg/config/public_factory_enums.go`, and the handwritten UI decoder in `ui/src/api/factory-definition/api.ts` before regenerating artifacts.
- When a public workstation contract key changes, update both authored factory surfaces together: top-level `factory.json` parsing in `pkg/config/openapi_factory.go` and split-workstation `AGENTS.md` parsing in `pkg/config/agents_config.go`, then sweep replay artifacts, init scaffolds, and dashboard projection consumers that deserialize generated `Workstation` payloads.
- Keep split authored worker exports aligned across `pkg/config/layout.go`, `pkg/config/runtime_config.go`, and `pkg/config/agents_config.go`: expanded `factory.json` now owns worker runtime metadata while `workers/<name>/AGENTS.md` is body-only, so load-path regressions must prove those three seams still merge one canonical worker runtime definition.
- Keep split authored workstation exports aligned across `pkg/config/layout.go`, `pkg/config/runtime_config.go`, and `pkg/config/agents_config.go`: expanded `factory.json` now owns workstation runtime metadata and prompt-file references while `workstations/<name>/AGENTS.md` is body-only, so authored-layout regressions must prove those three seams still merge one canonical workstation runtime definition.
- Treat metadata-only split worker and workstation entries in expanded `factory.json` as incomplete without their paired body-only `AGENTS.md` files. `pkg/config/runtime_config.go` should fail load with explicit entity context instead of silently normalizing those entries to empty bodies.
- Keep customer-path import/export regressions in `tests/functional/bootstrap_portability/api_export_import_e2e_smoke_test.go` observable at both seams: the API smoke should prove `GET /factory/~current` and `POST /factory` round-trip the canonical contract, and it should inspect the persisted named-factory directory to ensure imported `factory.json` stays thin while worker and workstation bodies remain body-only in split `AGENTS.md` files.
- When `factoryapi.SubmitWorkRequest` contract fields change, sweep the direct unary submit helpers under `tests/functional/bootstrap_portability/` as well as the shared runtime API helpers; those bootstrap portability tests author `SubmitWorkRequest` payloads locally and can retain stale pointer or optional-field assumptions after generated contract updates.
- When the exported workstation prompt field changes, keep `pkg/config/factory_config_mapping.go`, `pkg/replay/generated_factory_runtime.go`, and the generated-schema runtime smoke coverage aligned: the public `Factory` payload may serialize prompt content under `Workstation.body`, while replay and other runtime rebuild paths still need `PromptTemplate` restored from that canonical export field.
- Keep the PNG import/export boundary aligned with the canonical factory contract: PNG metadata should serialize the direct `Factory` fields plus `schemaVersion`, and the `/factory` API should receive the same `Factory` object without a wrapper or PNG-only fields.
- Keep UI preview and drop state on the same canonical PNG contract object. Import/export helpers may carry `schemaVersion` alongside the `Factory`, but should not duplicate wrapper-era fields like `factoryName`, nested `factory`, or parallel envelope objects in local state.
- Keep the handwritten UI factory-definition decoder and PNG importer strict to the generated contract. Retired factory-config aliases belong in historical fixtures only; the active browser boundary should reject them instead of normalizing them.
- In `ui/src/api/factory-definition/api.ts`, keep root `factory.guards`, workstation `guards`, and input `guards` on separate enum allowlists even when they share generated TypeScript unions; the browser decoder must reject unsupported guard types at each attachment point instead of reusing one broad set.
- Treat timeline replay inputs under `ui/src/state/factoryTimelineStore.ts`, `ui/src/state/factoryTimelineStore.test.ts`, and maintained `ui/integration/fixtures/*.jsonl` files as canonical `Factory` event surfaces. Initial-structure factory payloads in those paths must use `workTypes`, `workType`, `behavior`, `modelProvider`, and `executorProvider`; legacy aliases only degrade the projected topology and should be kept out of maintained fixtures.
- When a live canonical event stream must close on subscriber cancellation, keep `pkg/factory/event_history.go` responsible for bridging producer fanout onto a subscriber-owned output channel; do not send directly to a channel that a cancellation path may close concurrently.
- When dispatch event identity ownership changes, update `api/components/schemas/events/FactoryEventContext.yaml`, the dispatch payload schemas, `pkg/factory/event_history.go`, and `pkg/api/testdata/canonical-event-vocabulary-stream.json` together so emitted events, generated contracts, and bundled vocabulary fixtures do not drift.
- When replay or projection code reconstructs dispatch chaining lineage, read `FactoryEvent.context.currentChainingTraceId` and `FactoryEvent.context.previousChainingTraceIds` first, then fall back to deprecated payload copies only for legacy events or fixtures.
- For end-to-end dispatch fan-in lineage changes, extend `tests/functional_test/factory_request_batch_test.go` so `TestFactoryRequestBatch_ChainingTraceFanIn_EndToEndSmoke` asserts the emitted dispatch event context, reconstructed world state, and workstation-request projection together instead of splitting the contract proof across helper-only tests.
- When dashboard timeline reconstruction or request-history projections rebuild dispatch chaining lineage, keep `ui/src/state/factoryTimelineStore.ts` aligned with the backend contract by reading `FactoryEvent.context.currentChainingTraceId` and `FactoryEvent.context.previousChainingTraceIds` first and treating dispatch-payload copies as legacy fallback only.
- Keep replay `WorldCompletion -> DashboardTraceDispatch` ownership in `ui/src/features/timeline/state/timeline/replayCompletion.ts`; replay support helpers should import that projection owner instead of restating the trace-dispatch field map in parallel modules.
- When an API projection keeps single-caller nil-check fallback helpers that only feed one boundary assembly function, inline that fallback logic at the owning projection seam and keep regression proof in package-local tests that assert the generated request or response fields rather than helper presence.
- When public request-batch field names change, update `pkg/interfaces/factory_runtime.go`, `pkg/factory/work_request_json.go`, and watched-file/worker batch fixtures together; those handwritten JSON boundaries are not generated and will drift silently if only `api/openapi.yaml` and generated clients are regenerated.
- Keep batch relation normalization regressions in `pkg/factory/work_request_test.go` at the `NormalizeWorkRequest(...)` boundary; assert exact duplicate-relation errors or normalized relation outputs there instead of testing helper ownership or file structure.
- When `pkg/factory` event-history or work-request test owners cross the backend size gate, split them into same-package sibling files by concern such as `event_history_script_test.go`, `event_history_lineage_test.go`, `work_request_json_test.go`, and `work_request_submit_test.go`, while keeping shared normalization or assertion helpers package-local to the `factory` package.
- When `tests/functional/guards_batch/factory_request_batch_long_test.go` crosses the backend size gate, move chaining-trace fan-in and three-workstation lineage coverage into a sibling same-package owner such as `factory_request_batch_chaining_trace_test.go`, keep generated-batch executors and shared event helpers package-local, and shorten large harness builders by extracting reusable fixture config or generated-batch builders instead of changing the observable flow.
- When `tests/functional` owners cross the backend size gate, keep the observable scenario in the behavior-owned package and prefer same-package sibling `*_test.go` files or package-local helpers for fixture scaffolding, dispatch-history assertions, and command-runner request checks instead of moving those seams into shared production packages.
- When helper-heavy `tests/functional/runtime_api` regressions such as `api_inference_events_test.go` or `api_runtime_config_alignment_smoke_test.go` cross the backend size gate, split them into same-package sibling `*_support_test.go` or concern-specific files so thin-event, dashboard-projection, and runtime-config fixture helpers stay package-local while each observable smoke owner remains under the limit.
- When `tests/functional/runtime_api` smoke owners cross the backend size gate, keep the end-to-end assertion in the original same-package test file and extract setup, phase polling, and dashboard or event-log assertion bundles into package-local helpers so the observable runtime path stays reviewable without changing the functional server seam.
- Keep shared functional runtime polling helpers such as `waitForGeneratedWorkAtPlace(...)` fail-fast on timeout with the final `/work` snapshot instead of returning an ambiguous last response; long-running real-runtime lanes should then set explicit, platform-appropriate wait budgets at the call site so Windows or other slower hosted runners surface actionable timeout diagnostics rather than secondary lookup failures.
- Keep shared public worker and workstation enum aliases in `pkg/interfaces/public_factory_enums.go`; generated/OpenAPI boundary normalizers should call the strict helpers while generated/public round-trip helpers should call the permissive ones instead of cloning alias tables in `pkg/config` or UI export code.
- Keep generated public enum pointer wrappers in `pkg/interfaces` on `generatedPublicFactoryEnumPtr`; typed helpers such as `GeneratedPublicWorkstationKindPtr` should coerce into that shared nil-on-empty seam instead of re-implementing trim-and-pointer logic in a second package-local path.
- In `pkg/cli/dashboard`, derive completed and failed session work labels from `PlaceOccupancyWorkItemsByPlaceID` plus `PlaceCategoriesByID` first, and fall back to `Session.DispatchHistory` only when category-owned labels are absent so CLI summaries and failed-work details keep one compatibility path.
- In `pkg/config/factory_config_mapping.go`, keep mirrored generated-boundary retirement guards data-driven: define one retired-field inventory per boundary type and reuse it for both the top-level object and any nested `definition` object so only the caller-owned path text differs, but cap traversal at the explicitly supported generated-boundary levels instead of recursing through arbitrary nested `definition` chains.
- `POST /work` is the single-submit surface and should reject relation graphs explicitly; named relation wiring belongs on `PUT /work-requests/{requestId}`, where the handler can resolve `sourceWorkName` and `targetWorkName` safely.
- `decodeSubmitWorkRequestBody` is the handwritten validation seam for `POST /work`; keep unary required-name validation there so missing, empty, or whitespace-only `name` values fail with `BAD_REQUEST` before `WorkRequestFromSubmitRequests(...)` can synthesize fallback display names, and continue using the raw JSON field map whenever generated structs need compatibility checks beyond Go's default `json.Unmarshal` behavior. Retired alias and trace-conflict compatibility checks should stay owned by `pkg/factory.ValidateCanonicalWorkRequestJSON(...)`, including top-level unary aliases such as `target_state`, with `pkg/api/handlers.go` translating those shared failures into the stable API message text.
- Public work-content part validation belongs in the handwritten submit decoders before generated models are mapped into domain structs: keep `decodeSubmitWorkRequestBody`, `decodeWorkRequestBody`, and their raw `content[...]` field-path checks aligned so multimodal contract errors stay stable even when generated `oneOf` bindings change.
- Keep public work-content regression coverage in `pkg/api/server_submit_work_test.go` and `pkg/api/server_upsert_work_request_test.go`; assert caller-visible bad-request field paths plus zero-submit side effects for malformed content, and assert accepted canonical text/image content preserves type, order, and values through the API boundary instead of testing helper placement.
- For Redocly-bundled vendor extensions that must keep a schema pointer in `api/openapi.yaml`, store the extension value as a string JSON Pointer; nested `$ref` objects under `x-*` fields are inlined during bundling.
- When dashboard or world-view code needs runtime-only observability that is not reconstructed from canonical events, keep the event reducer unchanged and overlay the live engine snapshot fields at the projection boundary that already owns both inputs.
- Keep customer-visible dashboard failed summaries aligned across `pkg/factory/projections/simple_dashboard_projection.go`, `pkg/cli/dashboardrender/simple_dashboard.go`, and `ui/src/state/factoryTimelineStore.ts`; `failed_count` must count failed work items, matching the population used by `failed_by_work_type`.
- Active throttle-pause projection must match model-worker lanes against `FactoryWorker.ModelProvider` before executor `Provider`; event-derived topology can carry public uppercase model-provider enums while runtime-defined topology still carries internal executor/provider identifiers.
- Keep CLI dashboard dispatch-history fallback collection in `pkg/cli/dashboard/dashboard.go` behind one lane-owned helper that selects completed-versus-failed semantics centrally; future cleanup in this area should extend that shared seam instead of reintroducing parallel completed and failed wrapper owners.
- When `pkg/api` or `pkg/cli/dashboard` hits backend size limits, prefer same-package sibling file splits such as `handlers_*.go` or `dashboard_*.go` that separate entrypoints, decode/validation helpers, projections, and formatting while preserving the exported boundary and tests.
- When `pkg/api/server_test.go` or another API route-test monolith hits the backend size gate, split same-package route coverage into sibling `server_*_test.go` files by endpoint concern and keep shared HTTP/test-double helpers in `server_test_helpers_test.go` instead of leaving one catch-all API test owner.
- When `pkg/api/openapi_contract_test.go`, `pkg/api/generated_contract_test.go`, or similar API contract monoliths hit the backend size gate, split them into same-package `openapi_contract_*_test.go` and `generated_contract_*_test.go` siblings by concern, and keep shared schema/event helper seams in dedicated package-local helper test files instead of preserving one oversized contract owner.
- In `pkg/factory/runtime/dispatch_result_hook.go`, treat `CompletionDeliveryPlanner` presence as the only synchronous-dispatch mode switch; do not cache a second runtime boolean that can drift from `h.planner`.
- When local dashboard embed wiring drifts or a legacy `ui/dist_stamp.go` lingers in a working tree, rerun `bun ui/scripts/normalize-dist-output.mjs` or `make ui-build` so the retired stamp file is removed and the generated embed registration is refreshed automatically.
- In `pkg/config/portable_bundled_files.go`, keep lexical bundled target-path containment in `portableBundledTargetPath(...)` and reserve `validatePortableBundledFilesystemPath(...)` for filesystem-link escape checks along the already-resolved target segments.
- Canonical portable export and runtime recollection are intentionally asymmetric: `pkg/config/layout.go` must collect supported `factory/scripts/...` and `factory/docs/...` bundled-file entries with inline content for self-contained export payloads, while `pkg/config/runtime_config.go` must rehydrate those same supported entries back into thin disk-backed references instead of re-inlining them after materialization.
- For portable bundled-file escape regressions, seed an existing symlink or junction at the expanded destination path in `pkg/config/portable_bundled_files_test.go` or `pkg/config/portable_bundled_files_integration_test.go` and assert the observable error plus no-write outcome instead of testing helper ownership directly.
- For portable bundled-file validation regressions that need the standalone portable-config load path, keep the proof in `pkg/config/portable_bundled_files_integration_test.go` via `ExpandFactoryConfigLayout(...)` or `LoadRuntimeConfig(...)` and assert the user-visible target-path error plus the absence of materialized bundled files.
- For dispatcher pause-gating tests, use scheduler doubles that derive decisions from the `enabled` slice they receive so the assertions reflect the real FIFO/work-queue scheduler contract instead of depending on impossible post-selection decisions.
- When runtime guard or observability work needs a reusable reducer over normalized history, keep the pure derivation seam under `pkg/factory/internal/` and adapt dispatcher- or snapshot-specific inputs at the caller boundary instead of teaching the reducer about mutable subsystem state.
- When dispatcher throttle or guard work depends on failure timing, derive event time from `snapshot.DispatchHistory` and `interfaces.CompletedDispatch.EndTime`; `snapshot.Results` is a tick-local outcome surface and should not become the source of truth for pause timestamps.
- In `pkg/factory/subsystems/subsystem_dispatcher.go`, keep throttle policy guard-owned: dispatch history by itself must not synthesize provider/model pauses or pre-filter enabled transitions when no authored `INFERENCE_THROTTLE_GUARD` is present.
- When a copied fixture test needs to move from a retired runtime seam onto the supported authored config path, mutate `factory.json` through `pkg/testutil.UpdateFactoryJSON` or `pkg/testutil.AppendFactoryInferenceThrottleGuard` instead of adding new test-only factory options; that keeps functional coverage on the same root-guard contract customers author.
- When extracting pure runtime reducers from stateful dispatcher logic, keep the reducer tests focused on semantic lane derivation and add dispatcher-level regression coverage for legacy stateful details such as preserving the original `PausedAt` when overlapping failures extend a live window.
- When subsystem behavior is derived from engine snapshots instead of local mutable caches, write multi-tick tests so later snapshots carry forward the owning runtime fields such as `DispatchHistory` and prior `ActiveThrottlePauses`; direct subsystem tests should mirror the engine snapshot contract rather than relying on hidden cross-tick state.
- When a root guard depends on runtime-loaded worker metadata from `AGENTS.md`, lower it after `state.NormalizeTransitionTopology(...)` and evaluate it through `EnablementEvaluator.FindEnabledTransitionsWithSnapshot(...)` so authored policy can use normalized transition worker names plus dispatch-history context without baking runtime state into topology structs.
- When a subsystem feeds runtime-only `EngineStateSnapshot` values into scheduler or enablement helpers, reattach the authoritative net via the lane-owned snapshot-enrichment seam first; `RuntimeState.Snapshot()` intentionally omits topology, but runtime guards that reason across sibling transitions still need the full normalized transition-to-worker map.
- When a subsystem owns snapshot-fed observability state in `TickResult`, return the observed flag even when the new slice is empty so the engine can clear previously published runtime snapshot state instead of leaving stale values in place.
- For `pkg/petri` runtime-guard unit tests that need authored lane matching, prefer `pkg/testutil/runtimefixtures.RuntimeDefinitionLookupFixture` plus explicit `RuntimeGuardContext.TransitionWorkers` maps over broader dispatcher harnesses; that keeps provider/model history matching testable as pure guard logic.
- When dispatcher or config tests cover authored inference throttle guards, seed both the transition `WorkerType` and a runtime worker lookup fixture; authored throttle ownership now resolves only through runtime worker metadata and should not be reconstructed from transition IDs.
- Keep dispatcher-authored throttle observability regressions at the `Dispatcher.Execute(...)` boundary and assert `TickResult.ActiveThrottlePauses`, `ThrottlePausesObserved`, scheduler input, or dispatch output there; reserve direct `pkg/petri` guard tests for pure lane-matching logic.
- In `pkg/config/config_mapper.go`, lower factory-authored inference throttle guards only onto worker-backed transitions whose declared `FactoryConfig.Workers` entry matches the authored provider/model lane; config-mapper tests should prove matching worker transitions get the guard while logical or non-matching worker lanes do not.
- Keep planner-local dispatch/result-hook regressions in `pkg/factory/runtime/dispatch_result_hook_test.go` when the contract under change is limited to synchronous hook execution, planner-absent async worker-pool submission, missing-runner failures, delivery gating, or replay-tick validation; reserve `pkg/factory/runtime/factory_constructor_test.go`, `factory_event_history_test.go`, and `factory_modes_test.go` for broader factory lifecycle, event-history, replay, and runtime-mode behavior.
- In `pkg/config/runtime_config.go`, keep map-backed runtime worker and workstation lookups owned by one package-private helper and have canonical or staging config types forward to that owner instead of carrying duplicate `Worker(...)` and `Workstation(...)` method bodies.
- In `pkg/config/runtime_config.go`, `LoadRuntimeConfig(...)` only loads split `workers/*/AGENTS.md` or `workstations/*/AGENTS.md` definitions when the authored factory config has not already supplied those runtime fields inline, so runtime-config regression tests should choose fixtures that explicitly target either the inline or split path.
- When a caller already selected one concrete factory directory, load it through `pkg/config.LoadRuntimeConfigFromFactoryDir(...)` instead of `LoadRuntimeConfig(...)` so workspace roots do not silently follow `current-factory.txt` and reopen the wrong target during multi-session flows.
- When `make lint` fails on `cmd/deadcodecheck`, remove truly stale symbols first and only then refresh `docs/internal/development/deadcode-baseline.txt` for the remaining accepted library or test-helper debt in the same review.
- `cmd/deadcodecheck` runs `deadcode -test ./...` without optional build tags, so helpers that are only called from `functionallong` or other tagged suites need a focused default-build test contract before pruning them from the accepted baseline.
- When bootstrap or portability helpers are only live behind `//go:build functionallong`, move those helpers into clearly named `*_functionallong*_test.go` owners before updating `docs/internal/development/deadcode-baseline.txt`; deadcode runs in the default lane, so default-build helper files should only keep helpers with real untagged callers.
- When a `tests/functional/smoke` helper is intentionally shared between the default lane and the `functionallong` lane, keep one default-build behavioral smoke that exercises the shared scaffold directly and let the long lane carry any slower lifecycle sweep; compile-only coverage is not enough to protect cross-lane drift.
- When `tests/stress` scenarios hit the backend size gate, keep the observable stress owner in place and extract same-package setup, submission, and marking-assertion helpers into a sibling `*_helpers_test.go` file so concurrency and poison-token behavior stay reviewable without changing the harness seam.
- Keep worker event payload exit-code policy in `pkg/workers/event_exit_code.go`; script and inference helpers may differ in how they find command diagnostics, but the final zero-vs-nonzero emission rule should stay centralized in one package-private owner.
- Keep provider-specific CLI argument assembly and command-request shaping in `pkg/workers/provider_behavior.go`; `pkg/workers/inference_provider.go` should assemble execution around that seam rather than growing duplicate forwarding helpers.
- Keep provider CLI build-args regression tests adjacent to `pkg/workers/provider_behavior.go`; `inference_provider` tests should stay focused on command assembly and execution behavior instead of duplicating provider flag assertions through wrapper-oriented seams.
- Keep inference attempt observability in `pkg/workers/recording_provider.go`: retryable provider failures should preserve the incrementing attempt counter across retries, while terminal failures or successes must clear it so the next dispatch-visible request sequence restarts at attempt 1.
- When `inference_provider` tests need to verify provider command payloads, derive the expected args and command request from `providerBehaviorFor(...).BuildArgs(...)` and `BuildCommandRequest(...)` so the regression fails if orchestration drifts away from the canonical provider-behavior owner.
- When CLI dashboard fallback behavior changes, keep the proof in `pkg/cli/dashboard/dashboard_test.go` observable by asserting both `dashboardSessionViewFromRenderData` labels and the rendered `FormatSimpleDashboardWithRenderData` session rows instead of testing helper shape or source structure.
- When `pkg/cli/root_test.go` or `pkg/cli/run/run_test.go` hits the backend size gate, split coverage into same-package sibling `root_*_test.go` or `run_*_test.go` files by command concern and keep shared harness seams package-local instead of preserving one CLI test monolith.
- When tests exercise runtime paths that resolve the home directory through `os.UserHomeDir()`, set both `HOME` and the Windows home variables (`USERPROFILE`, plus `HOMEDRIVE`/`HOMEPATH` when needed) so record-path and runtime-log coverage stays portable across the required Windows lanes.
- When a shared typed UI API helper refactor is behavior-preserving, prove it through the owning API suites with observable request and error assertions such as empty-body handling, raw-text fallback, unknown-code normalization, invalid-success rejection, and structured target preservation instead of meta-tests that only inspect helper topology or imports.
- When factory-config work adds a new workstation or worker contract field, update the authored schema under `api/components/schemas/data-models/`, normalize it at the generated JSON boundary in `pkg/config/openapi_factory.go`, map it in both directions through `pkg/config/factory_config_mapping.go` and `pkg/config/factory_config_mapping_internal.go`, and cover it in both `pkg/config/openapi_factory_test.go` and the bundled OpenAPI contract tests under `pkg/api/`.
