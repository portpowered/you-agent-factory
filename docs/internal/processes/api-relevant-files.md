# API Relevant Files

Use this map when changing the public REST contract.

- Factory Session HTTP decoding, generated-contract mapping, service
  invocation, error mapping, and JSON/NDJSON/SSE streaming live in
  `pkg/services/factory_sessions/transports/http`. The top-level
  `pkg/transports/http` server only composes that injected adapter with other
  service-owned handlers and registers the generated routes; runtime binding
  occurs in `pkg/transports/http/application`.
- Recordings HTTP decoding, generated-contract mapping, Recordings root
  invocation, error mapping, and streaming policy live in
  `pkg/services/recordings/transports/http`. The adapter consumes the accepted
  `recordings.Service` root only; fake-root tests inject a focused root fake
  without constructing ledger, lifecycle, replay, or artifact-export graphs.
  Package-boundary tests must prove the adapter does not import
  `pkg/services/recordings/internal/**`.
- Factory Session CLI request construction, rendering, diagnostics, and
  operation handlers live under
  `pkg/services/factory_sessions/transports/cli`; Factory Session MCP tool
  decoding, invocation, result/error mapping, and catalog parity live under
  `pkg/services/factory_sessions/transports/mcp`. Top-level CLI retains
  command-tree, flag, and reusable protocol composition; top-level MCP retains
  generated discovery, SDK registration, and stdio composition. Service-owned
  adapters consume Factory Sessions root contracts and do not import or
  construct its implementation packages or private subservices.
- Shared filesystem documents represented in OpenAPI should decode and encode
  through the generated model in a focused `pkg/transports/mapping` package,
  then map into domain-owned values. Inject that codec into the service
  filesystem loader from `pkg/wire` so services retain IO and normalization
  ownership without importing generated HTTP contracts; global-config identity
  persistence follows this pattern in
  `pkg/transports/mapping/globalconfig` and `pkg/services/operator_settings`.
- `make api-smoke` regenerates twice and checks generated files against the Git
  index. During an intentional contract change, stage the regenerated
  `api/openapi.yaml`, Go server/client, and TypeScript outputs before running the
  smoke target so the drift check detects nondeterminism rather than the
  intended authored change.
- Factory output key normalization in
  `pkg/transports/mapping/factoryconfig/openapi_factory.go` must preserve keys
  inside opaque maps. Add new map-valued contract fields such as localized
  `NameValue.values` or structured invocation-example `args` to
  `preservesObjectKeys`; otherwise canonicalization can rewrite
  customer-authored locale tags or argument names.
- OpenAPI maps whose `additionalProperties` use `oneOf` generate typed Go union
  wrappers. Convert those wrappers explicitly at transport mapping boundaries;
  keep the internal representation limited to the allowed variants so JSON and
  YAML round trips do not silently widen or mutate values.

- OpenAPI 3.0 exclusive numeric bounds use `minimum`/`maximum` together with
  boolean `exclusiveMinimum`/`exclusiveMaximum`. The contract projection in
  `internal/contractopenapiconverter` must translate those pairs to numeric
  Draft 2020-12 exclusive bounds so packaged Factory schemas preserve the same
  acceptance behavior.
- The canonical `@you-agent-factory/factory-emulator` package keeps authored
  scenario structure, generated schema exports, runtime preflight, and rule
  semantics together under `ui/packages/factory-emulator`.
  Keep semantic diagnostics as stable JSON-Pointer records, validate semantic
  references before any emulator activity, and exercise authored rule ordering,
  lineage, exhaustion, and unmatched behavior through the package's public API.
  Deterministic lifecycle start/reset lives in `src/session.js`, while pure,
  domain-separated runtime identity derivation lives in `src/identity.js`.
  Pure logical scheduler selection and batch calculation live in
  `src/scheduler.js`, and normalized virtual-clock arithmetic lives in
  `src/virtual-time.js`; keep sink IO and committed-state publication in the
  session boundary so scheduler parity can evolve without owning lifecycle.
  Startup must revalidate before sink activity, normalize event time from the
  scenario's UTC `startAt`, and keep reset event-free so reruns reproduce event
  bytes and snapshots from the same immutable inputs.

- Public TypeScript consumer contracts live in `ui/packages/client`: generated
  OpenAPI namespaces stay in `src/generated/openapi.ts`, while stable aliases
  stay handwritten in `src/contracts.ts`. Recording shape validation consumes a
  byte-identical generated copy of the standalone API schema, while handwritten
  cross-event validation stays in `src/recording-parser.js` with declarations in
  `recording-parser.d.ts`. Compare validated RFC 3339 event times across the full
  schema domain with civil-date and offset arithmetic plus an explicit leap-second
  phase; JavaScript `Date` parsing does not support leap seconds and loses
  precision beyond milliseconds. Use the package-local `generate`, `verify`,
  pack-inventory, and installed-consumer commands; `make
  ui-public-package-release` composes those checks across the package family.
- Canonical backend `Work`, `WorkRequest`, ordered content, relations, dispatch
  identity, state-change, and payload-lineage contracts live in `pkg/work`.
  Generated OpenAPI conversion remains at transport boundaries such as
  `pkg/transports/mapping/workcontent` and `pkg/transports/http`; domain-owned
  parsing stays under `pkg/work/content/contract`, and the Work owner must not
  import generated clients or transport adapters.
- Work-list filtering, ordering, and validation policy belongs in `pkg/work/query`; `pkg/api/handlers_work_read.go` maps generated `ListWorkBySessionIdParams` and `Work` values into that domain projection, while `pkg/cli/work/list.go` maps normalized query options into URL parameters. Keep generated OpenAPI types and URL encoding at those transport boundaries, and prove policy with pure query tests plus route-level API and CLI request tests.
- Functional-scenario public component projection lives in `internal/functionalscenarios` with the maintenance entrypoint in `cmd/functionalscenarioproject`. Treat `contracts/cli/commands.json`, `contracts/mcp/tools.json`, and bundled `api/openapi.yaml` as its canonical inputs; derive SSE components from OpenAPI operations whose response media types include `text/event-stream` so the SSE inventory cannot drift into a handwritten route list.
- Test-only durable Factory Session cross-interface captures live in `tests/functional/sessionparity`. Keep REST event observations as the actual `text/event-stream` body (`data:` frames containing Factory Event JSON), CLI event observations as the direct `--json` array, and live MCP observations as complete JSON-RPC `tools/call` responses whose serialized `ToolResponse` is in `result.content[0].text`; do not substitute pre-extracted typed results for the live MCP boundary.
- Request-batch functional lifecycle coverage belongs in `tests/functional/runtime_api/api_request_batch_boundary_smoke_test.go`: use generated REST writes/reads and the Factory Event SSE stream, with `support.StartFunctionalAPIServiceModeServer` as the approved test seam. `make functional-boundary-check` accepts only repository `tests/functional/*_test.go` sources, scans the customer scenario, and rejects direct Factory runtime, projection, service, or Petri imports; owner-level package tests remain the place for those internals.
- Functional API replay setup belongs in `tests/functional/internal/support`: pass replay artifact and execution-root inputs through `FunctionalAPIServerConfig` so scenario files can observe only the public Factory Session event stream and terminal API payloads, rather than capturing `FactoryService`, engine snapshots, or reconstructed projections.
- `WaitForTerminalStatus` in `tests/functional/internal/support/http_observation.go` must require IDLE/FINISHED `runtimeStatus` plus a stable category window long enough to survive repeater CONTINUE handoffs under coverage-shard load; a short Initial=0/Processing=0 gap while Work is consumed but not yet marked in-flight must not count as completion (Ralph init smoke flakes under Backend Functional Coverage).
- Functional work-location observations belong on public Work listings through `support.CountWorkAtCustomerState` / `support.HasWorkAtCustomerState` (`workType:state` keys such as `task:complete`). Do not add support helpers that read `FactorySession.Runtime.Petri` markings for place occupancy; resource availability stays on `session.Runtime.Usage.Resources` or `GET /status` resource usage.
- Shared functional support must not return `workers.Token` (or other private engine snapshots). Use `support.FirstInputWorkID` / `FirstInputPayload` / `FirstInputTags` for dispatch input fields. Public HTTP upsert helpers such as `support.UpsertDefaultSessionWorkRequest` accept generated `factoryapi.WorkRequest`, not `work.WorkRequest`.
- Prove the support construction and observation boundary with focused behavioral tests in `tests/functional/internal/support` (`public_process_observation_test.go`): build through `support.BuildProcess` / `RunFactoryToCompletionWithEdges*`, replace only external effects via `edges.Edges`, assert Work/session progress helpers, and keep Petri/token exposure regressions covered by Work-listing vs contradictory Petri fixtures plus public field-type extractors—not source-layout inventory scans.
- Replay-contract functional scenarios must create recordings through the supported HTTP work-request route and observe replay through CLI or Factory Session SSE. When the scenario claims CLI process behavior, compile and invoke `cmd/factory`; a direct `pkg/transports/cli/run.Run` call omits the production composition root's dependency injection. Keep replay execution, service harnesses, projections, and runtime calls out of functional assertions; owner-package tests remain responsible for detailed replay and provider diagnostics.
- The reviewed customer-boundary coverage projection is checked in at `contracts/functional-scenarios.json`, with exact test-to-component bindings in `contracts/functional-scenario-evidence.json`. Generate the projection with `go run ./cmd/functionalscenarioproject -manifest -output contracts/functional-scenarios.json`; only `path::TestName` evidence at the record's own CLI, REST, MCP, or SSE boundary qualifies for `covered`, while gaps and non-applicable grouping or stream policy require explicit reviewed text. Evidence paths must resolve under repository-owned `tests/functional/` or `tests/release/` roots, name an existing top-level Go test, and bind the cited test to the scenario stable ID in the registry; internal package tests, stale files or symbols, same-boundary substitutions, and registry entries whose exact cited test does not directly own the matching declaration fail the check. Qualifying tests call `tests/internal/functionalevidence.Covers` directly and exactly once with literal stable IDs only after their customer-boundary assertions pass, and retained evidence must execute in its assigned required lane, so behavioral execution verifies the same stable IDs consumed by the drift gate. `go run ./cmd/functionalscenarioproject -check contracts/functional-scenarios.json` is the read-only drift gate composed into `make contracts-check`; it reprojects all canonical inputs and rejects unmapped, duplicate, no-longer-canonical, non-canonical metadata, invalid or mismatched evidence, and incorrect SSE policy records.

## Package staging artifacts

- `internal/contractopenapiconverter` owns the restricted OpenAPI 3.0.3 → Draft
  2020-12 conversion profile for Factory schema staging. The reviewed mapping
  lives in
  `docs/internal/contract/openapi-to-draft-2020-12-converter-profile.md`;
  `ConvertCoreSchema` covers the story-001 primitive/object/array/enum/required
  surface, `ConvertRefsSchema` covers story-002 internal component references and
  deterministic `$defs` materialization, `ConvertCompositionNullableSchema` covers
  story-003 composition and nullable forms, `ConvertFailClosedSchema` covers
  story-004 fail-closed ambiguous and unsupported rejection, with story-005
  expanding `internal/contractstaging` integration through
  `ConvertFailClosedSchema` and the checked-in
  `docs/internal/contract/factory-schema-b16-gaps.json` record when the canonical
  Factory graph is not yet fully endorsable. Golden fixtures and byte-stability
  tests live beside the converter package; reuse
  `contractjoiner.MarshalCanonicalJSON` for canonical output bytes. Prove the
  converter stays outside API/config/service/CLI/worker/runtime dependency paths in
  `contracts/converter_boundary_test.go`.
- Packaged Factory schema projections live beside the first-party definitions at
  `packages/packaged-factories/schemas/factory.schema.{json,yaml}`. Build both
  through `internal/contractstaging`: the JSON bytes stay identical to
  `packages/api/generated/schemas/factory.schema.json`, and YAML must be
  serialized from that same parsed schema value rather than maintained as a
  second contract. Keep both paths in the generation/check allowlist and the
  packaged Go and npm source surfaces. Regenerate through
  `make contracts-generate` and verify through `make contracts-check`;
  `make contracts-smoke` proves repeated byte stability and is the packaged
  contract gate run by the development-package CI workflow.
- Flattened packaged Factory artifact generation lives in
  `internal/packagedfactorycatalog`. Keep the authored `@you/*` identity as
  catalog metadata and use the directory slug for the schema-valid portable
  Factory `name`. Validate JSON and YAML from the same canonical Factory value.
  Plan the manifest, generated notice, and every artifact pair in memory twice
  before writing; normalized output collisions or byte/set differences between
  passes fail generation. `make packaged-factory-catalog-generate` stages the
  complete plan and replaces `packages/packaged-factories/generated` as one
  directory so failed planning or staging cannot leave mixed generations.
  `make packaged-factory-catalog-check` uses the same complete projection to
  compare exact checked-in paths and bytes without writing; this one comparison
  protects authored-inventory coverage, paired Factory equivalence, schema
  validation, and manifest locator/hash integrity.
  Exact `${parameter}` placeholders on `workers[].modelProvider` are part of
  the accepted Factory contract alongside concrete provider enum values. Keep
  the OpenAPI one-of placeholder grammar aligned with canonical mapping, and
  validate emitted catalog JSON and decoded YAML directly without substituting
  a validation-only provider.
- `internal/contractstaging` owns joined generation, the reviewed raw
  source-to-package projection map, standalone JSON Schema projections from
  canonical bundled OpenAPI component graphs, and the package contract
  manifest with artifact hashes. Factory schema staging calls
  `ConvertFailClosedSchema` first; when the canonical Factory graph converts
  without blocking diagnostics, staging emits
  `contracts/config/factory.schema.json` and copies the same bytes to
  `packages/api/generated/schemas/factory.schema.json` through
  `marshalFactorySchemaDocument` only. When the B16 gap record status is
  `converter_endorsed`, any converter diagnostic fails staging closed and does
  not use the legacy ref-materialization fallback. When the record status is still
  `blocks_full_endorsement`, staging may fall back only for diagnostics explicitly
  covered by `blockingCategories` or `approvedResidualExclusions` in
  `docs/internal/contract/factory-schema-b16-gaps.json`. When removing OpenAPI-only keywords or generator hints from authored
  Factory fragments for converter endorsement, keep stable Go enum/type names via
  `api/codegen_config/factory-codegen-overlay.yaml` referenced from the oapi-codegen
  configs rather than leaving `x-enum-varnames` / `x-go-type` in authored schema
  bodies. When a Factory property needs both an internal component `$ref` and a
  `description`, wrap the ref in `allOf: [{ $ref: ... }]` and keep
  `description` as a sibling of `allOf` so the refs profile sees `$ref`-only
  objects inside the composition array. Authored configuration schemas live under
  `contracts/config/`; do not substitute topology or parity inventories for
  those schemas. `make contracts-check` reports stale, missing, and unexpected
  staged paths across the complete projection.
- Standalone Factory Event and Factory Recording generation lives in
  `internal/contractstaging/factory_recording_schema.go`. It removes OpenAPI-only
  annotations before fail-closed conversion, restores the event discriminator
  with local `$defs` references, and projects the discriminator mapping into an
  event-level `oneOf` so event kinds that share a payload shape remain valid.
  Keep schema compilation, valid/malformed event and recording behavior, complete
  discriminator coverage, and repeated byte stability in
  `factory_recording_schema_test.go`; the UI client package generator and
  `client-generated-check` also generate/check contract staging.
- Raw JSON/YAML projections in `policy.go#rawArtifacts` are byte-identical copies
  from their canonical owners (CLI/MCP baselines, `contracts/config/` schemas,
  authored JavaScript runtime catalog). OpenAPI-derived standalone schemas are
  explicit generated projections rather than raw copies; Factory config
  generation writes the converter-backed authored root at
  `contracts/config/factory.schema.json` and
  the staged package projection at
  `packages/api/generated/schemas/factory.schema.json` with identical bytes.
  Prove repository parity in `internal/contractstaging/raw_artifacts_test.go`;
  prove repeated authored/staged Factory schema digest stability in
  `internal/contractstaging/factory_schema_test.go`
  (`TestFactorySchemaDigestsStableAcrossRepeatedArtifactsCalls`,
  `TestFactorySchemaGenerationLeavesAuthoredAndStagedDigestsStableOnSecondRun`);
  later-phase export-map families such as `components/*` stay omitted until a
  truthful owner exists.
- Configuration-family closeout lives in `internal/configcontractsmoke` with
  the read-only `cmd/configcontractsmoke` entrypoint and
  `make config-contract-smoke`. Keep its registry, production-loader/schema
  acceptance matrix, manifest export/hash validation, and deterministic
  `contractstaging.Check` composition together; run the runtime dependency
  boundary in `contracts/converter_boundary_test.go` from the same target so
  the converter remains approved build-time tooling without a second staging
  allowlist or generator.
- Authored JavaScript runtime symbol manifests live at
  `contracts/javascript/runtime-manifest.schema.json` with the authored product
  catalog at `contracts/javascript/runtime-api.json`, fixtures under
  `contracts/testdata/javascript/`, and registration in
  `internal/contractvalidator/javascript_registry.go` (`JavaScriptRegistry`,
  merged into `DefaultRegistry()`). Post-schema semantics for path/parent/member
  integrity, callable signatures, closed serializable values (including
  `sharedSchemas` entries keyed by `javascript.schema.*`), and supported
  surfaces live in `internal/contractvalidator/javascript_manifest.go` and
  `javascript_surface.go`. Authored-catalog path completeness against the
  identity baseline and installed binding descriptor lives in
  `pkg/orchestrators/javascript/runtime/catalog/catalog_identity.go` and is
  wired through `internal/contractvalidator/javascript_catalog_identity.go`.
  Representative call-behavior parity for `workflow.final`,
  `workflow.checkpoint`, `agent.run`, `parallel`, and `pipeline` lives in
  `pkg/orchestrators/javascript/runtime/catalog/catalog_call_behavior_parity.go`
  and is wired through
  `internal/contractvalidator/javascript_catalog_call_behavior_parity.go`.
  Focused runtime execution parity for the same representative symbols remains
  in `pkg/orchestrators/javascript/runtime/callbehavior_inventory_test.go`.
  Prove runtime manifest independence behaviorally in `contracts/runtime_manifest_boundary_test.go` by
  executing representative invocation, emitted-record, policy-denial, and resume behavior from a
  subprocess whose authored and staged manifests are unusable. Catalog boundaries remain in
  `contracts/javascript_runtime_api_test.go`; staged
  `packages/api/generated/javascript/runtime-api.json` is a byte-identical copy
  of the authored catalog at `contracts/javascript/runtime-api.json` through
  `internal/contractstaging/policy.go#rawArtifacts`. Maintainer-facing
  catalog/projection/binding/behavior parity closeout lives in
  `internal/javascriptcontractsmoke` with CLI entrypoint
  `cmd/javascriptcontractsmoke` and `make javascript-contract-smoke`; the same
  target runs focused installed-runtime regressions for signature, async result,
  emitted records, policy rejection, and resume, plus the unusable-manifest behavioral
  proof. Canonical forbidden host-global and comparison-project-helper classification
  lives in `pkg/orchestrators/javascript/runtime/symbolidentity`; route both schema
  validation and smoke/catalog checks through `symbolidentity.ClassifySurface` so the
  supported-surface policy cannot drift. Reuse
  `symbolidentity.ProjectInstalledBindings`, `callbehavior.ProjectInstalledCallBehavior`,
  `catalog.CatalogPathCompletenessIssues`, `catalog.CatalogForbiddenSymbolIssues`, and
  `catalog.CatalogCallBehaviorParityIssues`
  rather than inventing a second runtime descriptor. Prove missing/extra/duplicate
  symbol-path failures and forbidden/comparison-project helper failures with
  temp-dir catalog copies that keep authored and staged bytes aligned in
  `internal/javascriptcontractsmoke/check_test.go` and
  `cmd/javascriptcontractsmoke/main_test.go`. Stale staged-projection diagnostics
  report the generated repository-relative file path and, when valid JSON differs
  inside symbol records, the affected public symbol paths; the check remains
  read-only and directs maintainers to `make contracts-generate` followed by
  `make contracts-check`. Behavior-incomplete fixtures should delete or contradict
  call metadata in synchronized authored/staged temp catalogs, then assert the
  public symbol path, `/symbols/<key>/<field>` location, and call-behavior baseline
  remediation for signature, async/result, emitted-record, policy, and resume
  fields; this proves the smoke boundary without making the catalog executable.
- Staged OpenAPI byte policy lives in `internal/contractstaging/openapi.go`
  (`CanonicalOpenAPIPath`, `StagedOpenAPIPath`, `ReviewedOpenAPIBytePolicy`,
  `ProjectStagedOpenAPI`, `VerifyStagedOpenAPIParity`). The reviewed policy is
  byte-identical copy only: `packages/api/generated/openapi/openapi.yaml` must
  match `api/openapi.yaml` exactly. Prove repository parity and reject divergent
  staged bytes in `internal/contractstaging/openapi_test.go`; prove
  `make generate-api` and Go/UI generator inputs still reference
  `api/openapi.yaml` rather than the staged package path in the same test file.
- Factory layout node presentation metadata is authored under
  `api/components/schemas/data-models/FactoryLayoutNode.yaml` and its referenced
  fragments, then must cross the generated boundary, `pkg/config` internal/API
  mappers, and `CloneFactoryConfig`. Validate strict embedded-raster data before
  generated `format: byte` decoding at every authored Factory entrypoint,
  including `LoadAuthoredFactoryAPIFromPath` for CLI validate-only and invocation
  inspection paths, and carry node empty-state image bytes in the same
  Factory-wide budget as annotations. Canonical-node membership remains a
  `pkg/factory/validation` concern, where an empty state on an unknown node is a
  blocking layout target rather than a runtime/topology mutation.
- Development manifest generation lives in `internal/contractstaging/manifest.go`.
  The emitted `packages/api/generated/manifest.json` validates against the
  joined manifest contract and records SHA-256 digests for every staged artifact
  except itself. It intentionally omits commit-derived provenance so generation
  and drift checks are stable across rebases and shallow CI checkouts. Prove
  digest coverage, stability, source-change propagation, and rejected tampered
  hashes in `internal/contractstaging/manifest_test.go`; prove accepted/rejected
  manifest schema fixtures in `contracts/manifest_schema_test.go`. Release
  candidate staging remains responsible for adding and validating `sourceCommit`
  against the immutable candidate input.
- Regeneration cleanliness for owned staging outputs lives in
  `internal/contractstaging/generate.go` (`Generate`) with repository evidence in
  `internal/contractstaging/generate_test.go` and command-level reproduction in
  `cmd/contractsgenerate/main_test.go`. Repeated generation from unchanged
  inputs must leave `Check()` clean, keep owned staging digests stable, and not
  mutate authored OpenAPI or generated Go/TypeScript clients; `make
  contracts-smoke` runs the same generate/check loop twice at the Makefile
  layer.
- `packages/api/package.json`, `README.md`, and `LICENSE.md` are consumer-authored
  publication metadata only. Exports and `files` must map to `AllowedArtifacts()`
  paths under `packages/api/generated/` plus docs/license packaging; omit
  later-phase families such as `./components/*` until a truthful owner exists.
  `scripts/api-package-{pack,consumer,contract}.mjs` prove `npm pack` inventory
  and isolated export resolution without mutating staged artifact bytes.
  Development candidate identity and staging are shared through
  `scripts/package-release-candidate.mjs`: derive every package version from a
  stable base, immutable run ID, and fixed 12-character commit prefix; bind the
  staged public manifest to the full candidate source commit outside the source
  package; and retain exactly one tarball plus attributable digest/inventory
  evidence. `scripts/api-package-candidate.mjs` preserves the API-specific
  entrypoint and reviewed inventory gate, while
  `scripts/packaged-factories-package-candidate.mjs` supplies the Packaged
  Factories package identity to the same release-candidate core. Candidates
  prepared for one release must receive the same run ID and full source commit.
  Immutable registry reconciliation lives in
  `scripts/api-package-registry.mjs`: verify the preserved local tarball first,
  treat an absent exact version as a publish decision, digest-check an existing
  registry tarball for idempotent success, and fail authentication, permission,
  dependency, or immutable-conflict outcomes without publishing or changing a
  dist-tag. Pull-request development-package validation lives in
  `.github/workflows/development-package.yml`: keep prerequisite contract,
  generation/drift, API parity, package-boundary, repository-test, allowlist,
  pack, and isolated-consumer gates in its read-only `validate` job. The
  dependent dry-run job must pass the reviewed head SHA and one preserved
  candidate directly through `scripts/api-package-pr-dry-run.mjs`; it must not
  receive registry mutation credentials or OIDC permission.
  `scripts/package-development-policy.mjs` owns the shared pure event,
  prerequisite, and exact-head policy. Every candidate/publication job invokes
  `scripts/api-package-development-command.mjs`, which enforces that policy and
  calls the real mode-specific dry-run, preparation, or publication boundary;
  inject dependencies into that same production command for behavioral coverage
  without treating workflow source text as runtime evidence. Protected-main
  publication prepares and uploads its candidate in a read-only job, then the
  separately environment-protected `id-token: write` job downloads that exact
  artifact and calls `scripts/api-package-publish.mjs`. That boundary reconciles
  before mutation, publishes an absent version at most once with public access,
  provenance, and the `dev` tag, digest-verifies the registry result, and installs
  the exact registry version in a clean external consumer. Run `make
  api-package-pack-smoke` after contract, candidate, registry, dry-run, or
  development-package workflow guard changes and in
  `verify-build-contracts`/CI.

## REST operation identity inventory

- Authored Factory `modelProvider` fields reference the open
  `api/components/schemas/data-models/ProviderIdentity.yaml` syntax contract.
  `WorkerModelProvider.yaml` is a generated-client convenience enum for built-in
  constants only and must not be used to close authored worker or guard fields.
  Keep the `ProviderIdentity` pattern aligned with
  `provider/inferencecontract.ValidateIdentity`, preserve exact invocation
  placeholders through the worker field's one-of, and regenerate bundled Go and
  TypeScript contracts with `make generate-api`. Then run `npm run generate`
  from `ui/packages/client` to synchronize its package-local generated copy
  before dashboard typecheck.

- `internal/contractinventory` owns the pure read-only extractor that turns bundled
  OpenAPI YAML into canonical `rest-operations/v1` JSON (`formatVersion`,
  `operations[]` with `operationId`, `method`, `path`, optional `xDocId`,
  `hasSummary`, `hasDescription`, `requestMediaTypes`, and `responses[]`).
- `cmd/restoperationinventory` writes that inventory from `api/openapi.yaml` for
  baseline generation; the checked-in baseline lives at
  `contracts/testdata/baseline/rest-operations.json`.
- `internal/contractinventory/baseline_test.go` proves byte-identical repeated
  extraction from `api/openapi.yaml` without enforcing a route inventory.
- Story 004 preservation is proven behaviorally via focused extractor tests and
  `make api-smoke`; this inventory lane must not modify authored OpenAPI,
  generated clients, or `pkg/transports/http` handlers.

## Family compatibility inventory ownership

- Machine-readable API, CLI, and MCP compatibility classifications live in
  `contracts/api/deprecated.json`, `contracts/cli/deprecated.json`, and
  `contracts/mcp/deprecated.json` against
  `contracts/compatibility-inventory.schema.json` and the shared vocabulary in
  `contracts/common/compatibility-inventory.schema.json`.
- These family inventories are the **machine-readable source of truth** for
  classification status, successor `targetItemId`, evidence, lifecycle
  versions, removal-gate status, and maintainer `approvalStatus`. Update the
  JSON records first when classification changes; the human-readable appendix
  tables in [Compatibility Alias Inventory](#compatibility-alias-inventory)
  document boundary ownership and runtime evidence and must stay aligned.
- Supported `classification` values (see schema `$defs/classification`):
  - `retain-temporarily`: the alias remains callable; removal requires satisfying
    every recorded removal gate in a separate approved removal change.
  - `remove-now`: classified as removal-ready for a later approved removal
    story; inventory classification does not unregister or delete runtime
    aliases.
  - `separately-approved`: retained under explicit maintainer approval outside
    the default retain/remove posture.
- Supported `approvalStatus` values are `approved`, `pending`, and `rejected`.
  Inventory records compose lifecycle fields from
  `contracts/common/deprecations.schema.json`; classification metadata does not
  control runtime callability.
- Baseline scope for coverage assertions lives under
  `contracts/testdata/baseline/` (`api-compatibility-surfaces.json`,
  `cli-commands.json`, `mcp-aliases.json`). Add new compatibility-only surfaces
  to the relevant baseline file and the matching family inventory in the same
  change.
- `contracts/compatibility_inventory_coverage_test.go`
  (`TestCompatibilityInventoryBaselineCoverage`) loads each family inventory plus
  its baseline and fails when any in-scope compatibility surface lacks exactly
  one classified record or required fields (stable `itemId`, `family`,
  `publicName`, supported `classification`, successor, evidence, lifecycle
  versions, removal gates, approval status). Per-family schema validation stays
  in `contracts/*_deprecated_inventory_test.go`.

## MCP tool catalog contract ownership

- Authored MCP tool catalog schema lives at
  `contracts/mcp/tool-catalog.schema.json` (Draft 2020-12, protocol pin
  `2024-11-05`, format version `1.0.0`). Tool records compose B04 documentation
  and lifecycle shapes via `$ref` to `contracts/common/documentation.schema.json`
  and `contracts/common/deprecations.schema.json`.
- Authored MCP tool catalog document lives at `contracts/mcp/tools.json` with
  `sharedSchemas` keyed by stable `mcp.schema.*` identifiers for reusable source,
  wait, domain-error, transport, lifecycle, handler, and CallToolResult vocabulary.
  Tool records may reference shared schemas through in-document `$ref` pointers
  resolved by `internal/contractvalidator` before schema validation.
- Authored catalog identity completeness is proven by
  `pkg/services/factory_sessions/transports/mcp/catalog/catalog_identity.go`
  (`CatalogToolIdentitiesFromCatalogDocument`, `VerifyCatalogToolIdentityCompleteness`,
  `CatalogToolIDForName`) against `DiscoverTools()` stable ID (`mcp.tool.<name>`)
  and public name parity; compatibility alias names are rejected. Wire the check
  into `make contracts-validate` for `contracts/mcp/tools.json` via
  `internal/contractvalidator/mcp_tool_catalog_identity.go`, and prove success
  plus missing/extra/duplicate/alias failures in
  `contracts/mcp_tool_catalog_identity_test.go` and
  `pkg/services/factory_sessions/transports/mcp/catalog/catalog_identity_test.go`.
- Authored catalog input-schema parity is proven by
  `pkg/services/factory_sessions/transports/mcp/catalog/catalog_input_schema.go`
  (`CatalogInputSchemasFromCatalogDocument`, `PrepareCatalogInputSchemaForParity`,
  `VerifyCatalogInputSchemaParity`) against resolved `input.schema` maps and
  `DiscoverTools()` semantics; wire into `make contracts-validate` via
  `internal/contractvalidator/mcp_tool_catalog_input_schema.go`, and prove
  success plus nested valid/invalid argument validation in
  `contracts/mcp_tool_catalog_input_schema_test.go` and
  `pkg/services/factory_sessions/transports/mcp/catalog/catalog_input_schema_test.go`.
- Authored catalog publication guards live in
  `pkg/services/factory_sessions/transports/mcp/catalog/catalog_publication.go`
  (`VerifyCatalogAliasExclusion`, `VerifyCatalogModalityPolicy`,
  `VerifyAuthoredCatalogStagingBoundary`, `MarshalCatalogDocumentJSON`,
  `VerifyCatalogByteStability`). Wire alias exclusion, text-only modality,
  staging-boundary separation from `packages/api/generated/mcp/tools.json`, and
  byte-stable canonical serialization into `make contracts-validate` for
  `contracts/mcp/tools.json` via
  `internal/contractvalidator/mcp_tool_catalog_publication.go`; prove success and
  failure paths in `contracts/mcp_tool_catalog_publication_test.go` and
  `pkg/services/factory_sessions/transports/mcp/catalog/catalog_publication_test.go`. Staged MCP
  tools remain projected only from `contracts/testdata/baseline/mcp-tools.json`
  through `internal/contractstaging/policy.go`, not from the authored catalog.
- MCP boundary closeout comparison lives in `internal/mcpcontractcheck`, with
  the read-only `cmd/mcpcontractcheck` entrypoint. Keep the pure `Validate`
  function explicit over authored catalog, generated primary discovery,
  handwritten stable-ID registry, and compatibility-alias inputs. Repository
  loading reads retained alias identity and canonical target IDs from
  `contracts/mcp/deprecated.json` separately from the handwritten runtime alias
  routes; validate inventory/runtime parity without promoting aliases into
  canonical discovery. Repository loading may project handler identities through
  `factorysession.ProjectCanonicalToolHandlerBindings`, which intentionally
  omits executable functions and returns stable tool/handler IDs in sorted
  order; do not move handler behavior into catalog or generated code. Run the
  comparison through `make mcp-contract-check`; keep that read-only target in
  both `make mcp-contract-smoke` and `make verify-fast` so focused MCP checks
  and normal fast verification reject boundary drift.
- Valid MCP catalog fixtures live under `contracts/testdata/mcp/` and register
  through `internal/contractvalidator.MCPRegistry()` into
  `make contracts-validate`. The authored `contracts/mcp/tools.json` registers in
  `MCPRegistry()`; `valid-minimal.json` remains a focused schema fixture only.
  Registry-valid fixtures must not reuse stable documentation IDs already claimed
  by an authored `contracts/mcp/tools.json` tool record; retarget overlapping
  fixtures to uncontracted tools with fixture-prefixed documentation IDs when
  canonical tool title/description IDs would collide (for example
  `valid-text-error-result.json` uses `control` with `mcp.fixture.*` IDs;
  `valid-protocol-failures.json`, `valid-handler-binding.json`, and
  `invalid-unknown-protocol-version.json` retarget to `list` or `read_events`
  with fixture-prefixed documentation IDs once `validate_source` / `control` are
  authored in `tools.json`).
  Invalid fixtures are asserted in focused
  `internal/contractvalidator` tests with path-bearing diagnostics, not in the
  default valid-only registry pass. Consolidated fixture-matrix closure lives in
  `contracts/mcp_tool_catalog_fixture_matrix_test.go` (valid/invalid classes,
  directory coverage, registry alignment, and authored-catalog boundary).
- Tool `input` combines a closed Draft `schema` (`#/$defs/closedDraftSchema`)
  with object-keyed `arguments` using `mcp.arg.*` stable IDs; `execution.mode`
  is pinned to `tools-call` and `transports` to `stdio-json-rpc` for the
  supported protocol surface. `closedSchemaNode` requires explicit
  `additionalProperties` on object nodes and allows `additionalProperties: true`
  for discovery-open maps such as `args` on `start_sync` / `start_async` and
  `metadata` / `approvedPolicy` on `validate_source` / `control`; start tools
  reuse shared `#/sharedSchemas/mcp.schema.you.factory_session.source` and
  `wait` via `$ref` in tool input schemas. `validate_source` mirrors Factory
  preview request/result shapes from `pkg/services/factory_sessions/transports/mcp/schemas.go`;
  `control` mirrors lifecycle control request/response shapes with closed
  `links` nested objects in catalog results.
- Reusable MCP protocol components live under `contracts/mcp/protocol/`
  (`content.schema.json` documents broader MCP content kinds; `call-tool-result.schema.json`
  defines the `pinnedTextCallToolResult` envelope used by tool `result.examples`;
  `domain-tool-response.schema.json` documents typed domain success/failure payloads
  serialized into text; `json-rpc-error.schema.json` documents protocol failures
  distinct from domain errors). Tool `result.domain` groups domain success/failure
  documentation separately from `result.transport` and `result.examples`; catalog-level
  `protocolFailures` documents JSON-RPC invalid-params/unknown-method failures.
- Compatibility aliases remain only in `contracts/mcp/deprecated.json`; the
  tool-catalog schema is build-time contract validation only and does not cut
  over `packages/api/generated/mcp/tools.json` or runtime discovery.
- Tool `handler` binds handwritten implementations with `mcp.handler.*` stable
  IDs (`#/$defs/handler`). Duplicate documentation IDs, broken handler IDs,
  unknown `protocolVersion`, and malformed lifecycle/documentation records fail
  through schema validation or family-neutral `identity.duplicate` diagnostics in
  focused `validator_test.go` cases. Runtime `tools/call` binding stays handwritten
  in `pkg/services/factory_sessions/transports/mcp/client.go`: resolve canonical protocol names
  from generated discovery to stable `mcp.tool.*` IDs, then select the handwritten
  registry entry that records its contracted `mcp.handler.*` ID and implementation.
  Resolve compatibility aliases before stable-ID lookup; never add aliases to the
  generated primary discovery or move handler functions into generated code.

## Focused compatibility alias internal-use lint

- `internal/contractguard` loads inventoried compatibility alias match values from
  `contracts/mcp/deprecated.json`, `contracts/cli/deprecated.json`, and
  `contracts/api/deprecated.json`, then scans handwritten `cmd/`, `internal/`,
  `pkg/`, and `ui/src` sources for deliberate new adoption outside approved
  compatibility boundaries.
- `cmd/compatibilityaliascheck` is wired through `make compatibility-alias-check`
  and the default `make lint` target. Checked-in violation and clean fixtures
  live under `cmd/compatibilityaliascheck/testdata/{violation-repo,clean-repo}/`.
- Retained aliases remain callable at existing CLI/MCP/REST compatibility
  boundaries (`pkg/transports/`, `pkg/factory/`, `ui/src/api/workflow-preview/`,
  and related owners documented above). New first-party internal code must use
  canonical successors instead of inventoried compatibility alias names.
- The `~default` session-selector compatibility surface stays inventoried for
  classification but is excluded from this focused lint because it is accepted
  at many transport boundaries rather than as a workflow-preview alias name.
- Retained aliases remain callable until objective removal gates are satisfied
  in a separate approved removal change; the lint gate blocks new first-party
  internal adoption only.

## Primary navigation and reference guidance

- Packaged CLI reference (`docs/reference/`, `you docs <topic>`), dashboard
  navigation, OpenAPI primary discovery, and new first-party examples must
  present canonical successors rather than promoting compatibility aliases as
  primary entry points.
- Compatibility aliases belong only in the family compatibility inventories
  above and the maintainer appendix tables in
  [Compatibility Alias Inventory](#compatibility-alias-inventory); they are
  excluded from primary discovery and must not appear as recommended routes,
  nav labels, packaged topic names, or first-party examples in customer guides.
- When documenting public entry points, name the canonical successor (for
  example `POST /factories/preview`, Factory Session REST routes, and
  `you.factory_session.*` MCP tools). Mention retained workflow-named aliases
  only in explicit migration or compatibility appendix lanes.
- Reference topics may acknowledge long-lived CLI topic aliases (for example
  `batch-work` → `batch-inputs`) at the edges of canonical topic lists in
  `docs/reference/README.md`; workflow-named API/CLI/MCP aliases are not primary
  and must not be added to primary navigation or reference tables.

## Session response-event SSE surface

Use this lane when changing `GET /factory-sessions/{session_id}/response-events`,
`FactoryResponseEvent` schemas, retention or `STREAM_GAP` semantics, or packaged
session guidance for the ephemeral observation stream. This route is separate
from canonical `GET /factory-sessions/{session_id}/events` Factory event replay;
do not conflate reconnect parameters, retention, or error codes between them.

**Authored OpenAPI and fragments**

- Route and operation prose:
  `api/openapi-main.yaml` (`getFactoryResponseEventsBySessionId` on
  `/factory-sessions/{session_id}/response-events`)
- Query parameters:
  `api/components/parameters/ResponseEventAfterSequence.yaml`,
  `ResponseEventDispatchID.yaml`, `ResponseEventKind.yaml`
- Event schema graph:
  `api/components/schemas/response-events/` (envelope, kind/phase/provenance,
  payload union, content blocks)
- Typed error responses:
  `api/components/responses/ResponseEventSessionNotFound.yaml`,
  `ResponseEventStreamExpired.yaml`, `ResponseEventBadRequest.yaml`; codes in
  `api/components/schemas/api/ErrorResponse.yaml`

**Generated clients** (run `make generate-api` after OpenAPI edits)

- `api/openapi.yaml`
- `pkg/transports/http/generated/server.gen.go`
- `pkg/transports/http/client/client.gen.go`
- `ui/src/api/generated/openapi.ts`

Factory contract changes must also run `bun run generate` from
`ui/packages/client` so its package-local `src/generated/openapi.ts` and
standalone Factory schema copies stay aligned with these canonical outputs.

**Runtime, handler, and mapping ownership**

- HTTP SSE transport:
  `pkg/services/factory_sessions/transports/http/handlers_events.go` (`GetFactoryResponseEventsBySessionId`)
- Transport-neutral subscription contract:
  `pkg/transports/mapping/surface.go`, `pkg/transports/mapping/contract.go`
  (`SubscribeFactoryResponseEventsForSession`, `FactoryResponseEventRecord`)
- Session-owned ephemeral store and retention gaps:
  `pkg/factory/sessions/responseeventstore/`; canonical event vocabulary in
  `pkg/factory/sessions/responseevents/`
- Runtime composition and typed 410 mapping:
  `pkg/service/runtime_sessions.go`, `pkg/runtimehost/` session facades,
  `pkg/initializer/api_transport_internal_test.go`

**Packaged operator guidance**

- `docs/reference/sessions.md` (`## Response-event stream lifecycle and reconnect`);
  run `make docs-reference-smoke` after reference-topic edits

**Focused contract and behavior tests**

- OpenAPI authoring and generated-type parity:
  `pkg/transports/http/contracttests/openapi_contract_response_events_test.go`,
  `pkg/transports/http/contracttests/generated_contract_common_test.go`
- HTTP route behavior (retained-then-live, stale cursor `STREAM_GAP`, typed
  `404`/`410`, no default-session fallback):
  `pkg/transports/http/server_factory_sessions_test.go`,
  `pkg/transports/http/servertests/server_factory_session_orchestrator_test.go`
- Packaged docs align with authored operation prose:
  `pkg/transports/cli/docs/docs_response_stream_openapi_alignment_test.go`
  (`TestMarkdown_SessionsResponseEventStreamAlignsWithOpenAPI`,
  `TestMarkdown_WorkersProviderFidelityAlignsWithPublicContract`) and
  `tests/functional/smoke/cli_docs_smoke_test.go` sessions markers

**Maintainer verification commands**

- OpenAPI or schema edits: `make generate-api`, then `make api-smoke` when feasible;
  for Factory contract changes, also run `cd ui/packages/client && bun run
  generate` and verify with `make ui-public-package-release`
- Packaged session guidance edits: `make docs-reference-smoke`
- Focused HTTP lane:
  `go test ./pkg/transports/http/... -run 'FactoryResponseEvents' -count=1`

## OpenAPI contract semver comparator

- `internal/contractopenapidiff` owns the build-time OpenAPI comparator that
  classifies supported semantic deltas between two loaded documents as
  `major`, `minor`, or `patch` with stable change codes and operation/schema
  paths. Use `CompareYAML` or `CompareDocuments`; keep runtime packages from
  importing it.
- Documentation-only differences (description, summary, externalDocs, schema
  title) classify as `patch` when structural surfaces match. Compatible
  additions (new operations, optional parameters, optional schema properties,
  widened enums) classify as `minor` with `openapi.add.*` change codes.
  Compatible requiredness relaxations (parameter `required: true` to `false`,
  removing a name from `schema.required`) classify as `minor` with
  `openapi.relax.*` change codes. Removals and narrowing classify as `major`
  with `openapi.remove.*` and `openapi.narrow.*` change codes; major wins over
  minor and patch. Inline request/response content schemas use the same
  `collectSchemaRefChanges` path as component schemas (for example
  `{operation}.responses.{status}.content.{mediaType}.schema`). Unsupported
  or ambiguous structural deltas fail closed via `UnsupportedDiffError` with an
  operation or schema path instead of returning a guessed classification.
  Residual schema keywords outside the supported comparison surface (for example
  `nullable`, `additionalProperties`, `oneOf`, constraints, and `default`) also
  fail closed instead of silently classifying as `patch`. Unhandled parameter
  serialization fields (`style`, `explode`, `allowReserved`), response `headers`,
  and non-schema `components` maps (`parameters`, `securitySchemes`, etc.) also
  fail closed with path-aware `UnsupportedDiffError` outcomes. Operation
  `security`, path-item `parameters`/`servers`, operation `servers`, server
  `variables` (`default`/`enum`), media-type `encoding`/`example`, response
  `links`, and operation `callbacks` must also fail closed or classify
  explicitly; path-item parameter removal reuses `collectParameterChanges` as
  major with `openapi.remove.parameter`. Vendor extensions (`x-*` /
  kin-openapi `Extensions` maps on info, operation, parameter, requestBody,
  response, mediaType, schema, server, tag, and components) must fail closed via
  shared `checkUnsupportedExtensions` instead of silently classifying as `patch`.
- Focused fixtures live under
  `internal/contractopenapidiff/testdata/`; prove end-to-end outcomes in
  `internal/contractopenapidiff/compare_test.go` and the consolidated matrix in
  `internal/contractopenapidiff/fixture_matrix_test.go`; include the package in
  `make contracts-smoke`.

## Compatibility Alias Inventory

This section is the human-readable maintainer appendix for alias-accepting
boundaries, canonical successors, runtime evidence, and objective removal gates.
Classification status, approval posture, and removal-gate satisfaction are
owned by the machine-readable family inventories documented in
[Family compatibility inventory ownership](#family-compatibility-inventory-ownership);
update those JSON records first when classification changes.

An alias-accepting boundary may parse, route, or re-export an obsolete name, but
it does not own the behavior. The canonical owner and its tests remain the final
specification; compatibility-only tests prove deprecation signals and parity
while callers migrate. Removing an alias must satisfy its entire removal gate
and must be handled as a separate compatibility-removal change.

### Factory preview API and CLI

Canonical guidance: use `POST /factories/preview`, `FactoryPreviewRequest`, and
`FactoryPreviewResult` for the full source-resolution, validation, effective
policy, artifact-root, and result-constraint preview contract. The JavaScript
orchestrator implementation is owned by `pkg/orchestrators/javascript/preview`;
the API boundary is owned by `pkg/transports/mapping/factory_preview.go` and
`pkg/transports/http/handlers_factory.go` (`PreviewFactory`). This agrees with
`docs/reference/orchestrators.md` and `docs/reference/sessions.md`.

Every entry in this section has the same earliest removal boundary and
measurable gate: no earlier than the next major release after the owning
subsystem records passing audits for two consecutive minor releases. The
evidence source is the release issue's compatibility audit, assembled from the
public-documentation scan, supported-customer migration issues, and support
cases labeled `workflow-alias-migration`. The pass threshold for each release
is zero first-party examples recommending the alias and zero unresolved
supported-customer migration blockers. Both numeric counts must equal zero in
both releases. No entry below is recorded as satisfying this gate or approved
for removal.

| Compatibility alias | Alias-accepting boundary | Exact canonical successor | Durable owner | Compatibility-only evidence and canonical ownership | Objective removal gate |
|---|---|---|---|---|---|
| `POST /workflow-previews` (`previewWorkflow`) | Authored route in `api/openapi-main.yaml`; `PreviewWorkflow` in `pkg/transports/http/handlers_factory.go` accepts the request and emits `Deprecation` plus a `Link` to its successor. | `POST /factories/preview` (`previewFactory`) | Factory preview API boundary: `api/components/schemas/api/FactoryPreview*.yaml`, `pkg/transports/mapping/factory_preview.go`, and `PreviewFactory` in `pkg/transports/http/handlers_factory.go`; preview computation remains in `pkg/orchestrators/javascript/preview`. | Compatibility-only: the `CompatibilityOnly` cases in `pkg/transports/http/contracttests/openapi_contract_factory_validation_test.go` and `pkg/transports/http/servertests/server_factory_preview_test.go` prove deprecated contract metadata, successor headers, and response parity. Canonical behavior is owned by `TestOpenAPIContract_DefinesFactoryPreviewEndpoint`, `TestOpenAPIContract_FactoryPreview*`, `TestPreviewFactory_*`, and `pkg/transports/mapping/factory_preview_test.go`. | Remove only after (1) every supported external REST/generated-client consumer has migrated to `/factories/preview`, (2) published API and release deprecation guidance has named the successor for the supported deprecation window, and (3) canonical Factory preview contract, valid/invalid-source diagnostics, and path-aware diagnostic regression coverage remains green without alias tests. |
| `WorkflowPreviewRequest`, `WorkflowPreviewResult`, generated `PreviewWorkflowJSONRequestBody`, `PreviewWorkflow` server operation, and generated TypeScript `operations["previewWorkflow"]` | Deprecated schema fragments `api/components/schemas/api/WorkflowPreviewRequest.yaml` and `WorkflowPreviewResult.yaml`, plus artifacts generated from the obsolete route. These names only alias or transport the canonical models. | `FactoryPreviewRequest`, `FactoryPreviewResult`, generated `PreviewFactoryJSONRequestBody`, `PreviewFactory` server operation, and generated TypeScript `operations["previewFactory"]` | Factory preview contract and generation boundary: `api/components/schemas/api/FactoryPreview*.yaml` and `POST /factories/preview`; runtime mapping/behavior is owned by `pkg/transports/mapping/factory_preview.go` and `pkg/orchestrators/javascript/preview`. | Compatibility-only: `TestOpenAPIContract_WorkflowPreviewSchemasAreCompatibilityOnlyAliasesOfFactoryPreview` and the obsolete-path assertions in `ui/src/api/factory-preview/types.test.ts` prove alias shape/parity. Canonical schema and generated-helper ownership belongs to the `FactoryPreview*` contract tests and canonical path/type assertions in those files. | Remove only after (1) supported Go and TypeScript consumers no longer compile against or invoke any `WorkflowPreview*`/`PreviewWorkflow*` name, (2) published contract/client deprecation guidance has directed them to the exact `FactoryPreview*`/`PreviewFactory*` successor for the supported window, and (3) canonical generated Go/TypeScript Factory preview types, operation, and request/response regression coverage remains green without compatibility aliases. |
| `you workflow preview` | `newWorkflowPreviewCommand` and `pkg/transports/cli/workflow/preview.go`. | `POST /factories/preview`. | Factory CLI and Factory preview API. | Same source, policy, artifact-root, and result-constraint preview outcome without execution. | Common two-minor-release zero/zero gate above. |
| `you workflow validate` | `newWorkflowValidateCommand` and `pkg/transports/cli/workflow/validate.go`. | `POST /factories/preview` or MCP `you.factory_session.validate_source`. | Factory CLI and Factory preview API. | Same normalized source-validation outcome and diagnostics without session creation. | Common two-minor-release zero/zero gate above. |
| `you workflow run` | `newWorkflowRunCommand` and `pkg/services/factory_sessions/transports/cli/sessionexecution`. | `POST /factory-sessions/sync`. | Factory CLI and Factory Session execution. | Same shared request contract, synchronous status, result availability, result, and inspection links. | Common two-minor-release zero/zero gate above. |
| `you workflow start` | `newWorkflowStartCommand` and `pkg/services/factory_sessions/transports/cli/sessionexecution`. | `POST /factory-sessions/async`. | Factory CLI and Factory Session execution. | Same shared request contract, session id, initial status, result availability, and inspection links. | Common two-minor-release zero/zero gate above. |
| `you workflow status {session_id}` | `newWorkflowStatusCommand` and `pkg/services/factory_sessions/transports/cli/sessionexecution`. | `GET /factory-sessions/{session_id}`; `you session show {session_id}` for live-projected sessions. | Factory CLI and Factory Session reads. | Same identity, lifecycle, progress, result availability, and links, normalized for CLI output. | Common two-minor-release zero/zero gate above. |
| `you workflow result {session_id}` | `newWorkflowResultCommand` and `pkg/services/factory_sessions/transports/cli/sessionexecution`. | `GET /factory-sessions/{session_id}/results`. | Factory CLI and Factory Session results. | Same final, partial, or not-ready result and artifact references, normalized for CLI output. | Common two-minor-release zero/zero gate above. |
| `you workflow dispatches {session_id}` | `newWorkflowDispatchesCommand` and `pkg/services/factory_sessions/transports/cli/sessionexecution`. | `GET /factory-sessions/{session_id}/dispatches` or `you session dispatches {session_id}`. | Factory CLI and Factory Session dispatch inspection. | Same ordered session-owned dispatch records, normalized for CLI output. | Common two-minor-release zero/zero gate above. |
| `you workflow artifacts {session_id}` | `newWorkflowArtifactsCommand` and `pkg/services/factory_sessions/transports/cli/sessionexecution`. | `GET /factory-sessions/{session_id}/artifacts`. | Factory CLI and Factory Session artifact inspection. | Same session-owned artifact records and references, normalized for CLI output. | Common two-minor-release zero/zero gate above. |
| `you workflow events {session_id}` | `newWorkflowEventsCommand` and `pkg/services/factory_sessions/transports/cli/sessionexecution`. | `GET /factory-sessions/{session_id}/events`. | Factory CLI and Factory Session event inspection. | Same ordered events and reconnect-cursor behavior, normalized for CLI output. | Common two-minor-release zero/zero gate above. |

### MCP and dashboard workflow-named wrappers

Canonical guidance: new MCP hosts and clients must discover and invoke the
`you.factory_session.*` tools documented in `docs/reference/mcp.md` (packaged
as `you docs mcp`).
Factory Session MCP behavior is owned by `pkg/mcp/factorysession` over the
shared `pkg/factorysessionexecution` service contract. In the dashboard,
`ui/src/api/factory-preview` owns preview request/result types, error handling,
messages, and the `previewFactory` transport adapter. The active preview panel
already uses `useFactoryPreview` and `factoryPreviewQueryOptions` in
`ui/src/features/workflow-preview/hooks/useWorkflowPreview.ts`; its feature
directory name is not an API compatibility alias.

The dashboard route, navigation, link, and customer-visible label audit found
no retained workflow-named alias: the dashboard selects the canonical Factory
Session experience with `factorySessionId`, and session-owned links use
`/factory-sessions/{session_id}`. The `ui/src/api/workflow-preview` entry below
is a TypeScript import compatibility boundary, not a customer-reachable route
or a separate workflow-run view. Consequently there is no retained UI
navigation alias to exercise in a browser; browser verification applies to the
canonical Factory Session view documented in `docs/reference/sessions.md`.

Every MCP and UI entry below has the same earliest eligible removal boundary
and measurable gate: not before the next major release after at least two
consecutive minor releases of published deprecation guidance. For each of
those two releases, the owning subsystem must record (a) supported-consumer
usage from MCP tool-name telemetry or UI import/usage audit and (b) migration
blockers from support and release-readiness tracking. Eligibility requires
exactly zero supported alias consumers and zero open supported-customer
migration blockers in both releases. No entry below is recorded as satisfying
this gate or approved for removal.

| Compatibility alias | Alias-accepting boundary | Exact canonical successor | Durable owner | Compatibility-only evidence and canonical ownership | Objective removal gate |
|---|---|---|---|---|---|
| MCP `you.workflow.validate` | `ToolWorkflowValidate`, `DiscoverCompatibilityAliases`, and `ResolveToolName` in `pkg/mcp/factorysession` accept the workflow-named tool and route its unchanged request and response envelopes. | MCP `you.factory_session.validate_source` | Factory Session MCP validation tool in `pkg/mcp/factorysession/validate_source.go`; the validation result remains the canonical Factory preview contract owned by `pkg/apisurface/factory_preview.go` and `pkg/orchestrators/javascript/preview`. | Same request and valid/invalid validation result or typed error envelope. Compatibility tests: `TestMockClient_WorkflowValidateCompatibilityOnlyAliasMatchesCanonicalSuccess` and `TestMockClient_WorkflowValidateCompatibilityOnlyAliasMatchesCanonicalBehavior`. | Common two-minor-release zero/zero gate above. |
| MCP `you.workflow.run` | `ToolWorkflowRun`, `DiscoverCompatibilityAliases`, and `ResolveToolName` in `pkg/mcp/factorysession` route the same sync-start request and response envelopes. | MCP `you.factory_session.start_sync` | Factory Session MCP execution boundary in `pkg/mcp/factorysession/session_start.go`, backed by `pkg/factorysessionexecution`. | Same request and sync Factory Session status, result, inspection links, or typed error envelope. Compatibility tests: `TestMockClient_WorkflowRunCompatibilityOnlyAliasMatchesCanonicalSuccess` and the server dispatch alias assertion. | Common two-minor-release zero/zero gate above. |
| MCP `you.workflow.status` | `ToolWorkflowStatus`, `DiscoverCompatibilityAliases`, and `ResolveToolName` in `pkg/mcp/factorysession` route the same session-read request and response envelopes. | MCP `you.factory_session.get` | Factory Session MCP read boundary in `pkg/mcp/factorysession/session_read.go`, backed by `pkg/factorysessionexecution`. | Same durable session identity, lifecycle, progress, links, or typed not-found envelope. Compatibility test: `TestMockClient_WorkflowStatusCompatibilityOnlyAlias_MissingSessionMatchesCanonicalNotFound`. | Common two-minor-release zero/zero gate above. |
| MCP `you.workflow.result` | `ToolWorkflowResult`, `DiscoverCompatibilityAliases`, and `ResolveToolName` in `pkg/mcp/factorysession` route the same result-read request and response envelopes. | MCP `you.factory_session.get_result` | Factory Session MCP result-read boundary in `pkg/mcp/factorysession/session_read.go`, backed by `pkg/factorysessionexecution`. | Same final, partial, not-ready, or not-found result envelope and artifact references. Compatibility test: `TestMockClient_WorkflowResultCompatibilityOnlyAliasMatchesCanonicalTerminalResult`. | Common two-minor-release zero/zero gate above. |
| MCP `you.workflow.artifacts` | `ToolWorkflowArtifacts`, `DiscoverCompatibilityAliases`, and `ResolveToolName` in `pkg/mcp/factorysession` route the same artifact-list request and response envelopes. | MCP `you.factory_session.list_artifacts` | Factory Session MCP inspection boundary in `pkg/mcp/factorysession/inspection.go`, backed by `pkg/factorysessionexecution`. | Same ordered FactoryArtifact summaries, lineage, empty state, or typed session error. Compatibility test: `TestMockClient_ListArtifacts_WorkflowCompatibilityOnlyAliasParity`. | Common two-minor-release zero/zero gate above. |
| UI `WorkflowPreviewRequest`, `WorkflowPreviewResult`, `WorkflowPreviewAPIError`, `workflowPreviewAPIErrorMessages`, and `previewWorkflow` exports | Deprecated re-exports and delegation in `ui/src/api/workflow-preview/{api,index,messages}.ts` accept workflow-named imports; `previewWorkflow` delegates without reshaping request, options, result, or error behavior. | `FactoryPreviewRequest`, `FactoryPreviewResult`, `FactoryPreviewAPIError`, `factoryPreviewAPIErrorMessages`, and `previewFactory` from `ui/src/api/factory-preview` | Dashboard Factory preview API adapter in `ui/src/api/factory-preview/{api,index,messages}.ts`; active query behavior is owned by `useFactoryPreview` and `factoryPreviewQueryOptions` in the preview feature hook. | Same request/options identity and success or `FactoryPreviewAPIError` rejection; no customer-visible workflow-run route or resource. Compatibility tests: `ui/src/api/workflow-preview/api.test.ts` and `index.test.ts`. | Common two-minor-release zero/zero gate above. |

### Global events and default-session selection

- The reusable session SSE harness must send `Accept: text/event-stream`, retain backend-scope/logical-session-key/Factory Session/stream-generation identity as distinct values, and preserve complete `id`/`event`/`data`/comment frames. Decode data with the generated `FactoryEvent` contract, return malformed-data errors with the offending frame, and keep the prior valid frame available for diagnostics. A bounded idle read must retain its single pending transport read so a later live event remains observable rather than being consumed by an abandoned reader. Keep caller cancellation distinct from bounded waiting, and include the session selector, supplied checkpoint, original wait bound, and last valid frame (or an explicit no-frame statement) in typed read diagnostics; focused protocol and real-transport evidence lives in `factorysessionsse/harness_internal_test.go`, `factorysessionsse/initial_stream_test.go`, and `factorysessionsse/unknown_session_test.go`.

Canonical guidance: dashboard, replay, reconnect, and new integrations open
`GET /factory-sessions/{session_id}/events` with the resolved Factory Session
UUID. `~default` is only an accepted selector at API and CLI boundaries; a list
summary can retain it as the row `id`, so that field is not canonical identity
to persist. Callers obtain the resolved `factorySessionId` UUID from the list
runtime stream identity when present, sync preflight, a session read, or the
event-stream handshake, then use that UUID for subsequent reads, persisted
dashboard state, and event connections. This agrees with
`docs/reference/orchestrators.md` and `docs/reference/sessions.md`.

| Compatibility alias | Alias-accepting boundary | Exact canonical successor | Durable owner | Compatibility-only evidence and canonical ownership | Objective removal gate |
|---|---|---|---|---|---|
| `~default` (and an omitted/blank session selector where accepted) | Alias-aware `internal/sessionregistry.Registry.Get` / `Remove`, live-runtime resolution, and CLI/API defaults accept the selector and resolve it to the current default live session. The registry alias and compatibility record path are boundary details only. | The resolved UUID selected by `internal/livesession.CanonicalID`, carried on owner-projected read/open values, sync preflight, and the event-stream handshake; callers retain that UUID for later Factory Session reads, persisted dashboard state, and event connections. | Factory Session identity and selector resolution live under `pkg/services/factory_sessions/internal/livesession`, `internal/sessionregistry`, and `internal/sessionservice`; restart/remap identity is owned by the private identity and control-plane capabilities, while dashboard persistence and recovery are owned by `ui/src/features/dashboard/lib/preflight` and `ui/src/features/dashboard/hooks/preflight`. | Compatibility-only registry tests prove alias acceptance and routing. Canonical ownership belongs to `internal/livesession` identity/allocation tests, default-selector control-plane read/preflight tests, logical-session remap/restart tests, session-scoped event handshake tests, and dashboard snapshot/event-stream tests that persist and connect with the resolved UUID. | Remove only after (1) supported CLI, API, and UI callers require an explicit Factory Session UUID and no longer submit or persist `~default`, (2) published session/operator and release deprecation guidance has named UUID identity for the supported window, (3) restart and default-session replacement/remap behavior no longer depends on alias lookup or compatibility record paths, and (4) canonical UUID allocation, list/read identity, sync-preflight recovery, dashboard persistence, and session-scoped reconnect coverage remains green without selector tests. |

- Stream-responses final audit evidence for the Batch 07 gate lives in
  `docs/internal/development/plans/you-goal/stream-responses-final-audit.md`.
  Cite that artifact before claiming canonical `FactoryResponseEvent` or
  session-scoped response-event SSE parity; it records blocking residuals when
  the public response-event contract is absent from merged OpenAPI.
- API/CLI response-stream gate evidence for story 004 lives in
  `docs/internal/development/plans/you-goal/api-cli-response-stream-parity.md`.
  It records merged terminal `InvocationResponse` parity between session
  invocations and CLI JSON `primary_result` records plus explicit upstream
  exclusions for canonical response-event SSE until R1/R2 land.
- `@you/goal` API contract boundary audit lives in `docs/internal/development/plans/you-goal/api-contract-audit.md`. Default Factory Session compatibility alias resolution belongs in `pkg/services/factory_sessions/internal/sessionregistry`, canonical ID allocation/selection belongs in `pkg/services/factory_sessions/internal/livesession`, and owner-projected open/read results carry the resolved ID to transports. API/CLI callers may still pass `~default`; callers obtain canonical identity from an owner-projected list/read/open value, sync preflight, or the stream handshake, then use the resolved UUID for later session reads, dashboard state, and event connections rather than persisting the alias. Follow-on goal API PRs should cite that artifact before adding routes, OpenAPI fragments, or generated-client changes. Invocation reuse for goal mode stays on `POST /factory-sessions/{session_id}/invocations` with `Factory.invocationReturn` primary-result selection. Internal `SessionResponseStream` / `SessionResponseStreamEvent` models and existing lifecycle routes (`/pause`, `/resume`, `DISPATCH_INTERRUPTED`) are documented in the audit lifecycle section.
- Internal `SessionResponseStream` backpressure and diagnostics work stays below the public contract boundary. Verify it in `pkg/factorysessions/responsestream/*_test.go`, `pkg/factorysessions/stream/*_test.go`, and `pkg/service/runtime_session_runtime_test.go` using live-session `metricsSink` records and observed runtime logs rather than adding OpenAPI or generated-client changes.
- Provider-specific response-stream normalization must stay internal. When changing Cursor/Codex live-stream parsers, extend `pkg/api/contracttests/openapi_contract_surface_test.go` and `pkg/api/contracttests/generated_contract_events_test.go` with behavioral boundary assertions on published schemas and generated event JSON; do not use artifact string scans as the proof that parser-only fields stay internal.
- Live provider progress publication into internal session streams belongs in `pkg/workers/provider/inference_provider.go` (`WithInferenceProgressPublisher`, provider terminal success/failure markers, subprocess fragment kinds), `pkg/service/factory_build.go` (inject the session-owned publisher into ScriptWrapProvider construction and default subprocess runner wrapping), `pkg/factorysessions/stream` (`Manager.InferenceProgressPublisherFactory`, `DispatchCompletionObserverFactory`, fragment normalization), and `pkg/service/runtime_sessions.go` (session gateway host seams and metrics/logging only). Keep provider progress internal-only: normalize at the provider boundary, publish only `SessionResponseStreamEvent` kinds, and do not append raw provider progress into canonical `FactoryEvent` history or public API schemas. Treat publication as additive and failure-safe at the session-owned publisher seam: recover publication panics, emit bounded `session_response_stream.degraded` metrics and warn logs with reason labels such as `STREAM_UNAVAILABLE` or `PUBLISH_PANIC`, and never let internal stream faults replace the provider's final `WorkResult`. Additive verification belongs in `pkg/service/testmain_test.go`, where model-worker execution should prove an injected internal progress publisher can run alongside canonical inference-event recording without changing the final `WorkResult` or public event history; pressure and lagging-consumer verification belongs in `pkg/service/runtime_session_runtime_test.go`, where the live-session publisher seam should prove no-subscriber and slow-consumer cases degrade through bounded retained-window compaction rather than blocking execution.
- `pkg/service` is under `pkg-maint` enforcement for cyclomatic complexity and function-line limits even in tests. When extending the internal provider/session-stream verification path in `pkg/service/factory_build.go`, `pkg/service/testmain_test.go`, or `pkg/service/runtime_session_runtime_test.go`, prefer small fixture/assertion helpers over growing constructor branches or long same-package test bodies so mergeability fixes do not become a recurring CI blocker.
- `pkg/workers/provider` is also under a hard `make pkg-file-count` cap of 15 owned Go files for the package directory. Provider-stream follow-ups should prefer merging tiny focused helpers/tests into existing provider files before adding new companion files, or remove/recombine older small files in the same change, so mergeability fixes do not replace one CI blocker with another.

- `api/openapi-main.yaml` is the authored OpenAPI entrypoint. Register new component fragments under `components.schemas` here.
- `api/components/schemas/api/` contains API request/response fragments.
- `api/components/schemas/api/InvocationResponse.yaml` owns the shared session
  invocation response contract. When non-success invocation outcomes expose new
  recovery context such as `sessionId`, `workId`, `workName`, or `workState`,
  author that shape here first, then regenerate and update the session
  invocation handler plus contract tests.
- `api/components/schemas/data-models/` contains customer-facing factory and work data model fragments.
- Shared stopped-automation inspection summaries for existing `FactorySession` and `Work` reads belong in `api/components/schemas/api/FactoryStop*.yaml`, projected from live-session runtime state through `pkg/apisurface/contract.go`, attached to `FactorySessionRuntime.stopSummary` in `pkg/factorysessions/projection.go`, and attached to `Work.stopSummary` in `pkg/api/handlers_work_read.go`. Keep this path inspect-only: extend the summary when operators need more context, but do not add goal-specific control routes when the fix belongs on existing session or work surfaces. Recovery wording should also be derived here from existing token feedback/history and latest dispatch facts so API, CLI, and future UI reads stay aligned on the same inspect-first explanation. When a `Work` read needs interruption context that is not present on the raw engine snapshot, reuse the already projected session stop summary through `SessionAPISurface.GetFactorySession` before falling back to snapshot-derived interrupted-work synthesis, and keep `pkg/service/runtime_sessions.go` projecting JavaScript dispatch history into `ProjectionContext.JavaScript` even for non-JavaScript orchestrator sessions that can still surface interrupted work recovery.
- When replacing a public API field with a nested contract, remove the deprecated field from the authored fragment under `api/components/schemas/api/` instead of patching generated files; `make generate-api` and `make api-smoke` will keep reintroducing the stale field until the source fragment is corrected.
- Default-path policy belongs to each defining service: Operator Settings owns its config path, Factory Definitions owns named/global and project Factory roots, Factory Runtime owns workflow-source roots, Recordings owns recording targets, and logging/metrics own their artifact roots. `pkg/platform/defaultpaths` is retired. Policy-free shared runtime-artifact filename, collision, and UTC `YYYY/MM/DD` mechanics live under `pkg/platform/internal/runtimeartifact`; reservation creates parent directories with `MkdirAll`, reserves non-colliding paths with exclusive create, and hands the placeholder file to the logging and metrics rolling writers. Consolidated layout regression coverage lives in `pkg/platform/logging/runtime_artifact_layout_test.go` with reservation behavior in `pkg/platform/internal/runtimeartifact/reserve_test.go`.
- Factory invocation-signature contract changes start in `api/components/schemas/data-models/FactoryInvocation*.yaml` and the `Factory.invocationSignature` field in `api/components/schemas/data-models/Factory.yaml`; keep runtime round-trip mapping aligned in `pkg/factory/contracts/factory_config.go`, `pkg/config/factory_config_mapping.go`, and `pkg/config/factory_config_mapping_internal.go`, extend generated contract smoke in `pkg/transports/http/contracttests/generated_contract_{common,factory}_test.go`, and keep dashboard editable-definition decoding aligned in `ui/src/api/factory-definition/api.ts` plus focused normalization tests whenever the schema shape changes.
- Invocation-signature validation rules belong in `pkg/factory/validation/validate.go` (`InvocationSignatureTargets`) and consume the Factory-owned authored vocabulary from `pkg/factory/contracts/factory_config.go`; generated enum declarations must remain outside the validation owner. The generated-payload entry adapter belongs at `pkg/transports/mapping/validationentry`, which keeps `validation.Validate`, `validationentry.ValidateFactoryAPI`, `POST /factory-validations`, and `you factory config validate` aligned on the same blocking target codes. Prove package/API parity in `pkg/factory/validation/validation_test.go`, `pkg/transports/mapping/validationentry/api_test.go`, and `pkg/transports/http/servertests/server_factory_validation_test.go` rather than duplicating transport-specific validation logic.
- Runtime worker taxonomy lives in `api/components/schemas/data-models/WorkerType.yaml` (`INFERENCE_WORKER`, `AGENT_WORKER`, `SCRIPT_WORKER`, `POLLER_WORKER` plus legacy aliases), with canonical runtime identities and behavior projection in `pkg/workers/taxonomy`. Keep generated-boundary enum conversion in the transport-facing mapping path while routing model-capability, hosted-linear, and dispatch policy through the worker owner rather than comparing legacy strings directly. Worker configuration lives in `pkg/workers/config`; provider-session, execution, failure, and runner capability contracts live in `pkg/workers/execution`; safe generated diagnostic projection lives in `pkg/workers/diagnostics`; and stable model-provider command identities live in `pkg/models/provider`.
- Runtime workstation execution taxonomy lives beside worker taxonomy in `pkg/workers/taxonomy`; `pkg/workers/compatibility` owns behavior pairing and receives a narrow workstation view instead of importing Factory configuration. Generated enum conversion remains at the API mapping boundary. Derive `kind: POLLER` for explicit `POLLER_RUN` workstations during `normalizeCanonicalWorkstationRuntime` in `pkg/config/runtime_config.go`, and reject conflicting behavior through the Factory validation adapters that translate authored workstation definitions into the narrow compatibility input.
- Worker/workstation behavior compatibility belongs in `pkg/workers/compatibility` and is adapted from Factory definitions by validation and mapping owners. Preserve legacy `MODEL_WORKSTATION` + `MODEL_WORKER`, legacy `MODEL_WORKSTATION` + `SCRIPT_WORKER`, legacy empty standard workstations + inference workers, and poller script/hosted pairings through that owner; reject new explicit taxonomy mismatches with inference/agent/script/poller terminology before dispatch.
- Runtime taxonomy policy over Factory-owned definitions belongs in `pkg/factory/validation/worker_workstation_compatibility.go` (`WorkerWorkstationBehaviorCompatibilityTargets`, wired through `validation.Validate`). The generated OpenAPI compatibility adapter belongs in `pkg/transports/mapping/validationentry/worker_workstation_compatibility_api.go`, where `ValidateFactoryAPI` can preserve authored public taxonomy before config projection collapses compatibility aliases. Public rendering remains in `pkg/transports/mapping/factory_validation.go` (`FactoryRuntimeTaxonomySummary`, `RenderFactoryValidationHuman`) and `pkg/transports/cli/factory/validate.go` (`you factory config validate <factory-path>`). Keep authored worker/workstation type strings intact in inspection output; surface incompatible pairings through POST `/factory-validations` and the CLI validate command with the same blocking target code `workstation-worker-behavior-compatibility`.
- Inference runtime execution parity belongs in `pkg/service/inference_runtime_taxonomy_test.go` (bounded provider/model-invoke execution, canonical inference events via `providerCommandRunner` + `RecordingProvider`, legacy alias equivalence, OmniVoice packaged-factory inference behavior, and managed-runtime routing). Keep managed-runtime catalog/wrapper gates on `interfaces.IsInferenceWorkerType` in `pkg/models/local/runtime.go` and `pkg/models/local/catalog.go` instead of comparing only `MODEL_WORKER`.
- Dashboard runtime taxonomy editing belongs in `ui/src/features/current-factory-definition/lib/worker-workstation-taxonomy.ts` (defaults, editable option lists, and legacy alias helpers), exported through `ui/src/features/current-factory-definition/public/index.ts`; keep graph add-dialog defaults, current-selection enum labels, and save/projection tests on the shared helpers instead of hard-coding `MODEL_WORKER` / `MODEL_INVOKE` in feature modules.
- Factory orchestrator identity lives in `api/components/schemas/data-models/FactoryOrchestrator*.yaml`; wire through `pkg/factory/contracts/factory_config.go`, `pkg/config/factory_config_mapping_internal.go`, and `pkg/factory/validation/validate.go` orchestrator targets. Keep flattened/exported factories free of default PETRI orchestrator blocks via `FactoryConfigMapper.Flatten`; use `config.ProjectEffectiveOrchestratorForAPIRead` only on explicit read surfaces that need compatibility projection, not `replay.GeneratedFactoryFromRuntimeConfig`.
- JavaScript orchestrator source loading and resolution belong under `pkg/orchestrators/javascript/source` (see `docs/internal/development/plans/dynamic-workflows/dynamic-workflow-design.md`). Lookup order: project `.claude/workflows`, `~/.you-agent-factory/workflows`, package-relative `factory/workflows`, named JavaScript factories, then explicit factory lookup when requested. Normalize API, CLI, MCP, and website source requests through `pkg/apisurface/workflow_source.go` (CLI re-export in `pkg/cli/workflowsource/normalize.go`). Durable Factory Session start paths resolve sources through `pkg/factorysessionexecution/prepare_start.go` (`PrepareStart`, `StartSourceContext`). The removed root `pkg/workflowsource/` Batch 001 shim must not be reintroduced; core runtime/API imports must use `pkg/orchestrators/javascript/source` directly.
- JavaScript orchestrator source validation belongs under `pkg/orchestrators/javascript/validation` (static parse via `github.com/tdewolff/parse/v2/js`, no execution). File-backed `.js` and supported `.ts` sources load before `ValidateLoaded` with stable `SourceHash`, TypeScript syntax stripping, and authored-line remapping. Integrate factory validation through `pkg/factory/validation/validate.go` for inline source plus orchestrator metadata/argsSchema checks, and `pkg/factory/validation/WorkflowSourceTargets` with `validation.Options.WorkflowSourceReader` for file-backed `sourceRef` reads from `pkg/transports/mapping/validationentry/api.go`. The removed root `pkg/workflowvalidation/` Batch 001 shim must not be reintroduced; core runtime/API imports must use `pkg/orchestrators/javascript/validation` directly.
- JavaScript orchestrator policy defaults, validation, stable hashing, and capability denial belong under `pkg/orchestrators/javascript/policy`. Normalize API, CLI, MCP, and website policy requests through `pkg/apisurface/workflow_policy.go`. Integrate factory defaultPolicy validation through `pkg/factory/validation/validate.go` and session runtime policy hash/budget projection through `pkg/factorysessions/projection.go`. The removed root `pkg/workflowpolicy/` Batch 001 shim must not be reintroduced; core runtime/API imports must use `pkg/orchestrators/javascript/policy` directly.
- JavaScript named child-agent defaults are authored at `orchestrator.javascript.agents` in `api/components/schemas/data-models/FactoryOrchestratorJavaScript*.yaml`, mapped through `pkg/config/factory_config_mapping_internal.go`, and carried with named-factory source resolution into `pkg/orchestrators/javascript/runtime`. Operator preset existence is checked during `pkg/factorysessionexecution.PrepareStart` using the preset catalog loaded by service/runtime-host composition, before runtime dispatch records can be created.
- JavaScript orchestrator result validation, artifact URI parsing, and `FactorySessionResult.primaryResult` / `SESSION_RESULT_UPDATED` projection belong under `pkg/orchestrators/javascript/result`. Normalize API, CLI, MCP, and website result-shape checks through `pkg/apisurface/workflow_result.go`. Integrate terminal session result reads through `pkg/factorysessions/checkpoints.go` and replay through `pkg/factory/projections/world_state.go`. The removed root `pkg/workflowresult/` Batch 001 shim must not be reintroduced; core runtime/API imports must use `pkg/orchestrators/javascript/result` directly.
- JavaScript orchestrator durable session state (source snapshots, phase summaries, checkpoints, artifacts, replay indexes) belongs under `pkg/orchestrators/javascript/store` (`CheckpointStore`, `NewCheckpointStore`, `CheckpointSummary`, `BuildCheckpointSummary`, `LatestCheckpointSummaryFromRecords`). `pkg/factorysessions/checkpoint_store.go` type-aliases `JavaScriptCheckpointStore` for live-session compatibility; project checkpoint refs through `pkg/factorysessions/checkpoints.go`.
- JavaScript-orchestrated restart-resume from persisted checkpoint summaries belongs in `pkg/orchestrators/javascript/runtime` (`Request.Resume`, `ResumeContext`, `workflow.resumeState`, `ResumingChildExecutor`, `CompletedChildResultsFromRecords`) and `pkg/factorysessionexecution` (`ResumeSessionRequest`, `ResumeInterruptedSession`, `ResumeError`, `PersistedRuntimeSessionState.CheckpointSummary`, `persistSessionSnapshot`, `shouldPersistSessionSnapshot`, `loadResumeSessionState`, `validateResumeSessionState`). Rehydrate persisted `CheckpointState` into resumed execution through `workflow.resumeState()` and align pending child dispatch ordinals when checkpoint state is present (`ResumingChildExecutor` starts at `len(CompletedDispatchIDs)`). Preserve start metadata (`StartRequest`, `ResolvedSource`, `sourceContent`) and runtime records through terminal projection via `preserveRuntimeResumeState` / `restoreRuntimeResumeState` in `projection_consistency.go`; skip stale interrupt re-finalization after successful resume when `priorStatus == RESUMING` while rebuilding canonical events with preserved `DISPATCH_INTERRUPTED` history plus `ORCHESTRATOR_CHECKPOINT_WRITTEN` and restart `SESSION_RESUMED` (`RESUMING`) lineage in `listing.go` (`checkpointEventsFromRuntimeState`, `appendCanonicalOrchestratorCheckpointEvents`, `mergeResumeLifecycleLineage`). Map restart-resume validation failures through `pkg/apisurface/factorysession/factory_session_execution_errors.go` (`ExecutionErrorResponse` for `ResumeError`). Prove interrupted-to-resumed success through `pkg/factorysessionexecution/fixtures/runtime_restart_resume_test.go` (including checkpoint-state control-flow rehydration), resumed read/event/reconnect parity through `pkg/factorysessionexecution/fixtures/runtime_restart_resume_reads_test.go`, typed missing/corrupted/invalid resume failures through `pkg/factorysessionexecution/fixtures/runtime_restart_resume_failures_test.go`, and additive non-resumed fake-child/simple-final replay/reconnect/persistence regression through `pkg/factorysessionexecution/fixtures/runtime_restart_resume_non_regression_test.go`.
- Live Factory Session identity and restart-recovery preflight have one ownership map. Target normalization belongs in `pkg/services/factory_sessions/internal/logicaltarget`; canonical UUID allocation and selection belong in `internal/livesession`; registry lookup belongs in `internal/sessionregistry`; logical remap belongs in the private identity service; read/open projections carry the selected ID through `ProjectionContext.FactorySessionID` and `OpenResult.Session`; and preflight policy belongs in `internal/controlplane`. The service-owned HTTP adapter transports `GET /factory-sessions/{session_id}/sync-preflight` and the session-scoped SSE handshake. Persisted `backendScopeID` must flow through `FactorySessionRuntime.streamIdentity.backendScopeID`, preflight `backendScopeId`, and the SSE identity headers. Treat `~default` and other supported aliases only as boundary selectors: return and persist the resolved UUID already projected by Factory Sessions, use `logical_session_remap` for a matching replacement within the active backend scope, reject cross-scope resolution, and validate reconnect-cursor reuse before hydration or SSE. Keep `streamGenerationId` aligned with the live stream identity rather than synthesizing a preflight-only or per-read token. Operational semantics and canonical verification surfaces live in `docs/architecture/logical-session-identity.md`.
- Dashboard runtime cache isolation belongs in `ui/src/features/dashboard/lib/backend-runtime-cache-scope.ts` (`backendRuntimeCacheScopeKey`, `scopedRuntimeQueryKey`, `shouldResetDashboardRuntimeScopedState`). After session preflight resolves `streamIdentity.backendScopeID`, `useDashboardSnapshot` stores the normalized scope in `useDashboardStreamStore.backendRuntimeCacheScope`; `useDashboardSessionLifecycle` resets runtime-derived state when that scope changes. Runtime React Query keys for current-factory definition/document, factory-session detail, and provider-session detail must include the scope segment. IndexedDB checkpoints and in-memory timeline/materialized-outcome entries use the exact four-part `backendScopeID::factorySessionID::logicalSessionKeyID::streamGenerationID` identity owned by `ui/src/features/timeline/lib/stream-derived-cache-identity.ts`, `ui/src/features/timeline/state/timelineCheckpointPersistence.ts`, and `ui/src/features/timeline/state/factoryTimelineStore.ts`. Each `factorySessionID` must be the concrete UUID resolved by preflight or another identity-bearing boundary; never persist `~default` or another selector alias. Stale-cursor recovery clears the affected checkpoint through `clearTimelineCheckpoint` and reconnects cursor-free with the resolved live identity, not through session-wide deletion or hydrate-before-preflight behavior.
- Closing a dashboard Factory Session is the terminal cleanup boundary for its derived browser state. After the backend accepts the close, `use-dashboard-session-tabs-state.ts` must remove every in-memory timeline generation for that concrete `factorySessionID` and clear only that session's IndexedDB checkpoints before selecting the remaining tab; preserve other sessions' entries, reconnect cursors, checkpoints, and live stream. Prove the A-to-B-to-A restore and close-A/preserve-B paths in `ui/src/App.multi-session-timeline-isolation.test.tsx`, with registry cleanup coverage in `factoryTimelineStore.entries.test.ts`.
- Browser checkpoint durability is derived-cache behavior, not canonical Factory Session persistence. `ui/src/features/timeline/state/checkpoint-persistence/ordering/orderedCheckpointWriter.ts` serializes same-identity writes, and `indexedDBCheckpointRequests.ts` performs monotonic conditional replacement in one transaction whose completion establishes durability; failed, rejected, aborted, older, or equal replacement attempts preserve the last committed record. `ui/src/App.tsx` and its checkpoint persistence hooks own normal debounced writes plus best-effort stream-handoff, unmount, `visibilitychange`, and `pagehide` flushes while containing lifecycle-time failures. Keep materialized Work outcome samples and compact text bounded through `timelineCheckpointCodec.ts`; do not persist raw Dispatch collections, Factory events, or unbounded Work content. `ui/src/features/dashboard/lib/session-persistence/diagnostics.ts` may retain only its bounded redaction-safe outcome/action/mismatch/correlation-token schema, never raw identities, events, Work content, checkpoint payloads, or unbounded console/network history. Ordinary Factory Session reads remain dispatch-free; load Dispatch summaries or detail lazily from the dedicated `/factory-sessions/{session_id}/dispatches` inspection surface.
- When removing or nesting a public runtime field, update `pkg/transports/http/contracttests/openapi_contract_authoring_test.go` to assert the new schema location in the bundled contract; backend coverage runs this package even when `make api-smoke` already passes, so stale flat-field assertions can block mergeability after generated artifacts are correct.
- JavaScript orchestrator preview preparation belongs under `pkg/orchestrators/javascript/preview` and must use **Factory preview** or **Factory Session preview** semantics, not a standalone workflow-preview product surface. Canonical preview route is `POST /factories/preview` with `FactoryPreviewRequest` / `FactoryPreviewResult` in `api/components/schemas/api/FactoryPreview*.yaml`, handler `PreviewFactory` in `pkg/transports/http/handlers_factory.go`, apisurface `pkg/transports/mapping/factory_preview.go`, UI adapter `ui/src/api/factory-preview/`, and generated `previewFactory` operation. `POST /workflow-previews` remains a deprecated compatibility alias with `Deprecation` and `Link` successor headers; `WorkflowPreview*` schemas alias `FactoryPreview*`. `pkg/transports/mapping/workflow_preview.go`, CLI `you workflow preview`, MCP `pkg/transports/mcp/workflow/preview.go`, `ui/src/api/workflow-preview/`, and `ui/src/features/workflow-preview/` wrap the canonical Factory preview contract. The removed root `pkg/workflowpreview/` Batch 001 shim must not be reintroduced. Prove canonical preview and path-aware invalid-source diagnostics in `pkg/transports/http/servertests/server_factory_preview_test.go` (`POST /factories/preview`); keep `POST /workflow-previews` coverage compatibility-only (deprecation headers plus alias parity) in the same file via `server_preview_helpers_test.go`. Prove UI generated-type and adapter coverage in `ui/src/api/factory-preview/types.test.ts` and `ui/src/api/factory-preview/api.test.ts`; keep `ui/src/api/workflow-preview/` tests compatibility-only (delegation to `previewFactory`).
- JavaScript orchestrator CLI workflow source validation loopback belongs in `pkg/transports/mapping/factory_preview.go` (`BuildFactoryWorkflowValidation`, `FactoryWorkflowValidationResultFromPreview`) and `pkg/transports/cli/workflow/validate.go` (`you workflow validate`). Reuse `BuildFactoryPreview` / orchestrator preview validation rather than a CLI-only parser; JSON output uses generated `WorkflowSourceResolution` and `WorkflowDiagnostic` shapes with a `blockingDiagnostics` collection. Shared CLI source inputs live in `pkg/transports/cli/workflow/request.go` (`SourceConfig`, `validateSourceConfig`, optional `--args-schema` and `--requested-policy` flags on workflow validate/preview). Reject missing or conflicting `--kind`/`--value`/`--inline` combinations in `validateSourceConfig` before `DefaultContext` or preview validation; command errors keep stdout empty even when `--json` is set. Human blocking-validation output labels a `Blocking diagnostics:` section; JSON blocking-validation failure still emits the validation result then exits non-zero. Prove success, blocking-diagnostic, and command-error paths (missing/conflicting source inputs, invalid kind, invalid requested policy JSON) in `pkg/transports/cli/workflow/validate_test.go` and apisurface mapping in `pkg/transports/mapping/factory_preview_test.go`.
- JavaScript orchestrator CLI durable Factory Session listing loopback belongs in `pkg/services/factory_sessions/transports/cli/session/list.go`, `pkg/services/factory_sessions/transports/cli/session/durable_list.go`, and `pkg/services/factory_sessions/transports/cli/session/list_human.go` (`you session list --scope live|persisted|all`). Keep `scope=live` on the existing HTTP `GET /factory-sessions` path for workspace rows; load persisted/all durable rows through `factorysessionexecution.FakeService` (`NewFakeServiceFromContractFixtures`, catalog path `fixtures.ContractFixtureCatalogRelativePath`) and merge via `ApplySessionListScope` plus `factorysession.ListSessionsResponseToAPI`. Reject unsupported scopes through `factorysessionexecution.NormalizeListSessionsRequest` before HTTP or durable provider calls. Human output keeps the legacy live-session table and adds a `Factory Sessions (durable):` table with status, source identity, result availability, phase/progress, and action availability; JSON output uses the same generated `ListFactorySessionsResponse` / `FactorySessionDurableSummary` shapes (no CLI-only list schema). Scope-specific empty states distinguish reachable providers with no rows from provider failures; provider and scope-validation failures keep stdout empty for `--json`. Inject `ListConfig.DurableLister` in tests instead of relying on repo-root fixture discovery. Prove persisted JavaScript rows, all-scope live+durable merge, empty persisted state, unsupported scope handling, provider failure handling (persisted and all scopes), canonical JSON parity, deterministic durable ordering, and redaction of raw source/provider internals in `pkg/services/factory_sessions/transports/cli/session/list_test.go`.
- CLI compatibility wording for dynamic workflow validation and durable session listing: keep `you workflow validate` as the primary CLI path in `pkg/transports/cli/root.go` (`newWorkflowCommand`) and `pkg/transports/cli/workflow/validate.go`; keep `you workflow preview` compatibility-only with Factory preview human output in `pkg/transports/cli/workflow/preview.go`. Parent `you session` / `you session list` help in `pkg/transports/cli/root_work.go` should steer durable execution listing to Factory Session status, source identity, result availability, progress, and action availability rather than workflow-preview or dynamic-workflow-run nouns. Prove command help and human-output vocabulary in `pkg/transports/cli/root_workflow_session_terminology_test.go` using `fixtures.ForbiddenFixtureVocabularyTerms()` for forbidden public wording.
- Canonical operator-facing docs for the shipped JavaScript durable-session slice belong in `docs/reference/sessions.md`; keep that topic as the bounded validate/inspect/website-detail/lifecycle-control playbook. Keep `docs/reference/orchestrators.md` as the canonical noun and alias owner for `Factory`, `FactorySession`, `Dispatch`, `FactoryArtifact`, `FactoryEvent`, and dynamic-workflow shorthand. When wording drift appears, repair the owning topic instead of adding another operator summary.
- JavaScript orchestrator CLI durable Factory Session execution loopback belongs in `pkg/services/factory_sessions/transports/cli/sessionexecution/` (`RunSync`, `RunAsync`, `RunStatus`, `RunResult`, `RunDispatches`, `RunArtifacts`, `RunEvents`) with command wiring in `pkg/transports/cli/root_workflow.go` (`newWorkflowRunCommand`, `newWorkflowStartCommand`, `newWorkflowStatusCommand`, `newWorkflowResultCommand`, and inspection commands). Default `--execution-provider fake` keeps fixture-catalog-backed `factorysessionexecution.NewFakeServiceFromContractFixtures`; `--execution-provider javascript-runtime` routes through `factorysessionexecution.NewExecutionService` with `--project-root` and optional `--child-executor-mode live-provider`. JavaScript runtime services persist terminal session snapshots under `{projectRoot}/.you-agent-factory/durable-sessions/{sessionId}.json` via `pkg/factorysessionexecution/runtimepersist` so separate CLI invocations can reload status, result, dispatch, and artifact inspection without sharing one in-memory `Service`. Live-provider CLI smoke uses `factorysessionexecution.SmokeLiveChildProvider()` when no external provider is configured. Map `--child-executor-mode` into shared `StartRequest.Runtime` through `pkg/apisurface/factorysession/factory_session_execution_cli.go` (`CLIStartInput.Runtime`). **Scope boundary:** lane `dynamic-workflows-cell-cli-live-dispatch-smoke` proves direct shared execution-service CLI observability only; deferred follow-up cells are MCP live runtime serve (`follow-up-cell-mcp-session-serve.md`) and dashboard website inspection (`follow-up-cell-real-backend-api-session-parity-deferred.md`), recorded in `follow-up-cell-cli-live-dispatch-smoke-deferred.md`. CLI JSON dispatch/artifact output uses shared `ListDispatchesResponseToAPI` / `ListArtifactsResponseToAPI` projections rather than CLI-only dispatch semantics. Prove runtime-backed live-child run/status/result correlation in `pkg/services/factory_sessions/transports/cli/sessionexecution/run_test.go` (`TestRunSync_LiveProviderJavaScriptSession_ReReadStatusAndResult`, `TestRunSync_LiveProviderJavaScriptSession_ReReadAcrossFreshServices`, `TestRunSync_JavaScriptRuntimeBackend_UsesRealExecutionServiceWithoutFixtureStub`). Prove bridged live-child dispatch and artifact inspection through `RunDispatches` / `RunArtifacts` in `TestLiveProviderJavaScriptSession_DispatchAndArtifactCLIInspection` (provider session refs, output artifact ids, artifact dispatch linkage, shared projection parity). Preserve fixture-backed and fake-child CLI regression in `TestFixtureBackedCLIInspectionRegression_FullLoopWithoutLiveProviderFlags` (`loopback_test.go`, golden `fixtures.ListDispatchesResultHash` / `ListArtifactsResultHash` without live-provider markers) and `TestRunSync_JavaScriptRuntimeFakeChildCLIInspectionRegression` / `TestRunSync_ExplicitFakeChildMode_OverridesLiveConfiguredServiceCLI` (`run_test.go`, fake `executionMode` and no live-provider session refs). Focused verification: `go test ./pkg/services/factory_sessions/transports/cli/sessionexecution/... -count=1`, `go test ./pkg/factorysessionexecution/... ./pkg/orchestrators/javascript/...`, and `make test`.
- JavaScript orchestrator start-time checks (source resolution, validation, and policy enforcement before dispatch) belong under `pkg/orchestrators/javascript/{source,validation,policy}` and wire through Factory Session execution start surfaces (`POST /factory-sessions/async`, `POST /factory-sessions/sync`, and related durable start/read/control routes below) via `pkg/factorysessionexecution/` (`PrepareStart`, `StartPrepareContext`, `PreparedStart`) and `pkg/apisurface/factorysession/` — not through transitional preview-only routes or root `pkg/workflow*` shims. `PrepareStart` normalizes requests, resolves source, validates wrapped executable source (matching runtime IIFE wrapping), args JSON compatibility, requested policy, and sync wait options, then projects `PolicyProjection` before runtime execution.
- Canonical MCP tool identity inventory lives in `pkg/mcp/factorysession/registry.go` (`ProjectToolInventory`, `ProjectToolInventoryFromDiscovered`, `MarshalToolInventoryJSON`, `VerifyProjectedToolInventory`, `VerifyToolInventory`, `ToolInventory`, `ToolInventoryEntry`, `ToolInventoryBaselineRelativePath`) over `DiscoverTools`, with handler-registration evidence from `IsCanonicalToolHandlerRegistered` in `client.go` backed by the shared `canonicalToolHandlers` map used by `CallTool`. The projection deep-copies and recursively key-sorts `inputSchema` without mutating discovery maps, excludes `DiscoverCompatibilityAliases`, and omits result-contract fields. `VerifyToolInventory` fails when a canonical discovered tool lacks a registered handler or when a compatibility alias appears in the inventory. Prove shape, alias exclusion, handler verification failures, schema canonicalization, determinism, discovery parity, and byte-identical baseline fixture parity in `pkg/mcp/factorysession/registry_test.go`; the reviewed baseline fixture is `contracts/testdata/baseline/mcp-tools.json`.
- MCP compatibility alias and CallToolResult result/error transport policy inventories live in `pkg/mcp/factorysession/inventory_boundary.go` (`ProjectAliasInventory`, `ProjectAliasInventoryFromDiscovered`, `MarshalAliasInventoryJSON`, `VerifyProjectedAliasInventory`, `VerifyAliasInventory`, `AliasInventory`, `AliasInventoryEntry`, `AliasInventoryBaselineRelativePath`, `ProjectResultPolicyInventory`, `MarshalResultPolicyInventoryJSON`, `EncodeSuccessCallToolResult`, `MarshalSuccessCallToolResultJSON`, `VerifyProjectedResultPolicyInventory`, `VerifyResultPolicyInventory`, `VerifyProjectedMCPBoundaryInventories`, `ResultPolicyInventory`, `SuccessTransportPolicy`, `DomainErrorTransportPolicy`, `ProtocolErrorTransportPolicy`, `ResultPolicyFixture`, `DomainErrorFixture`, `ProtocolErrorFixture`, `ResultPolicyInventoryBaselineRelativePath`, `SuccessTextEncodingSerializedJSON`, `FailureClassDomain`, `FailureClassProtocol`, `ProtocolTransportJSONRPCError`) over `DiscoverCompatibilityAliases`, `ResolveToolName`, and live CallToolResult / JSON-RPC protocol behavior. Alias records include alias name, canonical tool name, `compatibilityOnly`, and description; aliases are sorted deterministically and must not appear in `contracts/testdata/baseline/mcp-tools.json`. Result-policy records document text-only serialized-JSON success envelopes with `isError` false, typed domain-error envelopes in the same CallToolResult shape with `isError` false, and JSON-RPC protocol errors (`-32602`, `-32601`) outside CallToolResult payloads. `VerifyAliasInventory` fails when an alias does not resolve exactly once to a discovered canonical tool or appears in the canonical tool inventory; `VerifyProjectedMCPBoundaryInventories` runs alias and result-policy verification together. Prove alias shape, resolution parity, canonical-inventory exclusion, transport-policy shape, domain/protocol separation, server encoding parity, discovery non-mutation, determinism, and byte-identical baseline fixture parity in `pkg/mcp/factorysession/inventory_boundary_test.go`; reviewed baseline fixtures are `contracts/testdata/baseline/mcp-aliases.json` and `contracts/testdata/baseline/mcp-result-policy.json`.
- Durable Factory Session MCP tool discovery lives in `pkg/services/factory_sessions/transports/mcp/` (`DiscoverTools`, `DiscoverCompatibilityAliases`, `ResolveToolName`, `ValidateSource` in `tool.go`, `ToolDefinition`, `CompatibilityAlias`, `ToolErrorEnvelope`, `ToolResponse`, `NewClient`, `NewClientWithService`). Canonical discovery metadata is authored in `contracts/mcp/tools.json`, projected by `pkg/transports/mcp/discoverygen/`, generated with `make mcp-discovery-generate` into the reviewable `pkg/transports/mcp/generated/discovery.json` and runtime `discovery.gen.go` artifacts, and checked for drift with `make mcp-discovery-check`; `pkg/transports/mcp/server` serves canonical `tools/list` descriptors from `generated.PrimaryDiscovery` while retaining the handwritten compatibility-alias overlay. Keep generated metadata limited to stable tool ID, protocol name, English description, and input schema, while handwritten handler behavior remains outside generated code. Canonical tool names use `you.factory_session.*` for list, validate_source, start_sync, start_async, get, get_result, list_dispatches, list_artifacts, control, and read_events; schemas mirror durable REST plus canonical Factory preview (`FactoryPreviewRequest` / `FactoryPreviewResult`) — not deprecated `POST /workflow-previews`. Workflow-named `you.workflow.*` aliases are compatibility-only and resolve through `ResolveToolName` to the same canonical handlers. Prove generator coverage, alias exclusion, determinism, and generated-artifact drift in `pkg/transports/mcp/discoverygen/generate_test.go`; prove live generated/legacy `tools/list` parity, alias overlay behavior, and exact text-only success/error `CallToolResult` envelopes in `pkg/transports/mcp/server/server_test.go`; prove discovery order, schema fields, shared error envelopes, compatibility-alias routing, vocabulary guardrails, and representative discovered response-schema field alignment with generated API types in `pkg/services/factory_sessions/transports/mcp/registry_test.go`. Keep invocation success, typed failure, runtime-backed, inspection, and control coverage in the focused tests beside `pkg/services/factory_sessions/transports/mcp/`. Run `make mcp-contract-smoke` to validate the catalog and generated discovery, all focused MCP transport/factory-session tests, and the stdio fixture-backed, runtime-backed, and resume CLI regression suites together.
- Runtime-backed durable dispatch controls must expose in-flight child dispatches on the real async path, not only after terminal projection. Mirror live child records into `pkg/factorysessionexecution/runtime_service.go` while the workflow is running, and prove the behavior with a blocking live-provider test in `pkg/factorysessionexecution/fixtures/runtime_live_child_test.go` instead of only seeding dispatch state in interrupt-specific helpers.
- JavaScript live-child provider retries belong in `pkg/factorysessionexecution/livechild/provider.go`: thread the resolved `workflowpolicy.EffectivePolicy` through `workflowruntime.Hooks.NewChildExecutor`, use `provider.NormalizeProviderExecutionError` plus `provider.WorkFailureDecisionFromProviderError` as the sole classification/decision boundary, and keep one dispatch identity while recording a positive attempt on each provider call. Preserve context cancellation and deadlines before normalization so a canceled run cannot begin another attempt. Prove retry-success, exhaustion, terminal-no-retry, and cancellation with deterministic provider sequences in `pkg/factorysessionexecution/livechild/provider_test.go`.
- Durable JavaScript live-child retry diagnostics flow from provider-owned safe failure detail and retry decisions into `workflowruntime.ChildDispatchRecord`, then through `pkg/factorysessionexecution.DispatchSummary` and `pkg/apisurface/factorysession` into both shared Dispatch list and detail contracts. Persist `attempt`, `retryable`, and canonical `failureClassification` on the runtime record; projections must copy those facts without parsing display messages, preserve a first-attempt fallback for older records, and regenerate authored OpenAPI plus Go/TypeScript clients when the public fields change.
- Dynamic workflow MCP host install guidance lives in `docs/reference/mcp.md` (packaged as `you docs mcp` through `docs/reference/embed.go` and `pkg/cli/docs/docs.go`). Document the current stdio server entrypoint `you mcp serve` from `pkg/cli/mcp/serve.go`, wired into the root CLI via `pkg/cli/root.go` (`newMCPCommand`), with protocol handling in `pkg/mcp/server/server.go`. Host examples must use current `you.factory_session.*` tool names, stdio transport assumptions, and explicit `command` / `args` / `cwd` requirements. Default server backing reuses the durable session fixture catalog discovery pattern from workflow CLI commands (`factorysessionexecution.NewFakeServiceFromContractFixtures` with optional `--fixture-catalog`); runtime-backed serve selects `factorysessionexecution.NewExecutionService(factorysessionexecution.ExecutionProviderJavaScriptRuntime, ...)` through `you mcp serve --runtime` with optional `--project-root`. Keep `## Choose A Backing Mode`, `## Run The First-Host Smoke`, `## Know What Is Proven`, and `## Troubleshoot Setup And Calls` aligned with automation: shared stdio server behavior is automated in-repo via `pkg/cli/mcp/serve_smoke_test.go`; practical host configuration remains documented manual work; HTTP/SSE transport and live Factory HTTP runtime backing remain unsupported. Cross-link from `docs/reference/agents.md`, `docs/reference/orchestrators.md`, and `docs/reference/README.md`; prove the packaged topic through installed `you docs mcp` behavior in `tests/functional/smoke/cli_docs_smoke_test.go`. Prove documented fixture-backed install-path smoke (stdio JSON-RPC `initialize` / `tools/list` / `tools/call` for validate_source, start_async, get, and not-ready get_result) through `pkg/cli/mcp/serve_smoke_test.go` against `mcpcli.RunServe` with injected `ServeConfig.Service` and `os.Pipe` stdio; prove runtime-backed serve smoke (discovery, INLINE_WORKFLOW async start, status polling, and not-ready or terminal result reads) through `pkg/cli/mcp/serve_runtime_smoke_test.go` against `mcpcli.RunServe` with `ServeConfig.RuntimeBacked` and no injected service; prove runtime-backed interrupted-to-resumed continuity (WORKFLOW_NAME async start, `you.factory_session.control` interrupt-dispatch and resume, stable session id, resumed lifecycle timestamps, terminal result reads, completed child dispatch continuity without replay, and typed `TERMINAL_SESSION` / `NO_OP` invalid resume outcomes) through `pkg/cli/mcp/serve_runtime_resume_smoke_test.go` against `mcpcli.RunServe` with injected `factorysessionexecution.NewJavaScriptRuntimeService` (live child executor plus blocking provider) and the shared stdio MCP client harness from `serve_smoke_test.go` rather than a separate transport package; prove additive fixture-backed and runtime-backed MCP serve regression after resume smoke through `pkg/cli/mcp/serve_runtime_resume_non_regression_test.go` (install smoke, async serve, canonical `you.factory_session.*` resume inspection, and `fixtures.ForbiddenFixtureVocabularyTerms` guardrails).
- The packaged beta JavaScript `agent.run` argument contract is documented in `docs/reference/orchestrators.md`. Keep its named `agent-run-valid` and `agent-run-invalid` examples executable through the real source validator in `pkg/cli/docs/docs_agent_run_examples_test.go`; this makes documentation drift from the six-field child contract fail the docs verification lanes.
- Recovery-lane preview install scope and runtime-backed serve boundaries live in `docs/reference/mcp.md` (`you docs mcp`): copyable preview-only host JSON, fixture-backed vs runtime-backed serve mode tables, automation-backed verification for `serve_smoke_test.go` and `serve_runtime_smoke_test.go`, and explicit scope boundaries deferring website inspection, HTTP/SSE transport, and broader host-matrix expansion. Completed follow-up evidence lives in `docs/internal/development/plans/dynamic-workflows/follow-up-cell-mcp-session-serve.md`. The serve path exposes `pkg/mcp/factorysession.DiscoverTools()` through both fixture-backed and runtime-backed clients; preview install proof centers on `you.factory_session.validate_source`.
- JavaScript orchestrator simple workflow execution belongs under `pkg/orchestrators/javascript/runtime` (`Run`, `RunWithTimeout`, `Request`, `Outcome`, `Hooks`, `ChildExecutor`, `ChildExecutorFactory`). The default boundary uses embedded `github.com/dop251/goja`, injects structured `args`/`meta`, progress/state host primitives (`phase`, `log`, `workflow.log`, `workflow.artifact`, `workflow.checkpoint`, `workflow.resumeState`, `workflow.budget`), `agent.run`, `parallel`, and `pipeline` (all child work routes through `ChildExecutor`; default `FakeChildExecutor`, replaceable via `Hooks.NewChildExecutor`), and `workflow.final` (no filesystem, process, network, or shell globals), supports returned finals and `workflow.final` (with `workflow.final` taking precedence when both are present), and validates successful terminal values with `pkg/orchestrators/javascript/result.ValidateTypedValue` before `OnResult` or `OK=true` (invalid finals return `WORKFLOW_RUNTIME_INVALID_RESULT` with `[workflow.result.*]` validation context). Host primitive calls append typed ordered `Outcome.Records` (`RuntimeRecord` with phase/log/artifact/checkpoint/budget/child_dispatch payloads) suitable for later factory session event mapping; `workflow.artifact` returns deterministic `you-artifact://sessions/{session_id}/artifacts/{artifact_id}` URIs via `pkg/orchestrators/javascript/result.FormatArtifactURI` and may invoke `Hooks.OnArtifact`. `workflow.resumeState()` returns persisted checkpoint state on resumed runs and `undefined` on fresh runs so scripts can branch on restored facts instead of rerunning completed phases. The canonical beta `agent.run` argument contract lives in `pkg/orchestrators/javascript/childcontract` and contains exactly `prompt`, `label`, `preset`, `modelProvider`, `model`, and `reasoningEffort`; source validation and runtime parsing both consume that contract, while factory/session policy remains the owner of global permissions and budgets. `agent.run` resolves a Promise, routes through `ChildExecutor`, and appends queued/running/completed `child_dispatch` records with dispatch-like metadata (`dispatchId`, `promptDigest`, `providerSessionRef`, `executionMode=fake`) plus a deterministic structured child result for later `Dispatch` mapping. `parallel` accepts an array of agent run specs or async functions, pre-reserves dispatch identities in input order, enforces `policy.maxAgents` before fanout via `denyChildSlots`, validates each child request through `pkg/orchestrators/javascript/policy.ValidateChildRequest` before fake dispatch, schedules fake child execution with `policy.concurrency`, returns results in input order, and represents per-item failures with `FAILED` child results plus `child_dispatch` records (`prompt` prefixes `fail:` simulate deterministic fake child failure in tests; policy-denied children fail before dispatch records are emitted). `pipeline(items, worker, next?)` runs staged fake child work per item in input order (worker then optional next), returns per-item `{ index, item, status, stages[] }` results with stage success/failure details, passes each stage's resolved result into the next stage for the same item, and routes stage `agent.run` calls through the same `ChildExecutor` path as `agent.run` and `parallel`. Pre-execution checks call `pkg/orchestrators/javascript/validation.Validate` on the IIFE-wrapped executable source (with authored-line remapping) before `goja` execution; shape/syntax issues return `WORKFLOW_RUNTIME_PRE_EXECUTION_INVALID` and forbidden host-access patterns return `WORKFLOW_RUNTIME_DENIED_CAPABILITY` (embedding `[workflow.source.forbiddenHostAccess]` validation context) without invoking result/artifact hooks. Execution failures return `WORKFLOW_RUNTIME_SCRIPT_ERROR` (preserving JavaScript `Error.message` via `javascriptExceptionMessage`, including ordered `Outcome.Records` collected before the failing host call) or `WORKFLOW_RUNTIME_UNRESOLVED_FINAL` when no terminal value is present; context cancellation and deadline timeout return `WORKFLOW_RUNTIME_CANCELED` and `WORKFLOW_RUNTIME_TIMEOUT` via `watchContext`/`vm.Interrupt` without invoking result/artifact hooks. Child host calls enforce `allowedModels` and `allowedReasoningEfforts` through `ValidateChildRequest`; `workflow.artifact` enforces `maxArtifactBytes` before hooks or artifact records. Fixture-backed runtime tests live beside the package (`runtime/testdata/*.workflow.js`, `runtime/runtime_test.go`, `runtime/host_primitives_test.go`, `runtime/host_children_test.go`, `runtime/host_pipeline_policy_test.go`); prove cancellation, timeout, host-access denial, policy-denied child/progress primitives, invalid terminal values (functions, unresolved promises, host-path JSON), progress/state primitive records, fake `agent.run` child dispatch records, bounded-concurrency `parallel` fanout, staged `pipeline` item ordering, and replaceable child execution through `Hooks.NewChildExecutor` in focused runtime tests rather than transport surfaces. Child host primitive implementations are consolidated in `runtime/host_children.go` to stay within the `pkg-file-count` package limit.
- Dynamic workflow public REST contract repair: author changes in `api/openapi-main.yaml` and `api/components/`, normalize at `pkg/apisurface/`, implement handlers and tests in `pkg/api/`, regenerate Go and TypeScript clients with `make generate-api`, and run `make api-smoke` when feasible after public REST contract changes.
- Factory session runtime projections live in `api/components/schemas/api/FactorySession*.yaml`; map live sessions through `pkg/factorysessions/projection.go` and `pkg/factorysessions/enablement.go`, with service assembly in `pkg/service/runtime_sessions.go` and transport in `pkg/api/handlers_factory.go`.
- Deterministic fake durable session service injection lives in `pkg/factorysessionexecution/fake_service.go` (`FakeService`, `NewFakeService`, `NewFakeServiceFromContractFixtures`, `WithFakeScenarios`, `WithPersistedSessionSeeds`), scenario loading in `pkg/factorysessionexecution/fake_fixture.go`, published stable scenario identity catalog in `pkg/factorysessionexecution/fixtures/catalog.go` (`PublishedFixtureScenarios`, `FixtureScenarioIdentityFromScenario`, `FixtureScenarioProjectionHash`, `ProjectedResultReadHash`, `SyncStartResultHash`, `LifecycleControlResultHash`, `ListDispatchesResultHash`, `DispatchDetailHash`, `ListArtifactsResultHash`, `ArtifactDetailHash`, `EventReadResultHash`, `LoadFixtureScenarioIdentities`, `TypedFailureKind`, `TypedFailureIdentityFromError`, `TypedFailureHash`, `ForbiddenFixtureVocabularyTerms`), canonical session event synthesis and runtime-backed dispatch lifecycle event synthesis (`RuntimeDispatchEventInput`, `rebuildRuntimeSessionCanonicalEvents`, `appendCanonicalRuntimeDispatchLifecycleEvents`) in `pkg/factorysessionexecution/listing.go` (`BuildCanonicalSessionEvents`, `BuildCanonicalRuntimeSessionEvents`), reconnect filtering (`FilterEventsAfterReconnect`) in `pkg/factorysessionexecution/lifecycle.go` alongside dispatch-interrupt lifecycle helpers (`AppendDispatchInterruptedEvent`, `ReplayDispatchProjection`, interrupted-dispatch suppression helpers), built-in interrupted/recoverable scenario in `pkg/factorysessionexecution/fake_fixture.go`; contract fixture scenarios may include canonical `events[]` arrays (running, terminal, and `AWAITING_APPROVAL` JavaScript sessions) in `pkg/api/testdata/durable-session-contract-fixtures.json`; keep `pkg/factorysessionexecution` at the `make pkg-file-count` limit (15 counted Go files) by consolidating small runtime/interrupt helpers into existing lifecycle/service files and placing published catalog helpers and direct tests in the `pkg/factorysessionexecution/fixtures` subpackage rather than expanding the root package; select fake vs real JavaScript runtime execution through `pkg/factorysessionexecution/service.go` (`ExecutionProvider`, `NewExecutionService`, `ProjectRuntimeExecutionRecords`, interrupt test seeding helpers) and `pkg/factorysessionexecution/runtime_service.go` (`JavaScriptRuntimeService`, `NewJavaScriptRuntimeService`, `prepareStart` → `PrepareStart`, `NewDurableSessionID`, async background execution via `StartAsync` with `projectRuntimeRunningSessionState`, policy `maxRunDurationMs` timeout via `workflowRunContext`); durable start normalization and validation live in `pkg/factorysessionexecution/prepare_start.go` (`PrepareStart`, `StartPrepareContext`, `PreparedStart`); lifecycle controls live in `pkg/factorysessionexecution/control.go` (`applyRuntimeExtendedLifecycleControl`, `applyRuntimeAcceptedLifecycleControl`, `lookupControlReplay`, `storeControlReplay`) with `controlReplay` on both `FakeService` and `JavaScriptRuntimeService`; prove real-runtime sync and async start plus result projection in `pkg/factorysessionexecution/fixtures/runtime_execution_test.go`; prove runtime record projection into dispatch/artifact inspection via `ProjectRuntimeExecutionRecords` / `applyRuntimeSuccessProjection` in `pkg/factorysessionexecution/projection_consistency.go` and `pkg/factorysessionexecution/fixtures/runtime_record_projection_test.go`; select fake vs live child execution through `StartRequest.Runtime.ChildExecutorMode` (`fake` default, `live-provider` requires `JavaScriptRuntimeServiceConfig.Provider`) via `pkg/factorysessionexecution/livechild/provider.go` (`ProviderChildExecutor`, `childExecutorHooks`, `resolveChildExecutorMode`) wired into `workflowruntime.Hooks.NewChildExecutor`, with deferred CLI/MCP/website follow-ups in `docs/internal/development/plans/dynamic-workflows/follow-up-cell-live-provider-dispatch-bridge-deferred.md` and coexistence proof in `pkg/factorysessionexecution/fixtures/runtime_child_executor_coexistence_test.go`, live child dispatch in `fixtures/runtime_live_child_dispatch_test.go`, and live child failure in `fixtures/runtime_live_child_failure_test.go`; prove runtime-backed `ReadEvents` + `ReplaySessionProjection` for completed, running, sync-timeout, and async-completed sessions in `pkg/factorysessionexecution/fixtures/runtime_execution_test.go` (`JavaScriptRuntimeService` must stamp events via `BuildCanonicalRuntimeSessionEvents`, not `BuildCanonicalSessionEvents`); prove real-runtime request replay, duplicate conflict, cross-mode conflict, first-observed async/sync replay preservation, invalid wait/policy rejection, typed missing-session/source failures, and lifecycle controls in `pkg/factorysessionexecution/fixtures/runtime_boundary_test.go` and `pkg/factorysessionexecution/fixtures/runtime_lifecycle_control_test.go` with runtime start replay/reservation helpers in `pkg/factorysessionexecution/idempotency.go` (`tryReplayAsyncStart`, `tryReplaySyncStart`, `recordAsyncStartReplay`, `recordSyncStartReplay`, `reserveStartSession`); prove fake-provider non-regression through `NewExecutionService(ExecutionProviderFake)` plus recovered-lane behavioral session read coverage in `pkg/factorysessionexecution/fixtures/fake_provider_non_regression_test.go`; wire explicitly in server/test doubles and prove API consumer projections through `pkg/apisurface/factorysession/factory_session_fake_consumer_test.go`, `pkg/factorysessionexecution/fake_service_test.go` (including lifecycle unit coverage), and published-scenario identity/start/list/read/result/dispatch/artifact/event/lifecycle/typed-failure coverage in `pkg/factorysessionexecution/fixtures/*_test.go`.
- Durable factory-session apisurface mapper round trips live in `pkg/apisurface/factorysession/factory_session_mapper.go` (`OrchestratorOverrideFromAPI`, `AsyncStartResultFromAPI`, `SessionReadResultFromAPI`, `ResultReadResultFromAPI`, `DispatchSummaryFromAPI`, `DispatchDetailFromAPI`, `ArtifactSummaryFromAPI`, `ArtifactDetailFromAPI`, `DurableSessionListSummaryFromAPI`, `LifecycleControlResultFromAPI`, `ControlErrorToAPI`) with fixture/idempotent round-trip coverage in `pkg/apisurface/factorysession/factory_session_mapper_test.go`; keep request normalization on existing `StartRequestFromAPI`, `ControlRequestFromAPI`, `ResultRequestFromAPI`, and `ListSessionsRequestFromAPI` mappers and response projection on existing `*ToAPI` helpers in `factory_session_execution.go`, `factory_session_lifecycle.go`, `factory_session_projection.go`, and `factory_session_listing.go`.
- Durable factory-session execution contract lives in `api/components/schemas/api/FactorySessionExecution*.yaml`, `FactorySessionRequestedPolicy.yaml`, `FactorySessionEffectivePolicy.yaml`, `FactorySessionWorkflowSourceResolutionOrder.yaml`, `FactorySessionResolvedSourceIdentity.yaml`, and `FactorySessionDurableLifecycleStatus.yaml`; register routes in `api/openapi-main.yaml` as `POST /factory-sessions/async` and `POST /factory-sessions/sync`, document workflow source resolution order and `requestId` idempotency on those routes and execution request schemas, keep `POST /factory-sessions` and `POST /factory-sessions/{session_id}/invocations` documented as live-session compatibility APIs, add contract coverage in `assertDurableExecutionSurfaceSchemas` and `assertDurableSourceResolutionAndIdempotencySurfaceSchemas` within `pkg/api/contracttests/openapi_contract_surface_test.go`, normalize API/CLI/MCP/UI start requests through `pkg/apisurface/factorysession/factory_session_execution.go` (`StartRequestFromAPI`, `StartRequestFromCLI`, `ResultRequestFromCLI`, `EventReconnectRequestFromCLI`, `AsyncStartResponseToAPI`, `SyncStartResponseToAPI`) with CLI flag/positional/stdin resolution in `pkg/services/factory_sessions/transports/cli/sessionexecution/` (`NormalizeStartRequest`, `WriteExecutionError`, `RunSync`, `RunAsync`, `RunStatus`, `RunResult`, `RunDispatches`, `RunArtifacts`, `RunEvents`), wire mock-backed synchronous CLI starts through `you workflow run`, asynchronous starts plus status polling through `you workflow start` / `you workflow status`, durable result reads through `you workflow result`, and dispatch/artifact/event inspection through `you workflow dispatches` / `you workflow artifacts` / `you workflow events` in `pkg/cli/root.go` (calls `RunSync` / `RunAsync` / `RunStatus` / `RunResult` / `RunDispatches` / `RunArtifacts` / `RunEvents` → `factorysessionexecution.Service` with contract-fixture `FakeService` fallback; prove failure/idempotency loopback contracts in `pkg/services/factory_sessions/transports/cli/sessionexecution/failure_paths_test.go` with human/JSON `WriteExecutionError` assertions and `ListSessions` live-scope counts to ensure bad source/unsupported mode/request-id conflict paths do not create sessions); async JSON adds `requestId` and shared `GetResult` partial `resultAvailability`; sync timeout JSON adds `requestId`, `cancelOnTimeout`, and shared `GetResult` final `resultAvailability` atop `FactorySessionSyncExecutionResponse`; `FakeService.StartSync` overlays `sessionCanceledByTimeout` and cancel-submitted session state when `wait.cancelOnTimeout` is true on timed-out fixtures; result reads render `FactorySessionResult` and exit non-zero for `NOT_READY`, `UNAVAILABLE`, and `FAILED_WITH_PARTIAL` via `ResultOutcomeError`; dispatch/artifact lists render `ListFactorySessionDispatchesResponse` / `ListFactorySessionArtifactsResponse`; event polls render `EventReadResponseToAPI` arrays and map `ErrReconnectCursorNotFound` to `RECONNECT_CURSOR_NOT_FOUND`), define the shared service contract in `pkg/factorysessionexecution/` (`Service`, `StartRequest`, `AsyncStartResult`, `SyncStartResult`, `NormalizeStartRequest`, `IdempotencyTupleHash`, `CheckRequestIDReplay`, `InspectionLinksForSession`), expose the transport seam via `apisurface.DurableSessionExecutionAPI` (embedded on `apisurface.SessionAPISurface`), implement production wiring in `pkg/service/durable_session_execution.go` (`StartDurableFactorySessionAsync`, `StartDurableFactorySessionSync`, lazy `RuntimeService` with `durableProjectRoot()`), map typed execution failures through `pkg/apisurface/factorysession/factory_session_execution_errors.go` (`ExecutionErrorResponse`), and prove runtime-backed API loopback in `pkg/api/server_durable_session_execution_test.go` (inject `factorysessionexecution.RuntimeService` through `testutil.MockFactory.DurableExecutionService`; keep contract-fixture isolation in mapper/contract tests).
- Durable factory-session execution contract lives in `api/components/schemas/api/FactorySessionExecution*.yaml`, `FactorySessionRequestedPolicy.yaml`, `FactorySessionEffectivePolicy.yaml`, `FactorySessionWorkflowSourceResolutionOrder.yaml`, `FactorySessionResolvedSourceIdentity.yaml`, and `FactorySessionDurableLifecycleStatus.yaml`; register routes in `api/openapi-main.yaml` as `POST /factory-sessions/async` and `POST /factory-sessions/sync`, document workflow source resolution order and `requestId` idempotency on those routes and execution request schemas, keep `POST /factory-sessions` and `POST /factory-sessions/{session_id}/invocations` documented as live-session compatibility APIs, add contract coverage in `assertDurableExecutionSurfaceSchemas` and `assertDurableSourceResolutionAndIdempotencySurfaceSchemas` within `pkg/api/contracttests/openapi_contract_surface_test.go`, normalize API/CLI/MCP/UI start requests through `pkg/apisurface/factorysession/factory_session_execution.go` (`StartRequestFromAPI`, `StartRequestFromCLI`, `ResultRequestFromCLI`, `EventReconnectRequestFromCLI`, `AsyncStartResponseToAPI`, `SyncStartResponseToAPI`) with CLI flag/positional/stdin resolution in `pkg/services/factory_sessions/transports/cli/sessionexecution/` (`NormalizeStartRequest`, `WriteExecutionError`, `RunSync`, `RunAsync`, `RunStatus`, `RunResult`, `RunDispatches`, `RunArtifacts`, `RunEvents`), wire mock-backed synchronous CLI starts through `you workflow run`, asynchronous starts plus status polling through `you workflow start` / `you workflow status`, durable result reads through `you workflow result`, and dispatch/artifact/event inspection through `you workflow dispatches` / `you workflow artifacts` / `you workflow events` in `pkg/cli/root.go` (calls `RunSync` / `RunAsync` / `RunStatus` / `RunResult` / `RunDispatches` / `RunArtifacts` / `RunEvents` → `factorysessionexecution.Service` with contract-fixture `FakeService` fallback; prove failure/idempotency loopback contracts in `pkg/services/factory_sessions/transports/cli/sessionexecution/failure_paths_test.go` with human/JSON `WriteExecutionError` assertions and `ListSessions` live-scope counts to ensure bad source/unsupported mode/request-id conflict paths do not create sessions); async JSON adds `requestId` and shared `GetResult` partial `resultAvailability`; sync timeout JSON adds `requestId`, `cancelOnTimeout`, and shared `GetResult` final `resultAvailability` atop `FactorySessionSyncExecutionResponse`; `FakeService.StartSync` overlays `sessionCanceledByTimeout` and cancel-submitted session state when `wait.cancelOnTimeout` is true on timed-out fixtures; result reads render `FactorySessionResult` and exit non-zero for `NOT_READY`, `UNAVAILABLE`, and `FAILED_WITH_PARTIAL` via `ResultOutcomeError`; dispatch/artifact lists render `ListFactorySessionDispatchesResponse` / `ListFactorySessionArtifactsResponse`; event polls render `EventReadResponseToAPI` arrays and map `ErrReconnectCursorNotFound` to `RECONNECT_CURSOR_NOT_FOUND`), define the shared service contract in `pkg/factorysessionexecution/` (`Service`, `StartRequest`, `AsyncStartResult`, `SyncStartResult`, `NormalizeStartRequest`, `IdempotencyTupleHash`, `CheckRequestIDReplay`, `InspectionLinksForSession`), expose the transport seam via `apisurface.DurableSessionExecutionAPI` (embedded on `apisurface.SessionAPISurface`), implement production wiring in `pkg/service/durable_session_execution.go` (`StartDurableFactorySessionAsync`, `StartDurableFactorySessionSync`, lazy `RuntimeService` with `durableProjectRoot()`), map typed execution failures through `pkg/apisurface/factorysession/factory_session_execution_errors.go` (`ExecutionErrorResponse`), and prove runtime-backed API loopback in `pkg/api/server_durable_session_execution_test.go` (inject `factorysessionexecution.RuntimeService` through `testutil.MockFactory.DurableExecutionService`; keep contract-fixture isolation in mapper/contract tests).
- Durable session get/list read models live in `api/components/schemas/api/FactorySessionDurable*.yaml`, `FactorySessionDurableActionAvailability.yaml`, `FactorySessionGetResponse.yaml`, `FactorySessionListScope.yaml`, and `FactorySessionResultStatus.yaml`; extend `GET /factory-sessions` with `scope=live|persisted|all` and `GET /factory-sessions/{session_id}` with `FactorySessionGetResponse` oneOf live `FactorySession` and `FactorySessionDurableReadModel`; `FactorySessionDurableSummary` carries phase, progress, resultSummary, artifactCount, recoverable, and action availability for list rows; normalize API/CLI/MCP/UI scoped session listing through `pkg/apisurface/factorysession/factory_session_listing.go` (`ListSessionsRequestFromAPI`, `ListSessionsResponseToAPI`, `DurableSessionSummaryToAPI`), extend the shared service contract in `pkg/factorysessionexecution/` (`ListSessions`, `ListSessionsRequest`, `ListSessionsResult`, `SessionListScope`, `SessionListFilters`, `NormalizeListSessionsRequest`, `ApplySessionListScope`, `FilterPersistedSessionSummaries`, `DeduplicateLiveSessionsForAllScope`, `SortDurableSessionSummaries`, `DurableListSummaryFromSessionRead`, `LiveListSummaryFromSessionRead`), expose the transport seam via `apisurface.DurableSessionListingAPI`, implement production wiring in `pkg/service/durable_session_read.go` (`ListDurableFactorySessions`, `ListDurableExecutionSessions`, `GetDurableFactorySession`), merge workspace and durable rows in `pkg/api/handlers_durable_read.go` (`mergeScopedFactorySessionList`, `isDurableExecutionSessionID`), route `GET /factory-sessions` and durable `dur-sess-*` reads in `pkg/api/handlers_factory.go`, and prove runtime-backed API loopback in `pkg/api/server_durable_session_read_test.go` (inject `factorysessionexecution.RuntimeService` through `testutil.MockFactory.DurableExecutionService`).
- JavaScript checkpoint refs and internal checkpoint bundles live in `api/components/schemas/data-models/FactoryArtifact*.yaml`, `api/components/schemas/api/FactorySessionLiveResult.yaml`, `api/components/schemas/api/FactorySessionPartialResult.yaml`, and `api/components/schemas/events/payloads/JavaScriptCheckpointRefEventPayload.yaml`; keep raw VM bodies in `pkg/factory/contracts/factory_runtime.go` and project public refs through `pkg/factorysessions/checkpoints.go`, with world replay in `pkg/factory/projections/world_state.go`.
- Durable session result retrieval lives in `api/components/schemas/api/FactorySessionResult.yaml`, `FactorySessionResultMode.yaml`, `FactorySessionResultAvailabilityDetail.yaml`, and `FactorySessionResultStatus.yaml`; extend `GET /factory-sessions/{session_id}/results` with `mode=final|partial` and `includeArtifacts=true|false`, keep live JavaScript terminal reads on `GET /factory-sessions/{session_id}/result` as `FactorySessionLiveResult`, normalize API/CLI/MCP/UI result reads through `pkg/apisurface/factorysession/factory_session_projection.go` (`ResultRequestFromAPI`, `ResultRequestFromCLI`, `ResultResponseToAPI`), extend the shared service contract in `pkg/factorysessionexecution/` (`GetResult`, `ProjectResultRead`, `ResultRequest`, `ResultReadResult`, `NormalizeResultRequest`, `ValidateResultMatchesSessionRead`, `ValidateResultMatchesEventProjection`), expose the transport seam via `apisurface.DurableSessionProjectionAPI`, implement production wiring in `pkg/service/runtime_sessions.go` (`GetDurableFactorySessionResult`), route durable `dur-sess-*` reads in `pkg/api/handlers_factory.go` via `requireDurableSessionResultGetter`, and prove runtime-backed API loopback in `pkg/api/servertests/server_durable_session_result_test.go` (inject `factorysessionexecution.JavaScriptRuntimeService` through `testutil.MockFactory.DurableExecutionService`; mapper fixture coverage stays in `pkg/apisurface/factorysession/factory_session_projection_test.go`).
- Durable session dispatch and artifact retrieval lives in `api/components/schemas/api/FactorySessionDispatchSummary.yaml`, `ListFactorySessionDispatchesResponse.yaml`, `FactorySessionArtifactSummary.yaml`, `FactorySessionArtifactDetail.yaml`, `FactorySessionArtifactRetrievalRef.yaml`, and `ListFactorySessionArtifactsResponse.yaml`; extend shared data models in `api/components/schemas/data-models/FactoryDispatch.yaml` and `FactoryArtifact.yaml`, add `GET /factory-sessions/{session_id}/dispatches`, `GET /factory-sessions/{session_id}/dispatches/{dispatch_id}`, `GET /factory-sessions/{session_id}/artifacts`, and `GET /factory-sessions/{session_id}/artifacts/{artifact_id}` with contract coverage in `assertDurableSessionDispatchArtifactSurfaceSchemas` plus empty-list fixture validation and typed `NOT_FOUND` generated JSON in `generated_contract_durable_session_test.go`, generated list/detail type alignment in `openapi_contract_authoring_test.go` (`TestGeneratedDispatchArtifactContracts_SessionReadListDetailTypesAgreeWithOpenAPI`), normalize API/CLI/MCP/UI dispatch and artifact reads through `pkg/apisurface/factorysession/factory_session_projection.go` (`ListDispatchesResponseToAPI` always emits required `dispatches: []` for empty lists, `DispatchDetailResponseToAPI`, `ListArtifactsResponseToAPI` always emits required `artifacts: []` for empty lists, `ArtifactDetailResponseToAPI` including summary `RetrievalRef` fallback to `contentRef`), prove empty-list and retrieval-ref mapper behavior in `factory_session_projection_test.go`, extend the shared service contract in `pkg/factorysessionexecution/` (`ListDispatches`, `GetDispatch`, `ListArtifacts`, `GetArtifact`, `DispatchSummary`, `DispatchDetail`, `ArtifactSummary`, `ArtifactDetail`, `ValidateDispatchListMatchesSessionProgress`), expose the transport seam via `apisurface.DurableSessionProjectionAPI`, route `GET /factory-sessions/{session_id}/dispatches` and `GET /factory-sessions/{session_id}/dispatches/{dispatch_id}` for `dur-sess-*` through `pkg/api/handlers_factory.go` (`isDurableExecutionSessionID`, `requireDurableSessionDispatchReader`, `writeDurableSessionReadError` including `ErrDispatchNotFound`) into `FactoryService.ListDurableFactorySessionDispatches` / `GetDurableFactorySessionDispatch` / `testutil.MockFactory.DurableExecutionService`, route `GET /factory-sessions/{session_id}/artifacts` and `GET /factory-sessions/{session_id}/artifacts/{artifact_id}` for `dur-sess-*` through `requireDurableSessionArtifactReader` (`writeDurableSessionReadError` including `ErrArtifactNotFound`) into `FactoryService.ListDurableFactorySessionArtifacts` / `GetDurableFactorySessionArtifact` / `testutil.MockFactory.DurableExecutionService`.
- Durable session event replay for projection consistency lives on `GET /factory-sessions/{session_id}/events`; normalize reconnect cursors through `pkg/apisurface/factorysession/factory_session_projection.go` (`EventReconnectRequestFromAPI`, `EventReadResponseToAPI`, `FactoryEventStreamFromReadResult` with a closed live channel so history-only durable replays terminate SSE cleanly), extend the shared service contract in `pkg/factorysessionexecution/` (`ReadEvents`, `EventReconnectRequest`, `EventReadResult`, `SessionProjectionEventKinds`, `BuildCanonicalSessionEvents`, `FilterEventsAfterReconnect`, `ErrReconnectCursorNotFound`), expose the transport seam via `apisurface.DurableSessionProjectionAPI`, implement production wiring in `pkg/service/runtime_sessions.go` (`ReadDurableFactorySessionEvents`), route durable `dur-sess-*` reads in `pkg/api/handlers_events.go` via `requireDurableSessionEventsReader` while preserving live Petri session streams on the existing `SessionAPI` path, validate fixture `events[]` through `assertDurableSessionScenarioEventFixtures` in `pkg/api/contracttests/generated_contract_durable_session_test.go`, prove runtime-backed API loopback in `pkg/api/servertests/server_durable_session_events_test.go`, and keep the real-backend API slice contract-aligned through `assertDurableSessionEventSurfaceSchemas` and `assertRealBackendSessionAPISliceRoutes` (reconnect cursor parameters, `400 BadRequest` invalid-cursor responses on `/events` and session-scoped `/factory-sessions/{session_id}/events`, `FactoryEvent` SSE schema, lifecycle-control routes in scope, empty deferred-route families in `assertDeferredRealBackendSessionRouteFamilies`, no workflow-run routes).
- Real-backend Factory Session API parity and dispatch/artifact regression coverage live in `docs/internal/development/plans/dynamic-workflows/follow-up-cell-real-backend-api-session-parity-deferred.md` plus `pkg/api/servertests/server_durable_session_lifecycle_control_test.go` (`TestRealBackendFactorySessionRoutes_LifecycleControlsAreImplemented`), shared durable session lifecycle HTTP helpers in `pkg/api/servertests/server_durable_session_read_test.go`, `pkg/api/servertests/server_durable_session_dispatch_artifact_test.go`, and lifecycle read-parity cases in `pkg/api/servertests/server_durable_session_execution_test.go` (`TestLifecycleControls_*PreservesInspectablePartialStateAcrossReadSurfaces` proves status/result/dispatch/artifact/event reads survive accepted and rejected lifecycle controls; `TestFactoryService_LifecycleControls_PreserveReadSurfacesAfterPauseResume_HTTPUsesProductionRuntime` proves production HTTP wiring): the completed slice covers async/sync start, scoped list/get, final result read, canonical event replay, durable dispatch/artifact list/detail reads, and lifecycle controls; dashboard website inspection and MCP host installation (`follow-up-cell-mcp-session-serve.md`) remain follow-up cells. Prove implemented lifecycle-route posture through `server_durable_session_lifecycle_control_test.go` and `assertRealBackendSessionAPISliceRoutes`; prove dispatch/artifact list/detail HTTP behavior and non-mutation parity in `server_durable_session_dispatch_artifact_test.go`; prove live-provider child dispatch progress through shared durable HTTP reads in `pkg/api/servertests/server_durable_session_interrupt_dispatch_test.go` and `pkg/api/servertests/server_durable_session_events_test.go` by injecting `JavaScriptRuntimeService` with `ChildExecutorModeLive` plus `SmokeLiveChildProvider()` (or a blocking test provider for in-flight `RUNNING` polling) through `testutil.MockFactory.DurableExecutionService` and asserting `FactorySessionDispatchSummary` / `FactoryDispatch` `status`, `javascript.executionMode`, `providerSessionRefs`, `statusTransitions`, session `progress` alignment, and canonical `DISPATCH_QUEUED` / `DISPATCH_RECONCILED` event replay alignment via `assertAPILiveProviderDispatchLifecycleEventsAlignWithReads` in `server_durable_session_events_test.go`; prove live-provider provider-session correlation and bridged-child artifact lineage through the same file by asserting `LoadableProviderSessionRef` vocabulary (`provider`, `kind=session_id`, `id`), `FactorySessionArtifactSummary` / `FactorySessionArtifactDetail` `dispatchId` linkage to `dispatch-1`, `CHILD_RESULT` artifact kind, session `artifactRefs`, and dispatch `outputArtifactIds` / `artifactIds` alignment with artifact list/detail reads, plus fake-child coexistence via separate runtime-backed servers in `TestLiveProviderAndFakeChildSessions_APIPreserveDistinctProviderAndArtifactProjections`; prove failed live-provider bridged-child typed failure detail through `TestLiveProviderChildDispatch_RuntimeBackedAPIProjectsFailedBridgedChildWithTypedFailureDetail` by reusing `newAPILifecycleFailingChildRuntimeService` / `startRuntimeBackedFailedSessionWithDispatch` from `server_durable_session_read_test.go` and asserting shared `FactoryDispatchFailureDetail` (`permanent_bad_request`, `terminal`, message), empty artifact surfaces, queued→running→failed `statusTransitions`, and `SESSION_COMPLETED` event alignment with FAILED session/dispatch reads; prove fake-child and simple-final durable transport regression after the live-provider lane through `TestFakeChildDurableSessionReads_APIPreservesShippedTransportSemantics` (full session/result/dispatch/artifact/event snapshot, rejected lifecycle control preserves read surfaces, no live-provider marker leakage, `fixtures.ForbiddenFixtureVocabularyTerms` guard) and `TestSimpleFinalDurableSessionReads_APIPreservesFinalResultWithoutChildDispatches` in `server_durable_session_lifecycle_control_test.go`; runtime child artifact projection uses shared `FactoryArtifactKind` `CHILD_RESULT` from `childArtifactFromDispatch` in `pkg/factorysessionexecution/service.go` and `ProviderSessionRef.kind=session_id` in `dispatchSummaryFromChildRecord`; prove lifecycle controls do not regress durable read/reconnect or dispatch/artifact inspection links in the consolidated execution/servertest files (keep each package at <=15 files by merging lifecycle/deferred/read-parity servertests; keep owned Go files/functions within `cmd/backendsizecheck` limits by splitting oversized lifecycle tests/helpers).
- Durable session lifecycle controls live in `api/components/schemas/api/FactorySessionLifecycleControl*.yaml`, `FactorySessionApproveRequest.yaml`, `FactorySessionRetryDispatchRequest.yaml`, and `FactorySessionInterruptDispatchRequest.yaml`; add `POST /factory-sessions/{session_id}/approve`, `/pause`, `/resume`, `/cancel`, `/terminate`, `/retry-dispatch`, and `/interrupt-dispatch` with typed `FactorySessionLifecycleControlResponse` outcomes (`ACCEPTED`, `NO_OP`, `INVALID_STATE`, `TERMINAL_SESSION`, `CONFLICT`), `FactorySessionLifecycleControlLinks` for post-control inspection of results/dispatches/artifacts, contract coverage in `assertDurableSessionLifecycleControlSurfaceSchemas`, conflict response `FactorySessionLifecycleControlConflict`, normalize API/CLI/MCP/UI read and control requests through `pkg/apisurface/factorysession/factory_session_lifecycle.go` (`SessionReadResponseToAPI`, `ControlRequestFromAPI`, `ApproveRequestFromAPI`, `RetryDispatchRequestFromAPI`, `InterruptDispatchRequestFromAPI`, `LifecycleControlResponseToAPI`, `ControlErrorToAPI`, `LifecycleControlSuccessStatus`, `LifecycleControlErrorResponse`) and extend the shared service contract in `pkg/factorysessionexecution/` (`GetSession`, `Pause`, `Resume`, `Cancel`, `Terminate`, `Approve`, `RetryDispatch`, `InterruptDispatch`, `SessionReadResult`, `LifecycleControlResult`, `EvaluateLifecycleControl`, `EvaluateInterruptDispatchControl`, `ControlIdempotencyTupleHash`, `lookupControlReplay`, `storeControlReplay`, `LifecycleControlLinksForSession`) with `controlReplay` on both `FakeService` and `JavaScriptRuntimeService`, expose the transport seam via `apisurface.DurableSessionLifecycleAPI`, implement production wiring in `pkg/service/runtime_sessions.go` (`PauseDurableFactorySession`, `ResumeDurableFactorySession`, `CancelDurableFactorySession`, `TerminateDurableFactorySession`, `ApproveDurableFactorySession`, `RetryDurableFactorySessionDispatch`, `InterruptDurableFactorySessionDispatch` delegating through `durableExecutionService()`), route `dur-sess-*` cancel/terminate/pause/resume/approve/retry-dispatch/interrupt-dispatch through `pkg/api/handlers_common.go` (`writeLifecycleControlSuccess` delegating success HTTP status to `LifecycleControlSuccessStatus` for both durable and live handlers) + `handlers_factory.go` with `testutil.MockFactory` durable lifecycle delegation for API tests in `pkg/api/servertests/server_durable_session_lifecycle_control_test.go` and production `FactoryService` coverage consolidated into `pkg/service/runtime_session_runtime_test.go`; fixture-backed `FakeService` scenarios cover approve and retry-dispatch when runtime-backed guarded approvals or retryable dispatches are unavailable; prove request normalization and error shaping in `factory_session_lifecycle_test.go` and `factory_session_mapper_test.go`; prove interrupt-before-completion and late-result suppression regressions through `pkg/factorysessionexecution/interrupt_dispatch_race_test.go`, `pkg/factorysessionexecution/fixtures/interrupt_dispatch_race_test.go`, and `TestInterruptFactorySessionDispatch_LateResultAfterInterruptSuppressedFromNormalRouting` in `server_durable_session_lifecycle_control_test.go` (seed running dispatches via `SeedRuntimeSessionWithRunningDispatch`, simulate terminal late child completion via `ApplyRuntimeTerminalOutcomeForTests`).
- Runtime-facing public FactoryEvent kind inventory and explicit non-public exclusions live in `pkg/factory/events/kinds/` (`PublicEmittableFactoryEventKinds`, `ExcludedNonPublicFactoryEventKinds`, `ContractOnlyFactoryEventKinds`); extend `knownRuntimeEmissionAnchors` in `inventory_test.go` whenever a new runtime emission path appends to canonical factory event history, keep retired vocabulary plus response-stream internals in the exclusion list with reviewable evidence rather than silent omission, and classify OpenAPI-only public kinds without runtime emission in `ContractOnlyFactoryEventKinds` with evidence.
- Bidirectional runtime↔OpenAPI FactoryEvent kind parity lives in `pkg/factory/events/kinds/parity.go` (`CompareFactoryEventKindParity`, `ValidateFactoryEventKindParity`); prove named runtime-only and contract-only failures plus explicit exclusion/classification coverage in `parity_test.go` without touching the OpenAPI semver comparator package or SSE transport behavior.
- Canonical FactoryEvent envelope ownership and detached unknown-payload round-trip coverage live in `pkg/factory/contracts/factory_events.go` and `factory_snapshot_test.go`; live and durable streams carry that domain envelope, while generated union discriminator round-trip coverage remains at the HTTP contract/test boundary in `pkg/transports/http/contracttests/factory_event_union_support_test.go` using `pkg/transports/http/testdata/canonical-event-vocabulary-stream.json`.
- Authored `FactoryEvent` type→payload parity uses `api/components/schemas/events/FactoryEvent.yaml` `discriminator.propertyName: type` with one mapping entry per `FactoryEventType` enum value; prove enum coverage, orphan keys, duplicate payload schemas, and payload oneOf alignment through `pkg/factory/events/kinds/openapi_mapping.go` plus `assertFactoryEventTypePayloadDiscriminator` in `pkg/transports/http/contracttests/openapi_contract_events_test.go`.
- Gap-closed runtime↔OpenAPI FactoryEvent kind parity is proven by `ValidateBundledFactoryEventKindParity` in `pkg/factory/events/kinds/parity.go` (zero runtime-only and zero unexplained contract-only kinds); wire the bundled contract gate through `assertPublicFactoryEventKindParityIsClosed` in `pkg/transports/http/contracttests/openapi_contract_events_test.go` and keep classified contract-only kinds (`ContractOnlyFactoryEventKinds`) explicit rather than inventing runtime emission paths.
- Orchestrator-aware factory events live in `api/components/schemas/events/payloads/JavaScriptPhaseChangeEventPayload.yaml` and `ArtifactCreatedEventPayload.yaml`; keep Petri marking progress on `WORK_STATE_CHANGE`, JavaScript workflow progress on `JAVASCRIPT_PHASE_CHANGE`, and replay orchestrator projections through `pkg/factory/projections/world_state.go` plus `pkg/factory/projections/world_view_runtime.go`.
- Session lifecycle factory events live in `api/components/schemas/events/payloads/SessionStartedEventPayload.yaml`, `SessionResultUpdatedEventPayload.yaml`, `SessionCompletedEventPayload.yaml`, `OrchestratorPhaseChangedEventPayload.yaml`, `OrchestratorCheckpointWrittenEventPayload.yaml`, `DispatchQueuedEventPayload.yaml`, `DispatchInterruptedEventPayload.yaml`, and `DispatchReconciledEventPayload.yaml`; keep shared identity and ordering on `FactoryEventContext` (`sessionId`, `sessionSequence`, `orchestratorKind`, `orchestratorDialect`, `phaseId`, `phaseName`, `checkpointId`, `dispatchId`), use `FactoryEventSessionResultStatus` for event payloads (distinct from durable read-model `FactorySessionResultStatus`), and prove contract parity through `pkg/transports/http/contracttests/` plus `ui/src/api/events/types.test.ts`.
- Session bracket runtime emission and replay live in `pkg/factory/events/event_history_session_lifecycle.go`, `pkg/factory/runtime/factory.go`, and `pkg/factory/projections/world_state_session_lifecycle.go`; prove successful and failed-with-partial reconstructability through `pkg/factory/projections/projectiontests/session_lifecycle_event_replay_test.go` and `pkg/factory/events/event_history_session_lifecycle_test.go`.
- Live workspace pause/resume on the shared `POST /factory-sessions/{session_id}/pause` and `/resume` routes route non-`dur-sess-*` session ids through `pkg/api/handlers_common.go` (`handleLiveLifecycleControl`) into `apisurface.SessionAPI` (`PauseLiveFactorySession`, `ResumeLiveFactorySession` on `pkg/service/live_session_lifecycle_control.go`), map Petri `FactoryState` to shared lifecycle vocabulary via `factorysessionexecution.LifecycleStatusFromFactoryRuntimeState`, evaluate outcomes with `EvaluateLifecycleControl`, and shape responses through `factorysession.LiveLifecycleControlLinksForSession` plus `LifecycleControlErrorResponse` (including `apisurface.ErrFactorySessionNotFound`).
- Live session lifecycle-control status reads derive `lifecycleControlStatus` from canonical `SESSION_PAUSED` / `SESSION_RESUMED` replay via `factorysessionexecution.ProjectedLifecycleControlStatus` and `EngineStateSnapshot.LifecycleControlStatus` populated in `pkg/factory/runtime/factory.go`; project the field through `pkg/factorysessions/projection.go` on `FactorySessionRuntime` and `pkg/api/handlers_events.go` on `StatusResponse`.
- Live session pause/resume diagnostics emit redaction-safe service logs and `runtime.lifecycle_control` metrics from `pkg/service/live_session_lifecycle_diagnostics.go` via `observeLiveLifecycleControl` in `pkg/service/live_session_lifecycle_control.go`; runtime transition and post-resume buffered drain logs live in `pkg/factory/runtime/factory.go` with drain observation wired through `engine.WithResultBufferDrainObserver` in `pkg/factory/engine/options.go`.
- Orchestrator phase/checkpoint runtime emission lives in `pkg/factory/events/event_history_orchestrator_progress.go`; replay projection lives in `pkg/factory/projections/world_state_session_lifecycle.go` alongside session bracket reducers; prove phase history, latest checkpoint ref, and checkpoint warnings through `pkg/factory/projections/projectiontests/orchestrator_event_replay_test.go` and `pkg/factory/events/event_history_orchestrator_progress_test.go`.
- Dispatch queue/interruption/reconciliation, artifact-created runtime emission, and canonical stream reconnect replay live in `pkg/factory/events/event_history_dispatch_lifecycle.go` (`BuildCanonicalReconnectReplay`). Runtime preflight and projection callers pass `FactoryEventHistory.CanonicalEvents` directly; generated conversion happens only when a transport response is assembled. Dispatch lifecycle replay projection lives in `pkg/factory/projections/world_state_dispatch.go` (project `DISPATCH_INTERRUPTED` as dispatch status `INTERRUPTED` and suppress non-replayed `DISPATCH_RECONCILED` after interruption); runtime terminal projection preserves interrupted dispatch state and suppresses late child completion routing through `applyTerminalRuntimeProjection` in `pkg/factorysessionexecution/projection_consistency.go`; when that terminal merge changes the durable session/result status, regenerate canonical runtime session events before re-merging preserved `DISPATCH_INTERRUPTED` events so session-level replay does not drift back to `SUCCEEDED`/final after interruption. Prove JavaScript dispatch lifecycle plus Petri dispatch-request compatibility through `pkg/factory/projections/projectiontests/dispatch_lifecycle_event_replay_test.go` and `pkg/factory/events/event_history_dispatch_lifecycle_test.go`, and prove interrupt/late-result race observability through `pkg/factorysessionexecution/interrupt_dispatch_race_test.go`.
- Event stream reconnect query params live in `api/components/parameters/AfterEventId.yaml`, `api/components/parameters/AfterSequence.yaml`, and `pkg/api/handlers_events.go`; prove cursor filtering and reconnect replay reconstruction through `pkg/factory/events/event_reconnect_test.go`, `pkg/factory/projections/projectiontests/session_reconnect_replay_test.go`, and `pkg/api/servertests/server_dashboard_events_test.go`.
- B08 session-scoped FactoryEvent SSE behavior integration reuses deterministic live-session fixtures and a bounded-timeout harness in `pkg/transports/http/servertests/factorysessionsse/fixtures.go` (`NewFactorySessionSSEFixture`, stable retained event ids/sequences plus `context.sessionSequence` for session-scoped `after_sequence` reconnect, buffered live channel via `PublishLive`) and `pkg/transports/http/servertests/factorysessionsse/harness.go` (`NewFactorySessionSSEHarness`, `Open`, `ReadNextEvent`, `TryReadNextEvent` fail-closed on timeout, `AssertConnectionKeepAlive`, `TryWaitForKeepalive` for idle `Connection: keep-alive` and optional SSE comment traffic, `GetSessionEvents`, `ProbeRecovery` for typed error and JSON reconnect-probe coverage); prove fixture/harness wiring in `factorysessionsse/fixtures_test.go`. Initial-stream retained-then-live ordering and SSE handshake headers are proved in `factorysessionsse/initial_stream_test.go`; valid reconnect suffix behavior for `after_event_id` and `after_sequence` is proved in `factorysessionsse/reconnect_test.go`; invalid/expired cursor SSE `BAD_REQUEST` responses and JSON `CURSOR_STALE` / `STREAM_READY` reconnect probes are proved in `factorysessionsse/invalid_cursor_test.go`; idle keepalive (`Connection: keep-alive` plus open-connection idle without new `FactoryEvent` kinds) and post-keepalive live continuation are proved in `factorysessionsse/keepalive_test.go`; unknown-session typed `NOT_FOUND` responses without `~default` or other-session SSE fallback and JSON `UNKNOWN_SESSION` reconnect probes distinct from `CURSOR_STALE` are proved in `factorysessionsse/unknown_session_test.go`. Authored SSE operation metadata alignment is proved through observable HTTP/SSE behavior in those focused suites plus `assertDurableSessionEventSurfaceSchemas` in `pkg/transports/http/contracttests/openapi_contract_events_test.go` (not source-scanning meta tests). `testutil.MockFactory.SubscribeFactoryEvents` must copy `BackendScopeID`, `LogicalSessionKeyID`, and `FactorySessionID` from nested `FactoryEventStream` fixtures so session handshake headers reach HTTP integration tests. Keep response-event SSE fixtures in `server_factory_session_orchestrator_test.go` separate from canonical FactoryEvent stream coverage.
- Factory Session event stream route wording lives on `GET /factory-sessions/{session_id}/events` in `api/openapi-main.yaml`; the canonical session-scoped operation (`getEventsBySessionId`) carries stable inventory identity via `x-doc-id: agent-factory/api/factory-session-events` and structured operation prose for OpenAPI 3.0.3 non-structural SSE semantics (ordering, reconnect cursors, identity handshake headers, keepalives, replay bounds, and `cursor_stale` recovery). Mirror canonical guidance in `docs/reference/sessions.md` (event stream lifecycle section) and `docs/reference/orchestrators.md` (Factory Event Stream section); run `make docs-reference-smoke` after reference-topic edits. Prove authored wording through `assertDurableSessionEventSurfaceSchemas` in `pkg/transports/http/contracttests/openapi_contract_events_test.go` (asserts `text/event-stream`, `x-event-schema`, reconnect cursor parameter refs, typed 400/404/500 responses, non-empty `x-doc-id`, and reviewer-verifiable SSE lifecycle prose fragments), then run `make generate-api` so bundled OpenAPI and generated Go/TypeScript clients stay aligned.
- Cross-surface lifecycle exposure lives in `ui/src/api/events/api.ts` (reconnect cursors), `ui/src/features/dashboard/hooks/event-stream/useFactoryEventStream.ts`, `ui/src/features/timeline/state/timeline/replaySessionLifecycle.ts`, `ui/src/features/timeline/state/timeline/replayOrchestratorProgress.ts`, `ui/src/features/timeline/state/timeline/replayDispatchLifecycle.ts`, `pkg/cli/session/show.go`, `pkg/cli/session/lifecycle_control.go`, and `pkg/cli/session/lifecycle_control_human.go` (`you session pause` / `you session resume`); document API/CLI/dashboard/MCP semantics in `docs/reference/sessions.md`. Dashboard event traffic must stay on `GET /factory-sessions/{session_id}/events` through `openFactoryEventStream` / `factorySessionScopedPath` with the resolved UUID `factorySessionID` from `streamIdentity` (`useFactoryEventStream.resolveStreamSessionID` via `normalizeStreamDerivedCacheIdentity`); prove initial load, reconnect cursors (`after_event_id`, `after_sequence`), and session tab switches in `ui/src/api/events/api.component.test.ts`, `ui/src/features/dashboard/hooks/event-stream/useFactoryEventStream*.test.tsx`, `ui/src/features/dashboard/hooks/useDashboardSnapshot.test.tsx`, and the feature-owned dashboard session component tests. Canonical session-scoped live and durable event behavior is proven in `TestSessionScopedLiveGetEvents_*` within `server_dashboard_events_test.go` and `pkg/api/servertests/server_durable_session_events_test.go` (`TestGetFactorySessionEvents_*`). Durable fixture inspection `links.events` values must stay on `/factory-sessions/{session_id}/events` via `assertDurableSessionFixtureInspectionEventLinksAreSessionScoped` in `pkg/api/contracttests/generated_contract_durable_session_test.go`. Dashboard/runtime smokes use `support.DefaultSessionEventsURL` / `openDefaultSessionFactoryEventHTTPStream` in `tests/functional/runtime_api/events_test.go`, `tests/functional/runtime_api/functional_server_test.go` (`OpenDashboardStream`), `internal/releasesmoke/harness.go`, and `ui/scripts/release-dashboard-smoke.mjs`.
- Dashboard session lifecycle replay/reconnect coverage uses shared canonical pause/resume fixtures in `ui/src/testing/session-lifecycle-replay-fixtures.ts`; prove reducer projection in `replaySessionLifecycle.test.ts` and `projectRuntime.test.ts`, timeline-store rebuild in `factoryTimelineStore.operations.test.ts`, and streamed composer/reconnect survival in `useDashboardSnapshot.test.tsx`. Live `you session pause` / `you session resume` controls append `SESSION_LIFECYCLE_CONTROL` events (not only legacy `SESSION_PAUSED` / `SESSION_RESUMED`); replay fixtures include `canonicalSessionLifecycleControlReplayEvents` and `replaySessionLifecycle.ts` maps accepted `PAUSE` / `RESUME` payloads into bracket `lifecycle_control_status`, `paused_at`, and `resumed_at`.
- Dashboard session lifecycle non-success coverage belongs in focused factory-session-detail panel suites under `ui/src/features/factory-session-detail/components/panel-behavior/` (`factory-session-detail-panel.baseline.test.tsx` for loading, `factory-session-detail-panel.non-success-lifecycle.test.tsx` for `404` not-found and API error states via `useFactorySessionDetail`, `factory-session-detail-panel.dispatch-drilldown.test.tsx` for dispatch drilldown loading/replacement/boundary states, `factory-session-detail-panel.dispatch-detail.test.tsx` for durable dispatch detail success/failure/warning payload rendering) with feature-local fetch fixtures under `ui/src/features/factory-session-detail/components/test-support/factory-session-detail-panel.*-fixtures.ts`; the monolithic `factory-session-detail-panel.test.tsx` was removed and no blanket `noExcessiveLines` ignores remain on the split suites. Prove the split pass with `cd ui && npm exec vitest run src/features/factory-session-detail/components/panel-behavior/` plus `npm run typecheck`. Dashboard shell no longer renders a session-lifecycle banner; prove absence for active sessions in `ui/src/features/dashboard/components/dashboard-screen.test.tsx` and `ui/src/features/dashboard/public/dashboard-screen.public.smoke.test.tsx`.
- CLI live-and-durable pause/resume lifecycle controls belong in `pkg/cli/session/lifecycle_control.go` (`Pause`, `Resume`, `LifecycleControlConfig`, `LifecycleControlRejectedError`) with operator-facing human lines in `pkg/cli/session/lifecycle_control_human.go`; wire `you session pause [session-id]` / `you session resume [session-id]` in `pkg/transports/cli/root_workflow.go`. POST to `sessionpath.ScopedPath("/pause"|"/resume", sessionID)` against the same `--server` base URI as workflow status; omit `session-id` to target `~default`, and allow named live session ids plus `dur-sess-*` durable ids so the CLI matches the shared live-session lifecycle API surface. Decode `FactorySessionLifecycleControlResponse` on HTTP 200/202/409, map 404 to not-found errors, and keep unreachable-host transport failures separate from typed lifecycle outcomes. Human output distinguishes paused/resumed, already-paused/already-running, invalid-state, not-found, and unreachable-host outcomes; global `--json` emits the API-shaped lifecycle-control response unchanged. Prove accepted/no-op/invalid-state/not-found/unreachable plus default-session and named-live-session routing, human wording, command help, and JSON outcome parity in `pkg/cli/session/lifecycle_control_test.go` and `pkg/cli/root_session_test.go`; prove CLI/API equivalence on the same lifecycle-control REST surface for default live, named live, and durable sessions in `pkg/cli/session/lifecycle_control_equivalence_test.go`. Required verification for this surface: `go test ./pkg/cli/...`, `go test ./pkg/api/servertests/... -run LifecycleControl`, and the broader repository lane `make test`.
- Durable Factory Session dispatch inspection over the shared HTTP session command surface belongs in `pkg/cli/session/dispatches.go` (`Dispatches`, `DispatchesConfig`) with wiring in `pkg/transports/cli/root_workflow.go` (`you session dispatches [session-id]`). GET `sessionpath.ScopedPath("/dispatches", sessionID)` against the same `--server` base URI as `session show` and `session resume`; require `dur-sess-*` ids, decode `ListFactorySessionDispatchesResponse`, and keep human output on FactorySession/Dispatch vocabulary. Durable `session show` rendering for `dur-sess-*` ids lives in `pkg/cli/session/show.go` alongside live session reads. Runtime-backed resume continuity smokes in `pkg/cli/session/smoke/resume_smoke_test.go` should compare pre-resume and post-resume `session show` progress plus `session dispatches` parity to prove completed child dispatches are not replayed as new work. Runtime-backed invalid resume smokes in the same file should assert typed `TERMINAL_SESSION` / `NO_OP` lifecycle-control outcomes through `session resume` plus unchanged `session show` reads for the same `dur-sess-*` id instead of opaque transport failures. Lane `dynamic-workflows-cell-cli-resume-smoke` scope and deferrals live in `follow-up-cell-cli-resume-smoke-deferred.md`; prove additive non-resume CLI session create/show/list/lifecycle-control regression and shared-surface scope boundaries in `pkg/cli/session/smoke/resume_non_regression_test.go`. Keep `pkg/cli/session` at or below the `make pkg-file-count` cap of 15 owned Go files by moving resume smokes into `pkg/cli/session/smoke` and merging durable show/dispatches unit tests into `show_test.go` when the parent package grows. Keep generic CLI response-stream renderer work in `pkg/factorysessions/responsestream` out of scope for this lane.
- Orchestrator-aware public surfaces: extend `ui/src/api/factory-sessions/api.ts` with `getFactorySession` / result reads, render dashboard runtime detail through `ui/src/features/factory-session-detail/`, and expose the `factory-session` bento widget from `ui/src/features/bento/hooks/dashboardLayoutSchema.ts`.
- Guarded dashboard bootstrap now depends on `GET /factory-sessions/{session_id}` in addition to `/factory-sessions`, `/factory-sessions/{session_id}/factory`, and `/factory-sessions/{session_id}/events`. When API contract or bootstrap sequencing changes touch that preflight route, keep browser/full-stack harnesses aligned in `ui/integration/browser-test-harness.mjs` and shared dashboard fetch doubles aligned in `ui/src/testing/app-shell-session-preflight-test-utils.ts` or the app can stall in preflight loading/error states and burn browser-lane timeouts.
- The bounded operator proof for durable JavaScript session verification also belongs in `docs/reference/sessions.md`. When a change affects the reusable validate/status/dispatch-artifact-event/dashboard/lifecycle comparison path, update that reference topic instead of creating a separate lane-local smoke guide.
- Orchestrator-aware public surfaces: extend `ui/src/api/factory-sessions/api.ts` with `getFactorySession` / result reads, normalize durable `FactorySessionDurableReadModel` GET responses into shared `FactorySession` runtime shape through `ui/src/api/factory-sessions/normalize-session-get.ts` (`normalizeFactorySessionGetResponse`, `runtimeStatusFromDurableLifecycle`, `scriptStatusFromDurableLifecycle`), add durable dispatch/result inspection adapters in `ui/src/api/factory-sessions/api-durable-inspection.ts` (`listFactorySessionDispatches`, `getFactorySessionDurableResults`) with summary/result projection helpers in `ui/src/api/factory-sessions/normalize-durable-inspection.ts` (`dispatchSummariesToFactoryDispatches`, `resultSurfacesFromDurableReadModel`, `resultSurfacesFromDurableResult`, `isDurableJavaScriptSession`), and keep shared dispatch-summary inspection fields aligned with full dispatch detail when durable JavaScript metadata expands (for example `FactorySessionDispatchSummary.javascript` must be projected through `pkg/factorysessionexecution`, `pkg/apisurface/factorysession`, generated OpenAPI types, fixture catalogs, and the website durable adapter together). Render dashboard runtime detail through `ui/src/features/factory-session-detail/` (customer-facing runtime labels in `messages/factory-session-runtime-display.ts`, durable dispatch/artifact/result enrichment in `hooks/use-factory-session-detail.ts`, loading/missing/error via shared `StatusNotice`, Storybook browser verification in `components/factory-session-detail-panel.stories.tsx` with `tags: ["test"]`, and bounded regression coverage in `components/panel-behavior/factory-session-detail-panel.dispatch-detail.test.tsx` so dispatch-detail missing/error states keep surrounding durable summary metadata visible when inspection fields expand), expose the `factory-session` bento widget from `ui/src/features/bento/hooks/dashboardLayoutSchema.ts`, and record deferred drilldown/replay/lifecycle/live-provider website follow-ups in `docs/internal/development/plans/dynamic-workflows/follow-up-cell-website-session-inspection-deferred.md`.
- Factory Session list and detail runtime reads remain dispatch-free: keep dispatch inspection on the dedicated `/factory-sessions/{session_id}/dispatches` routes and do not add dispatch collections to `FactorySessionRuntime`. Dashboard detail data carries explicitly loaded dispatch summaries as a sibling of `session`, preserves that sibling across lifecycle refreshes, and never merges it into `session.runtime`. Shared artifact runtime projections live in `api/components/schemas/data-models/FactoryArtifact.yaml`; map JavaScript artifacts from `pkg/factory/contracts/work_dispatch.go` and expose them through `FactorySessionRuntime.artifacts` in `pkg/factorysessions/projection.go`.
- `api/openapi.yaml`, `pkg/api/generated/server.gen.go`, `pkg/generatedclient/client.gen.go`, and `ui/src/api/generated/openapi.ts` are generated by `make generate-api`.
- Factory layout metadata is authored in `api/components/schemas/data-models/FactoryLayout*.yaml`, carried by `pkg/factory/contracts/factory_config.go`, and mapped and deep-cloned in `pkg/config/factory_config_mapping{,_internal}.go` and `config_mapper.go`; add portable-layout round-trip evidence to `pkg/config/openapitests/openapi_factory_test.go` and its fixture. Generated Go represents OpenAPI `format: byte` as decoded `[]byte`, so a domain layout contract that stores portable base64 must explicitly decode at the generated-API boundary and re-encode on the way back out. Validate strict base64, decoded-byte budgets, field limits, and annotation-only geometry from the raw JSON map before that generated decode: this preserves the authored field path and prevents permissive decoding from accepting whitespace, non-canonical padding, or invalid canvas values. Keep annotation coordinate/size limits in dedicated annotation schemas rather than tightening shared node and edge geometry. Every `oneOf` branch must repeat its concrete properties as well as its `required` list; required-only branches validate correctly but generate unusable `unknown`/`interface{}` variants. When a layout record uses `kind` to select fields, author closed `oneOf` branches that require the variant payload and variant-specific geometry so the projected Factory schema cannot accept a shape the raw boundary rejects. Mirror that closure at raw boundaries by rejecting prohibited key presence even when its JSON value is `null`; generated pointer decoding otherwise normalizes the invalid property away.
- Worker taxonomy changes start in `api/components/schemas/data-models/WorkerType.yaml`; keep domain normalization in `pkg/workers/taxonomy` and generated enum conversion in transport mapping, then wire load/save projection through `pkg/config/factory_config_mapping_internal.go`. Preserve legacy runtime identifiers internally while accepting the public worker vocabulary at the boundary.
- Workstation taxonomy changes start in `api/components/schemas/data-models/WorkstationType.yaml`; keep domain normalization in `pkg/workers/taxonomy`, worker/workstation policy in `pkg/workers/compatibility`, and generated conversion in transport mapping. Wire load/save projection through `pkg/config/factory_config_mapping_internal.go` and `pkg/config/factory_config_mapping.go`, preserving supported legacy runtime aliases at the API boundary.
- Workstation outcome parsing modes such as `outcomeFormat: decision-envelope` start in `api/components/schemas/data-models/WorkstationOutcomeFormat.yaml` (referenced from `Workstation.yaml`); keep closed-vocabulary canonicalization in `pkg/services/factory_definitions/contracts/public_factory_enums.go` (`StrictPublicFactoryWorkstationOutcomeFormat`) and boundary normalization in `pkg/transports/mapping/factoryconfig/openapi_factory.go` (`normalizeFactoryWorkstationEntries`); wire through `pkg/services/factory_definitions/contracts/factory_config.go` (`FactoryWorkstationConfig.OutcomeFormat`) and `pkg/transports/mapping/factoryconfig/factory_config_mapping{,_internal}.go` (`workstationAPIFromInternal`, `workstationInternalFromAPI`) so checked-in `factory.json` passes the generated-schema boundary. Prove unsupported values fail at the boundary through `pkg/transports/mapping/factoryconfig/openapitests/openapi_factory_boundary_enum_test.go`.
- Factory/OpenAPI/projected-schema config parity evidence belongs in `pkg/transports/mapping/factoryconfig/openapitests`. Keep it behavioral: table-driven cases should submit committed payload fixtures directly to `GeneratedFactoryFromOpenAPIJSON`, `FactoryConfigFromOpenAPIJSON`, and `contracts/config/factory.schema.json`, then assert accepted values or field-specific rejection diagnostics. Do not use source hashes, committed inventory baselines, fixture filename scans, or inventory-membership tests as contract evidence.
- Dashboard taxonomy UI: centralize worker/workstation alias handling and editor defaults in `ui/src/features/current-factory-definition/lib/worker-workstation-taxonomy.ts`; extend `ui/src/api/factory-definition/api.ts` enum sets (`WORKER_TYPE_VALUES`, `WORKSTATION_TYPE_VALUES`) whenever OpenAPI adds public taxonomy names so current-factory save round trips accept new and legacy values.
- Worker/workstation compatibility policy lives in `pkg/workers/compatibility`; structural and API validation targets under `pkg/factory/validation` translate Factory-owned definitions into its narrow workstation input. Usage-aware legacy projection remains in that worker owner and post-load normalization in `pkg/config/openapi_factory.go`; preserve the mixed-use legacy alias when one worker serves both agent and inference workstations. Keep replay merge paths and topology projection aligned, and treat all provider-boundary worker aliases equivalently in service metrics.
- Agent-run execution wiring lives in `pkg/workers/executor/agentrun/` with taxonomy routing in `pkg/workers/executor/workstation_behavior_router.go` and runtime assembly in `pkg/service/factory_build.go`. Keep `INFERENCE_RUN` on the existing provider executor path and route `AGENT_RUN` through the harness adapter. Author explicit tool policy in the OpenAPI worker schemas, map it into `pkg/workers/config.AgentToolsConfig`, and validate it at the Factory config boundary. Local inference and agent workers use `pkg/workers/taxonomy.UsesModelhostLease` to select modelhost leasing. Replay-safe inspection converts worker-internal diagnostics through `pkg/workers/diagnostics` before Factory event and dashboard projection; never expose raw prompt, environment, command, or provider-secret values.
- Factory-session discovery error classification lives in `pkg/factorysessions/discovery.go` + `validation.go`, with public target shaping in `pkg/factory/validation/operational_targets.go`, open and live read policy plus durable lifecycle routing in `pkg/factorysessions/controlplane` (`open.go`, `read.go`, `sync_preflight.go`, `result_read.go`, `durable_lifecycle.go`), live open startup and live lifecycle supervision in `pkg/factorysessions/dataplane` (`live_open.go`, `live_lifecycle.go`), transient response-stream and JavaScript checkpoint orchestration in `pkg/factorysessions/stream` (`manager.go`, `fragment.go`), gateway orchestration in `pkg/factorysessions/service` (`OpenFactorySession`, `ListFactorySessions`, `GetFactorySession`, `GetFactorySessionSyncPreflight`, `GetFactorySessionResult`, `GetFactorySessionPartialResult`, live lifecycle methods, durable lifecycle methods in `durable_lifecycle.go`, response-stream methods in `stream.go`), narrow host seams and compatibility delegation in `pkg/service/runtime_sessions.go` (`sessionGatewayHost`, `ResolveSyncPreflightTarget`, `BackendScopeID`, `StreamGenerationID`, `LiveSessionEvents`, `DurableExecution`, stream state and metrics), transport error-code mapping in `pkg/api/handlers_factory.go`, and typed dashboard client handling in `ui/src/api/factory-sessions/api.ts`. Inject list/get, sync-preflight, result, live lifecycle, durable lifecycle, and response-stream delegation tests through `FactoryServiceConfig.SessionGateway`, not `FactoryCoordinator`. Prove transport wiring through `TestBuildFactoryService_WiresSessionGatewayCollaborator` in `pkg/service/factory_runtime_mode_test.go` (`BuildFactoryService` attaches `*factorysessions/service.Service`) and `TestFactoryService_LifecycleMethodsDelegateToCoordinator` (session transport methods never call coordinator open/read/lifecycle/stream paths). Durable lifecycle routing uses `controlplane.IsDurableExecutionSessionID` and `ErrDurableSessionLifecycleRouting` before calling `factorysessionexecution.Service`. Preserve genuine empty-folder validate-only flows as `initsNewFactory`, but return explicit `FACTORY_SESSION_CONFIG_LOAD_FAILED` errors with structured `factory.session.target.config_load_failed` targets when discovery found a readable factory target whose runtime config load failed.
- Current and named factory definition reads belong in `pkg/factorydefinition/service` (`GetCurrentNamedFactory`, `GetCurrentFactoryForSession`, `SerializeNamedFactory`, `CurrentFactoryDefinitionVersionAtRoot`) with explicit session/runtime collaborators on `factorydefinition.Host`; root `pkg/service` keeps thin forwarding through `FactoryDefinitionService` and `factoryDefinitionHost` in `pkg/service/factory_editable_definition.go` for `GET /factory-sessions/{session_id}/factory` and legacy current-factory reads. Prove read compatibility in `pkg/factorydefinition/service/read_test.go` (including session-scoped version projection) plus existing `pkg/service/factory_test.go` and `pkg/api/servertests/server_factory*_test.go`.
- Editable definition serialization, optimistic-concurrency version checks, and durable persist-view preparation belong in `pkg/factorydefinition/service` (`SerializeNamedFactoryUpsertResponse`, `RequireFreshEditableFactoryVersion`, `NextEditableFactoryVersion`, `PreparePersistedFactoryPayload`, `PrepareEditableFactoryPersistView`); `pkg/service/factorysave` consumes them through expanded `factorysave.Host` methods on `factorySaveHost`, with focused coverage in `pkg/factorydefinition/service/version_test.go` and `persist_test.go`.
- Current-factory save orchestration (`SaveReplaceCurrentForSession`, `ValidateEditableFactoryTopology`, editable-definition sanitization, and split-layout target resolution) belongs in `pkg/factorydefinition/service`; root `pkg/service` and `pkg/service/factorysave` delegate REPLACE_CURRENT saves through `factorySaveHost.SaveReplaceCurrentForSession` into the definition service, with focused coverage in `pkg/factorydefinition/service/save_test.go` (split-layout replace plus stale-version rejection) and existing `pkg/service/factorysave/replace_*_test.go`.
- Session-scoped named-factory upsert/activate and persisted named-factory activation (`SaveUpsertNamedAndActivateForSession`, `ActivateNamedFactory`, `ValidateUpsertNamedFactoryRequest`) belong in `pkg/factorydefinition/service`; root `pkg/service` and `pkg/service/factorysave` delegate UPSERT_NAMED_AND_ACTIVATE saves through `factorySaveHost.SaveUpsertNamedAndActivateForSession`, and `ActivateNamedFactory` delegates through `factoryDefinitionHost` runtime/persistence collaborators (`RunSessionID`, `SessionForActivation`, `NamedFactoryActivationPaths`, `RequireIdleBeforeNamedFactoryActivation`, `SwapPersistedNamedFactoryRuntime`), with focused coverage in `pkg/factorydefinition/service/upsert_test.go` plus existing `pkg/service/factory_test.go` and `pkg/service/factorysave/replace_named_test.go`.
- Session event reconnect recovery lives on `GET /factory-sessions/{session_id}/events`: keep `text/event-stream` streaming as the default behavior, but preserve the `Accept: application/json` probe mode for structured reconnect outcomes (`FactorySessionEventStreamRecovery`) that classify `CURSOR_STALE` separately from reusable cursors and unknown sessions. Wire handler behavior in `pkg/api/handlers_events.go`, contract coverage in `pkg/api/contracttests/generated_contract_durable_session_test.go`, session-event API regressions in `pkg/api/servertests/server_dashboard_events_test.go`, and typed dashboard fetch helpers in `ui/src/api/events/api.ts`.
- When adding generated OpenAPI type aliases to handwritten frontend API wrappers under `ui/src/api/**`, keep the export surface trimmed to aliases that are actually consumed on the reviewed head; `make ui-deadcode` treats newly exported-but-unused aliases as mergeability blockers even when the generated schema types themselves are valid.
- Generated API `ErrorResponseCode` enum renames can break older functional assertions outside `pkg/api/`; after `make generate-api`, search `tests/functional/` plus CLI/API packages for legacy comparisons like `factoryapi.BADREQUEST`, `factoryapi.INVALIDFACTORY`, and `factoryapi.STALEFACTORYVERSION` and align them to the generated `factoryapi.ErrorResponseCode...` constants before calling the reviewed head mergeable.
- Controlled IndexedDB checkpoint tests must drive the current replacement contract as one `open` followed by `get` and optional `put` in the same transaction, then explicitly complete that transaction. Wait until `pendingOperations()` exposes each expected operation instead of assuming a fixed number of promise continuations; ordered same-identity writes may enter the test double only after the prior transaction settles.
- Resumable `sync-preflight` outcomes (`ok`, `cursor_stale`, `logical_session_remap`) must carry the full identity set all the way through the typed UI client. Keep `ui/src/api/factory-sessions/sync-preflight.ts` strict about `backendScopeId`, `logicalSessionKeyId`, `factorySessionId`, and `streamGenerationId` for those reason codes, and keep `ui/src/features/dashboard/lib/preflight/dashboard-session-sync-preflight.ts` treating any missing identity on resumable outcomes as non-recoverable so the dashboard cannot open SSE on a partial success body.
- Shared app-shell dashboard tests that render `<App />` through `ui/src/testing/app-shell-test-utils.tsx` also depend on that preflight route now; keep the default fetch harness answering `GET /factory-sessions/{session_id}/sync-preflight` with a valid typed response so replay/current-selection integration tests exercise the real dashboard shell instead of stalling in preflight recovery because the mock API forgot the new contract.
- App-shell integration tests that need custom `fetch` behavior should layer it through `chainRenderAppFetchMock(...)` in `ui/src/testing/app-shell-test-utils.tsx` instead of replacing `fetchMock` wholesale; overriding the mock directly can accidentally drop the shared `sync-preflight` route and boot the shell into recovery instead of the state the test intends to exercise.
- Browser-backed dashboard integration fixtures in `ui/integration/browser-test-harness.mjs` also need the sync-preflight route now; keep `startFactoryApiServer(...)` serving `GET /factory-sessions/{session_id}/sync-preflight` with the typed identity fields and production status semantics the dashboard requires. In particular, typed outcomes such as `reasonCode=session_not_found` still return HTTP `200`; do not turn them into non-OK transport failures or the browser lane will prove the wrong recovery path and can burn the timeout before failing.
- Shared-storage dashboard browser proofs must create multiple pages from one `openBrowserPage(...)` storage context and navigate them to the same preview origin; separate Playwright contexts isolate IndexedDB and do not prove cross-tab behavior. Seed concrete-session checkpoint envelopes concurrently only after both dashboards reach durable widget-picker readiness, use controlled replay release points instead of elapsed-time sleeps, cap captured URLs, and select `artifactMode: "bounded"` so CI artifacts contain only compact diagnostic counts rather than traces, screenshots, HTML, or raw error text. Install `installBrowserErrorCapture(...)` on every additional page with both `entryLimit` and `characterLimit`; a count-only cap can still retain a raw oversized payload. Stream-scope assertions must prove the expected session URL occurs exactly once and the opposite session URL never occurs, both after restore and after live continuation. Acquire API servers, contexts, and pages under nullable-handle `try/finally` ownership, make sibling cleanup independent with `Promise.allSettled`, and keep the shared harness close path attempting context closure even when artifact capture or page closure fails. `ui/integration/dashboard-shared-indexeddb-browser-contexts.integration.test.mjs` is the focused two-session restore-and-tail example.
- Non-recoverable typed preflight outcomes for dashboard restore belong in `ui/src/features/dashboard/lib/preflight/dashboard-session-sync-preflight.ts` and flow through `useDashboardSnapshot` into `ui/src/features/dashboard/components/dashboard-screen.tsx`; when `reasonCode=session_not_found` (or another typed non-recoverable outcome), suppress the generic offline shell, keep the dashboard header visible, and render a clean-replay recovery action instead of opening the event stream.
- If the dashboard advertises a clean-replay recovery action for a failed sync preflight request, `ui/src/features/dashboard/hooks/preflight/use-dashboard-checkpoint-preflight.ts` must clear the persisted timeline checkpoint before the retry path runs again; otherwise the next preflight will silently resend the stale reconnect cursor even though the UI claims cached replay data was cleared.
- `pkg/api/contracttests/` contains OpenAPI authoring, bundled-contract, and generated-type smoke tests for public API changes. Residual active-surface import gate verification uses scoped `rg` commands documented in `docs/internal/development/plans/dynamic-workflows/dynamic-workflows-plan.md` plus behavioral preview/alias tests in contract and server packages.
- Public `FactoryResponseEvent` OpenAPI fragments live under `api/components/schemas/response-events/` and mirror the Story 01 vocabulary in `pkg/factorysessions/responseevents` (envelope, kind/phase/provenance enums, capabilities, typed payload union under `payloads/`, and kind-discriminated message content blocks under `content-blocks/`). Register them through `api/openapi-main.yaml` and regenerate with `make generate-api`; keep them distinct from canonical `FactoryEvent` schemas under `api/components/schemas/events/`.
- The ephemeral response-event SSE contract lives on `GET /factory-sessions/{session_id}/response-events`. Keep its `ResponseEventAfterSequence`, `ResponseEventDispatchID`, and repeatable `ResponseEventKind` query components separate from canonical Factory-event reconnect parameters; declare `ResponseEventAfterSequence` as `integer` with `format: int64` so generated Go cursors preserve the type and range of `FactoryResponseEvent.sequence`. Its `x-event-schema` is `FactoryResponseEvent`, while `/factory-sessions/{session_id}/events` remains the canonical `FactoryEvent` stream. Unknown sessions must return the response-event-specific typed 404 without current/default fallback, and expired retained streams use the distinct typed 410 response.
- Keep the response-event subscription seam in `pkg/apisurface` transport-neutral: return sequence, kind, and canonical serialized JSON records rather than importing `pkg/factorysessions/responseevents` into the shared API surface. Adapt the session-owned store at the runtime edge so core packages that depend on `pkg/apisurface` do not acquire a reverse dependency on Factory Session implementation packages.
- Retention gaps for `GET /factory-sessions/{session_id}/response-events` are synthetic `STREAM_GAP` records owned by `pkg/factorysessions/responseeventstore`: emit them before the available suffix, always pass them through response-kind filtering, report the unavailable `fromSequence`/`toSequence` bounds plus the query-relative `firstAvailableSequence`, and never publish or persist them as canonical `FactoryEvent` history.
- Completed response-event stores accept late subscriptions for `responseeventstore.CompletedStreamRetentionWindow` (15 minutes), then return `ErrStoreExpired`; every production `apisurface.SessionAPI` implementation, including both the legacy `pkg/service` facade and initializer-backed `pkg/runtimehost.Host`, must map that domain outcome through `apisurface.ErrFactoryResponseEventStreamExpired` to the authored HTTP 410 before SSE headers are committed. Prove initializer/runtime-host composition behavior in `pkg/initializer/api_transport_internal_test.go`, not only mock or legacy service paths. When adding a typed HTTP status, also extend `errorFamilyForStatus` in `pkg/api/handlers_common.go` so the response's `family` matches its status instead of falling back to `INTERNAL_SERVER_ERROR`.
- B08 response-event backpressure transport proofs belong in `pkg/transports/http/servertests/server_factory_session_orchestrator_test.go` (`TestFactoryResponseEventsBySessionID_SlowConsumerFloodDoesNotBlockPublication` uses a gated SSE body reader so `GET /factory-sessions/{session_id}/response-events` can lag while store publication floods under a bounded budget) and `pkg/transports/cli/run/run_wire_api_test.go` (`runFactoryInvocationResponseStreamSlowStdoutFixture` covers human and NDJSON slow-stdout fixtures with `gatedResponseStreamWriter` plus terminal `invocation_result` assertions). Skip the HTTP fixture when `testing.Short()` is set.
- Response-event OpenAPI contract coverage lives in `pkg/transports/http/contracttests/openapi_contract_response_events_test.go` and `pkg/transports/http/contracttests/generated_contract_common_test.go`, with representative fixtures reused from `pkg/factory/sessions/responseevents/testdata/fixtures/` plus payload/content-block coverage fixtures under `pkg/transports/http/testdata/`. `pkg/factory/sessions/responseevents` owns the canonical compact JSON envelope; because Go marshals struct fields in declaration order while generated OpenAPI structs use generator-defined ordering, keep the domain envelope and provenance order aligned with the generated server model and compare complete marshaled bytes for every shared representative fixture.
- Internal provider/session response-stream changes stay out of the public contract by proving absence at the artifact boundary in `pkg/api/contracttests/`: when adding provider-specific normalization or internal `SessionResponseStream` semantics, add or update contract tests against `api/openapi.yaml`, `pkg/api/generated/server.gen.go`, `pkg/generatedclient/client.gen.go`, `ui/src/api/generated/openapi.ts`, and canonical public event fixtures so internal response-stream terms and provider-native event names do not leak into published schemas or `FactoryEvent` payload artifacts.
- Historical `FactoryEvent` compatibility translation starts with policy-free byte normalization in `pkg/platform/replay/storage.go`, then Factory-owned replay ingestion in `pkg/factory/replay/artifact.go` and `pkg/factory/replay/event_stream_artifact.go` decodes canonical events before generated event unions discard retired fields. Keep reducers and projections canonical-only, normalize artifact and SSE inputs through the same boundary helper, and limit translation to event payload objects so customer work content is not rewritten.
- Durable factory-session contract fixtures live in `pkg/transports/http/testdata/durable-session-contract-fixtures.json` with round-trip and OpenAPI validation coverage in `generated_contract_durable_session_test.go`; keep `WorkContent` payloads as ordered part arrays and use API-relative artifact retrieval refs instead of host filesystem paths.
- JavaScript child worker-selection inspection originates on `orchestrators/javascript/runtime.ChildDispatchRecord`, maps through `factorysessionexecution.DispatchSummary`, and is projected into dispatch list/detail responses plus `DISPATCH_QUEUED` events. Keep preset id, model provider, model, reasoning effort, runner, execution provider, and provider-session refs aligned across that path, and prove persistence/replay parity with the live-child runtime fixture.
- Session sync-preflight recovery contract fixtures live in `pkg/transports/http/testdata/sync-preflight-recovery-contract-fixtures.json` with OpenAPI validation, generated Go round-trip, and domain-owned `cursors.ClassifyIdentityMismatch` coverage from `pkg/factory/sessions/cursors` in `pkg/transports/http/contracttests/generated_contract_factory_test.go`. Map generated sync-preflight reason and identity fields to `cursors.PreflightResult` in the service or transport boundary before domain classification; `pkg/platform/cursors/session` only emits the resulting secret-safe diagnostic and must not import generated HTTP contracts. Prove typed HTTP `200` recovery outcomes for `ok`, `cursor_stale`, `session_not_found`, and `logical_session_remap` in `pkg/service/runtime_session_runtime_test.go`, `pkg/transports/http/servertests/server_factory_session_orchestrator_test.go`, and `ui/src/api/factory-sessions/api.test.ts` instead of relying on transport failures for stale or missing sessions.
- Adding a new OpenAPI operation updates `factoryapi.ServerInterface`; add or update the matching `pkg/transports/http` handler before running package tests. For an explicitly contract-only lane that defers its handler, list only that operation ID under `exclude-operation-ids` in `api/codegen_config/server.yaml`, continue generating the bundled and client contracts, and remove the exclusion when the handler implementation lands.
- Authored OpenAPI component changes also update the staged public API data package under `packages/api/generated/`. After `make generate-api`, run `make contracts-generate` and `make contracts-check`; otherwise unit tests that deliberately corrupt one staged schema can report unrelated stale `manifest.json` and `openapi/openapi.yaml` artifacts because the canonical OpenAPI bytes changed.
- Authored first-party provider publication data lives one manifest per canonical ID under `packages/model-providers/providers/<id>/provider.yaml`; keep support, availability, discovery, execution, and response-fidelity claims tied to exact repository evidence in `packages/model-providers/SUPPORT.md`. Contract coverage loads those manifests through the public `ProviderManifest` schema and generated Go projection without registering or probing runtime providers.
- Deterministic provider publication is owned by `internal/providercatalog`: `make provider-catalog-generate` converts the bundled authored `ProviderManifest` and `ProviderCatalog` OpenAPI components into explicit Draft 2020-12 schemas, validates every authored manifest and the aggregate catalog, sorts providers by canonical ID, and atomically replaces `packages/model-providers/generated/`. Keep schema-enforced field constraints and cross-entry semantic validation complementary: public documentation links must be public HTTPS DNS targets at the schema boundary, while identity/alias ownership, capability dependencies, and replacement relationships require catalog-level semantics. Keep `make provider-catalog-check` read-only and in the lint lane so stale or unexpected publication artifacts fail with regeneration guidance.
- The data-only npm projection lives in the same `packages/model-providers` boundary. `make model-provider-package-generate` derives focused TypeScript declarations from the explicit Provider Catalog schema and stages a commit-independent development manifest with SHA-256 hashes; it does not rewrite the canonical catalog or schemas. This manifest must remain stable across rebases and shallow CI checkouts; release publication is the boundary that adds immutable source provenance. `make model-provider-package-smoke` enforces the exact tarball allowlist, installs the tarball in an isolated consumer, typechecks every documented export, loads every runtime export through its installed package specifier, and compares the resolved artifact bytes to the canonical generated files.
- Website reference input belongs in a typed consumer adapter, not in a second provider inventory. `ui/scripts/model-provider-reference-input.ts` imports the data-only `@you-agent-factory/model-providers` catalog, types, and explicit schemas, validates schema compatibility plus the generator-owned cross-entry identity, capability, and deprecation invariants before projecting, and derives both the sorted index and schema-backed per-provider inputs from the packaged manifests.
- Public OpenAPI component changes also require the package-local `@you-agent-factory/client` projection under `ui/packages/client/src/generated/`. Run `bun run generate` in `ui/packages/client`, then use `make ui-public-package-release` to prove generated drift, package builds, inventories, isolated consumers, and browser-backed release checks across the canonical frontend package family.
- Managed-runtime catalog structure lives in `pkg/models/catalog`, with discovery assembly in `pkg/models/local/catalog.go`. `pkg/models/service/catalog.go` receives runtime configuration and `modelhost.Host` directly, overlays process readiness, and returns only model-owned list/detail contracts. Generated OpenAPI conversion belongs in `pkg/services/models/transports/http` and is applied by the service-owned HTTP adapter; do not spread generated model mapping across the top-level HTTP server. Process-wide local model lifecycle ownership belongs in `pkg/models/host` (`Host`, `CatalogHost`, `ManagedRuntimeFromSnapshot`); construct the host at service build time through `newRuntimeLocalModelDependencies` in `pkg/service/factory_build.go` and expose it from `FactoryCore.ModelHost()`. `pkg/wire/production.go` constructs the production model service from direct stable collaborators plus the dynamic active-runtime configuration callback, then adapts it for the public runtime facade. Supervised `LLAMACPP` load, readiness, health checks, process launch, and crash handling live together in `pkg/models/host/supervisor.go`; worker configs must include `--health-endpoint` for supervised server startup. Shared lease capacity, idle unload, and resource-pressure eviction live in `pkg/models/host/lease_policy.go` with `Options.IdleUnloadAfter` and `Options.MaxLoadedRuntimes`. Model host operator diagnostics (logs and `model_host.*` counters) live in `pkg/models/host/diagnostics.go`, with zap/metrics adapters in `pkg/service/factory_build.go` and `pkg/factory/service/config.go`; see `docs/architecture/model-host.md`.
- Managed-runtime asset pull results, downloaded-file metadata, and asset availability/source failure identities belong in `pkg/models/assets`; `pkg/transports/mapping` retains compatibility aliases and maps those model-owned results to public responses. Pull projection and outcome normalization live in `pkg/models/local/pull_lifecycle.go` and `pkg/models/local/catalog.go` (`PullModelWithOptions`); API transport mapping for `managedRuntimePull` belongs in `pkg/transports/mapping/managed_runtime.go`; pull logging and `managed_runtime.pull.*` metrics belong in `pkg/models/service/pull.go` with pull-metrics adaptation at the production composition boundary in `pkg/wire/production.go` and at retained compatibility seams in `pkg/service/model_catalog.go` and `pkg/runtimehost/model_catalog.go`.
- Managed-runtime factory dependency validation lives in `pkg/factory/validation/managed_runtime_dependency.go`; readiness projection for packaged and authored factories lives in `pkg/models/local/runtime_dependency.go`; consume validation through `pkg/config/config_validator_topology.go` canonical structural validation.
- Managed-runtime invocation readiness vocabulary, runtime projections, blocking errors, and recovery guidance belong in `pkg/models/managedruntime`; provider-neutral direct invocation requests, results, response modes, and target error context belong in `pkg/models/inference`. `pkg/models/service/invoke.go` consumes only those model-owned contracts, while `pkg/services/models/transports/http/adapter.go` maps generated content, bindings, options, and customer-facing failure projections at the outward boundary. Compatibility delegation continues from `pkg/service/model_catalog.go` to the injected `apisurface.ModelAPI`, with local execution in `localmodels.Manager.execute` under `pkg/models/local/runtime.go`; classify model-event failures via `modelEventErrorClass` in `pkg/service/factory_runtime_state.go`. Prove the domain-to-generated boundary in `pkg/services/models/transports/http/catalog_mapping_test.go` and `handler_test.go`, model-owned readiness behavior in `pkg/models/managedruntime/contract_test.go`, API wiring through `pkg/transports/http/server_models_events_additional_test.go`, and local CLI loopback through `pkg/transports/cli/run/run_startup_test.go`.
- Dashboard current-factory normalization for model-invoke authoring lives in `ui/src/api/factory-definition/api.ts` with shared decode helpers in `api.decode-helpers.ts` and model-operation/binding decoders in `api.model-invoke.ts`; keep `WORKER_KEYS.operations`, `WORKSTATION_KEYS.operation` / `operationBindings`, and `MODEL_INVOKE` in `WORKSTATION_TYPE_VALUES` aligned with the generated OpenAPI `Factory` schema when extending model-invoke fields.
- Functional API transport regressions should use `tests/functional/internal/support.StartFunctionalAPIServer`; that helper composes through `compose.InjectAPITransport` and runs `transport.Run()` instead of `compose.InjectFactoryService`. It disables runtime file logging by default through `service.FactoryServiceConfig.RuntimeFileLoggingPolicy` while preserving `cfg.Logger`-backed non-file diagnostics. Tests that assert on runtime `.log` artifacts must opt back in explicitly in `Configure`, ideally with an isolated temp `RuntimeLogDir`, `RuntimeFileLoggingPolicyEnabled`, and a stable `RuntimeInstanceID` so the test can match `RuntimeLogDiagnostics().Path` to the emitted startup record. Set `FunctionalAPIServerConfig.RecordPath` when a customer-facing functional scenario needs to inspect its saved recording, then submit through the session-scoped HTTP work-request route and keep assertions to generated event payloads or recording JSON rather than service state.
- Root `pkg/service` is a temporary compatibility shell for session runtime hosting and wire equivalence tests (`pkg/service/factory.go` package doc); transports must not call `InjectFactoryService` or `BuildFactoryService` at composition boundaries. Behavioral startup compatibility, invalid-input parity, and cross-transport successful composition evidence live in `pkg/initializer/startup_compatibility_test.go` and `pkg/initializer/initialize_test.go`; focused initializer migration verification: `go test ./cmd/... ./pkg/transports/http/... ./pkg/transports/cli/... ./pkg/transports/mcp/... ./pkg/initializer/... -short`.
- Canonical HTTP handler dependency composition lives in `pkg/transports/mapping/composition/surface.go` (`NewSessionAPISurface`). Compose it from five explicit collaborators: session operations (legacy unscoped factory operations, session lifecycle, and session-scoped work), model operations, factory-definition operations, invocation, and durable execution. Initializer accessors must return bounded collaborators rather than either compatibility facade; capture the constructor inputs in `pkg/initializer/api_transport_internal_test.go` when changing this wiring. The durable transport contracts and request/response shaping live in `pkg/transports/mapping`, while the durable mapper lives in `pkg/transports/mapping/factorysession` (`DurableAPI`). Both `FactoryService` and `runtimehost.Host` forward to that owner. The durable role must also preserve the server's optional listing, projection, and lifecycle interfaces so composed startup does not disable existing routes. Keep the composed adapter stateless and test its delegation in `pkg/transports/mapping/composition/surface_test.go`; test retained facade forwarding separately as compatibility behavior.
- API startup dependencies are composed once by the run/API initializer in `pkg/wire/production.go`: build the typed session API surface from the bundle-owned session, model, factory-definition, invocation, and durable-execution collaborators, then attach the HTTP handler and lifecycle directly to `bundle.Bundle`. The initial injected bundle and the stdio initializer do not construct that HTTP surface. `pkg/initializer` receives only the selected bundle's completed lifecycle handles and must not rebuild a runtime core or transport host from config. Focused construction and lifecycle coverage lives in `pkg/wire/production_test.go`, `pkg/wire/production_internal_test.go`, and `pkg/initializer/core.go` tests.
- Initializer-backed MCP serve composition belongs in `pkg/wire/process.go` (backing-service selection and typed graph construction), `pkg/initializer` (lifecycle), and `pkg/transports/cli/mcp/serve.go` (stdio adaptation only). Pass the exact wire-built durable execution service through the graph to `pkg/transports/mcp/server`; never construct persistence or execution services at the CLI boundary. Focused production-composition coverage lives in `pkg/root/root_test.go` and `pkg/wire/process_test.go`, with protocol smoke coverage in `pkg/transports/cli/mcp/serve_*_test.go`.
- Real-backend dashboard browser proofs can launch the same initializer-backed API transport path through `tests/functional/internal/support/cmd/browser_api_harness` (`compose.InjectAPITransport`), then point `ui/integration/browser-test-harness.mjs` at that local API origin. Keep live dashboard state on a real factory directory, seed durable workflow sessions through the service durable-session API, and use URL- or widget-level selection on the existing dashboard surface rather than introducing a browser-only backend double. Treat this path as one bounded customer-facing proof of the existing session-detail inspection lane, not as a replacement for the fast fixture-backed unit/component coverage or `factory-session-detail-panel.stories.tsx` Storybook verification that should remain the default regression surface. The harness supports `--start-mode sync|async`: reuse `sync` for terminal inspection paths and `async` with a long-running workflow fixture (for example `busy-loop.workflow.js`) when the dashboard proof must tolerate explicit loading or non-terminal durable status before detail becomes inspectable. For terminal dispatch-detail plus artifact-drilldown coverage, `agent-run-fake-child.workflow.js` is the narrow seeded fixture that already projects `dispatch-1`, JavaScript execution mode, and `child-artifact-1` through the shared durable inspection APIs. Reuse the focused lane `make test-ui-durable-session-real-backend` (`cd ui && bun run test:integration:durable-session-real-backend`) to rerun only `ui/integration/durable-session-real-backend.integration.test.mjs` and `ui/integration/browser-test-harness.real-backend-setup.integration.test.mjs`; file lists live in `ui/scripts/ui-integration-targets.mjs`.
- The canonical bounded operator proof for the shipped durable JavaScript session slice belongs in `docs/reference/sessions.md` under the reusable proof artifact matrix. When a lane needs exact validate, lifecycle-control, and dashboard-detail closeout evidence, reuse `pkg/transports/cli/workflow/validate_test.go`, the durable lifecycle tests in `pkg/service/runtime_session_runtime_test.go`, and `ui/integration/durable-session-real-backend.integration.test.mjs` rather than adding a parallel proof guide or a new broad browser harness.
- The Factory Emulator scenario contract and generator live under `ui/packages/factory-emulator`; `schema/scenario.schema.json` is the publishable schema and `scripts/generate-scenario-schema.mjs` owns its freshness check. Retain byte-stable and schema-mutation coverage whenever the public scenario vocabulary changes.
- Factory Emulator parsing belongs in `ui/packages/factory-emulator/src/scenario.ts`: keep it a pure preflight boundary that returns structured diagnostics and validates Factory support before any emulator activity or canonical Factory event can begin.
- The published, host-controlled Factory Emulator visualizer lives in `ui/packages/components/src/factory-emulator`. `FactoryEmulatorControls` receives all execution state and callbacks as props; it must not import an emulator runtime, timer, event history, replay state, or dashboard store. Keep the four permitted speeds in its typed public contract, delegate Step as one callback invocation, and add each category entrypoint to `category-paths.ts`, `package.json`, `tsconfig.build.json`, and the package root exports.
- The Factory Emulator's accepted definition boundary must compile the canonical
  generated `packages/api/generated/schemas/factory.schema.json`, not a
  handwritten subset. The package-local generator copies those exact bytes into
  the publishable emulator package; apply full structural validation after
  plain-data checks and before emulator capability or scenario-semantic checks,
  then prove malformed definitions cannot reach sink activity.
- Factory Emulator supported-capability inspection and Factory subset validation share `ui/packages/factory-emulator/src/compatibility.ts`; do not duplicate parser acceptance policy.
- Factory Emulator command lifecycle, calculate/write/commit publication, rejected-transaction retry, deterministic safety enforcement, and detached state projection live in `ui/packages/factory-emulator/src/session.ts`, with pure scheduling in `scheduler.ts` and direct asynchronous behavior proofs in `session.test.ts`.
- The Factory Emulator event budget must admit the mandatory atomic two-event bootstrap. Keep the `maxEvents` minimum in `limits.js` aligned with bootstrap behavior, reject smaller policies before sink activity, and prove the smallest accepted budget can pause after bootstrap with a virtual-time diagnostic and still close.
- Bound Factory Emulator scheduler materialization separately from cooperative cadence: inspect retained Work in resumable `maxSynchronousWorkItems` chunks before building an atomic dispatch/completion batch, return the exact candidate indexes and lineage lookup to pure calculation so it cannot rescan retained Work, and apply replacements plus detach command-result state with the same chunk/yield cadence rather than whole-state clones. Pause after observing the first over-limit eligible Work, and use the session-owned `yieldControl` boundary between Work chunks and after `maxSynchronousBatches` calculations so host tasks can run without changing canonical event order. Check initial and interactive submission cardinality before traversing members, and keep the shared data-only walk iterative with explicit total-node and depth bounds so hostile wide or deep inputs reject as plain diagnostics before cloning rather than consuming unbounded synchronous work or leaking a host stack error.
- Factory Emulator token ownership starts with submission identity in `session.js`; scripted dispatch lineage, authored-rule cursor state, and output-token derivation belong to the pure scheduler calculation. Treat authored IDs as arbitrary schema-valid strings when using plain-object indexes: read only own properties and define updates as own data properties so names such as `__proto__`, `constructor`, and `toString` cannot invoke prototype behavior. Keep token/lineage identity observable through contract-valid Work tags, dispatch-response metadata, and bounded state rather than adding Petri-only fields to canonical customer event schemas, and prove cursor behavior through emitted session events instead of identity-helper-only tests.
- `bun run check:generated` in `ui/packages/factory-emulator` compares the published schema with deterministic source-derived output and reports the exact regenerate-and-commit remediation when drift is found.
