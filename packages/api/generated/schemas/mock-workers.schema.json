{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.portpowered.com/you/config/mock-workers.schema.json",
  "title": "You mock-worker configuration",
  "description": "The strict mock-worker dispatch configuration accepted by ParseMockWorkersConfig. mockWorkers[] entries are matched in array order and the first matching entry wins. Unmatched dispatches follow unmatchedDispatchPolicy.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mockWorkers"
  ],
  "properties": {
    "mockWorkers": {
      "description": "Ordered mock-worker entries. First-match selection applies; there is no multi-step response-sequence authoring surface.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/mockWorker"
      }
    },
    "unmatchedDispatchPolicy": {
      "description": "Controls dispatches that do not match any mockWorkers[] entry. Omitted values behave as accept.",
      "enum": [
        "accept",
        "passthrough"
      ]
    }
  },
  "$defs": {
    "workInput": {
      "type": "object",
      "additionalProperties": false,
      "description": "Narrows a mock-worker match by consumed work input. Omitted selector fields do not constrain that dimension.",
      "properties": {
        "workId": { "type": "string" },
        "workType": { "type": "string" },
        "state": { "type": "string" },
        "inputName": { "type": "string" },
        "traceId": { "type": "string" },
        "channel": { "type": "string" },
        "payloadHash": { "type": "string" }
      }
    },
    "scriptConfig": {
      "type": "object",
      "additionalProperties": false,
      "description": "Declares the local script a script mock executes through the shared command-runner boundary.",
      "required": [
        "command"
      ],
      "properties": {
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "Required non-empty command when runType is script."
        },
        "args": { "type": "array", "items": { "type": "string" } },
        "env": { "type": "object", "additionalProperties": { "type": "string" } },
        "workingDirectory": { "type": "string" },
        "stdin": { "type": "string" },
        "timeout": {
          "type": "string",
          "description": "Optional local script execution time bound (for example 30s). Not a dispatch delay or timing field."
        }
      }
    },
    "rejectConfig": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional observable output for a rejected mock result when runType is reject.",
      "properties": {
        "stdout": { "type": "string" },
        "stderr": { "type": "string" },
        "exitCode": {
          "type": "integer",
          "minimum": 1,
          "maximum": 255,
          "description": "Optional rejected exit code between 1 and 255."
        }
      }
    },
    "mockWorker": {
      "type": "object",
      "additionalProperties": false,
      "description": "Selects a worker dispatch and declares the deterministic behavior to apply at the execution boundary.",
      "required": [
        "runType"
      ],
      "properties": {
        "id": { "type": "string" },
        "workerName": { "type": "string" },
        "workstationName": { "type": "string" },
        "workInputs": {
          "type": "array",
          "items": { "$ref": "#/$defs/workInput" }
        },
        "runType": {
          "description": "Required run-type union selecting accept, script, or reject behavior.",
          "enum": ["accept", "script", "reject"]
        },
        "scriptConfig": { "$ref": "#/$defs/scriptConfig" },
        "rejectConfig": { "$ref": "#/$defs/rejectConfig" }
      },
      "allOf": [
        {
          "if": { "properties": { "runType": { "const": "script" } }, "required": ["runType"] },
          "then": { "required": ["scriptConfig"] }
        }
      ]
    }
  }
}
