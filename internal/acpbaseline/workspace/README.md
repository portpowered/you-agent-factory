A fixture workspace for ACP baseline capture.

It holds three small files so a captured tool call has something stable to
read and summarize. Content must not change between captures, or transcripts
stop being comparable.
