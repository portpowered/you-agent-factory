openapi: 3.0.3
info:
  title: Unsupported Security Scheme Fixture
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: listPets
      responses:
        '200':
          description: Pet list.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
