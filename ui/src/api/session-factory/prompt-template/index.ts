/**
 * Session-scoped workstation prompt-template HTTP (GET contract, POST validation).
 * Routes use `currentFactoryWorkstationPath` under `/factory-sessions/{session_id}/factory/workstations/...`.
 */
export * from "./api";
