export const workflowPreviewAPIErrorMessages = {
  emptyEnvironment: "Workflow preview is unavailable in this environment.",
  invalidResponse: "The workflow preview API returned an invalid response.",
  network: "The dashboard could not reach the workflow preview API.",
  rejectedRequest: "The workflow preview request was rejected.",
} as const;
