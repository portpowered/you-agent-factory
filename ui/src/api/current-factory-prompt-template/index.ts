/**
 * Stable re-export for feature imports. Implementation lives in
 * `session-factory/prompt-template/`; prefer `session-factory` for new code.
 */
export * from "../session-factory/prompt-template";
