export const factoryPreviewAPIErrorMessages = {
  emptyEnvironment: "Factory preview is unavailable in this environment.",
  invalidResponse: "The factory preview API returned an invalid response.",
  network: "The dashboard could not reach the factory preview API.",
  rejectedRequest: "The factory preview request was rejected.",
} as const;
