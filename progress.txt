## Codebase Patterns
- Attach an existing typed Factory Definitions sentinel at the source/layout or compilation operation that knows the failure class; a focused public capability can then preserve both the stable classification and original cause with `errors.Join` without matching error text.
- Process-scoped Factory Definitions capabilities should be constructed in `pkg/wire`, retained as an explicit `FactoryDefinitionsRuntimeOpeningDependencies` member, and proven inert before any runtime-opening operation invokes them.
- When a directory loader resolves one concrete authored root, retain that root as an immutable optional value fact on the compiled source so narrow consumers can report the actual selection without reopening source bytes or widening mutable loader contracts.
- When a validated effective definition enters Runtime, rebuild the existing mutable loaded source through its public factory with an empty runtime-definition lookup, then transfer the selected-root identity through the narrow public identity receiver; Runtime need not retain authored loader authority.
- A typed authored-load wrapper must keep its safe classification and selected source while rendering its wrapped diagnostic in `Error()`; outer public error wrapping otherwise hides actionable validation, source-format, and layout failures.
- Every new backend package with measurable unit-test statements needs an import-path-sorted numeric entry in `go-unit-coverage-package-minimums.json`; use the exact profile ratio truncated to two decimal percentage points rather than a coverage exception or rounded-up floor.
- For a resumable provider attempt, bind the provider-authored typed session reference at its native live-observation point; post-result metadata cannot authorize an in-flight pause.
- For a required CI failure, compare the failing path on the reviewed head and PR base before repairing it; do not expand a delivery lane to fix a defect unchanged on both revisions.
- For a cross-service behavioral proof that needs one service's internal concrete collaborator, compose the peer through its owner-published `wire.New...` API inside the collaborator's package-level integration test; do not add a test-only production seam.
- Construct each Factory Session's Worker Sessions service only after its session-owned `WorkstationPoolBoundary` exists, so dispatch and cancellation share the exact same boundary.
- Worker Session controls must use `context.WithoutCancel` when calling the pool boundary; dispatch termination remains observable through the boundary callback rather than caller-context cancellation.
- Runtime tests inject a Worker Sessions service by wrapping it in a `WorkerSessionsFactory`, keeping runtime construction dependent on the public factory contract.
- Scope Factory child controls from `DISPATCH_WORKER_SESSION_ASSOCIATION` events' `context.requestId`, then retain one detached canonical-ledger snapshot per committed control; never rescan live dispatches or reselect on retry.
- A turn-targeted Factory control must carry the immutable turn and control identities; cache its detached fan-out result by both so retries cannot select associations committed later.
- When a turn-scoped stop also ends the Factory Runtime, serialize Run's pool teardown behind Worker Sessions fan-out so the pool cannot preempt exact child controls.
- An asynchronous `WorkstationPoolBoundary.Publish` must not return until Workers has admitted the dispatch into an exact cancellable queue or running set (or has already returned a terminal result); Worker Sessions may then safely issue exact cancellation.
- A pool boundary that offers synchronous completion must forward Workers' cancellable-admission acknowledgement before it waits for the terminal result; otherwise a valid running dispatch becomes unreachable to its supervising control plane.
- A late admission callback must never move a terminal Worker Session back to `RUNNING`; terminal lifecycle snapshots are absorbing under every control/completion interleaving.
- When Workers reports `ALREADY_CANCELED`, its terminal callback can still be in flight; `Terminate` must join that callback before returning its idempotent terminal snapshot, while ordinary `Cancel` may retain its non-joining no-op contract.
- Any successful `Terminate` no-op caused by an earlier accepted control must join the same supervision callback before returning; `controlDone` only serializes the boundary effect and is not a terminal-result barrier.
- A committed Chat turn control must carry its immutable `TurnID` plus stable `ControlRequest.RequestID` to Factory Runtime as `TurnID`/`ControlID`; on-demand target cancellation must fan out before invocation-context cancellation and retain that result across runtime replacement.
- A committed Chat close needs a control-aware target termination operation: preserve its opaque control ID and turn ID through Factory Sessions, fan out before target cleanup, and keep generic close separate for process lifecycle cleanup.
- Provider Session references must stay as complete providers.SessionRef values bound to the Worker Session's own immutable turn and dispatch correlation; never recreate one from runner, model, current provider, or a bare session ID.
- When Workers must publish before its session-owned Worker Sessions service is constructed, use a session-local bridge that binds once during Worker Sessions factory construction and forwards typed-reference progress only after association succeeds.
- `ProviderSessionMetadata` is response-only compatibility data: forward metadata-only fragments unchanged, but only an exact typed `providers.SessionRef` may create a Worker Session association or enable continuation.
- When a resume path must preserve provider-specific identity, carry a cloned typed providers.SessionRef on the Workers execution request and prioritize it over any legacy bare session-ID compatibility field.
- An exact Worker Session continuation must validate both Providers `ContinueResult.Reference` and any returned `ExecuteResult.SessionRef` before publishing output; when the execute-level field is omitted, propagate the already-validated exact reference rather than reconstructing one.
- Preserve provider-specific continuation classifications only on in-process Worker result fields excluded from Factory event serialization, then map whitelisted enum values into the Worker Session terminal snapshot.
- Worker Sessions root and internal service packages have 100% unit-coverage baselines; cover control interleavings through controlled boundary callbacks and channel handshakes, never sleeps or source-inventory tests.

## 2026-08-04 20:18 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-001
- Implemented typed Worker Session pause, resume, cancel, and terminate controls. Cancel and terminate now target the exact supervised dispatch through `WorkstationPoolBoundary.Cancel`; terminate waits for the established terminal callback, and pause/resume explicitly report unsupported until a truthful resumable capability exists.
- Changed Factory Runtime composition to construct each Worker Sessions service from the session-owned pool boundary, preserving Factory Runtime's existing terminal-result callback as the canonical result path.
- Added deterministic behavioral coverage for detached-context cancellation, exact dispatch identity, idempotency, termination join behavior, unsupported pause/resume, terminal no-ops, invalid/unknown IDs, and boundary failures.
- Files changed: `pkg/services/worker_sessions/**`, `pkg/services/factory_runtime/**`, `pkg/wire/worker_sessions_providers.go`, `prd.json`, `progress.txt`.
- Verification: focused Worker Sessions, Factory Runtime/internal, and wire tests; repeated `-race` control tests; and focused `go vet` pass. `make verify-fast` is blocked before Go tests by pre-existing dashboard typecheck environment/configuration failures (missing Bun types and TypeScript 6 `baseUrl` deprecation).
- **Learnings for future iterations:**
  - The runtime pool boundary deliberately detaches dispatch context; controls must invoke its explicit `Cancel` method to stop accepted work.
  - Worker Session terminal state must be committed from the Workers boundary callback so control does not replace Factory Runtime's accepted dispatch result.
  - Existing runtime test doubles bypass the real pool boundary, so their factory adapters must remain compatible with the public Worker Sessions factory contract.
---
## 2026-08-04 20:27 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-002
- Preserved each Factory dispatch's immutable request/turn correlation on its canonical Worker Session association event and added deterministic captured-turn target selection from the session-scoped canonical ledger.
- The selector has stable sequence ordering and deduplication, ignores malformed/direct/later-turn records, yields an empty no-op target set for no matches, and retains the original targets when a later association is committed after capture.
- Files changed: `pkg/services/recordings/**`, `pkg/services/factory_runtime/internal/services/orchestration/runtime/**`, `internal/testutil/recordingfixtures/scripted_runtime_ledger.go`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/factory_runtime/internal/services/orchestration/runtime ./pkg/services/recordings/internal/events ./pkg/services/recordings/internal/services/canonical_ledger`; matching focused `go vet`.
- **Learnings for future iterations:**
  - `WorkDispatch.Execution.RequestID` is the immutable Factory invocation correlation used to distinguish one captured ACP turn from a later turn sharing the same Factory Session.
  - `RuntimeLedger.CanonicalEvents()` supplies the control linearization snapshot; retain its detached selection for retries rather than rebuilding a target set.
  - A Factory Runtime ledger is session-scoped, so cross-Factory-Session isolation is achieved by selecting only from the captured runtime's ledger.
---
## 2026-08-04 20:39 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-003
- Added Factory Runtime pause/resume fan-out for an immutable captured turn and control ID. Each committed control selects the canonical Worker Session associations once, invokes every selected child through `worker_sessions.Service`, and returns ordered per-child plus aggregate evidence without changing the Runtime result path.
- Preserved idempotency by retaining the original captured evidence for a repeated control ID, including after a later lifecycle transition; late association events cannot enter that original control. Empty targets are explicit no-ops, and a child failure or unsupported response never prevents sibling attempts.
- Added deterministic coverage for ordered mixed outcomes, single and multiple failure continuation, detached caller cancellation, all aggregate classifications, duplicate/retry isolation, late-child behavior, and empty captured turns.
- Files changed: `pkg/services/factory_runtime/worker_session_control.go`, `pkg/services/factory_runtime/work_move_errors.go`, `pkg/services/factory_runtime/internal/services/orchestration/runtime/{factory.go,worker_pool.go,worker_session_control_fanout.go,worker_session_control_fanout_test.go}`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/factory_runtime/...`; `go test ./pkg/services/worker_sessions/...`; focused `go test -race ./pkg/services/factory_runtime/internal/services/orchestration/runtime -run 'Test(FanOutWorkerSessionControl|FactoryControls_)' -count=1`; focused `go vet ./pkg/services/factory_runtime/...`; `git diff --check`.
- **Learnings for future iterations:**
  - Public Runtime pause/resume controls can preserve compatibility with factory-wide callers by treating a blank turn ID as an explicit empty child fan-out.
  - After the Factory lifecycle control commits, fan-out should carry a cancellation-detached context and report individual failures as deterministic child evidence, so a disconnected caller cannot stop later siblings.
  - The aggregate action must not write Worker Session or Factory Runtime terminal state; Worker Sessions and the existing dispatch callback remain the result authorities.
---
## 2026-08-04 20:52 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Extended Factory Runtime turn-scoped stops with explicit CANCEL and TERMINATE Worker Session actions. `TerminateRequest` now carries the immutable turn/control IDs and returns the ordered, detached fan-out evidence; legacy stop requests default to TERMINATE.
- Serialized the Run-loop pool teardown behind the committed stop fan-out. Every selected child is attempted through `worker_sessions.Service` using its explicit cancellation path before shared-pool shutdown can run, while child completion remains on the established Worker Sessions/Factory Runtime callback path.
- Added deterministic coverage for cancel and terminate fan-out ordering, partial-failure continuation, detached caller cancellation, repeated captured-control evidence after a late association, default termination, and invalid actions.
- Files changed: `pkg/services/factory_runtime/interfaces.go`, `pkg/services/factory_runtime/worker_session_control.go`, `pkg/services/factory_runtime/work_move_errors.go`, `pkg/services/factory_runtime/internal/services/orchestration/runtime/{worker_pool.go,worker_session_control_fanout.go,worker_session_control_fanout_test.go}`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/factory_runtime/...`; `go test ./pkg/services/worker_sessions/...`; focused `go test -race` for Runtime fan-out and Worker Sessions controls; `go vet ./pkg/services/factory_runtime/...`; `make verify-fast` pass; `git diff --check`. `make lint` passes UI lint and vet but is blocked by 87 existing repository-wide backend-size violations outside this slice.
- **Learnings for future iterations:**
  - A turn-scoped stop must hold the same fan-out lock used by pause/resume until it has attempted every selected Worker Session; otherwise Run can tear down the pool before exact controls are issued.
  - `WorkerSessionAction` defaults to TERMINATE for compatibility, but a captured CANCEL control must retain CANCEL at the Worker Sessions boundary so terminal state remains truthful.
---
## 2026-08-04 21:33 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Addressed the blocking PR review races for the cancel/terminate story. Runtime reserves the Worker Session identity before publishing the canonical dispatch association, so control is exact and addressable throughout the association-to-start handoff. Worker Sessions now prevents a pre-admission control from reaching Workers, and returns its established canceled dispatch outcome.
- Added an explicit Workers admission acknowledgement. The asynchronous production `WorkstationPoolBoundary.Publish` now waits until the dispatch ID is present in the cancellable pool queue/running set before returning; immediate exact cancellation cannot race an unknown dispatch. A completion-wins `ALREADY_TERMINAL` cancellation now returns a no-op while retaining the completed Worker Session and dispatch result.
- Added deterministic tests for association-before-registration cancellation, production-boundary admission ordering, completion-wins cancellation, pre-admission controls, and concurrent control behavior. Updated boundary fakes for the public admission contract.
- Files changed: `pkg/services/factory_runtime/internal/services/orchestration/runtime/**`, `pkg/services/worker_sessions/**`, `pkg/services/workers/**`, `prd.json`, `progress.txt`.
- Verification: focused Workers/Worker Sessions/Runtime tests; repeated `go test -race -count=10` for Worker Sessions and Runtime; focused `go vet`; `make test`; `git diff --check`; Worker Sessions internal service coverage 100.0%. `make test-unit-coverage` cannot start locally on Windows because `gocoveragecheck` expands 573 package arguments beyond the command-line limit; Linux CI remains the required full-lane authority.
- **Learnings for future iterations:**
  - Association publication is only a safe external control point after the exact Worker Session identity has been reserved; record the association before opening lifecycle records, but never before reserving the identity.
  - An `ALREADY_TERMINAL` cancellation result is not a failure: wait for the authoritative terminal callback and return the preserved terminal snapshot as a no-op.
- When adding a synchronization callback to a public execution interface, update all focused test fakes to acknowledge admission before delegating to their existing dispatch behavior.
---
## 2026-08-04 21:38 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Merged current `fork/main` to resolve the PR's reported conflict. The only overlap was the task-local `prd.json` and `progress.txt`, which respectively belong to this L4 task; the incoming documentation and packaged-factory changes were retained unchanged.
- Files changed: merged current `fork/main`; resolved `prd.json` and `progress.txt`.
- **Learnings for future iterations:**
  - Task-local PRD and progress artifacts are branch-specific. When a normal current-main merge conflicts only in those files, retain the active task's artifacts while preserving all independent mainline files.
---

## 2026-08-04 22:00 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Addressed the final blocking review finding for synchronous pool dispatch. `WorkstationPoolBoundary.PublishWithAdmission` now forwards the Workers-owned admission signal to Worker Sessions before either asynchronous publication returns or synchronous publication waits for terminal completion. Worker Sessions marks the exact dispatch controllable at that signal, then still lets the existing callback own its terminal result.
- Added deterministic production-boundary coverage for a running `Async:false` dispatch: terminate reaches its exact dispatch before natural completion and returns the established canceled terminal result. Added a Factory Runtime fan-out integration test that starts two synchronous Worker Sessions through the real boundary and proves the first controlled child completes before the second is attempted.
- Files changed: `pkg/services/workers/workstation_pool_boundary_{contracts,impl}.go`, `pkg/services/worker_sessions/internal/service/**`, `pkg/services/worker_sessions/wire/wire_test.go`, `pkg/services/factory_runtime/internal/services/orchestration/runtime/worker_session_control_fanout_test.go`, `prd.json`, `progress.txt`.
- Verification: focused Worker Sessions/Runtime/Workers tests; `go test -race -count=10` for all production-boundary race cases and the synchronous fan-out case; focused `go vet`; `go test ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/... ./pkg/services/workers/...`; `make test`; `git diff --check`.
- **Learnings for future iterations:**
  - Workers admission is a control-plane fact, not a completion fact: synchronous callers must see it while their publish call is still blocked on the terminal callback.
  - A production-boundary fan-out test can prove sibling progress deterministically by blocking each admitted dispatch on exact cancellation rather than using timers or polling.
---

## 2026-08-04 22:12 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Restored the Worker Sessions package's enforced 100% unit-coverage floor after the synchronous-admission review fix introduced an unexercised terminal-state guard.
- Added a behavioral lifecycle test proving that a late admission/running transition cannot resurrect an already completed Worker Session or replace its terminal result.
- Files changed: `pkg/services/worker_sessions/internal/service/internal_test.go`, `prd.json`, `progress.txt`.
- Verification: focused Worker Sessions/Factory Runtime/Workers tests; focused `go vet`; the relevant production-boundary and terminal-state tests with `-race -count=10`; Worker Sessions atomic statement coverage 100.0%; `git diff --check`.
- **Learnings for future iterations:**
  - Every new lifecycle callback needs a terminal-state race proof: callbacks arriving after completion must preserve the committed terminal snapshot rather than re-open the session.
  - Per-package coverage floors are enforced by CI manifests, so add focused behavioral coverage for new safety guards instead of relaxing a baseline.
---

## 2026-08-04 22:31 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Addressed the final blocking Worker Sessions termination review finding. An exact boundary `ALREADY_CANCELED` result now makes `Terminate` wait for the canonical dispatch callback, then return a detached `NOOP` containing the established canceled terminal snapshot. Ordinary `Cancel` retains its intentional non-joining behavior for that already-canceled result.
- Added a deterministic real production-boundary regression: a first exact boundary cancellation commits the Workers cancellation while holding its callback; a following `Terminate` must not return until the callback delivers the canceled result.
- Files changed: `pkg/services/worker_sessions/internal/service/control.go`, `pkg/services/worker_sessions/internal/service/control_production_boundary_test.go`, `prd.json`, `progress.txt`.
- Verification: focused production race test with `-race -count=10`; `go test ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/... ./pkg/services/workers/...`; Worker Sessions atomic statement coverage 100.0%; focused `go vet`; `make test`; `git diff --check`.
- **Learnings for future iterations:**
  - A Workers `ALREADY_CANCELED` response is a terminal control fact, but it does not guarantee that Worker Sessions has observed the result callback yet.
  - Model cancellation/callback interleavings through production-boundary fakes with explicit channels so terminal joins are observable without timing assumptions.
---

## 2026-08-04 22:49 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Addressed the remaining blocking review feedback for repeated and cross-action termination. Every Worker Sessions no-op `Terminate` now joins its authoritative dispatch callback and reloads the terminal snapshot, including `Cancel` followed by `Terminate` and concurrent duplicate `Terminate` calls.
- Added deterministic callback-held behavioral tests that prove Cancel-then-Terminate and concurrent Terminate both return only after the callback, retain the established terminal state, and issue exactly one exact boundary cancellation.
- Files changed: `pkg/services/worker_sessions/internal/service/control.go`, `pkg/services/worker_sessions/internal/service/control_test.go`, `prd.json`, `progress.txt`.
- Verification: focused Worker Sessions controls and production-boundary races with `-race -count=10`; Worker Sessions 100.0% statement coverage; Worker Sessions, Factory Runtime, and Workers package suites; focused `go vet`; `git diff --check`.
- **Learnings for future iterations:**
  - `controlDone` proves the single cancellation side effect completed, not that the Worker Sessions terminal callback has committed; the `done` channel is the terminal-result join barrier.
  - A Terminate retry after Cancel must preserve the canceled result rather than manufacture a terminated result, while still waiting for the same callback.
---

## 2026-08-04 23:35 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Addressed the blocking customer-path review finding. ACP now forwards the committed Chat turn ID and stable control identity to Factory Sessions; the on-demand target sends turn-scoped `CANCEL` to the exact captured Factory Runtime before it cancels the invocation context or begins teardown, preserving Factory Runtime and Worker Sessions result authority.
- Retained each successful captured Runtime control under the opaque target generation lock. A retry after the target runtime is replaced cannot fan out to the later turn. Live Factory Session pause/resume forwarding now preserves the same turn/control identity instead of sending an empty Runtime request.
- Added deterministic ACP ingress, on-demand target, and live Runtime behavioral assertions. The target regression proves the Worker Session control request reaches the original Runtime before invocation cancellation and that a retry does not reach the replacement Runtime.
- Files changed: `pkg/transports/acp/internal/stdio/**`, `pkg/services/factory_sessions/{execution_contract.go,internal/execution/**,internal/ondemandtarget/**,internal/services/live_runtime/**}`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/factory_sessions/... ./pkg/transports/acp/...`; Runtime/Worker Sessions/Workers focused suites; targeted `-race -count=10` target-control test plus repeated Runtime fan-out races; focused `go vet`; `git diff --check`.
- **Learnings for future iterations:**
  - The control request's existing stable request ID is the natural Factory Runtime `ControlID`; only the captured turn ID needs to cross the Factory Sessions boundary explicitly.
  - An on-demand target's activation can be replaced under a stable opaque ID, so turn-control evidence belongs to the generation lock, not the activation object.
---
## 2026-08-04 23:57 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Addressed the remaining blocking customer-terminate ingress finding. ACP `session/close` now carries its committed turn and opaque control identity into Factory Sessions' control-aware target termination operation, which fans `TERMINATE` through the exact captured Factory Runtime before any invocation cancellation or runtime teardown.
- Preserved target cleanup's generic close behavior for process lifecycle callers while retaining captured TERMINATE evidence under the generation lock, so retries cannot select a replacement runtime or turn.
- Added deterministic ACP close ingress and on-demand target tests proving exact captured identities, pre-cleanup termination, idempotent retry behavior, and no invocation-context cancellation bypass.
- Files changed: `pkg/transports/acp/internal/stdio/**`, `pkg/services/factory_sessions/{target_execution_contract.go,internal/ondemandtarget/**}`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - A generic runtime close does not carry customer turn ownership; exposing a distinct target-termination seam prevents an otherwise correct Runtime fan-out from being bypassed at ingress.
- Keep opaque downstream control IDs action-specific, so separate committed CANCEL and TERMINATE controls cannot collide while each remains safely retryable.
---

## 2026-08-05 00:31 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Addressed the remaining review-evidence blocker with a deterministic package-integration proof that enters through the real Factory Sessions on-demand target termination boundary and composes the real Factory Runtime captured-turn selector, Worker Sessions service, and synchronous Workers boundary.
- The test starts two captured children plus one live replacement-turn child. It submits the committed close with a canceled caller context, proves both captured dispatches reach exact `WorkstationPoolBoundary.Cancel` before target cleanup, verifies their existing callback-owned canceled terminal results, and confirms the replacement child receives no control until explicit test cleanup.
- Files changed: `pkg/services/factory_runtime/internal/services/orchestration/runtime/captured_turn_terminate_integration_test.go`, `pkg/services/factory_runtime/internal/services/orchestration/runtime/worker_session_control_fanout_test.go`, `prd.json`, `progress.txt`.
- Verification: focused integration test; `go test -race -count=10` for the integration path; Factory Runtime, Worker Sessions, and Factory Sessions suites; focused `go vet`; `git diff --check`.
- **Learnings for future iterations:**
  - A cross-service package-integration test can retain internal Runtime construction while entering Factory Sessions through its public wire API, avoiding a test-only production seam.
  - A controlled dispatch that cannot finish without its exact boundary cancellation proves fan-out ordering and detached-context behavior more directly than observing a recording Runtime fake.
---

## 2026-08-04 21:35 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-001
- Added an isolated, bounded `acpx@0.13.0` acceptance test that builds the checked-out CLI, launches `you serve acp` through structured custom-agent argv, and verifies machine-readable session creation, protocol negotiation, and the shipped Factory Builder target before closing the session.
- Corrected `you serve acp` startup to run system initialization, ensuring the shipped packaged Factory catalog is available for a fresh ACP client home; added focused CLI regression coverage.
- Documented the safe reproduction path and enabled the opt-in real-client proof in the functional CI lane. Updated the UI compiler configuration to acknowledge the TypeScript 5.9 `baseUrl` deprecation so the existing typecheck gate remains green.
- Files changed: `.github/workflows/ci.yml`, `docs/reference/serve-acp.md`, `pkg/transports/cli/root_factory.go`, `pkg/transports/cli/root_serve_test.go`, `tests/functional/transport/acp/realclient/pinned_acpx_test.go`, `ui/tsconfig.app.json`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - `acpx@0.13.0` requires Node.js 22.13.0 or newer; the functional CI lane must install Node 22 before it runs the opt-in evidence test.
  - Fresh `you serve acp` state needs system initialization so its default `factory:@you/factory-builder` target is installed before ACP `session/new` resolves the catalog.
  - Run `bun install --frozen-lockfile` from `ui/` before `make typecheck` in a fresh worktree; the shared `tsconfig.app.json` setting is inherited by each typecheck project.
---

## 2026-08-04 21:50 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Extended the pinned `acpx@0.13.0` acceptance path through one ephemeral prompt against the shipped Factory Builder target. It asserts a non-empty assistant-result fact, one `end_turn` result, and exactly one disposable Codex-shaped provider invocation without retaining prompt, response, JSON-RPC, command, credential, or host-path payloads.
- Corrected the directly observed ACP blocking defect: Factory Sessions now maps text compatibility input to a Factory signature's primary positional or stdin parameter. The Factory Builder package accepts an omitted name and safely directs Builder to derive one or report a collision, allowing the default ACP target to admit an ordinary text turn.
- Regenerated packaged Factory artifacts and updated the canonical ACP and Factory-authoring documentation with the sanitized command, evidence facts, and no-transcript boundary.
- Files changed: `docs/reference/{authoring-factories,serve-acp}.md`, `packages/packaged-factories/{factories/factory-builder/{factory.yaml,prompts/build-factory.md},factory_builder_test.go,generated/{factories/factory-builder/{factory.json,factory.yaml},manifest.json}}`, `pkg/services/factory_sessions/internal/invocation/{session_owner.go,session_owner_test.go}`, `tests/functional/transport/acp/realclient/pinned_acpx_test.go`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - ACP `session/prompt` exposes text content rather than named Factory arguments; Factory Sessions must normalize that text through a signature's declared primary input before runtime interpolation.
  - A real-client acceptance test can exercise the production Codex subprocess adapter with a scenario-local executable, while a marker containing only `codex` proves identity and cardinality without preserving provider traffic.
  - Regenerate `packages/packaged-factories/generated/` with `make packaged-factory-catalog-generate` after changing an authored packaged Factory.
---

## 2026-08-04 21:59 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Verified the completed real-client story with the focused Factory Sessions and packaged-Factory tests, generated-catalog check, documentation CLI smoke test, pinned Node 22.13 acpx prompt run, and UI typecheck. Updated the packaged-docs smoke contract to use the sanitized assistant-result fact rather than the removed raw protocol marker.
- Files changed: `tests/functional/smoke/cli_docs_smoke_test.go`, `prd.json`, `progress.txt` (with the story's previously recorded implementation files).
- **Learnings for future iterations:**
  - The docs CLI smoke test checks literal embedded Markdown; choose a marker that does not span a source line break when documenting a sanitized protocol fact.
  - `make lint` currently stops at 87 pre-existing backend-size findings outside this story; capture that baseline constraint separately from focused change verification.
---

## 2026-08-04 22:11 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Repaired the failed Backend Functional Coverage lane by retaining the existing supplied-name prompt forms alongside the new optional-name guidance, then regenerated the packaged Factory catalog.
- Files changed: `packages/packaged-factories/factories/factory-builder/prompts/build-factory.md`, `packages/packaged-factories/generated/factories/factory-builder/{factory.json,factory.yaml}`, `packages/packaged-factories/generated/manifest.json`, `progress.txt`.
- **Learnings for future iterations:**
  - The Factory Builder functional command-runner validates its real provider prompt as part of the public builder-flow coverage; preserve supplied-name instructions when extending that prompt for omitted-name ACP input.
  - `go test ./tests/functional/factory/packaged/factory_builder -count=1 -timeout 2m`, `go test ./packages/packaged-factories -run TestFactoryBuilder -count=1 -timeout 2m`, and `make packaged-factory-catalog-check` validate this contract narrowly.
---

## 2026-08-04 22:40 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Addressed the blocking real-client review: added a separate required non-short CI run for `acpx@0.13.0`, strict prerequisite failure behavior, and a fact-only terminal artifact containing the revision, protocol/session/result/provider, cleanup, and outcome facts.
- Replaced inherited child environments with a minimal platform-safe allowlist, isolated transient paths, added Unix process-group and Windows process-tree timeout cleanup, and covered a timed-out descendant process to prove it terminates.
- Reconciled the scoped quality criterion with the confirmed 87 unrelated repository `backend-size` baseline violations rather than reporting root `make lint` as passing. Updated the ACP reference documentation with the sanitized command, CI evidence, isolation, and cleanup guarantees.
- Files changed: `.github/workflows/ci.yml`, `docs/reference/serve-acp.md`, `tests/functional/transport/acp/realclient/{pinned_acpx_test.go,pinned_acpx_process_test.go,pinned_acpx_process_unix_test.go,pinned_acpx_process_windows_test.go}`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - An opt-in test must turn missing pinned-runtime prerequisites into a failure in its required CI lane; leaving normal local skips intact avoids forcing a networked client into the short suite.
  - `exec.CommandContext` only guarantees direct-process cancellation. For owned client trees, use OS-specific group/tree termination and assert a recorded descendant has exited after the timeout path.
  - A retained real-client artifact can stay safe by encoding only asserted facts and a full repository SHA; never capture the command output, transcript, temp location, or effective environment.
---

## 2026-08-04 23:00 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Addressed the outstanding blocking PR feedback for the Windows non-zero-exit path. Every bounded real-client command now has an owned process tree; Windows uses a kill-on-close Job Object and Unix retains its process group through cleanup.
- Added a behavioral process-boundary regression that starts a descendant, has its direct parent exit non-zero, and verifies the descendant has terminated. The existing timeout case continues to prove timeout cleanup.
- Files changed: `tests/functional/transport/acp/realclient/{pinned_acpx_test.go,pinned_acpx_process_test.go,pinned_acpx_process_unix_test.go,pinned_acpx_process_windows_test.go}`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - On Windows, cleanup by parent PID is insufficient after `Wait` reaps that parent; a Job Object retained by the scenario owns remaining descendants independently of that PID.
  - The process-boundary test helper can use a controlled non-zero parent exit plus a recorded descendant PID to verify cleanup without inspecting internal implementation state.
  - `go test ./tests/functional/transport/acp/realclient -count=10 -timeout 45s` and a Windows cross-compile provide focused regression evidence when the local host cannot execute Windows process semantics.
---

## 2026-08-04 23:18 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Rechecked active PR #1798 after its final process-cleanup commit. The historical blocking feedback is satisfied at the current head: Windows owns each child tree with a kill-on-close Job Object, and the non-zero-parent behavioral regression proves its descendant terminates. No unresolved review threads remain; all required CI checks for the implementation head are successful.
- Updated the PR description from the current `prd.json` so its retained scope and acceptance evidence match the checked-in plan.
- Files changed: `progress.txt`, PR #1798 description.
- **Learnings for future iterations:**
  - A branch can be associated with a PR in its fork/upstream repository even when `gh pr view` against the local `origin` cannot find it; resolve the PR URL returned by `gh pr create` and query that repository explicitly.
  - The GitHub issue-comment feed can retain historical blocking comments after their requested commit lands; inspect thread resolution, comment target heads, current diff, and current required checks together before treating feedback as outstanding.
---

## 2026-08-04 23:35 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Corrected the canonical ACP guide after final PR review: Windows process-tree cleanup is established by a retained kill-on-close Job Object, while `taskkill /T` is only a fallback if Job Object ownership cannot be established. The guide now also names both timeout and non-zero-parent descendant-termination coverage.
- Files changed: `docs/reference/serve-acp.md`, `progress.txt`.
- **Learnings for future iterations:**
  - Reference documentation for process guarantees must name the mechanism that establishes the guarantee on every terminal path; a PID-based fallback cannot describe reaped-parent cleanup as the primary behavior.
---

## 2026-08-05 00:11 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-002
- Addressed the current PR review findings by removing the new ACP marker inventory from the legacy docs smoke test and the unrelated UI TypeScript compiler suppression. The canonical ACP guide retains the existing `session/update` and `stopReason` facts as accurate protocol documentation, while real-client process-boundary coverage remains the behavioral proof.
- Files changed: `docs/reference/serve-acp.md`, `tests/functional/smoke/cli_docs_smoke_test.go`, `ui/tsconfig.app.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Historical docs smoke assertions can require legacy protocol terms even when a feature change must not add new marker-inventory coverage; retain the accurate terminology in its canonical guide and prove the feature through the owning runtime boundary.
  - `make docs-reference-smoke`, `make typecheck`, and `go test ./tests/functional/transport/acp/realclient -count=1 -timeout 90s` are focused follow-up evidence for this review correction.
---

## 2026-08-05 00:41 -07:00 - ACP-L4-W6-WORKER-CONTROL-FANOUT-004
- Merged current `origin/main` to clear the PR mergeability conflict. The only conflicting files were the task-local `prd.json` and `progress.txt`; this task's PRD and review-remediation records were retained, while all independent mainline ACP, Factory Builder, CI, and documentation changes were preserved.
- Files changed: merged current `origin/main`; reconciled `prd.json` and `progress.txt`.
- Verification: JSON parsing confirms all four L4 W6 stories remain `passes:true`; focused Factory Runtime, Worker Sessions, and Factory Sessions suites plus `-race -count=10` integration verification pass; `git diff --check` is clean.
- **Learnings for future iterations:**
- Task-local PRD/progress conflicts can coexist with independent mainline progress entries; retain the active task records in the conflicted section and preserve non-conflicting mainline history.
---

## 2026-08-05 05:06 -07:00 - ACP-L3-DEF-CONSOLIDATE-AUTHORED-VALIDATION-001
- Published `ValidatedAuthoredFactoryDefinitionLoader`, a one-operation Factory Definitions contract with plain selection request, detached effective-definition result, selected-source identity, validation facts, and payload-safe typed failure vocabulary.
- Implemented the inert Definitions-owned capability using the existing current/direct source loaders and blocking validator. Its success path returns cloned effective definition and bundled-file facts without exposing mutable loaded-source or validator authority.
- Added canonical process composition: `pkg/wire` constructs one instance and Factory Sessions Runtime Opening retains the exact injected capability for the later loading-path migration, without adding lifecycle work or per-session construction.
- Added observable construction coverage that panics if loading or validation runs before invocation, plus focused detached-result behavior and runtime-opening direct-injection coverage.
- Files changed: `pkg/services/factory_definitions/authored_source_contract.go`, `pkg/services/factory_definitions/internal/services/authoredloading/**`, `pkg/services/factory_definitions/wire/{compile_load_bindings.go,construction_ports_test.go}`, `pkg/services/factory_sessions/internal/runtimeopening/{factory.go,factories_test.go}`, `pkg/wire/{factory_definition_services.go,wire.go,wire_gen.go}`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/factory_definitions/... ./pkg/services/factory_sessions/internal/runtimeopening ./pkg/wire -count=1`; focused `go vet`; `make typecheck`; `make verify-fast`; `make generate-wire`; `git diff --check`.
- **Learnings for future iterations:**
  - The canonical loader’s `LoadRuntimeSource` preserves current-directory resolution, while `LoadSourceFromFactoryDir` preserves explicit source/directory selection; the focused capability can compose both without changing their internals.
  - `pkg-file-count` and `pkg-boundary` currently fail on recorded repository-wide debt, but neither reports the new `authoredloading` leaf or this story’s changed boundaries.
---

## 2026-08-05 05:16 -07:00 - ACP-L3-DEF-CONSOLIDATE-AUTHORED-VALIDATION-002
- Preserved the actual authored root selected during compilation as an immutable source-identity fact, so the focused loader now reports the concrete `factory.json`, `factory.yaml`, or `factory.yml` selected from a direct or current directory rather than only the caller-provided directory.
- Added Definitions-owned behavioral coverage for explicit JSON/YAML/YML inputs, current and direct resolution, split worker/workstation bodies and prompts, Runtime base-directory overrides, bundled-file materialization, blocking validation, and detached nested results across repeated loads.
- Files changed: `pkg/services/factory_definitions/{authored_source_contract.go,internal/services/{authoredloading/{service.go,service_test.go},compilation/{loadedsource/source.go,loading/loader.go}},wire/compile_load_equivalence_test.go}`, `prd.json`, `progress.txt`.
- Verification: `go test -race ./pkg/services/factory_definitions/wire -run 'TestValidatedAuthoredFactoryDefinitionLoader' -count=10`; `go test ./pkg/services/factory_definitions/... -count=1`; `go vet ./pkg/services/factory_definitions/...`; `make typecheck`; `make verify-fast`; `git diff --check`.
- **Learnings for future iterations:**
  - The direct loader accepts a concrete root file or an unambiguous Factory directory; the current-directory resolver intentionally retains its existing `factory.json` fallback behavior.
  - A repeated real-loader invocation after mutating nested returned definition data proves detachment without a source-topology meta test.
---

## 2026-08-05 06:01 -07:00 - ACP-L3-DEF-CONSOLIDATE-AUTHORED-VALIDATION-003
- Classified focused authored-definition loading failures as missing, malformed, unresolved, validation, or dependency while retaining safe selected-source identity, actionable validation targets, and the raw cause for `errors.Is`/`errors.As` callers. Failure results are always empty.
- Attached the stable Factory Definitions failure sentinels at the authored source, root-selection, decoding, manifest, and split-runtime resolution boundaries. This removed error-message inference from the capability while preserving existing contextual error text and causes.
- Added real-loader behavioral coverage for absent selected files, rootless directories, malformed JSON, missing split content, unsafe portable content, and blocking findings; focused capability coverage additionally proves non-blocking warnings/hints, cancellation, and injected dependency failures.
- Files changed: `pkg/services/factory_definitions/{authored_source_contract.go,internal/services/{authoredloading/{service.go,service_test.go},authoring_layout/authoredlayout/source.go,compilation/loading/loader.go},wire/compile_load_equivalence_test.go}`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/factory_definitions/... -count=1`; `go test -race ./pkg/services/factory_definitions/internal/services/authoredloading ./pkg/services/factory_definitions/wire -run 'TestValidatedAuthoredFactoryDefinitionLoader|TestLoadValidatedAuthoredFactoryDefinition' -count=5`; `go vet ./pkg/services/factory_definitions/...`; `make typecheck`; `make verify-fast`; `git diff --check`.
- **Learnings for future iterations:**
  - A typed failure's `Unwrap` can return `errors.Join(classificationSentinel, cause)` so callers retain both the stable failure category and the underlying filesystem, parser, validation, or dependency cause.
  - Direct real-loader tests are a useful companion to injected-failure tests: they prove that selection/decoding/layout boundaries assign the intended category before the focused capability receives the error.
---

## 2026-08-05 05:39 -07:00 - ACP-L3-DEF-CONSOLIDATE-AUTHORED-VALIDATION-004
- Migrated the live Factory Session Runtime-opening path to request authored input exclusively through `ValidatedAuthoredFactoryDefinitionLoader`. Current-Factory selection and blocking validation stay in Factory Definitions; the runtime-opening factory no longer receives a separate named-path resolver for this load path or repeats its validation.
- Rebuilt the existing Runtime mutable source only from the capability's detached effective definition, Factory directory, Runtime base directory, bundled replacement facts, and selected-root identity. Replay loading and later Runtime-owned reload hooks retain their existing behavior.
- Added behavioral coverage for explicit selection forwarding, effective-definition/directory/base/bundled/identity preservation, and typed malformed failure propagation before mutable source construction. Updated construction and cleanup tests to inject the focused capability directly; regenerated canonical Wire output.
- Files changed: `pkg/services/factory_definitions/{authored_source_contract.go,internal/services/compilation/loading/loader.go}`, `pkg/services/factory_sessions/internal/runtimeopening/{construction.go,factory.go,open.go,runtime_load.go,runtime_load_test.go,runtime_root_test.go,factories_test.go,models_bind_test.go}`, `pkg/wire/wire_gen.go`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/factory_sessions/... ./pkg/services/factory_definitions/... ./pkg/wire -count=1`; focused `go test -race -count=5`; focused `go vet`; `make generate-wire`; `make verify-fast`; `git diff --check` all pass. `make lint` reaches 90 existing repository-wide `backend-size` findings; no changed package appears in that report.
- **Learnings for future iterations:**
  - A fully effective detached Factory definition can retain split-layout behavior when Runtime creates its normal mutable loaded source with an empty runtime-definition lookup; the public source factory performs its own clone and normalization.
  - Moving current-Factory selection into the focused loader removes a Runtime-facing `NamedPathResolver` dependency while preserving direct-source home normalization at the Factory Sessions boundary.
---

## 2026-08-05 01:24 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-001
- Implemented a Worker Sessions-owned detached Provider Session association that retains the exact providers.SessionRef with immutable Worker Session, Factory turn, dispatch, and attempt correlations. The association validates typed Provider identity, is cloned in all returned snapshots, accepts exact duplicate observations, and rejects conflicting, malformed, foreign-attempt, missing-session, and terminal writes without mutating the accepted or sibling association.
- Connected normal Workers completion to retain the exact provider/kind/opaque ID before terminal publication. Direct Workers-side observation can commit the association before a dependent Worker record is published; absent provider metadata remains explicitly unassociated and is never synthesized from the runner, model, or bare ID.
- Updated Factory Runtime test doubles for the expanded public Worker Sessions interface and added controlled behavioral coverage for ordering, detachment, duplicate/conflict behavior, invalid/missing/terminal writes, exact completion retention, and no-reference completion.
- Files changed: pkg/services/worker_sessions/{contracts.go,session.go,internal/service/{service.go,control.go,internal_test.go,start_test.go}}, Factory Runtime test adapters, prd.json, and progress.txt.
- Verification: go test ./pkg/services/worker_sessions/...; go test -race -count=10 ./pkg/services/worker_sessions/internal/service -run 'Test(Start_RetainsExactProviderSessionAssociationFromWorkerResult|AssociateProviderSession_)'; go test ./pkg/services/factory_runtime/...; go vet ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/...; and make test all pass. git diff --check passes. make pkg-file-count remains blocked by 48 unrelated pre-existing package-count findings (none in Worker Sessions). make typecheck remains blocked before this Go-only change by the existing missing Bun types and TypeScript 6 baseUrl deprecation errors.
- **Learnings for future iterations:**
  - Worker result Provider Session metadata is the trusted handoff from Workers; preserve its Provider, Kind, and opaque ID directly rather than deriving any field during Worker Sessions completion.
  - The Work dispatch request ID already supplies the stable Factory-turn correlation, while the Workers dispatch ID remains both the dispatch and provider-attempt identity.
  - Add the public Worker Sessions method to Factory Runtime test adapters at the narrow interface seam; runtime production composition does not need an association-specific dependency.
---

## 2026-08-05 01:28 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-001
- Added the completion-path regression for malformed Provider Session metadata: the session remains explicitly unassociated and emits only safe Worker Session/attempt rejection evidence.
- Files changed: pkg/services/worker_sessions/internal/service/start_test.go and progress.txt.
- Verification: go test -count=1 -cover ./pkg/services/worker_sessions/internal/service reports 100.0% statement coverage; the repeated focused race test and git diff --check pass.
- **Learnings for future iterations:**
  - A Provider Session returned by Workers is still untrusted input at the Worker Sessions boundary; preserve valid metadata exactly, but reject invalid metadata visibly without synthesizing a resumable state.
---

## 2026-08-05 01:44 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-002
- Implemented truthful Worker Session pause/resume continuation. A Worker Session with a committed exact Provider Session association pauses only by cancelling its exact admitted Workers dispatch; the original runtime completion callback remains pending until the same Worker Session's continuation finishes.
- Resuming creates one fresh continuation dispatch that preserves the Worker Session's workstation, Factory-turn request, and lineage payload while carrying the stored provider, provider-specific kind, and opaque ID unchanged. Workers propagates and clones this reference through both agent execution paths, and the provider runner routes it exclusively through `providers.Service.Continue`, ahead of the legacy bare `SessionID` compatibility path.
- Added behavioral coverage for pause/resume ordering, exact reference retention, resumed dispatch correlation, terminal repeated-resume no-op, detachment, and zero ordinary provider execution on the controlled exact-reference path.
- Files changed: `pkg/services/worker_sessions/internal/service/{control.go,control_test.go}`, `pkg/services/workers/execution_requests.go`, `pkg/services/workers/internal/services/runners/internal/services/agent/internal/service/{runner.go,runner_test.go}`, `pkg/services/workers/internal/services/workstations/executor/{agent.go,agent_inference_test.go,agentrun/executor.go}`, `prd.json`, `progress.txt`.
- Verification: `go test ./pkg/services/worker_sessions/... ./pkg/services/workers/...`; `go test ./pkg/services/factory_runtime/...`; focused `go test -race -count=10` continuation coverage; focused `go vet` for Worker Sessions, Workers, and Factory Runtime; and `make test` all pass. `git diff --check` passes.
- **Learnings for future iterations:**
  - A pause that promises resumability must retain the original Worker Session completion path until its continuation reaches a terminal callback; returning the pause cancellation as the Factory result would falsely terminalize the turn.
  - A continuation needs a new Workers dispatch identity for pool admission, but it must retain the original Worker Session association and Factory-turn lineage rather than exposing a replacement Worker Session.
- The exact typed reference must outrank legacy configuration session IDs at the provider edge, or a valid continuation can silently lose its provider-specific session kind.
---

## 2026-08-05 02:02 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-003
- Added stable typed rejection for a paused Worker Session missing its exact Provider Session association, and reject malformed retained associations before any continuation handoff. Rejections leave the session paused and its association unchanged.
- Preserved Provider continuation classifications for invalid, foreign, stale, unsupported, and operational failures through the runner, Agent/Agent-Run execution paths, and into safe Worker Session terminal evidence. The classifications contain only whitelisted enum values; no provider message or opaque session ID is copied into terminal diagnostics or Factory Event contracts.
- Added deterministic controlled Provider-edge and Worker Session tables proving zero ordinary provider executions on rejected exact continuations, retained associations, terminal retry no-ops, and repeated race safety.
- Files changed: `pkg/services/{worker_sessions,workers}/**`, `prd.json`, `progress.txt`.
- Verification: focused Worker Sessions/Workers/Providers tests and `go vet`; `go test ./pkg/services/workers/... ./pkg/services/providers/... ./pkg/services/factory_runtime/...`; repeated `-race -count=10` Worker Sessions continuation coverage; and `make test` all pass. `make typecheck` remains blocked by pre-existing missing Bun types and TypeScript 6 `baseUrl` deprecation errors. `make pkg-file-count` remains blocked by the documented unrelated 48 baseline violations; no package grew in this story.
- **Learnings for future iterations:**
  - Treat an exact Provider Session reference as an input boundary even after it has been stored: validate it again immediately before continuation handoff, and return a typed Worker Session failure instead of attempting repair or fallback.
  - Keep safe provider continuation classifications as closed enum facts; raw provider messages and opaque IDs may remain inside the owning provider path but must not become Worker Session terminal diagnostics.
  - Keep Factory Event schemas stable when only in-process control inspection needs additional fidelity by carrying the extra Worker result facts with `json:"-"` and reducing them at the Worker Session boundary.
---

## 2026-08-05 02:13 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-004
- Added a deterministic Factory Runtime package-integration proof for two paused children of one captured turn. The real Worker Sessions service gives each continuation its exact typed Provider Session reference and preserved turn/dispatch correlation; a controlled foreign failure completes before its sibling succeeds without any cross-delivery.
- Proved duplicate Factory resume retains the committed fan-out result without another continuation, while an associated Worker Session from another turn remains running and is never selected or resumed.
- Files changed: `pkg/services/factory_runtime/internal/services/orchestration/runtime/captured_turn_terminate_integration_test.go`, `prd.json`, `progress.txt`.
- Verification: focused test repeated 20 times; focused `-race -count=10`; `go test ./pkg/services/worker_sessions/... ./pkg/services/workers/... ./pkg/services/factory_runtime/...`; focused `go vet` for the same packages; and `git diff --check` all pass.
- **Learnings for future iterations:**
  - To prove multi-child continuation isolation, compose the real Factory Runtime fan-out and Worker Sessions service behind an asynchronous controlled Workers boundary, then release child terminal callbacks in a deliberate order; this makes cross-delivery observable without sleeps or polling.
  - A per-child Provider continuation failure is terminal evidence for that Worker Session only; Factory Runtime still fans out the already accepted resume to every captured sibling and retains its deterministic control result.
---

## 2026-08-05 02:15 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-004
- Re-ran the repository Go unit lane after the completed multi-child continuation integration test; `make test` passes.
- `make typecheck` remains blocked before this Go-only change by the documented UI environment/configuration baseline: missing Bun type definitions and TypeScript 6 `baseUrl` deprecation errors in three UI tsconfig projects.
- Files changed: `progress.txt`.
- **Learnings for future iterations:**
  - Record repository-wide gate failures with their exact external baseline separately from focused behavior evidence, so the PR can distinguish an unrelated UI toolchain condition from the changed runtime path.
---

## 2026-08-05 02:34 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-004
- Restored the CI-required Worker Sessions coverage baselines without changing production behavior. Added public-contract assertions for detached Provider Session associations, association requests, session ownership validation, control request validation, and safe continuation-classification rejection.
- Added deterministic internal control-path coverage for rejected/missing/mismatched associations, continuation preparation/reversion, publication failure recovery, duplicate/pre-admission/boundary-error pauses, and an active-control pause interleaving coordinated by channels rather than sleeps.
- Files changed: `pkg/services/worker_sessions/{session_test.go,terminal_test.go,internal/service/internal_test.go}`, `prd.json`, `progress.txt`.
- Verification: `go test -count=1 -race ./pkg/services/worker_sessions/internal/service`; `go test -count=1 -cover ./pkg/services/worker_sessions`; `go test -count=1 -cover ./pkg/services/worker_sessions/internal/service`; and `go test -count=1 ./pkg/services/worker_sessions/...` all pass with 100.0% coverage in both enforced packages. `make test-unit-coverage` reaches its coverage runner but cannot execute on this Windows host because the generated `go test` command exceeds the Windows command-length limit; the individual measured packages pass their declared 100.00% baselines locally.
- **Learnings for future iterations:**
  - The Worker Sessions coverage policy measures the public contract and internal service independently, so both packages need direct behavioral evidence when a public control flow spans private synchronization helpers.
  - For a deterministic active-control wait, use an unbuffered channel sender to prove the receiver reached the control wait, then record the terminal winner under the registry lock; this tests the interleaving without sleeps or polling.
---

## 2026-08-05 03:18 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-001
- Addressed the first blocking PR review finding: Factory Runtime now sends Provider runner progress through a session-local Worker Sessions bridge. The bridge resolves the live dispatch owner and commits the exact typed `providers.SessionRef` before forwarding any reference-bearing response fragment.
- Added trusted observation behavior to Worker Sessions, including dispatch-owner isolation across continuation dispatches. Metadata-only compatibility fields are explicitly suppressed rather than reconstructed into a resumable reference.
- Added controlled provider/workstation behavioral coverage proving association-before-output and pause/resume with the same exact reference without a direct test-side association; malformed, foreign, and conflicting observations remain isolated and rejected.
- Files changed: `pkg/services/{worker_sessions,workers}/**`, `pkg/services/factory_runtime/internal/**`, `prd.json`, `progress.txt`.
- Verification: `go test -count=1 -cover ./pkg/services/worker_sessions ./pkg/services/worker_sessions/internal/service` (both 100.0%); `go test -race -count=1 ./pkg/services/worker_sessions/...`; focused `go vet` plus Workers/Worker Sessions/Factory Runtime suites; `make test`; and `git diff --check` all pass.
- **Learnings for future iterations:**
  - A provider-session response projection is not evidence of a resumable provider reference. Only an exact typed reference emitted by Providers may unlock Worker Session continuation.
  - When construction order prevents direct wiring, bind a one-shot session-local progress bridge before dispatch admission so source progress cannot reach a response stream before its owning Worker Session records the association.
---

## 2026-08-05 03:32 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-002
- Addressed the remaining blocking review findings for continuation controls. A PAUSED Worker Session now reaches `CANCELED` or `TERMINATED` directly without attempting to cancel its already-completed pause dispatch, publishes its terminal record, and releases the original Start wait.
- Exact Provider continuations now validate both the continuation envelope reference and any returned execution reference before publishing resumed output. A missing, malformed, or mismatched result becomes a safe invalid-continuation failure while retaining the original association; the Worker Sessions terminal boundary independently enforces the same invariant for every Workers adapter path.
- Files changed: `pkg/services/{worker_sessions,workers,factory_runtime}/**`, `prd.json`, `progress.txt`.
- Verification: public and internal Worker Sessions coverage are both 100.0%; focused controlled Provider-edge and Worker Sessions regressions pass with `-race -count=5`; `go test ./pkg/services/worker_sessions/... ./pkg/services/workers/... ./pkg/services/factory_runtime/...`; focused `go vet`; `make test`; and `git diff --check` all pass.
- **Learnings for future iterations:**
  - A PAUSED Worker Session has no pending original dispatch callback to join; a terminal control must commit and signal its retained supervision directly instead of re-canceling and waiting forever.
  - `ContinueResult.Reference` and `ExecuteResult.SessionRef` are independent provider-boundary claims. Validate both before exposing resumed output, then keep the accepted Worker Session association immutable on rejection.
---

## 2026-08-05 03:46 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-001
- Corrected the Provider Session progress bridge after CI exposed a compatibility regression: metadata-only failure and timeout fragments remain visible on existing response streams, while only exact typed references can be associated with a Worker Session.
- Updated the bridge behavioral test to prove legacy metadata is forwarded without creating an association; reproduced and restored the ACP command-start failure and Agy timeout response-stream functional flows.
- Files changed: `pkg/services/worker_sessions/{publish.go,publish_test.go}`, `progress.txt`.
- Verification: focused Worker Sessions bridge test, `TestACPCommandStartFailureMapsToDependencyFailure`, `TestAgyGoldenTimeout`, and `git diff --check` pass.
- **Learnings for future iterations:**
  - A compatibility projection can remain observable without granting resumability; association validation belongs only to the exact provider-owned reference.
  - A response-stream regression can surface in unrelated provider fixtures, so retain provider-specific failure and timeout functional checks after changing a shared progress publisher.
---

## 2026-08-05 04:02 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-REVIEW
- Inspected PR #1799 with thread-aware GitHub review data. Its only blocking conversation comment applies to the older `2864f4e` head; the reviewed `f0c8303` head contains the three requested remediations for live Provider Session association, PAUSED cancel/terminate completion, and continuation-result reference validation. There are no unresolved inline review threads or newer review comments.
- Confirmed the PR title and description already match the requested name and current `prd.json`. Re-ran the focused Worker Sessions race suite plus Workers and Factory Runtime suites on `f0c8303`; all pass and `git diff --check` is clean.
- The PR's only failing required CI lane is the shared `customer-factory-emulator-demos` component test, which fails because `ResizeObserver` is missing. The test has identical content and last-touch commit (`5c8260a`) on both `fork/main` and this branch, so it is a separate base regression rather than W7 work and is left out of this single-story delivery lane.
- Files changed: `progress.txt`.
- **Learnings for future iterations:**
  - Thread-aware review inspection is necessary even when top-level comments exist; this PR had no inline threads and no newer feedback after the blocking comment.
  - Compare a failed CI path against the base before treating it as delivery-lane fallout; exact base/head identity is strong evidence to keep an unrelated repair scoped to its owning work.
---

## 2026-08-05 04:31 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-REVIEW
- Addressed the remaining blocking PR feedback: Providers now exposes a live, invocation-scoped exact `SessionRef` observation hook. Codex and Claude emit their native streaming session identities immediately, and ACP emits the identity as soon as its provider session opens, before its prompt completes.
- Wired the real Agent runner to publish that trusted exact reference through the existing session-local Worker Sessions bridge. The bridge commits the association but suppresses its internal bookkeeping fragment from the customer response stream, so a running Worker Session can truthfully pause and resume through `Providers.Continue` using the same opaque reference.
- Added controlled behavioral coverage for native live observation, actual provider/Agent/Worker Sessions composition, exact pause/resume continuation, and registry request wiring. The integration test starts the real runner, obtains a native Codex thread through the production seam, pauses without fabricated Worker fragments or direct association calls, then verifies the resumed command receives that same ID.
- Files changed: `pkg/services/providers/execute_contract.go`, `pkg/services/providers/internal/services/acp/internal/service/{service.go,protocol_failure_classification_test.go}`, `pkg/services/providers/internal/services/execution/internal/adapters/{codex,claude}/**`, `pkg/services/workers/internal/services/runners/{wire,internal/services/agent/internal/service}/**`, `pkg/services/workers/progress_observations.go`, `pkg/services/worker_sessions/{publish.go,publish_test.go}`, `progress.txt`.
- Verification: focused live-composition and adapter tests; `go test ./pkg/services/providers/... ./pkg/services/worker_sessions/... ./pkg/services/workers/... -count=1`; targeted `go test -race`; `go vet` for those service families; `make test`; `git diff --check`.
- **Learnings for future iterations:**
  - Provider session creation and provider result delivery are different lifecycle moments; the Worker Sessions control plane needs the former, while response streams should only receive customer-visible output.
  - An invocation-scoped callback is safe to preserve through the Providers continuation request because it carries no selectable session input; native adapters still own the identity and only publish validated typed references.
  - Controlled command/ACP edges plus channels can prove an observation happened during a live attempt without sleeps, polling, fabricated Worker fragments, or direct association calls.
---

## 2026-08-05 05:11 -07:00 - ACP-L4-W7-PROVIDER-SESSION-CONTINUATION-REVIEW
- Re-inspected PR #1799 using flat GitHub conversation reads and thread-aware GraphQL data. The two blocking comments both target superseded heads; there are no unresolved review threads, newer review submissions, or additional actionable feedback on the current `9299cc803` head.
- Investigated the initially failing Backend Functional Coverage job. Its first two attempts failed in changing, unrelated functional scenarios; the focused ACP scenarios from the failure logs passed 20 deterministic local repetitions each, and the other failed paths were unchanged from `origin/main`. A third rerun on the exact reviewed head passed every required check, including Backend Functional Coverage.
- Files changed: `progress.txt`.
- Verification: `go test -count=20 ./tests/functional/providers/acp -run '^TestProvidersACPSerializesConcurrentPromptsOnOneStdioConnection$'`; `go test -count=20 ./tests/functional/providers/acp -run '^TestFactoryRunRetainsLegacyNamedExecutorProviderCompatibility$'`; `gh pr checks 1799 --required`; thread-aware `fetch_comments.py`; and `git diff --check origin/main...HEAD` all pass. PR #1799 is open, clean, and points at the reviewed head.
- **Learnings for future iterations:**
  - A CI-only functional failure must be evaluated across reruns and against the PR base before changing a feature path; unrelated failures that rotate between unchanged functional packages are evidence of shared-suite instability, not an authorization to expand the story.
  - When an existing PR already has the requested title and full `prd.json` body, update its head rather than creating a duplicate pull request.
---

## 2026-08-05 05:43 -07:00 - ACP-L3-DEF-CONSOLIDATE-AUTHORED-VALIDATION
- Merged current `fork/main` to resolve PR #1800's mergeability conflict. Mainline provider-session continuation work merged automatically; only task-local `prd.json` and `progress.txt` conflicted.
- Retained this task's completed four-story PRD and delivery record, while preserving incoming reusable patterns and all non-conflicting mainline progress history.
- Files changed: merged `fork/main`; reconciled `prd.json` and `progress.txt`.
- Verification: Factory Sessions Runtime-opening, all Factory Definitions, and canonical Wire tests pass after the merge; JSON parsing and `git diff --check` are clean.
- **Learnings for future iterations:**
  - When independent worktrees use task-local PRD files, resolve a shared-file merge conflict by preserving the active branch's PRD and appending both progress histories; source ownership remains independent of task tracking.
---

## 2026-08-05 05:55 -07:00 - ACP-L3-DEF-CONSOLIDATE-AUTHORED-VALIDATION-004
- Repaired the Runtime migration's public diagnostic regression identified by PR #1800's Backend Functional Coverage lane. `AuthoredFactoryDefinitionLoadFailure.Error()` now includes its original cause after the safe typed classification and selected source, preserving actionable blocking-validation, malformed-source, missing-layout, and mapping details through Runtime's outer error wrapping.
- Added a behavioral assertion that every focused authored load failure renders its retained underlying diagnostic, while maintaining its typed `errors.Is`/`errors.As` contract.
- Files changed: `pkg/services/factory_definitions/authored_source_contract.go`, `pkg/services/factory_definitions/internal/services/authoredloading/service_test.go`, `progress.txt`.
- Verification: focused authored loading and Runtime-opening unit tests; the five authored-loading functional failures from CI; `TestCodexGoldenTextAndToolSuccess` repeated three times; and `git diff --check` all pass.
- **Learnings for future iterations:**
  - A `%w` boundary preserves inspection through `errors.Is` and `errors.As` but still renders only the outer `Error()` text, so public diagnostic contracts need deliberate message retention.
  - When CI reports several failures, target the ones causally tied to the diff first and rerun unrelated failures before treating them as code defects.
---

## 2026-08-05 06:05 -07:00 - ACP-L3-DEF-CONSOLIDATE-AUTHORED-VALIDATION-004
- Addressed the next PR #1800 CI failure: the new Factory Definitions `authoredloading` package participates in the unit-coverage lane and now has its required sorted package floor (`72.41%`, calculated from the focused 42/58 statement profile) in the checked-in unit coverage manifest.
- Files changed: `docs/internal/baselines/go-unit-coverage-package-minimums.json`, `progress.txt`.
- Verification: focused package coverage is 42/58 statements (72.4137%); manifest JSON and sorted import-path ordering are valid. `make test-unit-coverage` reaches the local Windows Go process command-length limitation while starting the full profile, so the Linux CI lane remains the authoritative full-coverage execution.
- **Learnings for future iterations:**
  - The coverage manifest is a closed measured-package inventory; adding a tested Go package requires its floor in the same delivery change.
  - On this Windows workspace, the full coverage runner can exceed the OS process command-length limit when constructing the backend cover-package argument; run focused coverage locally and use the required Linux CI lane for the complete profile.
---
