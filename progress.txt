## Codebase Patterns
- Process-boundary client tests must isolate every platform configuration root the child process can consult (HOME, USERPROFILE, APPDATA, LOCALAPPDATA, XDG roots, and package cache) beneath the test temp directory.
- Document `pkg/services/events` as the process-local, in-memory, session-scoped source-native observation stream; Recordings owns canonical Factory Event JSONL history, replay, artifacts, and historical projections.
- Architecture service inventories should name the package that publishes the public owner contract, and diagrams should show direct service operations unless a bounded delivery contract is explicitly involved.
- Define ACP customer terms from the detached `chat_sessions` and `worker_sessions` public contracts; use customer-facing relationship diagrams and keep their event lifetimes separate from Recordings history.
- Event documentation has two shipped contracts only: `workers.Kind`/`workers.Phase` observations retain source provenance, while Recordings `FactoryEvent` JSONL artifacts supply canonical replay history.
- When a PSS decision is superseded, update its canonical record and local planner plan/checklist together, then point all local links to the surviving canonical decision.
- Checklist entries and changed-path inventories that name a current decision document must use its canonical path, not its former planner location.

## 2026-08-04 20:30 -07:00 - ACP-L1-V4-H04-DOCUMENTATION-RECONCILIATION-001
- Reconciled the architecture service inventory, system-flow diagrams, canonical-history explanation, and frontend/backend event prose with the implemented Chat Sessions, Events, Worker Sessions, and Recordings boundaries.
- Replaced the obsolete Docs service, `event_stream` owner, and universal Event Bus model with direct service operations plus bounded, session-scoped source-native Event delivery.
- Clarified that Recordings owns canonical `FactoryEvent` JSONL artifacts, replay, and historical inspection, while Events is process-local and retained only for the session it serves.
- Files changed: `docs/architecture/architecture.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - `pkg/services/events` already documents its D1/D2 boundary in `doc.go`; use that contract wording when reconciling customer-facing documentation.
  - `pkg/services/chat_sessions` and `pkg/services/worker_sessions` publish the ACP session contracts; their explicit Events topics are bounded delivery paths, not a system-wide event bus.
  - `make typecheck` may require a lockfile-pinned `bun install --frozen-lockfile` in a fresh worktree before the declared `@types/bun` package is available.
---

## 2026-08-04 21:05 -07:00 - ACP-L1-V4-H04-DOCUMENTATION-RECONCILIATION-002
- Added concise customer-facing definitions for Chat Session, Target Episode, Turn, and Worker Session, along with a diagram that explains how a selected target, ordered turns, and one-or-more Worker Sessions relate within an episode.
- Clarified that `FactoryResponseEvent` is an ephemeral, process-local invocation-progress record and never enters the Recordings-owned canonical `FactoryEvent` replay ledger.
- Files changed: `docs/architecture/data-model.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - A target change creates a new immutable `TargetEpisode`; every admitted `Turn` records the episode it belongs to, so documentation should describe historical target selection without implying an internal runtime model.
  - Worker Sessions are stable execution/control identities and are distinct from Provider Sessions; an episode may require multiple Worker Sessions.
---

## 2026-08-04 21:30 -07:00 - ACP-L1-V4-H04-DOCUMENTATION-RECONCILIATION-003
- Replaced the obsolete nested Factory/Worker/User/Agent/Tool event outline with the shipped Worker observation vocabulary: twelve `workers.Kind` values, six `workers.Phase` values, source provenance, and typed-payload interpretation guidance.
- Documented the separate Recordings-owned canonical `FactoryEvent` JSONL history used for replay, historical inspection, and derived projections; clarified that Events is only process-local, session-scoped delivery.
- Files changed: `docs/architecture/event-streams.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - `workers.Draft` provides the published Kind, Phase, provenance, and typed-payload contract; consumers must not assume every Kind supports every Phase.
  - The Events service relays source-native observation JSON and does not supply durable history; use Recordings `FactoryEvent` artifacts for replay.
---

## 2026-08-04 22:15 -07:00 - ACP-L1-V4-H04-DOCUMENTATION-RECONCILIATION-004
- Reconciled the PSS durability decision and local planner README, plan, and checklist with D1: the private process-local `CheckpointStore` implementation is permanent, while Recordings-backed durable checkpoint storage and durable log/cursor/retention work are cancelled rather than deferred.
- Recorded the already-applied D2 PSS-I05 event-boundary rescope and D3 PSS-I01 structural-file narrowing, preserving ordinary additive composition as a shared normal-rebase surface.
- Repaired obsolete local decision-document links and the changed cancellation-heading anchor so the PSS documentation resolves to the canonical decision record.
- Files changed: `docs/internal/packaged-service-structure/dec-run-rec-durability.md`, `docs/internal/projects/packaged-service-structure/README.md`, `docs/temp/projects/packaged-service-structure/{README,plan,checklist}.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - The canonical PSS decision lives at `docs/internal/packaged-service-structure/dec-run-rec-durability.md`; older `docs/temp` decision links are obsolete and should point there.
  - PSS-I01 and PSS-I05 narrowing are already applied in the committed manifest, so planning notes should describe their final residual boundaries rather than schedule another rescope.
  - `make readme-check` and `make typecheck`, plus a focused Markdown link review, provide the appropriate evidence for documentation-only PSS reconciliation.
---

## 2026-08-04 20:50 -07:00 - ACP-L1-V4-H04-DOCUMENTATION-RECONCILIATION-004
- Addressed the blocking PR review by correcting the local PSS checklist's checked decision-location statement and expected-path inventory to the canonical `docs/internal/packaged-service-structure/dec-run-rec-durability.md` path.
- Files changed: `docs/temp/projects/packaged-service-structure/checklist.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - A Markdown link can be correct while adjacent checklist/inventory prose still names an obsolete path; review both when reconciling moved decision records.
  - The local PSS checklist is active verification guidance, so its expected-path inventory must be maintained as rigorously as its links.
---

## 2026-08-04 21:35 -07:00 - ACP-L1-V4-M01-ACPX-REAL-CLIENT-EVIDENCE-001
- Added an isolated, bounded `acpx@0.13.0` acceptance test that builds the checked-out CLI, launches `you serve acp` through structured custom-agent argv, and verifies machine-readable session creation, protocol negotiation, and the shipped Factory Builder target before closing the session.
- Corrected `you serve acp` startup to run system initialization, ensuring the shipped packaged Factory catalog is available for a fresh ACP client home; added focused CLI regression coverage.
- Documented the safe reproduction path and enabled the opt-in real-client proof in the functional CI lane. Updated the UI compiler configuration to acknowledge the TypeScript 5.9 `baseUrl` deprecation so the existing typecheck gate remains green.
- Files changed: `.github/workflows/ci.yml`, `docs/reference/serve-acp.md`, `pkg/transports/cli/root_factory.go`, `pkg/transports/cli/root_serve_test.go`, `tests/functional/transport/acp/realclient/pinned_acpx_test.go`, `ui/tsconfig.app.json`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - `acpx@0.13.0` requires Node.js 22.13.0 or newer; the functional CI lane must install Node 22 before it runs the opt-in evidence test.
  - Fresh `you serve acp` state needs system initialization so its default `factory:@you/factory-builder` target is installed before ACP `session/new` resolves the catalog.
  - Run `bun install --frozen-lockfile` from `ui/` before `make typecheck` in a fresh worktree; the shared `tsconfig.app.json` setting is inherited by each typecheck project.
---
