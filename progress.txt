## Codebase Patterns
- Provider-output bridges must suppress provider-authored forwarding when Worker Sessions cannot resolve or bind the dispatch; forwarding an uncommitted observation can violate opening-first ordering, while ordinary downstream publication failures may follow an explicit forwarding policy.
- Package coverage ratchets should be restored with behavioral tests for the uncovered boundary and error paths; a passing package test suite alone does not prove the checked-in per-package floor.
- When a historical Worker Session projection is merged with the live Worker Sessions service, preserve the live canonical opening timestamp; dispatch-request time is only a fallback and can precede the authoritative opening.
- Customer-boundary Worker Session evidence should parse the nested lifecycle payload, assert SESSION/UPDATED provider binding before provider records, and compare replay timestamps to the public observation rather than relying on fixed delays.
- Provider-authored Worker progress must carry explicit provider identity independently from optional Provider Session references; never infer a resumable reference from that identity.
- Provider-neutral Worker history must be addressed by the canonical Worker Session ID; provider-session keyed inspection cannot cover executions that emit no native reference, and optional transcript projection must not hide retained lifecycle/output records.
- Asynchronous service characterizations should wait on observed edge/context signals rather than arbitrary `time.After` ceilings; the test context remains the failure escape hatch.
- Continuous Petri service sessions intentionally remain `IDLE` after terminal Work; characterize the dispatch and Work/progress projection as the caller-visible result and assert the live-result route's typed unavailability instead of inventing a finite session result.
- Use an authored capacity-one resource in a deterministic fixture to serialize multi-work dispatches when exact Factory Event order must be characterized; this avoids sleeps and preserves the real dispatch/resource boundary.
- Correlate batch history from both sources: request/relationship and dispatch identity live in Factory Event context, while terminal per-work state is carried by dispatch-response outputWork.
- For hosted-source characterization, route the poller through an unseeded processing input and map its external submission to a separate normal processor; this isolates source effects from the downstream dispatch executor while preserving the real runtime path.
- Runtime-opening ownership inventories should source grouped fields from runtimeopening.Dependencies, invocation-local values from ExternalEffects, and late seams from the opener; keep the reconciliation reviewer-visible in documentation instead of adding source-inventory tests.
- When synchronizing a task branch with current main, preserve the active branch's task-scoped `prd.json` and merge unrelated main history into `progress.txt`; validate the resulting PRD before staging.
- When a termination drain consumes a runtime wake signal, the engine must re-enter its tick loop directly; returning to the outer wait loses the already-consumed wake and can strand a finite run.
- Customer-facing functional tests must compose through `root.BuildProcess` and assert public process/CLI behavior; keep service-specific typed-error assertions in service-layer tests because the functional boundary check rejects direct Runtime imports.
- Repository-wide `pkg-boundary` traversals must prune through one root-relative, slash-normalized policy before parsing entries; adding a new `WalkDir` requires using that same decision.
- Treat disposable artifact/worktree paths as exact repository roots, not directory-name substrings, so tracked production packages with nested artifact-like names remain visible to the gate.
- Canonical packaged CLI docs live under `docs/reference/`; adding a topic also requires its authored file in `docs/reference/embed.go` and a registry entry in `pkg/transports/cli/docs/docs.go`.
- The `you run --named <factory> --help` output is the authoritative invocation contract; omitted role provider/model values resolve through operator defaults.
- Documentation for model-backed workflows should describe observable result shape and status/artifact evidence rather than byte-stable generated prose.
- Isolate both `HOME` and `USERPROFILE` when verifying packaged named Factories so stale materialized operator copies cannot mask the current catalog source.
- A classifier mock must emit the provider's valid response protocol and an exact configured route label; the default synthetic mock acceptance text is not a valid `small`/`medium`/`large` classification.
- Planner and cycle-control workflows likewise need protocol-valid structured model output to exercise generated Work, concurrency, or replanning; an empty generic mock can prove only the terminal invocation envelope and may leave orchestration Work uncreated.
- JavaScript packaged Factories that parse planner or judge output need protocol-valid provider-command evidence; the CLI `--with-mock-workers` path returns generic text for those child calls, so use focused provider-runner assertions plus a provider-free schema-bound CLI smoke and document the distinction.
- Workstation prompt templates receive `PromptData`; access a bound work identifier through `(index .Inputs N).WorkID` rather than a root `.WorkID` field.
- Run-level `--quiet` cannot be combined with structured `--json` or `--output`; verify output-mode combinations against the live CLI before documenting them.
- For direct named packaged Factory invocations, bind inputs in authored workstation bodies with `${parameter}` interpolation; `.Inputs` payload can be empty even when the named CLI argument is present.
- Multi-input packaged Factories must give required parameters non-ambiguous invocation bindings; the ACP reachability guard requires each required parameter to expose a text-carrying route, while authored validation rejects routing stdin to multiple parameters.
- AGY structured-output replays require the provider's echoed `json_schema` to be JSON-equivalent to the authored request; keep historical fixture envelopes synchronized with the authored schema while preserving their recorded output evidence.
- A schema-valid structured QA `reroll` is an accepted inspection verdict; provider refusal, malformed output, and schema mismatch must fail before verdict interpretation, while provider failures may consume the bounded default retry budget.
- Packaged Factory additions that change the public catalog require synchronized authored/generated sources, catalog counts, the canonical reference topic, and CLI docs smoke markers; refresh generated output only through `make packaged-factory-catalog-generate`.
- Verify documented named-Factory commands against isolated live `you run --named ... --help`; role-pinned provider/model defaults should be documented as fixed policy when no override flags are exposed.

- Worker Session finite streams can use the injected Events `Read` contract with the first retained head as a snapshot boundary; replay-only paths must not call `Subscribe`.
- Provider progress bridges name the current attempt dispatch; resolve that dispatch through Worker Sessions before publishing onto the stable Worker Session topic.

## 2026-08-10 08:50 -0700 - wsr-001-opening-record coverage follow-up
- What was implemented
  - Addressed the first PR CI rerun's Worker Sessions unit coverage ratchet failure by adding focused behavioral coverage for provider binding validation, provider attribution conflicts and duplicates, publication-state failures, append failures/duplicates, dispatch lookup, and missing constructor dependencies.
  - Kept the tests split into focused helpers so the backend maintainability limit remains satisfied.
- Files changed
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
  - `progress.txt`
  - `prd.json` remains unchanged with all three stories marked `passes:true`.
- Verification
  - `make test` — passed.
  - Package-scoped `gocoveragecheck` with the CI floor — passed at 99.1% against the 98.72% package minimum.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` — passed.
  - `go vet ./...` — passed.
  - `git diff --check` — passed.
  - The repository-wide coverage command remains host-limited on Windows because its default all-package process arguments exceed the Windows command-line limit; the equivalent scoped lane passed, and CI will validate the Linux-wide lane after push.
- **Learnings for future iterations:**
  - Coverage regressions can be isolated by reading the package manifest's uncovered functions, then testing their externally visible error/result contracts through the narrowest real service setup.
  - Keep coverage-only edge tests in focused subtests/helpers; the package maintainability gate counts test complexity as well as production complexity.
---
- Capture the Worker Session start timestamp once at the observation boundary and reuse that exact value in the lifecycle opening payload; calling the injected clock separately for projection and publication breaks live/replay identity.
- Lifecycle metadata uses optional pointers/`omitempty` and exact typed continuation values; unknown provider, Factory, recording, and capability facts must stay absent instead of being inferred from runner or model defaults.
- Authored OpenAPI and CLI contracts generate their Go, TypeScript, published metadata, and family-manifest consumers; update the source contract first, then run the repository generators and drift checks.
- Provider-neutral output contracts belong on the workstation definition and are enforced after a successful worker response; provider process success or transport parsing alone must not satisfy a semantic report contract.
- Stream transport dispatch stays below the package maintainability threshold by delegating lifecycle-specific frame handling to focused helpers.
- Deterministic Worker Session lifecycle tests can combine the controlled workstation boundary with the real in-memory Events service to verify retained replay ordering without sleeps or cancellation.
- Detached Cobra command families should project local-only flag relationships without eagerly merging parent persistent flags; otherwise the detached storage can shadow the production root flag when the family is attached.
- Process-boundary CLI tests should build `./cmd/factory` only when proving OS-level behavior, keep the real server on a root-built process, and assign redirected stdout and diagnostics stderr separately.
- Restore package coverage thresholds with focused behavioral tests for typed edge outcomes; do not lower baseline manifests for new branches.
- Public CLI manifest additions require regenerating family metadata and the published CLI contract, then updating command input/help/tree baselines.
- Reviewed functional CLI coverage requires a sorted scenario declaration, an evidence-registry entry, a manifest mapping, and one literal `functionalevidence.Covers` marker.
- Replay completeness must be derived from a terminal lifecycle record inside the captured Events range, not from a separately synchronized terminal session state.
- When a package has a strict file-count baseline, put a small refactor in an existing focused file before adding a new helper file or changing the baseline.
- When broad functional coverage reports a cancellation-observation failure, rerun the failed lane and isolate the named test before changing an unrelated feature; parallel-suite failures can move between existing durability/restart tests while isolated runs pass.
- When a functional coverage job reports a missing evidence artifact, inspect the earlier test output first; the missing artifact can be a secondary symptom of the primary test failure.
- For cross-repository PRs, resolve review and CI state with an explicit owner/repository and compare local, fork-branch, and PR head SHAs; current-branch discovery may follow a different local origin.
- New functional scenarios must use an allowed product-domain noun plus subsection path such as `tests/functional/factory/batch`; transport names like `cli` and internal implementation names like `factory_runtime` are not durable domain cells.
- Ordered functional provider outcomes can use `support.NewShapedProviderCommandRunner` with queued provider-native `CommandResult` values, including a later non-zero result, while preserving the real command-runner edge and typed provider classification.

## 2026-08-09 13:12 - wo-d1-packaged-factories-docs-001
- What was implemented
  - Established the operator-facing discovery workflow and common seven-part entry contract in `docs/reference/packaged-factories.md`.
  - Added family-based catalog navigation, materialization/default-provider guidance, live-help evidence, and model-output stability guidance.
  - Registered `packaged-factories` as a real `you docs packaged-factories` topic and updated the packaged docs indexes plus observable CLI smoke expectations.
  - Verified the current-source binary with `you factory list`, `you run --named @you/goal --help`, and `you docs packaged-factories` (using a freshly built workspace binary).
- Files changed
  - `docs/reference/packaged-factories.md`
  - `docs/reference/embed.go`
  - `docs/reference/README.md`
  - `docs/README.md`
  - `pkg/transports/cli/docs/docs.go`
  - `pkg/transports/cli/root_docs_test.go`
  - `tests/functional/smoke/cli_docs_smoke_test.go`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The existing Markdown page was present but not embedded or registered, so `you docs packaged-factories` was previously rejected; keep the source file, embed list, registry, indexes, and user-visible smoke expectations synchronized.
  - The live catalog contains fifteen `@you/*` entries plus any local Factory names, and its directory column is operator-specific; use exact Factory names for portable documentation.
  - The current help contract explicitly prints Factory-defined request binding and role-specific flags, including the phrase that omitted provider/model values use operator defaults.

## 2026-08-09 13:16 - wo-d1-packaged-factories-docs-001 verification follow-up
- What was implemented
  - Installed the repository-pinned UI dependencies that were missing from the checkout so the broad verification tier could run.
- Files changed
  - No additional tracked files.
- Verification
  - Initial `make verify-fast` stopped at the environment dependency boundary (`@types/bun` missing and the globally selected TypeScript reported `TS5101`).
  - After `bun install --frozen-lockfile` in `ui/`, `make verify-fast` passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, and the short Go suite.
- **Learnings for future iterations:**
- This checkout may have `ui/node_modules` without all lockfile dependencies; run the pinned Bun install before treating a typecheck failure as a source regression.
## 2026-08-10 04:32 -07:00 - sac-s0-2-pkg-boundary-ignores-001
- What was implemented
  - Centralized repository-root-relative pruning for `.artifacts`, `.artifacts/bootstrap/worktrees`, generated transient worktrees, and the supported root worktree locations.
  - Applied the same skip decision before parsing in every `pkg-boundary` production traversal, including secondary service, transport, provider, edge, test-behavior, Petri, and production-default scans.
  - Preserved the existing Git metadata, dependency/vendor, node-module, and testdata exclusions without matching artifact-like or worktree-like names inside tracked production packages.
  - Expanded the normal `run` regression to use malformed stale `pkg/wire` imports beneath artifact/bootstrap/generated transient layouts and existing ignored roots, proving those files are pruned before parsing.
- Files changed
  - `cmd/pkgboundarycheck/main.go`
  - `cmd/pkgboundarycheck/main_test.go`
  - `cmd/pkgboundarycheck/constructed_service_edges.go`
  - `cmd/pkgboundarycheck/functional_process_edges.go`
  - `cmd/pkgboundarycheck/initializer_behavior.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/production_defaults.go`
  - `cmd/pkgboundarycheck/support_service_imports.go`
  - `cmd/pkgboundarycheck/transport_behavior.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run '^TestRunIgnoresRepositoryPolicyNonSourceRoots$' -count=1` — passed.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `make pkg-boundary` — reaches the checker and reports only the unchanged 176-violation baseline on current `main`; no new violation was introduced by this story.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checker has many independent `WalkDir` callbacks; updating only the primary repository scan leaves secondary scanners able to rediscover ignored files.
  - A malformed ignored fixture is useful behavioral evidence for prune-before-parse: a valid stale import alone may not exercise every parser path.
  - The next story should add the paired production `pkg` failure fixture and verify the `make pkg-boundary` subprocess path while keeping nested artifact-like directory names visible.
---

## 2026-08-09 13:17 - wo-d1-packaged-factories-docs-001 documentation routing follow-up
- What was implemented
  - Corrected the root README's packaged-Factory count from fourteen to fifteen while keeping the root page as routing/quick-start material.
- Files changed
  - `README.md`
- Verification
  - `make readme-check` — passed.
- **Learnings for future iterations:**
  - Keep the root count aligned with the canonical fifteen-entry catalog, but continue placing detailed Factory guidance in `docs/reference/packaged-factories.md`.
---

## 2026-08-09 13:20 - wo-d1-packaged-factories-docs-001 lint follow-up
- What was implemented
  - No source change; checked the broad repository lint tier against the completed story.
- Files changed
  - No additional tracked files.
- Verification
  - `make lint` completed the UI lint/dead-code checks, `go vet`, backend-size, pkg-maint, pkg-file-count, and related checks, then stopped at the existing `pkg-boundary` baseline with 176 violations. The reported violations are in pre-existing service-boundary/Petri migration surfaces and none are in this story's changed files.
- **Learnings for future iterations:**
  - Treat the current `pkg-boundary` findings as baseline debt unless a changed file introduces a new violation; this documentation story does not touch those ownership surfaces.
---

## 2026-08-09 13:36 - wo-d1-packaged-factories-docs-002
- What was implemented
  - Added full bounded/iterative operator entries for `@you/goal`, `@you/loop`, `@you/fusion`, `@you/classify`, and `@you/review` to `docs/reference/packaged-factories.md`.
  - Documented each current live invocation signature, all worker roles, role-specific provider/model overrides, operator-default fallback, prerequisites, workspace/session/recording side effects, stopping/routing/retry behavior, and final versus intermediate output shape.
  - Added `docs/examples/packaged-classify-mock-workers.json` and indexed it from `docs/examples/README.md` so the classify example can deterministically prove a valid `small` route without a live provider call.
  - Executed every worked command exactly as shown against a freshly built current-source `bin/you.exe` with isolated `HOME`/`USERPROFILE` state. `goal`, `loop`, `fusion`, and `review` returned `mock worker accepted`; `classify` routed through `small` and returned `mock worker accepted`.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `docs/examples/README.md`
  - `docs/examples/packaged-classify-mock-workers.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The live packaged catalog is lazily materialized and can be stale in an operator profile; use isolated home variables when collecting current help evidence.
  - `--with-mock-workers` is enough for most packaged examples, but classifier workflows need a protocol-valid scripted provider response for the exact route label before the selected lane can run.
  - Keep deterministic documentation fixtures under `docs/examples/` and explain their role so smoke output is not mistaken for a live model guarantee.
---

## 2026-08-09 14:04 - wo-d1-packaged-factories-docs-003
- What was implemented
  - Added distinct operator-ready entries for `@you/factory-builder`, `@you/plan-execute`, `@you/plan-parallel`, and `@you/full-flow` in `docs/reference/packaged-factories.md`.
  - Documented each live help signature, all worker roles and provider/model overrides, relative workflow choice, workspace and Git side effects, concurrency and retry bounds, validation/installation, local merge/replanning, and final versus intermediate output behavior.
  - Added exact current-binary smoke evidence: the builder returned `mock worker accepted`; plan-execute returned `mock worker accepted` and `<COMPLETE>`; plan-parallel returned `mock worker accepted`; full-flow's controlled invalid-bound check returned the documented `maxCycles` choice-validation error.
  - Marked Story 003 as passing in `prd.json` and recorded the generic-mock limitation so protocol-valid planner responses are not confused with end-to-end orchestration evidence.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `go test ./tests/functional/factory/packaged/factory_builder ./tests/functional/factory/packaged/plan_execute ./tests/functional/factory/packaged/plan_parallel ./tests/functional/factory/packaged/full_flow -count=1 -timeout 300s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The planning entries are not interchangeable: builder installs a new Factory, plan-execute stays sequential in the current workspace, plan-parallel shares one repository across a bounded DAG, and full-flow owns worktrees, local merges, and replanning.
  - Generic mock workers accept a planner dispatch without producing its required `FACTORY_REQUEST_BATCH`; use protocol-valid fixtures or provider-backed functional tests when verifying generated Work, concurrency, or cycle control.
  - Keep full-flow CLI smoke examples bounded and terminating; use controlled argument validation for the public command example and the isolated functional test for actual worktree/merge behavior.
---

## 2026-08-09 15:51 - wo-d1-packaged-factories-docs-004
- What was implemented
  - Added distinct operator-ready entries for `@you/deep-research`, `@you/spawn`, `@you/quorum`, and `@you/tournament` in `docs/reference/packaged-factories.md` using the shared seven-part entry contract.
  - Documented isolated current-binary invocation signatures, all worker roles and provider/model override flags, operator-default fallback, JavaScript policy bounds, quorum Graph Work and merge behavior, task-count/round bounds, side effects, and primary versus intermediate result shapes.
  - Added exact worked command evidence: deep-research completed in structured JSON with two specialist statuses, quorum returned `mock worker accepted`, spawn rejected an out-of-range count of 15, and tournament rejected an out-of-range round count of 4 before provider work.
  - Connected successful provider-backed result claims to the existing functional assertions for exact-count spawn merging and tournament champion/decision-trail output; did not treat generic JavaScript mocks as valid planner/judge protocol.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `go test ./tests/functional/factory/packaged/deep_research ./tests/functional/factory/packaged/quorum ./tests/functional/factory/packaged/javascript_families -count=1 -timeout 300s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Always collect packaged-Factory help and invocation evidence with isolated `HOME` and `USERPROFILE`; stale materialized copies can advertise obsolete flag names or defaults.
  - The current JavaScript mock runner proves generic terminal envelopes but cannot author `@you/spawn`'s exact planner JSON array or `@you/tournament`'s judge JSON; provider-command functional tests are the observable success proof for those protocol-sensitive workflows.
  - `@you/deep-research`'s structured primary result r…21719 tokens truncated…`de6db78a47666ecc94861ae645b18d6635685a29`.
- **Learnings for future iterations:**
  - For functional coverage failures, compare the reported package-floor deltas with the PR file list before changing code; a small drop in untouched packages is evidence for a targeted rerun, not a feature regression.
  - The dependent Verification Policy job may appear only after the rerun lane completes, so final readiness requires checking both the lane and its policy gate.
---
## 2026-08-10 02:09 -0700 - wo-b3-provider-capability-discovery-cli-review-audit
- What was implemented
  - Re-read the PRD and consolidated codebase patterns; all three provider discovery stories remain marked `passes:true`.
  - Audited PR #1843 after the existing blocking conversation item. The current head contains the requested catalog-to-execution-policy reconciliation: exact Codex model IDs, explicit AGY empty effort arrays with model-ID selection guidance, and an observable consistency regression; no inline review threads or formal reviews remain.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and acceptance notes are current.
- Verification
  - Local `HEAD` matches PR #1843 head `843750d423b4cc4ae1ff7f5346a15f1b313d7b8b`; the PR is open and mergeable.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - The fresh PR CI run is still in progress; terminal readiness will be checked after it completes.
- **Learnings for future iterations:**
  - A historical top-level blocker can remain in the merged PR timeline after the requested fix is present; verify the current head against the feedback and inspect thread resolution separately before treating it as unresolved work.
  - When the PR head is hosted in a different repository than the local default remote, resolve and inspect it with the explicit owner/repository and compare its SHA to local `HEAD`.
---
## 2026-08-10 02:43 -0700 - wo-b3-provider-capability-discovery-cli-final-delivery-audit
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three stories remain marked `passes:true`.
  - Verified that the existing PR #1843 already contains the latest provider-policy reconciliation requested by its blocking conversation item: exact Codex model IDs, explicit AGY empty effort arrays with model-ID selection guidance, and the catalog-to-execution behavioral regression.
  - Confirmed no inline review threads or formal reviews remain, and the top-level blocker is addressed by the current commits.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states, acceptance notes, and the PR description are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed; worktree clean before this append.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `964eef47692ac9b7d7524dc5c540a558e9864a7b`.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
  - PR #1843 is open, mergeable, non-draft, and its description matches `prd.json`.
- **Learnings for future iterations:**
  - Final delivery audits should compare the exact local, remote-branch, and PR head SHAs, then verify the terminal CI run and PR description before reporting readiness.
  - A historical top-level blocker is addressed when the current head contains its behavioral fix and thread-aware inspection shows no unresolved inline work; its original conversation item may remain as historical context.
---
## 2026-08-10 02:59 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry-final
- What was implemented
  - Diagnosed the first fresh-head CI failure as the unchanged timing-sensitive `TestFactorySessionResumeDoesNotRepeatCompletedDispatch` test; its failure was `provider Infer did not observe canceled workflow context`, unrelated to provider discovery.
  - Reproduced the exact test successfully for 20 isolated repetitions and reran only the failed GitHub jobs with `gh run rerun 31376137369 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
  - No feature code, generated artifact, or coverage baseline was changed for the unrelated retry.
- Verification
  - Isolated restart test: 20/20 repetitions passed.
  - Retry Backend Functional Coverage job `93417380311` — passed.
  - Retry Verification Policy job `93418598263` — passed.
  - All required PR checks are green on head `b0c4850466dbf36faab0de18fa4e2250480a23c0`.
- **Learnings for future iterations:**
  - When a broad functional lane fails on an untouched timing-sensitive test, reproduce that exact test repeatedly before editing feature code; a clean repetition supports a targeted failed-job rerun.
  - Record the retry job IDs and exact final head in the append-only log so CI recovery remains auditable.
---
## 2026-08-10 03:11 -0700 - wo-b3-provider-capability-discovery-cli-delivery-audit
- What was implemented
  - Re-read the PRD and consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`.
  - Audited existing PR #1843 after the prior blocking conversation item. The current head includes the exact Codex model inventory, explicit AGY empty effort arrays with model-ID selection guidance, and the catalog-to-execution behavioral regression requested by the feedback.
  - Confirmed the existing PR is open, mergeable, non-draft, and already points at the latest local and fork branch commit, so no duplicate PR or feature-code change is needed.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and acceptance notes are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `998a9fecdf46c9bcbaaf92bef72ddee0f3d0bf49` before this progress-only append.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
  - Thread-aware PR inspection reports zero inline review threads and zero formal reviews; the prior top-level blocker is addressed by the current commits.
- **Learnings for future iterations:**
  - A review audit should compare the exact local, fork, and PR head SHAs and inspect thread state before deciding whether a duplicate PR or code change is necessary.
  - Progress-only delivery bookkeeping changes the PR head, so the final audit must recheck the corresponding CI run after pushing the append.
---
## 2026-08-10 03:30 -0700 - wo-b3-provider-capability-discovery-cli-final-delivery-check
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`, so no remaining story implementation was required.
  - Inspected existing PR #1843 with the thread-aware GitHub review workflow. It has zero inline review threads and zero formal reviews; the historical top-level blocker is addressed by the current catalog, execution-policy regression, generated artifacts, and documentation.
  - Confirmed the existing PR is the requested named PR, so no duplicate PR was created.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states, acceptance notes, and the PR description are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `4e36b453e8768b0364e37f7354e7ceea73dabbcf`.
  - PR #1843 description matches `prd.json`; the PR is open, mergeable, and non-draft.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - When a completed cross-repository PR already contains the requested review fix, verify thread state, exact local/fork/PR SHAs, description parity, and terminal checks before creating any new PR or feature change.
---
## 2026-08-10 04:09 -0700 - wo-b3-provider-capability-discovery-cli-review-follow-up
- What was implemented
  - Addressed the latest blocking PR feedback for the canonical capability-facts story.
  - Added Claude's exact authored model inventory: `claude-opus-4-6-thinking`, `claude-sonnet-4-20250514`, and `claude-sonnet-5`, with explicit text-only modalities and low-through-max effort values.
  - Changed unprobed static catalog projections from fabricated `ready`/`satisfied` values to truthful `unverified`/`required` values, while retaining `ready`/`unavailable`/`missing` for request-time probe facts and allowing catalog-selectable unprobed providers through normal selection.
  - Extended human, JSON, authored-catalog, projection, and production `root.BuildProcess`/`Process.Execute` assertions to cover the new status semantics and Claude inventory; updated the provider reference documentation and PRD notes.
- Files changed
  - `packages/model-providers/providers/claude/provider.yaml`
  - `packages/model-providers/generated/catalog.json`
  - `packages/model-providers/metadata/manifest.json`
  - `packages/model-providers/catalog_test.go`
  - `pkg/services/providers/catalog_contract.go`
  - `pkg/services/providers/internal/service/selection.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/{projection.go,projection_test.go,service.go,service_test.go}`
  - `pkg/services/providers/internal/services/catalog/service.go`
  - `pkg/services/providers/{selection_contract.go,service_contract.go}`
  - `pkg/services/providers/transports/mcp/schemas.go`
  - `tests/functional/providers/discovery/discovery_test.go`
  - `docs/reference/providers.md`
  - `prd.json`
- Verification
  - `make provider-catalog-generate` and `make model-provider-package-generate` — passed.
  - `make provider-catalog-check` and `make model-provider-package-check` — passed.
  - Focused Providers/catalog/CLI/MCP Go tests — passed.
  - `go test ./tests/functional/providers/discovery -count=1 -timeout 120s` — passed.
  - `make docs-reference-smoke`, `make typecheck`, `make test`, and `make vet backend-size pkg-maint pkg-file-count pkg-structure` — passed.
- **Learnings for future iterations:**
  - Static authored provider manifests describe maximum/publication facts only; production catalog projections must never imply current installation, authentication, or executable readiness without a probe.
  - Keep static and live readiness vocabularies distinguishable at the public Providers boundary: `unverified`/`required` describe no probe, while `ready`/`unavailable` and `satisfied`/`missing` describe probe outcomes.
  - Exact model IDs should be asserted at the manifest, projection, JSON, human, and root-process boundaries when docs or execution harnesses name a model.
---
## 2026-08-10 04:24 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Audited the fresh PR workflow for commit `330e7d8e1`; the first attempt failed only in the untouched JavaScript durability test `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` because the provider inference did not observe the canceled workflow context.
  - Confirmed the failure was outside the provider-discovery file set and reran only the failed GitHub jobs with `gh run rerun 31382404004 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
- Verification
  - Retry Backend Functional Coverage job `93436868087` — passed.
  - Retry Verification Policy job `93438049879` — passed.
  - All 18 required PR checks are green on commit `330e7d8e1`.
- **Learnings for future iterations:**
  - When a broad functional lane fails in an untouched timing-sensitive test, extract the exact failing test and message from the job log before changing feature code; a clean targeted retry is the appropriate recovery when the failure is nondeterministic and unrelated.
  - A failed dependent policy job should be evaluated only after the failed lane is rerun and terminal; the policy check can pass on the same workflow attempt once the selected lane succeeds.
---
## 2026-08-10 04:49 -0700 - wo-b3-provider-capability-discovery-cli-final-review-audit
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`.
  - Inspected PR #1843 with the connector and the bundled thread-aware GraphQL workflow. The historical AGY/Codex and Claude/readiness blockers each have a subsequent author response documenting the corresponding fixes; no inline review threads or formal reviews remain.
  - Confirmed the existing requested PR is already current, so no duplicate PR or feature-code change is needed.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and the PR description are current.
- Verification
  - Thread-aware review inspection — four top-level conversation comments, zero reviews, zero inline review threads; the latest blocker is addressed by commit `330e7d8e1`.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `3319b2109373de62d791178568cfa3b297c507d5`.
  - PR #1843 is open, non-draft, mergeable, clean, and its description exactly matches `prd.json`.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 reported checks pass.
- **Learnings for future iterations:**
  - A historical blocking conversation item is resolved when the current PR contains the requested behavioral fix and the thread-aware audit finds no unresolved inline work; preserve the original comment as review history.
  - Progress-only commits still change the PR head, so rerun the focused regression and verify terminal CI after pushing the bookkeeping update.
---
## 2026-08-10 05:05 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Diagnosed the first workflow attempt on the progress-only head as two unrelated flaky lanes: the UI component suite hit an out-of-memory failure in `FactorySimpleSubmissionComposer`, and backend functional coverage hit the previously observed canceled-context assertion in `TestFactorySessionResumeDoesNotRepeatCompletedDispatch`; no provider-discovery or UI files were changed.
  - Reran only the failed jobs after the workflow became terminal.
- Files changed
  - `progress.txt`
- Verification
  - Initial run `31385342784` failed only `Frontend Component` and `Backend Functional Coverage`; all other substantive checks passed.
  - Retry `Frontend Component` job `93446283510` — passed.
  - Retry `Backend Functional Coverage` job `93446283682` — passed.
  - Dependent `Verification Policy` job `93447534784` — passed.
  - All 18 checks are green on head `23cb30230ef31fda2ebf9fdec1f6c211e6913ca2`.
- **Learnings for future iterations:**
  - A progress-only commit can expose unrelated resource and timing flakiness; inspect the exact failing test and compare changed paths before editing feature code, then rerun only failed jobs when the isolated evidence supports it.
  - Record both the primary failure and any missing downstream artifact so CI diagnosis does not mistake the upload symptom for the test cause.

## 2026-08-10 04:44 -07:00 - pf-plain-batch-drain-001
- What was implemented
  - Made Factory Runtime termination-drain coordination re-evaluate immediately when a result, submission, or dispatch-result hook wake is consumed at the termination boundary.
  - Added a focused engine regression proving a dispatch hook wake causes a second canonical termination check instead of returning to a blocked outer wait.
  - Added root-built public plain-batch coverage for stranded PROCESSING Work, exact typed incomplete-drain propagation and stderr, empty and terminal finite success, and continuous idle liveness.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/runtime_state.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine_termination_test.go`
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
  - `prd.json`
- Verification
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/... ./pkg/services/factory_sessions/internal/processlifecycle ./pkg/services/factory_sessions/internal/runtimehosting ./pkg/transports/cli/run ./tests/functional/workers/mock -run 'TestRun(ReevaluatesTerminationAfterDispatchHookWake|ReturnsIncompleteDrainErrorAfterTerminationClassification)$|TestPlainBatchDrain' -count=1 -timeout 180s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The Runtime termination checker already owns the finite incomplete classification and typed error; the lifecycle bug was that `finishTerminationDrain` reported a consumed wake without its caller looping back into `tickOnce`.
  - Dispatch-result hook wake channels are part of the engine’s pending-input contract and must be drained alongside result and submission signals at the final termination boundary.
---

## 2026-08-10 04:57 -07:00 - pf-plain-batch-drain-001 verification follow-up
- What was implemented
  - Kept the termination-drain result helper within the repository’s 1000-line backend file limit without changing its semantics.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/runtime_state.go`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_runtime/... -count=1 -timeout 300s` — passed.
  - `go test ./pkg/services/factory_sessions/... -count=1 -timeout 360s` — passed.
  - `go test ./tests/functional/sessions/execution -run 'TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal|TestHostedFiniteRunsKeepEmptyAndTerminalSuccess|TestHostedContinuousRunsStayLiveWhileIdle' -count=1 -timeout 240s` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — blocked before Go/UI tests by the checkout’s pre-existing dashboard baseline: missing `@types/bun` and TypeScript 6 `baseUrl` deprecation errors.
- **Learnings for future iterations:**
  - The backend quality gates report deletion-only baseline entries but pass newly changed files; a small private helper signature reduction is enough to preserve the strict engine file-size boundary.
  - `make test` remains the reliable repository-wide short Go evidence when the fast aggregate stops early at an unavailable UI dependency.
---

## 2026-08-10 05:08 -07:00 - pf-plain-batch-drain-001 CI boundary follow-up
- What was implemented
  - Adjusted the plain customer-boundary assertion to avoid a forbidden direct Runtime service import while retaining exact stderr and process failure coverage.
- Files changed
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run 'TestPlainBatchDrain' -count=1 -timeout 120s` — passed.
  - `go run ./cmd/functionalboundarycheck` — passed.
  - `git diff --check` — passed.
  - PR #1845's initial functional lane identified the same forbidden Runtime import; commit `085c00db6` removes it and is ready for a fresh canonical-repository CI run.
- **Learnings for future iterations:**
  - The functional boundary lane is intentionally stricter than package tests: public harness tests verify observable process behavior, while `errors.As` checks for service-owned types belong in the owning service package.
---

## 2026-08-10 05:09 -07:00 - pf-plain-batch-drain-001 PR handoff follow-up
- What was implemented
  - Re-submitted the current branch as review PR #1846 after the migrated PR #1845 retained checks for its obsolete pre-boundary-fix commit.
- Files changed
  - `progress.txt`
- Verification
  - PR #1846 is open, non-draft, points at commit `f3f6db0b5`, has no comments or reviews, and its body matches `prd.json` after newline normalization.
  - Local focused tests, the functional boundary checker, and the previously recorded repository quality gates remain passing; the checkout-local `make verify-fast` UI baseline remains blocked by missing `@types/bun` and TypeScript 6 `baseUrl` deprecation errors.
  - GitHub currently reports no fresh Actions run for the current head after the repository-migration PR recreation; this remains PR follow-up rather than a claimed green CI result.
- **Learnings for future iterations:**
  - When a repository migration leaves a PR’s checks attached to an obsolete head, recreating the review PR can restore a clean handoff, but verify that the new pull-request event actually creates checks before declaring review readiness.
## 2026-08-10 04:38 -07:00 - sac-s0-2-pkg-boundary-ignores-002
- What was implemented
  - Added a paired normal checker regression using the same prohibited `pkg/wire` import as the ignored-root fixture under production `pkg` paths containing nested `.artifacts`, `.claude/worktrees`, and `worktrees` directories.
  - Added a `make pkg-boundary` entry-path regression proving the nested `.artifacts` production package is still reported with the application-composition diagnostic and non-zero result.
  - Marked story 002 complete in `prd.json`.
- Files changed
  - `cmd/pkgboundarycheck/main_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run 'TestRunRejectsProductionPkgViolationInsideArtifactLikeSubtrees|TestMakePkgBoundaryTargetRejectsProductionPkgImportInsideArtifactLikeSubtree' -count=1 -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Root-relative ignore policies need paired positive fixtures under production `pkg`; testing ignored paths alone cannot prove that nested artifact/worktree-like directories remain visible.
  - Existing `make pkg-boundary` subprocess tests provide a stable way to verify the public checker entry path against temporary repository fixtures.
---
## 2026-08-10 05:15 -0700 - wo-b3-provider-capability-discovery-cli-final-merge-audit
- What was implemented
  - Reconciled the provider-discovery branch with current `main` at `353b0527a`; the merge conflict was limited to `prd.json` and `progress.txt`.
  - Preserved the B3 PRD and all three `passes:true` stories, combined both append-only progress histories, and retained the current-main `pkg-boundary` production-source changes.
- Files changed
  - Current-main `cmd/pkgboundarycheck/` implementation and focused tests were merged.
  - `progress.txt`
  - `prd.json` was verified as the B3 version after conflict resolution.
- Verification
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - PR #1843 is conflict-free and points to merge head `f80b38a8d29eace327d53895bc84c4ad4e3f6487` with base `353b0527a26163711043671f966466be30f478c4`.
  - Fresh CI run `31386632092` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - When a moving base changes an unrelated task PRD, resolve the bookkeeping conflict by keeping the branch’s task PRD and merging append-only progress entries; retain the base’s source changes and rerun both affected-package and feature-slice tests.
---
## 2026-08-10 05:25 -07:00 - pf-plain-batch-drain-001 synchronization follow-up
- What was implemented
  - Synchronized the branch with current `main` after PR #1844 advanced the base, retaining the plain-batch implementation and resolving the shared PRD/progress bookkeeping files without dropping either history.
  - Committed the conflict-free merge as `8bd1fb6eb`.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main `cmd/pkgboundarycheck` changes were merged as part of the base synchronization.
- Verification
  - Focused plain-batch/runtime/session tests passed.
  - Full `pkg/services/factory_runtime/...` and `pkg/services/factory_sessions/...` tests passed.
  - `go test ./cmd/pkgboundarycheck -count=1` and `go run ./cmd/functionalboundarycheck` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make test` passed.
  - After `bun install --frozen-lockfile` restored the checkout's missing UI dependencies, `make verify-fast` passed dashboard typecheck, MCP contract checks, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
- **Learnings for future iterations:**
  - The missing `@types/bun` dependency was an environment-only checkout issue; installing from the pinned lockfile restored the full fast verification tier without tracked dependency changes.
  - A current-main merge can conflict in shared task bookkeeping even when product files merge cleanly; keep the active branch PRD authoritative and preserve both chronological progress histories.
---
## 2026-08-10 05:29 -07:00 - pf-plain-batch-drain-001 PR description follow-up
- What was implemented
  - Restored the omitted word `plain` in the active PRD acceptance criterion so `prd.json` exactly matches the existing PR #1846 description.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses successfully.
  - PR #1846 body matches `prd.json` after newline normalization.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - After resolving a shared JSON conflict manually, compare the normalized PR body against the file content rather than relying only on JSON validity; small wording drift can otherwise go unnoticed.
---
## 2026-08-10 05:54 -07:00 - pf-plain-batch-drain-001 review follow-up
- What was implemented
  - Addressed PR #1846's blocking test-construction feedback by naming the continuous idle observation and documenting why the bounded negative-liveness guard is required at the public `Process.Execute` boundary.
- Files changed
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run 'TestPlainBatchDrain' -count=3 -timeout 180s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A functional test that proves a continuous run stays open must explain why no deterministic idle event or injected edge observation exists; name the bounded observation and keep it scoped to the negative-liveness guard.
## 2026-08-10 05:47 -0700 - wo-b3-provider-capability-discovery-cli-003
- What was implemented
  - Addressed the current blocking PR feedback by splitting the 88-line `assertJSONFacts` functional helper into focused JSON decoding, common inventory, Codex, Antigravity, and Claude behavioral assertions.
  - Preserved the existing runtime assertions for deterministic provider ordering, truthful unprobed readiness/prerequisite state, exact model IDs, unsupported modalities, typed limits, and zero provider command-runner calls.
- Files changed
  - `tests/functional/providers/discovery/discovery_test.go`
  - `progress.txt`
  - `prd.json` was verified unchanged because all stories remain `passes:true`; the review-only maintainability follow-up does not alter acceptance state.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1 -timeout 120s` — passed.
  - `go test ./tests/functional/providers/discovery/... -count=1 -timeout 120s` — passed.
  - `go vet ./tests/functional/providers/discovery/...` — passed.
  - `git diff --check` — passed; every helper is below the 80-line review limit.
- **Learnings for future iterations:**
  - Functional assertions that cover several provider-specific facts should be split by behavior area so maintainability limits do not force weaker runtime coverage.
  - A top-level review blocker can be resolved with a focused test-only change when the requested behavior is already correct; preserve the existing observable assertions while reducing helper scope.
---
## 2026-08-10 07:10 -0700 - wsr-001-opening-record
- What was implemented
  - Resolved the blocking provider-attribution review feedback: canonical provider identity now rejects contradictory provider fragments and suppresses mismatched provider output; provider binding and dispatch-to-Worker lookup are operations on the canonical Worker Sessions service contract.
  - Refactored Worker Sessions publication and lifecycle tests to stay within backend size/maintainability limits, removed the obsolete service-root interface baseline entry, and preserved unknown optional opening metadata as omitted.
  - Corrected the runtime recorded-observation merge to retain the live Worker opening `startedAt`, so the public observation and Worker Session replay expose the same authoritative timestamp.
  - Added named customer-boundary functional scenarios WSR-FT-001, WSR-FT-002, and WSR-FT-003 using `root.BuildProcess`, `Process.Execute`, production registrations, sanitized Codex/Antigravity fixtures, and deterministic public terminal/replay synchronization. Existing Claude streaming and Cursor/ACP suites remain the provider conformance coverage for those variants.
  - Regenerated published API/package/UI interface artifacts with `make interfaces-all`.
- Files changed
  - `pkg/services/worker_sessions/`
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/`
  - `tests/functional/provider_sessions/cli/worker_sessions_cli_test.go`
  - `tests/functional/workers/inference/opening_record_test.go`
  - `packages/api/generated/`
  - `ui/packages/client/src/generated/`
  - `docs/internal/baselines/package-structure-baseline.json`
  - `progress.txt`
  - `prd.json`
- Verification
  - `make interfaces-all` — passed.
  - `make backend-size pkg-maint pkg-structure pkg-file-count` — passed.
  - `make test` — passed.
  - Focused WSR-FT-001/002/003 and timestamp-merge tests — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Worker Session replay frames carry the lifecycle draft envelope plus a nested `payload` map; terminal status and opening timestamp must be read from that nested payload.
  - Provider resolution can legitimately follow the opening, so functional ordering should require a synthesized SESSION/UPDATED binding before the first provider-authored frame.
  - The generated package manifest and OpenAPI/UI artifacts can drift even when the authored REST contract is unchanged; run `make interfaces-all` after merging current main before diagnosing interface failures.
---
## 2026-08-10 06:24 -07:00 - pf-plain-batch-drain-001 current-main review follow-up
- What was implemented
  - Merged current `origin/main` into `pf-plain-batch-drain` and resolved the shared bookkeeping conflict by keeping the plain-batch `prd.json` authoritative while preserving both append-only progress histories.
  - Retained the plain-batch Runtime wake re-evaluation, typed incomplete-drain propagation, and public functional regression alongside current-main provider-discovery source and generated artifacts.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main provider-discovery source/generated files were retained by the merge.
- Verification
  - Focused Runtime and Factory Session packages passed.
  - `go test ./tests/functional/workers/mock -run '^TestPlainBatchDrain' -count=3 -timeout 240s` — passed.
  - `go run ./cmd/functionalboundarycheck` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make lint` passed UI lint/dead-code, vet, and backend gates, then reached the existing 176-violation `pkg-boundary` baseline; the baseline matches current main and no new task violation was introduced.
  - `git diff --check` — passed; PRD parses with the single story still marked `passes:true`.
- **Learnings for future iterations:**
  - When current main changes the task bookkeeping files, preserve the active task PRD exactly for PR-description parity and merge each side's progress entries rather than replacing the append-only log.
 - A clean local merge head can still report the repository's known `pkg-boundary` migration baseline; distinguish that baseline from changed-file regressions using the dedicated checker and functional-boundary gate.
---
## 2026-08-10 04:42 -07:00 - btrc-p0-characterization-001
- What was implemented
  - Published docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md as the authoritative P0 ownership map.
  - Reconciled the ten grouped dependency structs to 44 D rows, ExternalEffects to 15 E rows, deferred construction/callback roles to 38 F rows, and late completion/attachment/binding operations to 13 L rows.
  - Classified every row exactly once, assigned one destination owner or explicit deletion, documented lifecycle/cleanup responsibility, and linked the downstream retirement packet.
  - Marked story btrc-p0-characterization-001 as passed in prd.json; no production code or inventory-checker test was added.
- Files changed
  - docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md
  - prd.json
  - progress.txt
- Verification
  - git diff --check — passed.
  - go test ./pkg/services/factory_sessions/internal/runtimeopening ./pkg/services/factory_sessions/wire ./pkg/services/factory_sessions/internal/applicationopening -count=1 — passed.
  - Row totals and source sections were reviewed directly against factory.go, factories.go, open.go, and the adjacent Wire/application binding surfaces.
- **Learnings for future iterations:**
  - The canonical grouped dependency count is ten Dependencies structs totaling 44 fields; the flattened private opener fields are projections and should not be counted again.
  - ExternalEffects is the narrow invocation-local projection selected in pkg/wire/runtime_inputs.go; future characterization rows should distinguish these operation values from process-scoped construction inputs.
  - Workers.LocalRuntimeHooksFactory is composed and validated but not invoked by the current opener, so it is an explicit deletion row rather than a destination-owned capability.
---
## 2026-08-10 05:13 -07:00 - btrc-p0-characterization-002
- What was implemented
  - Added deterministic root-composition characterization for successful and partially failed two-work parent/child batches.
  - Froze the literal 20-event canonical sequence from RUN_REQUEST through WORK_REQUEST, relationship admission, serialized child dispatches, model/dispatch responses, RUN_RESPONSE, and the two session terminal events.
  - Asserted request/work counts, parent-child relation identity, default session identity, one-to-one dispatch/Worker Session correlation, terminal work states, typed `permanent_bad_request` failure metadata, and the current `SUCCEEDED`/`FINAL` aggregate result for both all-success and partial-failure batches.
  - Marked story btrc-p0-characterization-002 as passed in prd.json; no production behavior changed.
- Files changed
  - tests/functional/factory_runtime/root_composition/batch_characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/factory_runtime/root_composition -run '^TestBTRCP0Batch(Success|PartialFailure)Characterization$' -count=5 -timeout 240s` — passed.
  - `git diff --check` — passed.
  - The full `./tests/functional/factory_runtime/root_composition` package remains blocked by the unchanged neighboring W4 test `TestFactoryRuntimeMixedDirectAndFactoryChildDispatchesStayIsolatedThroughCLI`, which reports 18 `unexpected provider prompt` calls even when run alone; the failure is outside this story's changed files.
- **Learnings for future iterations:**
  - A capacity-one authored resource is the current deterministic boundary for serializing a multi-work batch without sleeps; it produces stable dispatch request/association/model/response ordering while retaining multiple child works.
  - A partially failed work can still yield a `SUCCEEDED`/`FINAL` Factory Session when every work token reaches a terminal state; the typed per-dispatch failure remains in `DISPATCH_RESPONSE`.
  - Canonical batch work identity is split across the WORK_REQUEST/relationship event context and the dispatch response `outputWork`; assert both instead of relying on a projection derived from the test itself.
---
## 2026-08-10 05:33 -07:00 - btrc-p0-characterization-003
- What was implemented
  - Added deterministic hosted/service characterization in tests/functional/automations/hosted_characterization_test.go without changing production code or public/generated contracts.
  - Root-composed service tests replace the hosted clock, Linear HTTP client, secret resolver, and provider command runner through edges.Edges; successful hosted ingress asserts the literal ten-event startup-to-dispatch sequence, ACCEPTED dispatch payload, terminal `complete` Work, and IDLE service progress.
  - Execution failure asserts the same literal event sequence, typed dispatch failure detail/error, terminal `failed` Work, one provider attempt, and no non-terminal progress.
  - Blocked hosted HTTP asserts the startup-only four-event prefix, no submission or dispatch, observed request-context cancellation during service stop, and an isolated independent second session that completes only its own Work.
  - Marked story btrc-p0-characterization-003 as passed in prd.json.
- Files changed
  - tests/functional/automations/hosted_characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/automations -run 'TestBTRCP0HostedService' -count=1 -v -timeout 120s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Hosted service mode remains live with runtime status IDLE after a terminal Work; its caller-visible terminal result is the dispatch response plus session-scoped Work/progress projection rather than a durable session-result endpoint.
  - A poller workstation without a worker executor fails after admission; use a separate normal workstation for the hosted target state when characterizing successful hosted ingress.
  - A blocking HTTP edge makes source cleanup observable through request-context cancellation, while a separate process with a distinct hosted Work ID provides a direct isolation assertion.
---
## 2026-08-10 05:54 -07:00 - btrc-p0-characterization-004
- What was implemented
  - Added `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go` using the checked-in `inference-events.replay.json` recording as the expected source of replay order.
  - The success characterization asserts the literal seven-event Factory Event order and IDs, sequence `0..6`, tick placement, request/work correlation, dispatch replay key, recorded provider output, submission timing, completion delivery tick, and `ACCEPTED` terminal result.
  - The failed-recording characterization mutates only the recorded terminal payload, then asserts the same event prefix, `FAILED`/`permanent_bad_request` result, typed `workers.ProviderError`, and typed `dispatch_mismatch` divergence for an incompatible observed dispatch.
  - Marked story `btrc-p0-characterization-004` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/recordings/internal/replay -run '^TestBTRCP0ReplayCharacterization_' -count=1 -v` — passed.
  - `go test ./pkg/root -run 'TestProcessExecute(RejectsInvalidPortableReplay|ReturnsPortableReplayInspection)' -count=1 -v` — passed; malformed replay and valid portable inspection each made zero provider/script/session/host calls.
  - `go test ./pkg/services/recordings/internal/replay ./pkg/services/recordings/internal/services/replay/internal/service -run 'Test(BTRCP0ReplayCharacterization_|TestLoadReplay|TestCreateReplayPlan|TestObserveReplay)' -count=1` — passed for the selected replay packages.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checked-in legacy inference fixture is a useful stable event-order source for reducer/delivery characterization even though its historical metadata is not a valid full current-root runtime input; full root replay should use a recording produced by the current runtime harness.
  - Portable historical replay rejects malformed order before live runtime construction, so its observable failure contract is a typed `ReplayInputError` with zero live effect calls; legacy runtime replay reports incompatible dispatches as typed `DivergenceError`.
---
## 2026-08-10 06:08 -07:00 - btrc-p0-characterization-005
- What was implemented
  - Added deterministic one-shot success and provider-failure characterization at the root `BuildProcess`/`Process.Execute` boundary in `tests/functional/cli/one_shot/characterization_test.go`.
  - Froze the literal 14-event `RUN_REQUEST` through `SESSION_COMPLETED` order from a recorded response, including request, trace, work, dispatch, Worker Session, and `~default` session identities.
  - Asserted the complete successful primary-result projection, terminal `SUCCEEDED`/`FINAL` session result, failed dispatch `unknown`/terminal typed metadata, failed `INVOCATION_RUNTIME_FAILURE` response, one provider command, and one terminal `invocation_result`.
  - Marked story `btrc-p0-characterization-005` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - tests/functional/cli/one_shot/characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/cli/one_shot -count=5 -v -timeout 300s` — passed.
  - `go test ./tests/functional/cli/one_shot -race -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/cli/one_shot` — passed.
  - `go test ./tests/functional/cli/... -count=1 -timeout 600s` — the new one-shot, output, input-contract, MCP-resume, root-discovery, and session-resume packages passed; unchanged `tests/functional/cli/named_invocation` has four pre-existing failures around unknown `--mode` compatibility input and unresolved `${executorProvider}` provider selection.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The one-shot invocation requires the provider-shaped output to contain the authored `COMPLETE` stop token; the current primary-result projection retains that marker in the returned text.
- A failed one-shot dispatch currently returns a typed failed `InvocationResponse` and process error while the aggregate canonical session terminal event still projects `SUCCEEDED`/`FINAL` once the single work is terminal; preserve both surfaces in future characterization.
- `--output response-stream` emits canonical Factory Event records followed by one `invocation_result`, making duplicate terminal publication observable without a live server or timing helper.
---
## 2026-08-10 06:26 -07:00 - btrc-p0-characterization-006
- What was implemented
  - Added deterministic ACP target success and JSON-RPC protocol-failure characterization at the existing offline ACP command/process boundary in `tests/functional/providers/acp/btrc_p0_characterization_test.go`.
  - Asserted the literal ten-event `RUN_REQUEST` through `DISPATCH_RESPONSE` order, contiguous sequences, request/dispatch/Worker Session correlation, provider/session identity, one canonical dispatch terminal payload, bounded typed failure detail, provider terminal response mapping, and complete IDLE session/Work projections.
  - Marked story `btrc-p0-characterization-006` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/providers/acp/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=1 -v -timeout 240s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=5 -timeout 300s` — passed.
  - `go test ./tests/functional/providers/acp -race -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/providers/acp` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The ACP worker path publishes ten canonical Factory events through one `DISPATCH_RESPONSE`; the continuous service session remains public `IDLE` while the Work projection carries the terminal target outcome.
  - ACP success ends in a provider `RUN/COMPLETED` response event, while protocol failure is surfaced as a synthesized provider `ERROR/FAILED` response event; canonical dispatch terminal uniqueness is asserted separately from provider-native response observations.
  - The existing JSON-RPC helper process is a deterministic command/protocol edge for ACP characterization and avoids live provider access or sleep-based coordination.
---
## 2026-08-10 06:33 -07:00 - btrc-p0-characterization-007
- What was implemented
  - Added deterministic direct JavaScript success and child-failure characterization at the existing `pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime` owner boundary.
  - Froze the literal `phase:activate` → `child_dispatch` QUEUED/RUNNING/COMPLETED → `phase:apply-result` → `terminal_result` order and checked contiguous runtime record sequences.
  - Asserted stable `dispatch-1`, child index, session-scoped artifact URI, fake Provider Session reference, prompt digest, complete projected child output, one terminal result callback, and typed script failure with `unknown` child failure detail and no primary result.
  - Marked story `btrc-p0-characterization-007` as passed in `prd.json`; no production behavior, public/generated contract, or Factory Sessions cutover changed.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime/host_children_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime -run '^TestBTRCP0DirectJavaScript' -count=5 -v` — passed.
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime -count=1` — passed.
  - `go vet ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The direct JavaScript owner publishes ordered `RuntimeRecord` facts through `OnRecord`; terminal typed-value application is observed afterward through exactly one `OnResult` callback rather than a separate runtime record.
  - The deterministic fake child executor gives the direct boundary stable dispatch, artifact, Provider Session, and failure metadata without constructing Factory Sessions or invoking a live provider.
  - A failed fake child emits QUEUED/RUNNING/FAILED records before the JavaScript runtime returns `WORKFLOW_RUNTIME_SCRIPT_ERROR`; the child failure detail remains typed and the terminal result hook is not called.
---
## 2026-08-10 06:50 -07:00 - btrc-p0-characterization-008
- What was implemented
  - Added deterministic standalone fixture characterization in `tests/functional/sessions/standalone/fixture_characterization_test.go` through `root.BuildProcess` and `Process.Execute` at the public fixture-backed MCP stdio boundary.
  - Froze the literal three-event `SESSION_STARTED` → `SESSION_RESULT_UPDATED` → `SESSION_COMPLETED` sequence, event IDs, session identities, sequence/tick cursors, terminal payload statuses, and the complete successful `Ticket triaged and resolved.` result with its artifact reference.
  - Characterized the `missing-source` failure as `FAILED`/`UNAVAILABLE` with no primary result, the same terminal event sequence, a typed `unknown` failure on the session projection, and zero lingering dispatches.
  - Repeated the same success request against two temporary catalog variants with independent session IDs and result text, asserting separate event histories, separate results, and a typed `factory_session.session.not_found` cross-store read.
  - Marked story `btrc-p0-characterization-008` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/sessions/standalone/fixture_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/sessions/standalone -run '^TestBTRCP0StandaloneFixture' -count=1 -v -timeout 240s` — passed.
  - `go test ./tests/functional/sessions/standalone -count=5 -timeout 300s` — passed.
  - `go test ./tests/functional/sessions/standalone -race -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/sessions/standalone` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Standalone fixture sessions synthesize canonical lifecycle events when the fixture omits an explicit event array; terminal success and failure therefore share the three-event lifecycle shape while result/session projections carry the meaningful outcome detail.
  - Fixture catalog variants must update the scenario session ID and both the sync-start and read-result session/result identities; the in-memory fake service keeps those projections separate.
- The current failure mapping intentionally preserves `UNAVAILABLE` without `failureDetail` on `start_sync` and `get_result`; the typed missing-source detail is observable on `get` and should remain characterized at that surface.
---
## [2026-08-10 07:20 -07:00] - btrc-p0-characterization-009
- What was implemented
  - Added a root `BuildProcess`/`Process.Execute` one-shot cancellation characterization with a deterministic blocking provider edge. It freezes the 13-event cancellation artifact, typed `CANCELED`/`INVOCATION_CANCELED` result and process error, current `SUCCEEDED`/`FINAL` aggregate session projection, one terminal response-stream record, one provider attempt, and repeated-stop idempotence.
  - Promoted the runtime-opening partial-failure coverage to a named characterization. It records Models and worker acquisition, the late Automations failure, reverse cleanup, the joined initiating/worker-cleanup errors, exactly-once resource stops, and the absence of Factory Runtime assembly.
  - Added concurrent on-demand target characterization proving cancellation and replacement of one opaque target does not affect a second blocked target; it asserts per-target result/session/request identity, isolated cleanup, and repeated cancel/close idempotence.
  - Marked story `btrc-p0-characterization-009` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/cli/one_shot/btrc_p0_cancellation_characterization_test.go`
  - `pkg/services/factory_sessions/internal/runtimeopening/models_bind_test.go`
  - `pkg/services/factory_sessions/internal/ondemandtarget/target_execution_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/cli/one_shot -run '^TestBTRCP0OneShotCancellationCharacterization$' -count=10` — passed.
  - `go test -race ./tests/functional/cli/one_shot -run '^TestBTRCP0OneShotCancellationCharacterization$' -count=5` — passed.
  - Focused runtime-opening and on-demand target repeat tests (`-count=10`) — passed.
  - Focused runtime-opening and on-demand target race tests (`-race -count=10`) — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The blocked one-shot cancellation path currently permits two complete artifact orders differing only in the adjacent `MODEL_RESPONSE` and `FACTORY_STATE_RESPONSE`; both retain contiguous canonical sequence numbers and exactly-once terminal publication.
  - On-demand cancellation closes the captured runtime, publishes the canceled invocation only after replacement succeeds, and treats controls against the replacement or an evicted identity as idempotent no-ops.
- Late runtime-opening failures use joined errors: the initiating Automations failure remains discoverable alongside earlier worker cleanup failure, while acquired resources close in reverse order and later assembly is not called.
---
## [2026-08-10 07:31 -07:00] - btrc-p0-characterization-009 verification follow-up
- What was implemented
  - Refactored the concurrent target characterization into a deterministic fixture and focused assertion helpers so the new test stays within repository maintainability limits.
  - Consolidated the replay characterization into the existing `pkg/services/recordings/internal/replay/runtime_decoder_test.go` file and split its success assertions, preserving the same observable replay coverage while restoring package file-count and maintainability baselines.
  - Updated the replay story note in `prd.json` to match the reconciled file location; the PR description will be refreshed from this exact PRD.
- Files changed
  - `pkg/services/factory_sessions/internal/ondemandtarget/target_execution_test.go`
  - `pkg/services/recordings/internal/replay/runtime_decoder_test.go`
  - `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go` (consolidated/deleted)
  - `prd.json`
  - `progress.txt`
- Verification
  - `make test` — passed (short Go suite).
  - `make backend-size` — passed.
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - Affected replay and concurrent-target repeat/race tests and vet — passed.
- **Learnings for future iterations:**
  - Characterization tests should be split into owner-focused helpers before running `pkg-maint`; detailed observable assertions can remain rich without making one test function exceed complexity or line limits.
  - When a P0 test adds a file to a package at its exact file-count baseline, consolidate it into a semantically adjacent existing test file rather than recording a new baseline entry.
---
## 2026-08-10 07:53 -07:00 - btrc-p0-characterization-003 review follow-up
- What was implemented
  - Reconciled the branch with current `upstream/main`, retaining the active BTRC `prd.json`, both append-only progress histories, and the current-main source/generated changes.
  - Moved the hosted characterization into the approved `tests/functional/workstations/poller/` domain/subsection cell.
  - Replaced hosted test timeout waits with `testing.T` context and a context-aware Factory Event stream observation helper.
  - Added explicit continuous-service result reconciliation: success and execution failure assert the typed `NOT_FOUND` live `/result` response while dispatch plus terminal Work/progress remains the complete caller-visible service result. The in-code comment documents why this hosted service path uses the public HTTP inspection contract; finite CLI result mapping is covered by story 005.
- Files changed
  - `tests/functional/workstations/poller/hosted_characterization_test.go`
  - `tests/functional/internal/support/factory_event_stream.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workstations/poller -run '^TestBTRCP0HostedService' -count=1 -v -timeout 180s` — passed.
- **Learnings for future iterations:**
  - The hosted/service success contract is intentionally not a terminal live Factory Session result: continuous Petri sessions stay IDLE, so `/result` returns typed NOT_FOUND while dispatch response, terminal Work, and progress are the stable service result.
  - Moving a functional scenario into an approved domain/subsection can preserve its package-private fixture helpers when the destination owns the same workstation behavior.
---
## 2026-08-10 08:10 -07:00 - btrc-p0-characterization review follow-up
- What was implemented
  - Addressed the blocking PR gate feedback by moving the batch characterization to `tests/functional/factory/batch/` and both one-shot characterizations to `tests/functional/factory/invocation/`, clearing the ratcheted functional package-structure violations.
  - Replaced the batch-specific in-process `ProviderOverride` with the existing `ProviderCommandRunner` edge and queued shaped command results; success remains two accepted dispatches and the failure case now observes the real command-edge `permanent_bad_request` classification on the second work.
  - Added an in-code explanation for the ACP completion ceiling: public terminal Factory Session observation is the positive synchronization, while the duration is only a bounded failure guard around an out-of-process protocol edge.
  - Added an observable runtime-preservation acceptance criterion to story `btrc-p0-characterization-001` and reconciled its notes and all moved test paths in `prd.json`.
- Files changed
  - `tests/functional/factory/batch/batch_characterization_test.go`
  - `tests/functional/factory/invocation/characterization_test.go`
  - `tests/functional/factory/invocation/btrc_p0_cancellation_characterization_test.go`
  - `tests/functional/providers/acp/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/batch -count=3 -v -timeout 300s` — passed.
  - `go test ./tests/functional/factory/invocation -count=3 -v -timeout 300s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=3 -v -timeout 300s` — passed.
  - `make pkg-structure` — passed with no new violations.
  - `go vet ./tests/functional/factory/batch ./tests/functional/factory/invocation ./tests/functional/providers/acp` — passed.
  - `git diff --check` — passed.
  - Parsed `prd.json` reports zero stories with `passes:false`.
- **Learnings for future iterations:**
  - The prior PR head’s Backend Functional Coverage failure was an unrelated flaky `runtime_api` log assertion (`TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`); the required local characterization and structure gates remain green, so rerun CI after publishing before changing unrelated runtime-api behavior.
  - `gh pr view` confirmed current `upstream/main` is already the PR base and the PR is mergeable; compare the explicit base/head SHAs before creating a needless merge commit.
---
## 2026-08-10 08:16 -07:00 - btrc-p0-characterization review verification follow-up
- What was implemented
  - Completed the local review-gate verification after the path, edge, ACP documentation, and PRD acceptance updates; no production behavior or public/generated contract changed.
- Files changed
  - No additional tracked files.
- Verification
  - `make test` — passed.
  - `make backend-size` — passed.
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - `make pkg-structure` — passed.
  - `make vet` — passed.
  - `make typecheck` — passed.
  - Hosted characterization repeated twice — passed.
  - The previously failing `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput` repeated three times locally — passed.
- **Learnings for future iterations:**
  - The prior GitHub functional-coverage failure is reproducible as a transient baseline failure rather than a deterministic source failure; fresh CI after this head is the authoritative check.
---
## 2026-08-10 07:25 -0700 - wsr-001-opening-record current-main verification
- What was implemented
  - Merged the latest `origin/main` at `ccea14bf8` as merge commit `9bc01fdd1`, retaining the WSR task PRD, append-only progress history, current-main runtime changes, and functional coverage.
  - Regenerated bundled, Go, package, and UI interface artifacts after synchronization; generation produced no tracked drift.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main orchestration engine and plain-batch functional changes were retained by the merge.
- Verification
  - `make interfaces-all` — passed with a clean working tree.
  - `make verify-fast` — passed dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make pkg-structure` — passed; 573 deletion-only baseline entries remain.
  - `go test ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/... -count=1` — passed.
  - WSR-FT-001/002 and WSR-FT-003 focused functional tests — passed on the merged head.
  - `make lint` passed UI lint/dead-code, vet, backend-size, maintainability, and file-count stages, then stopped at the existing 176-entry `pkg-boundary` migration baseline; no changed WSR file was reported as a new violation.
  - `git diff --check` — passed; `prd.json` parses with all three stories still marked `passes:true`.
- **Learnings for future iterations:**
  - A current-main merge can add unrelated product tests and runtime fixes while leaving the active PRD unchanged; preserve those files and rerun both the shared fast tier and task-specific functional cells.
  - The repository lint aggregate reports the known package-boundary migration baseline after all preceding checks pass; use the focused boundary output to distinguish baseline debt from task regressions.
---
## 2026-08-10 08:28 -0700 - wsr-001-opening-record review follow-up
- What was implemented
  - Merged current `origin/main` at `11b34c073` as merge commit `c8f25ec73`; the active WSR PRD remains authoritative and current-main product/test changes are retained.
  - Corrected the provider-output bridge so canonical and raw provider observations are suppressed when Worker Sessions cannot resolve the dispatch or detects a provider-binding conflict; pre-opening provider output can no longer be forwarded ahead of `SESSION/STARTED`.
  - Split the Worker publisher identity regression into focused helpers so backend-size and maintainability gates remain within their limits.
- Files changed
  - `pkg/services/worker_sessions/publish.go`
  - `pkg/services/worker_sessions/observation_test.go`
  - `progress.txt`
  - `prd.json` remains unchanged with all three stories marked `passes:true`.
- Verification
  - `make interfaces-all` — passed with no generated drift.
  - Focused Worker Sessions, Factory Runtime, and WSR-FT-001/002/003 tests — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract checks, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make lint` passed all stages through backend, then stopped at the known 176-entry package-boundary migration baseline; no WSR change was reported as a new violation.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Provider output that cannot be mapped to a supervised Worker Session is not safely recoverable at the response boundary; suppress it so the customer-visible stream cannot outrun the authoritative opening.
  - Keep ordinary publication-failure forwarding behavior separate from typed dispatch-resolution failures: the former preserves downstream resilience, while the latter would violate lifecycle ordering.
  - After current-main synchronization, rerun interface generation before interpreting contract or generated-artifact failures; a clean generator run is stronger evidence than comparing only the authored API fragments.
---
## 2026-08-10 09:40 -0700 - wsr-001-opening-record provider-history review follow-up
- What was implemented
  - Addressed the blocking PR feedback that WSR-FT-003 only checked Factory response events by adding a public canonical Worker Session-ID history route and draining it through the root-built functional support path.
  - Made Worker-ID history projection provider-neutral: retained lifecycle/output records remain inspectable when Provider Sessions transcript projection is unavailable, while any real Provider Session reference is preserved without synthesis.
  - Extended observable provider conformance evidence across Antigravity final-only, Claude full-stream, Codex partial-stream, and Cursor/ACP session-bearing executions. Each history asserts opening position 1, stable Work/Worker correlation, source-event idempotency and sequence, provider binding before output, provider provenance/fidelity, and terminal-last replay.
  - Synchronized the branch with current `origin/main`, regenerated REST, Go, package, and UI contract artifacts, and updated the active PRD story notes and consolidated patterns.
- Files changed
  - `api/openapi-main.yaml`
  - `api/openapi.yaml`
  - `api/components/parameters/WorkerSessionID.yaml`
  - `pkg/services/worker_sessions/`
  - `pkg/transports/http/`
  - `tests/functional/internal/support/`
  - `tests/functional/workers/inference/`
  - `tests/functional/providers/acp/acp_provider_events_test.go`
  - `packages/api/generated/`
  - `ui/src/api/generated/openapi.ts`
  - `ui/packages/client/src/generated/openapi.ts`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` — passed.
  - Root-built WSR-FT-003, Claude full-stream, and Codex partial-stream history tests — passed.
  - Root-built Cursor/ACP Worker Session history conformance test — passed.
  - `make interfaces-all` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A public Worker Session history contract must expose the stable Worker ID directly; requiring a Provider Session transcript projection makes valid provider-neutral histories appear unavailable.
  - Conformance is stronger when the same public history drain validates both no-reference and session-bearing providers, including source idempotency and terminal replay rather than only final response payloads.
---
## 2026-08-10 10:06 -0700 - wsr-001-opening-record final verification
- What was implemented
  - Kept the new conformance assertions within the repository backend-size and maintainability limits by moving focused observation edge helpers alongside the existing internal service coverage and splitting the Antigravity history assertion at its behavioral boundary.
  - Updated existing Worker Sessions service doubles for the expanded observation contract so the full repository short suite continues to compile and run.
- Files changed
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
  - `pkg/services/worker_sessions/internal/service/invoke_observation_test.go`
  - `tests/functional/workers/inference/opening_record_test.go`
  - `pkg/services/factory_runtime/internal/build_test.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/factory_runtime_test_helpers_test.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/dispatch_worker_sessions_idempotency_test.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/dispatch_worker_sessions_terminal_semantics_test.go`
  - `progress.txt`
- Verification
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract checks, 355 Vitest files / 2,970 tests, 191 Bun tests, and the short Go suite.
  - `make api-smoke` — passed OpenAPI validation, generation/drift checks, contract alignment, and provider parity smoke.
  - Focused Worker Sessions, WSR, Claude/Codex, and Cursor/ACP tests — passed.
  - `git diff --check` — passed; worktree clean before publish.
- **Learnings for future iterations:**
  - The repository’s parallel short-suite lane can expose an intermittent 300-second Worker Sessions timeout even when the package passes in isolation; rerunning the complete lane after isolating the named package establishes whether it is a concurrency flake.
  - Public interface additions require updating manually implemented service doubles across Runtime tests; embedding an existing root interface does not help doubles that implement methods explicitly.
---
