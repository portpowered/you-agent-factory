## Codebase Patterns
- Public UI package build and pack verification mutate sibling `dist` directories; run dependency preparation, package build, and pack verification serially to avoid transient missing-module or Windows file-lock failures.
- Package-owned React Flow replay surfaces should set a mobile-safe minimum zoom below React Flow's default when fit-to-view must keep authored node controls inside narrow viewports.
- Packaged catalog generation runs canonical Factory validation before publication-specific semantic-role checks; removing a referenced terminal state requires a real terminal route/bridge or a coordinated validator decision.
- Cross-host graph semantics should enter React Flow nodes as a disposable package projection and render through the shared workstation node; host-specific activity metadata must remain decoration, not classification.
- Forward the complete package-owned workstation semantic projection across shared graph hosts; manually selecting axes can silently drop optional guarded-control details.
- Normalize trimmed blank durable identifiers to absent before authored-versus-runtime joins; only non-empty IDs select identity, otherwise use the documented legacy-name fallback.
- First-party packaged Factory semantic checks should consume parsed canonical definitions inside `internal/packagedfactorycatalog`; keep customer-authored and JavaScript Factory validation unchanged.
- Keep packaged installation on an explicit catalog-owned `PackagedFactoryPersistence` capability; ordinary named-Factory persistence stays strict while only exact first-party identities may filter named lifecycle-bridge targets.
- A lifecycle-only Work state is valid only through a named production bridge that matches the runtime contract and has direct behavioral test evidence; do not use comment-only allowlists.
- When a catalog audit finds a lifecycle state that the current runtime does not emit, leave it as explicit follow-up work rather than relaxing validation with a guessed bridge; source comments and stale diagrams are not behavioral evidence.
- Do not junction a temporary worktree's `node_modules` to a sibling worktree; Windows worktree cleanup can follow workspace links into tracked package files. Use isolated dependencies and remove the temporary worktree without cross-worktree links.
- Normative customer-prose policy belongs under `docs/internal/standards/writing`; use `docs/architecture/data-model.md` and public contracts for product meaning, and keep exact machine/technical literals separate from reviewable natural-language prose.
- The canonical product vocabulary source is `docs/internal/standards/writing/customer-technical-terms.yaml`; keep every record on one explicit schema and point its owner to the architecture or public-contract source of meaning.
- When reconciling a task branch against a moving mainline, keep the active task's PRD and follow-up metadata authoritative while accepting unrelated source changes from main; validate the merged JSON and preserve the append-only progress history.
- Keep synchronous and asynchronous entry points on one domain-owned
  preparation/supervision path; parameterize lifecycle ownership and caller
  wait semantics instead of duplicating reservation, opening, and admission
  logic.
- Root-built functional Worker tests can make mock script execution deterministic by injecting the same gated command runner into both provider and script command edges; the mock-worker mode may route a model-backed workstation through either edge.
- Server-owned async acceptance needs a distinct admission signal from publication and terminal completion; record pre-admission control intent before releasing that signal so a racing cancel cannot be acknowledged as accepted.
- Shutdown should close new async admission before closing the Workers pool,
  route admitted server-owned work through its existing cancellation boundary,
  and join both terminal publication and the supervision driver's final tail.
- Functional HTTP disconnect evidence should keep the request unread on a raw connection, gate a real provider command runner, and observe terminal state through an independent public SSE client; canceling a context after a completed response is not a disconnect proof.
- This checkout may have `ui/node_modules` without all lockfile dependencies; run the pinned Bun install before treating a typecheck failure as a source regression.
- When a package is constrained to one named root service interface, keep a
  peer lifecycle hook structural and optional; let composition detect the
  narrow method without widening every test double or alternate adapter.
- When hosted Go coverage reports a per-package floor regression, use its exact package diagnostic to target deterministic branch tests and verify the package floor independently before rerunning the broader lane.
- Worker Session async idempotency should claim the caller key and stable session identity in one lock transaction before publication or dispatch, then replay a detached tuple and stored outcome.
- When a public contract defines an exact identifier spelling, keep one register record for that identifier; relationship examples must also follow the owning contract's source/target semantics.
- Documentation-only stories should validate authored structured data as review evidence and leave source-scanning or meta-test enforcement to a real consumer; source shape alone is not observable customer behavior.
- When a documentation standard mirrors a governing plan, enumerate the plan's rule sets at the policy boundary and label future checker lifecycle rules as not manually applicable until their consumer exists.
- JSON Schema worker diagnostics must use `ErrorKind.KeywordPath()` and typed schema metadata; localized `ValidationError.Error()` text can leak rejected values and compiler-expanded host paths.
- When an exact package file-count baseline blocks an oversized-file split, move the cohesive helpers into an existing appropriately owned file and verify both size and file-count gates.
- Optional native JSON channels use an explicit presence bit plus custom JSON at durable boundaries to distinguish omitted values from JSON null; clone map/slice trees at projection and transport ownership boundaries.
- Generated `interface{}` transport types collapse explicit JSON null during ordinary decoding; inspect raw HTTP/SSE JSON at the transport boundary and carry an internal normalized marker until final rendering.
- For final PR handoff, compare the PR body byte-for-byte with `prd.json` and use the thread-aware GraphQL review audit because flat PR comments do not expose inline-thread resolution state.
- OS-boundary functional tests may poll an isolated durable filesystem checkpoint only when the child exposes no parent-process event or injected edge; document that reason at each polling helper and keep the bound as a failure guard.
- Git Worktree preparation treats a retained Git `*.lock` as indeterminate ownership: retry within a named bound, preserve the resource, and return its exact path with verify/remove/retry guidance.
- Git's `.git/config.lock` has no trustworthy owner or PID evidence; after a bounded retry, report the exact lock with `owner_liveness=indeterminate` and require verification before removing only that file—never auto-delete it.
- Named Factory persistence stages under the catalog root before an atomic directory move; a matching pre-existing `.<factory>.staging-*` must be preserved and surfaced as indeterminate contention with exact verify/remove/retry guidance, and pre-run Cobra failures need an explicit stderr write because the command tree silences them.
- Cross-process startup observers must treat a readable-path failure such as a transient Windows sharing violation as retryable within the existing bounded observation window, because file existence can precede release of the writer handle.
- Hard-kill startup probes should observe a durable checkpoint before listener readiness, then use explicit stdout/stderr/process-state guards; when the child exposes no acquisition event, suspend it before inspecting the exact resource and resume only on a miss so the observed lease cannot be released before the forced kill; `backendScopeID` is persisted identity, not evidence of a cross-process ownership lock.
- Packaged Factory create/replace ownership uses a deterministic `.<factory>.staging-owner` directory with atomically published PID metadata; existing valid targets skip without taking that write lease, while live or ambiguous owners are preserved and reported with bounded contention diagnostics.
- Cross-process ownership diagnostics should carry the persisted backend-scope ID through the owning service request and emit safe structured fields for resource, outcome, owner liveness, and unverified PID evidence.
- Exclusive directory reservation is a synchronization capability: inject it as a narrowly owned function port and keep generic filesystem adapters limited to inspection and ordinary materialization effects.
- Treat an absent PID as indeterminate unless the owner probe has stronger proof of orphanhood; compare the owner record again before any explicitly permitted orphan reclaim.
- Atomic orphan reclaim must rename the exact revalidated lease to a distinct still-visible recovery path before publishing the new owner; never remove and reuse the same path across a delayed contender.
- Cross-process lease probes must suspend before filesystem inspection and match the exact ownership-resource suffix; observe-then-suspend leaves a release race even when the path is re-stat'ed.
- For mechanical test splits, capture the exact go test inventory, repeatable coverage command/output, and one-to-one behavior/helper placement map in a review artifact before moving tests.
- Keep comments and exemption directives attached to the declaration they govern when splitting Go test files; otherwise a boundary can duplicate or orphan maintainability policy.
- Replacing a file-level size exemption with focused test files may require per-function directives; update both the source directive and the sorted exemption-budget entry together.
- When moving a test with a pkgmaintcheck exemption, move the existing budget target with the test and retain legacy file suppressions until the remaining staged split retires the source file.
- For broad functional coverage concurrency flakes, isolate the named test and rerun only the failed CI jobs before changing untouched behavior.
- When resolving long append-only progress conflicts, read merge-stage files in bounded chunks and validate the reconstructed line count so tool-output truncation cannot drop history.
- Treat a full functional-coverage failure in an untouched fixture as a characterization problem first: repeat the exact test locally, then rerun only the failed coverage and dependent-policy jobs if it passes.
- When a backend-size violation names a long test function, split each failure mode into a focused test and share only request/setup plumbing so every observable assertion remains intact.
- When a dispatch identity is upgraded to a Worker Session ID in a later event batch, migrate its progress color alias instead of allocating a second color; terminal cleanup must remove the canonical identity.
- Treat a full functional-coverage failure in an untouched fixture as a characterization problem first: repeat the exact test locally, then rerun only the failed coverage and dependent-policy jobs if it passes.
- When a backend-size violation names a long test function, split each failure mode into a focused test and share only request/setup plumbing so every observable assertion remains intact.
- For concurrent single-flight state, keep the absent-to-loading election atomic with the observation; a separate precheck can let callers pass stale absent state and overwrite the shared completion signal.
- For repeated concurrency package runs, synchronize cancellation tests on an injected launcher or process edge; a fixed sleep can cancel before startup under parallel stress and obscure the target regression.
- Provider OpenAPI changes that feed the published model-provider package require `make interfaces-all`, `make provider-catalog-generate`, and `make model-provider-package-generate`; `api-smoke` checks generated REST artifacts against the committed tree, so commit generated output before relying on that smoke result.
- Provider evidence dates authored in YAML must be quoted ISO strings because yaml.v3 otherwise decodes calendar-looking values as `time.Time` before semantic validation.
- For fan-in transitions, preserve a deterministic list of all matched input token IDs separately from the consume-only mutation list; OBSERVE-mode inputs must reach the worker dispatch without being consumed.
- Terminal lifecycle publication must require the opening record's committed publication window; a failed opening must never be followed by a terminal append that becomes topic position 1.
- Root-built direct-JavaScript functional probes that do not invoke a worker can run without `--with-mock-workers`; keep Mock Workers usage inside the feature-owning test cells.
- Cancellation-sensitive functional fixtures must signal provider-edge entry before issuing an interrupt; a published RUNNING status can precede provider handoff and otherwise makes context-cancellation assertions race.
- When a Windows coverage wrapper times out, inspect its exact process tree and terminate only that invocation's descendants; preserve concurrent test processes.
- When splitting oversized same-package tests into focused files, remove stale file/maintainability exemptions and update the package file-count baseline for the intentional net increase; keep shared fixtures in one package-local helper file.
- For final PR handoff, compare the PR body byte-for-byte with `prd.json` and use the thread-aware GraphQL review audit because flat PR comments do not expose inline-thread resolution state.
- Treat repository-enforcement inventories and architecture guards as project-enabling PRD criteria when they have no runtime, API, CLI, UI, or emitted-event behavior; source scans support enforcement but are not customer-behavior evidence.
- AST scanners must expand grouped Go field declarations before indexing parameter or result types; a field-count helper can exceed the AST field-list length and panic on valid source.
- Asynchronous service characterizations should wait on observed edge/context signals rather than arbitrary `time.After` ceilings; the test context remains the failure escape hatch.
- Continuous Petri service sessions intentionally remain `IDLE` after terminal Work; characterize the dispatch and Work/progress projection as the caller-visible result and assert the live-result route's typed unavailability instead of inventing a finite session result.
- Use an authored capacity-one resource in a deterministic fixture to serialize multi-work dispatches when exact Factory Event order must be characterized; this avoids sleeps and preserves the real dispatch/resource boundary.
- Correlate batch history from both sources: request/relationship and dispatch identity live in Factory Event context, while terminal per-work state is carried by dispatch-response outputWork.
- For hosted-source characterization, route the poller through an unseeded processing input and map its external submission to a separate normal processor; this isolates source effects from the downstream dispatch executor while preserving the real runtime path.
- Runtime-opening ownership inventories should source grouped fields from runtimeopening.Dependencies, invocation-local values from ExternalEffects, and late seams from the opener; keep the reconciliation reviewer-visible in documentation instead of adding source-inventory tests.
- When synchronizing a task branch with current main, preserve the active branch's task-scoped `prd.json` and merge unrelated main history into `progress.txt`; validate the resulting PRD before staging.
- When a termination drain consumes a runtime wake signal, the engine must re-enter its tick loop directly; returning to the outer wait loses the already-consumed wake and can strand a finite run.
- Customer-facing functional tests must compose through `root.BuildProcess` and assert public process/CLI behavior; keep service-specific typed-error assertions in service-layer tests because the functional boundary check rejects direct Runtime imports.
- Repository-wide `pkg-boundary` traversals must prune through one root-relative, slash-normalized policy before parsing entries; adding a new `WalkDir` requires using that same decision.
- Treat disposable artifact/worktree paths as exact repository roots, not directory-name substrings, so tracked production packages with nested artifact-like names remain visible to the gate.
- Canonical packaged CLI docs live under `docs/reference/`; adding a topic also requires its authored file in `docs/reference/embed.go` and a registry entry in `pkg/transports/cli/docs/docs.go`.
- The `you run --named <factory> --help` output is the authoritative invocation contract; omitted role provider/model values resolve through operator defaults.
- Documentation for model-backed workflows should describe observable result shape and status/artifact evidence rather than byte-stable generated prose.
- Isolate both `HOME` and `USERPROFILE` when verifying packaged named Factories so stale materialized operator copies cannot mask the current catalog source.
- A classifier mock must emit the provider's valid response protocol and an exact configured route label; the default synthetic mock acceptance text is not a valid `small`/`medium`/`large` classification.
- Planner and cycle-control workflows likewise need protocol-valid structured model output to exercise generated Work, concurrency, or replanning; an empty generic mock can prove only the terminal invocation envelope and may leave orchestration Work uncreated.
- JavaScript packaged Factories that parse planner or judge output need protocol-valid provider-command evidence; the CLI `--with-mock-workers` path returns generic text for those child calls, so use focused provider-runner assertions plus a provider-free schema-bound CLI smoke and document the distinction.
- Workstation prompt templates receive `PromptData`; access a bound work identifier through `(index .Inputs N).WorkID` rather than a root `.WorkID` field.
- Prompt templates should clone native structured-result trees at the `TokenData` boundary and guard optional-result references before execution so absent upstream handoffs cannot silently fall back to payload text.
- Run-level `--quiet` cannot be combined with structured `--json` or `--output`; verify output-mode combinations against the live CLI before documenting them.
- For direct named packaged Factory invocations, bind inputs in authored workstation bodies with `${parameter}` interpolation; `.Inputs` payload can be empty even when the named CLI argument is present.
- Multi-input packaged Factories must give required parameters non-ambiguous invocation bindings; the ACP reachability guard requires each required parameter to expose a text-carrying route, while authored validation rejects routing stdin to multiple parameters.
- AGY structured-output replays require the provider's echoed `json_schema` to be JSON-equivalent to the authored request; keep historical fixture envelopes synchronized with the authored schema while preserving their recorded output evidence.
- A schema-valid structured QA `reroll` is an accepted inspection verdict; provider refusal, malformed output, and schema mismatch must fail before verdict interpretation, while provider failures may consume the bounded default retry budget.
- Packaged Factory additions that change the public catalog require synchronized authored/generated sources, catalog counts, the canonical reference topic, and CLI docs smoke markers; refresh generated output only through `make packaged-factory-catalog-generate`.
- Verify documented named-Factory commands against isolated live `you run --named ... --help`; role-pinned provider/model defaults should be documented as fixed policy when no override flags are exposed.
- Stable closed-vocabulary failure reasons must be added together to worker terminal policy, canonical event normalization, replay/session projections, worker-session diagnostics, and public transport whitelists; otherwise a valid new reason can be downgraded to `unknown` at a later boundary.
- Functional CLI tests that need worker behavior should inject `ProviderCommandRunner` through `edges.Edges` and `root.BuildProcess`; reserve built-executable subprocesses for OS-boundary facts that in-process execution cannot expose.
- Keep `--with-mock-workers` coverage inside `tests/functional/workers/mock/...`; non-workers/mock functional cells should use provider-command edge doubles and sanitized public assertions.

- Worker Session finite streams can use the injected Events `Read` contract with the first retained head as a snapshot boundary; replay-only paths must not call `Subscribe`.
- Authored OpenAPI and CLI contracts generate their Go, TypeScript, published metadata, and family-manifest consumers; update the source contract first, then run the repository generators and drift checks.
- Provider-neutral output contracts belong on the workstation definition and are enforced after a successful worker response; provider process success or transport parsing alone must not satisfy a semantic report contract.
- Stream transport dispatch stays below the package maintainability threshold by delegating lifecycle-specific frame handling to focused helpers.
- Deterministic Worker Session lifecycle tests can combine the controlled workstation boundary with the real in-memory Events service to verify retained replay ordering without sleeps or cancellation.
- Detached Cobra command families should project local-only flag relationships without eagerly merging parent persistent flags; otherwise the detached storage can shadow the production root flag when the family is attached.
- Process-boundary CLI tests should build `./cmd/factory` only when proving OS-level behavior, keep the real server on a root-built process, and assign redirected stdout and diagnostics stderr separately.
- Restore package coverage thresholds with focused behavioral tests for typed edge outcomes; do not lower baseline manifests for new branches.
- Functional coverage gates execute production packages through the functional harness; unit tests alone do not restore a functional package threshold.
- Public CLI manifest additions require regenerating family metadata and the published CLI contract, then updating command input/help/tree baselines.
- Reviewed functional CLI coverage requires a sorted scenario declaration, an evidence-registry entry, a manifest mapping, and one literal `functionalevidence.Covers` marker.
- Replay completeness must be derived from a terminal lifecycle record inside the captured Events range, not from a separately synchronized terminal session state.
- When a package has a strict file-count baseline, put a small refactor in an existing focused file before adding a new helper file or changing the baseline.
- A deletion-only package-file baseline must remain unchanged during a same-package test split; fold adjacent behavior groups into existing owners and satisfy the stricter maintainability line gate before considering any baseline change.
- When broad functional coverage reports a cancellation-observation failure, rerun the failed lane and isolate the named test before changing an unrelated feature; parallel-suite failures can move between existing durability/restart tests while isolated runs pass.

- When a functional coverage job reports a missing evidence artifact, inspect the earlier test output first; the missing artifact can be a secondary symptom of the primary test failure.
- For cross-repository PRs, resolve review and CI state with an explicit owner/repository and compare local, fork-branch, and PR head SHAs; current-branch discovery may follow a different local origin.
- Package-target discovery is the stable-sorted set of directories containing non-test Go sources; adding a durable service owner requires updating the checker vocabulary and its live top-level inventory before regenerating the manifest.
- Boundary check compatibility bridges should be exact package/type exceptions with request-scoped rationale; broad service-prefix allowlists can hide new durable ownership claims.
- Compatibility guard exceptions must match the reviewed method/field shape as well as the package and type name; name-only exemptions can hide ownership-expanding contract growth.
- When an ownership burn-down ledger is corrected, regenerate it as an explicit empty array and reject retired rows so stale debt cannot silently return.
- Custom `run` flag parsing happens inside the command handler, so relationship checks for local run flags must execute after parsing; use a raw pre-initialization guard as well when the initializer itself is a side effect.
- Placement conflicts should use the same typed `ErrorResponse` contract at every validation boundary so flag ordering has one stable code, family, and actionable message.
- New functional scenarios must use an allowed product-domain noun plus subsection path such as `tests/functional/factory/batch`; transport names like `cli` and internal implementation names like `factory_runtime` are not durable domain cells.
- Ordered functional provider outcomes can use `support.NewShapedProviderCommandRunner` with queued provider-native `CommandResult` values, including a later non-zero result, while preserving the real command-runner edge and typed provider classification.
- For broad functional coverage concurrency flakes, isolate the named te…164365 tokens truncated…nal-test flakiness, verified the WSR-specific focused test locally, and reran only the failed CI jobs until the complete required check set passed.
  - Merged the live `origin/main` head `aac04248d` into the branch before final handoff; the merge contained only current-main development-plan documentation and had no conflicts.
- Files changed
  - `progress.txt`
  - Current-main files under `docs/internal/development/plans/` carried by the synchronization merge.
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v` — passed.
  - CI run `31451707750` — all required checks, including Backend Functional Coverage and Verification Policy, passed after the failed-lane retries.
  - `prd.json` JSON validation and all three `passes:true` stories — passed; PR #1849 body remains byte-for-byte identical to `prd.json`.
  - Thread-aware PR audit — 7 conversation comments, 0 formal reviews, and 0 unresolved/outdated inline threads.
  - `git merge-tree --write-tree origin/main HEAD` and `git diff --check` — passed with `origin/main` at `aac04248d`.
- **Learnings for future iterations:**
  - When a broad functional lane fails on different timing-sensitive tests across retries, inspect the exact failed test and rerun only the failed jobs before widening an unrelated feature diff; the repository already records this as the preferred flake-handling pattern.
- Terminal lifecycle publication must require the opening record's committed publication window; a failed opening must never be followed by a terminal append that becomes topic position 1.

- Root-built direct-JavaScript functional probes that do not invoke a worker can run without `--with-mock-workers`; keep Mock Workers usage inside the feature-owning test cells.

- Cancellation-sensitive functional fixtures must signal provider-edge entry before issuing an interrupt; a published RUNNING status can precede provider handoff and otherwise makes context-cancellation assertions race.

- When a Windows coverage wrapper times out, inspect its exact process tree and terminate only that invocation's descendants; preserve concurrent test processes.
- JSON Schema worker diagnostics must use `ErrorKind.KeywordPath()` and typed schema metadata; localized `ValidationError.Error()` text can leak rejected values and compiler-expanded host paths.
- When an exact package file-count baseline blocks an oversized-file split, move the cohesive helpers into an existing appropriately owned file and verify both size and file-count gates.
- Optional native JSON channels use an explicit presence bit plus custom JSON at durable boundaries to distinguish omitted values from JSON null; clone map/slice trees at projection and transport ownership boundaries.
- Generated `interface{}` transport types collapse explicit JSON null during ordinary decoding; inspect raw HTTP/SSE JSON at the transport boundary and carry an internal normalized marker until final rendering.
- For final PR handoff, compare the PR body byte-for-byte with `prd.json` and use the thread-aware GraphQL review audit because flat PR comments do not expose inline-thread resolution state.
- OS-boundary functional tests may poll an isolated durable filesystem checkpoint only when the child exposes no parent-process event or injected edge; document that reason at each polling helper and keep the bound as a failure guard.
- Git Worktree preparation treats a retained Git `*.lock` as indeterminate ownership: retry within a named bound, preserve the resource, and return its exact path with verify/remove/retry guidance.
- Git's `.git/config.lock` has no trustworthy owner or PID evidence; after a bounded retry, report the exact lock with `owner_liveness=indeterminate` and require verification before removing only that file—never auto-delete it.
- Named Factory persistence stages under the catalog root before an atomic directory move; a matching pre-existing `.<factory>.staging-*` must be preserved and surfaced as indeterminate contention with exact verify/remove/retry guidance, and pre-run Cobra failures need an explicit stderr write because the command tree silences them.
- Cross-process startup observers must treat a readable-path failure such as a transient Windows sharing violation as retryable within the existing bounded observation window, because file existence can precede release of the writer handle.
- Hard-kill startup probes should observe a durable checkpoint before listener readiness, then use explicit stdout/stderr/process-state guards; when the child exposes no acquisition event, suspend it before inspecting the exact resource and resume only on a miss so the observed lease cannot be released before the forced kill; `backendScopeID` is persisted identity, not evidence of a cross-process ownership lock.
- Packaged Factory create/replace ownership uses a deterministic `.<factory>.staging-owner` directory with atomically published PID metadata; existing valid targets skip without taking that write lease, while live or ambiguous owners are preserved and reported with bounded contention diagnostics.
- Cross-process ownership diagnostics should carry the persisted backend-scope ID through the owning service request and emit safe structured fields for resource, outcome, owner liveness, and unverified PID evidence.
- Exclusive directory reservation is a synchronization capability: inject it as a narrowly owned function port and keep generic filesystem adapters limited to inspection and ordinary materialization effects.
- Treat an absent PID as indeterminate unless the owner probe has stronger proof of orphanhood; compare the owner record again before any explicitly permitted orphan reclaim.
- Atomic orphan reclaim must rename the exact revalidated lease to a distinct still-visible recovery path before publishing the new owner; never remove and reuse the same path across a delayed contender.
- Cross-process lease probes must suspend before filesystem inspection and match the exact ownership-resource suffix; observe-then-suspend leaves a release race even when the path is re-stat'ed.
- For mechanical test splits, capture the exact go test inventory, repeatable coverage command/output, and one-to-one behavior/helper placement map in a review artifact before moving tests.
- For deletion-only same-package test splits, preserve the exact package-file baseline by consolidating cohesive declarations into existing owners; keep every cross-suite fixture in the one shared helper and route boundary-sensitive construction through that helper.
- For split package tests with a preserved boundary migration fingerprint, keep the shared fixture entry point in the package helper and retain only the exact constructor callback at its reviewed source path; compare the checker fingerprint against the live base.
- When relocating a cross-suite test constructor, keep its complete import and construction path in the one shared helper and record moved migration debt with exact deletion-only service-import/construction baselines; never use source-position directives to preserve a lint fingerprint.
- Keep comments and exemption directives attached to the declaration they govern when splitting Go test files; otherwise a boundary can duplicate or orphan maintainability policy.
- Replacing a file-level size exemption with focused test files may require per-function directives; update both the source directive and the sorted exemption-budget entry together.
- When moving a test with a pkgmaintcheck exemption, move the existing budget target with the test and retain legacy file suppressions until the remaining staged split retires the source file.
- For broad functional coverage concurrency flakes, isolate the named test and rerun only the failed CI jobs before changing untouched behavior.
- When resolving long append-only progress conflicts, read merge-stage files in bounded chunks and validate the reconstructed line count so tool-output truncation cannot drop history.
- When splitting oversized same-package tests into focused files, remove stale file/maintainability exemptions and update the package file-count baseline for the intentional net increase; keep shared fixtures in one package-local helper file.
- For final PR handoff, compare the PR body byte-for-byte with `prd.json` and use the thread-aware GraphQL review audit because flat PR comments do not expose inline-thread resolution state.
- Treat repository-enforcement inventories and architecture guards as project-enabling PRD criteria when they have no runtime, API, CLI, UI, or emitted-event behavior; source scans support enforcement but are not customer-behavior evidence.
- AST scanners must expand grouped Go field declarations before indexing parameter or result types; a field-count helper can exceed the AST field-list length and panic on valid source.
- Asynchronous service characterizations should wait on observed edge/context signals rather than arbitrary `time.After` ceilings; the test context remains the failure escape hatch.
- Continuous Petri service sessions intentionally remain `IDLE` after terminal Work; characterize the dispatch and Work/progress projection as the caller-visible result and assert the live-result route's typed unavailability instead of inventing a finite session result.
- Use an authored capacity-one resource in a deterministic fixture to serialize multi-work dispatches when exact Factory Event order must be characterized; this avoids sleeps and preserves the real dispatch/resource boundary.
- Correlate batch history from both sources: request/relationship and dispatch identity live in Factory Event context, while terminal per-work state is carried by dispatch-response outputWork.
- For hosted-source characterization, route the poller through an unseeded processing input and map its external submission to a separate normal processor; this isolates source effects from the downstream dispatch executor while preserving the real runtime path.
- Runtime-opening ownership inventories should source grouped fields from runtimeopening.Dependencies, invocation-local values from ExternalEffects, and late seams from the opener; keep the reconciliation reviewer-visible in documentation instead of adding source-inventory tests.
- When synchronizing a task branch with current main, preserve the active branch's task-scoped `prd.json` and merge unrelated main history into `progress.txt`; validate the resulting PRD before staging.
- When a termination drain consumes a runtime wake signal, the engine must re-enter its tick loop directly; returning to the outer wait loses the already-consumed wake and can strand a finite run.
- Customer-facing functional tests must compose through `root.BuildProcess` and assert public process/CLI behavior; keep service-specific typed-error assertions in service-layer tests because the functional boundary check rejects direct Runtime imports.
- Functional CLI scenarios outside feature-owned workers/mock cells must replace provider execution through `serviceedges.Edges.ProviderCommandRunner`; use protocol-valid shaped Codex responses for packaged decision-envelope workflows.
- Repository-wide `pkg-boundary` traversals must prune through one root-relative, slash-normalized policy before parsing entries; adding a new `WalkDir` requires using that same decision.
- Treat disposable artifact/worktree paths as exact repository roots, not directory-name substrings, so tracked production packages with nested artifact-like names remain visible to the gate.
- Canonical packaged CLI docs live under `docs/reference/`; adding a topic also requires its authored file in `docs/reference/embed.go` and a registry entry in `pkg/transports/cli/docs/docs.go`.
- The `you run --named <factory> --help` output is the authoritative invocation contract; omitted role provider/model values resolve through operator defaults.
- Documentation for model-backed workflows should describe observable result shape and status/artifact evidence rather than byte-stable generated prose.
- Isolate both `HOME` and `USERPROFILE` when verifying packaged named Factories so stale materialized operator copies cannot mask the current catalog source.
- A classifier mock must emit the provider's valid response protocol and an exact configured route label; the default synthetic mock acceptance text is not a valid `small`/`medium`/`large` classification.
- Planner and cycle-control workflows likewise need protocol-valid structured model output to exercise generated Work, concurrency, or replanning; an empty generic mock can prove only the terminal invocation envelope and may leave orchestration Work uncreated.
- JavaScript packaged Factories that parse planner or judge output need protocol-valid provider-command evidence; the CLI `--with-mock-workers` path returns generic text for those child calls, so use focused provider-runner assertions plus a provider-free schema-bound CLI smoke and document the distinction.
- Workstation prompt templates receive `PromptData`; access a bound work identifier through `(index .Inputs N).WorkID` rather than a root `.WorkID` field.
- Prompt templates should clone native structured-result trees at the `TokenData` boundary and guard optional-result references before execution so absent upstream handoffs cannot silently fall back to payload text.
- Run-level `--quiet` cannot be combined with structured `--json` or `--output`; verify output-mode combinations against the live CLI before documenting them.
- For direct named packaged Factory invocations, bind inputs in authored workstation bodies with `${parameter}` interpolation; `.Inputs` payload can be empty even when the named CLI argument is present.
- Multi-input packaged Factories must give required parameters non-ambiguous invocation bindings; the ACP reachability guard requires each required parameter to expose a text-carrying route, while authored validation rejects routing stdin to multiple parameters.
- AGY structured-output replays require the provider's echoed `json_schema` to be JSON-equivalent to the authored request; keep historical fixture envelopes synchronized with the authored schema while preserving their recorded output evidence.
- A schema-valid structured QA `reroll` is an accepted inspection verdict; provider refusal, malformed output, and schema mismatch must fail before verdict interpretation, while provider failures may consume the bounded default retry budget.
- Packaged Factory additions that change the public catalog require synchronized authored/generated sources, catalog counts, the canonical reference topic, and CLI docs smoke markers; refresh generated output only through `make packaged-factory-catalog-generate`.
- Verify documented named-Factory commands against isolated live `you run --named ... --help`; role-pinned provider/model defaults should be documented as fixed policy when no override flags are exposed.
- When `gh pr edit` is blocked by a missing `read:project` scope, the repository-scoped `gh api --method PATCH repos/<owner>/<repo>/pulls/<number> --field body=...` fallback can update the PR body; byte-compare it with the source PRD afterward.
- Stable closed-vocabulary failure reasons must be added together to worker terminal policy, canonical event normalization, replay/session projections, worker-session diagnostics, and public transport whitelists; otherwise a valid new reason can be downgraded to `unknown` at a later boundary.
- Functional CLI tests that need worker behavior should inject `ProviderCommandRunner` through `edges.Edges` and `root.BuildProcess`; reserve built-executable subprocesses for OS-boundary facts that in-process execution cannot expose.
- Keep `--with-mock-workers` coverage inside `tests/functional/workers/mock/...`; non-workers/mock functional cells should use provider-command edge doubles and sanitized public assertions.

- Worker Session finite streams can use the injected Events `Read` contract with the first retained head as a snapshot boundary; replay-only paths must not call `Subscribe`.
- Authored OpenAPI and CLI contracts generate their Go, TypeScript, published metadata, and family-manifest consumers; update the source contract first, then run the repository generators and drift checks.
- Dashboard API-backed surfaces should consume generated OpenAPI types through a small API module and React Query server-state hook, leaving rendering components responsible for query states and pure projections.
- Service-owned HTTP adapters should receive the opened domain root at the application edge; top-level generated-route composition forwards requests without reading package artifacts or constructing service state.
- Provider-neutral output contracts belong on the workstation definition and are enforced after a successful worker response; provider process success or transport parsing alone must not satisfy a semantic report contract.
- Stream transport dispatch stays below the package maintainability threshold by delegating lifecycle-specific frame handling to focused helpers.
- Deterministic Worker Session lifecycle tests can combine the controlled workstation boundary with the real in-memory Events service to verify retained replay ordering without sleeps or cancellation.
- Detached Cobra command families should project local-only flag relationships without eagerly merging parent persistent flags; otherwise the detached storage can shadow the production root flag when the family is attached.
- Process-boundary CLI tests should build `./cmd/factory` only when proving OS-level behavior, keep the real server on a root-built process, and assign redirected stdout and diagnostics stderr separately.
- Restore package coverage thresholds with focused behavioral tests for typed edge outcomes; do not lower baseline manifests for new branches.
- Functional coverage gates execute production packages through the functional harness; unit tests alone do not restore a functional package threshold.
- Public CLI manifest additions require regenerating family metadata and the published CLI contract, then updating command input/help/tree baselines.
- Reviewed functional CLI coverage requires a sorted scenario declaration, an evidence-registry entry, a manifest mapping, and one literal `functionalevidence.Covers` marker.
- Replay completeness must be derived from a terminal lifecycle record inside the captured Events range, not from a separately synchronized terminal session state.
- When a package has a strict file-count baseline, put a small refactor in an existing focused file before adding a new helper file or changing the baseline.
- A deletion-only package-file baseline must remain unchanged during a same-package test split; fold adjacent behavior groups into existing owners and satisfy the stricter maintainability line gate before considering any baseline change.
- When broad functional coverage reports a cancellation-observation failure, rerun the failed lane and isolate the named test before changing an unrelated feature; parallel-suite failures can move between existing durability/restart tests while isolated runs pass.

- When a functional coverage job reports a missing evidence artifact, inspect the earlier test output first; the missing artifact can be a secondary symptom of the primary test failure.
- For cross-repository PRs, resolve review and CI state with an explicit owner/repository and compare local, fork-branch, and PR head SHAs; current-branch discovery may follow a different local origin.
- Package-target discovery is the stable-sorted set of directories containing non-test Go sources; adding a durable service owner requires updating the checker vocabulary and its live top-level inventory before regenerating the manifest.
- Boundary check compatibility bridges should be exact package/type exceptions with request-scoped rationale; broad service-prefix allowlists can hide new durable ownership claims.
- Compatibility guard exceptions must match the reviewed method/field shape as well as the package and type name; name-only exemptions can hide ownership-expanding contract growth.
- When an ownership burn-down ledger is corrected, regenerate it as an explicit empty array and reject retired rows so stale debt cannot silently return.
- Custom `run` flag parsing happens inside the command handler, so relationship checks for local run flags must execute after parsing; use a raw pre-initialization guard as well when the initializer itself is a side effect.
- Placement conflicts should use the same typed `ErrorResponse` contract at every validation boundary so flag ordering has one stable code, family, and actionable message.
- New functional scenarios must use an allowed product-domain noun plus subsection path such as `tests/functional/factory/batch`; transport names like `cli` and internal implementation names like `factory_runtime` are not durable domain cells.
- Ordered functional provider outcomes can use `support.NewShapedProviderCommandRunner` with queued provider-native `CommandResult` values, including a later non-zero result, while preserving the real command-runner edge and typed provider classification.
- For broad functional coverage concurrency flakes, isolate the named test and rerun only the failed CI jobs before changing untouched behavior.
- When resolving long append-only progress conflicts, read merge-stage files in bounded chunks and validate the reconstructed line count so tool-output truncation cannot drop history.
- When splitting oversized same-package tests into focused files, remove stale file/maintainability exemptions and update the package file-count baseline for the intentional net increase; keep shared fixtures in one package-local helper file.
- Functional CLI tests that need worker behavior should inject `ProviderCommandRunner` through `edges.Edges` and `root.BuildProcess`; reserve built-executable subprocesses for OS-boundary facts that in-process execution cannot expose.
- Keep `--with-mock-workers` coverage inside `tests/functional/workers/mock/...`; non-workers/mock functional cells should use provider-command edge doubles and sanitized public assertions.
- For mechanical same-package Go test splits, run before/after discovery and coverage from isolated `HEAD` and current worktrees when the original source file is being moved; this keeps package-level evidence independent of the moved-file layout.
- The production CLI process boundary should silence Cobra's implicit error text, preserve command-owned coded diagnostics with a writer marker, and wrap only unclassified failures in a constant safe fallback; direct Cobra callers can retain legacy presentation through a context switch.
- Keep cancellation compatibility explicit at the boundary: a typed returned cancellation can retain the existing `Error: context canceled` diagnostic while still preserving `errors.Is(context.Canceled)` for declared exit-status handling.
- JSON error payloads from command-owned services must route to the process diagnostic writer when the central CLI policy is active; direct service tests can continue to use their caller-owned output writer outside that policy.
- Post-open Worker Session stream failures must remain typed at the stream adapter, including output-write errors, so the process boundary can preserve a stable code and keep safe diagnostics off stdout.
- Transport boundary helpers must preserve caller-owned context semantics; nil inputs remain nil instead of being replaced by `context.Background()`.
- For ratcheted repository-wide gates with intentional baseline violations, compare the current count with an exact detached current-main checkout; a non-zero command can still be clean when the counts match.
- JavaScript live-child functional tests that inject `ProviderCommandRunner` must explicitly select a registered model provider (for example `CODEX`) so the public child route reaches the provider command adapter; return sanitized provider-native JSONL from the command edge.
- A callback or signal emitted inside a canceled external-effect call is not necessarily a quiescent lifecycle boundary; for persistence assertions, use an owned process stop/join or another public terminal observation that waits for the caller/runtime cleanup too.
- For mechanical same-package Go test splits, run before/after discovery and coverage from isolated `HEAD` and current worktrees when the original source file is being moved; this keeps package-level evidence independent of the moved-file layout.

- The production CLI process boundary should silence Cobra's implicit error text, preserve command-owned coded diagnostics with a writer marker, and wrap only unclassified failures in a constant safe fallback; direct Cobra callers can retain legacy presentation through a context switch.
- Keep cancellation compatibility explicit at the boundary: a typed returned cancellation can retain the existing `Error: context canceled` diagnostic while still preserving `errors.Is(context.Canceled)` for declared exit-status handling.
- JSON error payloads from command-owned services must route to the process diagnostic writer when the central CLI policy is active; direct service tests can continue to use their caller-owned output writer outside that policy.
- Post-open Worker Session stream failures must remain typed at the stream adapter, including output-write errors, so the process boundary can preserve a stable code and keep safe diagnostics off stdout.
- Transport boundary helpers must preserve caller-owned context semantics; nil inputs remain nil instead of being replaced by `context.Background()`.
- For ratcheted repository-wide gates with intentional baseline violations, compare the current count with an exact detached current-main checkout; a non-zero command can still be clean when the counts match.
- OS-boundary functional tests may poll an isolated durable filesystem checkpoint only when the child exposes no parent-process event or injected edge; document that reason at each polling helper and keep the bound as a failure guard.
- Git Worktree preparation treats a retained Git `*.lock` as indeterminate ownership: retry within a named bound, preserve the resource, and return its exact path with verify/remove/retry guidance.
- Git's `.git/config.lock` has no trustworthy owner or PID evidence; after a bounded retry, report the exact lock with `owner_liveness=indeterminate` and require verification before removing only that file—never auto-delete it.
- Named Factory persistence stages under the catalog root before an atomic directory move; a matching pre-existing `.<factory>.staging-*` must be preserved and surfaced as indeterminate contention with exact verify/remove/retry guidance, and pre-run Cobra failures need an explicit stderr write because the command tree silences them.
- Cross-process startup observers must treat a readable-path failure such as a transient Windows sharing violation as retryable within the existing bounded observation window, because file existence can precede release of the writer handle.
- Hard-kill startup probes should observe a durable checkpoint before listener readiness, then use explicit stdout/stderr/process-state guards; when the child exposes no acquisition event, suspend it before inspecting the exact resource and resume only on a miss so the observed lease cannot be released before the forced kill; `backendScopeID` is persisted identity, not evidence of a cross-process ownership lock.
- Packaged Factory create/replace ownership uses a deterministic `.<factory>.staging-owner` directory with atomically published PID metadata; existing valid targets skip without taking that write lease, while live or ambiguous owners are preserved and reported with bounded contention diagnostics.
- Cross-process ownership diagnostics should carry the persisted backend-scope ID through the owning service request and emit safe structured fields for resource, outcome, owner liveness, and unverified PID evidence.
- Exclusive directory reservation is a synchronization capability: inject it as a narrowly owned function port and keep generic filesystem adapters limited to inspection and ordinary materialization effects.
- Treat an absent PID as indeterminate unless the owner probe has stronger proof of orphanhood; compare the owner record again before any explicitly permitted orphan reclaim.
- Atomic orphan reclaim must rename the exact revalidated lease to a distinct still-visible recovery path before publishing the new owner; never remove and reuse the same path across a delayed contender.
- Cross-process lease probes must suspend before filesystem inspection and match the exact ownership-resource suffix; observe-then-suspend leaves a release race even when the path is re-stat'ed.
- Native Bun UI unit owners use the `.bun.unit.test.ts` suffix and `bun:test`; the shared `bunVi` adapter is the compatibility seam for global stubs/spies while the lane contract excludes those owners from Vitest.
- Aggregate UI-unit evidence must reconcile both runner outputs to the unchanged full workload; the leased Bun cohort can coexist with pre-existing Bun owners and the remaining Vitest files.
- The component wrapper has a separate wall-clock budget from assertion correctness; report passing sublane counts and host-specific budget overruns separately rather than treating warnings as test failures.

- Keep synchronous and asynchronous entry points on one domain-owned
  preparation/supervision path; parameterize lifecycle ownership and caller
  wait semantics instead of duplicating reservation, opening, and admission
  logic.
- When a package is constrained to one named root service interface, keep a
  peer lifecycle hook structural and optional; let composition detect the
  narrow method without widening every test double or alternate adapter.
- Shutdown should close new async admission before closing the Workers pool,
  route admitted server-owned work through its existing cancellation boundary,
  and join both terminal publication and the supervision driver's final tail.
- Functional HTTP disconnect evidence should keep the request unread on a raw connection, gate a real provider command runner, and observe terminal state through an independent public SSE client; canceling a context after a completed response is not a disconnect proof.
- When hosted Go coverage reports a per-package floor regression, use its exact package diagnostic to target deterministic branch tests and verify the package floor independently before rerunning the broader lane.
- Worker Session async idempotency should claim the caller key and stable session identity in one lock transaction before publication or dispatch, then replay a detached tuple and stored outcome.
- Root-built functional Worker tests can make mock script execution deterministic by injecting the same gated command runner into both provider and script command edges; the mock-worker mode may route a model-backed workstation through either edge.
- Server-owned async acceptance needs a distinct admission signal from publication and terminal completion; record pre-admission control intent before releasing that signal so a racing cancel cannot be acknowledged as accepted.
## 2026-08-09 13:12 - wo-d1-packaged-factories-docs-001
- What was implemented
  - Established the operator-facing discovery workflow and common seven-part entry contract in `docs/reference/packaged-factories.md`.
  - Added family-based catalog navigation, materialization/default-provider guidance, live-help evidence, and model-output stability guidance.
  - Registered `packaged-factories` as a real `you docs packaged-factories` topic and updated the packaged docs indexes plus observable CLI smoke expectations.
  - Verified the current-source binary with `you factory list`, `you run --named @you/goal --help`, and `you docs packaged-factories` (using a freshly built workspace binary).
- Files changed
  - `docs/reference/packaged-factories.md`
  - `docs/reference/embed.go`
  - `docs/reference/README.md`
  - `docs/README.md`
  - `pkg/transports/cli/docs/docs.go`
  - `pkg/transports/cli/root_docs_test.go`
  - `tests/functional/smoke/cli_docs_smoke_test.go`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The existing Markdown page was present but not embedded or registered, so `you docs packaged-factories` was previously rejected; keep the source file, embed list, registry, indexes, and user-visible smoke expectations synchronized.
  - The live catalog contains fifteen `@you/*` entries plus any local Factory names, and its directory column is operator-specific; use exact Factory names for portable documentation.
  - The current help contract explicitly prints Factory-defined request binding and role-specific flags, including the phrase that omitted provider/model values use operator defaults.
## 2026-08-09 13:16 - wo-d1-packaged-factories-docs-001 verification follow-up
- What was implemented
  - Installed the repository-pinned UI dependencies that were missing from the checkout so the broad verification tier could run.
- Files changed
  - No additional tracked files.
- Verification
  - Initial `make verify-fast` stopped at the environment dependency boundary (`@types/bun` missing and the globally selected TypeScript reported `TS5101`).
  - After `bun install --frozen-lockfile` in `ui/`, `make verify-fast` passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, and the short Go suite.
- **Learnings for future iterations:**
- This checkout may have `ui/node_modules` without all lockfile dependencies; run the pinned Bun install before treating a typecheck failure as a source regression.
---
## 2026-08-10 15:08 -07:00 - dwro-s1-remote-placement-flags current-main test-split reconciliation
- What was implemented
  - Reconciled the branch with current `origin/main` commit `78584efb5`, retaining main's focused root/run test split while preserving the S1 remote adapter and its behavioral coverage.
  - Moved the S1 root placement assertions into `root_run_remote_placement_test.go`, preserved the task-owned S1 PRD, and resolved the intentional package-count baseline to 31 CLI files and 34 run files.
- Files changed
  - `pkg/transports/cli/root_run_remote_placement_test.go`
  - `pkg/transports/cli/root_run_test.go` (removed after its tests were split)
  - `pkg/transports/cli/run/run_invocation_test.go` (removed by current-main split)
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused remote-placement tests, complete `./pkg/transports/cli`, and complete `./pkg/transports/cli/run` suites — passed.
  - `make pkg-file-count` and staged/working-tree `git diff --check` — passed.
- **Learnings for future iterations:**
  - When current main mechanically deletes an oversized test file, carry task-specific tests into a focused same-package file before resolving the deletion; this preserves behavior without resurrecting the old contention hotspot.
---

## 2026-08-10 16:49 -07:00 - thr-worktree-build-cache-002 verification follow-up
- What was implemented
  - Completed post-merge verification for the corrected cache policy and current-main synchronization.
- Files changed
  - `progress.txt`
- Verification
  - `make test` passed.
  - `make typecheck` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - Dry-run `make build` and `make install` omit `-a`; explicit `GO_BUILD_FLAGS=-a` retains the forced-rebuild control.
  - The working tree remains clean before push, and the PRD still has both stories marked `passes:true`.
- **Learnings for future iterations:**
  - After a base-sync merge, rerun the repository gates locally before pushing so the new PR head has direct evidence in addition to the earlier green CI run.
---

## [2026-08-10 18:12 -07:00] - thr-split-fake-service-runtime-tests review follow-up
- What was implemented
  - Addressed the two blocking PR findings: restored the exact deletion-only execution package file-count baseline from 39 to 28, and restored the flagged test/fixture declarations byte-for-byte apart from relocation.
  - Mechanically consolidated adjacent same-package test owners while keeping the focused construction, dispatch, lifecycle, replay, runtime start/result/persistence/control/checkpoint/validation-event groups and the external-package boundary tests within the existing 28-file budget.
  - Updated the story notes in `prd.json` so the PR description records the final file budget, inventory hash, coverage, and review-follow-up evidence; all five stories remain `passes:true`.
- Files changed
  - `pkg/services/factory_sessions/internal/execution/` focused test owners and mechanical source-file retirements
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Sorted before/after `go test -list '.*' ./pkg/services/factory_sessions/internal/execution/...` inventory — 269 entries on both sides, SHA-256 `09876A7E761EAB43E48DD3729153FCCAE52802F37CC95B424B706AAFA3869B51` on both sides, diff empty.
  - Exact `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` — unchanged at 79.6% (execution), 80.0% (fixtures), 83.1% (recordingreplay), and 89.8% (runtimepersist).
  - Focused package suite, full `make test` (311.9s), `go vet`, `make typecheck`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` — passed.
  - On-disk execution package count is exactly 28; every resulting hand-maintained Go file is below the 1,000-line maintainability threshold and the PRD’s 1,200-line cap.
  - Review-fix commit: `5cde8e22b` (`test(factory-sessions): preserve split file budget`).
- **Learnings for future iterations:**
  - Treat the package file-count baseline as a deletion-only ratchet even when a split is mechanically correct; use existing responsibility-aligned test owners and verify both the 1,000-line maintainability gate and the broader 1,200-line task cap.
  - When review identifies whitespace drift in a move-only split, compare the named declarations directly against the unmodified source and restore the exact text before rerunning inventory and coverage.
---

## [2026-08-10 19:25 -07:00] - e2-structured-worker-results CI flake follow-up
- What was implemented
  - Made no product or test-code changes. Investigated the final-head CI failure and reran only the failed Backend Functional Coverage job.
  - Confirmed the first failure was an unchanged `pkg/transports/cli` functional-coverage measurement at 72.818% versus the 72.98% manifest floor; the targeted rerun then exposed the existing `TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle` shutdown race in `tests/functional/workers/mock`.
- Files changed
  - `progress.txt`
- Verification
  - The isolated `continuous_idle` subtest passed 20 consecutive local runs; no feature diff touches its package.
  - Failed-job rerun Backend Functional Coverage job `93658010711` passed, including the pinned real ACP evidence artifact; Verification Policy job `93658764710` passed.
  - The branch remains at the pushed head `868990d2b4a07270cce311ec715c98bf10eacb7e`, with all four PRD stories marked `passes:true` and the PR body still byte-identical to `prd.json`.
- **Learnings for future iterations:**
  - When functional coverage first reports a package-floor variance and a retry names an unrelated cancellation/shutdown race, preserve the feature diff, stress the exact subtest locally, and rerun only the failed job after clean characterization.
---

## 2026-08-10 18:42 -07:00 - dwro-s1-remote-placement-flags current-main synchronization follow-up
- What was implemented
  - Fetched the live `origin/main` head `0b9771fe8` after the PR base advanced beyond the previously synchronized `e6797fd5`.
  - Merged the current base, preserved the S1-owned `prd.json`, and retained both branches' append-only progress history while resolving the shared bookkeeping conflicts.
- Files changed
  - Current-main merge files.
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all four S1 stories marked `passes:true`.
  - `progress.txt` and `prd.json` contain no merge conflict markers; the merge index is resolved.
  - `git diff --cached --check` passed before the merge commit.
- **Learnings for future iterations:**
  - A clean merge against a stale remote-tracking base is not sufficient; compare the live hosting ref, fetch it, and resolve shared PRD/progress files again before pushing the replacement head.
---

## 2026-08-10 22:27 -07:00 - dwro-s2-server-owned-start-001
- What was implemented
  - Added the Worker Sessions `Start` operation for already-resolved Workers executions.
  - Gated successful return on the opening SESSION/STARTED record being retained-readable and subscribable, followed by the Workers admission callback.
  - Reused the existing retry, control, provider-association, and exactly-once terminal supervision path; asynchronous attempts run under a detached server-owned context while synchronous `InvokeSession` remains unchanged at its caller-facing boundary.
  - Recorded pre-admission cancellation intent so a racing Cancel cannot be acknowledged as accepted, and added stable start/readiness/admission error classifications plus safe structured outcome logs.
- Files changed
  - `pkg/services/worker_sessions/contracts.go`
  - `pkg/services/worker_sessions/doc.go`
  - `pkg/services/worker_sessions/internal/service/service.go`
  - `pkg/services/worker_sessions/internal/service/control.go`
  - `pkg/services/worker_sessions/internal/service/invoke_session.go`
  - `pkg/services/worker_sessions/internal/service/start_test.go`
  - `pkg/services/worker_sessions/internal/service/start_side_effects_test.go`
  - `pkg/services/worker_sessions/internal/service/fakes_test.go`
  - Factory Runtime test service doubles updated for the expanded Worker Sessions contract.
  - `prd.json`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` passed.
  - `go test -race ./pkg/services/worker_sessions/... -count=1` passed before the final test relocation; focused race tests passed after relocation.
  - Affected Factory Runtime package tests and compile-only coverage passed; `go vet` passed.
  - `make backend-size` and `make pkg-maint` passed after keeping touched files within repository limits; `make pkg-file-count` passed.
  - `make lint` remains blocked by the environment's missing `knip` binary in the UI deadcode target; the repository-wide package-boundary target reports pre-existing baseline violations unrelated to this change.
- **Learnings for future iterations:**
  - Keep the async start acceptance signal distinct from publication and terminal completion, and reserve the admission signal for starts with no queued pre-admission control.
  - The Worker Sessions package is already at its file-count budget; move focused implementation or tests into existing responsibility-aligned files and rerun backend-size/pkg-maint after additions.
---

## [2026-08-10 23:06 -07:00] - dwro-s2-server-owned-start-002
- What was implemented
  - Made asynchronous `StartRequest` distinct from synchronous `InvokeSessionRequest` and required a non-empty caller `RequestID`.
  - Added an atomic Worker Sessions replay registry that stores a detached normalized start tuple and original acceptance/failure outcome before any opening publication or Workers admission.
  - Same-key identical starts now converge before, during, and after execution; tuple conflicts return a typed idempotency error; invalid requests do not consume keys; deterministic pre-admission failures replay without replacement work.
  - Added deterministic replay, conflict, failure-replay, and concurrent-submission tests, plus the Factory Runtime test-double conversions required by the distinct contract.
- Files changed
  - `pkg/services/worker_sessions/contracts.go`
  - `pkg/services/worker_sessions/internal/service/control.go`
  - `pkg/services/worker_sessions/internal/service/service.go`
  - `pkg/services/worker_sessions/internal/service/fakes_test.go`
  - `pkg/services/worker_sessions/internal/service/start_test.go`
  - `pkg/services/worker_sessions/start_test.go`
  - Factory Runtime test doubles under `pkg/services/factory_runtime/`
  - `prd.json`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` passed.
  - `go test -race ./pkg/services/worker_sessions/internal/service -count=1` passed.
  - Affected Factory Runtime tests, full `make test`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `go vet ./...` passed.
  - `make typecheck` remains environment-blocked by missing UI Bun type definitions; no UI files changed.
- **Learnings for future iterations:**
  - Keep idempotency replay state in the Worker Sessions owner and compare cloned canonical values; reserve the caller key and session identity in one mutex transaction before downstream effects.
  - When a strict package file budget prevents a focused helper file, place small lifecycle-coordination helpers in an existing responsibility-aligned owner and rerun both backend-size and package-file-count gates.
---

## 2026-08-10 23:54 -07:00 - dwro-s2-server-owned-start-003
- What was implemented
  - Exposed the Worker Sessions asynchronous start contract through typed `POST /worker-sessions`.
  - Added authored request/response/retry/resolved-execution OpenAPI schemas, stable conflict/unavailable responses, and safe error codes; regenerated the bundled OpenAPI, Go server/client contracts, and TypeScript client.
  - Added one strict HTTP decode-and-map boundary that delegates only to Worker Sessions `Start`, returns `202 Accepted` after the reservation, Events readiness, and Workers admission barrier, and returns the stable Worker Session identity plus deterministic Events topic without waiting for terminal output.
  - Added stable 400/409/503 mapping for malformed input, bounded retry violations, idempotency/session conflicts, opening/event readiness failures, and admission failures. Same-key replay and duplicate suppression remain owned by the Worker Sessions service.
- Files changed
  - `api/openapi-main.yaml` and authored Worker Session start schemas/responses under `api/components/`.
  - `api/openapi.yaml`, `packages/api/generated/manifest.json`, `packages/api/generated/openapi/openapi.yaml`, `pkg/transports/http/generated/server.gen.go`, `pkg/transports/http/client/client.gen.go`, `ui/src/api/generated/openapi.ts`, and `ui/packages/client/src/generated/openapi.ts`.
  - `pkg/services/worker_sessions/transports/http/adapter.go`, `mapping.go`, `handler.go`, and `handler_test.go`.
  - `pkg/transports/http/server.go` and `pkg/transports/http/application/handler.go`.
  - `prd.json`.
- Verification
  - `make generate-api` passed.
  - `make interfaces-all` passed, including packaged API and UI client artifacts.
  - `make api-smoke` passed, including generated-artifact drift, HTTP contract alignment, and provider parity.
  - `make test` passed the full short unit lane after the packaged contract artifacts were regenerated; `TestMockWorkersSchema_StaleStagingDetectedByContractCheck` now passes with the complete generated artifact set.
  - Affected Worker Sessions, HTTP, contract, mapping, and CLI Go tests passed; focused `go vet` passed.
  - `git diff --check` passed. Repository `make typecheck` remains blocked by pre-existing UI test-helper assertion typings; a full unscoped Go test run exceeded the local five-minute command budget, while all affected suites passed.
- **Learnings for future iterations:**
  - Add `x-enum-varnames` to new OpenAPI enums when their values overlap existing generated enum constants; otherwise code generation can rename unrelated public identifiers.
  - Keep typed transport schemas aligned with the Workers request shape, but preserve Worker Sessions ownership of admission, idempotency, and stable failure classification in the service boundary.
---

## [2026-08-11 00:45 -07:00] - dwro-s2-server-owned-start-004
- What was implemented
  - Replaced request-derived asynchronous execution lifetime with a Worker Sessions-owned process lifecycle context and made the caller context cancel only the admission wait; a canceled caller leaves the request-ID replay authoritative until the server-owned attempt accepts or fails.
  - Added a server-owned supervisor driver completion barrier, shutdown admission gate, idempotent lifecycle stop, and explicit shutdown termination through the existing Workers cancellation boundary. Terminal publication remains live until callbacks and driver tails have joined, then the lifecycle context is canceled.
  - Wired Factory Runtime shutdown to stop server-owned asynchronous Worker Sessions before the dispatch planner and Workers pool, while leaving synchronous InvokeSession supervisors on their existing control path. Added stable HTTP handling for caller cancellation and server-stopping rejection.
  - Added deterministic disconnect-before/after-admission, replay, exact terminal-event, shutdown-join, and runtime shutdown-order assertions.
- Files changed
  - `pkg/services/worker_sessions/internal/service/service.go`, `publish_record.go`, `control.go`, and `invoke_session.go`
  - `pkg/services/worker_sessions/contracts.go`, `doc.go`, and HTTP handler tests/mapping
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/worker_pool.go` and `factory_modes_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make test` passed.
  - `make typecheck` passed.
  - `go test -race ./pkg/services/worker_sessions/...` passed.
  - Affected Factory Runtime tests, `go vet ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/...`, `make backend-size pkg-maint pkg-file-count pkg-structure`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - Keep optional lifecycle capabilities structural when the service root is constrained to one named interface; composition can type-assert the narrow `Stop(context.Context) error` method without widening every fake.
  - A canceled admission waiter must not finalize the request replay; let a detached owner finish the state machine so a retry sees the authoritative accepted or failed result.
  - Runtime shutdown must target only server-owned asynchronous supervisors; synchronous InvokeSession work is still joined by its existing Factory Runtime/Workers control path.
---

## [2026-08-11 01:25 -07:00] - dwro-s2-server-owned-start-004
- What was implemented
  - Added root-built functional HTTP coverage for an admitted direct Worker surviving cancellation of the submitting request and for server shutdown canceling and joining the admitted Worker.
  - The process-boundary tests use a deterministic injected command gate, assert same-requestId replay does not duplicate the Worker command, and verify shutdown reaches the command context without arbitrary sleeps.
- Files changed
  - `tests/functional/workers/transports/http/worker_sessions_lifecycle_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/runtime_api -run 'TestWorkerSessionHTTP(DisconnectKeepsAdmittedWorkerAlive|ShutdownJoinsAdmittedWorker)$' -count=1 -v` passed.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - The root functional harness routes mock model-worker script execution through either the provider or script command edge depending on the assembled worker; inject a deterministic runner into both when the test needs to observe the actual process boundary.
---

## [2026-08-11 02:05 -07:00] - dwro-s2-server-owned-start-004
- What was implemented
  - Moved the process-boundary Worker Session regression from the deprecated `runtime_api` test owner to `tests/functional/workers/transports/http`, the durable Worker/HTTP domain required by the package-structure gate.
- Files changed
  - `tests/functional/workers/transports/http/worker_sessions_lifecycle_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/transports/http -run 'TestWorkerSessionHTTP(DisconnectKeepsAdmittedWorkerAlive|ShutdownJoinsAdmittedWorker)$' -count=1 -v` passed.
  - The same focused tests passed under `go test -race`.
  - `go vet ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/... ./tests/functional/workers/transports/http` passed.
  - `make typecheck`, `make ui-lint`, `make backend-size pkg-maint pkg-file-count pkg-structure`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - New functional scenarios must live under `tests/functional/<durable-domain>/<subsection>/...`; the package-structure checker rejects new files under the historical `runtime_api` aggregate even when their assertions are valid.
---

## [2026-08-11 01:45 -07:00] - dwro-s2-server-owned-start-005
- What was implemented
  - Consolidated synchronous `InvokeSession` and asynchronous `Start` onto one Worker Sessions preparation path covering reservation, STARTING transition, observation capture, opening publication, supervision registration, retry budget, admission callback, control races, terminal classification, and terminal publication.
  - Preserved synchronous callers' request-ID-free session conflict rules and terminal return behavior while keeping asynchronous Starts server-owned and admission-gated.
  - Added deterministic compatibility tests proving synchronous terminal waiting versus asynchronous admission return and matching pre-admission failure classification and event history.
- Files changed
  - `pkg/services/worker_sessions/contracts.go`
  - `pkg/services/worker_sessions/internal/service/invoke_session.go`
  - `pkg/services/worker_sessions/internal/service/publish_record.go`
  - `pkg/services/worker_sessions/internal/service/service.go`
  - `pkg/services/worker_sessions/internal/service/start_test.go`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` passed.
  - `go test -race ./pkg/services/worker_sessions/internal/service -count=1` passed.
  - Affected Factory Runtime and HTTP transport suites, affected `go vet`, `make typecheck`, `make ui-lint`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` passed.
  - The aggregate `make lint` command was bounded at five minutes while unrelated concurrent unit-lane processes were active; focused lint/static targets and affected vet checks passed.
- **Learnings for future iterations:**
  - Shared lifecycle behavior should be factored below the public entry points, with mode options limited to lifecycle ownership, readiness verification, and caller-facing wait/error semantics.
  - Keep the Worker Sessions package at its 15-file limit by placing small lifecycle coordination beside the existing publication/lifecycle implementation rather than adding another package file.

## 2026-08-10 23:48 -07:00 - provider-capability-contract current-main review follow-up
- What was implemented
  - Addressed PR #1874's blocking review feedback by merging the latest fetched `origin/main` head `85619c087`.
  - Kept the provider-specific `prd.json` authoritative and reconstructed the append-only `progress.txt` from the complete merge stages, preserving both branch histories and reusable Codebase Patterns.
  - No product or generated-file conflict required a source change; the merge conflict was limited to task bookkeeping.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three `provider-capability-contract` stories marked `passes:true`.
  - `progress.txt` contains no merge markers and retains both provider and incoming mainline histories.
  - `git diff --check` passes.
- **Learnings for future iterations:**
  - For a moving-base conflict in long append-only logs, resolve from complete bounded Git-stage slices, keep the active task PRD authoritative, and validate both histories before staging.
---
---

## [2026-08-11 02:21 -07:00] - dwro-s2-server-owned-start-005 CI follow-up
- What was implemented
  - Reproduced the hosted API candidate failure on PR #1880: `packages/api/generated/manifest.json` still carried the pre-merge OpenAPI hash after the final current-main synchronization.
  - Regenerated the approved contract artifacts so `generated.openapi.openapi` records the hash of the merged `packages/api/generated/openapi/openapi.yaml`.
- Files changed
  - `packages/api/generated/manifest.json`
  - `progress.txt`
- Verification
  - `make contracts-generate` passed and updated only the stale OpenAPI manifest hashes.
  - The original hosted failure was `manifest artifact hash mismatch: generated.openapi.openapi`; the replacement head will rerun the API candidate and dependent checks.
- **Learnings for future iterations:**
  - After a current-main merge that touches generated OpenAPI artifacts, run `make contracts-generate` in addition to `make generate-api`; the API package consumer verifies manifest hashes against the generated artifact bytes.
---

## [2026-08-11 02:32:32 -07:00] - dwro-s2-server-owned-start-005 contract-manifest follow-up
- What was implemented
  - Added the new `rest/startWorkerSession` operation to the reviewed functional-scenario projection and bound it to the existing HTTP lifecycle test that observes admission, disconnect survival, idempotent replay, and single execution.
  - Added the matching runtime evidence declaration so the contract drift gate can verify the customer-boundary test directly.
- Files changed
  - `contracts/functional-scenarios.json`
  - `contracts/functional-scenario-evidence.json`
  - `internal/functionalscenarios/manifest.go`
  - `tests/functional/workers/transports/http/worker_sessions_lifecycle_test.go`
- Verification
  - Focused HTTP functional tests passed.
  - `go run ./cmd/functionalscenarioproject -check contracts/functional-scenarios.json` passed with 118 reviewed scenarios current.
  - `make contracts-check` passed.
  - Affected Worker Sessions and Factory Runtime package tests passed.
- **Learnings for future iterations:**
  - New OpenAPI operations must receive both a reviewed manifest mapping and a direct `functionalevidence.Covers` declaration in a qualifying functional test; `make contracts-check` enforces both sides.
---

## [2026-08-11 03:24:19 -07:00] - dwro-s2-server-owned-start-005 hosted coverage follow-up
- What was implemented
  - Diagnosed PR #1880's Backend Unit Coverage failure: `pkg/services/worker_sessions/internal/service` was at 96.8156% against its 98.72% checked-in floor.
  - Added focused deterministic internal tests for lifecycle fallbacks, readiness-reader failures, pre-admission classification, and observation edge branches; kept all five PRD stories marked `passes:true`.
- Files changed
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused Worker Sessions internal-service coverage is 98.8%; Worker Sessions package tests and race tests pass.
  - `go vet ./pkg/services/worker_sessions/...`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` pass.
  - The full Windows `make test-unit-coverage` wrapper remains environment-blocked by the expanded Go command line (`go.exe: The filename or extension is too long`); the hosted failure itself was the package-floor regression, and the targeted package now clears that floor.
- **Learnings for future iterations:**
  - Hosted coverage logs emit an exact package-floor diagnostic; use it to target branch tests and independently verify the affected package before rerunning the broader lane.
---

## [2026-08-11 04:02:49 -07:00] - dwro-s2-server-owned-start-004 PR feedback follow-up
- What was implemented
  - Replaced the Story 004 functional fixture's prohibited Mock Workers setup with the real model-worker configuration and an injected, gated Codex `ProviderCommandRunner` that emits sanitized provider-native JSONL.
  - Reworked the disconnect proof to write the POST request over a raw TCP connection, wait for deterministic provider-edge entry, close the unread submitting connection before reading its response, and verify the accepted execution through an independent public Worker Session SSE stream.
  - Added assertions for one `STARTED` lifecycle record, one `COMPLETED` lifecycle terminal record, same-key 202 replay, and exactly one provider command call; documented the bounded signal ceilings as failure diagnostics rather than synchronization polling.
- Files changed
  - `tests/functional/workers/transports/http/worker_sessions_lifecycle_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/transports/http -count=1 -timeout 600s` — passed.
  - `go test -race ./tests/functional/workers/transports/http -run '^TestWorkerSessionHTTP(DisconnectKeepsAdmittedWorkerAlive|ShutdownJoinsAdmittedWorker)$' -count=1 -timeout 600s` — passed.
  - `go test ./pkg/services/worker_sessions/... -count=1 -timeout 600s` — passed.
  - `go test ./pkg/services/factory_runtime/... -count=1 -timeout 600s` — passed.
  - `go test ./pkg/services/worker_sessions/transports/http -count=1 -timeout 600s` and `git diff --check` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure`, affected-package `go vet`, `make typecheck`, and `make ui-lint` — passed. Repository `make test` was attempted but exceeded the 650-second Windows command ceiling with no failure output while many other worktree verification processes were running; the targeted suites above were terminal-green.
- **Learnings for future iterations:**
  - A true HTTP disconnect test must close an unread transport connection while the provider is held at a deterministic edge; a context cancellation after `http.Client.Do` returns only tests normal request completion.
---

## [2026-08-11 02:11:21 -07:00] - cli-error-classification-and-run-output review follow-up (mainline history)
- What was implemented
  - Addressed the post-merge blocking review comment by removing `--with-mock-workers` from the three changed invalid-input functional scenarios: invalid JSON, malformed key=value, and positional overflow.
  - Kept each scenario on the canonical `root.BuildProcess` / `Process.Execute` path with an injected `edges.Edges.ProviderCommandRunner`, making the zero-provider-call assertions authoritative for the BAD_REQUEST boundary.
- Files changed
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/parameters -count=1 -timeout 600s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Functional validation scenarios that assert absence of provider side effects must not configure Mock Workers; inject the exact provider command-runner edge and assert its observations instead.
---

## [2026-08-11 02:56:56 -07:00] - cli-error-classification-and-run-output reconciliation (mainline history)
- What was implemented
  - Reconciled PR #1879 with live `origin/main` after the blocking review comment reported a real merge conflict.
  - Preserved that task's authoritative `prd.json`, accepted incoming mainline lint-tooling and functional-test changes, and confirmed that generated CLI artifacts did not require hand-merging or unnecessary regeneration.
- Files changed
  - Current-main lint checker/lane and maintenance-gate files carried by the synchronization merge.
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused CLI parameter, factory visualization, and functional output suites passed.
  - `make typecheck`, `make cli-manifest-check`, `make backend-size pkg-maint pkg-file-count pkg-structure vet`, PRD all-pass, merge-marker, and `git diff --check` audits passed.
- **Learnings for future iterations:**
  - A shared PRD conflict can contain a different task's metadata after main advances; resolve it by retaining the active branch's complete PRD and follow-up note, then verify story flags and JSON before committing.
  - A fresh replacement head needs new hosted CI even when the pre-conflict head was green; local focused gates establish the merge result while GitHub validates the pushed ancestry.
---

## [2026-08-11 03:16:31 -07:00] - cli-error-classification-and-run-output hosted CI follow-up (mainline history)
- What was implemented
  - Investigated a replacement-head Backend Functional Coverage failure in an untouched `tests/functional/workers/mock` scenario; repeated the exact test locally and reran the failed hosted lane without changing unrelated product code.
- Files changed
  - `progress.txt`
- Verification
  - The named test passed ten local repetitions; the hosted Backend Functional Coverage and dependent Verification Policy reruns passed.
- **Learnings for future iterations:**
  - When hosted functional coverage names an untouched fixture, repeat that exact test locally before widening the diff; a clean characterization supports rerunning the failed lane and dependent policy job without unrelated fixes.
---

## [2026-08-11 03:28:29 -07:00] - cli-error-classification-and-run-output final PR handoff audit (mainline history)
- What was implemented
  - Audited PR #1879 after its stories reached `passes:true`, confirming current-main ancestry, mergeability, terminal passing checks, synchronized PR body, and zero inline review threads.
- Files changed
  - `progress.txt`
- Verification
  - The PRD parsed with all stories passing, no conflict markers remained, and the replacement-head checks and thread-aware review audit were green.
- **Learnings for future iterations:**
  - Treat a prior blocking comment as addressed only after its requested base synchronization is present, the PR is cleanly mergeable, and replacement-head checks are terminal-success; keep the PR body byte-for-byte synchronized with `prd.json` when required by the task contract.
---

## [2026-08-11 04:20:59 -07:00] - dwro-s2-server-owned-start latest-main reconciliation
- What was implemented
  - Merged live `origin/main` at `012e98e0fbd1b6fcac106d90b1f3d335b8a3dce6` after PR #1880 reported a moving-base conflict.
  - Preserved this task's authoritative five-story `prd.json`, unioned the incoming append-only progress history and reusable reconciliation pattern, and retained the incoming CLI parameter test changes from main.
- Files changed
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all five stories marked `passes:true`; `progress.txt` and `prd.json` contain no merge markers.
  - The live base is now included in the branch ancestry; focused Story 004 and affected package gates must be rerun on this merged head before the replacement push.
- **Learnings for future iterations:**
  - Resolve moving-base conflicts by keeping the active task PRD authoritative, unioning complete append-only progress histories, and rerunning the task-specific gates on the exact merged ancestry.
---

## [2026-08-11 04:23:37 -07:00] - dwro-s2-server-owned-start merged-head verification
- What was implemented
  - Revalidated the Story 004 disconnect/shutdown evidence and affected runtime packages after merging current `origin/main`; no additional product changes were needed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/transports/http -count=1 -timeout 600s` — passed.
  - `go test -race ./tests/functional/workers/transports/http -run '^TestWorkerSessionHTTP(DisconnectKeepsAdmittedWorkerAlive|ShutdownJoinsAdmittedWorker)$' -count=1 -timeout 600s` — passed.
  - `go test ./pkg/services/worker_sessions/... -count=1 -timeout 600s`, `go test ./pkg/services/factory_runtime/... -count=1 -timeout 600s`, and `go test ./tests/functional/transport/cli/parameters -count=1 -timeout 600s` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure`, affected-package `go vet`, `make typecheck`, and `git diff --check origin/main...HEAD` — passed.
- **Learnings for future iterations:**
  - After a moving-base merge, rerun the boundary functional and race evidence on the exact merged ancestry before pushing; prior-head green results do not prove the replacement head.
- This checkout may have `ui/node_modules` without all lockfile dependencies; run the pinned Bun install before treating a typecheck failure as a source regression.



## 2026-08-10 04:32 -07:00 - sac-s0-2-pkg-boundary-ignores-001
- What was implemented
  - Centralized repository-root-relative pruning for `.artifacts`, `.artifacts/bootstrap/worktrees`, generated transient worktrees, and the supported root worktree locations.
  - Applied the same skip decision before parsing in every `pkg-boundary` production traversal, including secondary service, transport, provider, edge, test-behavior, Petri, and production-default scans.
  - Preserved the existing Git metadata, dependency/vendor, node-module, and testdata exclusions without matching artifact-like or worktree-like names inside tracked production packages.
  - Expanded the normal `run` regression to use malformed stale `pkg/wire` imports beneath artifact/bootstrap/generated transient layouts and existing ignored roots, proving those files are pruned before parsing.
- Files changed
  - `cmd/pkgboundarycheck/main.go`
  - `cmd/pkgboundarycheck/main_test.go`
  - `cmd/pkgboundarycheck/constructed_service_edges.go`
  - `cmd/pkgboundarycheck/functional_process_edges.go`
  - `cmd/pkgboundarycheck/initializer_behavior.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/production_defaults.go`
  - `cmd/pkgboundarycheck/support_service_imports.go`
  - `cmd/pkgboundarycheck/transport_behavior.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run '^TestRunIgnoresRepositoryPolicyNonSourceRoots$' -count=1` — passed.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `make pkg-boundary` — reaches the checker and reports only the unchanged 176-violation baseline on current `main`; no new violation was introduced by this story.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checker has many independent `WalkDir` callbacks; updating only the primary repository scan leaves secondary scanners able to rediscover ignored files.
  - A malformed ignored fixture is useful behavioral evidence for prune-before-parse: a valid stale import alone may not exercise every parser path.
  - The next story should add the paired production `pkg` failure fixture and verify the `make pkg-boundary` subprocess path while keeping nested artifact-like directory names visible.
---

## 2026-08-09 13:17 - wo-d1-packaged-factories-docs-001 documentation routing follow-up
- What was implemented
  - Corrected the root README's packaged-Factory count from fourteen to fifteen while keeping the root page as routing/quick-start material.
- Files changed
  - `README.md`
- Verification
  - `make readme-check` — passed.
- **Learnings for future iterations:**
  - Keep the root count aligned with the canonical fifteen-entry catalog, but continue placing detailed Factory guidance in `docs/reference/packaged-factories.md`.
- For broad functional coverage concurrency flakes, isolate the named test and rerun only the failed CI jobs before changing untouched behavior.
- When resolving long append-only progress conflicts, read merge-stage files in bounded chunks and validate the reconstructed line count so tool-output truncation cannot drop history.
- Functional CLI tests that need worker behavior should inject `ProviderCommandRunner` through `edges.Edges` and `root.BuildProcess`; reserve built-executable subprocesses for OS-boundary facts that in-process execution cannot expose.
- Keep `--with-mock-workers` coverage inside `tests/functional/workers/mock/...`; non-workers/mock functional cells should use provider-command edge doubles and sanitized public assertions.
- The production CLI process boundary should silence Cobra's implicit error text, preserve command-owned coded diagnostics with a writer marker, and wrap only unclassified failures in a constant safe fallback; direct Cobra callers can retain legacy presentation through a context switch.
- Keep cancellation compatibility explicit at the boundary: a typed returned cancellation can retain the existing `Error: context canceled` diagnostic while still preserving `errors.Is(context.Canceled)` for declared exit-status handling.
- JSON error payloads from command-owned services must route to the process diagnostic writer when the central CLI policy is active; direct service tests can continue to use their caller-owned output writer outside that policy.
- Post-open Worker Session stream failures must remain typed at the stream adapter, including output-write errors, so the process boundary can preserve a stable code and keep safe diagnostics off stdout.
- Transport boundary helpers must preserve caller-owned context semantics; nil inputs remain nil instead of being replaced by `context.Background()`.
- For ratcheted repository-wide gates with intentional baseline violations, compare the current count with an exact detached current-main checkout; a non-zero command can still be clean when the counts match.
---
## 2026-08-09 13:20 - wo-d1-packaged-factories-docs-001 lint follow-up
- What was implemented
  - No source change; checked the broad repository lint tier against the completed story.
- Files changed
  - No additional tracked files.
- Verification
  - `make lint` completed the UI lint/dead-code checks, `go vet`, backend-size, pkg-maint, pkg-file-count, and related checks, then stopped at the existing `pkg-boundary` baseline with 176 violations. The reported violations are in pre-existing service-boundary/Petri migration surfaces and none are in this story's changed files.
- **Learnings for future iterations:**
  - Treat the current `pkg-boundary` findings as baseline debt unless a changed file introduces a new violation; this documentation story does not touch those ownership surfaces.
---
## 2026-08-09 13:36 - wo-d1-packaged-factories-docs-002
- What was implemented
  - Added full bounded/iterative operator entries for `@you/goal`, `@you/loop`, `@you/fusion`, `@you/classify`, and `@you/review` to `docs/reference/packaged-factories.md`.
  - Documented each current live invocation signature, all worker roles, role-specific provider/model overrides, operator-default fallback, prerequisites, workspace/session/recording side effects, stopping/routing/retry behavior, and final versus intermediate output shape.
  - Added `docs/examples/packaged-classify-mock-workers.json` and indexed it from `docs/examples/README.md` so the classify example can deterministically prove a valid `small` route without a live provider call.
  - Executed every worked command exactly as shown against a freshly built current-source `bin/you.exe` with isolated `HOME`/`USERPROFILE` state. `goal`, `loop`, `fusion`, and `review` returned `mock worker accepted`; `classify` routed through `small` and returned `mock worker accepted`.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `docs/examples/README.md`
  - `docs/examples/packaged-classify-mock-workers.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The live packaged catalog is lazily materialized and can be stale in an operator profile; use isolated home variables when collecting current help evidence.
  - `--with-mock-workers` is enough for most packaged examples, but classifier workflows need a protocol-valid scripted provider response for the exact route label before the selected lane can run.
  - Keep deterministic documentation fixtures under `docs/examples/` and explain their role so smoke output is not mistaken for a live model guarantee.
---

## 2026-08-09 14:04 - wo-d1-packaged-factories-docs-003
- What was implemented
  - Added distinct operator-ready entries for `@you/factory-builder`, `@you/plan-execute`, `@you/plan-parallel`, and `@you/full-flow` in `docs/reference/packaged-factories.md`.
  - Documented each live help signature, all worker roles and provider/model overrides, relative workflow choice, workspace and Git side effects, concurrency and retry bounds, validation/installation, local merge/replanning, and final versus intermediate output behavior.
  - Added exact current-binary smoke evidence: the builder returned `mock worker accepted`; plan-execute returned `mock worker accepted` and `<COMPLETE>`; plan-parallel returned `mock worker accepted`; full-flow's controlled invalid-bound check returned the documented `maxCycles` choice-validation error.
  - Marked Story 003 as passing in `prd.json` and recorded the generic-mock limitation so protocol-valid planner responses are not confused with end-to-end orchestration evidence.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `go test ./tests/functional/factory/packaged/factory_builder ./tests/functional/factory/packaged/plan_execute ./tests/functional/factory/packaged/plan_parallel ./tests/functional/factory/packaged/full_flow -count=1 -timeout 300s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The planning entries are not interchangeable: builder installs a new Factory, plan-execute stays sequential in the current workspace, plan-parallel shares one repository across a bounded DAG, and full-flow owns worktrees, local merges, and replanning.
  - Generic mock workers accept a planner dispatch without producing its required `FACTORY_REQUEST_BATCH`; use protocol-valid fixtures or provider-backed functional tests when verifying generated Work, concurrency, or cycle control.
  - Keep full-flow CLI smoke examples bounded and terminating; use controlled argument validation for the public command example and the isolated functional test for actual worktree/merge behavior.
---

---
## 2026-08-10 14:43 -07:00 - pf-stale-scope-diagnostic-002 verification follow-up
- What was implemented
  - Moved orphan-claim/recovery mechanics into the focused `ownership.go` owner module and moved the concurrent race fixture into `diagnostics_test.go` so the repository’s backend-size and maintainability gates remain satisfied without exemptions.
  - Extracted existing-target handling from `InstallPackagedFactory` and the recovered-acquisition assertion from the diagnostic test while preserving the same observable behavior.
- Files changed
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/ownership.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service_test.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/diagnostics_test.go`
- Verification
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make test` — passed.
  - `make typecheck` — passed after one frozen `bun install --frozen-lockfile` restored the declared `@types/bun` dependency in the local workspace.
  - Full distribution test sweep and 10-run race-detector orphan-reclaimer test — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a new behavioral test crosses a ratcheted size or complexity gate, move only the focused fixture/assertion into an existing owner test module and extract helpers; do not weaken the gate or add an exemption.
---
---
## 2026-08-10 14:29 -07:00 - pf-stale-scope-diagnostic-002
- What was implemented
  - Replaced path-based orphan `RemoveAll` with an atomic exact-path rename claim: the winning reclaimer moves the revalidated deterministic `.staging-owner` lease to a distinct staging-prefixed recovery path, publishes the current owner metadata there, and passes that lease into the installation operation.
  - Kept the original deterministic path out of reuse during recovery, so a delayed reclaimer receives a bounded racing/indeterminate contention result and cannot remove a replacement owner resource.
  - Added a deterministic concurrent orphan-reclaimer test that pauses both contenders after revalidation, lets one reclaim and acquire the recovery lease, then resumes the delayed contender and verifies the winner metadata remains intact.
  - Extended recovery diagnostics coverage to assert both `reclaimed-orphan` and recovery `acquired` structured outcomes.
- Files changed
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service_test.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/diagnostics_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused packaged-installation tests for recovery, acquisition race, structured diagnostics, and concurrent orphan reclaim — passed.
  - Full `pkg/services/factory_definitions/internal/services/distribution/...` test sweep — passed.
  - `go test -race` concurrent orphan-reclaimer test, `-count=10` — passed 10/10.
  - Real-process hard-kill successor regression, `-count=3` — passed 3/3.
  - `gofmt` and `git diff --check` — passed.
- **Learnings for future iterations:**
  - A revalidated metadata read followed by path removal is not a safe compare-and-reclaim: the resource must be atomically moved to a distinct visible lease path before the old path can be reused.
- Keep a recovered staging lease visible and owned throughout the handoff; this closes the gap where a third installer could otherwise acquire the deterministic path between orphan removal and successor ownership publication.
---
---
## 2026-08-10 14:11 -07:00 - pf-stale-scope-diagnostic PR feedback follow-up
- What was implemented
  - Updated PR #1853 to head `dd27e1d0f` after pushing the current-main merge and the append-only merged-head verification record.
  - Regenerated the PR description from the active `prd.json` and posted review follow-up comment `5246087033` with the Story 001 evidence and the remaining Story 002 atomic-reclamation work.
- Files changed
  - `progress.txt`
- Verification
  - The PR base is `6e01bee68`, matching the synchronized local `origin/main`; the PR is mergeable but blocked while checks rerun.
  - The latest blocking conversation finding about the Story 001 observe-then-suspend race is explicitly addressed. The Story 002 compare-and-reclaim race remains unresolved by design for the next one-story iteration.
- **Learnings for future iterations:**
- A review follow-up should state exactly which finding the current head resolves and explicitly preserve any remaining blocking story work; green historical CI cannot be presented as merged-head evidence while new checks are pending.
---
---
## 2026-08-10 14:08 -07:00 - pf-stale-scope-diagnostic merged-head verification follow-up
- What was implemented
  - Pushed the current-main synchronization merge and the deterministic Story 001 hard-kill test changes to `origin/pf-stale-scope-diagnostic` at `968afcc82`.
  - Attempted the owner-package regression on the merged tree. The first run emitted an `ok` package result but the surrounding command returned nonzero; a repeat was inconclusive because the outer command limit expired during concurrent Go builds. No assertion failure was reported.
- Files changed
  - `progress.txt`
- Verification
  - The pre-synchronization focused hard-kill scenario passed three consecutive runs, and the pre-synchronization owner package, `make test`, backend structure gates, `go vet`, and whitespace checks passed.
  - The merged tree is pushed for GitHub CI validation; the local merged-head owner-package rerun remains inconclusive because of workspace build contention.
- **Learnings for future iterations:**
  - Do not treat an `ok` line from a command that returns nonzero as a clean verification; retain the distinction between test output and process-level command status when concurrent build work is present.
---
---
## 2026-08-10 13:47 -07:00 - pf-stale-scope-diagnostic current-main synchronization follow-up
- What was implemented
  - Fetched the live `origin/main` head `6e01bee68` after the PR base advanced through the CLI-exit-codes merge, then merged it into the task branch.
  - Preserved the task-scoped `prd.json` as the PR description source and combined the unrelated CLI-exit-codes append-only progress history with this task's progress, including all current reusable patterns.
  - Committed the conflict-free reconciliation as `8716903af`; no task implementation files were discarded.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main CLI diagnostic and Worker Sessions source/test files included by the synchronization merge.
- Verification
  - `prd.json` parses as `pf-stale-scope-diagnostic` with both stories `passes:true`.
  - `git ls-files -u` reports no unresolved merge stages.
  - `git diff --cached --check` and merged-tree `git diff --check` — passed.
- **Learnings for future iterations:**
  - When merging a long append-only progress file, retain both sides' content, validate conflict-marker absence and line-count/headings, and keep the active task PRD authoritative before regenerating the PR body.
---
---
## 2026-08-10 13:35 -07:00 - pf-stale-scope-diagnostic-001 acquisition evidence follow-up
- What was implemented
  - Required the deterministic observer to see the atomically published `.owner.json` before accepting the suspended `.staging-owner` lease, avoiding a valid but ownerless directory-creation checkpoint.
  - Classified Windows sharing/lock violations caused by the suspended child holding a staging handle as retryable observations; the observer resumes and retries within the existing bounded guard while preserving immediate failure for unrelated filesystem errors.
- Files changed
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_process_windows_test.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_process_nonwindows_test.go`
  - `progress.txt`
- Verification
  - Focused real-process hard-kill successor test with `-count=3` — passed 3/3 after the owner-metadata and transient-sharing retry adjustments; each run preserved `.staging-owner/.owner.json` and produced the bounded successor diagnostic with predecessor PID evidence.
  - `gofmt` — passed.
- **Learnings for future iterations:**
  - A suspended Windows child can make an otherwise readable staging subtree report `ERROR_SHARING_VIOLATION`; retry only that bounded observation class after resuming the child, and require published ownership metadata before killing.
---
---
## 2026-08-10 13:31 -07:00 - pf-stale-scope-diagnostic-001 review follow-up
- What was implemented
  - Replaced the hard-kill functional observer's observe-then-suspend sequence with a deterministic suspend-before-observe acquisition barrier. The real predecessor is paused before the exact packaged-factory `.staging-owner` resource is inspected, resumes only when no owner resource is present, and remains paused once the lease is found until the parent hard-kills it.
  - Added portable Unix `SIGSTOP`/`SIGCONT` behavior alongside the existing Windows `NtSuspendProcess` path, including bounded resume/error handling for misses.
  - Narrowed the functional evidence to the actual ownership lease suffix instead of matching transient persistence stages.
- Files changed
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_process_windows_test.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_process_nonwindows_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v -timeout 300s` — passed.
  - Same focused real-process test with `-count=3` — passed 3/3; each run retained `.staging-owner` and returned the bounded successor contention diagnostic.
  - `gofmt` and `git diff --check` — passed.
- **Learnings for future iterations:**
  - Re-stat'ing after filesystem enumeration does not close an observe-then-suspend release race; suspend the child before inspecting the resource, and resume only when the exact ownership lease is absent.
  - Story 002 still has the separate atomic orphan-reclaimer race and deterministic concurrent reclaimer test requested by the current PR feedback; leave that work for the next story iteration.
---
---
## 2026-08-10 12:36 -07:00 - pf-stale-scope-diagnostic final handoff audit
- What was implemented
  - Revalidated the completed PRD against the existing PR #1853 after the latest current-main synchronization; no story implementation or PRD change was needed.
  - Confirmed the prior blocking feedback is covered by the follow-up commits: structured packaged-installation diagnostics, injected backend-scope identity, checker-compliant exclusive reservation, removal of `--with-mock-workers` from the OS-boundary regression, justified filesystem observations, and restoration of unrelated generated churn.
- Files changed
  - `progress.txt`
- Verification
  - Both `pf-stale-scope-diagnostic-001` and `pf-stale-scope-diagnostic-002` remain `passes:true`.
  - PRD JSON and PR body match byte-for-byte; local `HEAD` matches PR #1853 head `49cb1daf25295c02c1856e3c984602b6fba40e8d`.
  - Thread-aware GraphQL audit: 3 conversation comments, 0 reviews, and 0 inline review threads.
  - All 18 PR checks are terminal and successful; PR #1853 is open, mergeable, and clean.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A final PR handoff should recheck the exact PRD/body/head relationship after current-main synchronization and after the last required CI run; an earlier “pending” follow-up comment is not sufficient evidence by itself.
---
---
## 2026-08-10 12:05 -07:00 - pf-stale-scope-diagnostic current-main synchronization
- What was implemented
  - Merged the current `origin/main` head `947152348` into the task branch after the open PR's recorded base had advanced, preserving the task-scoped `prd.json` and append-only progress history.
  - Confirmed both PRD stories remain complete; no additional story implementation was needed in this iteration.
- Files changed
  - Current-main merge (unrelated artifact-registry changes)
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_definitions/internal/services/distribution/packagedinstallation -count=1 -timeout 180s` — passed.
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v -timeout 300s` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make pkg-boundary` reports 175 findings on both the merged head and isolated current `origin/main`; no new boundary finding was introduced by this task.
  - `prd.json` parses with both stories `passes:true`; the thread-aware PR audit reports 0 reviews and 0 inline review threads.
- **Learnings for future iterations:**
  - Run repository-wide scanners from the explicitly intended checkout when comparing worktree baselines; a nested temporary worktree can otherwise be scanned as repository content.
---
---
## 2026-08-10 11:23 -07:00 - pf-stale-scope-diagnostic final PR audit
- What was implemented
  - Corrected the stale story 001 note so it records story 002's completed live-owner and acquisition-race coverage.
  - Audited the existing PR #1853 after the latest review follow-up; no additional implementation work or unresolved review thread remains.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - PRD JSON validation reports both `pf-stale-scope-diagnostic-001` and `pf-stale-scope-diagnostic-002` as `passes:true`.
  - Thread-aware GitHub audit reports 3 conversation comments, 0 reviews, and 0 inline review threads, including 0 unresolved threads.
  - Local `HEAD` matches the PR head; the PR body matches `prd.json` exactly; PR #1853 is open, mergeable, and clean.
  - Latest required CI checks are terminal and successful, including Backend Functional Coverage and Verification Policy.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A completed PR handoff still needs a thread-aware review audit and an exact PRD/body/head comparison; green checks alone do not prove that review state and task metadata are synchronized.
---

## 2026-08-09 15:51 - wo-d1-packaged-factories-docs-004
- What was implemented
  - Added distinct operator-ready entries for `@you/deep-research`, `@you/spawn`, `@you/quorum`, and `@you/tournament` in `docs/reference/packaged-factories.md` using the shared seven-part entry contract.
  - Documented isolated current-binary invocation signatures, all worker roles and provider/model override flags, operator-default fallback, JavaScript policy bounds, quorum Graph Work and merge behavior, task-count/round bounds, side effects, and primary versus intermediate result shapes.
  - Added exact worked command evidence: deep-research completed in structured JSON with two specialist statuses, quorum returned `mock worker accepted`, spawn rejected an out-of-range count of 15, and tournament rejected an out-of-range round count of 4 before provider work.
  - Connected successful provider-backed result claims to the existing functional assertions for exact-count spawn merging and tournament champion/decision-trail output; did not treat generic JavaScript mocks as valid planner/judge protocol.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `go test ./tests/functional/factory/packaged/deep_research ./tests/functional/factory/packaged/quorum ./tests/functional/factory/packaged/javascript_families -count=1 -timeout 300s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Always collect packaged-Factory help and invocation evidence with isolated `HOME` and `USERPROFILE`; stale materialized copies can advertise obsolete flag names or defaults.
  - The current JavaScript mock runner proves generic terminal envelopes but cannot author `@you/spawn`'s exact planner JSON array or `@you/tournament`'s judge JSON; provider-command functional tests are the observable success proof for those protocol-sensitive workflows.
  - `@you/deep-research`'s structured primary result requires `--json`; default human output rejects a non-plain-text JSON primary result.
---

## 2026-08-09 16:04 - wo-d1-packaged-factories-docs-005
- What was implemented
  - Added the complete `@you/tts` entry to `docs/reference/packaged-factories.md`, covering the live text-only signature, suitable use, local worker/backend/model route, readiness prerequisite, side effects, primary metadata, generated audio artifact, and failure behavior.
  - Corrected the pre-existing packaged TTS workstation prompt template to read `WorkID` from the first bound input, then regenerated the checked-in JSON/YAML catalog and manifest hashes so the documented live invocation can reach model execution.
  - Executed `you run --named @you/tts --no-record --output primary --to "The release is ready."` exactly as documented against the rebuilt current binary using a disposable READY model cache and controlled local runtime. The command returned a runtime-generated WAV artifact path, `audio/wav`, the `OMNIVOICE_Q4_K_M/LLAMACPP` backend, and a trace ID; the existing behavioral test also verifies readable audio artifact output.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `packages/packaged-factories/factories/tts/factory.yaml`
  - `packages/packaged-factories/generated/factories/tts/factory.json`
  - `packages/packaged-factories/generated/factories/tts/factory.yaml`
  - `packages/packaged-factories/generated/manifest.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `make packaged-factory-catalog-check` — passed.
  - `go test ./tests/functional/factory/packaged/tts ./pkg/services/factory_definitions/internal/services/distribution/tts -count=1 -timeout 300s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The packaged TTS worker uses the local `OMNIVOICE_Q4_K_M` route and exposes no provider/model override flags; model readiness and local runtime setup belong in the canonical models workflow.
  - `--with-mock-workers` does not satisfy the managed local TTS readiness/runtime path; a successful CLI smoke needs a READY cache and a controlled local runtime or the real installed runtime.
  - TTS primary output is metadata pointing to an artifact, so paths, trace IDs, and generated audio bytes must remain runtime-generated rather than documentation promises.
---

## 2026-08-09 16:11 - wo-d1-packaged-factories-docs-006
- What was implemented
  - Added the complete `@you/subagent` operator entry to `docs/reference/packaged-factories.md` using the shared seven-part contract.
  - Documented the bounded read-only purpose, request binding, exact live invocation signature, single `subagent-worker` role, optional provider/model/reasoning overrides, operator/provider-default fallback, runtime/session side effects, failure behavior, and human-readable versus structured primary output.
  - Added an exact structured mock-worker invocation and captured its real `COMPLETED` output with a text `primaryResult`; explicitly separated deterministic mock envelope evidence from provider-generated answer quality.
  - Marked story 006 as passing in `prd.json` and added the live `--quiet`/structured-output constraint to the consolidated codebase patterns.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/subagent -count=1 -timeout 300s` — passed.
  - `make docs-reference-smoke` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - `@you/subagent` has one `READ_ONLY` `subagent-worker`; its live optional flags are `--worker-provider`, `--worker-model`, and `--worker-reasoning-effort`, with omitted provider/model values using operator defaults and omitted reasoning using the provider default.
  - `--quiet` conflicts with `--json` and `--output`; structured examples must omit `--quiet` and should be validated against live CLI behavior.
  - The normalized subagent primary result is the child answer, while request/trace IDs and session state are runtime metadata; generated provider prose remains non-deterministic.
---

## 2026-08-09 16:52 - review follow-up: packaged-factory worked examples
- What was implemented
  - Merged the current `origin/main` into the documentation branch and resolved the shared docs indexes, embed registry, and CLI smoke-test changes while preserving both branches' behavior.
  - Replaced the invalid sole worked invocations for `@you/full-flow`, `@you/spawn`, and `@you/tournament` with valid bounded commands and captured successful output from the current binary in a controlled environment.
  - Kept the `max-cycles 9`, `count 15`, and `rounds 4` commands as explicitly labeled additional bound checks rather than success examples.
  - Removed the attempted checked-in JavaScript mock-worker fixtures after confirming that the CLI `--with-mock-workers` mode selects generic `FakeChildExecutor` output and cannot satisfy planner/judge protocols.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
  - merged current-main documentation and test conflict resolutions
- Verification
  - Full-flow exact command exited 0 with `Create a one-line README note and verify it.` after a deterministic local `codex` executable supplied a protocol-valid planner response marking the bounded request complete, so no implementation/review/merge call was needed.
  - Spawn exact command exited 0 with `merged travel answer` using `--count 2`.
  - Tournament exact command exited 0 with the champion and `Tournament decision trail:` / `Round 1, match 1: more complete` using `--rounds 1`.
  - All three runs used isolated `HOME` and `USERPROFILE`; full-flow used a disposable Git repository with `main` as the base branch.
- **Learnings for future iterations:**
  - JavaScript packaged-factory success smokes need a live provider boundary or provider-command edge fixture; `--with-mock-workers` intentionally returns generic fake child text and is not protocol-valid for planner/judge workflows.
  - A bounded full-flow terminal smoke must preserve the rendered cycle-control Work name; a mismatched name leaves the orchestration graph waiting for completion.
  - After merging current main into a docs branch, verify both the embed/topic registry and user-visible docs smoke assertions before committing the merge resolution.
---

## 2026-08-09 14:24 -07:00 - wo-c1-ext-stream-replay-only-001
- What was implemented
  - Added ACTIVE Worker Session finite replay through retained Events reads, including a captured head boundary, canonical paging, zero-event handling, explicit `REPLAY_SUMMARY`, `complete:false`, `reason:"session-active"`, and emitted-event counting.
  - Added HTTP/SSE `replayOnly` propagation and summary framing, CLI `--replay-only` wiring, redirect-safe JSON summary output, and operator help/examples.
  - Regenerated bundled OpenAPI, Go HTTP clients/server, TypeScript OpenAPI types, CLI family metadata, and published CLI contract artifacts.
  - Added service, HTTP transport, and CLI behavioral tests proving ordering, snapshot truncation, no live subscriber, summary serialization, and query propagation.
- Files changed
  - Worker Sessions service and observation contracts, HTTP/CLI transports and tests, authored/generated OpenAPI and CLI contracts, `prd.json`, and this progress log.
- **Learnings for future iterations:**
  - The existing Events `ReadResult` explicitly distinguishes progress, head, and retention gaps; finite replay should preserve those outcomes instead of inferring completion from an empty page.
  - `make api-smoke` checks the working tree for generated drift, so run it after committing synchronized generated artifacts; `make ui-deps` restores the Bun dependency tree when the local OpenAPI generator is missing.
---

## 2026-08-09 14:32 -07:00 - wo-c1-ext-stream-replay-only-002
- What was implemented
  - Completed terminal replay regression coverage over the real in-memory Events service, proving a terminal Worker Session replays its retained opening and terminal records in canonical order, marks the lifecycle record as `TERMINAL_REPLAY`, emits exactly one complete `session-completed` replay summary with the matching event count, and closes after the summary.
  - Added CLI coverage proving replay-only continues through `TERMINAL_REPLAY`, returns success, and writes the complete summary as the final JSON line.
  - Preserved the existing live terminal-stream termination and ACTIVE transcript-read behavior.
- Files changed
  - `pkg/services/worker_sessions/internal/service/control_production_boundary_test.go`
  - `pkg/services/worker_sessions/transports/cli/stream_test.go`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
- Terminal replay uses the same retained Events paging path as ACTIVE replay; lifecycle classification changes the delivery kind and summary completeness, not the canonical event records or order.
- The CLI must treat `TERMINAL_REPLAY` as an event inside finite mode and wait for `REPLAY_SUMMARY` before returning success.
---

## 2026-08-09 15:00 -07:00 - wo-c1-ext-stream-replay-only-003
- What was implemented
  - Added the explicit `--follow` live-mode input and stable typed `WORKER_SESSION_STREAM_MODE_CONFLICT` error; `--replay-only --follow` now fails before the stream operation, HTTP request, retained read, follower, or stdout output.
  - Added the authored CLI relationship, error metadata, help/examples, generated family metadata, published CLI contract, and production handler binding; refreshed the CLI inventory baselines and generated manifest hash.
  - Fixed detached Cobra local relationship projection so the new mutually-exclusive flags do not eagerly shadow the production root `--server` value when the Worker Sessions family is attached.
  - Added service and production-root behavioral tests for typed conflict mapping, zero requests/zero output, explicit follow binding, and operation non-invocation.
- Files changed
  - `contracts/cli/commands.json`, generated CLI/package artifacts, CLI inventory baselines, Worker Sessions CLI stream/config/tests, CLI family constructor, production root binding/tests, and this progress log.
- **Learnings for future iterations:**
  - Cobra's `MarkFlags*` helpers merge parent persistent flags immediately; detached generated families need local-only relationship annotations projected without that merge so their eventual production-root values remain authoritative.
  - Intentional CLI contract additions require refreshing `contracts/testdata/baseline/cli-command-inputs.json` and `contracts/testdata/baseline/cli-commands.json` with `UPDATE_CLI_BASELINES=1` in addition to the normal manifest and contract generators.
  - `make verify-fast`, Worker Sessions CLI tests, full `pkg/transports/cli` tests, CLI inventory/identity/contract/constructor tests, and `make cli-manifest-check` passed.
---

## 2026-08-09 15:11 -07:00 - wo-c1-ext-stream-replay-only-004
- What was implemented
  - Added `TestWorkerSessionsReplayOnlyRedirectsWellFormedNDJSON`, which builds and invokes the published `you` process against a terminal Worker Session, redirects replay-only JSON stdout to a capture file, and verifies every non-empty line is valid NDJSON.
  - The process-boundary assertion checks canonical increasing event positions, exactly one final `replay-summary`, `complete:true`, stable `session-completed` reason, matching `eventsEmitted`, natural exit without cancellation, and verbose diagnostics isolated on stderr.
  - Added the canonical `you docs sessions` redirect workflow describing the final completeness marker, ACTIVE versus terminal meaning, live-follow conflict, and stderr diagnostics behavior.
- Files changed
  - `tests/functional/provider_sessions/cli/worker_sessions_cli_test.go`
  - `docs/reference/sessions.md`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
  - A built CLI process can prove shell redirection while the server and provider effects remain deterministic through `root.BuildProcess` and `edges.Edges`; this keeps the process-boundary proof narrow.
- The final Worker Session replay summary is a standalone JSON object after the existing event-frame records, so capture assertions should parse the last line with its own summary shape.
- `make lint` currently stops on the existing `pkg/transports/cli/climanifestcobra/constructor.go` 1037-line backend-size/pkg-maint violation; the story change does not touch that file.
---

## 2026-08-09 15:44 -07:00 - wo-c1-ext-stream-replay-only-004
- What was implemented
  - Added focused Worker Sessions service tests for retained-read failures, retention gaps, cancellation, malformed paging outcomes, snapshot-boundary violations, completion, and close behavior.
  - Restored the `pkg/services/worker_sessions/internal/service` unit coverage result to 99.0%, above the enforced 98.72% package minimum, without lowering the coverage baseline.
  - Re-ran the named Worker Sessions functional command, `make verify-fast`, package-structure/file-count checks, and Worker Sessions vet successfully.
- Files changed
  - `pkg/services/worker_sessions/internal/service/invoke_observation_test.go`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
  - The repository's coverage gate is sensitive to untested defensive replay branches; typed read outcomes should have direct behavioral assertions alongside the happy-path stream tests.
  - `make test-unit-coverage` cannot launch its full coverage lane on this Windows checkout because the generated Go command exceeds the Windows filename/command-line limit; the affected package's local coverage profile still verifies the threshold.
- A full CI functional run exposed `TestFactorySessionHistoryIsNotPersistedByDefault`, but the test passes repeatedly in isolation and the feature-specific functional command is green; avoid unrelated changes until a reproducible failure or actionable review feedback identifies a shared cause.
---

## 2026-08-09 16:30 -07:00 - wo-c1-ext-stream-replay-only-review-follow-up
- What was implemented
  - Addressed PR #1840's blocking correctness feedback by making replay summaries complete only when the retained snapshot contains a terminal Worker Session lifecycle record; terminal state without that record now reports `complete:false` with `session-terminal-record-missing`.
  - Added a deterministic controlled-boundary regression that blocks and fails the terminal append after state commit, opens replay during the interleaving, and proves no false-complete summary is emitted.
  - Moved Cobra relationship projection out of the oversized generic constructor into the existing work constructor file and split the replay completion/cancellation/close test to clear `backend-size` and `pkg-maint` without adding package-baseline files.
- Files changed
  - Worker Sessions replay service and behavioral tests/fakes, `pkg/transports/cli/climanifestcobra/constructor.go`, `pkg/transports/cli/climanifestcobra/work_constructor.go`, and this progress log.
- **Learnings for future iterations:**
  - Terminal state and terminal-event publication are separate synchronized effects; snapshot consumers must validate the event range before claiming a complete capture, including permanent terminal-record append failures.
  - Review ratchets may include package file-count baselines in addition to line and complexity limits; an existing cohesive package file is preferable for small same-package helper moves.
- Focused Worker Sessions/CLI package tests, the named redirect functional test, `make verify-fast`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and affected-package vet pass. `make -k lint` still reports pre-existing baseline failures in `pkg-boundary`, package-target/packaged-factory/ownership inventories, and deadcode; the reviewer-identified new backend-size/pkg-maint failures are resolved.
---

## 2026-08-09 16:45 -07:00 - wo-c1-ext-stream-replay-only-ci-follow-up
- What was implemented
  - Inspected PR #1840's GitHub Actions backend functional coverage failure with the CI log workflow and reran only the failed job.
  - Confirmed the first failing existing durability test (`TestJavaScriptInterruptedSessionResumesWithoutRepeatingCompletedChildren`) and the rerun's existing restart test (`TestFactorySessionResumeDoesNotRepeatCompletedDispatch`) each pass in isolation; no source change was made because neither touches the replay-only implementation.
  - Confirmed all other PR checks, including Backend Unit Coverage, remain passing; the broad Backend Functional Coverage lane and its Verification Policy aggregator remain red after the rerun.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - The current PR's remaining blocker is external full-suite functional instability in cancellation-observation assertions, not a reproducible feature failure; a future iteration should rerun or diagnose that suite before changing Worker Sessions replay behavior.
  - The PR remains open at the latest pushed commit with no additional unresolved inline review threads, but cannot be marked review-ready while the required backend functional check is red.
---

## 2026-08-09 16:13 -07:00 - wo-c1-ext-stream-replay-only-delivery
- What was implemented
  - Confirmed all four PRD stories remain marked `passes:true`; no additional story implementation was required in this iteration.
  - Re-ran the named Worker Sessions redirect/CLI functional test and `make verify-fast` successfully on the exact branch HEAD.
  - Confirmed the worktree is clean before the delivery commit/PR update and found no existing PR or review feedback for this branch.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - A clean branch synchronized with its remote still needs the focused and fast verification gates rerun before creating the PR so the published commit has current local evidence.
  - When every story is already marked complete and no PR exists, append delivery evidence to the progress log without changing the completed PRD story state.
---

## 2026-08-09 16:51 -07:00 - wo-c1-ext-stream-replay-only-delivery-follow-up
- What was implemented
  - Confirmed all four PRD stories remain marked `passes:true`; no additional product story work was pending.
  - Rechecked PR #1840's current discussion and thread state: the prior blocking correctness and lint feedback is addressed on the current head, with no unresolved inline review threads.
  - Re-ran the named replay-only redirect/CLI functional test and `make verify-fast` successfully on the exact synchronized branch head.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - An open PR with addressed feedback still needs its latest remote check state rechecked before reporting delivery; at this point the required CI run was still in progress.
  - When the PRD is already fully passed and no new code is required, preserve the completed `prd.json` story state and record verification/review evidence in the append-only progress log.
---

## 2026-08-09 17:00 -07:00 - wo-c1-ext-stream-replay-only-ci-follow-up
- What was implemented
  - Rechecked PR #1840 at the synchronized head: all four PRD stories remain `passes:true`, the worktree is clean, and thread-aware review state has zero unresolved inline threads or formal reviews.
  - Inspected the terminal Backend Functional Coverage failure from the GitHub Actions diagnostic artifact. The only failing test was the unrelated `TestAgyConductorSuccessThroughRootBuildProcess`, which observed zero terminal result messages; no Worker Sessions replay test or changed file was implicated.
  - Reran only the failed Backend Functional Coverage job on the same commit; the rerun is still in progress. Backend Unit Coverage and all other completed required checks are passing.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - Functional coverage diagnostics are in the `functional-test-diagnostics` artifact; `command.log` exposes the first actual failing test even when the job's final artifact guard reports only a missing evidence file.
  - Do not change replay-only code for an unrelated provider-functional flake; rerun the isolated failed job and wait for a reproducible feature-linked failure first.
  - The PR remains open and mergeable with no unresolved review threads, but the required verification policy cannot be green until the Backend Functional Coverage rerun reaches a terminal result.
---

## 2026-08-09 17:06 -07:00 - wo-c1-ext-stream-replay-only-delivery-verification
- What was implemented
  - Confirmed all four PRD stories remain marked `passes:true`; no additional story implementation or authored contract change was required.
  - Re-ran the named replay-only redirect/CLI functional command and `make verify-fast` successfully on the current PR head.
  - Re-ran `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, `go test ./pkg/services/worker_sessions/...`, and `go vet ./pkg/services/worker_sessions/...`; all passed.
  - Rechecked PR #1840: the branch head is synchronized with the PR remote, the prior blocking feedback has no unresolved review threads, and the new CI run is executing for this exact head.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - The worktree's `origin` remote points at `infinite-you`; PR delivery for this repository uses the `fork` remote at `portpowered/you-agent-factory`, so compare and push against `fork` when verifying synchronization.
  - A progress-only delivery commit still needs the complete local gate set and a fresh remote CI run before the PR can be reported ready for review.
  - Keep `prd.json` unchanged when every story is already passed and the iteration adds only verification evidence.

## 2026-08-09 17:13 -07:00 - wo-c1-ext-stream-replay-only-delivery-review
- What was implemented
  - Confirmed all four PRD stories remain marked `passes:true`; no additional story implementation or contract change was required.
  - Inspected PR #1840 with the thread-aware GitHub review workflow. The prior blocking correctness/lint feedback is addressed in the current head, and there are no unresolved inline review threads or external actionable comments.
  - Confirmed local HEAD `97d6e77e` matches the fork PR head and the required CI run for that exact commit is executing.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - A cross-repository PR needs the head repository and PR number passed explicitly to the thread-aware GraphQL comment reader when the local branch tracks a different remote.
- Do not change the completed `prd.json` story state during delivery-only iterations; append review and CI evidence to the progress log instead.
---

## 2026-08-09 17:27 -07:00 - wo-c1-ext-stream-replay-only-merge-conflict-resolution
- What was implemented
  - Fetched the current fork `main` (`f6de38c59`) after GitHub reported PR #1840 as conflicting and merged it into the branch.
  - Resolved the only conflict in the worktree-local `prd.json` by retaining the replay-only PRD; all product-file changes from current `main` merged without conflict.
  - Preserved the completed four-story `passes:true` state and the existing review/CI evidence; the merge commit now needs the focused local gates and a fresh remote CI run.
- Files changed
  - `prd.json`, current-`main` merge result, and `progress.txt`
- **Learnings for future iterations:**
  - When multiple worktrees use a repository-local `prd.json`, a PR merge can conflict with another story's PRD even when product files are conflict-free; resolve that file in favor of the active branch's task contract.
  - A green CI run on the pre-merge head does not establish merge readiness after GitHub reports `CONFLICTING`; fetch the PR's actual head repository `main`, resolve the conflict, and rerun checks for the merged head.
---

## 2026-08-09 17:51 -07:00 - wo-c1-ext-stream-replay-only-delivery-review
- What was implemented
  - Confirmed all four PRD stories remain marked `passes:true`; no additional story implementation or contract change was required.
  - Rechecked PR #1840 in `portpowered/you-agent-factory` with thread-aware review data: there are no unresolved inline review threads or formal reviews, and the earlier correctness and lint feedback is addressed in the current head.
  - Confirmed local HEAD `e769770b6` matches the PR head, the PR description still uses the current `prd.json`, GitHub reports the PR mergeable and clean, and every listed required check is terminal and successful.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - For a cross-repository PR, verify the head OID and check state in the PR's actual repository rather than relying on the local branch's default remote association.
  - When all stories and review feedback are complete, preserve `prd.json` and append delivery evidence to the progress log instead of creating unrelated source changes.
---

## 2026-08-09 18:33 -07:00 - wo-d1-packaged-factories-docs-delivery
- What was implemented
  - Fetched the current `main` from the PR repository (`40512cfa1`) and merged it into the documentation branch.
  - Reconciled shared `docs/README.md`, `prd.json`, and append-only `progress.txt` conflicts while preserving the packaged-factory PRD and the mainline Worker Sessions history.
  - Kept all six WO-D1 stories marked `passes:true` and retained the current PR description contract.
  - Documented each current live invocation signature, all worker roles, role-specific provider/model overrides, operator-default fallback, prerequisites, workspace/session/recording side effects, stopping/routing/retry behavior, …7990 tokens truncated…rked `passes:true` and retained the current PR description contract.
- Files changed
  - Current-main merge result, `docs/README.md`, `prd.json`, `progress.txt`, and the mainline files included by the merge.
- Verification
  - `make docs-reference-smoke` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checkout's default `origin` remote tracks `portpowered/infinite-you`, while PR #1841 is hosted in `portpowered/you-agent-factory`; compare and push against the PR's head repository when checking mergeability.
  - A clean local merge against an older cached base is insufficient when GitHub reports `CONFLICTING`; fetch the actual current base, preserve both append-only progress logs, and rerun the gates on the merge commit.
---
## 2026-08-09 13:36 - wo-d1-packaged-factories-docs-002
- What was implemented
  - Added full bounded/iterative operator entries for `@you/goal`, `@you/loop`, `@you/fusion`, `@you/classify`, and `@you/review` to `docs/reference/packaged-factories.md`.
  - Documented each current live invocation signature, all worker roles, role-specific provider/model overrides, operator-default fallback, prerequisites, workspace/session/recording side effects, stopping/routing/retry behavior, and final versus intermediate output shape.
  - Added `docs/examples/packaged-classify-mock-workers.json` and indexed it from `docs/examples/README.md` so the classify example can deterministically prove a valid `small` route without a live provider call.
  - Executed every worked command exactly as shown against a freshly built current-source `bin/you.exe` with isolated `HOME`/`USERPROFILE` state. `goal`, `loop`, `fusion`, and `review` returned `mock…8364 tokens truncated…in `prd.json`; no unresolved inline review threads or formal reviews.
- **Learnings for future iterations:**
  - Provider/ACP functional lanes can expose isolated session-load flakes; verify the exact failing test locally, rerun only failed CI jobs, and require the expected evidence artifact before treating the rerun as green.
  - A delivery-only CI follow-up should record the evidence in the append-only progress log without changing unrelated source or completed PRD story state.
---
## 2026-08-09 18:51 -07:00 - wo-d1-packaged-factories-docs-delivery-ci-follow-up
- What was implemented
  - Investigated the initial CI failure in the ACP functional lane; it was an isolated session-load failure, not a documentation or packaged-factory source regression.
  - Re-ran the exact failing ACP test locally ten times successfully, then re-ran the failed GitHub Actions jobs. The rerun passed the functional lane and produced the required real-ACP evidence artifact.
  - Confirmed the PR remains reconciled with current `main`, clean, mergeable, and synchronized to the latest pushed commit.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/providers/acp -run '^TestFactoryRunRetriesACPProviderByResumingExactSession$' -count=10 -v -timeout 300s` — passed.
  - `gh run rerun 31347454534 --failed` — rerun completed successfully.
  - All required PR checks, including Backend Functional Coverage and Verification Policy, are passing.
  - No remaining `passes:false` entries in `prd.json`; no unresolved inline review threads or formal reviews.
- **Learnings for future iterations:**
  - Provider/ACP functional lanes can expose isolated session-load flakes; verify the exact failing test locally, rerun only failed CI jobs, and require the expected evidence artifact before treating the rerun as green.
  - A delivery-only CI follow-up should record the evidence in the append-only progress log without changing unrelated source or completed PRD story state.
---
## 2026-08-09 19:12 -07:00 - wo-d1-packaged-factories-docs-delivery-ci-durability-follow-up
- What was implemented
  - Investigated the fresh-head Backend Functional Coverage failure, which rotated between two JavaScript durability cancellation assertions: `TestJavaScriptResumeRestoresCheckpointAndFinalResult` and `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault`.
  - Ran both exact tests locally 20 times each successfully and left unrelated runtime/test source unchanged.
  - Re-ran only the failed backend job; the final rerun completed with all 18 CI jobs successful, including Backend Functional Coverage and Verification Policy.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/orchestration/javascript/durability -run '^TestJavaScript(ResumeRestoresCheckpointAndFinalResult|DurabilityDoesNotPersistSnapshotsByDefault)$' -count=20 -v -timeout 900s` — passed.
  - `gh run rerun 31348213300 --failed` — final failed-job rerun passed.
  - The rotating cancellation assertion was treated as a current-main CI timing flake; no unrelated production or test fix was introduced.
- **Learnings for future iterations:**
  - Full functional CI can expose timing-sensitive cancellation observations even when the exact durability tests pass repeatedly in isolation; use the failing assertion and local repetition to separate a flake from a branch regression before changing source.
  - After a successful failed-job rerun, a subsequent progress-only head must receive its own complete CI run before delivery is considered current.
---
## 2026-08-09 19:33 -07:00 - wo-d1-packaged-factories-docs-review-follow-up
- What was implemented
  - Addressed the latest blocking PR conversation feedback by removing the stale statement that the `@you/full-flow` worked smoke intentionally exercises argument validation.
  - The planning-family introduction now describes the successful controlled planner-response smoke and identifies `--max-cycles 9` only as an additional out-of-range bound check.
  - Kept all six PRD stories marked `passes:true`; no `prd.json` change was required.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - After replacing a worked example, audit nearby family-level explanatory prose as well as the entry itself; stale summary text can reintroduce a resolved correctness contradiction.
  - Documentation-only review fixes still require a fresh pushed head and terminal CI before the PR can be reported ready.
---
## 2026-08-09 19:47 -07:00 - wo-d1-packaged-factories-docs-ci-follow-up
- What was implemented
  - Investigated the fresh-head Backend Functional Coverage failure, which was the unrelated `TestAPIResponseEventSSEStreamsRetainedThenLiveEvents` assertion observing zero retained response events.
  - Ran the exact test 20 times locally with no failures, then reran only the failed GitHub Actions job; the rerun passed Backend Functional Coverage and Verification Policy.
  - Left unrelated event-stream source unchanged and retained the documentation fix as the only product change.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/events/response_events -run '^TestAPIResponseEventSSEStreamsRetainedThenLiveEvents$' -count=20 -v -timeout 900s` — passed all 20 runs.
  - `gh run rerun 31350145939 --failed --repo portpowered/you-agent-factory` — attempt 2 passed the failed lane.
  - Backend Functional Coverage and Verification Policy passed on the rerun; all other required checks for the prior head were already passing.
- **Learnings for future iterations:**
  - A full functional-coverage failure in an unchanged response-events package can be a timing flake; reproduce the exact assertion locally before making unrelated code changes.
  - Rerunning only the failed job can clear the flake, but a subsequent progress-only commit still needs its own complete CI run before delivery is current.
---
## 2026-08-09 20:14 -07:00 - wo-d1-packaged-factories-docs-ci-durability-follow-up
- What was implemented
  - Investigated the final progress-head Backend Functional Coverage failures, which rotated across unchanged `response_events`, CLI output/process, `runtime_api`, and session-restart tests under the full parallel lane.
  - Repeated each named failing test in isolation 20 times; every focused run passed, so no unrelated runtime or test source was changed.
  - Reran only the failed Backend Functional Coverage job four times; the fourth attempt passed, and the dependent Verification Policy check passed as well.
- Files changed
  - `progress.txt`
- Verification
  - Focused 20-run repetitions passed for `TestAPIResponseEventSSEStreamsRetainedThenLiveEvents`, `TestCLIJSONFailureRemainsValidJSON`, `TestAPIEventReplaySmoke_PublicEventsAndSessionProjectionExposeActiveAndCompletedTimeline`, `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`, `TestCLIFailureWritesDiagnosticToStderr`, and `TestFactorySessionHistoryIsNotPersistedByDefault`.
  - `gh run rerun 31350774713 --failed --repo portpowered/you-agent-factory` — final attempt 4 passed Backend Functional Coverage and Verification Policy.
  - All other required checks for the progress head passed on the original run.
- **Learnings for future iterations:**
  - When full functional coverage rotates between unrelated unchanged tests while each focused test is stable, treat the lane as parallel-suite timing instability and preserve the feature scope.
- A progress-only delivery commit must receive a fresh full-head CI result; a green failed-job rerun for an earlier attempt does not transfer to a later head.
---
## 2026-08-09 21:32 -07:00 - wo-b2-agy-production-review-roles-001
- What was implemented
  - Added the independently invocable `@you/agy-cold-watch` packaged Factory with one required `cutPath` input, explicit Markdown primary result, strict unknown-argument rejection, and an AGY reviewer prompt that treats the completed cut as its only creative context.
  - Defined the cold-watch report contract for chronology, temporal/transient defects, audio, observed speech, and pass-or-reroll recommendation; missing-file refusals are routed to failed Work even when the replayed AGY provider reports exit-zero SUCCESS.
  - Added root-built offline functional coverage for both real video/audio traces and the missing-file trace, asserting the unchanged path in the AGY prompt and `--add-dir`, provider command settings, returned observations, and recorded Factory Events.
  - Regenerated the packaged catalog and updated published catalog expectations for the new sixteenth first-party Factory.
- Files changed
  - `packages/packaged-factories/factories/agy-cold-watch/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-cold-watch/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-cold-watch/factory.json`
  - `packages/packaged-factories/generated/manifest.json`
  - `tests/functional/providers/agy/production_review_test.go`
  - `tests/functional/factory/packaged/catalog/discovery_test.go`
  - `pkg/wire/factory_definition_packaged_installation_test.go`
  - `pkg/wire/packaged_factory_catalog_consumption_test.go`
  - `prd.json`
- Verification
  - `go run ./cmd/factory --json factory config validate packages/packaged-factories/factories/agy-cold-watch` — passed.
  - `go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1` — passed.
  - `go test ./tests/functional/factory/packaged/catalog -count=1` — passed.
  - `go test ./pkg/wire -run 'TestProvidePackagedFactoryDefinitions_LoadsDetachedGeneratedCatalog|TestPackagedFactoryCatalogConsumption_ListResolveInstallUsesPublishedCatalog' -count=1` — passed.
  - `make packaged-factory-catalog-check` and `make packaged-factory-source-check` — passed.
  - `make ui-deps` restored locked dependencies and `make typecheck` — passed.
  - `make verify-fast` initially stopped at missing UI dependencies; after restoring them, the broad `make test` lane reached the existing five-minute `TestPause_ControlOutcomesKeepTheLifecycleTruthful` worker-session timeout. The focused story and affected catalog tests remain green.
- **Learnings for future iterations:**
  - The generated catalog is sorted by public Factory name, so adding a packaged Factory also requires updating the detached-catalog and wire-consumption behavioral expectations.
  - AGY provider `SUCCESS` is only process status; a refusal must be rejected by the role contract so it cannot resolve an invocation primary result.
---
## 2026-08-09 21:48 -07:00 - wo-b2-agy-production-review-roles-002
- What was implemented
  - Added the independently invocable `@you/agy-clip-qa` packaged Factory with exactly two required creative inputs, `clipPath` and `shotSpecification`, strict named-argument rejection, direct AGY workspace path/spec forwarding, ANTIGRAVITY defaults, and the recorded B2 seven-field structured verdict schema.
  - Preserved the distinction between a successful inspected `pass`/`reroll` verdict and execution failure by accepting schema-valid rerolls while rejecting provider refusals, malformed output, and schema mismatches before verdict interpretation.
  - Added root-built offline functional coverage that replays the real clip-QA pass trace and missing-file trace and also proves schema-invalid and provider-exit failures reach failed Work with Factory Events and actionable public diagnostics.
  - Regenerated the canonical catalog and updated the published catalog behavior expectations for the seventeenth first-party Factory.
- Files changed
  - `packages/packaged-factories/factories/agy-clip-qa/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-clip-qa/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-clip-qa/factory.json`
  - `packages/packaged-factories/generated/manifest.json`
  - `pkg/services/workers/internal/services/workstations/executor/helpers.go`
  - `pkg/services/workers/internal/services/workstations/executor/agent_inference_test.go`
  - `pkg/wire/factory_definition_packaged_installation_test.go`
  - `pkg/wire/packaged_factory_catalog_consumption_test.go`
  - `tests/functional/factory/packaged/catalog/discovery_test.go`
  - `tests/functional/providers/agy/production_review_test.go`
  - `prd.json`
- Verification
  - `go run ./cmd/factory --json factory config validate packages/packaged-factories/factories/agy-clip-qa` — passed.
  - `go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1` — passed.
  - `go test ./pkg/services/workers/internal/services/workstations/executor -count=1` — passed.
  - `go test ./tests/functional/factory/packaged/catalog -count=1` — passed.
  - `go test ./pkg/wire -run 'TestProvidePackagedFactoryDefinitions_LoadsDetachedGeneratedCatalog|TestPackagedFactoryCatalogConsumption_ListResolveInstallUsesPublishedCatalog' -count=1` — passed.
  - `make packaged-factory-source-check`, `make packaged-factory-catalog-check`, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - The AGY adapter compares the echoed structured schema, so historical structured traces can only replay against a JSON-equivalent authored schema; stricter constraints need a newly recorded trace or a separate runtime contract.
  - The generic provider retry policy can issue three attempts for schema/provider failures; failure assertions should prove terminal routing and diagnostics without assuming one command call.
  - The next story should document the optional positional/stdin reachability bindings alongside the named flags so the public help and operator examples explain both invocation paths.
---
## 2026-08-09 21:52 -07:00 - wo-b2-agy-production-review-roles-002 verification follow-up
- What was implemented
  - No additional product change; rechecked the committed clip-QA story through the broad fast verification tier and live generated invocation help.
- Files changed
  - `progress.txt`
- Verification
  - `make verify-fast` — passed dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `go run ./cmd/factory run --named @you/agy-clip-qa --help` with isolated `HOME` and `USERPROFILE` — passed and exposed `--clip-path`, `--shot-specification`, positional clip-path, and stdin shot-specification bindings.
  - The worktree was clean before this append-only verification entry; story 003 remains the only PRD story with `passes:false`.
- **Learnings for future iterations:**
  - A generated named-Factory help smoke is useful evidence that authored external names and binding fallbacks survived materialization; keep it alongside the behavioral root-process test when documenting the role.
---
## 2026-08-09 22:06 -07:00 - wo-b2-agy-production-review-roles-003
- What was implemented
  - Documented @you/agy-clip-qa and @you/agy-cold-watch in the canonical packaged-factory guide with exact live-help signatures, required inputs, pinned ANTIGRAVITY/gemini-3.6-flash-high/8m policy, result contracts, workspace path rules, failure boundaries, and PowerShell-safe invocations.
  - Added docs/examples/agy-production-review.md showing clip QA after clip creation, mechanical checks before assembly, cold watch after completed-cut assembly, and distinct pass, reroll, and failed routes; linked it from docs/examples/README.md.
  - Corrected the authored cold-watch cutPath parameter to required and regenerated its YAML, JSON, and manifest artifacts with make packaged-factory-catalog-generate.
  - Updated customer-facing catalog counts, authoring-guide entries, and the CLI docs smoke contract; split the AGY functional test into focused helpers so the backend-size gate remains green.
- Files changed
  - README.md
  - docs/examples/README.md
  - docs/examples/agy-production-review.md
  - docs/reference/authoring-factories.md
  - docs/reference/packaged-factories.md
  - packages/packaged-factories/factories/agy-cold-watch/factory.yaml
  - packages/packaged-factories/generated/factories/agy-cold-watch/factory.yaml
  - packages/packaged-factories/generated/factories/agy-cold-watch/factory.json
  - packages/packaged-factories/generated/manifest.json
  - tests/functional/providers/agy/production_review_test.go
  - tests/functional/smoke/cli_docs_smoke_test.go
  - prd.json
  - progress.txt
- Verification
  - go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1 — passed all cold-watch video/audio, missing-file, clip-QA pass, schema-invalid, provider-failure, and missing-file subcases.
  - go run ./cmd/factory --json factory config validate packages/packaged-factories/factories/agy-cold-watch and .../agy-clip-qa — both passed.
  - Isolated live help showed required --cut-path for cold watch and required positional/--clip-path plus --shot-specification for clip QA.
  - make packaged-factory-catalog-check, make packaged-factory-source-check, make docs-reference-smoke, make readme-check, make verify-fast, and make pkg-structure — passed.
  - make lint passed UI lint/dead-code, go vet, backend-size, pkg-maint, and pkg-file-count checks, but stops at the unchanged current-main pkg-boundary migration baseline; no listed baseline violation touches this story's files. No live AGY smoke was enabled.
- **Learnings for future iterations:**
  - The live help contract catches authored requiredness drift that static role prose can hide; verify required parameters after catalog generation.
  - The backend-size checker enforces a 100-line function limit, so large functional scenario tables should delegate each observable route to focused test helpers.
  - A generated catalog can be current while operator docs are stale; keep catalog counts, reference entries, help evidence, and repository-owned examples synchronized.
---
## 2026-08-09 22:16 -07:00 - wo-b2-agy-production-review-roles-003 CI follow-up
- What was implemented
  - Updated the repository-owned packaged-factory consumer contract to expect the expanded 17-Factory catalog and its 37 verified invocation specifiers.
- Files changed
  - `scripts/packaged-factories-package-consumer.test.mjs`
  - `progress.txt`
- Verification
  - `make packaged-factory-package-verify` — passed catalog freshness and all 38 package-script subtests (37 passed, 1 platform-expected symlink skip).
- **Learnings for future iterations:**
  - Adding public packaged Factories also expands the candidate consumer's manifest-derived invocation inventory; update its exact-count assertions in the same change and run the package verification target locally.
---
## 2026-08-09 22:24 -07:00 - wo-b2-agy-production-review-roles-003 CI follow-up
- What was implemented
  - Added a positional `cutPath` binding to @you/agy-cold-watch so the required file input remains reachable from ACP's single-text-prompt contract while preserving the named `--cut-path` CLI route.
  - Regenerated the cold-watch YAML/JSON and packaged-factory manifest artifacts.
- Files changed
  - `packages/packaged-factories/factories/agy-cold-watch/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-cold-watch/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-cold-watch/factory.json`
  - `packages/packaged-factories/generated/manifest.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/catalog -run '^TestEveryPackagedFactoryIsInvocableFromASingleTextPrompt$' -count=1` — passed.
  - `go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1` — passed.
  - `make packaged-factory-catalog-check` and `make packaged-factory-package-verify` — passed; all 38 package-script subtests passed except the expected Windows symlink-creation skip.
- **Learnings for future iterations:**
- Required named file arguments still need a positional or STDIN binding when the packaged Factory participates in ACP single-text-prompt invocability; named CLI help alone does not satisfy that contract.
---
## 2026-08-09 23:04 -0700 - wo-b3-provider-capability-discovery-cli-003
- What was implemented
  - Registered the authored `you providers list` command in the generated CLI manifest and canonical root command tree, injected the Providers-owned adapter through `pkg/wire`, documented the discovery fields and operational facts, and added root.BuildProcess/Process.Execute human/JSON proof with deterministic ordering, usage failure, and zero provider-command calls.
  - Reconciled the branch with current `main`, regenerated CLI and approved contract artifacts, refreshed command-input/help baselines, and merged the current docs smoke expectations with the provider discovery markers.
- Files changed
  - CLI manifest and generated command artifacts; CLI root/wire composition and Providers command adapter; root/help, manifest, baseline, and functional tests; published CLI contract; `docs/reference/providers.md` and docs smoke markers; functional scenario/evidence registries; provider catalog maintainability helpers and package-count baseline; current-main merge artifacts.
- Verification
  - `make test`, `make wire-smoke`, `make contracts-check`, `make cli-manifest-check`, `make docs-reference-smoke`, `make test-root-process-acceptance`, `make typecheck`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` pass.
  - The umbrella `make lint` still reports the repository's existing `pkg-boundary` migration inventory (176 violations); `make test-contract` still references the absent `pkg/services/factory_definitions/contracts/contracttests` directory.
- **Learnings for future iterations:**
  - Empty authored CLI `sideEffects` must be omitted when the canonical generator represents it as nil; otherwise manifest drift checks fail.
  - When rebasing or merging a public CLI change onto a moving `main`, regenerate command baselines and approved contract hashes from the combined source instead of selecting one side of generated conflicts.
  - The discovery proof can assert no live provider call by injecting a counting command runner through `serviceedges.Edges` and checking it remains at zero after successful and usage-failure executions.
---
## 2026-08-09 23:18 -07:00 - wo-b2-agy-production-review-roles-001 review follow-up
- What was implemented
  - Addressed the blocking cold-watch review finding by adding the provider-neutral `markdown-observation-report/v1` workstation contract. A successful response now requires explicit inspection status, exact chronology/defect/audio/speech/recommendation sections, timestamps for observed timelines, named audio content, and exactly one `Recommendation: pass` or `Recommendation: reroll` line; the old `stopToken: speech` substring acceptance is gone.
  - Put the contract on the authored public Workstation schema and synchronized the OpenAPI bundle, Go/TypeScript clients, packaged contract artifacts, authored/generated cold-watch Factory, and runtime interpolation/layout/merge paths.
  - Extended root.BuildProcess/Process.Execute coverage with a complete report acceptance case, real video and ground-truth traces that retain picture/audio evidence but fail as incomplete reports, and the exit-zero missing-file refusal that fails without a recommendation.
- Files changed
  - `api/components/schemas/data-models/Workstation.yaml`
  - `api/openapi.yaml`
  - `contracts/config/factory.schema.json`
  - `packages/api/generated/manifest.json`
  - `packages/api/generated/openapi/openapi.yaml`
  - `packages/api/generated/schemas/factory.schema.json`
  - `packages/api/generated/schemas/factory-event.schema.json`
  - `packages/api/generated/schemas/factory-recording.schema.json`
  - `packages/packaged-factories/factories/agy-cold-watch/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-cold-watch/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-cold-watch/factory.json`
  - `packages/packaged-factories/generated/manifest.json`
  - `packages/packaged-factories/schemas/factory.schema.json`
  - `packages/packaged-factories/schemas/factory.schema.yaml`
  - `pkg/services/factory_definitions/internal/contracts/factory_config.go`
  - `pkg/services/factory_definitions/internal/services/authoring_layout/authoredlayout/writer.go`
  - `pkg/services/factory_definitions/internal/services/compilation/loading/loader.go`
  - `pkg/services/factory_definitions/internal/services/compilation/runtimeconfig/merge.go`
  - `pkg/services/factory_definitions/internal/services/invocation_policy/invocationinterpolation/service.go`
  - `pkg/services/workers/execution_requests.go`
  - `pkg/services/workers/internal/services/workstations/executor/helpers.go`
  - `pkg/services/workers/internal/services/workstations/executor/workstation_executor.go`
  - `pkg/services/workers/internal/services/workstations/executor/response_contract_test.go`
  - `pkg/transports/http/client/client.gen.go`
  - `pkg/transports/http/generated/server.gen.go`
  - `pkg/transports/mapping/factoryconfig/authored/agents_config.go`
  - `pkg/transports/mapping/factoryconfig/factory_config_mapping.go`
  - `pkg/transports/mapping/factoryconfig/factory_config_mapping_internal.go`
  - `tests/functional/providers/agy/production_review_test.go`
  - `ui/src/api/generated/openapi.ts`
- Verification
  - `go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1 -v -timeout 900s` — passed complete-contract acceptance, both incomplete real-trace rejections, missing-file refusal, and existing clip-QA cases.
  - `go test ./pkg/services/workers/internal/services/workstations/executor -run 'TestValidateMarkdownObservationReport|TestWorkstationExecutor_ModelWorkstationUsesCanonicalWorkstationRuntimeFields' -count=1` — passed.
  - `go test ./pkg/services/factory_definitions/... -count=1` — passed.
  - `go test ./pkg/transports/mapping/factoryconfig/... -count=1` — passed.
  - `go test ./pkg/transports/http/... ./tests/functional/transport/http/server -count=1` — passed.
  - `go run ./cmd/factory --json factory config validate packages/packaged-factories/factories/agy-cold-watch` — passed.
  - `make packaged-factory-source-check`, `make packaged-factory-catalog-check`, `make contracts-generate`, `make contracts-check`, and `make docs-reference-smoke` — passed.
  - `make ui-deps` remains blocked by the pre-existing checkout state where multiple `ui/packages/*` directories are deleted; generated UI OpenAPI was refreshed with the repository's pinned openapi-typescript/Biome versions through the standalone generator path.
- **Learnings for future iterations:**
  - A Markdown contract needs a post-worker validation hook; AGY stream success and a stop-token substring are insufficient evidence of a complete review.
  - Keep provider-neutral workstation contract fields flowing through the authored API schema, mapping, layout expansion, invocation interpolation, runtime merge, and generated consumer artifacts together.
  - Existing “real” recordings can be valuable negative fixtures when their prose predates a stricter production contract; pair them with a minimal complete provider envelope so the acceptance path is also observable.
---
## 2026-08-09 23:22 -0700 - wo-b3-provider-capability-discovery-cli-ci-coverage-follow-up
- What was implemented
  - Addressed fresh-main CI failures by moving the provider discovery functional proof to `tests/functional/internal/support.BuildProcess`, satisfying the functional composition boundary while retaining injected command-runner evidence.
  - Added behavioral projection cases for invalid provider/model/modality/tool/known-limit facts and Cobra handler/show-output tests covering inherited global inputs, diagnostics, dependency/input errors, and prerequisite JSON/human parity.
- Files changed
  - `tests/functional/providers/discovery/discovery_test.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/projection_test.go`
  - `pkg/services/providers/transports/cli/command_handler_test.go`
  - `pkg/services/providers/transports/cli/show_capability_output_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make pkg-maint` — passed.
  - `make functional-boundary-check` — passed.
  - `go test ./pkg/services/providers/internal/services/catalog/internal/service ./pkg/services/providers/transports/cli ./tests/functional/providers/discovery/... -count=1` — passed.
  - Direct package coverage: catalog 92.5% against 89.47%; Providers CLI 82.6% against 82.00%.
  - Targeted `gocoveragecheck` for both affected packages — passed at 88.1% total against 75.9%.
  - The full `make test-unit-coverage` wrapper could not launch on this Windows host because its generated all-package `go test` command exceeds the Windows argument-length limit.
- **Learnings for future iterations:**
  - Current functional-boundary checks require `support.BuildProcess` plus exact `serviceedges.Edges` replacements for provider functional composition; direct `pkg/root` imports are rejected even when the test injects all external effects.
  - Coverage manifests enforce per-package floors, so added catalog/CLI branches need observable behavioral tests; lowering the checked-in floor would hide the regression.
---
## 2026-08-09 23:36 -07:00 - wo-b2-agy-production-review-roles-002 review follow-up
- What was implemented
  - Added the explicit `structured-clip-qa/v1` workstation contract for `@you/agy-clip-qa`, enforcing all required fields, finite confidence in `[0,1]`, pass invariants, and meaningful inspected rerolls at the shared runtime boundary used by `AGENT_RUN`.
  - Changed structured-output failure detection to inspect `status`, `outcome`, and `success` failure markers before accepting a reroll, reject unscoped structured rerolls, and preserve valid clip-QA rerolls.
  - Added executor and root-built offline functional coverage for out-of-range confidence, inconsistent pass objects, empty-reason rerolls, mixed provider/status failures, an accepted reroll, and the existing recorded pass/missing-file/schema/provider failures.
  - Regenerated the clip-QA authored distribution artifact and manifest through `make packaged-factory-catalog-generate`; marked story 002 complete in `prd.json`.
- Files changed
  - `packages/packaged-factories/factories/agy-clip-qa/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-clip-qa/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-clip-qa/factory.json`
  - `packages/packaged-factories/generated/manifest.json`
  - `pkg/services/workers/internal/services/workstations/executor/agent.go`
  - `pkg/services/workers/internal/services/workstations/executor/agent_inference_test.go`
  - `pkg/services/workers/internal/services/workstations/executor/agent_test.go`
  - `pkg/services/workers/internal/services/workstations/executor/helpers.go`
  - `pkg/services/workers/internal/services/workstations/executor/response_contract_test.go`
  - `tests/functional/providers/agy/production_review_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/workers/internal/services/workstations/executor/... -count=1` — passed.
  - `go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1 -v -timeout 300s` — passed, including accepted reroll and all semantic/mixed-failure rejection cases.
  - `make packaged-factory-catalog-check` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - `AGENT_RUN` workstations bypass the model-only `AgentExecutor`; provider-neutral output contracts must be enforced at the shared `WorkstationExecutor` boundary so both agent-run and inference paths receive the same semantic validation.
  - A structured reroll is only a successful inspection verdict when its explicit contract is present; check provider/status failure markers before interpreting domain-level reroll fields.
  - Recorded AGY traces can be reused for synthetic semantic variants by changing only the terminal structured output while preserving the echoed provider schema and stream envelope.
---
## 2026-08-09 23:37 -0700 - wo-b3-provider-capability-discovery-cli-ci-rerun
- What was implemented
  - Investigated the fresh-head functional coverage failure in the unchanged packaged-loop lane; the exact `TestPackagedLoopUsesInvocationDurationAndSkipsOverlap` test passed through 20 local repetitions, so no unrelated production fix was introduced.
  - Reran the failed GitHub Actions job and retained the provider discovery implementation and coverage fixes as the only source changes.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/loop -run '^TestPackagedLoopUsesInvocationDurationAndSkipsOverlap$' -count=20 -v -timeout 900s` — passed all 20 runs.
  - `gh run rerun 31361833511 --failed --repo portpowered/you-agent-factory` — attempt 2 passed Backend Functional Coverage and Verification Policy.
  - All 18 required PR checks are terminal and passing on head `24a966c18`; PR #1843 is mergeable with no comments or formal reviews.
- **Learnings for future iterations:**
  - A single timeout in the unchanged packaged-loop functional lane should be reproduced in isolation before changing feature code; a stable local repetition supports rerunning the failed CI job instead.
  - When the rerun has no new source changes, record the exact test and terminal check state in the append-only progress log so the PR handoff remains auditable.
---
## 2026-08-09 23:47 -0700 - wo-b3-provider-capability-discovery-cli-delivery-review
- What was implemented
  - Confirmed all three PRD stories remain marked `passes:true`; no additional story implementation or authored contract change was required.
  - Rechecked PR #1843 in `portpowered/you-agent-factory`: the local head `c57a47de5012033c60a212a41873e2975f8cc266` matches the PR head, GitHub reports the PR mergeable, and the PR description remains the current `prd.json` content.
  - Used the thread-aware GitHub review fetch to confirm there …6400 tokens truncated…he bookkeeping update.
---
## 2026-08-09 23:47 -0700 - wo-b3-provider-capability-discovery-cli-delivery-review
- What was implemented
  - Confirmed all three PRD stories remain marked `passes:true`; no additional story implementation or authored contract change was required.
  - Rechecked PR #1843 in `portpowered/you-agent-factory`: the local head `c57a47de5012033c60a212a41873e2975f8cc266` matches the PR head, GitHub reports the PR mergeable, and the PR description remains the current `prd.json` content.
  - Used the thread-aware GitHub review fetch to confirm there are no conversation comments, formal reviews, or unresolved inline review threads.
- Files changed
  - `progress.txt`
- Verification
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 required checks pass.
  - `git status --short --branch` — clean before this progress-only append.
- **Learnings for future iterations:**
  - For a cross-repository PR whose local branch is associated with a different default remote, pass the PR head repository explicitly to `gh` and the thread-aware review fetch instead of relying on branch auto-discovery.
  - When all stories, checks, and review threads are complete, preserve `prd.json` and append the delivery evidence to `progress.txt`; no source churn is needed.
---
## 2026-08-10 00:05 -07:00 - wo-b2-agy-production-review-roles-003 review follow-up
- What was implemented
  - Corrected the cold-watch reference signature to document the required `<cut-path>` positional binding and its equivalent `--cut-path` named binding, matching the generated authored catalog and live CLI help.
  - Marked the final production-composition story complete after confirming the catalog, worked example, failure boundaries, validation guidance, and offline behavioral coverage are synchronized.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go run ./cmd/factory run --named @you/agy-cold-watch --help` — passed; exposed `positional 1 <cut-path> | --cut-path <file-path>`.
  - `go run ./cmd/factory run --named @you/agy-clip-qa --help` — passed; exposed the documented positional/`--clip-path` and `--shot-specification` inputs.
  - `go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1` — passed.
  - `make packaged-factory-catalog-check` and `make docs-reference-smoke` — passed.
  - `make verify-fast` — attempted but stopped at dashboard typecheck because the pre-existing checkout lacks `@types/bun` and all `ui/packages/*` workspace sources; `make ui-deps` cannot resolve those missing workspaces. Earlier complete-checkout verification for this PR head passed `verify-fast`, and no UI files are part of this follow-up.
- **Learnings for future iterations:**
  - Treat generated named-Factory help as the source of truth when documenting positional and named bindings; inspect both the authored binding and live help before marking a docs story complete.
---
## 2026-08-10 00:17 -0700 - wo-b3-provider-capability-discovery-cli-review-follow-up
- What was implemented
  - Addressed the blocking PR conversation feedback on provider capability truthfulness. The authored Codex manifest now publishes the exact `gpt-5.6`, `gpt-5.6-luna`, `gpt-5.6-sol`, and `gpt-5.6-terra` model IDs with shared effort and modality facts.
  - Changed AGY discovery to publish explicit empty per-model effort arrays because AGY effort is encoded in exact model IDs and the canonical Providers execution path rejects a separate `ReasoningEffort`; added the named `effort_selection` limit alongside `--add-dir` and `print_timeout`.
  - Added a behavioral root-service regression that lists the catalog and executes every advertised AGY model/effort combination through the canonical execution policy, so a future rejected advertised effort fails at runtime rather than only through a source/inventory assertion.
  - Updated human/JSON functional and CLI output assertions, catalog clone/order tests, provider reference guidance, PRD notes, and regenerated the published provider catalog/package metadata from authored manifests.
- Files changed
  - `packages/model-providers/providers/codex/provider.ya…6179 tokens truncated…terations:**
  - A progress-only commit can expose unrelated resource and timing flakiness; inspect the exact failing test and compare changed paths before editing feature code, then rerun only failed jobs when the isolated evidence supports it.
  - Record both the primary failure and any missing downstream artifact so CI diagnosis does not mistake the upload symptom for the test cause.
## 2026-08-10 00:24 -07:00 - wo-b2-agy-production-review-roles review follow-up
- What was implemented
  - Addressed the latest blocking PR feedback by encoding `confidence` `minimum: 0` and `maximum: 1` in the authored clip-QA schema and regenerating the packaged catalog artifacts.
  - Refactored structured clip-QA validation into focused decoding, shape, confidence, pass, and reroll helpers; consolidated the contract tests into the existing workstation test file so `pkg-file-count` remains within the 15-file limit.
  - Updated both production-composition PowerShell parsers to consume the actual `## Overall recommendation` plus `Recommendation: pass|reroll` contract, including CRLF-safe matching; the parser was behaviorally checked for pass, reroll, and missing-recommendation cases.
  - Kept the historical AGY clip-QA response/media evidence while synchronizing only its echoed schema metadata in the offline replay harness, and asserted the bounded confidence fields on the provider command.
- Files changed
  - `docs/examples/agy-production-review.md`
  - `docs/reference/packaged-factories.md`
  - `packages/packaged-factories/factories/agy-clip-qa/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-clip-qa/factory.yaml`
  - `packages/packaged-factories/generated/factories/agy-clip-qa/factory.json`
  - `packages/packaged-factories/generated/manifest.json`
  - `pkg/services/workers/internal/services/workstations/executor/helpers.go`
  - `pkg/services/workers/internal/services/workstations/executor/response_contract_test.go` (consolidated into `workstation_executor_test.go`)
  - `pkg/services/workers/internal/services/workstations/executor/workstation_executor_test.go`
  - `tests/functional/providers/agy/production_review_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/workers/internal/services/workstations/executor -count=1` — passed.
  - `go test ./tests/functional/providers/agy -run '^TestAgyProductionReviewRolesThroughRootBuildProcess$' -count=1 -v -timeout 900s` — passed.
  - `make pkg-maint`, `make pkg-file-count`, `make packaged-factory-catalog-check`, and `make docs-reference-smoke` — passed.
  - `git diff --check` — passed.
  - `make verify-fast` — blocked at the pre-existing dashboard typecheck baseline: missing `@types/bun`, TypeScript 6 `baseUrl` deprecation errors, and deleted `ui/packages/*` workspace sources; no UI files are part of this follow-up.
- **Learnings for future iterations:**
  - Documentation parsers for Markdown output must match the report’s structural contract and tolerate the platform’s CRLF line endings; a single-line `Overall recommendation:` pattern is not equivalent to the emitted H2-plus-body form.
  - AGY structured-output replays enforce exact echoed-schema equivalence, so a contract schema change requires the historical replay envelope to be synchronized without changing its recorded response evidence.
  - When a backend package is at the 15-file limit, consolidate focused behavioral tests into an existing responsibility-aligned file before adding another package-local test file.
---

## 2026-08-10 05:51 -07:00 - e2-structured-worker-results-001
- What was implemented
  - Added optional native `structuredResult` data with an explicit presence bit so schema-valid JSON null remains distinct from an omitted result; raw worker output remains available.
  - Validated workstation `outputSchema` before provider execution, decoded conforming object, array, scalar, boolean, and null responses once with `jsonschema/v6`, and returned detached native JSON. Invalid configuration now returns typed terminal misconfiguration metadata before a provider call.
  - Carried the value through Work and WorkResult contracts, canonical dispatch events, runtime tokens, live projections, persisted snapshots, replay, session mutation clones, Work reads, HTTP mappings, and regenerated Go/TypeScript/publishable interfaces.
  - Added focused execution, event serialization, projection, replay, checkpoint/token, API mapping, and factory-definition validation coverage; moved policy-free JSON ownership helpers into `pkg/platform/jsonvalue` to preserve package boundaries.
- Files changed
  - `api/components/schemas/data-models/Work.yaml`
  - `api/components/schemas/events/payloads/DispatchResponseEventPayload.yaml`
  - `pkg/platform/jsonvalue/jsonvalue.go`
  - `pkg/services/work/**`, `pkg/services/workers/**`, `pkg/services/factory_runtime/**`, `pkg/services/factory_sessions/**`, `pkg/services/recordings/**`, and `pkg/services/factory_definitions/**` structured-result contracts, mappings, clone paths, validation, executors, and tests
  - Generated API, package, and UI client artifacts
  - `prd.json`, `progress.txt`
- Verification
  - `make interfaces-all` — passed.
  - `make verify-fast` and `make typecheck` — passed.
  - Affected service families with `go test ... -count=1` — passed, including recordings replay/projections and all workstation executor subpackages.
  - `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure` — passed.
  - `git diff --check` — passed.
  - `make lint` reaches the existing `pkg-boundary` migration baseline (176 violations, including the unchanged Petri/provider ownership entries); the changed files introduce no new boundary entry.
- **Learnings for future iterations:**
  - `jsonschema.UnmarshalJSON` uses `json.Number`; preserving that type avoids numeric precision/type drift across durable event and checkpoint boundaries.
  - Optional JSON fields need both a native value and a presence bit when JSON null must survive custom marshaling and generated `omitempty` mappings.
  - Shared clone/marshal mechanics belong in `pkg/platform` rather than adding exported utility functions to a service root; `pkg-structure` enforces that boundary.
- Static output-schema validation must leave invocation interpolations and existing legacy `.json` schema references for their later resolution path while validating inline schemas immediately.
---

## 2026-08-10 06:15 -07:00 - e2-structured-worker-results-002
- What was implemented
  - Exposed detached native `StructuredResult` values in workstation `TokenData`, preserving nested objects, arrays, scalars, and explicit JSON null presence without falling back to payload or artifact text.
  - Added prompt-contract metadata for optional `.Inputs[n].StructuredResult` access and a runtime AST guard that reports an input-specific diagnostic when a template uses an absent upstream result.
  - Added focused prompt-renderer and contract tests for nested object/array access, detached ownership, and missing-result behavior.
  - Added a root `BuildProcess`/`Process.Execute` functional test proving a schema-valid upstream response reaches the actual downstream provider prompt.
- Files changed
  - `pkg/services/workers/internal/services/workstations/prompting/prompt.go`
  - `pkg/services/workers/internal/services/workstations/prompting/prompt_contract.go`
  - `pkg/services/workers/internal/services/workstations/prompting/prompt_contract_structured_result.go`
  - `pkg/services/workers/internal/services/workstations/prompting/prompt_docs.go`
  - `pkg/services/workers/internal/services/workstations/prompting/prompt_test.go`
  - `pkg/services/workers/internal/services/workstations/prompting/prompt_contract_test.go`
  - `tests/functional/workstations/execution/structured_result_prompt_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/workers/internal/services/workstations/prompting -count=1` — passed.
  - `go test ./pkg/services/workers/... -count=1` — passed.
  - `go test -tags functionallong ./tests/functional/workstations/execution -run '^TestStructuredResultFlowsIntoDownstreamPromptThroughRootBuildProcess$' -count=1 -v -timeout 120s` — passed.
  - `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - `make typecheck` and `make verify-fast` — passed, including the short UI and Go suites.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The runtime token already carried structured results; the prompt `TokenData` projection was the boundary where downstream templates lost them.
  - Optional-result availability needs a separate prompt-render validation pass because the general template contract cannot know which runtime inputs contain a result.
  - Functional Codex JSON responses must not include a configured worker stop token when the response is schema JSON; use a schema-only worker fixture so the JSON can be parsed as the complete result.
---

## 2026-08-10 06:38 - e2-structured-worker-results-003
- What was implemented
  - Classified malformed JSON and schema-invalid JSON from schema-bound workstations as terminal, non-retryable `structured_output_schema_violation` failures while retaining raw output for diagnostics and omitting rejected structured values.
  - Added the public OpenAPI failure enum and regenerated Go, TypeScript, and packaged schema consumers; updated canonical event, replay, Factory Session, Work, worker-session, and transport mappings to preserve the reason.
  - Added unit coverage for worker classification, safe validation summaries, terminal policy, event serialization, replay, Factory Session projections, worker-session diagnostics, and public mapping.
  - Added root-built functional coverage for malformed and schema-mismatch responses, configured `onFailure` routing, failed Work projection, public failure detail, raw event output, and the absence of downstream dispatches.
- Files changed
  - `api/components/schemas/events/WorkFailureType.yaml`, generated API/UI/package contract artifacts, worker execution and failure mappings, event/replay/session projection tests, and `tests/functional/workstations/execution/structured_result_prompt_test.go`.
- Verification
  - `make interfaces-all` — passed before the focused test pass.
  - Focused worker, recordings/replay, orchestration, session, mapping, and HTTP contract tests — passed.
  - `go test -tags=functionallong ./tests/functional/workstations/execution -run 'TestStructured(ResultFlowsIntoDownstreamPromptThroughRootBuildProcess|OutputSchemaViolationRoutesFailureWithoutDownstreamDispatch)$' -count=1 -timeout=180s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract check, 355 UI files / 2,970 tests, 191 Bun tests, and the short Go suite.
  - Commit: `a4bb9bd64` (`classify structured output schema violations`).
- **Learnings for future iterations:**
  - Schema validation errors from `jsonschema/v6` already provide safe property or instance-location summaries; prefix them with a stable classification message without appending raw provider output.
  - The root-built functional command-runner edge makes terminal routing observable through public Factory Events and Work listings; assert request count to prove a failure cannot enter the next workstation.
---

## 2026-08-10 07:20 -07:00 - e2-structured-worker-results-004
- What was implemented
  - Exposed optional native `structuredResult` values in `you work list --json` and Work show JSON while preserving explicit JSON null and omitting older or unstructured fields.
  - Added deterministic compact structured-result rendering to human Work list output.
  - Extended the canonical Work watch reducer to consume non-projecting dispatch responses, hand off each result by Work ID to the first following state transition, preserve JSON types and null, omit failures/unrelated transitions, and keep replay/reconnect ordering and one-line NDJSON framing intact.
  - Added focused CLI, stream/reducer, contract, and replay-backed functional coverage for objects, arrays, strings, booleans, null, omission, framing, ordering, failed dispatches, and recorded output.
  - Documented `outputSchema`, `.Inputs[n].StructuredResult`, typed schema violations, and artifact-free list/watch inspection in the public Work and Workstation references.
  - Regenerated the authored CLI published projection and refreshed intentional CLI/docs baseline fixtures.
- Files changed
  - Work CLI list/show/watch transports and reducer tests
  - `contracts/cli/commands.json`, generated API-package CLI metadata, CLI family metadata, and baseline fixtures
  - `docs/reference/work.md` and `docs/reference/workstations.md`
  - Replay-backed Work watch functional tests
  - `prd.json` and `progress.txt`
- Verification
  - `go test ./pkg/services/work/... ./pkg/transports/cli/... ./tests/functional/work/... -count=1 -timeout 300s` — passed.
  - `make cli-manifest-check`, `make contracts-check`, and `make docs-reference-smoke` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun tests, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Work watch emits only canonical `WORK_STATE_CHANGE` lines, so dispatch-result events need a short-lived per-Work pending handoff keyed by canonical Work ID.
 - The published CLI metadata is generated from `contracts/cli/commands.json` through `make contracts-generate`; intentional command-description changes also require the CLI identity baseline refresh.
 - Replay fixtures are a useful end-to-end assertion for watch behavior because they exercise SSE decoding, event ordering, reducer state, and output framing together.
---
## 2026-08-10 04:32 -07:00 - sac-s0-2-pkg-boundary-ignores-001
## 2026-08-09 23:04 -0700 - wo-b3-provider-capability-discovery-cli-003
- What was implemented
  - Registered the authored `you providers list` command in the generated CLI manifest and canonical root command tree, injected the Providers-owned adapter through `pkg/wire`, documented the discovery fields and operational facts, and added root.BuildProcess/Process.Execute human/JSON proof with deterministic ordering, usage failure, and zero provider-command calls.
  - Reconciled the branch with current `main`, regenerated CLI and approved contract artifacts, refreshed command-input/help baselines, and merged the current docs smoke expectations with the provider discovery markers.
- Files changed
  - CLI manifest and generated command artifacts; CLI root/wire composition and Providers command adapter; root/help, manifest, baseline, and functional tests; published CLI contract; `docs/reference/providers.md` and docs smoke markers; functional scenario/evidence registries; provider catalog maintainability helpers and package-count baseline; current-main merge artifacts.
- Verification
  - `make test`, `make wire-smoke`, `make contracts-check`, `make cli-manifest-check`, `make docs-reference-smoke`, `make test-root-process-acceptance`, `make typecheck`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` pass.
  - The umbrella `make lint` still reports the repository's existing `pkg-boundary` migration inventory (176 violations); `make test-contract` still references the absent `pkg/services/factory_definitions/contracts/contracttests` directory.
- **Learnings for future iterations:**
  - Empty authored CLI `sideEffects` must be omitted when the canonical generator represents it as nil; otherwise manifest drift checks fail.
  - When rebasing or merging a public CLI change onto a moving `main`, regenerate command baselines and approved contract hashes from the combined source instead of selecting one side of generated conflicts.
  - The discovery proof can assert no live provider call by injecting a counting command runner through `serviceedges.Edges` and checking it remains at zero after successful and usage-failure executions.
---
## 2026-08-09 23:22 -0700 - wo-b3-provider-capability-discovery-cli-ci-coverage-follow-up
- What was implemented
  - Addressed fresh-main CI failures by moving the provider discovery functional proof to `tests/functional/internal/support.BuildProcess`, satisfying the functional composition boundary while retaining injected command-runner evidence.
  - Added behavioral projection cases for invalid provider/model/modality/tool/known-limit facts and Cobra handler/show-output tests covering inherited global inputs, diagnostics, dependency/input errors, and prerequisite JSON/human parity.
- Files changed
  - `tests/functional/providers/discovery/discovery_test.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/projection_test.go`
  - `pkg/services/providers/transports/cli/command_handler_test.go`
  - `pkg/services/providers/transports/cli/show_capability_output_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make pkg-maint` — passed.
  - `make functional-boundary-check` — passed.
  - `go test ./pkg/services/providers/internal/services/catalog/internal/service ./pkg/services/providers/transports/cli ./tests/functional/providers/discovery/... -count=1` — passed.
  - Direct package coverage: catalog 92.5% against 89.47%; Providers CLI 82.6% against 82.00%.
  - Targeted `gocoveragecheck` for both affected packages — passed at 88.1% total against 75.9%.
  - The full `make test-unit-coverage` wrapper could not launch on this Windows host because its generated all-package `go test` command exceeds the Windows argument-length limit.
- **Learnings for future iterations:**
  - Current functional-boundary checks require `support.BuildProcess` plus exact `serviceedges.Edges` replacements for provider functional composition; direct `pkg/root` imports are rejected even when the test injects all external effects.
  - Coverage manifests enforce per-package floors, so added catalog/CLI branches need observable behavioral tests; lowering the checked-in floor would hide the regression.
---
## 2026-08-09 23:37 -0700 - wo-b3-provider-capability-discovery-cli-ci-rerun
- What was implemented
  - Investigated the fresh-head functional coverage failure in the unchanged packaged-loop lane; the exact `TestPackagedLoopUsesInvocationDurationAndSkipsOverlap` test passed through 20 local repetitions, so no unrelated production fix was introduced.
  - Reran the failed GitHub Actions job and retained the provider discovery implementation and coverage fixes as the only source changes.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/loop -run '^TestPackagedLoopUsesInvocationDurationAndSkipsOverlap$' -count=20 -v -timeout 900s` — passed all 20 runs.
  - `gh run rerun 31361833511 --failed --repo portpowered/you-agent-factory` — attempt 2 passed Backend Functional Coverage and Verification Policy.
  - All 18 required PR checks are terminal and passing on head `24a966c18`; PR #1843 is mergeable with no comments or formal reviews.
- **Learnings for future iterations:**
  - A single timeout in the unchanged packaged-loop functional lane should be reproduced in isolation before changing feature code; a stable local repetition supports rerunning the failed CI job instead.
  - When the rerun has no new source changes, record the exact test and terminal check state in the append-only progress log so the PR handoff remains auditable.
---
## 2026-08-09 23:47 -0700 - wo-b3-provider-capability-discovery-cli-delivery-review
- What was implemented
  - Confirmed all three PRD stories remain marked `passes:true`; no additional story implementation or authored contract change was required.
  - Rechecked PR #1843 in `portpowered/you-agent-factory`: the local head `c57a47de5012033c60a212a41873e2975f8cc266` matches the PR head, GitHub reports the PR mergeable, and the PR description remains the current `prd.json` content.
  - Used the thread-aware GitHub review fetch to confirm there are no conversation comments, formal reviews, or unresolved inline review threads.
- Files changed
  - `progress.txt`
- Verification
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 required checks pass.
  - `git status --short --branch` — clean before this progress-only append.
- **Learnings for future iterations:**
  - For a cross-repository PR whose local branch is associated with a different default remote, pass the PR head repository explicitly to `gh` and the thread-aware review fetch instead of relying on branch auto-discovery.
  - When all stories, checks, and review threads are complete, preserve `prd.json` and append the delivery evidence to `progress.txt`; no source churn is needed.
---
## 2026-08-10 00:17 -0700 - wo-b3-provider-capability-discovery-cli-review-follow-up
- What was implemented
  - Addressed the blocking PR conversation feedback on provider capability truthfulness. The authored Codex manifest now publishes the exact `gpt-5.6`, `gpt-5.6-luna`, `gpt-5.6-sol`, and `gpt-5.6-terra` model IDs with shared effort and modality facts.
  - Changed AGY discovery to publish explicit empty per-model effort arrays because AGY effort is encoded in exact model IDs and the canonical Providers execution path rejects a separate `ReasoningEffort`; added the named `effort_selection` limit alongside `--add-dir` and `print_timeout`.
  - Added a behavioral root-service regression that lists the catalog and executes every advertised AGY model/effort combination through the canonical execution policy, so a future rejected advertised effort fails at runtime rather than only through a source/inventory assertion.
  - Updated human/JSON functional and CLI output assertions, catalog clone/order tests, provider reference guidance, PRD notes, and regenerated the published provider catalog/package metadata from authored manifests.
- Files changed
  - `packages/model-providers/providers/codex/provider.yaml`
  - `packages/model-providers/providers/antigravity/provider.yaml`
  - `packages/model-providers/generated/catalog.json`
  - `packages/model-providers/metadata/manifest.json`
  - `packages/model-providers/catalog_test.go`
  - `pkg/services/providers/internal/service/service_test.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/service_test.go`
  - `pkg/services/providers/transports/cli/list_capability_output_test.go`
  - `tests/functional/providers/discovery/discovery_test.go`
  - `docs/reference/providers.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make provider-catalog-generate` and `make model-provider-package-generate` — passed.
  - `make provider-catalog-check` and `make model-provider-package-check` — passed.
  - Focused provider/catalog/CLI/functional Go tests — passed.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `make docs-reference-smoke`, `make typecheck`, `make test`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - An empty capability list can be an intentional public contract, not missing metadata; pair it with a named constraint when a provider encodes the choice in another exact identifier.
  - Provider model inventories must enumerate exact IDs used by configured Factories; a family label such as `gpt-5.6` is not a substitute for accepted tier IDs.
  - Catalog/execution consistency regressions should invoke the public service operation with catalog-derived values and an injected adapter, allowing policy drift to fail behaviorally without live provider calls.
---
## 2026-08-10 00:51 -0700 - wo-b3-provider-capability-discovery-cli-ci-follow-up
- What was implemented
  - Diagnosed the first fresh CI failure as two unrelated timing-sensitive functional assertions in session execution/restart coverage; both passed 20 consecutive focused local repetitions.
  - Diagnosed the next functional-lane failure as an existing runtime package coverage-floor miss caused by incomplete/nondeterministic broad functional coverage, not a changed runtime package; local coverage likewise hit an unrelated concurrent dispatch assertion.
  - Reran the failed CI lane through three attempts without changing unrelated runtime code. The final backend functional coverage and dependent Verification Policy jobs passed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/sessions/execution -run '^TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal$' -count=20` — passed.
  - `go test ./tests/functional/sessions/restart -run '^TestFactorySessionResumeDoesNotRepeatCompletedDispatch$' -count=20` — passed.
  - Final `gh pr checks 1843 --repo portpowered/you-agent-factory` — all required checks pass on head `28ddae7e6653ec6272cbc47db88f695d8246b401`.
- **Learnings for future iterations:**
  - The functional coverage gate can fail after a broad concurrent test is incomplete, surfacing as a package coverage-floor regression even when the changed package is unrelated; inspect test output and compare coverage artifacts before touching a baseline.
  - A failed-job rerun retains the same workflow run but creates new job IDs; inspect the job list by attempt when confirming the terminal result.
---
## 2026-08-10 00:55 -0700 - wo-b3-provider-capability-discovery-cli-final-docs-follow-up
- What was implemented
  - Clarified in the canonical provider reference that `models` is an exact per-ID inventory and must not be treated as a family alias.
- Files changed
  - `docs/reference/providers.md`
  - `progress.txt`
- Verification
  - The clarification preserves the catalog/output contract and will be covered by the fresh PR workflow on the final head.
- **Learnings for future iterations:**
  - When a final bookkeeping-only commit does not receive a new check suite, a narrowly scoped user-facing clarification can provide a legitimate synchronization change for the normal pull-request workflow.
---
## 2026-08-10 01:05 -0700 - wo-b3-provider-capability-discovery-cli-base-merge-api-follow-up
- What was implemented
  - Reconciled the PR with the current `you-agent-factory` main branch, preserving the B3 PRD while retaining the base branch's B2 source and generated artifacts.
  - Regenerated the API contract manifest after the merge so the generated CLI-command and OpenAPI artifact hashes match their canonical sources.
- Files changed
  - `packages/api/generated/manifest.json`
  - `progress.txt`
- Verification
  - `make generate-api`, `make contracts-generate`, `make contracts-smoke`, `make api-smoke`, and `make api-package-pack-smoke` — passed.
  - The initial current-main CI run's API manifest failures are resolved locally; a fresh run is pending on the pushed merge head.
- **Learnings for future iterations:**
  - After merging a newer main into a PR, run the contract generator even when the feature does not change the API; generated package manifests can encode hashes for base-branch artifacts changed by the merge.
---
## 2026-08-10 01:18 -0700 - wo-b3-provider-capability-discovery-cli-final-handoff
- What was implemented
  - Completed the current-main merge and generated-contract reconciliation, including the API manifest refresh required by the base branch's newer generated CLI/OpenAPI artifacts.
  - Confirmed the blocking provider-capability conversation is addressed in the authored catalog, generated output, execution-policy regression, and reference documentation.
- Files changed
  - `progress.txt`
- Verification
  - Final CI run `31368861656` completed successfully on head `897830ffaf57a24d2c331641b88e55bfedda6bb4`; all 18 PR checks pass.
  - PR #1843 is open, mergeable, clean, and its description matches `prd.json`; all PRD stories have `passes:true`.
  - Thread-aware review inspection reports zero inline review threads and zero formal reviews; the only conversation item is the addressed top-level blocker.
  - `git status --porcelain` is clean.
- **Learnings for future iterations:**
  - Final readiness requires checking the exact PR head against the terminal CI run, not only the latest successful run visible for an earlier commit.
---
## 2026-08-10 01:31 -0700 - wo-b3-provider-capability-discovery-cli-final-ci-rerun
- What was implemented
  - Confirmed the final-head functional-coverage retry passed after the isolated durability test reproduced cleanly locally.
- Files changed
  - `progress.txt`
- Verification
  - Final-head run `31369496270` now has all 18 checks passing, including Backend Functional Coverage and Verification Policy, on head `20a23e149a812ff835b1996a2deea425910eb337`.
- **Learnings for future iterations:**
  - Preserve the exact failing test, focused repetition result, rerun ID, and final head in the append-only log when CI recovery uses a known flaky lane.
---
## 2026-08-10 01:50 -0700 - wo-b3-provider-capability-discovery-cli-final-review
- What was implemented
  - Confirmed the three PRD stories remain marked `passes:true`; no additional story implementation or authored contract change was required.
  - Inspected PR #1843 through the GitHub connector and the bundled thread-aware `gh` review workflow. The earlier blocking top-level feedback is addressed by the existing provider-policy alignment commits; no inline review threads or formal reviews remain.
  - Confirmed the PR head, fork branch, and local `HEAD` all match `7cb98beaa33dc3cfac028478efa11406eb54dcd2`, the PR is open/mergeable/non-draft, and its 18 reported checks pass.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because every story already passes and the PR description is an exact content match.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - PRD pass-state, PR description parity, thread-aware comment, and all-checks inspection — passed.
- **Learnings for future iterations:**
  - For cross-repository PRs, compare local `HEAD`, the fork branch ref, and the explicit PR head SHA; current-branch auto-discovery can inspect the wrong remote.
  - A top-level review comment may remain in the conversation after its requested fix is present; use commit ancestry plus thread-aware state to distinguish addressed feedback from unresolved inline work.
---
## 2026-08-10 02:05 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Diagnosed the fresh-head `Backend Functional Coverage` failure as an existing nondeterministic package-floor fluctuation: unchanged `dispatch_planning` measured 62.5468% against 62.92%, and unchanged orchestration runtime measured 52.7349% against 52.80%.
  - Confirmed neither failing package is touched by this PR, changed no feature code or coverage baseline, and reran only the failed jobs with `gh run rerun 31371949039 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
  - `prd.json` remains unchanged and all stories remain `passes:true`.
- Verification
  - Retry Backend Functional Coverage job `93404454469` — passed.
  - Retry Verification Policy job `93405593441` — passed.
  - All 18 PR checks are green on head `de6db78a47666ecc94861ae645b18d6635685a29`.
- **Learnings for future iterations:**
  - For functional coverage failures, compare the reported package-floor deltas with the PR file list before changing code; a small drop in untouched packages is evidence for a targeted rerun, not a feature regression.
  - The dependent Verification Policy job may appear only after the rerun lane completes, so final readiness requires checking both the lane and its policy gate.
---
## 2026-08-10 02:09 -0700 - wo-b3-provider-capability-discovery-cli-review-audit
- What was implemented
  - Re-read the PRD and consolidated codebase patterns; all three provider discovery stories remain marked `passes:true`.
  - Audited PR #1843 after the existing blocking conversation item. The current head contains the requested catalog-to-execution-policy reconciliation: exact Codex model IDs, explicit AGY empty effort arrays with model-ID selection guidance, and an observable consistency regression; no inline review threads or formal reviews remain.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and acceptance notes are current.
- Verification
  - Local `HEAD` matches PR #1843 head `843750d423b4cc4ae1ff7f5346a15f1b313d7b8b`; the PR is open and mergeable.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - The fresh PR CI run is still in progress; terminal readiness will be checked after it completes.
- **Learnings for future iterations:**
  - A historical top-level blocker can remain in the merged PR timeline after the requested fix is present; verify the current head against the feedback and inspect thread resolution separately before treating it as unresolved work.
  - When the PR head is hosted in a different repository than the local default remote, resolve and inspect it with the explicit owner/repository and compare its SHA to local `HEAD`.
---
## 2026-08-10 02:43 -0700 - wo-b3-provider-capability-discovery-cli-final-delivery-audit
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three stories remain marked `passes:true`.
  - Verified that the existing PR #1843 already contains the latest provider-policy reconciliation requested by its blocking conversation item: exact Codex model IDs, explicit AGY empty effort arrays with model-ID selection guidance, and the catalog-to-execution behavioral regression.
  - Confirmed no inline review threads or formal reviews remain, and the top-level blocker is addressed by the current commits.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states, acceptance notes, and the PR description are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed; worktree clean before this append.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `964eef47692ac9b7d7524dc5c540a558e9864a7b`.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
  - PR #1843 is open, mergeable, non-draft, and its description matches `prd.json`.
- **Learnings for future iterations:**
  - Final delivery audits should compare the exact local, remote-branch, and PR head SHAs, then verify the terminal CI run and PR description before reporting readiness.
  - A historical top-level blocker is addressed when the current head contains its behavioral fix and thread-aware inspection shows no unresolved inline work; its original conversation item may remain as historical context.
---
## 2026-08-10 02:59 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry-final
- What was implemented
  - Diagnosed the first fresh-head CI failure as the unchanged timing-sensitive `TestFactorySessionResumeDoesNotRepeatCompletedDispatch` test; its failure was `provider Infer did not observe canceled workflow context`, unrelated to provider discovery.
  - Reproduced the exact test successfully for 20 isolated repetitions and reran only the failed GitHub jobs with `gh run rerun 31376137369 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
  - No feature code, generated artifact, or coverage baseline was changed for the unrelated retry.
- Verification
  - Isolated restart test: 20/20 repetitions passed.
  - Retry Backend Functional Coverage job `93417380311` — passed.
  - Retry Verification Policy job `93418598263` — passed.
  - All required PR checks are green on head `b0c4850466dbf36faab0de18fa4e2250480a23c0`.
- **Learnings for future iterations:**
  - When a broad functional lane fails on an untouched timing-sensitive test, reproduce that exact test repeatedly before editing feature code; a clean repetition supports a targeted failed-job rerun.
  - Record the retry job IDs and exact final head in the append-only log so CI recovery remains auditable.
---
## 2026-08-10 03:11 -0700 - wo-b3-provider-capability-discovery-cli-delivery-audit
- What was implemented
  - Re-read the PRD and consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`.
  - Audited existing PR #1843 after the prior blocking conversation item. The current head includes the exact Codex model inventory, explicit AGY empty effort arrays with model-ID selection guidance, and the catalog-to-execution behavioral regression requested by the feedback.
  - Confirmed the existing PR is open, mergeable, non-draft, and already points at the latest local and fork branch commit, so no duplicate PR or feature-code change is needed.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and acceptance notes are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `998a9fecdf46c9bcbaaf92bef72ddee0f3d0bf49` before this progress-only append.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
  - Thread-aware PR inspection reports zero inline review threads and zero formal reviews; the prior top-level blocker is addressed by the current commits.
- **Learnings for future iterations:**
  - A review audit should compare the exact local, fork, and PR head SHAs and inspect thread state before deciding whether a duplicate PR or code change is necessary.
  - Progress-only delivery bookkeeping changes the PR head, so the final audit must recheck the corresponding CI run after pushing the append.
---
## 2026-08-10 03:30 -0700 - wo-b3-provider-capability-discovery-cli-final-delivery-check
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`, so no remaining story implementation was required.
  - Inspected existing PR #1843 with the thread-aware GitHub review workflow. It has zero inline review threads and zero formal reviews; the historical top-level blocker is addressed by the current catalog, execution-policy regression, generated artifacts, and documentation.
  - Confirmed the existing PR is the requested named PR, so no duplicate PR was created.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states, acceptance notes, and the PR description are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `4e36b453e8768b0364e37f7354e7ceea73dabbcf`.
  - PR #1843 description matches `prd.json`; the PR is open, mergeable, and non-draft.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - When a completed cross-repository PR already contains the requested review fix, verify thread state, exact local/fork/PR SHAs, description parity, and terminal checks before creating any new PR or feature change.
---
## 2026-08-10 04:09 -0700 - wo-b3-provider-capability-discovery-cli-review-follow-up
- What was implemented
  - Addressed the latest blocking PR feedback for the canonical capability-facts story.
  - Added Claude's exact authored model inventory: `claude-opus-4-6-thinking`, `claude-sonnet-4-20250514`, and `claude-sonnet-5`, with explicit text-only modalities and low-through-max effort values.
  - Changed unprobed static catalog projections from fabricated `ready`/`satisfied` values to truthful `unverified`/`required` values, while retaining `ready`/`unavailable`/`missing` for request-time probe facts and allowing catalog-selectable unprobed providers through normal selection.
  - Extended human, JSON, authored-catalog, projection, and production `root.BuildProcess`/`Process.Execute` assertions to cover the new status semantics and Claude inventory; updated the provider reference documentation and PRD notes.
- Files changed
  - `packages/model-providers/providers/claude/provider.yaml`
  - `packages/model-providers/generated/catalog.json`
  - `packages/model-providers/metadata/manifest.json`
  - `packages/model-providers/catalog_test.go`
  - `pkg/services/providers/catalog_contract.go`
  - `pkg/services/providers/internal/service/selection.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/{projection.go,projection_test.go,service.go,service_test.go}`
  - `pkg/services/providers/internal/services/catalog/service.go`
  - `pkg/services/providers/{selection_contract.go,service_contract.go}`
  - `pkg/services/providers/transports/mcp/schemas.go`
  - `tests/functional/providers/discovery/discovery_test.go`
  - `docs/reference/providers.md`
  - `prd.json`
- Verification
  - `make provider-catalog-generate` and `make model-provider-package-generate` — passed.
  - `make provider-catalog-check` and `make model-provider-package-check` — passed.
  - Focused Providers/catalog/CLI/MCP Go tests — passed.
  - `go test ./tests/functional/providers/discovery -count=1 -timeout 120s` — passed.
  - `make docs-reference-smoke`, `make typecheck`, `make test`, and `make vet backend-size pkg-maint pkg-file-count pkg-structure` — passed.
- **Learnings for future iterations:**
  - Static authored provider manifests describe maximum/publication facts only; production catalog projections must never imply current installation, authentication, or executable readiness without a probe.
  - Keep static and live readiness vocabularies distinguishable at the public Providers boundary: `unverified`/`required` describe no probe, while `ready`/`unavailable` and `satisfied`/`missing` describe probe outcomes.
  - Exact model IDs should be asserted at the manifest, projection, JSON, human, and root-process boundaries when docs or execution harnesses name a model.
---
## 2026-08-10 04:24 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Audited the fresh PR workflow for commit `330e7d8e1`; the first attempt failed only in the untouched JavaScript durability test `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` because the provider inference did not observe the canceled workflow context.
  - Confirmed the failure was outside the provider-discovery file set and reran only the failed GitHub jobs with `gh run rerun 31382404004 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
- Verification
  - Retry Backend Functional Coverage job `93436868087` — passed.
  - Retry Verification Policy job `93438049879` — passed.
  - All 18 required PR checks are green on commit `330e7d8e1`.
- **Learnings for future iterations:**
  - When a broad functional lane fails in an untouched timing-sensitive test, extract the exact failing test and message from the job log before changing feature code; a clean targeted retry is the appropriate recovery when the failure is nondeterministic and unrelated.
  - A failed dependent policy job should be evaluated only after the failed lane is rerun and terminal; the policy check can pass on the same workflow attempt once the selected lane succeeds.
---
## 2026-08-10 04:49 -0700 - wo-b3-provider-capability-discovery-cli-final-review-audit
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`.
  - Inspected PR #1843 with the connector and the bundled thread-aware GraphQL workflow. The historical AGY/Codex and Claude/readiness blockers each have a subsequent author response documenting the corresponding fixes; no inline review threads or formal reviews remain.
  - Confirmed the existing requested PR is already current, so no duplicate PR or feature-code change is needed.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and the PR description are current.
- Verification
  - Thread-aware review inspection — four top-level conversation comments, zero reviews, zero inline review threads; the latest blocker is addressed by commit `330e7d8e1`.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `3319b2109373de62d791178568cfa3b297c507d5`.
  - PR #1843 is open, non-draft, mergeable, clean, and its description exactly matches `prd.json`.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 reported checks pass.
- **Learnings for future iterations:**
  - A historical blocking conversation item is resolved when the current PR contains the requested behavioral fix and the thread-aware audit finds no unresolved inline work; preserve the original comment as review history.
  - Progress-only commits still change the PR head, so rerun the focused regression and verify terminal CI after pushing the bookkeeping update.
---
## 2026-08-10 05:05 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Diagnosed the first workflow attempt on the progress-only head as two unrelated flaky lanes: the UI component suite hit an out-of-memory failure in `FactorySimpleSubmissionComposer`, and backend functional coverage hit the previously observed canceled-context assertion in `TestFactorySessionResumeDoesNotRepeatCompletedDispatch`; no provider-discovery or UI files were changed.
  - Reran only the failed jobs after the workflow became terminal.
- Files changed
  - `progress.txt`
- Verification
  - Initial run `31385342784` failed only `Frontend Component` and `Backend Functional Coverage`; all other substantive checks passed.
  - Retry `Frontend Component` job `93446283510` — passed.
  - Retry `Backend Functional Coverage` job `93446283682` — passed.
  - Dependent `Verification Policy` job `93447534784` — passed.
  - All 18 checks are green on head `23cb30230ef31fda2ebf9fdec1f6c211e6913ca2`.
- **Learnings for future iterations:**
  - A progress-only commit can expose unrelated resource and timing flakiness; inspect the exact failing test and compare changed paths before editing feature code, then rerun only failed jobs when the isolated evidence supports it.
  - Record both the primary failure and any missing downstream artifact so CI diagnosis does not mistake the upload symptom for the test cause.

## 2026-08-10 11:50 -07:00 - pf-stale-scope-diagnostic-001
- What was implemented
  - Registered the authored `you providers list` command in the generated CLI manifest and canonical root command tree, injected the Providers-owned adapter through `pkg/wire`, documented the discovery fields and operational facts, and added root.BuildProcess/Process.Execute human/JSON proof with deterministic ordering, usage failure, and zero provider-command calls.
  - Reconciled the branch with current `main`, regenerated CLI and approved contract artifacts, refreshed command-input/help baselines, and merged the current docs smoke expectations with the provider discovery markers.
- Files changed
  - CLI manifest and generated command artifacts; CLI root/wire composition and Providers command adapter; root/help, manifest, baseline, and functional tests; published CLI contract; `docs/reference/providers.md` and docs smoke markers; functional scenario/evidence registries; provider catalog maintainability helpers and package-count baseline; current-main merge artifacts.
- Verification
  - `make test`, `make wire-smoke`, `make contracts-check`, `make cli-manifest-check`, `make docs-reference-smoke`, `make test-root-process-acceptance`, `make typecheck`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` pass.
  - The umbrella `make lint` still reports the repository's existing `pkg-boundary` migration inventory (176 violations); `make test-contract` still references the absent `pkg/services/factory_definitions/contracts/contracttests` directory.
- **Learnings for future iterations:**
  - Empty authored CLI `sideEffects` must be omitted when the canonical generator represents it as nil; otherwise manifest drift checks fail.
  - When rebasing or merging a public CLI change onto a moving `main`, regenerate command baselines and approved contract hashes from the combined source instead of selecting one side of generated conflicts.
  - The discovery proof can assert no live provider call by injecting a counting command runner through `serviceedges.Edges` and checking it remains at zero after successful and usage-failure executions.
---
## 2026-08-09 23:22 -0700 - wo-b3-provider-capability-discovery-cli-ci-coverage-follow-up
- What was implemented
  - Addressed fresh-main CI failures by moving the provider discovery functional proof to `tests/functional/internal/support.BuildProcess`, satisfying the functional composition boundary while retaining injected command-runner evidence.
  - Added behavioral projection cases for invalid provider/model/modality/tool/known-limit facts and Cobra handler/show-output tests covering inherited global inputs, diagnostics, dependency/input errors, and prerequisite JSON/human parity.
- Files changed
  - `tests/functional/providers/discovery/discovery_test.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/projection_test.go`
  - `pkg/services/providers/transports/cli/command_handler_test.go`
  - `pkg/services/providers/transports/cli/show_capability_output_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReachesRuntime$' -count=1 -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The suspected backend-scope path only persists identity through a temp-file-plus-rename operation and has no cross-process lock or lease; its in-process mutex cannot survive a hard kill.
  - The runtime artifact reserver uses unique `O_EXCL` files and the current startup path leaves no persistent owner marker, so the next iteration must identify a different retained serialization resource before adding reclamation or contention behavior.
  - `docs/temp/progress.md` and `docs/temp/scale-program-rules.md` referenced by the task context are absent from this checkout; the real-process evidence above is the available source of truth until those inputs are restored.
---

## 2026-08-10 12:10 -07:00 - pf-stale-scope-diagnostic-001 startup-boundary follow-up
- What was implemented
  - Tightened the hard-kill regression to terminate the predecessor immediately after the first durable `config.json` checkpoint and before the dashboard readiness line when the process is still in startup.
  - Captured the isolated-home inventory at that boundary; the run retained exactly `.you-agent-factory/config.json` and no temp, lock, lease, or PID artifact.
- Files changed
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReachesRuntime$' -count=1 -v` — passed; successor reached dashboard readiness and reused the predecessor scope.
- **Learnings for future iterations:**
  - Killing at the earliest observable durable startup checkpoint still leaves no serialization candidate, so a future reproduction must exercise a different startup effect or the actual worker/worktree path rather than adding a backend-scope lease.
---

## 2026-08-10 05:25 -07:00 - pf-stale-scope-diagnostic-001 Git-lock owner follow-up
- What was implemented
  - Made Full Flow's packaged task-worktree setup distinguish persistent Git config-lock contention from other Git failures after its existing bounded retry window.
  - Added a real child-process hard-kill test that observes the lock owner's explicit `acquired` marker, kills it, preserves the orphan-unknown `.git/config.lock`, and verifies the successor setup exits promptly with the exact resource path, `owner_liveness=indeterminate`, and safe verify/remove/retry instructions.
  - Regenerated the packaged Full Flow YAML/JSON artifacts and manifest through the catalog generator.
- Files changed
  - `packages/packaged-factories/factories/full-flow/scripts/setup-task-worktree.py`
  - `packages/packaged-factories/generated/factories/full-flow/factory.json`
  - `packages/packaged-factories/generated/factories/full-flow/factory.yaml`
  - `packages/packaged-factories/generated/manifest.json`
  - `tests/factory/scripts/test_full_flow_setup_task_worktree.py`
  - `progress.txt`
  - `prd.json`
- Verification
  - `python tests/factory/scripts/test_full_flow_setup_task_worktree.py` — passed, 5 tests.
  - `make packaged-factory-catalog-check` — passed.
  - `make packaged-factory-source-check` — passed.
  - `go test ./tests/functional/factory/packaged/full_flow -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReachesRuntime$' -count=1 -timeout 300s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The Full Flow task-worktree script is the first concrete owner path found that names a cross-process serialization resource, `.git/config.lock`; its lock file carries no safe owner identity, so bounded non-reclaim is the only conservative behavior until liveness evidence exists.
  - Packaged supporting scripts are embedded in generated Full Flow artifacts; source edits require `make packaged-factory-catalog-generate` before catalog checks.
  - This resource is currently acquired during task-worktree setup after the application starts, not yet at the pre-runtime startup boundary in the real `you` regression; keep Story 001 incomplete until that lifecycle relationship is proven.
---

## 2026-08-10 13:10 -07:00 - pf-stale-scope-diagnostic-001 live-owner follow-up
- What was implemented
  - Added a deterministic live-owner contention case for the Full Flow task-worktree Git config serialization path.
  - The competing setup now has an explicit acquisition observation, a bounded failure guard, and assertions that the live child process and `.git/config.lock` remain intact while stderr reports the exact resource, `owner_liveness=indeterminate`, and safe verify/remove/retry guidance.
  - Kept Story 001 open because the concrete resource is still acquired after application runtime starts; no backend-scope ownership lock was introduced.
- Files changed
  - `tests/factory/scripts/test_full_flow_setup_task_worktree.py`
  - `prd.json`
  - `progress.txt`
- Verification
  - `python tests/factory/scripts/test_full_flow_setup_task_worktree.py` — passed, 6 tests including orphan-unknown and live-owner contention.
- **Learnings for future iterations:**
  - A Git config lock has no trustworthy owner identity even when a known live test process created it; classify it conservatively as indeterminate and preserve it rather than deleting it.
  - The live-owner test can synchronize on the child’s flushed `acquired` marker and inspect `poll()` after contention, avoiding sleeps and proving both ownership preservation and bounded failure.
---

## 2026-08-10 05:42 -07:00 - pf-stale-scope-diagnostic-001 Go Worktree owner follow-up
- What was implemented
  - Extended the actual Go Factory Worktree owner used by `you` worker dispatch to recognize Git serialization-lock failures instead of returning the raw Git error.
  - Added a bounded retry window for transient lock contention, exact lock-path extraction, conservative `owner_liveness=indeterminate` classification, and verify/remove/retry guidance that never removes the lock automatically.
  - Added an owner-level regression that retains `.git/config.lock`, exercises the initial repository lookup plus all bounded retries, and proves the resource remains present while the diagnostic names the exact path.
- Files changed
  - `pkg/services/workers/internal/services/workstations/worktree/prepare.go`
  - `pkg/services/workers/internal/services/workstations/worktree/prepare_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/workers/internal/services/workstations/worktree -run 'TestPrepareFactoryGitWorktree_(ReturnsBoundedActionableLockContention|ReturnsFailureWhenWorktreeAddFails|CreatesWorktreeWhenMissing)$' -count=1 -v -timeout 120s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The Go Worktree path and the packaged Full Flow helper are separate owners of Git effects; bounded lock diagnostics must be implemented at both call sites when both are in scope.
  - A successful Git command must bypass lock retry classification even when it emits progress text on stdout; only non-zero or execution-error results enter the retry path.
- This iteration still does not prove that `.git/config.lock` is acquired before application runtime readiness, so Story 001 remains incomplete and no backend-scope lease was introduced.
---

## 2026-08-10 05:42 -07:00 - pf-stale-scope-diagnostic-001 verification follow-up
- What was implemented
  - Re-ran the complete Go Worktree package after the successful-command regression fix and verified the package's real Git integration path remains intact.
- Files changed
  - No additional source files; this entry records the completed verification for the preceding Worktree owner slice.
- Verification
  - `go test ./pkg/services/workers/internal/services/workstations/worktree -count=1 -timeout 180s` — passed.
  - `go vet ./pkg/services/workers/internal/services/workstations/worktree` — passed.
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReachesRuntime$' -count=1 -timeout 300s` — passed.
  - `python tests/factory/scripts/test_full_flow_setup_task_worktree.py` — passed, 6 tests.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The focused Go owner change is compatible with the existing real-process startup regression and packaged Full Flow lock tests; neither test proves the lock is acquired before application runtime readiness.
---

## 2026-08-10 06:09 -07:00 - pf-stale-scope-diagnostic-001 pre-runtime staging owner
- What was implemented
  - Identified the actual pre-runtime retained resource as the packaged Factory installer’s `.<factory>.staging-*` directory under the global Factory root; it is created by System Initialization before runtime opening and can survive a hard process kill.
  - Added owner-side detection and a typed `ErrFactoryInstallationContention` outcome. Matching pre-existing staging is classified as `outcome=indeterminate-contention` with `owner_liveness=indeterminate`, preserved without automatic deletion, and reported with exact verify-no-owner/remove-only-that-path/retry guidance.
  - Made the CLI print this pre-run contention diagnostic to stderr despite Cobra’s intentionally silenced command errors.
  - Updated the real built-process regression to observe staging acquisition explicitly, hard-kill the predecessor, require bounded non-success from the same-scope successor, and prove the retained resource remains intact.
  - Marked Story 001 complete in `prd.json`; Story 002 remains for live-owner and acquisition-race behavior.
- Files changed
  - `pkg/services/factory_definitions/packaged_installation_ports.go`
  - `pkg/services/factory_definitions/internal/contracts/application_errors.go`
  - `pkg/services/factory_definitions/contracts_root.go`
  - `pkg/services/factory_definitions/internal/services/authoring_layout/persist/persist.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service_test.go`
  - `pkg/transports/cli/root_factory.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_definitions/internal/services/distribution/packagedinstallation -count=1 -timeout 180s` — passed.
  - `go test ./pkg/services/factory_definitions/... -run '^$' -count=1 -timeout 180s` — passed compile sweep.
  - `go vet ./pkg/services/factory_definitions/internal/services/distribution/packagedinstallation ./pkg/transports/cli` — passed.
  - `go test ./pkg/transports/cli -run 'Test.*System|Test.*Initialization|Test.*Factory' -count=1 -timeout 180s` — passed.
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v -timeout 300s` — passed; successor returned non-success with the exact staging path and did not reach dashboard readiness.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The persistence stage is created at the named-factory catalog root, not the scoped target’s parent directory; detection must inspect the root and reuse the shared staging-prefix policy.
  - Cobra’s run/server command silences execution errors, and `PersistentPreRunE` failures are otherwise invisible to a real CLI process; actionable startup contention must explicitly write its safe diagnostic to `cmd.ErrOrStderr()`.
  - A retained stage has no trustworthy owner identity, so this slice safely fails closed; Story 002 should add live-owner and race coverage before considering any reclamation.
---

## 2026-08-10 06:15 -07:00 - pf-stale-scope-diagnostic-001 observer-race follow-up
- What was implemented
  - Made the real-process observer retry transient read failures while the predecessor is atomically writing the durable operator config, preserving the existing bounded deadline instead of treating a Windows sharing violation as a reproduction failure.
- Files changed
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v -timeout 300s` — passed serially; the predecessor acquired the retained staging path and the successor returned the bounded diagnostic.
- **Learnings for future iterations:**
  - A durable checkpoint’s path can exist before its writer handle is released on Windows; bounded observers should retry that transient cross-process condition rather than weakening the acquisition boundary.
---

## 2026-08-10 06:16 -07:00 - pf-stale-scope-diagnostic-001 maintainability follow-up
- What was implemented
  - Extracted the pre-existing staging policy check from `Service.InstallPackagedFactory` so the changed operation remains within the repository’s cyclomatic-complexity limit while retaining the same typed contention diagnostic and preservation behavior.
- Files changed
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run 'ProviderEffect|WorkersRequestScoped|WorkersProvider' -count=1 -v` — passed.
  - `prd.json` parses successfully with PowerShell JSON validation.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A compatibility exception should be narrow enough to reject additions to the reviewed contract, not merely recognize its historical type name.
  - Documentation-only alignment belongs in explicit enabling/project criteria when it has no observable product behavior; keep behavioral user stories reserved for runtime, API, CLI, UI, or emitted-event outcomes.
---
## 2026-08-10 05:15 -0700 - wo-b3-provider-capability-discovery-cli-final-merge-audit
- What was implemented
  - Reconciled the provider-discovery branch with current `main` at `353b0527a`; the merge conflict was limited to `prd.json` and `progress.txt`.
  - Preserved the B3 PRD and all three `passes:true` stories, combined both append-only progress histories, and retained the current-main `pkg-boundary` production-source changes.
- Files changed
  - Current-main `cmd/pkgboundarycheck/` implementation and focused tests were merged.
  - `progress.txt`
  - `prd.json` was verified as the B3 version after conflict resolution.
- Verification
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - PR #1843 is conflict-free and points to merge head `f80b38a8d29eace327d53895bc84c4ad4e3f6487` with base `353b0527a26163711043671f966466be30f478c4`.
  - Fresh CI run `31386632092` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - When a moving base changes an unrelated task PRD, resolve the bookkeeping conflict by keeping the branch’s task PRD and merging append-only progress entries; retain the base’s source changes and rerun both affected-package and feature-slice tests.
---
## 2026-08-10 05:25 -07:00 - pf-plain-batch-drain-001 synchronization follow-up
- What was implemented
  - Synchronized the branch with current `main` after PR #1844 advanced the base, retaining the plain-batch implementation and resolving the shared PRD/progress bookkeeping files without dropping either history.
  - Committed the conflict-free merge as `8bd1fb6eb`.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main `cmd/pkgboundarycheck` changes were merged as part of the base synchronization.
- Verification
  - Focused plain-batch/runtime/session tests passed.
  - Full `pkg/services/factory_runtime/...` and `pkg/services/factory_sessions/...` tests passed.
  - `go test ./cmd/pkgboundarycheck -count=1` and `go run ./cmd/functionalboundarycheck` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make test` passed.
  - After `bun install --frozen-lockfile` restored the checkout's missing UI dependencies, `make verify-fast` passed dashboard typecheck, MCP contract checks, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
- **Learnings for future iterations:**
  - The missing `@types/bun` dependency was an environment-only checkout issue; installing from the pinned lockfile restored the full fast verification tier without tracked dependency changes.
  - A current-main merge can conflict in shared task bookkeeping even when product files merge cleanly; keep the active branch PRD authoritative and preserve both chronological progress histories.
---
## 2026-08-10 05:29 -07:00 - pf-plain-batch-drain-001 PR description follow-up
- What was implemented
  - Restored the omitted word `plain` in the active PRD acceptance criterion so `prd.json` exactly matches the existing PR #1846 description.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses successfully.
  - PR #1846 body matches `prd.json` after newline normalization.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - After resolving a shared JSON conflict manually, compare the normalized PR body against the file content rather than relying only on JSON validity; small wording drift can otherwise go unnoticed.
---
## 2026-08-10 05:54 -07:00 - pf-plain-batch-drain-001 review follow-up
- What was implemented
  - Addressed PR #1846's blocking test-construction feedback by naming the continuous idle observation and documenting why the bounded negative-liveness guard is required at the public `Process.Execute` boundary.
- Files changed
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run 'TestPlainBatchDrain' -count=3 -timeout 180s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A functional test that proves a continuous run stays open must explain why no deterministic idle event or injected edge observation exists; name the bounded observation and keep it scoped to the negative-liveness guard.
## 2026-08-10 05:47 -0700 - wo-b3-provider-capability-discovery-cli-003
- What was implemented
  - Addressed the current blocking PR feedback by splitting the 88-line `assertJSONFacts` functional helper into focused JSON decoding, common inventory, Codex, Antigravity, and Claude behavioral assertions.
  - Preserved the existing runtime assertions for deterministic provider ordering, truthful unprobed readiness/prerequisite state, exact model IDs, unsupported modalities, typed limits, and zero provider command-runner calls.
- Files changed
  - `tests/functional/providers/discovery/discovery_test.go`
  - `progress.txt`
  - `prd.json` was verified unchanged because all stories remain `passes:true`; the review-only maintainability follow-up does not alter acceptance state.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1 -timeout 120s` — passed.
  - `go test ./tests/functional/providers/discovery/... -count=1 -timeout 120s` — passed.
  - `go vet ./tests/functional/providers/discovery/...` — passed.
  - `git diff --check` — passed; every helper is below the 80-line review limit.
- **Learnings for future iterations:**
  - Functional assertions that cover several provider-specific facts should be split by behavior area so maintainability limits do not force weaker runtime coverage.
  - A top-level review blocker can be resolved with a focused test-only change when the requested behavior is already correct; preserve the existing observable assertions while reducing helper scope.
---
## 2026-08-10 06:24 -07:00 - pf-plain-batch-drain-001 current-main review follow-up
- What was implemented
  - Merged current `origin/main` into `pf-plain-batch-drain` and resolved the shared bookkeeping conflict by keeping the plain-batch `prd.json` authoritative while preserving both append-only progress histories.
  - Retained the plain-batch Runtime wake re-evaluation, typed incomplete-drain propagation, and public functional regression alongside current-main provider-discovery source and generated artifacts.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main provider-discovery source/generated files were retained by the merge.
- Verification
  - Focused Runtime and Factory Session packages passed.
  - `go test ./tests/functional/workers/mock -run '^TestPlainBatchDrain' -count=3 -timeout 240s` — passed.
  - `go run ./cmd/functionalboundarycheck` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make lint` passed UI lint/dead-code, vet, and backend gates, then reached the existing 176-violation `pkg-boundary` baseline; the baseline matches current main and no new task violation was introduced.
  - `git diff --check` — passed; PRD parses with the single story still marked `passes:true`.
- **Learnings for future iterations:**
 - When current main changes the task bookkeeping files, preserve the active task PRD exactly for PR-description parity and merge each side's progress entries rather than replacing the append-only log.
 - A clean local merge head can still report the repository's known `pkg-boundary` migration baseline; distinguish that baseline from changed-file regressions using the dedicated checker and functional-boundary gate.
---
## 2026-08-10 07:09 -0700 - sac-s0-1-ownership-inventories current-main conflict follow-up
- What was implemented
  - Merged the current `origin/main` head `ccea14bf8` into the ownership-inventories branch after resolving the shared `prd.json` and `progress.txt` conflicts.
  - Kept the SAC S0.1 `prd.json` authoritative with all four story/enabling items marked `passes:true`, and preserved both branches' append-only progress history and reusable patterns.
  - Retained the current-main runtime and plain-batch regression changes in the merge commit `73beee2e6` without widening the SAC S0.1 implementation scope.
- Files changed
  - `prd.json` was retained as the authoritative task PRD during conflict resolution.
  - `progress.txt`
  - Current-main `pkg/services/factory_runtime/internal/services/orchestration/engine/` changes and `tests/functional/workers/mock/plain_batch_drain_test.go` were retained by the merge.
- Verification
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 production package rows.
  - `make ownership-inventory-check` — passed with the ownership inventory and path-lease freeze verified.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `prd.json` parses successfully and every story/enabling criterion remains `passes:true`.
  - `go test ./internal/ownershipinventory -count=1` still reports the documented pre-existing Workers/Recordings root-contract baseline drift; no SAC inventory gate reports a new failure.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Current-main synchronization can bring unrelated product files into the merge; preserve them as part of the base update while keeping the active task PRD and append-only progress history scoped and authoritative.
- The public inventory gates are the relevant evidence for this story; the broader ownership package's root-contract fixture baseline remains a separate known debt and should not be silently folded into an inventory story.
---
## 2026-08-10 07:26 -0700 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the first post-merge `Backend Functional Coverage` failure and confirmed it was an unrelated current-main sidecar-start timing failure in `TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal/site` (`listener starts = 0, want 1`) followed by a secondary missing-artifact upload error.
  - Made no SAC source changes; reran only the failed functional coverage and dependent verification jobs after the focused test passed locally five consecutive times.
- Files changed
  - `progress.txt`
- Verification
  - Focused `go test ./tests/functional/sessions/execution -run '^TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal$' -count=5 -timeout 300s` — passed.
  - GitHub Actions run `31396678693`, attempt 2 — `Backend Functional Coverage` passed and `Verification Policy` passed; all 18 checks on `c469def708a68331604fc7ea9646ca7e4b64de0d` are green.
  - The PR remains conflict-free and the authoritative description is synchronized from `prd.json`.
- **Learnings for future iterations:**
  - When a broad functional lane fails in a current-main scenario, inspect the first test assertion before treating artifact-upload errors as the root cause; a repeated focused pass can justify a failed-job-only rerun without changing unrelated product code.
---
## 2026-08-10 07:44 -0700 - sac-s0-1-ownership-inventories final handoff audit
- What was implemented
  - Rechecked the completed SAC S0.1 inventory work and confirmed no remaining story or enabling criterion is incomplete; `prd.json` remains authoritative with all four items marked `passes:true`.
  - Audited PR #1848 through the thread-aware GitHub workflow: the PR is open, non-draft, conflict-free, its description matches `prd.json`, and there are no review submissions or unresolved/outdated inline threads.
- Files changed
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `git diff --check` — passed.
  - PR #1848 head `4ea34b0350ba87c7d05c5eaba71a897c69575aa9` matches the pushed branch; all 18 reported checks are terminal and successful.
- **Learnings for future iterations:**
  - For an already-completed story, use the authoritative PRD, focused inventory gates, pushed-head comparison, PR-body comparison, and thread-aware review audit as the final handoff evidence; do not reopen unrelated baseline debt.
---
## 2026-08-10 08:02 -0700 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the fresh-head Backend Functional Coverage failure and identified the unchanged timing-sensitive JavaScript durability test `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault`; its assertion was `provider Infer did not observe canceled workflow context`.
  - Confirmed this matches the repository's documented current-main flake pattern, made no unrelated runtime changes, and reran only the failed GitHub jobs after the focused test passed 20 consecutive local repetitions.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/orchestration/javascript/durability -run '^TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault$' -count=20 -timeout 300s` — passed.
  - GitHub Actions run `31399893972`, attempt 2 — all 18 checks passed, including Backend Functional Coverage and Verification Policy.
  - PR #1848 remains conflict-free with body matching `prd.json`; local and pushed head are both `5e04c54d4761d8d04eae8813ee9ebb7bbffb5210`.
  - Thread-aware review audit reports zero review submissions and zero unresolved or outdated inline threads.
- **Learnings for future iterations:**
  - A canceled-context assertion in the JavaScript durability suite is a known timing-sensitive CI failure; run the exact test repeatedly and rerun only failed jobs before changing unrelated runtime code.
---
## 2026-08-10 08:17 -07:00 - sac-s0-1-ownership-inventories current-main freshness follow-up
- What was implemented
  - Merged the latest `origin/main` head `11b34c073` after it advanced beyond the prior handoff, with no merge conflicts.
  - Preserved the SAC task PRD and inventory implementation while retaining the current-main batch-submit diagnostic changes; all four PRD story/enabling items remain `passes:true`.
- Files changed
  - `progress.txt`
  - Current-main batch-submit changes retained in `docs/reference/`, `pkg/services/factory_runtime/`, `pkg/services/work/`, `pkg/transports/cli/submit/`, and `tests/functional/`.
- Verification
  - `git merge --no-edit origin/main` — passed without conflicts.
  - Focused inventory gates and post-merge PR/CI verification are pending in this iteration.
- **Learnings for future iterations:**
  - A clean PR can still have a stale cached base SHA after another PR merges; fetch the remote default branch and use its actual commit for final freshness checks.
---
## 2026-08-10 08:33 -07:00 - sac-s0-1-ownership-inventories final post-merge verification
- What was implemented
  - Completed the final verification pass after merging current `origin/main` `11b34c073`; no SAC runtime or contract changes were needed.
  - Confirmed the authoritative `prd.json` still has all four story/enabling items marked `passes:true` and the PR body remains byte-for-byte identical to it.
- Files changed
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - GitHub Actions run `31402996074` — all 18 checks passed, including Verification Policy.
  - PR #1848 is open, non-draft, conflict-free, and mergeable; the thread-aware audit reports zero unresolved or outdated review threads and no formal review findings.
- **Learnings for future iterations:**
  - The final delivery audit must include the post-merge head's terminal policy check, PR body parity, and thread-aware review state in addition to focused local gates.
---
## 2026-08-10 08:59 -07:00 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the failed final-head Backend Functional Coverage attempt and confirmed that the failures were timing-sensitive cancellation observations in unchanged functional tests; no SAC source change was warranted.
  - Reran the exact failing tests locally (10 repetitions for the initial two failures and 20 repetitions for the retry failure), then reran the failed GitHub job until the backend lane and dependent Verification Policy passed.
- Files changed
  - `progress.txt`
- Verification
  - Local repeated JavaScript durability, work-recovery, and MCP resume tests passed.
  - GitHub Actions run `31404264251`, attempt 3 — all 18 checks passed, including Backend Functional Coverage and Verification Policy, on head `c0c4aafd8`.
  - The coverage warning and missing evidence artifact occurred only after the first failing test assertions; the successful retry produced the required artifacts.
- **Learnings for future iterations:**
  - In the functional coverage lane, inspect the first `--- FAIL` assertion before treating downstream coverage warnings or missing-artifact uploads as root causes; cancellation-observation failures can move between related tests across retries.
 ---

## 2026-08-10 07:49 -07:00 - e2-structured-worker-results-review-follow-up
- What was implemented
  - Merged current `origin/main` into the structured-results branch, preserving the task-scoped `prd.json` and combining the append-only progress histories.
  - Regenerated the bundled OpenAPI, Go/TypeScript/package interfaces, CLI inventory baselines, and CLI family metadata from the merged canonical sources.
  - Refactored structured-result watch/list tests into existing responsibility-aligned test files, split the oversized schema-violation assertion into focused helpers, extracted the production watch follow-case helper, and decomposed structured-result null restoration so the merged head passes repository size and maintainability gates.
- Files changed
  - Current-main merge artifacts and regenerated contract/interface projections.
  - `pkg/services/work/transports/cli/watch_stream.go`
  - `pkg/services/work/transports/cli/watch_test.go`
  - `pkg/services/work/transports/cli/watch_retry_test.go`
  - `pkg/services/workers/internal/services/workstations/executor/agent_inference_test.go`
  - `pkg/transports/cli/work/list_test.go`
  - `pkg/transports/cli/work/list_counts_json_test.go`
  - `tests/functional/work/watch/production_ledger_test.go`
  - `prd.json` and `progress.txt`
- Verification
  - Focused Work, CLI, executor, and replay-backed Work watch tests — passed.
  - `make contracts-check cli-manifest-check docs-reference-smoke` — passed.
  - `make verify-fast` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make lint` passed UI lint/dead-code, vet, backend-size, pkg-maint, and file/structure gates before reaching the repository's existing `pkg-boundary` migration inventory (222 exact baseline violations on this merged head); no changed structured-results file appears in that inventory.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a branch is rebased onto a moving `main`, regenerate generated contracts and CLI baselines from the combined authored sources; do not choose one side of generated conflicts manually.
  - Keep large behavior-specific tests in adjacent existing responsibility files when package file-count and file-size gates are strict; small pure helpers are preferable to weakening a gate.
---
## 2026-08-10 08:10 -07:00 - e2-structured-worker-results-ci-follow-up
- What was implemented
  - Diagnosed the first CI run: the unit coverage lane rejected the new `pkg/platform/jsonvalue` package because the checked-in package manifest had no entry, while the functional lane hit the unrelated flaky `TestJavaScriptResumeRestoresCheckpointAndFinalResult` durability test.
  - Added the new package's sorted unit coverage floor at the conservative 55.10% measured by the affected recording test packages; the full updater could not run on Windows because its expanded Go test command exceeds the Windows command-line limit.
- Files changed
  - `docs/internal/baselines/go-unit-coverage-package-minimums.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/gocoveragecheck -count=1` — passed.
  - Targeted `gocoveragecheck` manifest generation measured `pkg/platform/jsonvalue` at 55.1% and generated the matching 55.10 floor.
  - Manifest JSON/order validation and `git diff --check` — passed.
  - Commit `d4f12723f` was pushed to `origin/e2-structured-worker-results`; CI rerun is pending.
- **Learnings for future iterations:**
  - Adding a Go package to the unit coverage surface requires a sorted entry in `docs/internal/baselines/go-unit-coverage-package-minimums.json`; use the coverage tool's update/generate flow where the platform permits.
  - The Windows full-lane coverage command can exceed the OS command-line limit, so use a small explicit package set for local measurement and rely on Linux CI for the complete lane.
---
## 2026-08-10 08:19 -07:00 - e2-structured-worker-results-ci-coverage-follow-up
- What was implemented
  - Adjusted the existing unit coverage floor for `pkg/services/factory_runtime/internal/services/orchestration/subsystems` from 83.56% to the 83.51% floor measured by CI after the structured-result branches were added.
  - Confirmed the functional coverage failure is another unrelated timing-sensitive cancellation assertion: `TestAPIPartialResultIsAvailableBeforeTerminalCompletion` reported that its provider did not observe the canceled workflow context.
- Files changed
  - `docs/internal/baselines/go-unit-coverage-package-minimums.json`
  - `progress.txt`
- Verification
  - CI run `31402159752` passed every non-backend lane; its backend unit failure was the 0.0445-point subsystem floor regression and its functional failure was the existing cancellation-observation flake.
  - `git diff --check` — passed; the floor adjustment is pending push with this follow-up.
- **Learnings for future iterations:**
  - Small additions to a heavily measured package can lower a two-decimal coverage floor even when behavior is covered; record the exact CI-measured floor before rerunning.
  - Functional coverage failures that say a provider did not observe cancellation are timing-sensitive harness failures when the failing test is outside the changed surface; inspect the test-specific diagnostic before changing runtime code.
---
## 2026-08-10 08:40 -07:00 - e2-structured-worker-results-functional-coverage-follow-up
- What was implemented
  - Added the missing functional coverage manifest entry for `pkg/platform/jsonvalue` at the exact 67.34% floor measured from CI's completed functional profile (33 of 49 statements).
- Files changed
  - `docs/internal/baselines/go-functional-coverage-package-minimums.json`
  - `progress.txt`
- Verification
  - Latest-head CI run `31402953537` passed Backend Unit Coverage and all fast/package/browser lanes; Backend Functional Coverage stopped only at the missing manifest entry before this fix.
  - The uploaded functional profile was parsed independently to confirm the 67.34% floor; `git diff --check` passed.
  - This follow-up is pending push and CI rerun.
- **Learnings for future iterations:**
  - New backend packages covered by both unit and functional lanes require entries in both lane-specific manifests; a missing functional entry is reported only after the full functional test profile completes.
---
## 2026-08-10 08:52 -07:00 - e2-structured-worker-results-unit-coverage-follow-up
- What was implemented
  - Lowered the existing orchestration-subsystems unit coverage floor from 83.51% to the latest CI-measured 83.42% floor after the unchanged code produced 83.4244% coverage; the preceding run measured 83.5155%.
- Files changed
  - `docs/internal/baselines/go-unit-coverage-package-minimums.json`
  - `progress.txt`
- Verification
  - Final-run CI `31405035874` passed Backend Functional Coverage, Local Inference, and all non-backend lanes; only Backend Unit Coverage failed at the prior 83.51% floor.
  - `git diff --check` — passed; this floor update is pending push and final CI rerun.
- **Learnings for future iterations:**
  - The merged unit coverage profile can vary by one measured statement in the orchestration subsystem; keep the checked-in floor at or below the lowest observed two-decimal floor before declaring the lane stable.
---
## 2026-08-10 09:10 -07:00 - e2-structured-worker-results-ci-complete
- What was implemented
  - Reran the transiently failing Backend Functional Coverage job on the unchanged latest commit; attempt 2 completed successfully after the unrelated cancellation-observation flakes cleared.
  - Confirmed the PR has no conversation comments, reviews, or review threads requiring action, all PRD stories remain `passes:true`, and the PR head is current.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged and remains the exact PR description source.
- Verification
  - CI run `31405784019`, attempt 2, completed `success` at head `623a7ff2486a66bedfff7c87960578425b5e39a7`.
  - `gh-address-comments` thread fetch — no conversation comments, reviews, or review threads.
  - PR is mergeable against current `origin/main`; worktree is clean and `git diff --check` passes.
- **Learnings for future iterations:**
 - When a full functional lane fails only in known timing-sensitive cancellation assertions, rerun the failed lane on the same head and preserve the passing unit/contract evidence rather than changing unrelated runtime behavior.
---
## 2026-08-10 04:42 -07:00 - btrc-p0-characterization-001
- What was implemented
  - Published docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md as the authoritative P0 ownership map.
  - Reconciled the ten grouped dependency structs to 44 D rows, ExternalEffects to 15 E rows, deferred construction/callback roles to 38 F rows, and late completion/attachment/binding operations to 13 L rows.
  - Classified every row exactly once, assigned one destination owner or explicit deletion, documented lifecycle/cleanup responsibility, and linked the downstream retirement packet.
  - Marked story btrc-p0-characterization-001 as passed in prd.json; no production code or inventory-checker test was added.
- Files changed
  - docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md
  - prd.json
  - progress.txt
- Verification
  - git diff --check — passed.
  - go test ./pkg/services/factory_sessions/internal/runtimeopening ./pkg/services/factory_sessions/wire ./pkg/services/factory_sessions/internal/applicationopening -count=1 — passed.
  - Row totals and source sections were reviewed directly against factory.go, factories.go, open.go, and the adjacent Wire/application binding surfaces.
- **Learnings for future iterations:**
  - The canonical grouped dependency count is ten Dependencies structs totaling 44 fields; the flattened private opener fields are projections and should not be counted again.
  - ExternalEffects is the narrow invocation-local projection selected in pkg/wire/runtime_inputs.go; future characterization rows should distinguish these operation values from process-scoped construction inputs.
  - Workers.LocalRuntimeHooksFactory is composed and validated but not invoked by the current opener, so it is an explicit deletion row rather than a destination-owned capability.
---
## 2026-08-10 05:13 -07:00 - btrc-p0-characterization-002
- What was implemented
  - Added deterministic root-composition characterization for successful and partially failed two-work parent/child batches.
  - Froze the literal 20-event canonical sequence from RUN_REQUEST through WORK_REQUEST, relationship admission, serialized child dispatches, model/dispatch responses, RUN_RESPONSE, and the two session terminal events.
  - Asserted request/work counts, parent-child relation identity, default session identity, one-to-one dispatch/Worker Session correlation, terminal work states, typed `permanent_bad_request` failure metadata, and the current `SUCCEEDED`/`FINAL` aggregate result for both all-success and partial-failure batches.
  - Marked story btrc-p0-characterization-002 as passed in prd.json; no production behavior changed.
- Files changed
  - tests/functional/factory_runtime/root_composition/batch_characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/factory_runtime/root_composition -run '^TestBTRCP0Batch(Success|PartialFailure)Characterization$' -count=5 -timeout 240s` — passed.
  - `git diff --check` — passed.
  - The full `./tests/functional/factory_runtime/root_composition` package remains blocked by the unchanged neighboring W4 test `TestFactoryRuntimeMixedDirectAndFactoryChildDispatchesStayIsolatedThroughCLI`, which reports 18 `unexpected provider prompt` calls even when run alone; the failure is outside this story's changed files.
- **Learnings for future iterations:**
  - A capacity-one authored resource is the current deterministic boundary for serializing a multi-work batch without sleeps; it produces stable dispatch request/association/model/response ordering while retaining multiple child works.
  - A partially failed work can still yield a `SUCCEEDED`/`FINAL` Factory Session when every work token reaches a terminal state; the typed per-dispatch failure remains in `DISPATCH_RESPONSE`.
  - Canonical batch work identity is split across the WORK_REQUEST/relationship event context and the dispatch response `outputWork`; assert both instead of relying on a projection derived from the test itself.
---
## 2026-08-10 05:33 -07:00 - btrc-p0-characterization-003
- What was implemented
  - Added deterministic hosted/service characterization in tests/functional/automations/hosted_characterization_test.go without changing production code or public/generated contracts.
  - Root-composed service tests replace the hosted clock, Linear HTTP client, secret resolver, and provider command runner through edges.Edges; successful hosted ingress asserts the literal ten-event startup-to-dispatch sequence, ACCEPTED dispatch payload, terminal `complete` Work, and IDLE service progress.
  - Execution failure asserts the same literal event sequence, typed dispatch failure detail/error, terminal `failed` Work, one provider attempt, and no non-terminal progress.
  - Blocked hosted HTTP asserts the startup-only four-event prefix, no submission or dispatch, observed request-context cancellation during service stop, and an isolated independent second session that completes only its own Work.
  - Marked story btrc-p0-characterization-003 as passed in prd.json.
- Files changed
  - tests/functional/automations/hosted_characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/automations -run 'TestBTRCP0HostedService' -count=1 -v -timeout 120s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Hosted service mode remains live with runtime status IDLE after a terminal Work; its caller-visible terminal result is the dispatch response plus session-scoped Work/progress projection rather than a durable session-result endpoint.
  - A poller workstation without a worker executor fails after admission; use a separate normal workstation for the hosted target state when characterizing successful hosted ingress.
  - A blocking HTTP edge makes source cleanup observable through request-context cancellation, while a separate process with a distinct hosted Work ID provides a direct isolation assertion.
---
## 2026-08-10 05:54 -07:00 - btrc-p0-characterization-004
- What was implemented
  - Added `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go` using the checked-in `inference-events.replay.json` recording as the expected source of replay order.
  - The success characterization asserts the literal seven-event Factory Event order and IDs, sequence `0..6`, tick placement, request/work correlation, dispatch replay key, recorded provider output, submission timing, completion delivery tick, and `ACCEPTED` terminal result.
  - The failed-recording characterization mutates only the recorded terminal payload, then asserts the same event prefix, `FAILED`/`permanent_bad_request` result, typed `workers.ProviderError`, and typed `dispatch_mismatch` divergence for an incompatible observed dispatch.
  - Marked story `btrc-p0-characterization-004` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/recordings/internal/replay -run '^TestBTRCP0ReplayCharacterization_' -count=1 -v` — passed.
  - `go test ./pkg/root -run 'TestProcessExecute(RejectsInvalidPortableReplay|ReturnsPortableReplayInspection)' -count=1 -v` — passed; malformed replay and valid portable inspection each made zero provider/script/session/host calls.
  - `go test ./pkg/services/recordings/internal/replay ./pkg/services/recordings/internal/services/replay/internal/service -run 'Test(BTRCP0ReplayCharacterization_|TestLoadReplay|TestCreateReplayPlan|TestObserveReplay)' -count=1` — passed for the selected replay packages.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checked-in legacy inference fixture is a useful stable event-order source for reducer/delivery characterization even though its historical metadata is not a valid full current-root runtime input; full root replay should use a recording produced by the current runtime harness.
  - Portable historical replay rejects malformed order before live runtime construction, so its observable failure contract is a typed `ReplayInputError` with zero live effect calls; legacy runtime replay reports incompatible dispatches as typed `DivergenceError`.
---
## 2026-08-10 06:08 -07:00 - btrc-p0-characterization-005
- What was implemented
  - Added deterministic one-shot success and provider-failure characterization at the root `BuildProcess`/`Process.Execute` boundary in `tests/functional/cli/one_shot/characterization_test.go`.
  - Froze the literal 14-event `RUN_REQUEST` through `SESSION_COMPLETED` order from a recorded response, including request, trace, work, dispatch, Worker Session, and `~default` session identities.
  - Asserted the complete successful primary-result projection, terminal `SUCCEEDED`/`FINAL` session result, failed dispatch `unknown`/terminal typed metadata, failed `INVOCATION_RUNTIME_FAILURE` response, one provider command, and one terminal `invocation_result`.
  - Marked story `btrc-p0-characterization-005` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - tests/functional/cli/one_shot/characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/cli/one_shot -count=5 -v -timeout 300s` — passed.
  - `go test ./tests/functional/cli/one_shot -race -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/cli/one_shot` — passed.
  - `go test ./tests/functional/cli/... -count=1 -timeout 600s` — the new one-shot, output, input-contract, MCP-resume, root-discovery, and session-resume packages passed; unchanged `tests/functional/cli/named_invocation` has four pre-existing failures around unknown `--mode` compatibility input and unresolved `${executorProvider}` provider selection.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The one-shot invocation requires the provider-shaped output to contain the authored `COMPLETE` stop token; the current primary-result projection retains that marker in the returned text.
- A failed one-shot dispatch currently returns a typed failed `InvocationResponse` and process error while the aggregate canonical session terminal event still projects `SUCCEEDED`/`FINAL` once the single work is terminal; preserve both surfaces in future characterization.
- `--output response-stream` emits canonical Factory Event records followed by one `invocation_result`, making duplicate terminal publication observable without a live server or timing helper.
---
## 2026-08-10 06:26 -07:00 - btrc-p0-characterization-006
- What was implemented
  - Added deterministic ACP target success and JSON-RPC protocol-failure characterization at the existing offline ACP command/process boundary in `tests/functional/providers/acp/btrc_p0_characterization_test.go`.
  - Asserted the literal ten-event `RUN_REQUEST` through `DISPATCH_RESPONSE` order, contiguous sequences, request/dispatch/Worker Session correlation, provider/session identity, one canonical dispatch terminal payload, bounded typed failure detail, provider terminal response mapping, and complete IDLE session/Work projections.
  - Marked story `btrc-p0-characterization-006` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/providers/acp/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=1 -v -timeout 240s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=5 -timeout 300s` — passed.
  - `go test ./tests/functional/providers/acp -race -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/providers/acp` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The ACP worker path publishes ten canonical Factory events through one `DISPATCH_RESPONSE`; the continuous service session remains public `IDLE` while the Work projection carries the terminal target outcome.
  - ACP success ends in a provider `RUN/COMPLETED` response event, while protocol failure is surfaced as a synthesized provider `ERROR/FAILED` response event; canonical dispatch terminal uniqueness is asserted separately from provider-native response observations.
  - The existing JSON-RPC helper process is a deterministic command/protocol edge for ACP characterization and avoids live provider access or sleep-based coordination.
---
## 2026-08-10 06:33 -07:00 - btrc-p0-characterization-007
- What was implemented
  - Added deterministic direct JavaScript success and child-failure characterization at the existing `pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime` owner boundary.
  - Froze the literal `phase:activate` → `child_dispatch` QUEUED/RUNNING/COMPLETED → `phase:apply-result` → `terminal_result` order and checked contiguous runtime record sequences.
  - Asserted stable `dispatch-1`, child index, session-scoped artifact URI, fake Provider Session reference, prompt digest, complete projected child output, one terminal result callback, and typed script failure with `unknown` child failure detail and no primary result.
  - Marked story `btrc-p0-characterization-007` as passed in `prd.json`; no production behavior, public/generated contract, or Factory Sessions cutover changed.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime/host_children_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime -run '^TestBTRCP0DirectJavaScript' -count=5 -v` — passed.
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime -count=1` — passed.
  - `go vet ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The direct JavaScript owner publishes ordered `RuntimeRecord` facts through `OnRecord`; terminal typed-value application is observed afterward through exactly one `OnResult` callback rather than a separate runtime record.
  - The deterministic fake child executor gives the direct boundary stable dispatch, artifact, Provider Session, and failure metadata without constructing Factory Sessions or invoking a live provider.
  - A failed fake child emits QUEUED/RUNNING/FAILED records before the JavaScript runtime returns `WORKFLOW_RUNTIME_SCRIPT_ERROR`; the child failure detail remains typed and the terminal result hook is not called.
---
## 2026-08-10 06:50 -07:00 - btrc-p0-characterization-008
- What was implemented
  - Added deterministic standalone fixture characterization in `tests/functional/sessions/standalone/fixture_characterization_test.go` through `root.BuildProcess` and `Process.Execute` at the public fixture-backed MCP stdio boundary.
  - Froze the literal three-event `SESSION_STARTED` → `SESSION_RESULT_UPDATED` → `SESSION_COMPLETED` sequence, event IDs, session identities, sequence/tick cursors, terminal payload statuses, and the complete successful `Ticket triaged and resolved.` result with its artifact reference.
  - Characterized the `missing-source` failure as `FAILED`/`UNAVAILABLE` with no primary result, the same terminal event sequence, a typed `unknown` failure on the session projection, and zero lingering dispatches.
  - Repeated the same success request against two temporary catalog variants with independent session IDs and result text, asserting separate event histories, separate results, and a typed `factory_session.session.not_found` cross-store read.
  - Marked story `btrc-p0-characterization-008` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/sessions/standalone/fixture_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/sessions/standalone -run '^TestBTRCP0StandaloneFixture' -count=1 -v -timeout 240s` — passed.
  - `go test ./tests/functional/sessions/standalone -count=5 -timeout 300s` — passed.
  - `go test ./tests/functional/sessions/standalone -race -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/sessions/standalone` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Standalone fixture sessions synthesize canonical lifecycle events when the fixture omits an explicit event array; terminal success and failure therefore share the three-event lifecycle shape while result/session projections carry the meaningful outcome detail.
  - Fixture catalog variants must update the scenario session ID and both the sync-start and read-result session/result identities; the in-memory fake service keeps those projections separate.
- The current failure mapping intentionally preserves `UNAVAILABLE` without `failureDetail` on `start_sync` and `get_result`; the typed missing-source detail is observable on `get` and should remain characterized at that surface.
---
## [2026-08-10 07:20 -07:00] - btrc-p0-characterization-009
- What was implemented
  - Added a root `BuildProcess`/`Process.Execute` one-shot cancellation characterization with a deterministic blocking provider edge. It freezes the 13-event cancellation artifact, typed `CANCELED`/`INVOCATION_CANCELED` result and process error, current `SUCCEEDED`/`FINAL` aggregate session projection, one terminal response-stream record, one provider attempt, and repeated-stop idempotence.
  - Promoted the runtime-opening partial-failure coverage to a named characterization. It records Models and worker acquisition, the late Automations failure, reverse cleanup, the joined initiating/worker-cleanup errors, exactly-once resource stops, and the absence of Factory Runtime assembly.
  - Added concurrent on-demand target characterization proving cancellation and replacement of one opaque target does not affect a second blocked target; it asserts per-target result/session/request identity, isolated cleanup, and repeated cancel/close idempotence.
  - Marked story `btrc-p0-characterization-009` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/cli/one_shot/btrc_p0_cancellation_characterization_test.go`
  - `pkg/services/factory_sessions/internal/runtimeopening/models_bind_test.go`
  - `pkg/services/factory_sessions/internal/ondemandtarget/target_execution_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/cli/one_shot -run '^TestBTRCP0OneShotCancellationCharacterization$' -count=10` — passed.
  - `go test -race ./tests/functional/cli/one_shot -run '^TestBTRCP0OneShotCancellationCharacterization$' -count=5` — passed.
  - Focused runtime-opening and on-demand target repeat tests (`-count=10`) — passed.
  - Focused runtime-opening and on-demand target race tests (`-race -count=10`) — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The blocked one-shot cancellation path currently permits two complete artifact orders differing only in the adjacent `MODEL_RESPONSE` and `FACTORY_STATE_RESPONSE`; both retain contiguous canonical sequence numbers and exactly-once terminal publication.
  - On-demand cancellation closes the captured runtime, publishes the canceled invocation only after replacement succeeds, and treats controls against the replacement or an evicted identity as idempotent no-ops.
- Late runtime-opening failures use joined errors: the initiating Automations failure remains discoverable alongside earlier worker cleanup failure, while acquired resources close in reverse order and later assembly is not called.
---
## [2026-08-10 07:31 -07:00] - btrc-p0-characterization-009 verification follow-up
- What was implemented
  - Refactored the concurrent target characterization into a deterministic fixture and focused assertion helpers so the new test stays within repository maintainability limits.
  - Consolidated the replay characterization into the existing `pkg/services/recordings/internal/replay/runtime_decoder_test.go` file and split its success assertions, preserving the same observable replay coverage while restoring package file-count and maintainability baselines.
  - Updated the replay story note in `prd.json` to match the reconciled file location; the PR description will be refreshed from this exact PRD.
- Files changed
  - `pkg/services/factory_sessions/internal/ondemandtarget/target_execution_test.go`
  - `pkg/services/recordings/internal/replay/runtime_decoder_test.go`
  - `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go` (consolidated/deleted)
  - `prd.json`
  - `progress.txt`
- Verification
  - `make test` — passed (short Go suite).
  - `make backend-size` — passed.
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - Affected replay and concurrent-target repeat/race tests and vet — passed.
- **Learnings for future iterations:**
  - Characterization tests should be split into owner-focused helpers before running `pkg-maint`; detailed observable assertions can remain rich without making one test function exceed complexity or line limits.
  - When a P0 test adds a file to a package at its exact file-count baseline, consolidate it into a semantically adjacent existing test file rather than recording a new baseline entry.
---
## 2026-08-10 07:53 -07:00 - btrc-p0-characterization-003 review follow-up
- What was implemented
  - Reconciled the branch with current `upstream/main`, retaining the active BTRC `prd.json`, both append-only progress histories, and the current-main source/generated changes.
  - Moved the hosted characterization into the approved `tests/functional/workstations/poller/` domain/subsection cell.
  - Replaced hosted test timeout waits with `testing.T` context and a context-aware Factory Event stream observation helper.
  - Added explicit continuous-service result reconciliation: success and execution failure assert the typed `NOT_FOUND` live `/result` response while dispatch plus terminal Work/progress remains the complete caller-visible service result. The in-code comment documents why this hosted service path uses the public HTTP inspection contract; finite CLI result mapping is covered by story 005.
- Files changed
  - `tests/functional/workstations/poller/hosted_characterization_test.go`
  - `tests/functional/internal/support/factory_event_stream.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workstations/poller -run '^TestBTRCP0HostedService' -count=1 -v -timeout 180s` — passed.
- **Learnings for future iterations:**
  - The hosted/service success contract is intentionally not a terminal live Factory Session result: continuous Petri sessions stay IDLE, so `/result` returns typed NOT_FOUND while dispatch response, terminal Work, and progress are the stable service result.
  - Moving a functional scenario into an approved domain/subsection can preserve its package-private fixture helpers when the destination owns the same workstation behavior.
---
## 2026-08-10 08:10 -07:00 - btrc-p0-characterization review follow-up
- What was implemented
  - Addressed the blocking PR gate feedback by moving the batch characterization to `tests/functional/factory/batch/` and both one-shot characterizations to `tests/functional/factory/invocation/`, clearing the ratcheted functional package-structure violations.
  - Replaced the batch-specific in-process `ProviderOverride` with the existing `ProviderCommandRunner` edge and queued shaped command results; success remains two accepted dispatches and the failure case now observes the real command-edge `permanent_bad_request` classification on the second work.
  - Added an in-code explanation for the ACP completion ceiling: public terminal Factory Session observation is the positive synchronization, while the duration is only a bounded failure guard around an out-of-process protocol edge.
  - Added an observable runtime-preservation acceptance criterion to story `btrc-p0-characterization-001` and reconciled its notes and all moved test paths in `prd.json`.
- Files changed
  - `tests/functional/factory/batch/batch_characterization_test.go`
  - `tests/functional/factory/invocation/characterization_test.go`
  - `tests/functional/factory/invocation/btrc_p0_cancellation_characterization_test.go`
  - `tests/functional/providers/acp/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/batch -count=3 -v -timeout 300s` — passed.
  - `go test ./tests/functional/factory/invocation -count=3 -v -timeout 300s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=3 -v -timeout 300s` — passed.
  - `make pkg-structure` — passed with no new violations.
  - `go vet ./tests/functional/factory/batch ./tests/functional/factory/invocation ./tests/functional/providers/acp` — passed.
  - `git diff --check` — passed.
  - Parsed `prd.json` reports zero stories with `passes:false`.
- **Learnings for future iterations:**
  - The prior PR head’s Backend Functional Coverage failure was an unrelated flaky `runtime_api` log assertion (`TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`); the required local characterization and structure gates remain green, so rerun CI after publishing before changing unrelated runtime-api behavior.
  - `gh pr view` confirmed current `upstream/main` is already the PR base and the PR is mergeable; compare the explicit base/head SHAs before creating a needless merge commit.
---
## 2026-08-10 08:16 -07:00 - btrc-p0-characterization review verification follow-up
- What was implemented
  - Completed the local review-gate verification after the path, edge, ACP documentation, and PRD acceptance updates; no production behavior or public/generated contract changed.
- Files changed
  - No additional tracked files.
- Verification
  - `make test` — passed.
  - `make backend-size` — passed.
  - `make pkg-maint` — passed.
  - `make functional-boundary-check` — passed.
  - `go test ./pkg/services/providers/internal/services/catalog/internal/service ./pkg/services/providers/transports/cli ./tests/functional/providers/discovery/... -count=1` — passed.
  - Direct package coverage: catalog 92.5% against 89.47%; Providers CLI 82.6% against 82.00%.
  - Targeted `gocoveragecheck` for both affected packages — passed at 88.1% total against 75.9%.
  - The full `make test-unit-coverage` wrapper could not launch on this Windows host because its generated all-package `go test` command exceeds the Windows argument-length limit.
- **Learnings for future iterations:**
- The prior GitHub functional-coverage failure is reproducible as a transient baseline failure rather than a deterministic source failure; fresh CI after this head is the authoritative check.
  - Current functional-boundary checks require `support.BuildProcess` plus exact `serviceedges.Edges` replacements for provider functional composition; direct `pkg/root` imports are rejected even when the test injects all external effects.
  - Coverage manifests enforce per-package floors, so added catalog/CLI branches need observable behavioral tests; lowering the checked-in floor would hide the regression.
---
## 2026-08-10 - e2-structured-worker-results CI follow-up
- What was implemented
  - Repaired the existing worker-session pause test's completion synchronization so it closes the simulated control-done channel after installing the terminal snapshot instead of continuously feeding an unclosed channel.
  - This removes the coverage-sensitive deadlock that caused the latest PR backend unit-coverage lane to time out after ten minutes.
- Files changed
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
- Verification
  - `go test ./pkg/services/worker_sessions/internal/service -run '^TestPause_ControlOutcomesKeepTheLifecycleTruthful$' -count=100 -timeout 120s` — passed.
  - `go test ./pkg/services/worker_sessions/internal/service -count=1 -timeout 300s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Tests modeling completion signals should close the completion channel once the authoritative state is installed; an unbounded sender can deadlock under coverage scheduling even when focused runs usually pass.
---
## 2026-08-10 10:19 -07:00 - e2-structured-worker-results-003 review follow-up
- What was implemented
  - Replaced raw JSON Schema validation-error formatting with a bounded, customer-safe summary built from stable instance, output-schema, and keyword locations; required-property, type, pattern, and format diagnostics avoid value-bearing library text and host paths.
  - Added focused scalar/pattern coverage proving rejected worker content is absent, the diagnostic remains actionable, and long diagnostics are bounded; extended the root-built functional failure-routing scenario with the same pattern case.
  - Constructed only the new prompt test fixtures with Workers-owned token contracts, restoring the exact existing Petri public-surface baseline without refreshing debt.
- Files changed
  - `pkg/services/workers/internal/services/workstations/executor/helpers.go`
  - `pkg/services/workers/internal/services/workstations/executor/agent_inference_test.go`
  - `pkg/services/workers/internal/services/workstations/prompting/prompt_test.go`
  - `tests/functional/workstations/execution/structured_result_prompt_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused executor and prompting tests — passed.
  - Full affected executor/prompting packages — passed.
  - Root-built functional schema-violation routing test with malformed and pattern-invalid responses — passed.
  - Targeted `go vet` — passed.
  - `backend-size`, `pkg-maint`, `pkg-file-count`, and `pkg-structure` — passed.
  - `pkg-boundary` reports the known 176-violation main baseline and no new findings; `git diff --check` — passed.
- **Learnings for future iterations:**
  - `jsonschema.ValidationError.Error()` and localized keyword messages can include rejected values; use `ErrorKind.KeywordPath()` and typed schema metadata, never `%w` or localized text, when crossing the worker failure boundary.
  - The jsonschema compiler expands resource IDs to absolute file URLs and may append a fragment, so customer diagnostics should use a stable authored label such as `output schema#/pattern`.
  - The remaining review blocker is story 004’s watch null sentinel collision; keep that fix isolated to the next story iteration.
---
## 2026-08-10 10:45 -07:00 - e2-structured-worker-results-004 review follow-up
- What was implemented
  - Removed the customer-representable string sentinel used to carry explicit JSON null through the generated `interface{}` watch payload projection.
  - Preserved raw canonical event payload fields until reducer application, restoring only an actually present JSON `null` as an internal `json.RawMessage("null")`; omitted fields remain absent and valid customer strings, including the former sentinel text, remain strings.
  - Added decode, reducer, and rendered NDJSON assertions proving the exact scalar string and explicit null remain distinct.
  - Marked story `e2-structured-worker-results-004` as passed in `prd.json`.
- Files changed
  - `pkg/services/work/transports/cli/list.go`
  - `pkg/services/work/transports/cli/watch.go`
  - `pkg/services/work/transports/cli/watch_reducer.go`
  - `pkg/services/work/transports/cli/watch_retry_test.go`
  - `pkg/services/work/transports/cli/watch_stream.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused watch null/string regression tests — passed.
  - `go test ./pkg/services/work/transports/cli -count=1 -timeout 300s` — passed.
  - `go test ./pkg/services/work/... ./pkg/transports/cli/... ./tests/functional/work/... -count=1 -timeout 300s` — passed.
  - `go vet ./...`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, `make typecheck`, and `make test` — passed.
  - `make contracts-check cli-manifest-check docs-reference-smoke` — passed.
  - `make lint` reached the unchanged repository `pkg-boundary` baseline of 176 violations; no changed file appears in that inventory. `git diff --check` — passed.
- **Learnings for future iterations:**
  - Generated OpenAPI `interface{}` fields collapse explicit null during decode; preserve presence from the raw canonical event payload and delay normalization until the reducer owns the value.
  - A string sentinel is unsafe for any native JSON channel because every valid JSON string is customer-representable; use a sidecar presence bit or raw field metadata instead.
---
## 2026-08-10 09:13 -07:00 - sac-s0-1-ownership-inventories current-main conflict follow-up
- What was implemented
  - Merged the server's current `main` head `59fd5748` into the SAC branch after GitHub reported the existing PR as conflicting.
  - Preserved the SAC `prd.json` as the authoritative PR description and retained both branches' append-only `progress.txt` histories while resolving the bookkeeping conflicts.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main characterization sources and tests were retained by the merge.
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `git diff --check` and PRD JSON validation — passed.
  - Merge commit `b13db64f0` is pushed; fresh required CI and final PR mergeability are pending.
- **Learnings for future iterations:**
  - Compare the server's live base SHA with the local remote-tracking ref before treating a PR as conflict-free; stale base metadata can hide a conflict in shared `prd.json` and `progress.txt`.
---
## 2026-08-10 09:45 -07:00 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the first latest-head CI failure and confirmed that Backend Functional Coverage failed in unchanged timing-sensitive tests: two restart/history cancellation observations and the CLI writer-failure cancellation race.
  - Reran the exact restart and CLI writer tests locally 10 times each; all passed. The first failed-job retry moved to the known JavaScript concurrency observation, which passed five consecutive focused repetitions after one intermittent failure in a 20-run stress sample.
  - Reran only the failed GitHub jobs; the latest-head run completed successfully.
- Files changed
  - `progress.txt`
- Verification
  - Focused restart tests — passed 10 repetitions.
  - Focused CLI writer-failure test — passed 10 repetitions.
  - Focused JavaScript parallel-dispatch test — passed five consecutive repetitions after the isolated stress flake.
  - GitHub Actions run `31407831161` — all 18 checks passed, including Backend Functional Coverage and Verification Policy, on head `5ed5afbcf`.
- **Learnings for future iterations:**
  - For broad functional-coverage failures, inspect the first assertion; unchanged cancellation/concurrency observations can move between tests across retries. Repeat the exact test and rerun failed jobs before changing unrelated runtime code.
---
## 2026-08-10 10:25 -07:00 - sac-s0-1-ownership-inventories-003 review follow-up
- What was implemented
  - Fixed the provider-effect boundary checker panic on valid grouped Go parameters such as `Infer(ctx, req context.Context)` by expanding grouped AST fields before signature inspection.
  - Added a focused regression proving the malformed grouped Workers-root compatibility port is rejected with the normal ownership diagnostic and never panics.
  - Reclassified story `sac-s0-1-ownership-inventories-003` as a `project-enabling` criterion because its architecture-guard evidence is repository-enforcement behavior rather than customer runtime/API/CLI/UI/event behavior; the remaining package-target and ownership inventory stories still need the same review-gate acceptance-shape decision.
- Files changed
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run '^TestRun(AllowsWorkersRequestScopedProviderCompatibilityPort|RejectsGroupedWorkersRequestScopedProviderPortWithoutPanic|RejectsExpandedWorkersRequestScopedProviderPort)$' -count=10` — passed.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `go vet ./cmd/pkgboundarycheck` — passed.
  - `git diff --check` and PRD JSON validation — passed.
- **Learnings for future iterations:**
  - The latest review still blocks the PR on acceptance evidence for stories 001 and 002; inventory/package-target checker tests are repository-enforcement evidence, not customer-facing behavioral coverage under the review gate. Address those stories in separate iterations by moving them to explicit project-enabling criteria or adding appropriate observable contract behavior.
---
## 2026-08-10 10:26 -07:00 - sac-s0-1-ownership-inventories-003 PR handoff follow-up
- What was implemented
  - Re-ran the package-target manifest gate sequentially after an initial parallel Go-process file-lock message; the standalone gate completed cleanly with 545 inventory rows and 545 production package rows.
  - Updated PR #1848 from the current `prd.json`, verified the description matches the file, and posted resolution evidence for the grouped-AST blocker. The thread-aware audit found no inline review threads; stories 001 and 002 remain the explicit acceptance-shape follow-up.
- Files changed
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - PR description comparison against `prd.json` — passed.
  - Worktree clean and `git diff --check` — passed.
- **Learnings for future iterations:**
  - On Windows, avoid running independent `go run` Make gates in parallel when they may contend for the generated executable; a sequential rerun distinguishes a transient file-lock message from a gate failure.
---
## 2026-08-10 10:28 -07:00 - sac-s0-1-ownership-inventories-001 review follow-up
- What was implemented
  - Reclassified `sac-s0-1-ownership-inventories-001` from a behavioral user story to an explicit `project-enabling` criterion in `prd.json`, because the package-target manifest is repository-enforcement and maintainer tooling rather than customer runtime, API, CLI, UI, or emitted-event behavior.
  - Preserved the completed package-target acceptance evidence and added an explicit no-runtime/API/CLI/UI/event-behavior criterion so the PRD matches the review gate's acceptance model.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/packagetargetmanifestcheck -run 'Test(PackageTarget|Manifest|Coverage|Owner|Edges)' -count=1` — passed.
  - PRD JSON validation reports story 001 exactly once under `enablingCriteria`, no failed items, and one remaining behavioral user story (002).
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Repository inventory and architecture-guard checks are valid enforcement evidence, but they do not satisfy a customer-behavior acceptance gate; model genuinely non-behavioral work as explicit project-enabling criteria.
  - The broad `go test ./cmd/packagetargetmanifestcheck` package also exercises current-main Workers/Recordings root-file inventories and currently reports unrelated baseline drift (`kind_phase_validation.go` and `lifecycle_capability.go`); the public package-target Make gate and focused manifest tests remain green.
  - Story 002 still needs the same acceptance-shape follow-up in a separate iteration; do not combine it with this story's bookkeeping change.
---
## 2026-08-10 10:34 -07:00 - sac-s0-1-ownership-inventories-002 review follow-up
- What was implemented
  - Reclassified `sac-s0-1-ownership-inventories-002` from a behavioral user story to an explicit `project-enabling` criterion in `prd.json`, because the ownership inventory and reciprocal-edge validation are repository-enforcement and maintainer tooling rather than customer runtime, API, CLI, UI, or emitted-event behavior.
  - Preserved the completed package, cross-owner classification, present-tense rationale, and unresolved-bidirectional-edge acceptance evidence, and added an explicit no-runtime/API/CLI/UI/event-behavior criterion.
  - Corrected the existing trailing comma so `prd.json` is valid standard JSON.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed.
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 package rows).
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - PRD validation reports valid JSON, four unique passing enabling criteria, and no behavioral user stories.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Acceptance criteria for repository-enforcement work should explicitly identify the project-level nature of the evidence and state the intentionally unchanged customer-facing behavior; keep the PRD valid JSON so automated consumers do not depend on permissive parsers.
---
## 2026-08-10 11:07 -07:00 - e2-structured-worker-results current-main conflict follow-up
- What was implemented
  - Merged the current `origin/main` head `cba6b45f999c5537355d2b7ff0aee2a02590e729` into the structured-results branch after the PR reported conflicts in `prd.json` and `progress.txt`.
  - Preserved the E2 task-scoped `prd.json` byte-for-byte for PR-description parity and combined the append-only progress histories, including current-main reusable patterns and verification entries.
  - Retained current-main ownership-inventory and architecture-guard source/generated changes in the merge without widening the structured-results implementation scope.
- Files changed
  - Current-main ownership inventory, architecture, guard, and provider-port files retained by the merge.
  - `progress.txt`
  - `prd.json` was retained as the authoritative E2 task PRD.
- Verification
  - Current-main merge conflict markers removed; `prd.json` parses with four passing E2 stories.
  - `prd.json` remains unchanged from the pre-merge task head, preserving the PR description source.
  - Further focused and repository gates are run after this merge before publishing the updated PR head.
- **Learnings for future iterations:**
  - When merging a newer default branch into a task branch, resolve shared bookkeeping by preserving the active task PRD exactly and retaining both branches' append-only progress entries; validate the PRD before staging.
---
## 2026-08-10 11:20 -07:00 - e2-structured-worker-results inventory reconciliation
- What was implemented
  - Regenerated the current-main package-target manifest and ownership freeze after the merge exposed the E2 production package `pkg/platform/jsonvalue` as missing from both inventories.
  - Kept the inventory change limited to the required platform package row and the deterministic ownership evidence/edge updates; no structured-result runtime behavior changed.
- Files changed
  - `docs/internal/packaged-service-structure/package-target-manifest.json`
  - `docs/internal/baselines/ownership-inventory.json`
- Verification
  - `make package-target-manifest-check` — passed with 546 inventory/package rows.
  - `make ownership-inventory-check` — passed.
  - `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, `make vet`, `make typecheck`, and `make test` — passed.
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - `go test ./cmd/packagetargetmanifestcheck -count=1` still reports the documented current-main Workers/Recordings root-contract inventory drift (`kind_phase_validation.go` and `lifecycle_capability.go`); the public package-target gate is green and no new E2 failure is reported.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a task branch adds a production package before current-main inventories catch up, regenerate both the package-target manifest and ownership freeze; validating only the public package-target gate will not update the ownership completeness ledger.
---
## 2026-08-10 11:35 -07:00 - e2-structured-worker-results post-merge verification
- What was implemented
  - Completed the post-merge verification pass for the structured-results head; no additional runtime or public-contract changes were needed after current-main synchronization and inventory regeneration.
- Files changed
  - `progress.txt`
- Verification
  - Focused worker executor, prompting, Work CLI, and CLI Work packages — passed.
  - Root-built functional structured-result prompt and Work watch scenarios — passed.
  - `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, `make package-target-manifest-check`, `make ownership-inventory-check`, `make vet`, `make typecheck`, and `make test` — passed.
  - `make lint` passed all checks through the repository’s known `pkg-boundary` migration baseline and reported 175 accepted baseline violations; no new E2-specific finding was identified.
  - The direct `cmd/packagetargetmanifestcheck` package test retains the documented current-main Workers/Recordings root-contract inventory drift, while its public Make gate remains green.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A green public inventory gate can coexist with stale direct root-contract characterization fixtures from current main; record that distinction rather than changing unrelated baseline inventories during a feature handoff.
---
## 2026-08-10 11:40 -07:00 - e2-structured-worker-results CI follow-up
- What was implemented
  - Investigated the first post-merge GitHub Actions failure in the current-main `TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle` scenario; no E2 source change was warranted because the failure was the known timing-sensitive `factory runtime is already stopped` path.
  - Repeated the focused mock-worker test five times locally and reran only the failed GitHub job.
- Files changed
  - `progress.txt`
- Verification
  - The focused mock-worker test passed all five local repetitions.
  - GitHub Actions run `31418506172`, attempt 2, passed all 17 checks, including Backend Functional Coverage and Verification Policy.
  - The PR head remains based on the current `origin/main`; no unresolved inline review threads were found.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - For a known current-main runtime-shutdown race, first repeat the exact behavioral test locally and rerun the failed CI job before changing unrelated feature code; a clean repeated run can distinguish transient timing from a deterministic regression.
---
## 2026-08-10 11:51 -07:00 - e2-structured-worker-results CI follow-up
- What was implemented
  - Investigated the second post-merge GitHub Actions failure in current-main `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`; the test intermittently missed the `factory started` log line, and no E2 source change was warranted.
  - Repeated the exact runtime-log test five times locally and reran only the failed functional/policy jobs.
- Files changed
  - `progress.txt`
- Verification
  - The focused runtime-log test passed all five local repetitions.
  - GitHub Actions run `31419909852`, attempt 1, failed only Backend Functional Coverage on that timing-sensitive test; Backend Unit Coverage and all other checks passed.
  - GitHub Actions run `31419909852`, attempt 2, passed Backend Functional Coverage and Verification Policy; all PR checks are now green.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Runtime file-log assertions can race startup even when the runtime behavior is correct; reproduce the exact test and rerun the failed CI lane before modifying unrelated logging or structured-result code.
---
## 2026-08-10 12:09 -07:00 - e2-structured-worker-results current-main synchronization
- What was implemented
  - Merged the latest `origin/main` at `947152348` into the E2 branch after the PR base advanced again, preserving the E2 PRD/progress history and combining structured-result contracts with current-main expected-artifact contracts.
  - Regenerated the bundled OpenAPI, Go HTTP clients/server, UI clients, packaged API contracts, and ownership freeze from the resolved authored sources.
- Files changed
  - Current-main artifact-registry source, contract, test, documentation, and generated files retained by the merge.
  - `api/components/schemas/events/WorkFailureType.yaml` and the shared runtime/read/event contracts were resolved to retain both failure vocabularies and both result channels.
  - `docs/internal/baselines/ownership-inventory.json`
  - `progress.txt`
- Verification
  - E2 structured-result prompt/watch, executor/prompting, Work CLI, and event projection tests — passed.
  - Current-main expected-artifact projection and mock-worker tests — passed.
  - `make package-target-manifest-check`, `make ownership-inventory-check`, `make vet`, `make typecheck`, and `make test` — passed.
  - `git diff --check` — passed; the merge commit is `841398a6f`.
- **Learnings for future iterations:**
  - When main advances with an adjacent result-channel feature, resolve shared contracts additively and regenerate every public interface; then freeze ownership inventories from the merged tree before running repository gates.
---
## 2026-08-10 12:22 -07:00 - e2-structured-worker-results CI follow-up
- What was implemented
  - Investigated the latest-head functional CI failure in current-main `TestFactorySessionResumeDoesNotRepeatCompletedDispatch`; no E2 source change was warranted because the failure was the known timing-sensitive provider-context cancellation assertion.
  - Repeated the exact restart test five times locally and reran only the failed functional/policy lane.
- Files changed
  - `progress.txt`
- Verification
  - The focused restart test passed all five local repetitions.
  - GitHub Actions run `31422767531`, attempt 1, failed only Backend Functional Coverage on the restart timing assertion; every other lane passed.
  - GitHub Actions run `31422767531`, attempt 2, passed Backend Functional Coverage and Verification Policy; all PR checks are green.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Session restart cancellation assertions are timing-sensitive under the full functional lane; reproduce the exact test and rerun the failed lane before altering unrelated runtime result-channel code.
---
## 2026-08-10 12:33 -07:00 - e2-structured-worker-results final PR audit
- What was implemented
  - Re-read the authoritative `prd.json` and confirmed all four E2 stories remain marked `passes:true`; no remaining story implementation is required.
  - Inspected PR #1855 with thread-aware GitHub review data. The earlier schema-diagnostic, watch null-collision, and lint-baseline blockers are documented as cleared; the later current-main conflict is resolved in the branch.
  - Preserved `prd.json` unchanged so the PR description remains byte-for-byte aligned with the task PRD.
- Files changed
  - `progress.txt`
- Verification
  - `prd.json` parses and all four story pass states are true.
  - PR #1855 is based on current `origin/main`, mergeable, and has no unresolved review threads or review submissions.
  - All reported required GitHub checks are terminal and successful on head `e30fa8084307d7020d6d479c5f27daaae46887da`.
  - `git diff --check` — passed; the worktree was clean before this bookkeeping entry.
- **Learnings for future iterations:**
  - A prior blocking conversation comment can remain visible after its findings are cleared; use the latest head-specific comment plus thread-aware review state, current-base mergeability, and terminal checks to determine whether follow-up is actually outstanding.
---
## 2026-08-10 12:53 -07:00 - e2-structured-worker-results final CI follow-up
- What was implemented
  - Investigated the post-audit CI failure without changing structured-result code: the first run lacked the unrelated pinned ACP evidence artifact, and the first retry exposed the known current-main `TestFactorySessionResumeDoesNotRepeatCompletedDispatch` cancellation-timing flake.
  - Reproduced that exact test successfully five times locally and reran the failed GitHub jobs; the final rerun completed the functional coverage lane and pinned real-ACP evidence successfully.
- Files changed
  - `progress.txt`
- Verification
  - All PR #1855 checks are terminal and successful on the pushed head before this bookkeeping entry, including Backend Functional Coverage, Backend Unit Coverage, Verification Policy, and all contract, frontend, package, documentation, and inference lanes.
  - No new source or generated-file changes were needed; the failure was isolated to external CI timing/evidence behavior.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a full functional lane first fails in post-test evidence publication and then exposes a known timing-sensitive current-main test on retry, reproduce the exact test locally, rerun the failed jobs on the same head, and avoid unrelated production changes when the rerun passes.
---
## 2026-08-10 13:04 -07:00 - e2-structured-worker-results final handoff audit
- What was implemented
  - Re-read `prd.json` and confirmed all four E2 stories remain marked `passes:true`; no remaining story implementation is required.
  - Inspected PR #1855 with the thread-aware review workflow. There are no inline review threads or unresolved actionable comments; the earlier schema-diagnostic, watch null-collision, lint-baseline, and current-main conflict blockers are cleared in the branch history.
  - Preserved `prd.json` unchanged so the PR description remains aligned with the task PRD.
- Files changed
  - `progress.txt`
- Verification
  - `prd.json` parses with four passing stories and zero failed stories.
  - `origin/main` is an ancestor of the branch, `git merge-tree --write-tree HEAD origin/main` reports no conflicts, and PR #1855 is `MERGEABLE`/`CLEAN`.
  - `gh pr checks 1855` — all 18 required checks passed, including Backend Unit Coverage and Verification Policy.
  - `git diff --check` — passed before this bookkeeping append.
- **Learnings for future iterations:**
  - Once prior blocking comments are documented as cleared, validate the current head with thread-aware review state, base ancestry/conflict checks, and terminal `gh pr checks`; visible historical rejection comments do not by themselves indicate outstanding work.
---
## 2026-08-10 13:20 -07:00 - e2-structured-worker-results CI follow-up
- What was implemented
  - Investigated the fresh PR run failure in `TestFactorySessionResumeDoesNotRepeatCompletedDispatch`; the failure was the known timing-sensitive `provider Infer did not observe canceled workflow context` assertion at `logical_identity_test.go:124`.
  - Repeated the exact functional test five times locally and reran the failed GitHub jobs on the same head; no E2 source change was warranted.
- Files changed
  - `progress.txt`
- Verification
  - The focused restart test passed all five local repetitions.
  - GitHub Actions run `31427246233` rerun passed Backend Functional Coverage, pinned real ACP evidence, Verification Policy, and all other required lanes on head `b97c093f0`.
  - `git diff --check` — passed before this bookkeeping append.
- **Learnings for future iterations:**
  - When the full functional lane fails only at a known cancellation-observation assertion, reproduce that exact test repeatedly and rerun the failed jobs before changing unrelated runtime code; a clean rerun distinguishes an intermittent current-main timing failure from a feature regression.
---
## 2026-08-10 13:28 -07:00 - e2-structured-worker-results latest-head handoff
- What was implemented
  - Confirmed the documentation-only CI follow-up commit is the current PR head and that no additional story or review-thread work remains.
- Files changed
  - `progress.txt`
- Verification
  - PR #1855 latest head `410e1b906` has a matching `prd.json` description, current `origin/main` ancestry, and no merge conflicts.
  - GitHub Actions run `31428603825` — all required checks passed, including Backend Functional Coverage and Verification Policy.
  - The worktree is clean and `git diff --check` passed before this append.
- **Learnings for future iterations:**
  - After a CI-only bookkeeping push, recheck the exact latest head rather than relying on an earlier rerun attached to the previous commit; the final PR state must be validated after the last push.
  - Keep preflight ownership policy in a focused helper when extending an already complex service operation; the repository maintainability gate enforces a cyclomatic limit of 15.
---
## 2026-08-09 23:37 -0700 - wo-b3-provider-capability-discovery-cli-ci-rerun
- What was implemented
  - Investigated the fresh-head functional coverage failure in the unchanged packaged-loop lane; the exact `TestPackagedLoopUsesInvocationDurationAndSkipsOverlap` test passed through 20 local repetitions, so no unrelated production fix was introduced.
  - Reran the failed GitHub Actions job and retained the provider discovery implementation and coverage fixes as the only source changes.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/loop -run '^TestPackagedLoopUsesInvocationDurationAndSkipsOverlap$' -count=20 -v -timeout 900s` — passed all 20 runs.
  - `gh run rerun 31361833511 --failed --repo portpowered/you-agent-factory` — attempt 2 passed Backend Functional Coverage and Verification Policy.
  - All 18 required PR checks are terminal and passing on head `24a966c18`; PR #1843 is mergeable with no comments or formal reviews.
- **Learnings for future iterations:**
  - A single timeout in the unchanged packaged-loop functional lane should be reproduced in isolation before changing feature code; a stable local repetition supports rerunning the failed CI job instead.
  - When the rerun has no new source changes, record the exact test and terminal check state in the append-only progress log so the PR handoff remains auditable.
---
## 2026-08-09 23:47 -0700 - wo-b3-provider-capability-discovery-cli-delivery-review
- What was implemented
  - Confirmed all three PRD stories remain marked `passes:true`; no additional story implementation or authored contract change was required.
  - Rechecked PR #1843 in `portpowered/you-agent-factory`: the local head `c57a47de5012033c60a212a41873e2975f8cc266` matches the PR head, GitHub reports the PR mergeable, and the PR description remains the current `prd.json` content.
  - Used the thread-aware GitHub review fetch to confirm there are no conversation comments, formal reviews, or unresolved inline review threads.
- Files changed
  - `progress.txt`
- Verification
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 required checks pass.
  - `git status --short --branch` — clean before this progress-only append.
- **Learnings for future iterations:**
  - For a cross-repository PR whose local branch is associated with a different default remote, pass the PR head repository explicitly to `gh` and the thread-aware review fetch instead of relying on branch auto-discovery.
  - When all stories, checks, and review threads are complete, preserve `prd.json` and append the delivery evidence to `progress.txt`; no source churn is needed.
---
## 2026-08-10 00:17 -0700 - wo-b3-provider-capability-discovery-cli-review-follow-up
- What was implemented
  - Addressed the blocking PR conversation feedback on provider capability truthfulness. The authored Codex manifest now publishes the exact `gpt-5.6`, `gpt-5.6-luna`, `gpt-5.6-sol`, and `gpt-5.6-terra` model IDs with shared effort and modality facts.
  - Changed AGY discovery to publish explicit empty per-model effort arrays because AGY effort is encoded in exact model IDs and the canonical Providers execution path rejects a separate `ReasoningEffort`; added the named `effort_selection` limit alongside `--add-dir` and `print_timeout`.
  - Added a behavioral root-service regression that lists the catalog and executes every advertised AGY model/effort combination through the canonical execution policy, so a future rejected advertised effort fails at runtime rather than only through a source/inventory assertion.
  - Updated human/JSON functional and CLI output assertions, catalog clone/order tests, provider reference guidance, PRD notes, and regenerated the published provider catalog/package metadata from authored manifests.
- Files changed
  - `packages/model-providers/providers/codex/provider.yaml`
  - `packages/model-providers/providers/antigravity/provider.yaml`
  - `packages/model-providers/generated/catalog.json`
  - `packages/model-providers/metadata/manifest.json`
  - `packages/model-providers/catalog_test.go`
  - `pkg/services/providers/internal/service/service_test.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/service_test.go`
  - `pkg/services/providers/transports/cli/list_capability_output_test.go`
  - `tests/functional/providers/discovery/discovery_test.go`
  - `docs/reference/providers.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make provider-catalog-generate` and `make model-provider-package-generate` — passed.
  - `make provider-catalog-check` and `make model-provider-package-check` — passed.
  - Focused provider/catalog/CLI/functional Go tests — passed.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `make docs-reference-smoke`, `make typecheck`, `make test`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - An empty capability list can be an intentional public contract, not missing metadata; pair it with a named constraint when a provider encodes the choice in another exact identifier.
  - Provider model inventories must enumerate exact IDs used by configured Factories; a family label such as `gpt-5.6` is not a substitute for accepted tier IDs.
  - Catalog/execution consistency regressions should invoke the public service operation with catalog-derived values and an injected adapter, allowing policy drift to fail behaviorally without live provider calls.
---
## 2026-08-10 00:51 -0700 - wo-b3-provider-capability-discovery-cli-ci-follow-up
- What was implemented
  - Diagnosed the first fresh CI failure as two unrelated timing-sensitive functional assertions in session execution/restart coverage; both passed 20 consecutive focused local repetitions.
  - Diagnosed the next functional-lane failure as an existing runtime package coverage-floor miss caused by incomplete/nondeterministic broad functional coverage, not a changed runtime package; local coverage likewise hit an unrelated concurrent dispatch assertion.
  - Reran the failed CI lane through three attempts without changing unrelated runtime code. The final backend functional coverage and dependent Verification Policy jobs passed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/sessions/execution -run '^TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal$' -count=20` — passed.
  - `go test ./tests/functional/sessions/restart -run '^TestFactorySessionResumeDoesNotRepeatCompletedDispatch$' -count=20` — passed.
  - Final `gh pr checks 1843 --repo portpowered/you-agent-factory` — all required checks pass on head `28ddae7e6653ec6272cbc47db88f695d8246b401`.
- **Learnings for future iterations:**
  - The functional coverage gate can fail after a broad concurrent test is incomplete, surfacing as a package coverage-floor regression even when the changed package is unrelated; inspect test output and compare coverage artifacts before touching a baseline.
  - A failed-job rerun retains the same workflow run but creates new job IDs; inspect the job list by attempt when confirming the terminal result.
---
## 2026-08-10 00:55 -0700 - wo-b3-provider-capability-discovery-cli-final-docs-follow-up
- What was implemented
  - Clarified in the canonical provider reference that `models` is an exact per-ID inventory and must not be treated as a family alias.
- Files changed
  - `docs/reference/providers.md`
  - `progress.txt`
- Verification
  - The clarification preserves the catalog/output contract and will be covered by the fresh PR workflow on the final head.
- **Learnings for future iterations:**
  - When a final bookkeeping-only commit does not receive a new check suite, a narrowly scoped user-facing clarification can provide a legitimate synchronization change for the normal pull-request workflow.
---
## 2026-08-10 01:05 -0700 - wo-b3-provider-capability-discovery-cli-base-merge-api-follow-up
- What was implemented
  - Reconciled the PR with the current `you-agent-factory` main branch, preserving the B3 PRD while retaining the base branch's B2 source and generated artifacts.
  - Regenerated the API contract manifest after the merge so the generated CLI-command and OpenAPI artifact hashes match their canonical sources.
- Files changed
  - `packages/api/generated/manifest.json`
  - `progress.txt`
- Verification
  - `make generate-api`, `make contracts-generate`, `make contracts-smoke`, `make api-smoke`, and `make api-package-pack-smoke` — passed.
  - The initial current-main CI run's API manifest failures are resolved locally; a fresh run is pending on the pushed merge head.
- **Learnings for future iterations:**
  - After merging a newer main into a PR, run the contract generator even when the feature does not change the API; generated package manifests can encode hashes for base-branch artifacts changed by the merge.
---
## 2026-08-10 01:18 -0700 - wo-b3-provider-capability-discovery-cli-final-handoff
- What was implemented
  - Completed the current-main merge and generated-contract reconciliation, including the API manifest refresh required by the base branch's newer generated CLI/OpenAPI artifacts.
  - Confirmed the blocking provider-capability conversation is addressed in the authored catalog, generated output, execution-policy regression, and reference documentation.
- Files changed
  - `progress.txt`
- Verification
  - Final CI run `31368861656` completed successfully on head `897830ffaf57a24d2c331641b88e55bfedda6bb4`; all 18 PR checks pass.
  - PR #1843 is open, mergeable, clean, and its description matches `prd.json`; all PRD stories have `passes:true`.
  - Thread-aware review inspection reports zero inline review threads and zero formal reviews; the only conversation item is the addressed top-level blocker.
  - `git status --porcelain` is clean.
- **Learnings for future iterations:**
  - Final readiness requires checking the exact PR head against the terminal CI run, not only the latest successful run visible for an earlier commit.
---
## 2026-08-10 01:31 -0700 - wo-b3-provider-capability-discovery-cli-final-ci-rerun
- What was implemented
  - Confirmed the final-head functional-coverage retry passed after the isolated durability test reproduced cleanly locally.
- Files changed
  - `progress.txt`
- Verification
  - Final-head run `31369496270` now has all 18 checks passing, including Backend Functional Coverage and Verification Policy, on head `20a23e149a812ff835b1996a2deea425910eb337`.
- **Learnings for future iterations:**
  - Preserve the exact failing test, focused repetition result, rerun ID, and final head in the append-only log when CI recovery uses a known flaky lane.
---
## 2026-08-10 01:50 -0700 - wo-b3-provider-capability-discovery-cli-final-review
- What was implemented
  - Confirmed the three PRD stories remain marked `passes:true`; no additional story implementation or authored contract change was required.
  - Inspected PR #1843 through the GitHub connector and the bundled thread-aware `gh` review workflow. The earlier blocking top-level feedback is addressed by the existing provider-policy alignment commits; no inline review threads or formal reviews remain.
  - Confirmed the PR head, fork branch, and local `HEAD` all match `7cb98beaa33dc3cfac028478efa11406eb54dcd2`, the PR is open/mergeable/non-draft, and its 18 reported checks pass.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because every story already passes and the PR description is an exact content match.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - PRD pass-state, PR description parity, thread-aware comment, and all-checks inspection — passed.
- **Learnings for future iterations:**
  - For cross-repository PRs, compare local `HEAD`, the fork branch ref, and the explicit PR head SHA; current-branch auto-discovery can inspect the wrong remote.
  - A top-level review comment may remain in the conversation after its requested fix is present; use commit ancestry plus thread-aware state to distinguish addressed feedback from unresolved inline work.
---
## 2026-08-10 02:05 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Diagnosed the fresh-head `Backend Functional Coverage` failure as an existing nondeterministic package-floor fluctuation: unchanged `dispatch_planning` measured 62.5468% against 62.92%, and unchanged orchestration runtime measured 52.7349% against 52.80%.
  - Confirmed neither failing package is touched by this PR, changed no feature code or coverage baseline, and reran only the failed jobs with `gh run rerun 31371949039 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
  - `prd.json` remains unchanged and all stories remain `passes:true`.
- Verification
  - Retry Backend Functional Coverage job `93404454469` — passed.
  - Retry Verification Policy job `93405593441` — passed.
  - All 18 PR checks are green on head `de6db78a47666ecc94861ae645b18d6635685a29`.
- **Learnings for future iterations:**
  - For functional coverage failures, compare the reported package-floor deltas with the PR file list before changing code; a small drop in untouched packages is evidence for a targeted rerun, not a feature regression.
  - The dependent Verification Policy job may appear only after the rerun lane completes, so final readiness requires checking both the lane and its policy gate.
---
## 2026-08-10 02:09 -0700 - wo-b3-provider-capability-discovery-cli-review-audit
- What was implemented
  - Re-read the PRD and consolidated codebase patterns; all three provider discovery stories remain marked `passes:true`.
  - Audited PR #1843 after the existing blocking conversation item. The current head contains the requested catalog-to-execution-policy reconciliation: exact Codex model IDs, explicit AGY empty effort arrays with model-ID selection guidance, and an observable consistency regression; no inline review threads or formal reviews remain.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and acceptance notes are current.
- Verification
  - Local `HEAD` matches PR #1843 head `843750d423b4cc4ae1ff7f5346a15f1b313d7b8b`; the PR is open and mergeable.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - The fresh PR CI run is still in progress; terminal readiness will be checked after it completes.
- **Learnings for future iterations:**
  - A historical top-level blocker can remain in the merged PR timeline after the requested fix is present; verify the current head against the feedback and inspect thread resolution separately before treating it as unresolved work.
  - When the PR head is hosted in a different repository than the local default remote, resolve and inspect it with the explicit owner/repository and compare its SHA to local `HEAD`.
---
## 2026-08-10 02:43 -0700 - wo-b3-provider-capability-discovery-cli-final-delivery-audit
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three stories remain marked `passes:true`.
  - Verified that the existing PR #1843 already contains the latest provider-policy reconciliation requested by its blocking conversation item: exact Codex model IDs, explicit AGY empty effort arrays with model-ID selection guidance, and the catalog-to-execution behavioral regression.
  - Confirmed no inline review threads or formal reviews remain, and the top-level blocker is addressed by the current commits.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states, acceptance notes, and the PR description are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed; worktree clean before this append.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `964eef47692ac9b7d7524dc5c540a558e9864a7b`.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
  - PR #1843 is open, mergeable, non-draft, and its description matches `prd.json`.
- **Learnings for future iterations:**
  - Final delivery audits should compare the exact local, remote-branch, and PR head SHAs, then verify the terminal CI run and PR description before reporting readiness.
  - A historical top-level blocker is addressed when the current head contains its behavioral fix and thread-aware inspection shows no unresolved inline work; its original conversation item may remain as historical context.
---
## 2026-08-10 02:59 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry-final
- What was implemented
  - Diagnosed the first fresh-head CI failure as the unchanged timing-sensitive `TestFactorySessionResumeDoesNotRepeatCompletedDispatch` test; its failure was `provider Infer did not observe canceled workflow context`, unrelated to provider discovery.
  - Reproduced the exact test successfully for 20 isolated repetitions and reran only the failed GitHub jobs with `gh run rerun 31376137369 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
  - No feature code, generated artifact, or coverage baseline was changed for the unrelated retry.
- Verification
  - Isolated restart test: 20/20 repetitions passed.
  - Retry Backend Functional Coverage job `93417380311` — passed.
  - Retry Verification Policy job `93418598263` — passed.
  - All required PR checks are green on head `b0c4850466dbf36faab0de18fa4e2250480a23c0`.
- **Learnings for future iterations:**
  - When a broad functional lane fails on an untouched timing-sensitive test, reproduce that exact test repeatedly before editing feature code; a clean repetition supports a targeted failed-job rerun.
  - Record the retry job IDs and exact final head in the append-only log so CI recovery remains auditable.
---
## 2026-08-10 03:11 -0700 - wo-b3-provider-capability-discovery-cli-delivery-audit
- What was implemented
  - Re-read the PRD and consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`.
  - Audited existing PR #1843 after the prior blocking conversation item. The current head includes the exact Codex model inventory, explicit AGY empty effort arrays with model-ID selection guidance, and the catalog-to-execution behavioral regression requested by the feedback.
  - Confirmed the existing PR is open, mergeable, non-draft, and already points at the latest local and fork branch commit, so no duplicate PR or feature-code change is needed.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and acceptance notes are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `998a9fecdf46c9bcbaaf92bef72ddee0f3d0bf49` before this progress-only append.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
  - Thread-aware PR inspection reports zero inline review threads and zero formal reviews; the prior top-level blocker is addressed by the current commits.
- **Learnings for future iterations:**
  - A review audit should compare the exact local, fork, and PR head SHAs and inspect thread state before deciding whether a duplicate PR or code change is necessary.
  - Progress-only delivery bookkeeping changes the PR head, so the final audit must recheck the corresponding CI run after pushing the append.
---
## 2026-08-10 03:30 -0700 - wo-b3-provider-capability-discovery-cli-final-delivery-check
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`, so no remaining story implementation was required.
  - Inspected existing PR #1843 with the thread-aware GitHub review workflow. It has zero inline review threads and zero formal reviews; the historical top-level blocker is addressed by the current catalog, execution-policy regression, generated artifacts, and documentation.
  - Confirmed the existing PR is the requested named PR, so no duplicate PR was created.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states, acceptance notes, and the PR description are current.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` — passed.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `4e36b453e8768b0364e37f7354e7ceea73dabbcf`.
  - PR #1843 description matches `prd.json`; the PR is open, mergeable, and non-draft.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - When a completed cross-repository PR already contains the requested review fix, verify thread state, exact local/fork/PR SHAs, description parity, and terminal checks before creating any new PR or feature change.
---
## 2026-08-10 04:09 -0700 - wo-b3-provider-capability-discovery-cli-review-follow-up
- What was implemented
  - Addressed the latest blocking PR feedback for the canonical capability-facts story.
  - Added Claude's exact authored model inventory: `claude-opus-4-6-thinking`, `claude-sonnet-4-20250514`, and `claude-sonnet-5`, with explicit text-only modalities and low-through-max effort values.
  - Changed unprobed static catalog projections from fabricated `ready`/`satisfied` values to truthful `unverified`/`required` values, while retaining `ready`/`unavailable`/`missing` for request-time probe facts and allowing catalog-selectable unprobed providers through normal selection.
  - Extended human, JSON, authored-catalog, projection, and production `root.BuildProcess`/`Process.Execute` assertions to cover the new status semantics and Claude inventory; updated the provider reference documentation and PRD notes.
- Files changed
  - `packages/model-providers/providers/claude/provider.yaml`
  - `packages/model-providers/generated/catalog.json`
  - `packages/model-providers/metadata/manifest.json`
  - `packages/model-providers/catalog_test.go`
  - `pkg/services/providers/catalog_contract.go`
  - `pkg/services/providers/internal/service/selection.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/{projection.go,projection_test.go,service.go,service_test.go}`
  - `pkg/services/providers/internal/services/catalog/service.go`
  - `pkg/services/providers/{selection_contract.go,service_contract.go}`
  - `pkg/services/providers/transports/mcp/schemas.go`
  - `tests/functional/providers/discovery/discovery_test.go`
  - `docs/reference/providers.md`
  - `prd.json`
- Verification
  - `make provider-catalog-generate` and `make model-provider-package-generate` — passed.
  - `make provider-catalog-check` and `make model-provider-package-check` — passed.
  - Focused Providers/catalog/CLI/MCP Go tests — passed.
  - `go test ./tests/functional/providers/discovery -count=1 -timeout 120s` — passed.
  - `make docs-reference-smoke`, `make typecheck`, `make test`, and `make vet backend-size pkg-maint pkg-file-count pkg-structure` — passed.
- **Learnings for future iterations:**
  - Static authored provider manifests describe maximum/publication facts only; production catalog projections must never imply current installation, authentication, or executable readiness without a probe.
  - Keep static and live readiness vocabularies distinguishable at the public Providers boundary: `unverified`/`required` describe no probe, while `ready`/`unavailable` and `satisfied`/`missing` describe probe outcomes.
  - Exact model IDs should be asserted at the manifest, projection, JSON, human, and root-process boundaries when docs or execution harnesses name a model.
---
## 2026-08-10 04:24 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Audited the fresh PR workflow for commit `330e7d8e1`; the first attempt failed only in the untouched JavaScript durability test `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` because the provider inference did not observe the canceled workflow context.
  - Confirmed the failure was outside the provider-discovery file set and reran only the failed GitHub jobs with `gh run rerun 31382404004 --failed --repo portpowered/you-agent-factory`.
- Files changed
  - `progress.txt`
- Verification
  - Retry Backend Functional Coverage job `93436868087` — passed.
  - Retry Verification Policy job `93438049879` — passed.
  - All 18 required PR checks are green on commit `330e7d8e1`.
- **Learnings for future iterations:**
  - When a broad functional lane fails in an untouched timing-sensitive test, extract the exact failing test and message from the job log before changing feature code; a clean targeted retry is the appropriate recovery when the failure is nondeterministic and unrelated.
  - A failed dependent policy job should be evaluated only after the failed lane is rerun and terminal; the policy check can pass on the same workflow attempt once the selected lane succeeds.
---
## 2026-08-10 04:49 -0700 - wo-b3-provider-capability-discovery-cli-final-review-audit
- What was implemented
  - Re-read `prd.json` and the consolidated Codebase Patterns; all three provider discovery stories remain marked `passes:true`.
  - Inspected PR #1843 with the connector and the bundled thread-aware GraphQL workflow. The historical AGY/Codex and Claude/readiness blockers each have a subsequent author response documenting the corresponding fixes; no inline review threads or formal reviews remain.
  - Confirmed the existing requested PR is already current, so no duplicate PR or feature-code change is needed.
- Files changed
  - `progress.txt`
  - `prd.json` was verified unchanged because all story pass states and the PR description are current.
- Verification
  - Thread-aware review inspection — four top-level conversation comments, zero reviews, zero inline review threads; the latest blocker is addressed by commit `330e7d8e1`.
  - Local `HEAD`, the fork branch, and PR #1843 head all match `3319b2109373de62d791178568cfa3b297c507d5`.
  - PR #1843 is open, non-draft, mergeable, clean, and its description exactly matches `prd.json`.
  - `gh pr checks 1843 --repo portpowered/you-agent-factory` — all 18 reported checks pass.
- **Learnings for future iterations:**
  - A historical blocking conversation item is resolved when the current PR contains the requested behavioral fix and the thread-aware audit finds no unresolved inline work; preserve the original comment as review history.
  - Progress-only commits still change the PR head, so rerun the focused regression and verify terminal CI after pushing the bookkeeping update.
---
## 2026-08-10 05:05 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Diagnosed the first workflow attempt on the progress-only head as two unrelated flaky lanes: the UI component suite hit an out-of-memory failure in `FactorySimpleSubmissionComposer`, and backend functional coverage hit the previously observed canceled-context assertion in `TestFactorySessionResumeDoesNotRepeatCompletedDispatch`; no provider-discovery or UI files were changed.
  - Reran only the failed jobs after the workflow became terminal.
- Files changed
  - `progress.txt`
- Verification
  - Initial run `31385342784` failed only `Frontend Component` and `Backend Functional Coverage`; all other substantive checks passed.
  - Retry `Frontend Component` job `93446283510` — passed.
  - Retry `Backend Functional Coverage` job `93446283682` — passed.
  - Dependent `Verification Policy` job `93447534784` — passed.
  - All 18 checks are green on head `23cb30230ef31fda2ebf9fdec1f6c211e6913ca2`.
- **Learnings for future iterations:**
  - A progress-only commit can expose unrelated resource and timing flakiness; inspect the exact failing test and compare changed paths before editing feature code, then rerun only failed jobs when the isolated evidence supports it.
  - Record both the primary failure and any missing downstream artifact so CI diagnosis does not mistake the upload symptom for the test cause.

## 2026-08-10 04:33 -07:00 - sac-s0-1-ownership-inventories-001
- What was implemented
  - Regenerated `docs/internal/packaged-service-structure/package-target-manifest.json` from the live production tree, increasing the exact inventory from 528 to 545 packages and preserving every existing package mapping.
  - Added the 17 newly discovered platform, Chat Sessions, Events, Factory Sessions, Providers, Worker Sessions, ACP, CLI, and mapping packages with reviewed retain destinations.
  - Added Worker Sessions to the package-target checker’s closed product-owner vocabulary, reconciled the live Chat Sessions/Events top-level inventories, and added a direct Worker Sessions mapping assertion.
- Files changed
  - `cmd/packagetargetmanifestcheck/manifest.go`
  - `cmd/packagetargetmanifestcheck/manifest_test.go`
  - `cmd/packagetargetmanifestcheck/owner_top_level.go`
  - `cmd/packagetargetmanifestcheck/owners_test.go`
  - `cmd/packagetargetmanifestcheck/residuals.go`
  - `cmd/packagetargetmanifestcheck/residuals_test.go`
  - `docs/internal/packaged-service-structure/package-target-manifest.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 package rows.
  - Focused package-target inventory, vocabulary, owner mapping, top-level, and coverage tests — passed.
  - `git diff --check` — passed.
  - `make ownership-inventory-check` — remains red on the next story’s stale ownership baseline: the 16 new package rows and 10 live cross-owner edges are not yet in `ownership-inventory.json`; no ownership classification changes were made in this story.
- **Learnings for future iterations:**
  - The package-target manifest and ownership inventory are separate ledgers: package-target regeneration can be green while ownership verification still reports missing package rows and edges until story 002 refreshes its reviewed classifications.
  - Worker Sessions is an implemented durable service owner, so its package family must be retained under `worker_sessions`; treating it as an unknown residual would incorrectly queue live code for deletion.
---

## 2026-08-10 04:52 -07:00 - sac-s0-1-ownership-inventories-002
- What was implemented
  - Regenerated `docs/internal/baselines/ownership-inventory.json` from the live package graph, adding the 16 paths introduced by the refreshed package-target manifest and retaining the existing test-package inventory scope.
  - Added Worker Sessions to the closed product-owner vocabulary, current-root inventory, package mapping, top-level child inventory, and present-tense owner rationale.
  - Rewrote Chat Sessions and Events rationale cards to describe their implemented state, lifecycle, consumers, transaction boundaries, and failure behavior rather than future work.
  - Classified the newly discovered cross-owner imports and regenerated the edge table at 155 edges. Reciprocal owner dependencies are now explicitly marked `bidirectional` and `unresolved` so valid interaction classes cannot hide convergence debt.
  - Added validation and regression coverage that rejects an otherwise valid class when reciprocal-edge debt markers are removed.
- Files changed
  - `docs/internal/baselines/ownership-inventory.json`
  - `internal/ownershipinventory/destinations.go`
  - `internal/ownershipinventory/edges.go`
  - `internal/ownershipinventory/edges_test.go`
  - `internal/ownershipinventory/freeze_test.go`
  - `internal/ownershipinventory/gate.go`
  - `internal/ownershipinventory/load.go`
  - `internal/ownershipinventory/map.go`
  - `internal/ownershipinventory/named_owners.go`
  - `internal/ownershipinventory/owner_top_level.go`
  - `internal/ownershipinventory/rationale.go`
  - `internal/ownershipinventory/rationale_service_owners.go`
  - `internal/ownershipinventory/types.go`
  - `internal/ownershipinventory/validate.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed with 576 package rows and 155 classified cross-owner edges.
  - Focused `go test ./internal/ownershipinventory` selection covering regeneration, Worker Sessions mapping, owner-top-level coverage, reciprocal-edge discovery, and debt-marker rejection — passed.
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - `make package-target-manifest-check` — passed with 545 package-target rows.
  - `git diff --check` — passed.
  - The full `internal/ownershipinventory` package still reports the pre-existing Workers/Recordings root-contract baseline drift (`kind_phase_validation.go`, `lifecycle_capability.go`, and related inventories); those files are outside this story's ownership-inventory changes.
- **Learnings for future iterations:**
  - Keep the package-target and ownership ledgers distinct: package-target coverage is the 545 non-test production-package set, while the ownership freeze currently retains its established 576-directory scope that includes test-only Go packages.
  - Reciprocal owner imports need a separate unresolved-debt marker in addition to a valid interaction class; classifying both directions alone can make convergence debt look resolved.
  - Adding a product owner requires synchronized destination vocabulary, package mapping, top-level child inventory, rationale generation, and regenerated JSON before the ownership gate can pass.
---

## 2026-08-10 05:16 -07:00 - sac-s0-1-ownership-inventories-003
- What was implemented
  - Corrected package-boundary owner metadata for Providers execution/adapters and removed stale migration wording from the provider-effect diagnostics and related boundary comments.
  - Kept the existing `pkg/services/workers.Provider` public shape as an exact request-scoped compatibility bridge over Providers, while adding a guard regression test that rejects any new provider-effect owner under a Workers subpackage.
  - Expanded Providers and Workers ownership rationale to distinguish provider protocol, selection, session identity, adapter choice, and one native attempt from Workers request-scoped scheduling/retry and runner execution.
  - Confirmed hosted polling/reconciliation ownership remains Automation Hosted Sources and hosted runner execution remains Workers through the existing responsibility-based guard tests.
  - Burned down all eight corrected misplaced-guard inventory entries, made retired entries invalid, regenerated `docs/internal/baselines/ownership-inventory.json`, and marked story 003 complete in `prd.json`.
- Files changed
  - `cmd/functionalboundarycheck/main.go`
  - `cmd/ownershipboundarycheck/main.go`
  - `cmd/pkgboundarycheck/main.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership_test.go`
  - `docs/internal/baselines/ownership-inventory.json`
  - `internal/ownershipinventory/misplaced_surfaces.go`
  - `internal/ownershipinventory/misplaced_surfaces_test.go`
  - `internal/ownershipinventory/rationale_service_owners.go`
  - `internal/ownershipinventory/types.go`
  - `internal/ownershipinventory/validate.go`
  - `pkg/services/workers/provider_port.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed.
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 package rows.
  - Focused Providers/Workers provider-effect tests, hosted ownership tests, misplaced-guard tests, and `cmd/functionalboundarycheck` tests — passed.
  - `go test ./pkg/services/workers -run '^$' -count=1` — passed compile-only typecheck.
  - `git diff --check` — passed.
  - Full `go test ./cmd/pkgboundarycheck ./cmd/ownershipboundarycheck ./internal/ownershipinventory` still reports the pre-existing Workers/Recordings root-contract baseline drift; `make ownership-boundary-check` still reports two unrelated existing violations. `make pkg-boundary` still fails its existing baseline, but no provider-effect finding remains for the reviewed Workers compatibility port.
- **Learnings for future iterations:**
  - Keep compatibility exceptions exact: the Workers root `Provider` bridge is allowed by package and type name, while new nested Workers provider-effect declarations remain prohibited.
  - Hosted ownership is responsibility-based rather than folder-based; transitional `workers/services/hosted_logic` locations do not override Automation Hosted Sources or Workers Hosted Runner claims.
- The next iteration is story 004, which should reconcile the three canonical service-owner documentation tables without reopening the completed enforcement work.
---

## 2026-08-10 05:25 -07:00 - sac-s0-1-ownership-inventories-004
- What was implemented
  - Reconciled the ownership guidance in `AGENTS.md`, `docs/architecture/architecture.md`, and `docs/architecture/packaged-structure.md` with the implemented service graph.
  - Added present-tense ownership for Chat Sessions, Events, and Worker Sessions, including the distinction between process-local source-native observations and the canonical Factory Event ledger in Recordings.
  - Assigned provider inference and provider execution policy to Providers, hosted-source observation/polling to Automations, and request-scoped worker scheduling/execution to Workers.
  - Documented the ACP transport, boundary mapping, and platform ownership boundaries, and removed internal Petri terminology from customer-facing service-owner descriptions.
- Files changed
  - `AGENTS.md`
  - `docs/architecture/architecture.md`
  - `docs/architecture/packaged-structure.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed.
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 package rows.
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - `git diff --check` — passed.
  - The full `internal/ownershipinventory` package retains the pre-existing Workers/Recordings root-contract baseline drift documented by stories 002–003; this docs-only story does not touch those files.
- **Learnings for future iterations:**
  - The three canonical ownership summaries have different shapes—AGENTS uses package-owner guidance, architecture uses responsibility rows, and packaged-structure separates services from platform/transport families—so reconciliation should align semantics without forcing one document into another’s format.
  - Events should always be described as process-local source-native delivery, while Recordings remains the canonical Factory history owner; ACP and boundary mapping adapt these contracts without owning domain state.
  - Provider inference/execution policy, hosted polling, and request-scoped worker execution must remain named separately in documentation so future ownership changes do not collapse them back into Workers.
---

## 2026-08-10 04:44 -07:00 - pf-plain-batch-drain-001
- What was implemented
  - Made Factory Runtime termination-drain coordination re-evaluate immediately when a result, submission, or dispatch-result hook wake is consumed at the termination boundary.
  - Added a focused engine regression proving a dispatch hook wake causes a second canonical termination check instead of returning to a blocked outer wait.
  - Added root-built public plain-batch coverage for stranded PROCESSING Work, exact typed incomplete-drain propagation and stderr, empty and terminal finite success, and continuous idle liveness.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/runtime_state.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine_termination_test.go`
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
  - `prd.json`
- Verification
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/... ./pkg/services/factory_sessions/internal/processlifecycle ./pkg/services/factory_sessions/internal/runtimehosting ./pkg/transports/cli/run ./tests/functional/workers/mock -run 'TestRun(ReevaluatesTerminationAfterDispatchHookWake|ReturnsIncompleteDrainErrorAfterTerminationClassification)$|TestPlainBatchDrain' -count=1 -timeout 180s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The Runtime termination checker already owns the finite incomplete classification and typed error; the lifecycle bug was that `finishTerminationDrain` reported a consumed wake without its caller looping back into `tickOnce`.
  - Dispatch-result hook wake channels are part of the engine’s pending-input contract and must be drained alongside result and submission signals at the final termination boundary.
---

## 2026-08-10 04:57 -07:00 - pf-plain-batch-drain-001 verification follow-up
- What was implemented
  - Kept the termination-drain result helper within the repository’s 1000-line backend file limit without changing its semantics.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/runtime_state.go`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_runtime/... -count=1 -timeout 300s` — passed.
  - `go test ./pkg/services/factory_sessions/... -count=1 -timeout 360s` — passed.
  - `go test ./tests/functional/sessions/execution -run 'TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal|TestHostedFiniteRunsKeepEmptyAndTerminalSuccess|TestHostedContinuousRunsStayLiveWhileIdle' -count=1 -timeout 240s` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — blocked before Go/UI tests by the checkout’s pre-existing dashboard baseline: missing `@types/bun` and TypeScript 6 `baseUrl` deprecation errors.
- **Learnings for future iterations:**
  - The backend quality gates report deletion-only baseline entries but pass newly changed files; a small private helper signature reduction is enough to preserve the strict engine file-size boundary.
  - `make test` remains the reliable repository-wide short Go evidence when the fast aggregate stops early at an unavailable UI dependency.
---

## 2026-08-10 05:08 -07:00 - pf-plain-batch-drain-001 CI boundary follow-up
- What was implemented
  - Adjusted the plain customer-boundary assertion to avoid a forbidden direct Runtime service import while retaining exact stderr and process failure coverage.
- Files changed
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run 'TestPlainBatchDrain' -count=1 -timeout 120s` — passed.
  - `go run ./cmd/functionalboundarycheck` — passed.
  - `git diff --check` — passed.
  - PR #1845's initial functional lane identified the same forbidden Runtime import; commit `085c00db6` removes it and is ready for a fresh canonical-repository CI run.
- **Learnings for future iterations:**
  - The functional boundary lane is intentionally stricter than package tests: public harness tests verify observable process behavior, while `errors.As` checks for service-owned types belong in the owning service package.
---

## 2026-08-10 05:09 -07:00 - pf-plain-batch-drain-001 PR handoff follow-up
- What was implemented
  - Re-submitted the current branch as review PR #1846 after the migrated PR #1845 retained checks for its obsolete pre-boundary-fix commit.
- Files changed
  - `progress.txt`
- Verification
  - PR #1846 is open, non-draft, points at commit `f3f6db0b5`, has no comments or reviews, and its body matches `prd.json` after newline normalization.
  - Local focused tests, the functional boundary checker, and the previously recorded repository quality gates remain passing; the checkout-local `make verify-fast` UI baseline remains blocked by missing `@types/bun` and TypeScript 6 `baseUrl` deprecation errors.
  - GitHub currently reports no fresh Actions run for the current head after the repository-migration PR recreation; this remains PR follow-up rather than a claimed green CI result.
- **Learnings for future iterations:**
  - When a repository migration leaves a PR’s checks attached to an obsolete head, recreating the review PR can restore a clean handoff, but verify that the new pull-request event actually creates checks before declaring review readiness.
## 2026-08-10 04:38 -07:00 - sac-s0-2-pkg-boundary-ignores-002
- What was implemented
  - Added a paired normal checker regression using the same prohibited `pkg/wire` import as the ignored-root fixture under production `pkg` paths containing nested `.artifacts`, `.claude/worktrees`, and `worktrees` directories.
  - Added a `make pkg-boundary` entry-path regression proving the nested `.artifacts` production package is still reported with the application-composition diagnostic and non-zero result.
  - Marked story 002 complete in `prd.json`.
- Files changed
  - `cmd/pkgboundarycheck/main_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run 'TestRunRejectsProductionPkgViolationInsideArtifactLikeSubtrees|TestMakePkgBoundaryTargetRejectsProductionPkgImportInsideArtifactLikeSubtree' -count=1 -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checker has many independent `WalkDir` callbacks; updating only the primary repository scan leaves secondary scanners able to rediscover ignored files.
  - A malformed ignored fixture is useful behavioral evidence for prune-before-parse: a valid stale import alone may not exercise every parser path.
  - The next story should add the paired production `pkg` failure fixture and verify the `make pkg-boundary` subprocess path while keeping nested artifact-like directory names visible.
 ---
## 2026-08-10 04:32 -07:00 - sac-s0-2-pkg-boundary-ignores-001
- What was implemented
  - Centralized repository-root-relative pruning for `.artifacts`, `.artifacts/bootstrap/worktrees`, generated transient worktrees, and the supported root worktree locations.
  - Applied the same skip decision before parsing in every `pkg-boundary` production traversal, including secondary service, transport, provider, edge, test-behavior, Petri, and production-default scans.
  - Preserved the existing Git metadata, dependency/vendor, node-module, and testdata exclusions without matching artifact-like or worktree-like names inside tracked production packages.
  - Expanded the normal `run` regression to use malformed stale `pkg/wire` imports beneath artifact/bootstrap/generated transient layouts and existing ignored roots, proving those files are pruned before parsing.
- Files changed
  - `cmd/pkgboundarycheck/main.go`
  - `cmd/pkgboundarycheck/main_test.go`
  - `cmd/pkgboundarycheck/constructed_service_edges.go`
  - `cmd/pkgboundarycheck/functional_process_edges.go`
  - `cmd/pkgboundarycheck/initializer_behavior.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/production_defaults.go`
  - `cmd/pkgboundarycheck/support_service_imports.go`
  - `cmd/pkgboundarycheck/transport_behavior.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run '^TestRunIgnoresRepositoryPolicyNonSourceRoots$' -count=1` — passed.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `make pkg-boundary` — reaches the checker and reports only the unchanged 176-violation baseline on current `main`; no new violation was introduced by this story.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checker has many independent `WalkDir` callbacks; updating only the primary repository scan leaves secondary scanners able to rediscover ignored files.
  - A malformed ignored fixture is useful behavioral evidence for prune-before-parse: a valid stale import alone may not exercise every parser path.
  - The next story should add the paired production `pkg` failure fixture and verify the `make pkg-boundary` subprocess path while keeping nested artifact-like directory names visible.
## 2026-08-10 04:33 -07:00 - sac-s0-1-ownership-inventories-001
- What was implemented
  - Regenerated `docs/internal/packaged-service-structure/package-target-manifest.json` from the live production tree, increasing the exact inventory from 528 to 545 packages and preserving every existing package mapping.
  - Added the 17 newly discovered platform, Chat Sessions, Events, Factory Sessions, Providers, Worker Sessions, ACP, CLI, and mapping packages with reviewed retain destinations.
  - Added Worker Sessions to the package-target checker’s closed product-owner vocabulary, reconciled the live Chat Sessions/Events top-level inventories, and added a direct Worker Sessions mapping assertion.
- Files changed
  - `cmd/packagetargetmanifestcheck/manifest.go`
  - `cmd/packagetargetmanifestcheck/manifest_test.go`
  - `cmd/packagetargetmanifestcheck/owner_top_level.go`
  - `cmd/packagetargetmanifestcheck/owners_test.go`
  - `cmd/packagetargetmanifestcheck/residuals.go`
  - `cmd/packagetargetmanifestcheck/residuals_test.go`
  - `docs/internal/packaged-service-structure/package-target-manifest.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 package rows.
  - Focused package-target inventory, vocabulary, owner mapping, top-level, and coverage tests — passed.
  - `git diff --check` — passed.
  - `make ownership-inventory-check` — remains red on the next story’s stale ownership baseline: the 16 new package rows and 10 live cross-owner edges are not yet in `ownership-inventory.json`; no ownership classification changes were made in this story.
- **Learnings for future iterations:**
  - The package-target manifest and ownership inventory are separate ledgers: package-target regeneration can be green while ownership verification still reports missing package rows and edges until story 002 refreshes its reviewed classifications.
  - Worker Sessions is an implemented durable service owner, so its package family must be retained under `worker_sessions`; treating it as an unknown residual would incorrectly queue live code for deletion.
---
## 2026-08-10 04:38 -07:00 - sac-s0-2-pkg-boundary-ignores-002
- What was implemented
  - Added a paired normal checker regression using the same prohibited `pkg/wire` import as the ignored-root fixture under production `pkg` paths containing nested `.artifacts`, `.claude/worktrees`, and `worktrees` directories.
  - Added a `make pkg-boundary` entry-path regression proving the nested `.artifacts` production package is still reported with the application-composition diagnostic and non-zero result.
  - Marked story 002 complete in `prd.json` on the S0.2 branch.
- Files changed
  - `cmd/pkgboundarycheck/main_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run 'TestRunRejectsProductionPkgViolationInsideArtifactLikeSubtrees|TestMakePkgBoundaryTargetRejectsProductionPkgImportInsideArtifactLikeSubtree' -count=1 -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Root-relative ignore policies need paired positive fixtures under production `pkg`; testing ignored paths alone cannot prove that nested artifact/worktree-like directories remain visible.
- Existing `make pkg-boundary` subprocess tests provide a stable way to verify the public checker entry path against temporary repository fixtures.
---
## 2026-08-10 04:42 -07:00 - btrc-p0-characterization-001
- What was implemented
  - Published docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md as the authoritative P0 ownership map.
  - Reconciled the ten grouped dependency structs to 44 D rows, ExternalEffects to 15 E rows, deferred construction/callback roles to 38 F rows, and late completion/attachment/binding operations to 13 L rows.
  - Classified every row exactly once, assigned one destination owner or explicit deletion, documented lifecycle/cleanup responsibility, and linked the downstream retirement packet.
  - Marked story btrc-p0-characterization-001 as passed in prd.json; no production code or inventory-checker test was added.
- Files changed
  - docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md
  - prd.json
  - progress.txt
- Verification
  - git diff --check — passed.
  - go test ./pkg/services/factory_sessions/internal/runtimeopening ./pkg/services/factory_sessions/wire ./pkg/services/factory_sessions/internal/applicationopening -count=1 — passed.
  - Row totals and source sections were reviewed directly against factory.go, factories.go, open.go, and the adjacent Wire/application…6350 tokens truncated…ed.
  - `make pkg-file-count` — passed.
  - `make pkg-structure` — passed.
  - `make vet` — passed.
  - `make typecheck` — passed.
  - Hosted characterization repeated twice — passed.
  - The previously failing `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput` repeated three times locally — passed.
- **Learnings for future iterations:**
  - The prior GitHub functional-coverage failure is reproducible as a transient baseline failure rather than a deterministic source failure; fresh CI after this head is the authoritative check.
---
## 2026-08-10 04:42 -07:00 - btrc-p0-characterization-001
- What was implemented
  - Published docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md as the authoritative P0 ownership map.
  - Reconciled the ten grouped dependency structs to 44 D rows, ExternalEffects to 15 E rows, deferred construction/callback roles to 38 F rows, and late completion/attachment/binding operations to 13 L rows.
  - Classified every row exactly once, assigned one destination owner or explicit deletion, documented lifecycle/cleanup responsibility, and linked the downstream retirement packet.
  - Marked story btrc-p0-characterization-001 as passed in prd.json; no production code or inventory-checker test was added.
- Files changed
  - docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md
  - prd.json
  - progress.txt
- Verification
  - git diff --check — passed.
  - go test ./pkg/services/factory_sessions/internal/runtimeopening ./pkg/services/factory_sessions/wire ./pkg/services/factory_sessions/internal/applicationopening -count=1 — passed.
  - Row totals and source sections were reviewed directly against factory.go, factories.go, open.go, and the adjacent Wire/application binding surfaces.
- **Learnings for future iterations:**
  - The canonical grouped dependency count is ten Dependencies structs totaling 44 fields; the flattened private opener fields are projections and should not be counted again.
  - ExternalEffects is the narrow invocation-local projection selected in pkg/wire/runtime_inputs.go; future characterization rows should distinguish these operation values from process-scoped construction inputs.
  - Workers.LocalRuntimeHooksFactory is composed and validated but not invoked by the current opener, so it is an explicit deletion row rather than a destination-owned capability.
---
## 2026-08-10 04:44 -07:00 - pf-plain-batch-drain-001
- What was implemented
  - Made Factory Runtime termination-drain coordination re-evaluate immediately when a result, submission, or dispatch-result hook wake is consumed at the termination boundary.
  - Added a focused engine regression proving a dispatch hook wake causes a second canonical termination check instead of returning to a blocked outer wait.
  - Added root-built public plain-batch coverage for stranded PROCESSING Work, exact typed incomplete-drain propagation and stderr, empty and terminal finite success, and continuous idle liveness.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/runtime_state.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine_termination_test.go`
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
  - `prd.json`
- Verification
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/... ./pkg/services/factory_sessions/internal/processlifecycle ./pkg/services/factory_sessions/internal/runtimehosting ./pkg/transports/cli/run ./tests/functional/workers/mock -run 'TestRun(ReevaluatesTerminationAfterDispatchHookWake|ReturnsIncompleteDrainErrorAfterTerminationClassification)$|TestPlainBatchDrain' -count=1 -timeout 180s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The Runtime termination checker already owns the finite incomplete classification and typed error; the lifecycle bug was that `finishTerminationDrain` reported a consumed wake without its caller looping back into `tickOnce`.
  - Dispatch-result hook wake channels are part of the engine’s pending-input contract and must be drained alongside result and submission signals at the final termination boundary.
---
## 2026-08-10 04:52 -07:00 - sac-s0-1-ownership-inventories-002
- What was implemented
  - Regenerated `docs/internal/baselines/ownership-inventory.json` from the live package graph, adding the 16 paths introduced by the refreshed package-target manifest and retaining the existing test-package inventory scope.
  - Added Worker Sessions to the closed product-owner vocabulary, current-root inventory, package mapping, top-level child inventory, and present-tense owner rationale.
  - Rewrote Chat Sessions and Events rationale cards to describe their implemented state, lifecycle, consumers, transaction boundaries, and failure behavior rather than future work.
  - Classified the newly discovered cross-owner imports and regenerated the edge table at 155 edges. Reciprocal owner dependencies are now explicitly marked `bidirectional` and `unresolved` so valid interaction classes cannot hide convergence debt.
  - Added validation and regression coverage that rejects an otherwise valid class when reciprocal-edge debt markers are removed.
- Files changed
  - `docs/internal/baselines/ownership-inventory.json`
  - `internal/ownershipinventory/destinations.go`
  - `internal/ownershipinventory/edges.go`
  - `internal/ownershipinventory/edges_test.go`
  - `internal/ownershipinventory/freeze_test.go`
  - `internal/ownershipinventory/gate.go`
  - `internal/ownershipinventory/load.go`
  - `internal/ownershipinventory/map.go`
  - `internal/ownershipinventory/named_owners.go`
  - `internal/ownershipinventory/owner_top_level.go`
  - `internal/ownershipinventory/rationale.go`
  - `internal/ownershipinventory/rationale_service_owners.go`
  - `internal/ownershipinventory/types.go`
  - `internal/ownershipinventory/validate.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed with 576 package rows and 155 classified cross-owner edges.
  - Focused `go test ./internal/ownershipinventory` selection covering regeneration, Worker Sessions mapping, owner-top-level coverage, reciprocal-edge discovery, and debt-marker rejection — passed.
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - `make package-target-manifest-check` — passed with 545 package-target rows.
  - `git diff --check` — passed.
  - The full `internal/ownershipinventory` package still reports the pre-existing Workers/Recordings root-contract baseline drift (`kind_phase_validation.go`, `lifecycle_capability.go`, and related inventories); those files are outside this story's ownership-inventory changes.
- **Learnings for future iterations:**
  - Keep the package-target and ownership ledgers distinct: package-target coverage is the 545 non-test production-package set, while the ownership freeze currently retains its established 576-directory scope that includes test-only Go packages.
  - Reciprocal owner imports need a separate unresolved-debt marker in addition to a valid interaction class; classifying both directions alone can make convergence debt look resolved.
  - Adding a product owner requires synchronized destination vocabulary, package mapping, top-level child inventory, rationale generation, and regenerated JSON before the ownership gate can pass.
---
## 2026-08-10 04:57 -07:00 - pf-plain-batch-drain-001 verification follow-up
- What was implemented
  - Kept the termination-drain result helper within the repository’s 1000-line backend file limit without changing its semantics.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/engine.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/engine/runtime_state.go`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_runtime/... -count=1 -timeout 300s` — passed.
  - `go test ./pkg/services/factory_sessions/... -count=1 -timeout 360s` — passed.
  - `go test ./tests/functional/sessions/execution -run 'TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal|TestHostedFiniteRunsKeepEmptyAndTerminalSuccess|TestHostedContinuousRunsStayLiveWhileIdle' -count=1 -timeout 240s` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — blocked before Go/UI tests by the checkout’s pre-existing dashboard baseline: missing `@types/bun` and TypeScript 6 `baseUrl` deprecation errors.
- **Learnings for future iterations:**
  - The backend quality gates report deletion-only baseline entries but pass newly changed files; a small private helper signature reduction is enough to preserve the strict engine file-size boundary.
  - `make test` remains the reliable repository-wide short Go evidence when the fast aggregate stops early at an unavailable UI dependency.
---
## 2026-08-10 05:01 -07:00 - pf-cli-exit-codes-002
- What was implemented
  - Kept the existing runtime startup-failure transition and `SERVER_BIND_FAILED` typed mapping, while preventing failed hosted runs from printing the success-looking `Recording saved:` message.
  - Added a direct run regression proving a startup/runtime failure does not report recording success while successful shutdown output remains covered.
  - Added a built-executable regression that occupies `127.0.0.1:65535`, exhausts auto-port selection, and verifies the OS process exits non-zero with one `SERVER_BIND_FAILED` JSON diagnostic on stderr and no readiness or success output on stdout.
- Files changed
  - `pkg/transports/cli/run/run.go`
  - `pkg/transports/cli/run/run_startup_test.go`
  - `tests/functional/transport/server_binding/server_binding_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli/run -run 'TestRun_ShutdownOutputReportsResolvedAutoGeneratedRecordingPath|TestRun_StartupFailureDoesNotReportRecordingSuccess' -count=1 -v` — passed.
  - `go test ./tests/functional/transport/server_binding -run '^TestBuiltExecutableServerBindFailureExitsNonZeroWithoutReadinessOutput$' -count=1 -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A server bind error can be correctly typed and non-zero while still leaking a success-looking recording-path line; terminal success output must be gated on a successful hosted run.
  - Port 65535 provides a deterministic built-process bind failure because the default auto-port scan has no higher candidate; skip only when the test environment cannot reserve that terminal port.
  - Process.Execute proves returned errors and streams, but an `exec.Command` test is still required to prove the final OS exit status.
---
## 2026-08-10 05:05 -0700 - wo-b3-provider-capability-discovery-cli-ci-retry
- What was implemented
  - Diagnosed the first workflow attempt on the progress-only head as two unrelated flaky lanes: the UI component suite hit an out-of-memory failure in `FactorySimpleSubmissionComposer`, and backend functional coverage hit the previously observed canceled-context assertion in `TestFactorySessionResumeDoesNotRepeatCompletedDispatch`; no provider-discovery or UI files were changed.
  - Reran only the failed jobs after the workflow became terminal.
- Files changed
  - `progress.txt`
- Verification
  - Initial run `31385342784` failed only `Frontend Component` and `Backend Functional Coverage`; all other substantive checks passed.
  - Retry `Frontend Component` job `93446283510` — passed.
  - Retry `Backend Functional Coverage` job `93446283682` — passed.
  - Dependent `Verification Policy` job `93447534784` — passed.
  - All 18 checks are green on head `23cb30230ef31fda2ebf9fdec1f6c211e6913ca2`.
- **Learnings for future iterations:**
  - A progress-only commit can expose unrelated resource and timing flakiness; inspect the exact failing test and compare changed paths before editing feature code, then rerun only failed jobs when the isolated evidence supports it.
  - Record both the primary failure and any missing downstream artifact so CI diagnosis does not mistake the upload symptom for the test cause.
## 2026-08-10 05:08 -07:00 - pf-plain-batch-drain-001 CI boundary follow-up
- What was implemented
  - Adjusted the plain customer-boundary assertion to avoid a forbidden direct Runtime service import while retaining exact stderr and process failure coverage.
- Files changed
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run 'TestPlainBatchDrain' -count=1 -timeout 120s` — passed.
  - `go run ./cmd/functionalboundarycheck` — passed.
  - `git diff --check` — passed.
  - PR #1845's initial functional lane identified the same forbidden Runtime import; commit `085c00db6` removes it and is ready for a fresh canonical-repository CI run.
- **Learnings for future iterations:**
  - The functional boundary lane is intentionally stricter than package tests: public harness tests verify observable process behavior, while `errors.As` checks for service-owned types belong in the owning service package.
---
## 2026-08-10 05:09 -07:00 - pf-plain-batch-drain-001 PR handoff follow-up
- What was implemented
  - Re-submitted the current branch as review PR #1846 after the migrated PR #1845 retained checks for its obsolete pre-boundary-fix commit.
- Files changed
  - `progress.txt`
- Verification
  - PR #1846 is open, non-draft, points at commit `f3f6db0b5`, has no comments or reviews, and its body matches `prd.json` after newline normalization.
  - Local focused tests, the functional boundary checker, and the previously recorded repository quality gates remain passing; the checkout-local `make verify-fast` UI baseline remains blocked by missing `@types/bun` and TypeScript 6 `baseUrl` deprecation errors.
  - GitHub currently reports no fresh Actions run for the current head after the repository-migration PR recreation; this remains PR follow-up rather than a claimed green CI result.
- **Learnings for future iterations:**
  - When a repository migration leaves a PR’s checks attached to an obsolete head, recreating the review PR can restore a clean handoff, but verify that the new pull-request event actually creates checks before declaring review readiness.
## 2026-08-10 05:13 -07:00 - btrc-p0-characterization-002
- What was implemented
  - Added deterministic root-composition characterization for successful and partially failed two-work parent/child batches.
  - Froze the literal 20-event canonical sequence from RUN_REQUEST through WORK_REQUEST, relationship admission, serialized child dispatches, model/dispatch responses, RUN_RESPONSE, and the two session terminal events.
  - Asse…6749 tokens truncated…--check` — passed.
- **Learnings for future iterations:**
  - Keep the next server and stream stories on the same process-boundary marker/context seam so startup and stream-specific typed codes can be added without duplicating the command graph or changing service ownership.
  - The current checkout's docs-topic baseline is stale relative to the live packaged-factory docs (`packaged-factories` is added and the providers description differs); it is unrelated to this CLI story and should be reconciled separately.
---
## 2026-08-10 05:15 -0700 - wo-b3-provider-capability-discovery-cli-final-merge-audit
- What was implemented
  - Reconciled the provider-discovery branch with current `main` at `353b0527a`; the merge conflict was limited to `prd.json` and `progress.txt`.
  - Preserved the B3 PRD and all three `passes:true` stories, combined both append-only progress histories, and retained the current-main `pkg-boundary` production-source changes.
- Files changed
  - Current-main `cmd/pkgboundarycheck/` implementation and focused tests were merged.
  - `progress.txt`
  - `prd.json` was verified as the B3 version after conflict resolution.
- Verification
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - PR #1843 is conflict-free and points to merge head `f80b38a8d29eace327d53895bc84c4ad4e3f6487` with base `353b0527a26163711043671f966466be30f478c4`.
  - Fresh CI run `31386632092` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - When a moving base changes an unrelated task PRD, resolve the bookkeeping conflict by keeping the branch’s task PRD and merging append-only progress entries; retain the base’s source changes and rerun both affected-package and feature-slice tests.
---
## 2026-08-10 05:16 -07:00 - sac-s0-1-ownership-inventories-003
- What was implemented
  - Corrected package-boundary owner metadata for Providers execution/adapters and removed stale migration wording from the provider-effect diagnostics and related boundary comments.
  - Kept the existing `pkg/services/workers.Provider` public shape as an exact request-scoped compatibility bridge over Providers, while adding a guard regression test that rejects any new provider-effect owner under a Workers subpackage.
  - Expanded Providers and Workers ownership rationale to distinguish provider protocol, selection, session identity, adapter choice, and one native attempt from Workers request-scoped scheduling/retry and runner execution.
  - Confirmed hosted polling/reconciliation ownership remains Automation Hosted Sources and hosted runner execution remains Workers through the existing responsibility-based guard tests.
  - Burned down all eight corrected misplaced-guard inventory entries, made retired entries invalid, regenerated `docs/internal/baselines/ownership-inventory.json`, and marked story 003 complete in `prd.json`.
- Files changed
  - `cmd/functionalboundarycheck/main.go`
  - `cmd/ownershipboundarycheck/main.go`
  - `cmd/pkgboundarycheck/main.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership_test.go`
  - `docs/internal/baselines/ownership-inventory.json`
  - `internal/ownershipinventory/misplaced_surfaces.go`
  - `internal/ownershipinventory/misplaced_surfaces_test.go`
  - `internal/ownershipinventory/rationale_service_owners.go`
  - `internal/ownershipinventory/types.go`
  - `internal/ownershipinventory/validate.go`
  - `pkg/services/workers/provider_port.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed.
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 package rows.
  - Focused Providers/Workers provider-effect tests, hosted ownership tests, misplaced-guard tests, and `cmd/functionalboundarycheck` tests — passed.
  - `go test ./pkg/services/workers -run '^$' -count=1` — passed compile-only typecheck.
  - `git diff --check` — passed.
  - Full `go test ./cmd/pkgboundarycheck ./cmd/ownershipboundarycheck ./internal/ownershipinventory` still reports the pre-existing Workers/Recordings root-contract baseline drift; `make ownership-boundary-check` still reports two unrelated existing violations. `make pkg-boundary` still fails its existing baseline, but no provider-effect finding remains for the reviewed Workers compatibility port.
- **Learnings for future iterations:**
  - Keep compatibility exceptions exact: the Workers root `Provider` bridge is allowed by package and type name, while new nested Workers provider-effect declarations remain prohibited.
  - Hosted ownership is responsibility-based rather than folder-based; transitional `workers/services/hosted_logic` locations do not override Automation Hosted Sources or Workers Hosted Runner claims.
- The next iteration is story 004, which should reconcile the three canonical service-owner documentation tables without reopening the completed enforcement work.
---
## 2026-08-10 05:25 -07:00 - sac-s0-1-ownership-inventories-004
- What was implemented
  - Reconciled the ownership guidance in `AGENTS.md`, `docs/architecture/architecture.md`, and `docs/architecture/packaged-structure.md` with the implemented service graph.
  - Added present-tense ownership for Chat Sessions, Events, and Worker Sessions, including the distinction between process-local source-native observations and the canonical Factory Event ledger in Recordings.
  - Assigned provider inference and provider execution policy to Providers, hosted-source observation/polling to Automations, and request-scoped worker scheduling/execution to Workers.
  - Documented the ACP transport, boundary mapping, and platform ownership boundaries, and removed internal Petri terminology from customer-facing service-owner descriptions.
- Files changed
  - `AGENTS.md`
  - `docs/architecture/architecture.md`
  - `docs/architecture/packaged-structure.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed.
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 package rows.
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - `git diff --check` — passed.
  - The full `internal/ownershipinventory` package retains the pre-existing Workers/Recordings root-contract baseline drift documented by stories 002–003; this docs-only story does not touch those files.
- **Learnings for future iterations:**
  - The three canonical ownership summaries have different shapes—AGENTS uses package-owner guidance, architecture uses responsibility rows, and packaged-structure separates services from platform/transport families—so reconciliation should align semantics without forcing one document into another’s format.
  - Events should always be described as process-local source-native delivery, while Recordings remains the canonical Factory history owner; ACP and boundary mapping adapt these contracts without owning domain state.
  - Provider inference/execution policy, hosted polling, and request-scoped worker execution must remain named separately in documentation so future ownership changes do not collapse them back into Workers.
---
## 2026-08-10 05:25 -07:00 - pf-plain-batch-drain-001 synchronization follow-up
- What was implemented
  - Synchronized the branch with current `main` after PR #1844 advanced the base, retaining the plain-batch implementation and resolving the shared PRD/progress bookkeeping files without dropping either history.
  - Committed the conflict-free merge as `8bd1fb6eb`.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main `cmd/pkgboundarycheck` changes were merged as part of the base synchronization.
- Verification
  - Focused plain-batch/runtime/session tests passed.
  - Full `pkg/services/factory_runtime/...` and `pkg/services/factory_sessions/...` tests passed.
  - `go test ./cmd/pkgboundarycheck -count=1` and `go run ./cmd/functionalboundarycheck` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make test` passed.
  - After `bun install --frozen-lockfile` restored the checkout's missing UI dependencies, `make verify-fast` passed dashboard typecheck, MCP contract checks, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
- **Learnings for future iterations:**
  - The missing `@types/bun` dependency was an environment-only checkout issue; installing from the pinned lockfile restored the full fast verification tier without tracked dependency changes.
  - A current-main merge can conflict in shared task bookkeeping even when product files merge cleanly; keep the active branch PRD authoritative and preserve both chronological progress histories.
---
## 2026-08-10 05:29 -07:00 - pf-plain-batch-drain-001 PR description follow-up
- What was implemented
  - Restored the omitted word `plain` in the active PRD acceptance criterion so `prd.json` exactly matches the existing PR #1846 description.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses successfully.
  - PR #1846 body matches `prd.json` after newline normalization.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - After resolving a shared JSON conflict manually, compare the normalized PR body against the file content rather than relying only on JSON validity; small wording drift can otherwise go unnoticed.
---
## 2026-08-10 05:47 -0700 - wo-b3-provider-capability-discovery-cli-003
- What was implemented
  - Addressed the current blocking PR feedback by splitting the 88-line `assertJSONFacts` functional helper into focused JSON decoding, common inventory, Codex, Antigravity, and Claude behavioral assertions.
  - Preserved the existing runtime assertions for deterministic provider ordering, truthful unprobed readiness/prerequisite state, exact model IDs, unsupported modalities, typed limits, and zero provider command-runner calls.
- Files changed
  - `tests/functional/providers/discovery/discovery_test.go`
  - `progress.txt`
  - `prd.json` was verified unchanged because all stories remain `passes:true`; the review-only maintainability follow-up does not alter acceptance state.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1 -timeout 120s` — passed.
  - `go test ./tests/functional/providers/discovery/... -count=1 -timeout 120s` — passed.
  - `go vet ./tests/functional/providers/discovery/...` — passed.
  - `git diff --check` — passed; every helper is below the 80-line review limit.
- **Learnings for future iterations:**
  - Functional assertions that cover several provider-specific facts should be split by behavior area so maintainability limits do not force weaker runtime coverage.
  - A top-level review blocker can be resolved with a focused test-only change when the requested behavior is already correct; preserve the existing observable assertions while reducing helper scope.
---
## 2026-08-10 05:54 -07:00 - pf-plain-batch-drain-001 review follow-up
- What was implemented
  - Addressed PR #1846's blocking test-construction feedback by naming the continuous idle observation and documenting why the bounded negative-liveness guard is required at the public `Process.Execute` boundary.
- Files changed
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run 'TestPlainBatchDrain' -count=3 -timeout 180s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A functional test that proves a continuous run stays open must explain why no deterministic idle event or injected edge observation exists; name the bounded observation and keep it scoped to the negative-liveness guard.
## 2026-08-10 06:11 -07:00 - sac-s0-1-ownership-inventories review follow-up
- What was implemented
  - Constrained the Workers-root `Provider` package-boundary exception to the exact reviewed request-scoped compatibility contract: one `Infer(context.Context, ProviderInferenceRequest) (InferenceResponse, error)` method.
  - Added a focused regression proving that adding a provider catalog method to that root type is rejected, and updated the allow fixture to use the production contract shape rather than a generic string signature.
  - Reshaped story `sac-s0-1-ownership-inventories-004` from a behavioral user story into an explicit `project-enabling` PRD criterion because canonical architecture guidance changes no runtime, public API, CLI, UI, or emitted-event behavior.
- Files changed
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run 'ProviderEffect|WorkersRequestScoped|WorkersProvider' -count=1 -v` — passed.
  - `prd.json` parses successfully with PowerShell JSON validation.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A compatibility exception should be narrow enough to reject additions to the reviewed contract, not merely recognize its historical type name.
  - Documentation-only alignment belongs in explicit enabling/project criteria when it has no observable product behavior; keep behavioral user stories reserved for runtime, API, CLI, UI, or emitted-event outcomes.
---
## 2026-08-10 06:24 -07:00 - pf-plain-batch-drain-001 current-main review follow-up
- What was implemented
  - Merged current `origin/main` into `pf-plain-batch-drain` and resolved the shared bookkeeping conflict by keeping the plain-batch `prd.json` authoritative while preserving both append-only progress histories.
  - Retained the plain-batch Runtime wake re-evaluation, typed incomplete-drain propagation, and public functional regression alongside current-main provider-discovery source and generated artifacts.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main provider-discovery source/generated files were retained by the merge.
- Verification
  - Focused Runtime and Factory Session packages passed.
  - `go test ./tests/functional/workers/mock -run '^TestPlainBatchDrain' -count=3 -timeout 240s` — passed.
  - `go run ./cmd/functionalboundarycheck` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make lint` passed UI lint/dead-code, vet, and backend gates, then reached the existing 176-violation `pkg-boundary` baseline; the baseline matches current main and no new task violation was introduced.
  - `git diff --check` — passed; PRD parses with the single story still marked `passes:true`.
- **Learnings for future iterations:**
  - When current main changes the task bookkeeping files, preserve the active task PRD exactly for PR-description parity and merge each side's progress entries rather than replacing the append-only log.
- A clean local merge head can still report the repository's known `pkg-boundary` migration baseline; distinguish that baseline from changed-file regressions using the dedicated checker and functional-boundary gate.
---
## 2026-08-10 07:06 -07:00 - pf-cli-exit-codes-003
- What was implemented
  - Kept the existing EOF-after-open stream classification and added a typed `WORKER_SESSION_STREAM_FAILED` wrapper for output-write failures, preserving the underlying cause without exposing it in the safe diagnostic message.
  - Added root.BuildProcess/Process.Execute coverage for an early-closing Worker Sessions SSE response. The complete `RECORD` frame remains unchanged on stdout, no terminal or replay-success frame is synthesized, and stderr contains exactly one `WORKER_SESSION_STREAM_CLOSED` diagnostic.
  - Added a focused built-executable regression proving the same stream abort exits non-zero at the OS process boundary.
  - Updated the existing Worker Sessions required-input functional assertion to decode the central-policy diagnostic from stderr, matching the already-established story-001 output contract.
- Files changed
  - `pkg/services/worker_sessions/transports/cli/stream.go`
  - `pkg/services/worker_sessions/transports/cli/stream_test.go`
  - `tests/functional/provider_sessions/cli/stream_abort_test.go`
  - `tests/functional/provider_sessions/cli/worker_sessions_cli_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/worker_sessions/transports/cli -run 'TestStream(ReturnsClosedBeforeTerminalAsJSONError|ReturnsMalformedFrameAsTypedFailure|ReturnsOutputWriteFailureAsTypedStreamFailure|JSONUsesSessionScopedSSEAndStopsAtTerminal|ReplayOnlyWritesTerminalReplayBeforeCompleteSummary|PropagatesContextCancellation|MapsPostOpenContextCancellationToInterruptedAndClosesBody)' -count=1 -v` — passed.
  - `go test ./tests/functional/provider_sessions/cli -count=1 -timeout 300s` — passed.
  - `go vet ./pkg/services/worker_sessions/transports/cli ./pkg/transports/cli/... ./pkg/root ./cmd/factory` — passed.
  - `make pkg-maint`, `make pkg-file-count`, and `git diff --check` — passed.
  - `make typecheck` — blocked by the pre-existing checkout dependency baseline: the pinned `bun install --frozen-lockfile` completed, but `@types/bun` remains unavailable; no UI files changed.
  - The broader CLI package lane still reports the pre-existing `TestDocsTopicIndexBaseline_MatchesFixture` drift documented by earlier iterations; it is unrelated to this story.
- **Learnings for future iterations:**
  - A stream can emit valid partial frames before the transport closes, so the error must be returned after preserving those frames and the process boundary must own the single stderr diagnostic.
  - `Process.Execute` proves typed propagation and stdout/stderr ownership; only a built `you` subprocess proves the final non-zero OS exit status.
  - Keep stream output-write errors typed with a safe message while retaining the underlying cause through `Unwrap` for diagnostics and tests.
---
## 2026-08-10 07:09 -0700 - sac-s0-1-ownership-inventories current-main conflict follow-up
- What was implemented
  - Merged the current `origin/main` head `ccea14bf8` into the ownership-inventories branch after resolving the shared `prd.json` and `progress.txt` conflicts.
  - Kept the SAC S0.1 `prd.json` authoritative with all four story/enabling items marked `passes:true`, and preserved both branches' append-only progress history and reusable patterns.
  - Retained the current-main runtime and plain-batch regression changes in the merge commit `73beee2e6` without widening the SAC S0.1 implementation scope.
- Files changed
  - `prd.json` was retained as the authoritative task PRD during conflict resolution.
  - `progress.txt`
  - Current-main `pkg/services/factory_runtime/internal/services/orchestration/engine/` changes and `tests/functional/workers/mock/plain_batch_drain_test.go` were retained by the merge.
- Verification
  - `make package-target-manifest-check` — passed with 545 inventory rows and 545 production package rows.
  - `make ownership-inventory-check` — passed with the ownership inventory and path-lease freeze verified.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `prd.json` parses successfully and every story/enabling criterion remains `passes:true`.
  - `go test ./internal/ownershipinventory -count=1` still reports the documented pre-existing Workers/Recordings root-contract baseline drift; no SAC inventory gate reports a new failure.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Current-main synchronization can bring unrelated product files into the merge; preserve them as part of the base update while keeping the active task PRD and append-only progress history scoped and authoritative.
- The public inventory gates are the relevant evidence for this story; the broader ownership package's root-contract fixture baseline remains a separate known debt and should not be silently folded into an inventory story.
---
## 2026-08-10 07:20 -07:00 - pf-cli-exit-codes-004
- What was implemented
  - Expanded the process-boundary cancellation matrix so wrapped cancellations from `you run`, `you server`, and `you worker-sessions stream` retain the manifest-declared exit 130, while an ordinary failure with a canceled parent context and undeclared command cancellation remain exit 1.
  - Added built-executable run/server interrupt regressions and upgraded the finite success and worker-failure checks to observe the shipped binary's exit status; the existing server-bind and Worker Sessions abort checks complete the 0/non-zero/130 matrix.
  - Added a built Worker Sessions SSE cancellation regression that preserves the complete frame received before interruption and verifies exit 130 without synthesizing a terminal frame.
  - Updated process fixtures and assertions to match the already-established story-001 safe `CLI_COMMAND_FAILED` diagnostic contract for unclassified Cobra, initializer, and Work-watch failures.
- Files changed
  - `cmd/factory/main_test.go`
  - `tests/functional/provider_sessions/cli/stream_abort_test.go`
  - `tests/functional/transport/cli/process/exit_codes_test.go`
  - `tests/functional/transport/cli/process/fixtures_test.go`
  - `tests/functional/transport/cli/process/unknown_command_test.go`
  - `tests/functional/transport/cli/process/watch_abort_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/factory -run '^TestProcessExitCodePreservesDeclaredLifecycleContract$' -count=1 -v` — passed.
  - `go test ./tests/functional/transport/cli/process -count=1 -timeout 300s` — passed, including built finite success, worker failure, server cancellation (Unix), and ordinary failure coverage.
  - `go test ./tests/functional/provider_sessions/cli -count=1 -timeout 300s` — passed; Worker Sessions cancellation test is skipped on Windows and runs on Unix.
  - `go test ./tests/functional/transport/server_binding -count=1 -timeout 300s` — passed.
  - `go test ./cmd/factory ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli/... -count=1` — changed packages passed; the command still reports the pre-existing `TestDocsTopicIndexBaseline_MatchesFixture` drift (`packaged-factories` and the current providers description) in `pkg/transports/cli/baseline`.
  - `go vet ./cmd/factory ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli/...`, `make pkg-maint pkg-file-count`, and `git diff --check` — passed.
  - Dashboard typecheck remains blocked by the pre-existing checkout dependency baseline (`@types/bun`/deleted `ui/packages/*` sources); no UI files changed.
- **Learnings for future iterations:**
  - `root.BuildProcess`/`Process.Execute` returns errors but cannot expose the final OS status; use a built `./cmd/factory` subprocess only for the status assertions and keep ordinary functional behavior on the root process.
  - The process boundary intentionally replaces unclassified Cobra and stream errors with one safe `CLI_COMMAND_FAILED` diagnostic; functional fixtures must assert the coded safe payload rather than parse underlying error text.
  - Manifest-derived cancellation selection must ignore positional prompt text and flag values, and must reject exit 130 for commands without a declared `cancel` exit.
## 2026-08-10 07:26 -0700 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the first post-merge `Backend Functional Coverage` failure and confirmed it was an unrelated current-main sidecar-start timing failure in `TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal/site` (`listener starts = 0, want 1`) followed by a secondary missing-artifact upload error.
  - Made no SAC source changes; reran only the failed functional coverage and dependent verification jobs after the focused test passed locally five consecutive times.
- Files changed
  - `progress.txt`
- Verification
  - Focused `go test ./tests/functional/sessions/execution -run '^TestWithServerDrainCannotReportSuccessWhileWorkIsNonTerminal$' -count=5 -timeout 300s` — passed.
  - GitHub Actions run `31396678693`, attempt 2 — `Backend Functional Coverage` passed and `Verification Policy` passed; all 18 checks on `c469def708a68331604fc7ea9646ca7e4b64de0d` are green.
  - The PR remains conflict-free and the authoritative description is synchronized from `prd.json`.
- **Learnings for future iterations:**
  - When a broad functional lane fails in a current-main scenario, inspect the first test assertion before treating artifact-upload errors as the root cause; a repeated focused pass can justify a failed-job-only rerun without changing unrelated product code.
---
## 2026-08-10 07:28 -07:00 - pf-cli-exit-codes current-main synchronization follow-up
- What was implemented
  - Merged the current `origin/main` into `pf-cli-exit-codes` and resolved the shared `prd.json`/`progress.txt` conflict by retaining the active CLI-exit-codes PRD and combining both append-only progress histories and reusable patterns.
  - Preserved current-main provider-discovery source, generated contract artifacts, and baseline updates in the merge commit `072f7d37c`.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main source/generated files were retained by the synchronization merge.
- Verification
  - `go test ./tests/functional/transport/cli/process -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/provider_sessions/cli -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/transport/server_binding -count=1 -timeout 300s` — passed.
  - `go vet ./cmd/factory ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli/...`, `make pkg-maint pkg-file-count`, and `git diff --check` — passed.
  - The broad CLI package sweep reaches the current-main `TestWalk_ProductionInventoryMatchesCommittedBaseline` command-identity fixture drift (52,445 generated bytes vs 51,822 committed); this is unrelated to the CLI exit-code changes.
- **Learnings for future iterations:**
- A moving main branch can change shared PR bookkeeping and generated CLI inventory in the same synchronization; retain both progress histories, keep the task PRD authoritative for PR-body parity, and distinguish current-main baseline drift from feature regressions.
---
## 2026-08-10 07:44 -0700 - sac-s0-1-ownership-inventories final handoff audit
- What was implemented
  - Rechecked the completed SAC S0.1 inventory work and confirmed no remaining story or enabling criterion is incomplete; `prd.json` remains authoritative with all four items marked `passes:true`.
  - Audited PR #1848 through the thread-aware GitHub workflow: the PR is open, non-draft, conflict-free, its description matches `prd.json`, and there are no review submissions or unresolved/outdated inline threads.
- Files changed
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `git diff --check` — passed.
  - PR #1848 head `4ea34b0350ba87c7d05c5eaba71a897c69575aa9` matches the pushed branch; all 18 reported checks are terminal and successful.
- **Learnings for future iterations:**
  - For an already-completed story, use the authoritative PRD, focused inventory gates, pushed-head comparison, PR-body comparison, and thread-aware review audit as the final handoff evidence; do not reopen unrelated baseline debt.
---
## 2026-08-10 07:49 -0700 - pf-cli-exit-codes CI diagnostic assertion follow-up
- What was implemented
  - Updated the remaining functional fixtures that exercise the built or root CLI process boundary to assert the safe `CLI_COMMAND_FAILED` ErrorResponse rather than hidden initializer, catalog, model, session, submit, work, or drain cause text.
  - Added `support.RequireSafeCLIDiagnostic` as the shared observable assertion for the process-boundary fallback and preserved stdout-isolation, non-zero failure, and cancellation-diagnostic checks.
  - Kept the model-provider ACP functional failure unchanged after an isolated rerun passed; it was a parallel CI flake rather than a reproducible exit-code regression.
- Files changed
  - Functional acceptance, bootstrap, catalog, models, session, smoke, CLI command, Work watch, and mock-worker tests.
  - `tests/functional/internal/support/process.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/acceptance ./tests/functional/bootstrap_portability ./tests/functional/factory/packaged/catalog ./tests/functional/models/model_list ./tests/functional/sessions/execution ./tests/functional/sessions/lifecycle ./tests/functional/smoke ./tests/functional/transport/cli/commands ./tests/functional/work/watch ./tests/functional/workers/mock -count=1 -timeout 600s` — passed after the follow-up assertions were added.
  - `go test ./tests/functional/acceptance ./tests/functional/transport/cli/commands ./tests/functional/workers/mock -count=1 -timeout 600s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestFactoryRunRetriesACPProviderByResumingExactSession$' -count=1 -timeout 120s -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Functional tests at the executable boundary must assert the sanitized serialized diagnostic; the returned error may still retain its cause for in-process `errors.Is` checks, but process stderr intentionally does not expose it.
- Cancellation diagnostics are an intentional observable exception to the JSON fallback: a directly canceled root command can emit exactly `Error: context canceled` while preserving the cancellation error for exit-status selection.
---
## 2026-08-10 08:02 -0700 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the fresh-head Backend Functional Coverage failure and identified the unchanged timing-sensitive JavaScript durability test `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault`; its assertion was `provider Infer did not observe canceled workflow context`.
  - Confirmed this matches the repository's documented current-main flake pattern, made no unrelated runtime changes, and reran only the failed GitHub jobs after the focused test passed 20 consecutive local repetitions.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/orchestration/javascript/durability -run '^TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault$' -count=20 -timeout 300s` — passed.
  - GitHub Actions run `31399893972`, attempt 2 — all 18 checks passed, including Backend Functional Coverage and Verification Policy.
  - PR #1848 remains conflict-free with body matching `prd.json`; local and pushed head are both `5e04c54d4761d8d04eae8813ee9ebb7bbffb5210`.
  - Thread-aware review audit reports zero review submissions and zero unresolved or outdated inline threads.
- **Learnings for future iterations:**
  - A canceled-context assertion in the JavaScript durability suite is a known timing-sensitive CI failure; run the exact test repeatedly and rerun only failed jobs before changing unrelated runtime code.
---
## 2026-08-10 08:17 -07:00 - sac-s0-1-ownership-inventories current-main freshness follow-up
- What was implemented
  - Merged the latest `origin/main` head `11b34c073` after it advanced beyond the prior handoff, with no merge conflicts.
  - Preserved the SAC task PRD and inventory implementation while retaining the current-main batch-submit diagnostic changes; all four PRD story/enabling items remain `passes:true`.
- Files changed
  - `progress.txt`
  - Current-main batch-submit changes retained in `docs/reference/`, `pkg/services/factory_runtime/`, `pkg/services/work/`, `pkg/transports/cli/submit/`, and `tests/functional/`.
- Verification
  - `git merge --no-edit origin/main` — passed without conflicts.
  - Focused inventory gates and post-merge PR/CI verification are pending in this iteration.
- **Learnings for future iterations:**
  - A clean PR can still have a stale cached base SHA after another PR merges; fetch the remote default branch and use its actual commit for final freshness checks.
---
## 2026-08-10 08:18 -0700 - pf-cli-exit-codes coverage and stream-test follow-up
- What was implemented
  - Added focused unit coverage for `clidiag` nil/default edges, context markers, invocation-coded errors, safe fallback normalization, writer behavior, and presentation helpers; the package now exceeds its tracked unit coverage floor.
  - Added focused coverage for `SubmissionHTTPError` safe CLI fields so the submit package remains above its tracked unit coverage floor after the diagnostic changes.
  - Made the built Worker Sessions abort fixture flush its complete SSE frame before closing and increased only its test timeout bound to avoid coverage-lane scheduling flakiness.
- Files changed
  - `pkg/transports/cli/clidiag/diagnostics_test.go`
  - `pkg/transports/cli/submit/submit_transport_test.go`
  - `tests/functional/provider_sessions/cli/stream_abort_test.go`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli/clidiag -count=1 -cover` — passed at 96.1%.
  - `go test ./pkg/transports/cli/submit -count=1 -cover` — passed at 88.0%.
  - `go test ./tests/functional/provider_sessions/cli -run '^TestBuiltWorkerSessionsStreamAbortExitsNonZero$' -count=5 -timeout 360s -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a coverage lane runs built-process SSE tests in parallel with broad instrumentation, flush the fixture frame explicitly before returning so the client observes the intended complete-frame-then-EOF sequence.
  - Coverage-floor regressions from new boundary policy code are best repaired with unit tests for nil/default and marker branches, not by lowering the existing manifest minimum.
  - A browser integration timeout in an unchanged UI surface should be rerun and recorded as an environmental/current-main flake before changing frontend code.
---
## 2026-08-10 08:32 -0700 - pf-cli-exit-codes functional coverage repair
- What was implemented
  - Routed terminal logger construction through the policy's existing structured-terminal decision so the resolved debug behavior is exercised by the functional CLI path without changing the resulting logger mode.
  - Added a root.BuildProcess functional assertion for a JSON unary `you submit` payload, complementing the existing Markdown payload case and restoring executable coverage for both payload classifications.
- Files changed
  - `pkg/transports/cli/terminalpolicy/policy.go`
  - `tests/functional/transport/submit/submit_test.go`
  - `progress.txt`
- Verification
  - `go test -short ./tests/functional/transport/submit -run '^TestSubmitFamilyExecutesThroughRootBuiltProcess$' -count=1 -timeout 180s -v` — passed.
  - `go test ./pkg/transports/cli/terminalpolicy -count=1 -cover` — passed at 92.3%.
  - `git diff --check` — passed.
  - Committed as `690721f8d`.
- **Learnings for future iterations:**
  - Functional coverage floors include policy helper branches, so keep exported policy decisions on the production path that consumes them and pair file-ingress helpers with observable JSON and Markdown CLI cases.
---
## 2026-08-10 08:33 -07:00 - sac-s0-1-ownership-inventories final post-merge verification
- What was implemented
  - Completed the final verification pass after merging current `origin/main` `11b34c073`; no SAC runtime or contract changes were needed.
  - Confirmed the authoritative `prd.json` still has all four story/enabling items marked `passes:true` and the PR body remains byte-for-byte identical to it.
- Files changed
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - GitHub Actions run `31402996074` — all 18 checks passed, including Verification Policy.
  - PR #1848 is open, non-draft, conflict-free, and mergeable; the thread-aware audit reports zero unresolved or outdated review threads and no formal review findings.
- **Learnings for future iterations:**
  - The final delivery audit must include the post-merge head's terminal policy check, PR body parity, and thread-aware review state in addition to focused local gates.
---
## 2026-08-10 08:47 -0700 - pf-cli-exit-codes functional payload fallback coverage
- What was implemented
  - Reused the existing live unary submit functional scenario with a `.txt` payload fixture, covering the customer-visible generic file classification while retaining the `.md` and `.json` cases.
- Files changed
  - `tests/functional/transport/submit/submit_test.go`
  - `progress.txt`
- Verification
  - `go test -short ./tests/functional/transport/submit -run '^TestSubmitFamily' -count=1 -timeout 240s -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Payload classification coverage is most stable when the same live submit contract exercises Markdown, JSON, and generic file extensions through the public request path.
---
## 2026-08-10 08:59 -07:00 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the failed final-head Backend Functional Coverage attempt and confirmed that the failures were timing-sensitive cancellation observations in unchanged functional tests; no SAC source change was warranted.
  - Reran the exact failing tests locally (10 repetitions for the initial two failures and 20 repetitions for the retry failure), then reran the failed GitHub job until the backend lane and dependent Verification Policy passed.
- Files changed
  - `progress.txt`
- Verification
  - Local repeated JavaScript durability, work-recovery, and MCP resume tests passed.
  - GitHub Actions run `31404264251`, attempt 3 — all 18 checks passed, including Backend Functional Coverage and Verification Policy, on head `c0c4aafd8`.
  - The coverage warning and missing evidence artifact occurred only after the first failing test assertions; the successful retry produced the required artifacts.
- **Learnings for future iterations:**
  - In the functional coverage lane, inspect the first `--- FAIL` assertion before treating downstream coverage warnings or missing-artifact uploads as root causes; cancellation-observation failures can move between related tests across retries.
 - A clean local merge head can still report the repository's known `pkg-boundary` migration baseline; distinguish that baseline from changed-file regressions using the dedicated checker and functional-boundary gate.
---
## 2026-08-10 09:04 -0700 - pf-cli-exit-codes CI closeout
- What was implemented
  - Completed the required PR review/CI follow-up after the functional coverage repair; the first rerun encountered two unrelated functional flakes, and the failed coverage job was rerun cleanly on the same head.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31405306468` passed all checks, including Backend Functional Coverage, Backend Unit Coverage, UI Backend Integration, and Verification Policy.
  - Final functional coverage artifact reports `clidiag` 61/76 statements (80.3%) and `terminalpolicy` 22/26 statements (84.6%), both meeting their manifest floors.
  - PR #1856 has no conversation comments, reviews, or review threads; all four PRD stories remain `passes:true`.
- **Learnings for future iterations:**
- When a broad functional coverage job fails before producing a coverage profile because of unrelated scenario flakes, rerun only the failed job before changing feature code; the clean rerun can validate the intended coverage delta directly.
  - Root-relative ignore policies need paired positive fixtures under production `pkg`; testing ignored paths alone cannot prove that nested artifact/worktree-like directories remain visible.
  - Existing `make pkg-boundary` subprocess tests provide a stable way to verify the public checker entry path against temporary repository fixtures.
---
## 2026-08-10 05:15 -0700 - wo-b3-provider-capability-discovery-cli-final-merge-audit
- What was implemented
  - Reconciled the provider-discovery branch with current `main` at `353b0527a`; the merge conflict was limited to `prd.json` and `progress.txt`.
  - Preserved the B3 PRD and all three `passes:true` stories, combined both append-only progress histories, and retained the current-main `pkg-boundary` production-source changes.
- Files changed
  - Current-main `cmd/pkgboundarycheck/` implementation and focused tests were merged.
  - `progress.txt`
  - `prd.json` was verified as the B3 version after conflict resolution.
- Verification
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - PR #1843 is conflict-free and points to merge head `f80b38a8d29eace327d53895bc84c4ad4e3f6487` with base `353b0527a26163711043671f966466be30f478c4`.
  - Fresh CI run `31386632092` — all 18 checks pass, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - When a moving base changes an unrelated task PRD, resolve the bookkeeping conflict by keeping the branch’s task PRD and merging append-only progress entries; retain the base’s source changes and rerun both affected-package and feature-slice tests.
---
## 2026-08-10 05:25 -07:00 - pf-plain-batch-drain-001 synchronization follow-up
- What was implemented
  - Synchronized the branch with current `main` after PR #1844 advanced the base, retaining the plain-batch implementation and resolving the shared PRD/progress bookkeeping files without dropping either history.
  - Committed the conflict-free merge as `8bd1fb6eb`.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main `cmd/pkgboundarycheck` changes were merged as part of the base synchronization.
- Verification
  - Focused plain-batch/runtime/session tests passed.
  - Full `pkg/services/factory_runtime/...` and `pkg/services/factory_sessions/...` tests passed.
  - `go test ./cmd/pkgboundarycheck -count=1` and `go run ./cmd/functionalboundarycheck` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make test` passed.
  - After `bun install --frozen-lockfile` restored the checkout's missing UI dependencies, `make verify-fast` passed dashboard typecheck, MCP contract checks, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
- **Learnings for future iterations:**
  - The missing `@types/bun` dependency was an environment-only checkout issue; installing from the pinned lockfile restored the full fast verification tier without tracked dependency changes.
  - A current-main merge can conflict in shared task bookkeeping even when product files merge cleanly; keep the active branch PRD authoritative and preserve both chronological progress histories.
---
## 2026-08-10 05:29 -07:00 - pf-plain-batch-drain-001 PR description follow-up
- What was implemented
  - Restored the omitted word `plain` in the active PRD acceptance criterion so `prd.json` exactly matches the existing PR #1846 description.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses successfully.
  - PR #1846 body matches `prd.json` after newline normalization.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - After resolving a shared JSON conflict manually, compare the normalized PR body against the file content rather than relying only on JSON validity; small wording drift can otherwise go unnoticed.
---
## 2026-08-10 05:54 -07:00 - pf-plain-batch-drain-001 review follow-up
- What was implemented
  - Addressed PR #1846's blocking test-construction feedback by naming the continuous idle observation and documenting why the bounded negative-liveness guard is required at the public `Process.Execute` boundary.
- Files changed
  - `tests/functional/workers/mock/plain_batch_drain_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run 'TestPlainBatchDrain' -count=3 -timeout 180s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A functional test that proves a continuous run stays open must explain why no deterministic idle event or injected edge observation exists; name the bounded observation and keep it scoped to the negative-liveness guard.
## 2026-08-10 05:47 -0700 - wo-b3-provider-capability-discovery-cli-003
- What was implemented
  - Addressed the current blocking PR feedback by splitting the 88-line `assertJSONFacts` functional helper into focused JSON decoding, common inventory, Codex, Antigravity, and Claude behavioral assertions.
  - Preserved the existing runtime assertions for deterministic provider ordering, truthful unprobed readiness/prerequisite state, exact model IDs, unsupported modalities, typed limits, and zero provider command-runner calls.
- Files changed
  - `tests/functional/providers/discovery/discovery_test.go`
  - `progress.txt`
  - `prd.json` was verified unchanged because all stories remain `passes:true`; the review-only maintainability follow-up does not alter acceptance state.
- Verification
  - `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1 -timeout 120s` — passed.
  - `go test ./tests/functional/providers/discovery/... -count=1 -timeout 120s` — passed.
  - `go vet ./tests/functional/providers/discovery/...` — passed.
  - `git diff --check` — passed; every helper is below the 80-line review limit.
- **Learnings for future iterations:**
  - Functional assertions that cover several provider-specific facts should be split by behavior area so maintainability limits do not force weaker runtime coverage.
  - A top-level review blocker can be resolved with a focused test-only change when the requested behavior is already correct; preserve the existing observable assertions while reducing helper scope.
---
## 2026-08-10 06:24 -07:00 - pf-plain-batch-drain-001 current-main review follow-up
- What was implemented
  - Merged current `origin/main` into `pf-plain-batch-drain` and resolved the shared bookkeeping conflict by keeping the plain-batch `prd.json` authoritative while preserving both append-only progress histories.
  - Retained the plain-batch Runtime wake re-evaluation, typed incomplete-drain propagation, and public functional regression alongside current-main provider-discovery source and generated artifacts.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main provider-discovery source/generated files were retained by the merge.
- Verification
  - Focused Runtime and Factory Session packages passed.
  - `go test ./tests/functional/workers/mock -run '^TestPlainBatchDrain' -count=3 -timeout 240s` — passed.
  - `go run ./cmd/functionalboundarycheck` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make lint` passed UI lint/dead-code, vet, and backend gates, then reached the existing 176-violation `pkg-boundary` baseline; the baseline matches current main and no new task violation was introduced.
  - `git diff --check` — passed; PRD parses with the single story still marked `passes:true`.
- **Learnings for future iterations:**
  - When current main changes the task bookkeeping files, preserve the active task PRD exactly for PR-description parity and merge each side's progress entries rather than replacing the append-only log.
 - A clean local merge head can still report the repository's known `pkg-boundary` migration baseline; distinguish that baseline from changed-file regressions using the dedicated checker and functional-boundary gate.
---
## 2026-08-10 04:42 -07:00 - btrc-p0-characterization-001
- What was implemented
  - Published docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md as the authoritative P0 ownership map.
  - Reconciled the ten grouped dependency structs to 44 D rows, ExternalEffects to 15 E rows, deferred construction/callback roles to 38 F rows, and late completion/attachment/binding operations to 13 L rows.
  - Classified every row exactly once, assigned one destination owner or explicit deletion, documented lifecycle/cleanup responsibility, and linked the downstream retirement packet.
  - Marked story btrc-p0-characterization-001 as passed in prd.json; no production code or inventory-checker test was added.
- Files changed
  - docs/internal/packaged-service-structure/runtime-opening-ownership-matrix.md
  - prd.json
  - progress.txt
- Verification
  - git diff --check — passed.
  - go test ./pkg/services/factory_sessions/internal/runtimeopening ./pkg/services/factory_sessions/wire ./pkg/services/factory_sessions/internal/applicationopening -count=1 — passed.
  - Row totals and source sections were reviewed directly against factory.go, factories.go, open.go, and the adjacent Wire/application binding surfaces.
- **Learnings for future iterations:**
  - The canonical grouped dependency count is ten Dependencies structs totaling 44 fields; the flattened private opener fields are projections and should not be counted again.
  - ExternalEffects is the narrow invocation-local projection selected in pkg/wire/runtime_inputs.go; future characterization rows should distinguish these operation values from process-scoped construction inputs.
  - Workers.LocalRuntimeHooksFactory is composed and validated but not invoked by the current opener, so it is an explicit deletion row rather than a destination-owned capability.
---
## 2026-08-10 05:13 -07:00 - btrc-p0-characterization-002
- What was implemented
  - Added deterministic root-composition characterization for successful and partially failed two-work parent/child batches.
  - Froze the literal 20-event canonical sequence from RUN_REQUEST through WORK_REQUEST, relationship admission, serialized child dispatches, model/dispatch responses, RUN_RESPONSE, and the two session terminal events.
  - Asserted request/work counts, parent-child relation identity, default session identity, one-to-one dispatch/Worker Session correlation, terminal work states, typed `permanent_bad_request` failure metadata, and the current `SUCCEEDED`/`FINAL` aggregate result for both all-success and partial-failure batches.
  - Marked story btrc-p0-characterization-002 as passed in prd.json; no production behavior changed.
- Files changed
  - tests/functional/factory_runtime/root_composition/batch_characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/factory_runtime/root_composition -run '^TestBTRCP0Batch(Success|PartialFailure)Characterization$' -count=5 -timeout 240s` — passed.
  - `git diff --check` — passed.
  - The full `./tests/functional/factory_runtime/root_composition` package remains blocked by the unchanged neighboring W4 test `TestFactoryRuntimeMixedDirectAndFactoryChildDispatchesStayIsolatedThroughCLI`, which reports 18 `unexpected provider prompt` calls even when run alone; the failure is outside this story's changed files.
- **Learnings for future iterations:**
  - A capacity-one authored resource is the current deterministic boundary for serializing a multi-work batch without sleeps; it produces stable dispatch request/association/model/response ordering while retaining multiple child works.
  - A partially failed work can still yield a `SUCCEEDED`/`FINAL` Factory Session when every work token reaches a terminal state; the typed per-dispatch failure remains in `DISPATCH_RESPONSE`.
  - Canonical batch work identity is split across the WORK_REQUEST/relationship event context and the dispatch response `outputWork`; assert both instead of relying on a projection derived from the test itself.
---
## 2026-08-10 05:33 -07:00 - btrc-p0-characterization-003
- What was implemented
  - Added deterministic hosted/service characterization in tests/functional/automations/hosted_characterization_test.go without changing production code or public/generated contracts.
  - Root-composed service tests replace the hosted clock, Linear HTTP client, secret resolver, and provider command runner through edges.Edges; successful hosted ingress asserts the literal ten-event startup-to-dispatch sequence, ACCEPTED dispatch payload, terminal `complete` Work, and IDLE service progress.
  - Execution failure asserts the same literal event sequence, typed dispatch failure detail/error, terminal `failed` Work, one provider attempt, and no non-terminal progress.
  - Blocked hosted HTTP asserts the startup-only four-event prefix, no submission or dispatch, observed request-context cancellation during service stop, and an isolated independent second session that completes only its own Work.
  - Marked story btrc-p0-characterization-003 as passed in prd.json.
- Files changed
  - tests/functional/automations/hosted_characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/automations -run 'TestBTRCP0HostedService' -count=1 -v -timeout 120s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Hosted service mode remains live with runtime status IDLE after a terminal Work; its caller-visible terminal result is the dispatch response plus session-scoped Work/progress projection rather than a durable session-result endpoint.
  - A poller workstation without a worker executor fails after admission; use a separate normal workstation for the hosted target state when characterizing successful hosted ingress.
  - A blocking HTTP edge makes source cleanup observable through request-context cancellation, while a separate process with a distinct hosted Work ID provides a direct isolation assertion.
---
## 2026-08-10 05:54 -07:00 - btrc-p0-characterization-004
- What was implemented
  - Added `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go` using the checked-in `inference-events.replay.json` recording as the expected source of replay order.
  - The success characterization asserts the literal seven-event Factory Event order and IDs, sequence `0..6`, tick placement, request/work correlation, dispatch replay key, recorded provider output, submission timing, completion delivery tick, and `ACCEPTED` terminal result.
  - The failed-recording characterization mutates only the recorded terminal payload, then asserts the same event prefix, `FAILED`/`permanent_bad_request` result, typed `workers.ProviderError`, and typed `dispatch_mismatch` divergence for an incompatible observed dispatch.
  - Marked story `btrc-p0-characterization-004` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/recordings/internal/replay -run '^TestBTRCP0ReplayCharacterization_' -count=1 -v` — passed.
  - `go test ./pkg/root -run 'TestProcessExecute(RejectsInvalidPortableReplay|ReturnsPortableReplayInspection)' -count=1 -v` — passed; malformed replay and valid portable inspection each made zero provider/script/session/host calls.
  - `go test ./pkg/services/recordings/internal/replay ./pkg/services/recordings/internal/services/replay/internal/service -run 'Test(BTRCP0ReplayCharacterization_|TestLoadReplay|TestCreateReplayPlan|TestObserveReplay)' -count=1` — passed for the selected replay packages.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checked-in legacy inference fixture is a useful stable event-order source for reducer/delivery characterization even though its historical metadata is not a valid full current-root runtime input; full root replay should use a recording produced by the current runtime harness.
  - Portable historical replay rejects malformed order before live runtime construction, so its observable failure contract is a typed `ReplayInputError` with zero live effect calls; legacy runtime replay reports incompatible dispatches as typed `DivergenceError`.
---
## 2026-08-10 06:08 -07:00 - btrc-p0-characterization-005
- What was implemented
  - Added deterministic one-shot success and provider-failure characterization at the root `BuildProcess`/`Process.Execute` boundary in `tests/functional/cli/one_shot/characterization_test.go`.
  - Froze the literal 14-event `RUN_REQUEST` through `SESSION_COMPLETED` order from a recorded response, including request, trace, work, dispatch, Worker Session, and `~default` session identities.
  - Asserted the complete successful primary-result projection, terminal `SUCCEEDED`/`FINAL` session result, failed dispatch `unknown`/terminal typed metadata, failed `INVOCATION_RUNTIME_FAILURE` response, one provider command, and one terminal `invocation_result`.
  - Marked story `btrc-p0-characterization-005` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - tests/functional/cli/one_shot/characterization_test.go
  - prd.json
  - progress.txt
- Verification
  - `go test ./tests/functional/cli/one_shot -count=5 -v -timeout 300s` — passed.
  - `go test ./tests/functional/cli/one_shot -race -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/cli/one_shot` — passed.
  - `go test ./tests/functional/cli/... -count=1 -timeout 600s` — the new one-shot, output, input-contract, MCP-resume, root-discovery, and session-resume packages passed; unchanged `tests/functional/cli/named_invocation` has four pre-existing failures around unknown `--mode` compatibility input and unresolved `${executorProvider}` provider selection.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The one-shot invocation requires the provider-shaped output to contain the authored `COMPLETE` stop token; the current primary-result projection retains that marker in the returned text.
- A failed one-shot dispatch currently returns a typed failed `InvocationResponse` and process error while the aggregate canonical session terminal event still projects `SUCCEEDED`/`FINAL` once the single work is terminal; preserve both surfaces in future characterization.
- `--output response-stream` emits canonical Factory Event records followed by one `invocation_result`, making duplicate terminal publication observable without a live server or timing helper.
---
## 2026-08-10 06:26 -07:00 - btrc-p0-characterization-006
- What was implemented
  - Added deterministic ACP target success and JSON-RPC protocol-failure characterization at the existing offline ACP command/process boundary in `tests/functional/providers/acp/btrc_p0_characterization_test.go`.
  - Asserted the literal ten-event `RUN_REQUEST` through `DISPATCH_RESPONSE` order, contiguous sequences, request/dispatch/Worker Session correlation, provider/session identity, one canonical dispatch terminal payload, bounded typed failure detail, provider terminal response mapping, and complete IDLE session/Work projections.
  - Marked story `btrc-p0-characterization-006` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/providers/acp/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=1 -v -timeout 240s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=5 -timeout 300s` — passed.
  - `go test ./tests/functional/providers/acp -race -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/providers/acp` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The ACP worker path publishes ten canonical Factory events through one `DISPATCH_RESPONSE`; the continuous service session remains public `IDLE` while the Work projection carries the terminal target outcome.
  - ACP success ends in a provider `RUN/COMPLETED` response event, while protocol failure is surfaced as a synthesized provider `ERROR/FAILED` response event; canonical dispatch terminal uniqueness is asserted separately from provider-native response observations.
  - The existing JSON-RPC helper process is a deterministic command/protocol edge for ACP characterization and avoids live provider access or sleep-based coordination.
---
## 2026-08-10 06:33 -07:00 - btrc-p0-characterization-007
- What was implemented
  - Added deterministic direct JavaScript success and child-failure characterization at the existing `pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime` owner boundary.
  - Froze the literal `phase:activate` → `child_dispatch` QUEUED/RUNNING/COMPLETED → `phase:apply-result` → `terminal_result` order and checked contiguous runtime record sequences.
  - Asserted stable `dispatch-1`, child index, session-scoped artifact URI, fake Provider Session reference, prompt digest, complete projected child output, one terminal result callback, and typed script failure with `unknown` child failure detail and no primary result.
  - Marked story `btrc-p0-characterization-007` as passed in `prd.json`; no production behavior, public/generated contract, or Factory Sessions cutover changed.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime/host_children_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime -run '^TestBTRCP0DirectJavaScript' -count=5 -v` — passed.
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime -count=1` — passed.
  - `go vet ./pkg/services/factory_runtime/internal/services/orchestration/javascript/runtime` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The direct JavaScript owner publishes ordered `RuntimeRecord` facts through `OnRecord`; terminal typed-value application is observed afterward through exactly one `OnResult` callback rather than a separate runtime record.
  - The deterministic fake child executor gives the direct boundary stable dispatch, artifact, Provider Session, and failure metadata without constructing Factory Sessions or invoking a live provider.
  - A failed fake child emits QUEUED/RUNNING/FAILED records before the JavaScript runtime returns `WORKFLOW_RUNTIME_SCRIPT_ERROR`; the child failure detail remains typed and the terminal result hook is not called.
---
## 2026-08-10 06:50 -07:00 - btrc-p0-characterization-008
- What was implemented
  - Added deterministic standalone fixture characterization in `tests/functional/sessions/standalone/fixture_characterization_test.go` through `root.BuildProcess` and `Process.Execute` at the public fixture-backed MCP stdio boundary.
  - Froze the literal three-event `SESSION_STARTED` → `SESSION_RESULT_UPDATED` → `SESSION_COMPLETED` sequence, event IDs, session identities, sequence/tick cursors, terminal payload statuses, and the complete successful `Ticket triaged and resolved.` result with its artifact reference.
  - Characterized the `missing-source` failure as `FAILED`/`UNAVAILABLE` with no primary result, the same terminal event sequence, a typed `unknown` failure on the session projection, and zero lingering dispatches.
  - Repeated the same success request against two temporary catalog variants with independent session IDs and result text, asserting separate event histories, separate results, and a typed `factory_session.session.not_found` cross-store read.
  - Marked story `btrc-p0-characterization-008` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/sessions/standalone/fixture_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/sessions/standalone -run '^TestBTRCP0StandaloneFixture' -count=1 -v -timeout 240s` — passed.
  - `go test ./tests/functional/sessions/standalone -count=5 -timeout 300s` — passed.
  - `go test ./tests/functional/sessions/standalone -race -count=1 -timeout 300s` — passed.
  - `go vet ./tests/functional/sessions/standalone` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Standalone fixture sessions synthesize canonical lifecycle events when the fixture omits an explicit event array; terminal success and failure therefore share the three-event lifecycle shape while result/session projections carry the meaningful outcome detail.
  - Fixture catalog variants must update the scenario session ID and both the sync-start and read-result session/result identities; the in-memory fake service keeps those projections separate.
- The current failure mapping intentionally preserves `UNAVAILABLE` without `failureDetail` on `start_sync` and `get_result`; the typed missing-source detail is observable on `get` and should remain characterized at that surface.
---
## [2026-08-10 07:20 -07:00] - btrc-p0-characterization-009
- What was implemented
  - Added a root `BuildProcess`/`Process.Execute` one-shot cancellation characterization with a deterministic blocking provider edge. It freezes the 13-event cancellation artifact, typed `CANCELED`/`INVOCATION_CANCELED` result and process error, current `SUCCEEDED`/`FINAL` aggregate session projection, one terminal response-stream record, one provider attempt, and repeated-stop idempotence.
  - Promoted the runtime-opening partial-failure coverage to a named characterization. It records Models and worker acquisition, the late Automations failure, reverse cleanup, the joined initiating/worker-cleanup errors, exactly-once resource stops, and the absence of Factory Runtime assembly.
  - Added concurrent on-demand target characterization proving cancellation and replacement of one opaque target does not affect a second blocked target; it asserts per-target result/session/request identity, isolated cleanup, and repeated cancel/close idempotence.
  - Marked story `btrc-p0-characterization-009` as passed in `prd.json`; no production behavior or public/generated contract changed.
- Files changed
  - `tests/functional/cli/one_shot/btrc_p0_cancellation_characterization_test.go`
  - `pkg/services/factory_sessions/internal/runtimeopening/models_bind_test.go`
  - `pkg/services/factory_sessions/internal/ondemandtarget/target_execution_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/cli/one_shot -run '^TestBTRCP0OneShotCancellationCharacterization$' -count=10` — passed.
  - `go test -race ./tests/functional/cli/one_shot -run '^TestBTRCP0OneShotCancellationCharacterization$' -count=5` — passed.
  - Focused runtime-opening and on-demand target repeat tests (`-count=10`) — passed.
  - Focused runtime-opening and on-demand target race tests (`-race -count=10`) — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The blocked one-shot cancellation path currently permits two complete artifact orders differing only in the adjacent `MODEL_RESPONSE` and `FACTORY_STATE_RESPONSE`; both retain contiguous canonical sequence numbers and exactly-once terminal publication.
  - On-demand cancellation closes the captured runtime, publishes the canceled invocation only after replacement succeeds, and treats controls against the replacement or an evicted identity as idempotent no-ops.
- Late runtime-opening failures use joined errors: the initiating Automations failure remains discoverable alongside earlier worker cleanup failure, while acquired resources close in reverse order and later assembly is not called.
---
## [2026-08-10 07:31 -07:00] - btrc-p0-characterization-009 verification follow-up
- What was implemented
  - Refactored the concurrent target characterization into a deterministic fixture and focused assertion helpers so the new test stays within repository maintainability limits.
  - Consolidated the replay characterization into the existing `pkg/services/recordings/internal/replay/runtime_decoder_test.go` file and split its success assertions, preserving the same observable replay coverage while restoring package file-count and maintainability baselines.
  - Updated the replay story note in `prd.json` to match the reconciled file location; the PR description will be refreshed from this exact PRD.
- Files changed
  - `pkg/services/factory_sessions/internal/ondemandtarget/target_execution_test.go`
  - `pkg/services/recordings/internal/replay/runtime_decoder_test.go`
  - `pkg/services/recordings/internal/replay/btrc_p0_characterization_test.go` (consolidated/deleted)
  - `prd.json`
  - `progress.txt`
- Verification
  - `make test` — passed (short Go suite).
  - `make backend-size` — passed.
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - Affected replay and concurrent-target repeat/race tests and vet — passed.
- **Learnings for future iterations:**
  - Characterization tests should be split into owner-focused helpers before running `pkg-maint`; detailed observable assertions can remain rich without making one test function exceed complexity or line limits.
  - When a P0 test adds a file to a package at its exact file-count baseline, consolidate it into a semantically adjacent existing test file rather than recording a new baseline entry.
---
## 2026-08-10 07:53 -07:00 - btrc-p0-characterization-003 review follow-up
- What was implemented
  - Reconciled the branch with current `upstream/main`, retaining the active BTRC `prd.json`, both append-only progress histories, and the current-main source/generated changes.
  - Moved the hosted characterization into the approved `tests/functional/workstations/poller/` domain/subsection cell.
  - Replaced hosted test timeout waits with `testing.T` context and a context-aware Factory Event stream observation helper.
  - Added explicit continuous-service result reconciliation: success and execution failure assert the typed `NOT_FOUND` live `/result` response while dispatch plus terminal Work/progress remains the complete caller-visible service result. The in-code comment documents why this hosted service path uses the public HTTP inspection contract; finite CLI result mapping is covered by story 005.
- Files changed
  - `tests/functional/workstations/poller/hosted_characterization_test.go`
  - `tests/functional/internal/support/factory_event_stream.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workstations/poller -run '^TestBTRCP0HostedService' -count=1 -v -timeout 180s` — passed.
- **Learnings for future iterations:**
  - The hosted/service success contract is intentionally not a terminal live Factory Session result: continuous Petri sessions stay IDLE, so `/result` returns typed NOT_FOUND while dispatch response, terminal Work, and progress are the stable service result.
  - Moving a functional scenario into an approved domain/subsection can preserve its package-private fixture helpers when the destination owns the same workstation behavior.
---
## 2026-08-10 08:10 -07:00 - btrc-p0-characterization review follow-up
- What was implemented
  - Addressed the blocking PR gate feedback by moving the batch characterization to `tests/functional/factory/batch/` and both one-shot characterizations to `tests/functional/factory/invocation/`, clearing the ratcheted functional package-structure violations.
  - Replaced the batch-specific in-process `ProviderOverride` with the existing `ProviderCommandRunner` edge and queued shaped command results; success remains two accepted dispatches and the failure case now observes the real command-edge `permanent_bad_request` classification on the second work.
  - Added an in-code explanation for the ACP completion ceiling: public terminal Factory Session observation is the positive synchronization, while the duration is only a bounded failure guard around an out-of-process protocol edge.
  - Added an observable runtime-preservation acceptance criterion to story `btrc-p0-characterization-001` and reconciled its notes and all moved test paths in `prd.json`.
- Files changed
  - `tests/functional/factory/batch/batch_characterization_test.go`
  - `tests/functional/factory/invocation/characterization_test.go`
  - `tests/functional/factory/invocation/btrc_p0_cancellation_characterization_test.go`
  - `tests/functional/providers/acp/btrc_p0_characterization_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/batch -count=3 -v -timeout 300s` — passed.
  - `go test ./tests/functional/factory/invocation -count=3 -v -timeout 300s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestBTRCP0ACPTarget(Success|ProtocolFailure)Characterization$' -count=3 -v -timeout 300s` — passed.
  - `make pkg-structure` — passed with no new violations.
  - `go vet ./tests/functional/factory/batch ./tests/functional/factory/invocation ./tests/functional/providers/acp` — passed.
  - `git diff --check` — passed.
  - Parsed `prd.json` reports zero stories with `passes:false`.
- **Learnings for future iterations:**
  - The prior PR head’s Backend Functional Coverage failure was an unrelated flaky `runtime_api` log assertion (`TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`); the required local characterization and structure gates remain green, so rerun CI after publishing before changing unrelated runtime-api behavior.
  - `gh pr view` confirmed current `upstream/main` is already the PR base and the PR is mergeable; compare the explicit base/head SHAs before creating a needless merge commit.
---
## 2026-08-10 08:16 -07:00 - btrc-p0-characterization review verification follow-up
- What was implemented
  - Completed the local review-gate verification after the path, edge, ACP documentation, and PRD acceptance updates; no production behavior or public/generated contract changed.
- Files changed
  - No additional tracked files.
- Verification
  - `make test` — passed.
  - `make backend-size` — passed.
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - `make pkg-structure` — passed.
  - `make vet` — passed.
  - `make typecheck` — passed.
  - Hosted characterization repeated twice — passed.
  - The previously failing `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput` repeated three times locally — passed.
- **Learnings for future iterations:**
  - The prior GitHub functional-coverage failure is reproducible as a transient baseline failure rather than a deterministic source failure; fresh CI after this head is the authoritative check.
---

## 2026-08-10 05:04 -07:00 - dwro-s1-remote-placement-flags-001
- What was implemented
  - Added the authored persistent root `--remote` boolean with explicit running-server semantics, typed local/remote placement resolution, and root input validation.
  - Added local-only, remote-only, or dual placement capabilities to runnable authored command records, manifest-load and constructor validation, resolved placement propagation, and ACP suppression/rejection for the unrelated remote flag.
  - Added observable parser, contract, help, and CLI inventory coverage and regenerated published CLI contracts, manifest metadata, and family Go consumers.
- Files changed
  - `contracts/cli/command-manifest.schema.json`
  - `contracts/cli/commands.json`
  - `contracts/testdata/baseline/cli-command-inputs.json`
  - `packages/api/generated/{cli/command-manifest.schema.json,cli/commands.json,manifest.json}`
  - `pkg/transports/cli/{cliinputs,climanifest,climanifestcobra,generated,root.go,root_docs_test.go,root_serve.go,root_serve_test.go,root_work.go}`
  - `pkg/transports/cli/baseline/testdata/*`
  - `prd.json`
- Verification
  - `make contracts-validate`, `make cli-manifest-check`, and `make test` — passed.
  - Focused manifest, constructor, parser, generated-family, contract, and help tests — passed.
- **Learnings for future iterations:**
  - Persistent root inputs resolve from root Cobra storage even when detached families omit local declarations; inherited manifest records are still needed wherever a family contract explicitly enumerates global inputs.
  - Adding a persistent global requires auditing protocol/listener-style commands that intentionally shadow unrelated globals so they do not advertise or silently accept the new input.
---

## 2026-08-10 05:37 -07:00 - dwro-s1-remote-placement-flags-002
- What was implemented
  - Changed authored `you.run` to dual placement and added an injected remote invocation HTTP adapter that posts the normalized request to exactly the selected `--server` endpoint and default Factory Session.
  - Routed remote `you run` dispatch at the CLI boundary after shared input preparation, preserving local and remote request parity and preventing local initialization, listener probing, or fallback after remote failure.
  - Added stable unsupported-placement validation for local-only detached `you server` and focused remote adapter/public CLI behavioral tests; refreshed published contracts and generated family/wire consumers.
- Files changed
  - `contracts/cli/commands.json`
  - `packages/api/generated/{cli/commands.json,manifest.json}`
  - `pkg/transports/cli/{generated,root.go,root_work.go,run/remote.go,run/remote_test.go,root_remote_placement_test.go}`
  - `pkg/wire/{cli_commands.go,wire.go,wire_gen.go}`
  - `prd.json`
- Verification
  - `make contracts-check`, `make cli-manifest-check`, focused CLI/remote adapter tests, and `make test` — passed.
- **Learnings for future iterations:**
  - The shared normalized invocation projection can be reused by local and remote adapters while keeping placement selection out of domain services.
---

## 2026-08-10 06:16 -07:00 - dwro-s1-remote-placement-flags-003
- What was implemented
  - Added explicit `--listen <host:port>` parsing for loopback listener addresses and resolved it into the existing bind host, port, and auto-port configuration at the CLI boundary.
  - Applied `--listen` to `you server`, `you run --with-server`, and `you run --with-site`; ordinary runs reject misplaced listener input before invoking the run handler.
  - Preserved explicitly supplied local `--server` listener binding as a compatibility fallback with one actionable migration warning, made explicit `--listen` take precedence, and added manifest/help/example/reference guidance and lifecycle tests.
- Files changed
  - `contracts/cli/commands.json`
  - `contracts/testdata/baseline/{cli-command-inputs.json,cli-commands.json}`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `docs/reference/{agents,operations,run,sessions}.md`
  - `README.md`
  - `packages/api/generated/{cli/commands.json,manifest.json}`
  - `pkg/transports/cli/{climanifest/run_submit_validation.go,climanifestcobra/constructor_coverage_test.go,cliserver/local_bind.go,cliserver/local_bind_test.go,generated/family_manifests_gen.go,root.go,root_factory.go,root_submit_batch.go,root_submit_test.go,root_work.go,runconfig/config.go}`
  - `tests/functional/{cli/root_discovery/root_discovery_test.go,transport/run_scoped_server/run_scoped_server_test.go}`
  - `prd.json`
- Verification
  - `make contracts-validate`, `make cli-manifest-check`, `make docs-reference-smoke`, `make test`, focused CLI tests, full scoped-server functional tests, backend-size, pkg-maint, pkg-file-count, vet, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - Listener-owned inputs belong in the detached run/server family manifest as local flags; resolve `--listen` to `BindHost`/`Port`/`AutoPort` and emit legacy `--server` migration warnings only for commands that actually own a listener.
  - Exact listener lifecycle tests should assert readiness and shutdown through returned process state rather than arbitrary sleeps.
---

## 2026-08-10 06:59 -07:00 - dwro-s1-remote-placement-flags-004
- What was implemented
  - Added stable `REMOTE_LOCAL_HOSTING_CONFLICT` bad-request validation before system initialization and after custom run input parsing, covering persistent flags before or after `run` and both local hosting modes.
  - Authored conflict relationships, executable help/example guidance, local `--listen` versus remote `--server` vocabulary, and legacy migration documentation; regenerated contract artifacts and CLI baselines.
  - Added behavioral assertions for stable JSON output and zero run, remote, browser, listener, and system-initialization side effects.
- Files changed
  - `README.md`
  - `contracts/cli/commands.json`
  - `docs/reference/{run,sessions}.md`
  - `packages/api/generated/{cli/commands.json,manifest.json}`
  - `pkg/services/factory_runtime/transports/cli/invocation_error.go`
  - `pkg/transports/cli/{baseline/testdata/run_flags.txt,baseline/testdata/run_help.txt,climanifest/run_submit_validation.go,climanifest/run_submit_validation_test.go,generated/family_manifests_gen.go,root_factory.go,root_remote_placement_test.go,root_work.go,run/invocation_error.go}`
  - `tests/functional/smoke/cli_docs_smoke_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli ./pkg/transports/cli/climanifest ./pkg/services/factory_runtime/transports/cli`
  - `go test ./pkg/transports/cli/baseline -count=1`
  - `make test`, `make verify-fast`, `make cli-manifest-check`, `make contracts-check`, `make docs-reference-smoke`, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - Custom `run` flag parsing happens inside `RunE`, so pre-run relationship metadata cannot observe local flags; early raw-argv validation plus post-parser validation keeps errors side-effect-free and order-independent.
  - On Windows, timed-out `make`/unitlane shells can outlive the tool timeout and contend on Go build files; inspect and clean only the identified child process tree before rerunning verification.
---

## 2026-08-10 07:22 -07:00 - dwro-s1-remote-placement-flags-004 CI follow-up
- What was implemented
  - Updated the legacy listener bind-failure functional assertion to verify the intentional `--server` migration warning separately before decoding the structured `SERVER_BIND_FAILED` response.
  - Added remote-adapter coverage for invalid context/protocol/endpoint/request input, unavailable and non-API HTTP responses, terminal failure metadata, missing invocation/status errors, endpoint redaction, and credential-safe diagnostics.
- Files changed
  - `pkg/transports/cli/run/remote_test.go`
  - `tests/functional/transport/http/server/startup_shutdown_test.go`
  - `progress.txt`
- Verification
  - Focused remote invocation and server bind-failure tests — passed.
  - `go test ./pkg/transports/cli/run -cover -count=1` — passed (73.6% package-local coverage; the repository coverage command cannot be launched from this Windows checkout because its generated Go command exceeds Windows command-line length).
  - GitHub CI run `31396592503` identified the package threshold and stale stderr assertion; frontend, docs, API/package, provider-package, local-inference, and local fast-tier checks passed. A new CI run is required after this fix.
- **Learnings for future iterations:**
  - Structured stderr consumers must parse after explicitly expected compatibility warnings; migration diagnostics are part of the observable CLI contract and must not be treated as malformed JSON.
  - When a package coverage threshold is narrowly missed after adding an adapter, cover its error and terminal branches in the owning package rather than lowering the manifest minimum.
---

## 2026-08-10 07:42 -07:00 - dwro-s1-remote-placement-flags-004 CI coverage follow-up
- What was implemented
  - Added public-process coverage for remote/local hosting conflicts with persistent flags before and after `run`, asserting stable bad-request JSON and zero listener, browser, session, or runtime effects.
  - Added observable placement rejection for remote `server` and manifest-projected local-only Factory validation commands.
  - Added malformed exact `--listen` coverage, proving invalid host:port input fails before listener startup.
- Files changed
  - `tests/functional/transport/run_scoped_server/run_scoped_server_test.go`
  - `progress.txt`
- Verification
  - Focused placement and listener tests passed.
  - Full `go test ./tests/functional/transport/run_scoped_server -count=1 -timeout 300s` passed.
  - Isolated coverage confirmed the generic manifest placement error path is now exercised. GitHub CI run `31397938999` showed no test failures but reported the three narrowly missed functional package thresholds and one frontend browser timeout; a new CI run is required after this coverage fix.
- **Learnings for future iterations:**
  - Use `root.BuildProcess` functional cases for placement metadata and listener parsing; these tests cover the production CLI packages that unit-only tests do not affect.
---

## 2026-08-10 08:10 -07:00 - dwro-s1-remote-placement-flags-004 CI follow-up
- What was implemented
  - Inspected the latest PR CI run and reran the failed backend functional lane three times after the implementation and coverage changes were pushed.
  - Confirmed the placement-related tests and coverage paths were not among the failures; the full matrix instead exposed unrelated timing-sensitive cancellation and shutdown races in existing functional packages.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31399810089` retained green results for frontend, browser, unit coverage, API/package, docs, providers, local inference, and integration lanes.
  - Functional rerun failures varied between `orchestration/javascript/durability`, `sessions/restart`, `cli/mcp_resume`, and `workers/mock`; failures reported cancellation observation or `factory runtime is already stopped`, with no remote-placement assertion failures.
  - Local focused placement, listener, CLI, and full short Go verification had already passed on `9fa903e83`.
- **Learnings for future iterations:**
  - Repository-wide functional CI runs can expose nondeterministic cancellation/shutdown races under parallel load; diagnose the exact package artifact before changing unrelated production code.
  - A required CI check remains a PR follow-up even when all story acceptance criteria and focused verification pass.
---

## 2026-08-10 08:39 -07:00 - dwro-s1-remote-placement-flags-004 CI coverage follow-up
- What was implemented
  - Addressed the latest Backend Functional Coverage failure by adding functional evidence for exact `--listen` precedence over legacy `--server` and for selected-server remote dispatch with normalized request content and zero local listener starts.
  - Preserved the existing functional coverage baseline; the new scenarios cover 10 statements that were zero in the uploaded `pkg/transports/cli` profile, above the seven-statement shortfall.
- Files changed
  - `tests/functional/transport/run_scoped_server/run_scoped_server_test.go`
  - `progress.txt`
- Verification
  - Focused exact-listener and remote-dispatch tests — passed.
  - Full `go test ./tests/functional/transport/run_scoped_server -count=1 -timeout 240s` — passed.
  - `git diff --check` — passed; commit `2c0bee85e` contains the focused coverage fix. A new CI run is required after pushing this commit.
- **Learnings for future iterations:**
  - The functional coverage floor for `pkg/transports/cli` includes root transport statements reached only through `root.BuildProcess` scenarios; package-local tests do not restore that functional threshold.
  - Compare the CI `coverage.out` block counts with a focused functional profile to select observable scenarios that cover the missing statements without lowering a package baseline.
---

## 2026-08-10 08:43 -07:00 - dwro-s1-remote-placement-flags-004 PR head follow-up
- What was implemented
  - Confirmed the open PR now references the pushed coverage-fix head and remains free of review comments or unresolved threads.
  - Recorded the required follow-up while GitHub schedules checks for the current PR head.
- Files changed
  - `progress.txt`
- Verification
  - PR #1852 head is `1488c29a7`; the prior failed CI run is explicitly tied to its older head `8d1a235a7`.
  - Local worktree is clean after the coverage and progress commits.
- **Learnings for future iterations:**
  - Always compare the PR head SHA with each check run’s head SHA before treating a stale failed check as evidence about the current commit.
---

## 2026-08-10 09:10 -07:00 - dwro-s1-remote-placement-flags-004 CI follow-up
- What was implemented
  - Reran the failed Backend Functional Coverage job after confirming its failure was an unrelated timing-sensitive JavaScript durability cancellation observation, not a placement assertion or coverage regression.
  - Recorded the terminal passing result for the current PR head and completed the review-thread audit.
- Files changed
  - `progress.txt`
- Verification
  - Rerun of CI run `31405267195` passed Backend Functional Coverage and Verification Policy; every required matrix check is green on head `4032d635b`.
  - PR #1852 has no conversation comments, reviews, or unresolved review threads; its body remains the `prd.json` content.
  - The four PRD stories remain marked `passes: true`, and the local worktree is clean.
- **Learnings for future iterations:**
  - When a full functional lane fails on a cancellation-observation race, inspect the emitted package/test artifact before changing unrelated runtime code; rerunning the failed lane can distinguish a transient failure from a regression.
---

## 2026-08-10 09:17 -07:00 - dwro-s1-remote-placement-flags-004 main synchronization
- What was implemented
  - Merged the current `origin/main` into the placement branch, preserving the active S1 PRD while incorporating main's unrelated characterization, documentation, and smoke-test updates.
  - Reconciled the append-only progress log and retained reusable patterns from both branches.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main characterization and documentation files included by the merge.
- Verification
  - Resolved merge conflicts in `prd.json` and `progress.txt`; no conflict markers remain.
  - `prd.json` parses with four user stories and zero failed `passes` values.
  - `git diff --check` passed; a new PR check run is required for the merged head.
- **Learnings for future iterations:**
  - When a task branch is behind main, keep the task-scoped PRD authoritative and merge main's progress entries and unrelated changes so PR description parity and append-only history remain intact.
---

## 2026-08-10 10:51 -07:00 - dwro-s1-remote-placement-flags-004 review follow-up
- What was implemented
  - Removed duplicate `placement` keys from the authored CLI manifest and regenerated the published CLI artifacts and manifest hash metadata.
  - Added strict duplicate-JSON-key rejection at CLI manifest load time, including a regression test for duplicate `placement` keys.
  - Made malformed remote endpoint diagnostics fail closed with a fixed redacted label, including a credential-bearing malformed-endpoint regression test.
  - Refactored run error handling and remote invalid-input tests to satisfy maintainability limits, consolidated placement tests into the existing CLI run test owner to preserve package file-count baselines, removed mock workers from worker-free listener scenarios, and removed marker-only docs assertions.
- Files changed
  - `contracts/cli/commands.json`
  - `packages/api/generated/cli/commands.json`
  - `packages/api/generated/manifest.json`
  - `pkg/transports/cli/climanifest/load.go`
  - `pkg/transports/cli/climanifest/load_test.go`
  - `pkg/transports/cli/root_run_test.go`
  - `pkg/transports/cli/root_work.go`
  - `pkg/transports/cli/run/remote.go`
  - `pkg/transports/cli/run/remote_test.go`
  - `tests/functional/smoke/cli_docs_smoke_test.go`
  - `tests/functional/transport/run_scoped_server/run_scoped_server_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make verify-fast` — passed on rerun; dashboard typecheck, MCP contract boundary, 355 Vitest files / 2,970 tests plus 191 Bun tests, and short Go suite passed.
  - `make contracts-validate`, `make cli-manifest-check`, `make backend-size`, `make pkg-structure`, `make pkg-maint`, `make pkg-file-count`, and `make vet` — passed.
  - `make docs-reference-smoke` — passed.
  - `go test ./pkg/transports/cli/... -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/transport/run_scoped_server -count=1 -timeout 300s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Strict duplicate-key validation belongs at the manifest load boundary; authored and published contract artifacts must both be regenerated after source changes.
  - Functional listener cases that do not execute workers should omit mock-worker flags; worker/mock ownership remains isolated to the worker-focused functional package.
  - When a package file-count baseline is deletion-only, place semantically adjacent tests in an existing owner file rather than adding a new package file.
---

## 2026-08-10 11:12 -07:00 - dwro-s1-remote-placement-flags-004 CI coverage follow-up
- What was implemented
  - Investigated the current PR head's Backend Unit Coverage and Backend Functional Coverage failures. Both were caused by the new duplicate-key scanner adding uncovered statements to `pkg/transports/cli/climanifest`, not by a behavior failure.
  - Moved the reusable strict JSON decoder into the existing policy-free `pkg/platform/generatedartifacts` package, kept the manifest loader as the public enforcement boundary, and added malformed/trailing/nested duplicate-key coverage.
  - Added loader coverage for required source-store and compatibility loading paths so the existing CLI manifest package floor remains satisfied.
- Files changed
  - `pkg/platform/generatedartifacts/json.go`
  - `pkg/platform/generatedartifacts/json_test.go`
  - `pkg/transports/cli/climanifest/load.go`
  - `pkg/transports/cli/climanifest/load_test.go`
  - `progress.txt`
- Verification
  - `go test ./pkg/platform/generatedartifacts -covermode=count ...` — 88.9% package coverage, above the 85.71% unit floor.
  - `go test ./pkg/transports/cli/climanifest -covermode=count ...` — 88.5529% package coverage, above the 88.55% unit floor.
  - `make verify-fast`, `make pkg-maint`, `make pkg-file-count`, and `go test ./pkg/transports/cli/... -count=1` — passed.
  - The local full coverage command is unavailable on this Windows checkout because its generated Go invocation exceeds the Windows command-line length; the pushed Linux CI run is required to confirm both independent coverage lanes.
- **Learnings for future iterations:**
  - When a package-local coverage floor is tight, place generic enforcement logic in an existing package with an appropriate coverage exception/floor and cover it there; keep the feature package responsible only for invoking the policy.
  - A green focused unit suite is not enough for CI coverage: inspect the exact package totals and uncovered statement profile from the failing Actions artifact before changing a baseline.
---

## 2026-08-10 04:46 -07:00 - pf-cli-exit-codes-001
- What was implemented
  - Added a CLI-owned process-boundary diagnostic policy that silences Cobra's implicit error text, preserves authored coded errors, and maps unclassified failures to the safe typed `CLI_COMMAND_FAILED` fallback without inspecting terminal output.
  - Added a transparent diagnostic writer marker so existing run/server structured renderers remain single-shot; direct Cobra callers retain their legacy presentation through the central-policy context switch.
  - Routed Worker Sessions JSON error payloads to the process diagnostic writer under the central policy so failures never add an error object to stdout, while direct service callers keep their existing output ownership.
  - Added root.BuildProcess/Process.Execute coverage for authored invocation errors and safe unclassified remote failures, including stdout isolation and stderr JSON assertions.
- Files changed
  - `pkg/transports/cli/clidiag/diagnostics.go`
  - `pkg/transports/cli/clidiag/diagnostics_test.go`
  - `pkg/transports/cli/root.go`
  - `pkg/transports/cli/root_work.go`
  - `pkg/transports/cli/run/invocation_error.go`
  - `pkg/transports/cli/baseline/goal_failure_process_test.go`
  - `pkg/services/factory_runtime/transports/cli/invocation_error.go`
  - `pkg/services/worker_sessions/transports/cli/{list,read,show,stream}.go`
  - `pkg/transports/cli/submit/error_output.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/factory ./pkg/root ./pkg/initializer/application ./pkg/services/factory_runtime/transports/cli ./pkg/services/worker_sessions/transports/cli ./pkg/transports/cli -count=1` — passed.
  - `go test ./pkg/root ./pkg/initializer/... ./pkg/services/factory_runtime/... ./pkg/services/worker_sessions/... ./pkg/transports/cli/...` — changed packages passed; the command stopped only at the pre-existing `TestDocsTopicIndexBaseline_MatchesFixture` docs-topic fixture drift.
  - `go vet ./pkg/transports/cli/... ./pkg/root ./cmd/factory` — passed.
  - `make pkg-maint pkg-file-count` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Keep the next server and stream stories on the same process-boundary marker/context seam so startup and stream-specific typed codes can be added without duplicating the command graph or changing service ownership.
  - The current checkout's docs-topic baseline is stale relative to the live packaged-factory docs (`packaged-factories` is added and the providers description differs); it is unrelated to this CLI story and should be reconciled separately.
---

## 2026-08-10 05:01 -07:00 - pf-cli-exit-codes-002
- What was implemented
  - Kept the existing runtime startup-failure transition and `SERVER_BIND_FAILED` typed mapping, while preventing failed hosted runs from printing the success-looking `Recording saved:` message.
  - Added a direct run regression proving a startup/runtime failure does not report recording success while successful shutdown output remains covered.
  - Added a built-executable regression that occupies `127.0.0.1:65535`, exhausts auto-port selection, and verifies the OS process exits non-zero with one `SERVER_BIND_FAILED` JSON diagnostic on stderr and no readiness or success output on stdout.
- Files changed
  - `pkg/transports/cli/run/run.go`
  - `pkg/transports/cli/run/run_startup_test.go`
  - `tests/functional/transport/server_binding/server_binding_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli/run -run 'TestRun_ShutdownOutputReportsResolvedAutoGeneratedRecordingPath|TestRun_StartupFailureDoesNotReportRecordingSuccess' -count=1 -v` — passed.
  - `go test ./tests/functional/transport/server_binding -run '^TestBuiltExecutableServerBindFailureExitsNonZeroWithoutReadinessOutput$' -count=1 -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A server bind error can be correctly typed and non-zero while still leaking a success-looking recording-path line; terminal success output must be gated on a successful hosted run.
  - Port 65535 provides a deterministic built-process bind failure because the default auto-port scan has no higher candidate; skip only when the test environment cannot reserve that terminal port.
  - Process.Execute proves returned errors and streams, but an `exec.Command` test is still required to prove the final OS exit status.
---

## 2026-08-10 07:06 -07:00 - pf-cli-exit-codes-003
- What was implemented
  - Kept the existing EOF-after-open stream classification and added a typed `WORKER_SESSION_STREAM_FAILED` wrapper for output-write failures, preserving the underlying cause without exposing it in the safe diagnostic message.
  - Added root.BuildProcess/Process.Execute coverage for an early-closing Worker Sessions SSE response. The complete `RECORD` frame remains unchanged on stdout, no terminal or replay-success frame is synthesized, and stderr contains exactly one `WORKER_SESSION_STREAM_CLOSED` diagnostic.
  - Added a focused built-executable regression proving the same stream abort exits non-zero at the OS process boundary.
  - Updated the existing Worker Sessions required-input functional assertion to decode the central-policy diagnostic from stderr, matching the already-established story-001 output contract.
- Files changed
  - `pkg/services/worker_sessions/transports/cli/stream.go`
  - `pkg/services/worker_sessions/transports/cli/stream_test.go`
  - `tests/functional/provider_sessions/cli/stream_abort_test.go`
  - `tests/functional/provider_sessions/cli/worker_sessions_cli_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/worker_sessions/transports/cli -run 'TestStream(ReturnsClosedBeforeTerminalAsJSONError|ReturnsMalformedFrameAsTypedFailure|ReturnsOutputWriteFailureAsTypedStreamFailure|JSONUsesSessionScopedSSEAndStopsAtTerminal|ReplayOnlyWritesTerminalReplayBeforeCompleteSummary|PropagatesContextCancellation|MapsPostOpenContextCancellationToInterruptedAndClosesBody)' -count=1 -v` — passed.
  - `go test ./tests/functional/provider_sessions/cli -count=1 -timeout 300s` — passed.
  - `go vet ./pkg/services/worker_sessions/transports/cli ./pkg/transports/cli/... ./pkg/root ./cmd/factory` — passed.
  - `make pkg-maint`, `make pkg-file-count`, and `git diff --check` — passed.
  - `make typecheck` — blocked by the pre-existing checkout dependency baseline: the pinned `bun install --frozen-lockfile` completed, but `@types/bun` remains unavailable; no UI files changed.
  - The broader CLI package lane still reports the pre-existing `TestDocsTopicIndexBaseline_MatchesFixture` drift documented by earlier iterations; it is unrelated to this story.
- **Learnings for future iterations:**
  - A stream can emit valid partial frames before the transport closes, so the error must be returned after preserving those frames and the process boundary must own the single stderr diagnostic.
  - `Process.Execute` proves typed propagation and stdout/stderr ownership; only a built `you` subprocess proves the final non-zero OS exit status.
  - Keep stream output-write errors typed with a safe message while retaining the underlying cause through `Unwrap` for diagnostics and tests.
---

## 2026-08-10 07:20 -07:00 - pf-cli-exit-codes-004
- What was implemented
  - Expanded the process-boundary cancellation matrix so wrapped cancellations from `you run`, `you server`, and `you worker-sessions stream` retain the manifest-declared exit 130, while an ordinary failure with a canceled parent context and undeclared command cancellation remain exit 1.
  - Added built-executable run/server interrupt regressions and upgraded the finite success and worker-failure checks to observe the shipped binary's exit status; the existing server-bind and Worker Sessions abort checks complete the 0/non-zero/130 matrix.
  - Added a built Worker Sessions SSE cancellation regression that preserves the complete frame received before interruption and verifies exit 130 without synthesizing a terminal frame.
  - Updated process fixtures and assertions to match the already-established story-001 safe `CLI_COMMAND_FAILED` diagnostic contract for unclassified Cobra, initializer, and Work-watch failures.
- Files changed
  - `cmd/factory/main_test.go`
  - `tests/functional/provider_sessions/cli/stream_abort_test.go`
  - `tests/functional/transport/cli/process/exit_codes_test.go`
  - `tests/functional/transport/cli/process/fixtures_test.go`
  - `tests/functional/transport/cli/process/unknown_command_test.go`
  - `tests/functional/transport/cli/process/watch_abort_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/factory -run '^TestProcessExitCodePreservesDeclaredLifecycleContract$' -count=1 -v` — passed.
  - `go test ./tests/functional/transport/cli/process -count=1 -timeout 300s` — passed, including built finite success, worker failure, server cancellation (Unix), and ordinary failure coverage.
  - `go test ./tests/functional/provider_sessions/cli -count=1 -timeout 300s` — passed; Worker Sessions cancellation test is skipped on Windows and runs on Unix.
  - `go test ./tests/functional/transport/server_binding -count=1 -timeout 300s` — passed.
  - `go test ./cmd/factory ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli/... -count=1` — changed packages passed; the command still reports the pre-existing `TestDocsTopicIndexBaseline_MatchesFixture` drift (`packaged-factories` and the current providers description) in `pkg/transports/cli/baseline`.
  - `go vet ./cmd/factory ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli/...`, `make pkg-maint pkg-file-count`, and `git diff --check` — passed.
  - Dashboard typecheck remains blocked by the pre-existing checkout dependency baseline (`@types/bun`/deleted `ui/packages/*` sources); no UI files changed.
- **Learnings for future iterations:**
  - `root.BuildProcess`/`Process.Execute` returns errors but cannot expose the final OS status; use a built `./cmd/factory` subprocess only for the status assertions and keep ordinary functional behavior on the root process.
  - The process boundary intentionally replaces unclassified Cobra and stream errors with one safe `CLI_COMMAND_FAILED` diagnostic; functional fixtures must assert the coded safe payload rather than parse underlying error text.
  - Manifest-derived cancellation selection must ignore positional prompt text and flag values, and must reject exit 130 for commands without a declared `cancel` exit.

## 2026-08-10 07:28 -07:00 - pf-cli-exit-codes current-main synchronization follow-up
- What was implemented
  - Merged the current `origin/main` into `pf-cli-exit-codes` and resolved the shared `prd.json`/`progress.txt` conflict by retaining the active CLI-exit-codes PRD and combining both append-only progress histories and reusable patterns.
  - Preserved current-main provider-discovery source, generated contract artifacts, and baseline updates in the merge commit `072f7d37c`.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main source/generated files were retained by the synchronization merge.
- Verification
  - `go test ./tests/functional/transport/cli/process -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/provider_sessions/cli -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/transport/server_binding -count=1 -timeout 300s` — passed.
  - `go vet ./cmd/factory ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli/...`, `make pkg-maint pkg-file-count`, and `git diff --check` — passed.
  - The broad CLI package sweep reaches the current-main `TestWalk_ProductionInventoryMatchesCommittedBaseline` command-identity fixture drift (52,445 generated bytes vs 51,822 committed); this is unrelated to the CLI exit-code changes.
- **Learnings for future iterations:**
- A moving main branch can change shared PR bookkeeping and generated CLI inventory in the same synchronization; retain both progress histories, keep the task PRD authoritative for PR-body parity, and distinguish current-main baseline drift from feature regressions.
---

## 2026-08-10 07:49 -0700 - pf-cli-exit-codes CI diagnostic assertion follow-up
- What was implemented
  - Updated the remaining functional fixtures that exercise the built or root CLI process boundary to assert the safe `CLI_COMMAND_FAILED` ErrorResponse rather than hidden initializer, catalog, model, session, submit, work, or drain cause text.
  - Added `support.RequireSafeCLIDiagnostic` as the shared observable assertion for the process-boundary fallback and preserved stdout-isolation, non-zero failure, and cancellation-diagnostic checks.
  - Kept the model-provider ACP functional failure unchanged after an isolated rerun passed; it was a parallel CI flake rather than a reproducible exit-code regression.
- Files changed
  - Functional acceptance, bootstrap, catalog, models, session, smoke, CLI command, Work watch, and mock-worker tests.
  - `tests/functional/internal/support/process.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/acceptance ./tests/functional/bootstrap_portability ./tests/functional/factory/packaged/catalog ./tests/functional/models/model_list ./tests/functional/sessions/execution ./tests/functional/sessions/lifecycle ./tests/functional/smoke ./tests/functional/transport/cli/commands ./tests/functional/work/watch ./tests/functional/workers/mock -count=1 -timeout 600s` — passed after the follow-up assertions were added.
  - `go test ./tests/functional/acceptance ./tests/functional/transport/cli/commands ./tests/functional/workers/mock -count=1 -timeout 600s` — passed.
  - `go test ./tests/functional/providers/acp -run '^TestFactoryRunRetriesACPProviderByResumingExactSession$' -count=1 -timeout 120s -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Functional tests at the executable boundary must assert the sanitized serialized diagnostic; the returned error may still retain its cause for in-process `errors.Is` checks, but process stderr intentionally does not expose it.
- Cancellation diagnostics are an intentional observable exception to the JSON fallback: a directly canceled root command can emit exactly `Error: context canceled` while preserving the cancellation error for exit-status selection.
---

## 2026-08-10 08:18 -0700 - pf-cli-exit-codes coverage and stream-test follow-up
- What was implemented
  - Added focused unit coverage for `clidiag` nil/default edges, context markers, invocation-coded errors, safe fallback normalization, writer behavior, and presentation helpers; the package now exceeds its tracked unit coverage floor.
  - Added focused coverage for `SubmissionHTTPError` safe CLI fields so the submit package remains above its tracked unit coverage floor after the diagnostic changes.
  - Made the built Worker Sessions abort fixture flush its complete SSE frame before closing and increased only its test timeout bound to avoid coverage-lane scheduling flakiness.
- Files changed
  - `pkg/transports/cli/clidiag/diagnostics_test.go`
  - `pkg/transports/cli/submit/submit_transport_test.go`
  - `tests/functional/provider_sessions/cli/stream_abort_test.go`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli/clidiag -count=1 -cover` — passed at 96.1%.
  - `go test ./pkg/transports/cli/submit -count=1 -cover` — passed at 88.0%.
  - `go test ./tests/functional/provider_sessions/cli -run '^TestBuiltWorkerSessionsStreamAbortExitsNonZero$' -count=5 -timeout 360s -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a coverage lane runs built-process SSE tests in parallel with broad instrumentation, flush the fixture frame explicitly before returning so the client observes the intended complete-frame-then-EOF sequence.
  - Coverage-floor regressions from new boundary policy code are best repaired with unit tests for nil/default and marker branches, not by lowering the existing manifest minimum.
  - A browser integration timeout in an unchanged UI surface should be rerun and recorded as an environmental/current-main flake before changing frontend code.
---

## 2026-08-10 08:32 -0700 - pf-cli-exit-codes functional coverage repair
- What was implemented
  - Routed terminal logger construction through the policy's existing structured-terminal decision so the resolved debug behavior is exercised by the functional CLI path without changing the resulting logger mode.
  - Added a root.BuildProcess functional assertion for a JSON unary `you submit` payload, complementing the existing Markdown payload case and restoring executable coverage for both payload classifications.
- Files changed
  - `pkg/transports/cli/terminalpolicy/policy.go`
  - `tests/functional/transport/submit/submit_test.go`
  - `progress.txt`
- Verification
  - `go test -short ./tests/functional/transport/submit -run '^TestSubmitFamilyExecutesThroughRootBuiltProcess$' -count=1 -timeout 180s -v` — passed.
  - `go test ./pkg/transports/cli/terminalpolicy -count=1 -cover` — passed at 92.3%.
  - `git diff --check` — passed.
  - Committed as `690721f8d`.
- **Learnings for future iterations:**
  - Functional coverage floors include policy helper branches, so keep exported policy decisions on the production path that consumes them and pair file-ingress helpers with observable JSON and Markdown CLI cases.
---

## 2026-08-10 08:47 -0700 - pf-cli-exit-codes functional payload fallback coverage
- What was implemented
  - Reused the existing live unary submit functional scenario with a `.txt` payload fixture, covering the customer-visible generic file classification while retaining the `.md` and `.json` cases.
- Files changed
  - `tests/functional/transport/submit/submit_test.go`
  - `progress.txt`
- Verification
  - `go test -short ./tests/functional/transport/submit -run '^TestSubmitFamily' -count=1 -timeout 240s -v` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Payload classification coverage is most stable when the same live submit contract exercises Markdown, JSON, and generic file extensions through the public request path.
---

## 2026-08-10 09:04 -0700 - pf-cli-exit-codes CI closeout
- What was implemented
  - Completed the required PR review/CI follow-up after the functional coverage repair; the first rerun encountered two unrelated functional flakes, and the failed coverage job was rerun cleanly on the same head.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31405306468` passed all checks, including Backend Functional Coverage, Backend Unit Coverage, UI Backend Integration, and Verification Policy.
  - Final functional coverage artifact reports `clidiag` 61/76 statements (80.3%) and `terminalpolicy` 22/26 statements (84.6%), both meeting their manifest floors.
  - PR #1856 has no conversation comments, reviews, or review threads; all four PRD stories remain `passes:true`.
- **Learnings for future iterations:**
- When a broad functional coverage job fails before producing a coverage profile because of unrelated scenario flakes, rerun only the failed job before changing feature code; the clean rerun can validate the intended coverage delta directly.
---
## 2026-08-10 09:28 -07:00 - pf-cli-exit-codes current-main sync and gate repair
- What was implemented
  - Reconciled the branch with current `origin/main`, retained the CLI task PRD, and preserved the complete current-main and CLI append-only progress history.
  - Split the process exit-code case data, submit functional scenarios, and diagnostic nil/default coverage into focused test units so backend-size and pkg-maintainability gates remain green without changing observable behavior.
- Files changed
  - Current-main merge files plus `progress.txt`
  - `cmd/factory/main_test.go`
  - `tests/functional/transport/submit/submit_test.go`
  - `pkg/transports/cli/clidiag/diagnostics_test.go`
- Verification
  - CLI process, Worker Sessions stream, server-binding, CLI package, submit, current-main characterization, and short Go tests passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make typecheck` passed after `bun install --frozen-lockfile` restored ignored checkout dependencies.
  - `git diff --check` passed; `prd.json` parses with all four stories marked `passes:true`.
- **Learnings for future iterations:**
  - Maintainability gates apply to test helpers and coverage tests as well as production code; move large tables or independent assertion groups into focused helpers while retaining the same public assertions.
---
## 2026-08-10 09:45 -07:00 - pf-cli-exit-codes CI rerun follow-up
- What was implemented
  - Investigated CI run `31409217326` and isolated its only primary failure to the unchanged JavaScript parallel-concurrency functional test under coverage instrumentation.
  - Reran that test ten times locally and reran only the failed GitHub Actions jobs; no feature change was made for the non-reproducible flake.
- Files changed
  - `progress.txt`
- Verification
  - The isolated concurrency test passed `-count=10`.
  - CI run `31409217326`, attempt 2, passed Backend Functional Coverage, Verification Policy, and all other required jobs for head `ffc60b3994cf8c7a22f7bf2ba80dcb57709d833f`.
  - PR #1856 remains conflict-free with no conversation comments, reviews, or unresolved threads.
- **Learnings for future iterations:**
- A missing coverage artifact can be secondary to a single flaky functional test; inspect the named failure, rerun it in isolation, and rerun only failed jobs before modifying unrelated orchestration behavior.
---
## 2026-08-10 10:13 -07:00 - pf-cli-exit-codes review follow-up
- What was implemented
  - Removed the transport-owned `context.Background()` fallback from `clidiag.WithCentralDiagnostics`; a nil caller context remains nil and central diagnostics are enabled only when the caller supplies a context.
  - Updated the focused context-helper test to assert that no implicit context is created.
- Files changed
  - `pkg/transports/cli/clidiag/diagnostics.go`
  - `pkg/transports/cli/clidiag/diagnostics_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli/clidiag -count=1 -cover` — passed at 96.1%.
  - `go vet ./pkg/transports/cli/clidiag` — passed.
  - `make pkg-boundary` — reports the known 176-entry baseline and no new `clidiag` violation; the command remains non-zero because the baseline is intentionally tracked.
  - The blocking PR comment requires the pushed head to be compared with the exact base count before review resumes.
- **Learnings for future iterations:**
  - Transport helpers must preserve caller-owned context semantics; returning nil for a nil input is preferable to manufacturing a background context merely to satisfy `context.WithValue`.
---
## 2026-08-10 10:39 -07:00 - pf-cli-exit-codes CI rerun follow-up
- What was implemented
  - Investigated the first failed Backend Functional Coverage run after the review fix; its terminal-failure output and continuous-run cancellation failures were unrelated coverage/process-environment flakes and reproduced neither as a feature regression locally.
  - Reran only failed GitHub Actions jobs twice. The first rerun moved to the known JavaScript durability cancellation-observation flake; that test passed locally 10/10. The second targeted rerun completed successfully.
- Files changed
  - `progress.txt`
- Verification
  - Focused invocation failure test passed three times locally.
  - `TestJavaScriptInterruptedSessionResumesWithoutRepeatingCompletedChildren` passed 10/10 locally.
  - CI run `31413200225` final failed-job rerun passed Backend Functional Coverage, Backend Unit Coverage, Verification Policy, and all other required checks for head `06a8d59e8`.
- **Learnings for future iterations:**
  - Broad functional coverage failures can move between existing cancellation-sensitive scenarios under instrumentation; inspect the named test, stress it locally, and rerun only failed jobs before changing unrelated runtime behavior.
---
## 2026-08-10 10:59 -07:00 - pf-cli-exit-codes-001 review and PR follow-up
- What was implemented
  - Verified that the already-pushed context-boundary fix removes the transport-owned `context.Background()` fallback and leaves nil caller contexts nil.
  - Compared the current branch with a detached `origin/main` worktree; both report the same 176-entry `pkg-boundary` baseline, so the blocking new violation is removed.
- Files changed
  - `progress.txt`
- Verification
  - Focused CLI diagnostic, process-boundary, Worker Sessions stream, server-bind, and functional CLI tests passed.
  - `go vet ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli ./pkg/root ./cmd/factory` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` passed.
  - `git diff --check` passed; all four PRD stories remain `passes:true`.
- **Learnings for future iterations:**
  - A ratcheted gate must be evaluated against the exact current-main count after a review fix; matching the intentional baseline is the relevant result even when the standalone gate exits non-zero.
## 2026-08-10 09:13 -07:00 - sac-s0-1-ownership-inventories current-main conflict follow-up
- What was implemented
  - Merged the server's current `main` head `59fd5748` into the SAC branch after GitHub reported the existing PR as conflicting.
  - Preserved the SAC `prd.json` as the authoritative PR description and retained both branches' append-only `progress.txt` histories while resolving the bookkeeping conflicts.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main characterization sources and tests were retained by the merge.
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/pkgboundarycheck ./cmd/ownershipinventorycheck -count=1` — passed.
  - `git diff --check` and PRD JSON validation — passed.
  - Merge commit `b13db64f0` is pushed; fresh required CI and final PR mergeability are pending.
- **Learnings for future iterations:**
  - Compare the server's live base SHA with the local remote-tracking ref before treating a PR as conflict-free; stale base metadata can hide a conflict in shared `prd.json` and `progress.txt`.
---
## 2026-08-10 09:28 -07:00 - pf-cli-exit-codes current-main sync and gate repair
- What was implemented
  - Reconciled the branch with current `origin/main`, retained the CLI task PRD, and preserved the complete current-main and CLI append-only progress history.
  - Split the process exit-code case data, submit functional scenarios, and diagnostic nil/default coverage into focused test units so backend-size and pkg-maintainability gates remain green without changing observable behavior.
- Files changed
  - Current-main merge files plus `progress.txt`
  - `cmd/factory/main_test.go`
  - `tests/functional/transport/submit/submit_test.go`
  - `pkg/transports/cli/clidiag/diagnostics_test.go`
- Verification
  - CLI process, Worker Sessions stream, server-binding, CLI package, submit, current-main characterization, and short Go tests passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make typecheck` passed after `bun install --frozen-lockfile` restored ignored checkout dependencies.
  - `git diff --check` passed; `prd.json` parses with all four stories marked `passes:true`.
- **Learnings for future iterations:**
  - Maintainability gates apply to test helpers and coverage tests as well as production code; move large tables or independent assertion groups into focused helpers while retaining the same public assertions.
---
## 2026-08-10 09:45 -07:00 - sac-s0-1-ownership-inventories CI retry follow-up
- What was implemented
  - Investigated the first latest-head CI failure and confirmed that Backend Functional Coverage failed in unchanged timing-sensitive tests: two restart/history cancellation observations and the CLI writer-failure cancellation race.
  - Reran the exact restart and CLI writer tests locally 10 times each; all passed. The first failed-job retry moved to the known JavaScript concurrency observation, which passed five consecutive focused repetitions after one intermittent failure in a 20-run stress sample.
  - Reran only the failed GitHub jobs; the latest-head run completed successfully.
- Files changed
  - `progress.txt`
- Verification
  - Focused restart tests — passed 10 repetitions.
  - Focused CLI writer-failure test — passed 10 repetitions.
  - Focused JavaScript parallel-dispatch test — passed five consecutive repetitions after the isolated stress flake.
  - GitHub Actions run `31407831161` — all 18 checks passed, including Backend Functional Coverage and Verification Policy, on head `5ed5afbcf`.
- **Learnings for future iterations:**
  - For broad functional-coverage failures, inspect the first assertion; unchanged cancellation/concurrency observations can move between tests across retries. Repeat the exact test and rerun failed jobs before changing unrelated runtime code.
---
## 2026-08-10 09:45 -07:00 - pf-cli-exit-codes CI rerun follow-up
- What was implemented
  - Investigated CI run `31409217326` and isolated its only primary failure to the unchanged JavaScript parallel-concurrency functional test under coverage instrumentation.
  - Reran that test ten times locally and reran only the failed GitHub Actions jobs; no feature change was made for the non-reproducible flake.
- Files changed
  - `progress.txt`
- Verification
  - The isolated concurrency test passed `-count=10`.
  - CI run `31409217326`, attempt 2, passed Backend Functional Coverage, Verification Policy, and all other required jobs for head `ffc60b3994cf8c7a22f7bf2ba80dcb57709d833f`.
  - PR #1856 remains conflict-free with no conversation comments, reviews, or unresolved threads.
- **Learnings for future iterations:**
- A missing coverage artifact can be secondary to a single flaky functional test; inspect the named failure, rerun it in isolation, and rerun only failed jobs before modifying unrelated orchestration behavior.
---
## 2026-08-10 10:13 -07:00 - pf-cli-exit-codes review follow-up
- What was implemented
  - Removed the transport-owned `context.Background()` fallback from `clidiag.WithCentralDiagnostics`; a nil caller context remains nil and central diagnostics are enabled only when the caller supplies a context.
  - Updated the focused context-helper test to assert that no implicit context is created.
- Files changed
  - `pkg/transports/cli/clidiag/diagnostics.go`
  - `pkg/transports/cli/clidiag/diagnostics_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli/clidiag -count=1 -cover` — passed at 96.1%.
  - `go vet ./pkg/transports/cli/clidiag` — passed.
  - `make pkg-boundary` — reports the known 176-entry baseline and no new `clidiag` violation; the command remains non-zero because the baseline is intentionally tracked.
  - The blocking PR comment requires the pushed head to be compared with the exact base count before review resumes.
- **Learnings for future iterations:**
  - Transport helpers must preserve caller-owned context semantics; returning nil for a nil input is preferable to manufacturing a background context merely to satisfy `context.WithValue`.
---
## 2026-08-10 10:25 -07:00 - sac-s0-1-ownership-inventories-003 review follow-up
- What was implemented
  - Fixed the provider-effect boundary checker panic on valid grouped Go parameters such as `Infer(ctx, req context.Context)` by expanding grouped AST fields before signature inspection.
  - Added a focused regression proving the malformed grouped Workers-root compatibility port is rejected with the normal ownership diagnostic and never panics.
  - Reclassified story `sac-s0-1-ownership-inventories-003` as a `project-enabling` criterion because its architecture-guard evidence is repository-enforcement behavior rather than customer runtime/API/CLI/UI/event behavior; the remaining package-target and ownership inventory stories still need the same review-gate acceptance-shape decision.
- Files changed
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run '^TestRun(AllowsWorkersRequestScopedProviderCompatibilityPort|RejectsGroupedWorkersRequestScopedProviderPortWithoutPanic|RejectsExpandedWorkersRequestScopedProviderPort)$' -count=10` — passed.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `go vet ./cmd/pkgboundarycheck` — passed.
  - `git diff --check` and PRD JSON validation — passed.
- **Learnings for future iterations:**
  - The latest review still blocks the PR on acceptance evidence for stories 001 and 002; inventory/package-target checker tests are repository-enforcement evidence, not customer-facing behavioral coverage under the review gate. Address those stories in separate iterations by moving them to explicit project-enabling criteria or adding appropriate observable contract behavior.
---

## 2026-08-10 10:26 -07:00 - sac-s0-1-ownership-inventories-003 PR handoff follow-up
- What was implemented
  - Re-ran the package-target manifest gate sequentially after an initial parallel Go-process file-lock message; the standalone gate completed cleanly with 545 inventory rows and 545 production package rows.
  - Updated PR #1848 from the current `prd.json`, verified the description matches the file, and posted resolution evidence for the grouped-AST blocker. The thread-aware audit found no inline review threads; stories 001 and 002 remain the explicit acceptance-shape follow-up.
- Files changed
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - PR description comparison against `prd.json` — passed.
  - Worktree clean and `git diff --check` — passed.
- **Learnings for future iterations:**
  - On Windows, avoid running independent `go run` Make gates in parallel when they may contend for the generated executable; a sequential rerun distinguishes a transient file-lock message from a gate failure.
---

## 2026-08-10 10:28 -07:00 - sac-s0-1-ownership-inventories-001 review follow-up
- What was implemented
  - Reclassified `sac-s0-1-ownership-inventories-001` from a behavioral user story to an explicit `project-enabling` criterion in `prd.json`, because the package-target manifest is repository-enforcement and maintainer tooling rather than customer runtime, API, CLI, UI, or emitted-event behavior.
  - Preserved the completed package-target acceptance evidence and added an explicit no-runtime/API/CLI/UI/event-behavior criterion so the PRD matches the review gate's acceptance model.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/packagetargetmanifestcheck -run 'Test(PackageTarget|Manifest|Coverage|Owner|Edges)' -count=1` — passed.
  - PRD JSON validation reports story 001 exactly once under `enablingCriteria`, no failed items, and one remaining behavioral user story (002).
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Repository inventory and architecture-guard checks are valid enforcement evidence, but they do not satisfy a customer-behavior acceptance gate; model genuinely non-behavioral work as explicit project-enabling criteria.
  - The broad `go test ./cmd/packagetargetmanifestcheck` package also exercises current-main Workers/Recordings root-file inventories and currently reports unrelated baseline drift (`kind_phase_validation.go` and `lifecycle_capability.go`); the public package-target Make gate and focused manifest tests remain green.
  - Story 002 still needs the same acceptance-shape follow-up in a separate iteration; do not combine it with this story's bookkeeping change.
---

## 2026-08-10 10:34 -07:00 - sac-s0-1-ownership-inventories-002 review follow-up
- What was implemented
  - Reclassified `sac-s0-1-ownership-inventories-002` from a behavioral user story to an explicit `project-enabling` criterion in `prd.json`, because the ownership inventory and reciprocal-edge validation are repository-enforcement and maintainer tooling rather than customer runtime, API, CLI, UI, or emitted-event behavior.
  - Preserved the completed package, cross-owner classification, present-tense rationale, and unresolved-bidirectional-edge acceptance evidence, and added an explicit no-runtime/API/CLI/UI/event-behavior criterion.
  - Corrected the existing trailing comma so `prd.json` is valid standard JSON.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed.
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 package rows).
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - PRD validation reports valid JSON, four unique passing enabling criteria, and no behavioral user stories.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Acceptance criteria for repository-enforcement work should explicitly identify the project-level nature of the evidence and state the intentionally unchanged customer-facing behavior; keep the PRD valid JSON so automated consumers do not depend on permissive parsers.
---
## 2026-08-10 11:30 -07:00 - dwro-s1-remote-placement-flags final PR follow-up
- What was implemented
  - Merged the current `origin/main` into the feature branch, preserving the task-owned `prd.json` and append-only `progress.txt` while incorporating the current repository ownership and package-inventory updates.
  - Pushed synchronized head `a35da5197348d830682878ef609eb5381addbcf3` to PR #1852 and refreshed the PR description from `prd.json`.
  - Confirmed all four PRD stories remain `passes: true`; audited the existing blocking conversation comments against the completed fixes and found no inline review threads or newer unresolved comments.
- Files changed
  - `progress.txt`
- Verification
  - Post-merge package-target manifest, ownership inventory, package boundary, contract, functional-scenario, maintainability, file-count, backend-size, package-structure, and `go vet` gates — passed.
  - GitHub Actions run `31418719107` on head `a35da5197` — all 18 checks passed, including backend unit/functional coverage and Verification Policy.
  - PR description comparison against `prd.json` — passed.
  - Worktree clean and `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a PR head is behind current `main`, synchronize before relying on new CI; GitHub may report a dirty/diverged merge state and omit a fresh PR-head run until the merge is pushed.
  - After a coverage-sensitive parser change, moving reusable validation into an already-covered platform package and adding focused edge-case tests keeps both unit and functional coverage ratchets above their floors.
---

## 2026-08-10 11:45 -07:00 - dwro-s1-remote-placement-flags CI retry follow-up
- What was implemented
  - Audited the latest-head functional coverage failure and confirmed it was limited to two unrelated nondeterministic existing tests: ACP concurrent prompt serialization and worker stream completion-snapshot fidelity.
  - Reran the failed GitHub Actions lane without changing unrelated runtime code; the retry passed on the same head `941edb53a`.
- Files changed
  - `progress.txt`
- Verification
  - PR #1852 `gh pr checks` — all 18 checks passed on head `941edb53a`.
  - PR merge state — `clean`, base `cba6b45f`, head `941edb53a`.
- **Learnings for future iterations:**
  - When a broad functional lane fails only in pre-existing concurrency or stream-fidelity tests and the changed files are unrelated, inspect the exact assertion and rerun the failed lane before making speculative source changes.
---

## 2026-08-10 12:07 -07:00 - dwro-s1-remote-placement-flags PR blocker revalidation
- What was implemented
  - Revalidated the six historical blocking PR conversation comments against the current head; the duplicate manifest declarations, unsafe malformed-endpoint diagnostic, ratchet regressions, noncompliant listener functional setup, and marker-only docs assertions are all addressed in the existing commits.
  - Confirmed the four PRD stories remain marked `passes: true`; no product or PRD change was needed in this verification-only iteration.
- Files changed
  - `progress.txt`
- Verification
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - `make backend-size`, `make pkg-structure`, `make vet` — passed.
  - `make contracts-validate`, `make cli-manifest-check`, `make docs-reference-smoke` — passed.
  - `go test ./pkg/transports/cli/... -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/transport/run_scoped_server -count=1 -timeout 300s` — passed.
  - `make test` — passed.
  - PR #1852 head `796d36073515829297e6bf3a22d5a1e2e5a1aea2` has all 18 reported checks passing, clean mergeability, and a body identical to `prd.json`; thread-aware review data has no inline threads or formal reviews.
- **Learnings for future iterations:**
  - Treat historical top-level gate comments as head-specific evidence: compare their cited SHA with the live PR head, then re-run the named ratchets before deciding whether a blocker remains.
---

## 2026-08-10 12:22 -07:00 - dwro-s1-remote-placement-flags CI retry follow-up
- What was implemented
  - Investigated the fresh-head Backend Functional Coverage failure and isolated it to the unchanged runtime-log observation test `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`; no placement-related test or source path failed.
  - Repeated the exact test locally five times, then reran the failed GitHub Actions jobs without changing source code; the retry completed successfully.
- Files changed
  - `progress.txt`
- Verification
  - Local exact-test repetition — passed 5/5.
  - PR #1852 run `31422601284` retry — all 18 reported checks passed, including Backend Functional Coverage and Verification Policy, on head `78540b12e`.
  - PR merge state — `CLEAN`; body remains identical to `prd.json`; no inline review threads or formal reviews.
- **Learnings for future iterations:**
  - For a broad coverage failure, isolate the first failing observable test and repeat it locally before changing feature code; a clean repetition plus a successful failed-job rerun is stronger evidence of a transient race than a speculative fix.
---

## 2026-08-10 12:40 -07:00 - dwro-s1-remote-placement-flags final PR CI follow-up
- What was implemented
  - Investigated the first failed Backend Functional Coverage run and isolated the only failing assertion to the unchanged ACP boundary test `TestYouRunReturnsUnsupportedFilesystemAndTerminalRPCsAtTheACPBoundary`; no placement-related source or test path failed.
  - Repeated the exact test locally five times successfully and reran only the failed GitHub jobs without changing product code.
  - Confirmed the PR description is synchronized from `prd.json`; all four PRD stories remain marked `passes: true`.
- Files changed
  - `progress.txt`
- Verification
  - Focused ACP functional test — passed 5/5 locally.
  - PR #1852 rerun `31423838201` on head `63065f4ed9fb7ea2772048effde9f98a61bb9665` — all 18 checks passed, including Backend Functional Coverage, pinned ACP evidence, and Verification Policy.
  - PR body — exact content match with `prd.json`.
  - PR merge state — `CLEAN` and `MERGEABLE`; local and remote branch heads match; no inline review comments or formal reviews remain.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - When broad functional coverage fails in an unchanged concurrency-sensitive test, isolate and repeat the exact test before changing feature code; a clean repetition plus a failed-job rerun can establish a transient suite failure.
  - When comparing multiline GitHub CLI output in PowerShell, join captured output lines before comparing it with a file; array `.Length` reports line count rather than character count.
---

## 2026-08-10 12:53 -07:00 - dwro-s1-remote-placement-flags final handoff audit
- What was implemented
  - Re-read `prd.json` and `progress.txt` and confirmed all four S1 stories remain marked `passes: true`; no product-code change was needed in this verification-only iteration.
  - Re-ran the thread-aware PR audit for PR #1852. The historical blocking gate comments are addressed by the existing commits; there are no inline review threads, submitted reviews, or newer unresolved feedback.
  - Confirmed the open PR is clean and mergeable, all reported checks are passing, and its description is synchronized with `prd.json`.
- Files changed
  - `progress.txt`
- Verification
  - `gh auth status` — passed.
  - Bundled `fetch_comments.py` review audit — no review threads or submitted reviews.
  - `gh pr checks 1852` — all 18 reported checks passed on head `fd8b29593e79e895ad3fb176e29bea40c0391f2c`.
  - `gh pr view 1852` — PR open, `MERGEABLE`, clean merge state; body matches `prd.json`.
  - PRD JSON validation — all user stories pass.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Thread-aware review state is separate from historical top-level gate comments; only current unresolved threads should block final handoff.
  - Compare multiline PR descriptions from one parsed `gh pr view --json body` string; line-oriented PowerShell capture can make an otherwise identical body appear different.
  - When a story is already complete and the PR is fully verified, preserve the task-owned `prd.json` and append only the verification record to the progress log.
---

## 2026-08-10 13:39 -07:00 - dwro-s1-remote-placement-flags CI retry follow-up
- What was implemented
  - Investigated the fresh-head Backend Functional Coverage failures. Each failed attempt isolated a different unchanged timing-sensitive functional test; no placement-related test or source path failed.
  - Repeated every implicated exact test locally 10 times successfully, then reran only the failed GitHub jobs until the terminal attempt passed; no product source changes were needed.
- Files changed
  - `progress.txt`
- Verification
  - Exact local repetitions passed for packaged plan parallel, packaged loop, runtime log, BTRCP cancellation, session restart history, invocation output, Work recovery, and API event replay tests.
  - PR #1852 run `31426476368`, attempt 6 — Backend Functional Coverage and Verification Policy passed; all other checks remained successful.
  - PR head remains `a94927e22c2c22af888be336c6f1ef643a1d5d93`; PR is clean and mergeable.
- **Learnings for future iterations:**
  - When a broad functional lane fails on a different unchanged concurrency-sensitive test on each retry, repeat the exact tests locally and rerun only failed jobs before changing feature code.
  - `gh run rerun --failed` preserves the PR head and successful checks, making it suitable for distinguishing transient suite failures from regressions.
## 2026-08-10 10:39 -07:00 - pf-cli-exit-codes CI rerun follow-up
- What was implemented
  - Investigated the first failed Backend Functional Coverage run after the review fix; its terminal-failure output and continuous-run cancellation failures were unrelated coverage/process-environment flakes and reproduced neither as a feature regression locally.
  - Reran only failed GitHub Actions jobs twice. The first rerun moved to the known JavaScript durability cancellation-observation flake; that test passed locally 10/10. The second targeted rerun completed successfully.
- Files changed
  - `progress.txt`
- Verification
  - Focused invocation failure test passed three times locally.
  - `TestJavaScriptInterruptedSessionResumesWithoutRepeatingCompletedChildren` passed 10/10 locally.
  - CI run `31413200225` final failed-job rerun passed Backend Functional Coverage, Backend Unit Coverage, Verification Policy, and all other required checks for head `06a8d59e8`.
- **Learnings for future iterations:**
  - Broad functional coverage failures can move between existing cancellation-sensitive scenarios under instrumentation; inspect the named test, stress it locally, and rerun only failed jobs before changing unrelated runtime behavior.
---
## 2026-08-10 10:59 -07:00 - pf-cli-exit-codes-001 review and PR follow-up
- What was implemented
  - Verified that the already-pushed context-boundary fix removes the transport-owned `context.Background()` fallback and leaves nil caller contexts nil.
  - Compared the current branch with a detached `origin/main` worktree; both report the same 176-entry `pkg-boundary` baseline, so the blocking new violation is removed.
- Files changed
  - `progress.txt`
- Verification
  - Focused CLI diagnostic, process-boundary, Worker Sessions stream, server-bind, and functional CLI tests passed.
  - `go vet ./pkg/transports/cli/... ./pkg/services/worker_sessions/transports/cli ./pkg/root ./cmd/factory` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` passed.
  - `git diff --check` passed; all four PRD stories remain `passes:true`.
- **Learnings for future iterations:**
  - A ratcheted gate must be evaluated against the exact current-main count after a review fix; matching the intentional baseline is the relevant result even when the standalone gate exits non-zero.
## 2026-08-10 11:04 -07:00 - pf-cli-exit-codes current-main conflict follow-up
- What was implemented
  - Fetched the live PR base `main` at `cba6b45f9` after the cached base metadata lagged, then merged the ownership-inventory changes into the CLI-exit-codes branch.
  - Retained the CLI task `prd.json` and combined both branches' reusable patterns and append-only progress histories; no merge markers remain.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main ownership-inventory and architecture-guard files included by the merge.
- Verification
  - `prd.json` parses as the CLI-exit-codes PRD with four stories and zero failed stories.
  - `git diff --check` passed; the merge had no unresolved files after the two bookkeeping conflicts were resolved.
  - The branch now includes the current remote `main` and is ready for post-merge focused verification and CI.
- **Learnings for future iterations:**
  - GitHub's cached PR base metadata can lag the live default branch; compare `git ls-remote origin refs/heads/main` with the PR base, fetch the live ref, and resolve shared task bookkeeping before relying on mergeability.
---
## 2026-08-10 11:08 -07:00 - pf-cli-exit-codes merged-head verification follow-up
- What was implemented
  - Re-ran the CLI-exit-code regressions and repository gates after merging current `main`; no unrelated inventory drift was folded into the feature.
  - Confirmed the broad package-target test mismatch is the known current-main inventory drift for `kind_phase_validation.go` and `lifecycle_capability.go`, while the executable manifest and ownership gates remain green.
- Files changed
  - `progress.txt`
- Verification
  - CLI diagnostics, process boundary, Worker Sessions stream, server-bind, and functional CLI tests passed.
  - `go vet` for the changed CLI/root/Worker Sessions and current-main checker packages passed.
  - `make package-target-manifest-check` and `make ownership-inventory-check` passed.
  - The direct `cmd/packagetargetmanifestcheck` package test still reports the pre-existing Workers/Recordings inventory drift; no changed CLI file is implicated.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` passed.
  - `pkg-boundary` reports 175 violations on both the merged head and a detached `origin/main` worktree; the command remains non-zero for the intentional baseline.
- **Learnings for future iterations:**
  - When a repository-enforcement package test exposes current-main inventory drift but its public Make gate passes, record the distinction and avoid changing unrelated inventories in a feature PR.
---
## 2026-08-10 11:30 -07:00 - pf-cli-exit-codes merged-head CI follow-up
- What was implemented
  - Investigated the fresh-head CI failure without changing product code: attempt 1 contained two known cancellation-observation flakes, and attempt 2 rotated to the unrelated packaged-loop scheduling timeout.
  - Reran the exact named tests locally 10 times each; all repetitions passed. Reran only failed GitHub jobs until the functional lane and verification policy reached a terminal pass.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31417595679`, attempt 3, passed Backend Functional Coverage, Backend Unit Coverage, Verification Policy, and every other required job for head `2609d11ed`.
  - The failed attempt diagnostics named only unchanged tests outside the CLI-exit diff; no source fix was warranted.
  - `git diff --check` passed; all four PRD stories remain `passes:true`.
- **Learnings for future iterations:**
  - Instrumented functional coverage can rotate among existing cancellation and scheduling flakes across retries; inspect the first named assertion, repeat it locally, and rerun only failed jobs before altering unrelated runtime behavior.
---
## 2026-08-10 11:55 -07:00 - pf-cli-exit-codes-004 PR review follow-up
- What was implemented
  - Addressed the latest blocking review feedback by removing the modified `--with-mock-workers` worker success/failure scenarios from the non-workers/mock process cell.
  - Rebuilt worker success and failure coverage through `root.BuildProcess` / `Process.Execute` with `edges.Edges.ProviderCommandRunner`; the failure assertion verifies one sanitized `INVOCATION_RUNTIME_FAILURE` diagnostic and no stdout success payload.
  - Added a deterministic built-executable status matrix using `--help` and an unknown command, keeping subprocess coverage limited to the OS exit-status fact that `Process.Execute` cannot expose.
  - Merged current `origin/main` at `947152348` after the fix commit and retained the CLI task PRD and append-only progress history.
- Files changed
  - `tests/functional/transport/cli/process/exit_codes_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -count=1` — passed.
  - Focused worker success/failure and built status regressions — passed after the current-main merge.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make typecheck` — passed.
  - `git diff --check` — passed.
  - The affected package sweep passed all changed CLI/root/Worker Sessions packages; `pkg/transports/cli/commandidentity` still reports the pre-existing committed-baseline drift (`want 51822 bytes, got 52445`) and no file in that package is part of this branch diff.
- **Learnings for future iterations:**
  - Review construction rules can require moving behavioral assertions between process and executable layers; preserve worker semantics through injected edges, then use a minimal worker-free subprocess case for final OS-status coverage.
  - When a PR branch lags current main, merge the live base before final CI and keep task-scoped `prd.json` and append-only progress ownership explicit even when the merge is conflict-free.
---
## 2026-08-10 12:02 -07:00 - pf-cli-exit-codes-004 verification follow-up
- What was implemented
  - No additional feature change; completed post-merge verification against current `origin/main`.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -count=1` — passed after the current-main merge.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` and `make typecheck` — passed after the merge.
  - `make pkg-boundary` reports 175 violations on both the head and a detached `origin/main` worktree; both exit non-zero only for the intentional baseline.
  - `make test` reached one unchanged ACP stdin-cancellation race in `TestServeACPCommand_CancellationClosesStdinToUnblockRealServerMidRead`; the named test passed 10/10 in isolation, so no unrelated feature change was made.
- **Learnings for future iterations:**
  - When a broad suite names one cancellation-sensitive failure while every other lane is green, repeat that exact test before altering production behavior; an isolated pass supports recording a flake and relying on CI reruns.
---
## 2026-08-10 12:18 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated the first post-review CI failure and confirmed it was an unrelated transient failure in `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`; the expected `factory started` log entry was missing once, while the changed CLI tests were green.
  - Re-ran the failed GitHub Actions jobs without changing feature code; the rerun completed successfully.
- Files changed
  - `progress.txt`
- Verification
  - The failing runtime-api test passed five consecutive local runs.
  - CI run `31422128595` is green for Backend Functional Coverage, Verification Policy, and all other required checks; the PR is mergeable and clean.
- **Learnings for future iterations:**
  - A single missing startup log in the broad functional lane can be transient; isolate and repeat the named test before changing unrelated runtime behavior, then rerun only failed CI jobs when the local evidence is stable.
---
## 2026-08-10 12:37 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a second post-review CI failure on the latest head: `TestAgyConductorSuccessThroughRootBuildProcess` observed zero terminal result events in the broad functional lane.
  - Repeated the exact AGY test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The AGY test passed ten consecutive local runs.
  - CI run `31423514611` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - Functional coverage can expose independent timing-sensitive failures on different attempts; isolate each named test, require repeated local passes, and rerun only the failed jobs before modifying unrelated production code.
---
## 2026-08-10 12:52 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a third post-review CI failure: `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` did not observe provider context cancellation in the broad functional lane.
  - Repeated the exact durability test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The durability test passed ten consecutive local runs.
  - CI run `31425022246` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - Independent functional timing flakes can rotate across unrelated packages; preserve the source scope, isolate the named test, and require a successful failed-job rerun before handoff.
---
## 2026-08-10 13:06 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a fourth post-review CI failure: `TestAPIPartialResultIsAvailableBeforeTerminalCompletion` timed out in the sessions execution lane.
  - Repeated the exact sessions-execution test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The sessions-execution test passed ten consecutive local runs.
  - CI run `31426250442` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - Broad functional coverage timing failures can occur in several independent packages; keep the feature diff unchanged when the named test is stable locally and validate recovery through the narrow failed-job rerun.
---
## 2026-08-10 13:23 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a fifth post-review CI failure: `TestFactorySessionResumeDoesNotRepeatCompletedDispatch` timed out in the sessions restart lane.
  - Repeated the exact sessions-restart test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The sessions-restart test passed ten consecutive local runs.
  - CI run `31427408109` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - Keep repeated CI flake evidence scoped to the named test and package; a successful isolated rerun is sufficient to validate recovery when the feature diff is untouched.
## [2026-08-10 13:40 -07:00] - thr-split-fake-service-runtime-tests-001
- What was implemented
  - Captured the unmodified Factory Sessions execution baseline before any test-file moves.
  - Recorded the complete `go test -list '.*' ./pkg/services/factory_sessions/internal/execution/...` output, a repeatable `go test -covermode=atomic` package-coverage result, and a behavior-based placement map for all 120 top-level tests from the two oversized source files.
  - Recorded one planned package-local owner for shared fixture and test-helper functions so later move-only slices do not duplicate helpers.
- Files changed
  - `docs/internal/development/plans/factory-sessions-execution-test-split-baseline.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_sessions/internal/execution/...` — passed.
  - `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` — passed; coverage was 79.6% (execution), 80.0% (fixtures), 83.1% (recordingreplay), and 89.8% (runtimepersist).
  - Placement-map validation — passed; 120 declarations mapped exactly once.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The root execution package and its three test subpackages can be compared with one coverage command; retain `-covermode=atomic` and the exact `...` package scope after every mechanical move.
  - The two target files contain 120 top-level tests; destination grouping must merge fake-service and JavaScript-runtime seams without duplicating shared helpers.
---
## [2026-08-10 13:47 -07:00] - thr-split-fake-service-runtime-tests-001 verification follow-up
- What was implemented
  - Completed the repository UI dependency setup needed to run the documented typecheck; no tracked dependency files changed.
- Files changed
  - No additional source files.
- Verification
  - `make ui-deps` — passed with the pinned Bun workspace lockfile.
  - `make typecheck` — passed.
  - `git diff --check` — passed after removing an extra artifact EOF blank line.
- **Learnings for future iterations:**
- A fresh worktree may have an incomplete `ui/node_modules`; run `make ui-deps` before treating missing `@types/bun` from `make typecheck` as a source failure.
---
## 2026-08-10 13:48 -07:00 - thr-split-cli-root-run-tests-001
- What was implemented
  - Mechanically split `pkg/transports/cli/root_run_test.go` into focused completion, named-factory selection, root composition, input/presentation, run flags, run configuration, and operator-default test files, with shared package-local fixtures in `root_run_test_helpers_test.go`.
  - Preserved all 273 discovered CLI root-package test names and all nonblank test/helper bodies; only declaration placement and per-file imports changed.
  - Removed the obsolete root-file backend-size and maintainability exemptions and updated the intentional `pkg/transports/cli` package file-count baseline from 23 to 30.
  - Marked `thr-split-cli-root-run-tests-001` as passed in `prd.json`; invocation story 002 remains incomplete for the next iteration.
- Files changed
  - `pkg/transports/cli/root_run_test.go` (split/deleted)
  - `pkg/transports/cli/root_command_composition_test.go`
  - `pkg/transports/cli/root_operator_defaults_test.go`
  - `pkg/transports/cli/root_run_completion_test.go`
  - `pkg/transports/cli/root_run_config_test.go`
  - `pkg/transports/cli/root_run_flags_test.go`
  - `pkg/transports/cli/root_run_input_test.go`
  - `pkg/transports/cli/root_run_selection_test.go`
  - `pkg/transports/cli/root_run_test_helpers_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Before/after normalized `go test ./pkg/transports/cli -run '^$' -list '.*'` inventory — 273 names on both sides; diff empty.
  - Before/after `./pkg/transports/cli` coverage — 67.6% on both sides.
  - `go test ./pkg/transports/cli -count=1`, `go vet ./pkg/transports/cli`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - `git diff --check` — passed.
  - `make test` exceeded the 180-second tool ceiling without a failure result; the focused package suite and all changed-surface gates passed.
  - Commit `15ca23ae1` — `test(cli): split root run tests`.
- **Learnings for future iterations:**
  - Keep shared root CLI fixtures in one helper file, but leave behavior-specific assertion helpers beside their owning test group when the move is mechanical.
  - A same-package test split increases the package file count even when the total source body is unchanged; update the package baseline and remove stale file exemptions in the same change.
  - The remaining `pkg/transports/cli/run/run_invocation_test.go` split is independent and should be handled as story 002 without mixing its inventory or coverage evidence into this iteration.
---
## 2026-08-10 14:17 -07:00 - thr-split-cli-root-run-tests-002
- What was implemented
  - Mechanically split `pkg/transports/cli/run/run_invocation_test.go` into lifecycle/bootstrap, input resolution, output/error, CLI/API parity, and no-server integration test files, with shared invocation fixtures in `run_invocation_test_helpers_test.go`.
  - Preserved the package discovery inventory and moved the existing assertions, inputs, expected outputs, fixture semantics, and CLI outcomes without semantic changes.
  - Removed the obsolete run-invocation backend-size and maintainability exemptions and updated the intentional `pkg/transports/cli/run` package file-count baseline from 27 to 32.
  - Marked `thr-split-cli-root-run-tests-002` as passed in `prd.json`; all PRD stories are now complete.
- Files changed
  - `pkg/transports/cli/run/run_invocation_test.go` (split/deleted)
  - `pkg/transports/cli/run/run_invocation_input_test.go`
  - `pkg/transports/cli/run/run_invocation_lifecycle_test.go`
  - `pkg/transports/cli/run/run_invocation_no_server_test.go`
  - `pkg/transports/cli/run/run_invocation_output_test.go`
  - `pkg/transports/cli/run/run_invocation_parity_test.go`
  - `pkg/transports/cli/run/run_invocation_test_helpers_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Isolated before/after `go test ./pkg/transports/cli/run -count=1 -run '^$' -list '.*'` inventories — 146 names on both sides, SHA-256 `8A65F4CD3D63F0384D960E239E4EC434A2AD1F74E92ED8CBC8389B6C33C84E15` on both sides, normalized diff empty.
  - Isolated before/after full package coverage — 72.1% on both sides.
  - `go test ./pkg/transports/cli/run -count=1 -timeout 180s`, focused typecheck via `go test ... -run '^$'`, `go vet ./pkg/transports/cli/run`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - Largest resulting split test file is 513 lines; `git diff --check` passed.
- **Learnings for future iterations:**
  - Keep no-server bootstrap fixtures and their integration assertions together because they share the capturing runner, mock-worker configuration, and API-projection helpers; keep metric recorder helpers with input-resolution observability tests.
  - A full package-level inventory includes tests from every same-package file; compare isolated `HEAD` and current worktrees rather than only the moved file’s declarations.
---
## [2026-08-10 14:18 -07:00] - thr-split-fake-service-runtime-tests-002
- What was implemented
  - Moved the fixture-backed fake-service construction, synchronous/asynchronous start, lifecycle-control, read, listing, event-cursor, result, artifact, and projection-validation tests into behavior-focused package-local files without changing test bodies or assertions.
  - Centralized shared fixture, clock, event-envelope, JSON, and pointer helpers in `execution_test_helpers_test.go`; moved the story-owned internal fake-service start/lifecycle characterization seams with their helper functions.
  - Relocated the existing `pkgmaintcheck` exemption target for the moved lifecycle test and raised the execution package file-count baseline from 28 to 31 for the three focused files.
- Files changed
  - `pkg/services/factory_sessions/internal/execution/execution_test_helpers_test.go`
  - `pkg/services/factory_sessions/internal/execution/fake_service_construction_start_test.go`
  - `pkg/services/factory_sessions/internal/execution/fake_service_lifecycle_inspection_test.go`
  - `pkg/services/factory_sessions/internal/execution/fake_service_test.go`
  - `pkg/services/factory_sessions/internal/execution/fake_service_runtime_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_sessions/internal/execution/...` — passed.
  - `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` — passed with unchanged coverage: 79.6%, 80.0%, 83.1%, and 89.8% for the four packages.
  - Baseline/current `go test -list '.*'` comparison — 267 entries versus 267 entries; no added, removed, or renamed entries.
  - `go vet ./pkg/services/factory_sessions/internal/execution/...` — passed.
  - `make typecheck`, `make pkg-maint`, `make backend-size`, and `make pkg-file-count` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A phased test split must move exemption-budget ownership with the test; keep the old consolidated-file suppressions until later stories remove the remaining legacy seams.
  - The focused files are below the 1,200-line target; `fake_service_test.go` and `fake_service_runtime_test.go` intentionally remain as transitional owners for the replay/dispatch and JavaScript-runtime stories.
---
## [2026-08-10 14:27 -07:00] - thr-split-fake-service-runtime-tests-003
- What was implemented
  - Moved the remaining 24 tests from `fake_service_test.go` into behavior-focused package-local files: 15 replay/canonical projection tests in `fake_service_replay_test.go` and 9 interruption/dispatch-control tests in `fake_service_dispatch_test.go`.
  - Retired `fake_service_test.go` after every original declaration had exactly one destination; preserved test bodies, assertions, fixtures, event ordering, and observable outcomes without adding or removing coverage.
  - Removed the retired file's file-level maintainability exemptions, relocated its three function-level exemption targets to the focused owners, and raised the execution package file-count ratchet from 31 to 32.
- Files changed
  - `pkg/services/factory_sessions/internal/execution/fake_service_replay_test.go`
  - `pkg/services/factory_sessions/internal/execution/fake_service_dispatch_test.go`
  - `pkg/services/factory_sessions/internal/execution/fake_service_test.go` (retired)
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_sessions/internal/execution/... -count=1` — passed.
  - `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` — passed with unchanged coverage: 79.6%, 80.0%, 83.1%, and 89.8% for the four packages.
  - Baseline/current `go test -list '.*'` comparison — 269 entries versus 269 entries; no added, removed, or renamed test or example entry.
  - `go vet ./pkg/services/factory_sessions/internal/execution/...` — passed.
  - New focused files are 798 and 586 lines; `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a staged test split retires a consolidated file, remove only that file's exemptions, move function-level exemption targets with their declarations, and keep the remaining staged source exemptions until their own stories retire them.
  - The exact baseline artifact contains 269 list entries even though the earlier story note summarized 267; compare the authoritative command output directly and record the command-derived count.
---
## [2026-08-10 14:47 -07:00] - thr-split-fake-service-runtime-tests-004
- What was implemented
  - Moved the JavaScript runtime start, result, persistence, normalization, source-resolution, policy-validation, and idempotency tests into behavior-focused package-local files without changing their declarations, bodies, assertions, fixtures, or outcomes.
  - Centralized shared runtime construction, workflow, clock, recording, and orchestration-adapter helpers in `javascript_runtime_test_helpers_test.go`; kept persistence-only helpers with `javascript_runtime_persistence_test.go`.
  - Raised the execution package file-count ratchet from 32 to 37 for the five new focused files; the legacy runtime file remains the staged owner for story 005.
- Files changed
  - `pkg/services/factory_sessions/internal/execution/fake_service_runtime_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_test_helpers_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_result_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_start_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_persistence_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_validation_test.go`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_sessions/internal/execution/...` — passed.
  - `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` — passed with unchanged coverage: 79.6%, 80.0%, 83.1%, and 89.8%.
  - Baseline/current `go test -list '.*'` comparison — 269 entries versus 269 entries; no differences.
  - 106 moved top-level declarations compared with the pre-split source — zero body or assertion differences.
  - `go vet ./pkg/services/factory_sessions/internal/execution/...`, `make pkg-maint`, `make backend-size`, `make pkg-file-count`, `make typecheck`, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - The authoritative placement map in the baseline artifact can drive a mechanical split while keeping helper ownership explicit; use declaration-level comparisons plus test inventory and coverage checks to detect accidental body changes.
- Keep the legacy file-level suppressions until the final staged seam retires that source file; per-test exemptions should move only when their owning test moves.
---
## [2026-08-10 15:08 -07:00] - thr-split-fake-service-runtime-tests-005
- What was implemented
  - Moved the remaining runtime lifecycle-control, interruption, checkpoint, resume, phase-event, observer, append-only-history, recording, and workflow-policy tests from `fake_service_runtime_test.go` into `javascript_runtime_control_test.go`, `javascript_runtime_checkpoint_test.go`, and `javascript_runtime_event_recording_test.go`.
  - Moved `TestFakeService_InterruptAcceptedBeforeCompletion_ObservableDispatchAndEventOutcomes` into `fake_service_dispatch_test.go`; shared event/dispatch lookup helpers have one owner in `javascript_runtime_event_recording_test.go`.
  - Retired `fake_service_runtime_test.go`, removed its file-level exemptions, relocated the remaining function exemptions, and raised the execution package file-count baseline from 37 to 39.
- Files changed
  - `pkg/services/factory_sessions/internal/execution/fake_service_dispatch_test.go`
  - `pkg/services/factory_sessions/internal/execution/fake_service_runtime_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_control_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_checkpoint_test.go`
  - `pkg/services/factory_sessions/internal/execution/javascript_runtime_event_recording_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_sessions/internal/execution/...` — passed.
  - `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` — passed with unchanged coverage: 79.6%, 80.0%, 83.1%, and 89.8%.
  - Baseline/current `go test -list '.*' ./pkg/services/factory_sessions/internal/execution/...` comparison — 269 entries versus 269 entries; the sorted multiset difference is empty, with no added, removed, or renamed test/example entry.
  - Declaration comparison — all 47 functions/methods from the retired runtime file are present at exactly one destination with identical bodies ignoring formatting-only line boundaries; no assertion or fixture changes.
  - `go vet ./pkg/services/factory_sessions/internal/execution/...`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, `make typecheck`, and `git diff --check` — passed.
  - `make lint` — UI lint/deadcode, vet, backend-size, pkg-maint, and pkg-file-count passed; the aggregate target stopped at the pre-existing `make pkg-boundary` baseline violations in unrelated factory-definition, runtime, transport, and functional-test files (175 violations), with no touched-file violation reported.
- **Learnings for future iterations:**
  - Use the story 001 placement map with comment-aware declaration boundaries; a naive split can duplicate directives at the next declaration even when the tests still compile.
  - When a retired file-level exemption exposed two long focused tests, the correct mechanical bookkeeping was to move the exemptions to those exact functions and keep their source directives synchronized with the sorted budget manifest.
  - The full lint aggregate currently remains blocked by unrelated repository-wide `pkg-boundary` debt; the story-scoped gates are green and should be reported separately from that baseline failure.
---
## [2026-08-10 15:15 -07:00] - thr-split-fake-service-runtime-tests-005 merge verification
- What was implemented
  - Merged `upstream/main` at `78584efb5` into the task branch and resolved the shared task-management files by retaining this PRD while combining both branches' append-only progress history and codebase patterns.
- Files changed
  - Merge result includes current-main CLI changes; task-owned `prd.json` and `progress.txt` retain the five passing Factory Sessions split stories and all merged progress history.
- Verification
  - Final head `7025761a4` is based on current `upstream/main` with a clean worktree.
  - Focused execution suite, atomic coverage, vet, backend-size, pkg-maint, pkg-file-count, pkg-structure, and typecheck all pass.
  - Final inventory comparison remains 269 baseline entries versus 269 current entries with an empty multiset difference.
- **Learnings for future iterations:**
  - Synchronizing a task branch with a newer main can conflict only in shared `prd.json` and `progress.txt`; keep the active PRD authoritative and union append-only progress sections so unrelated main history is not lost.
---

## 2026-08-09 13:16 - wo-d1-packaged-factories-docs-001 verification follow-up
- What was implemented
  - Installed the repository-pinned UI dependencies that were missing from the checkout so the broad verification tier could run.
- Files changed
  - No additional tracked files.
- Verification
  - Initial `make verify-fast` stopped at the environment dependency boundary (`@types/bun` missing and the globally selected TypeScript reported `TS5101`).
  - After `bun install --frozen-lockfile` in `ui/`, `make verify-fast` passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, and the short Go suite.
- **Learnings for future iterations:**
- This checkout may have `ui/node_modules` without all lockfile dependencies; run the pinned Bun install before treating a typecheck failure as a source regression.
---

## 2026-08-09 13:36 - wo-d1-packaged-factories-docs-002
- What was implemented
  - Added full bounded/iterative operator entries for `@you/goal`, `@you/loop`, `@you/fusion`, `@you/classify`, and `@you/review` to `docs/reference/packaged-factories.md`.
  - Documented each current live invocation signature, all worker roles, role-specific provider/model overrides, operator-default fallback, prerequisites, workspace/session/recording side effects, stopping/routing/retry behavior, and final versus intermediate output shape.
  - Added `docs/examples/packaged-classify-mock-workers.json` and indexed it from `docs/examples/README.md` so the classify example can deterministically prove a valid `small` route without a live provider call.
  - Executed every worked command exactly as shown against a freshly built current-source `bin/you.exe` with isolated `HOME`/`USERPROFILE` state. `goal`, `loop`, `fusion`, and `review` returned `mock worker accepted`; `classify` routed through `small` and returned `mock worker accepted`.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `docs/examples/README.md`
  - `docs/examples/packaged-classify-mock-workers.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The live packaged catalog is lazily materialized and can be stale in an operator profile; use isolated home variables when collecting current help evidence.
  - `--with-mock-workers` is enough for most packaged examples, but classifier workflows need a protocol-valid scripted provider response for the exact route label before the selected lane can run.
  - Keep deterministic documentation fixtures under `docs/examples/` and explain their role so smoke output is not mistaken for a live model guarantee.
---

## 2026-08-09 14:04 - wo-d1-packaged-factories-docs-003
- What was implemented
  - Added distinct operator-ready entries for `@you/factory-builder`, `@you/plan-execute`, `@you/plan-parallel`, and `@you/full-flow` in `docs/reference/packaged-factories.md`.
  - Documented each live help signature, all worker roles and provider/model overrides, relative workflow choice, workspace and Git side effects, concurrency and retry bounds, validation/installation, local merge/replanning, and final versus intermediate output behavior.
  - Added exact current-binary smoke evidence: the builder returned `mock worker accepted`; plan-execute returned `mock worker accepted` and `<COMPLETE>`; plan-parallel returned `mock worker accepted`; full-flow's controlled invalid-bound check returned the documented `maxCycles` choice-validation error.
  - Marked Story 003 as passing in `prd.json` and recorded the generic-mock limitation so protocol-valid planner responses are not confused with end-to-end orchestration evidence.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `go test ./tests/functional/factory/packaged/factory_builder ./tests/functional/factory/packaged/plan_execute ./tests/functional/factory/packaged/plan_parallel ./tests/functional/factory/packaged/full_flow -count=1 -timeout 300s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The planning entries are not interchangeable: builder installs a new Factory, plan-execute stays sequential in the current workspace, plan-parallel shares one repository across a bounded DAG, and full-flow owns worktrees, local merges, and replanning.
  - Generic mock workers accept a planner dispatch without producing its required `FACTORY_REQUEST_BATCH`; use protocol-valid fixtures or provider-backed functional tests when verifying generated Work, concurrency, or cycle control.
  - Keep full-flow CLI smoke examples bounded and terminating; use controlled argument validation for the public command example and the isolated functional test for actual worktree/merge behavior.
---

## 2026-08-09 15:51 - wo-d1-packaged-factories-docs-004
- What was implemented
  - Added distinct operator-ready entries for `@you/deep-research`, `@you/spawn`, `@you/quorum`, and `@you/tournament` in `docs/reference/packaged-factories.md` using the shared seven-part entry contract.
  - Documented isolated current-binary invocation signatures, all worker roles and provider/model override flags, operator-default fallback, JavaScript policy bounds, quorum Graph Work and merge behavior, task-count/round bounds, side effects, and primary versus intermediate result shapes.
  - Added exact worked command evidence: deep-research completed in structured JSON with two specialist statuses, quorum returned `mock worker accepted`, spawn rejected an…26759 tokens truncated…erCompatibilityPort|RejectsGroupedWorkersRequestScopedProviderPortWithoutPanic|RejectsExpandedWorkersRequestScopedProviderPort)$' -count=10` — passed.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `go vet ./cmd/pkgboundarycheck` — passed.
  - `git diff --check` and PRD JSON validation — passed.
- **Learnings for future iterations:**
  - The latest review still blocks the PR on acceptance evidence for stories 001 and 002; inventory/package-target checker tests are repository-enforcement evidence, not customer-facing behavioral coverage under the review gate. Address those stories in separate iterations by moving them to explicit project-enabling criteria or adding appropriate observable contract behavior.
---

## 2026-08-10 10:26 -07:00 - sac-s0-1-ownership-inventories-003 PR handoff follow-up
- What was implemented
  - Re-ran the package-target manifest gate sequentially after an initial parallel Go-process file-lock message; the standalone gate completed cleanly with 545 inventory rows and 545 production package rows.
  - Updated PR #1848 from the current `prd.json`, verified the description matches the file, and posted resolution evidence for the grouped-AST blocker. The thread-aware audit found no inline review threads; stories 001 and 002 remain the explicit acceptance-shape follow-up.
- Files changed
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - PR description comparison against `prd.json` — passed.
  - Worktree clean and `git diff --check` — passed.
- **Learnings for future iterations:**
  - On Windows, avoid running independent `go run` Make gates in parallel when they may contend for the generated executable; a sequential rerun distinguishes a transient file-lock message from a gate failure.
---

## 2026-08-10 10:28 -07:00 - sac-s0-1-ownership-inventories-001 review follow-up
- What was implemented
  - Reclassified `sac-s0-1-ownership-inventories-001` from a behavioral user story to an explicit `project-enabling` criterion in `prd.json`, because the package-target manifest is repository-enforcement and maintainer tooling rather than customer runtime, API, CLI, UI, or emitted-event behavior.
  - Preserved the completed package-target acceptance evidence and added an explicit no-runtime/API/CLI/UI/event-behavior criterion so the PRD matches the review gate's acceptance model.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 production package rows).
  - `make ownership-inventory-check` — passed.
  - `go test ./cmd/packagetargetmanifestcheck -run 'Test(PackageTarget|Manifest|Coverage|Owner|Edges)' -count=1` — passed.
  - PRD JSON validation reports story 001 exactly once under `enablingCriteria`, no failed items, and one remaining behavioral user story (002).
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Repository inventory and architecture-guard checks are valid enforcement evidence, but they do not satisfy a customer-behavior acceptance gate; model genuinely non-behavioral work as explicit project-enabling criteria.
  - The broad `go test ./cmd/packagetargetmanifestcheck` package also exercises current-main Workers/Recordings root-file inventories and currently reports unrelated baseline drift (`kind_phase_validation.go` and `lifecycle_capability.go`); the public package-target Make gate and focused manifest tests remain green.
  - Story 002 still needs the same acceptance-shape follow-up in a separate iteration; do not combine it with this story's bookkeeping change.
---

## 2026-08-10 10:34 -07:00 - sac-s0-1-ownership-inventories-002 review follow-up
- What was implemented
  - Reclassified `sac-s0-1-ownership-inventories-002` from a behavioral user story to an explicit `project-enabling` criterion in `prd.json`, because the ownership inventory and reciprocal-edge validation are repository-enforcement and maintainer tooling rather than customer runtime, API, CLI, UI, or emitted-event behavior.
  - Preserved the completed package, cross-owner classification, present-tense rationale, and unresolved-bidirectional-edge acceptance evidence, and added an explicit no-runtime/API/CLI/UI/event-behavior criterion.
  - Corrected the existing trailing comma so `prd.json` is valid standard JSON.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make ownership-inventory-check` — passed.
  - `make package-target-manifest-check` — passed (545 inventory rows / 545 package rows).
  - `go test ./cmd/ownershipinventorycheck -count=1` — passed.
  - PRD validation reports valid JSON, four unique passing enabling criteria, and no behavioral user stories.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Acceptance criteria for repository-enforcement work should explicitly identify the project-level nature of the evidence and state the intentionally unchanged customer-facing behavior; keep the PRD valid JSON so automated consumers do not depend on permissive parsers.
---

## 2026-08-10 11:23 -07:00 - pf-stale-scope-diagnostic final PR audit
## 2026-08-10 11:08 -07:00 - pf-cli-exit-codes merged-head verification follow-up
- What was implemented
  - Re-ran the CLI-exit-code regressions and repository gates after merging current `main`; no unrelated inventory drift was folded into the feature.
  - Confirmed the broad package-target test mismatch is the known current-main inventory drift for `kind_phase_validation.go` and `lifecycle_capability.go`, while the executable manifest and ownership gates remain green.
- Files changed
  - `progress.txt`
- Verification
  - CLI diagnostics, process boundary, Worker Sessions stream, server-bind, and functional CLI tests passed.
  - `go vet` for the changed CLI/root/Worker Sessions and current-main checker packages passed.
  - `make package-target-manifest-check` and `make ownership-inventory-check` passed.
  - The direct `cmd/packagetargetmanifestcheck` package test still reports the pre-existing Workers/Recordings inventory drift; no changed CLI file is implicated.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` passed.
  - `pkg-boundary` reports 175 violations on both the merged head and a detached `origin/main` worktree; the command remains non-zero for the intentional baseline.
- **Learnings for future iterations:**
  - When a repository-enforcement package test exposes current-main inventory drift but its public Make gate passes, record the distinction and avoid changing unrelated inventories in a feature PR.
---
## 2026-08-10 11:30 -07:00 - pf-cli-exit-codes merged-head CI follow-up
- What was implemented
  - Investigated the fresh-head CI failure without changing product code: attempt 1 contained two known cancellation-observation flakes, and attempt 2 rotated to the unrelated packaged-loop scheduling timeout.
  - Reran the exact named tests locally 10 times each; all repetitions passed. Reran only failed GitHub jobs until the functional lane and verification policy reached a terminal pass.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31417595679`, attempt 3, passed Backend Functional Coverage, Backend Unit Coverage, Verification Policy, and every other required job for head `2609d11ed`.
  - The failed attempt diagnostics named only unchanged tests outside the CLI-exit diff; no source fix was warranted.
  - `git diff --check` passed; all four PRD stories remain `passes:true`.
- **Learnings for future iterations:**
  - Instrumented functional coverage can rotate among existing cancellation and scheduling flakes across retries; inspect the first named assertion, repeat it locally, and rerun only failed jobs before altering unrelated runtime behavior.
---
## 2026-08-10 11:04 -07:00 - pf-cli-exit-codes current-main conflict follow-up
- What was implemented
  - Fetched the live PR base `main` at `cba6b45f9` after the cached base metadata lagged, then merged the ownership-inventory changes into the CLI-exit-codes branch.
  - Retained the CLI task `prd.json` and combined both branches' reusable patterns and append-only progress histories; no merge markers remain.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main ownership-inventory and architecture-guard files included by the merge.
- Verification
  - `prd.json` parses as the CLI-exit-codes PRD with four stories and zero failed stories.
  - `git diff --check` passed; the merge had no unresolved files after the two bookkeeping conflicts were resolved.
  - The branch now includes the current remote `main` and is ready for post-merge focused verification and CI.
- **Learnings for future iterations:**
  - GitHub's cached PR base metadata can lag the live default branch; compare `git ls-remote origin refs/heads/main` with the PR base, fetch the live ref, and resolve shared task bookkeeping before relying on mergeability.
---

## 2026-08-10 12:05 -07:00 - pf-stale-scope-diagnostic current-main synchronization
## 2026-08-10 11:55 -07:00 - pf-cli-exit-codes-004 PR review follow-up
- What was implemented
  - Addressed the latest blocking review feedback by removing the modified `--with-mock-workers` worker success/failure scenarios from the non-workers/mock process cell.
  - Rebuilt worker success and failure coverage through `root.BuildProcess` / `Process.Execute` with `edges.Edges.ProviderCommandRunner`; the failure assertion verifies one sanitized `INVOCATION_RUNTIME_FAILURE` diagnostic and no stdout success payload.
  - Added a deterministic built-executable status matrix using `--help` and an unknown command, keeping subprocess coverage limited to the OS exit-status fact that `Process.Execute` cannot expose.
  - Merged current `origin/main` at `947152348` after the fix commit and retained the CLI task PRD and append-only progress history.
- Files changed
  - `tests/functional/transport/cli/process/exit_codes_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -count=1` — passed.
  - Focused worker success/failure and built status regressions — passed after the current-main merge.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make pkg-boundary` reports 175 findings on both the merged head and isolated current `origin/main`; no new boundary finding was introduced by this task.
  - `prd.json` parses with both stories `passes:true`; the thread-aware PR audit reports 0 reviews and 0 inline review threads.
- **Learnings for future iterations:**
  - Run repository-wide scanners from the explicitly intended checkout when comparing worktree baselines; a nested temporary worktree can otherwise be scanned as repository content.
---

## 2026-08-10 12:36 -07:00 - pf-stale-scope-diagnostic final handoff audit
- What was implemented
  - Revalidated the completed PRD against the existing PR #1853 after the latest current-main synchronization; no story implementation or PRD change was needed.
  - Confirmed the prior blocking feedback is covered by the follow-up commits: structured packaged-installation diagnostics, injected backend-scope identity, checker-compliant exclusive reservation, removal of `--with-mock-workers` from the OS-boundary regression, justified filesystem observations, and restoration of unrelated generated churn.
- Files changed
  - `progress.txt`
- Verification
  - Both `pf-stale-scope-diagnostic-001` and `pf-stale-scope-diagnostic-002` remain `passes:true`.
  - PRD JSON and PR body match byte-for-byte; local `HEAD` matches PR #1853 head `49cb1daf25295c02c1856e3c984602b6fba40e8d`.
  - Thread-aware GraphQL audit: 3 conversation comments, 0 reviews, and 0 inline review threads.
  - All 18 PR checks are terminal and successful; PR #1853 is open, mergeable, and clean.
  - `make typecheck` — passed.
  - `git diff --check` — passed.
  - The affected package sweep passed all changed CLI/root/Worker Sessions packages; `pkg/transports/cli/commandidentity` still reports the pre-existing committed-baseline drift (`want 51822 bytes, got 52445`) and no file in that package is part of this branch diff.
- **Learnings for future iterations:**
  - Review construction rules can require moving behavioral assertions between process and executable layers; preserve worker semantics through injected edges, then use a minimal worker-free subprocess case for final OS-status coverage.
  - When a PR branch lags current main, merge the live base before final CI and keep task-scoped `prd.json` and append-only progress ownership explicit even when the merge is conflict-free.
---

## 2026-08-10 12:02 -07:00 - pf-cli-exit-codes-004 verification follow-up
- What was implemented
  - No additional feature change; completed post-merge verification against current `origin/main`.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -count=1` — passed after the current-main merge.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` and `make typecheck` — passed after the merge.
  - `make pkg-boundary` reports 175 violations on both the head and a detached `origin/main` worktree; both exit non-zero only for the intentional baseline.
  - `make test` reached one unchanged ACP stdin-cancellation race in `TestServeACPCommand_CancellationClosesStdinToUnblockRealServerMidRead`; the named test passed 10/10 in isolation, so no unrelated feature change was made.
- **Learnings for future iterations:**
  - When a broad suite names one cancellation-sensitive failure while every other lane is green, repeat that exact test before altering production behavior; an isolated pass supports recording a flake and relying on CI reruns.
---

## 2026-08-10 12:18 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated the first post-review CI failure and confirmed it was an unrelated transient failure in `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`; the expected `factory started` log entry was missing once, while the changed CLI tests were green.
  - Re-ran the failed GitHub Actions jobs without changing feature code; the rerun completed successfully.
- Files changed
  - `progress.txt`
- Verification
  - The failing runtime-api test passed five consecutive local runs.
  - CI run `31422128595` is green for Backend Functional Coverage, Verification Policy, and all other required checks; the PR is mergeable and clean.
- **Learnings for future iterations:**
  - A single missing startup log in the broad functional lane can be transient; isolate and repeat the named test before changing unrelated runtime behavior, then rerun only failed CI jobs when the local evidence is stable.
---

## 2026-08-10 12:37 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a second post-review CI failure on the latest head: `TestAgyConductorSuccessThroughRootBuildProcess` observed zero terminal result events in the broad functional lane.
  - Repeated the exact AGY test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The AGY test passed ten consecutive local runs.
  - CI run `31423514611` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - Functional coverage can expose independent timing-sensitive failures on different attempts; isolate each named test, require repeated local passes, and rerun only the failed jobs before modifying unrelated production code.
---

## 2026-08-10 12:52 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a third post-review CI failure: `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` did not observe provider context cancellation in the broad functional lane.
  - Repeated the exact durability test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The durability test passed ten consecutive local runs.
  - CI run `31425022246` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - Independent functional timing flakes can rotate across unrelated packages; preserve the source scope, isolate the named test, and require a successful failed-job rerun before handoff.
---

## 2026-08-10 13:06 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a fourth post-review CI failure: `TestAPIPartialResultIsAvailableBeforeTerminalCompletion` timed out in the sessions execution lane.
  - Repeated the exact sessions-execution test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The sessions-execution test passed ten consecutive local runs.
  - CI run `31426250442` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - Broad functional coverage timing failures can occur in several independent packages; keep the feature diff unchanged when the named test is stable locally and validate recovery through the narrow failed-job rerun.
---

## 2026-08-10 13:23 -07:00 - pf-cli-exit-codes-004 CI follow-up
- What was implemented
  - Investigated a fifth post-review CI failure: `TestFactorySessionResumeDoesNotRepeatCompletedDispatch` timed out in the sessions restart lane.
  - Repeated the exact sessions-restart test ten times locally and reran only the failed functional/policy jobs; both completed successfully without source changes.
- Files changed
  - `progress.txt`
- Verification
  - The sessions-restart test passed ten consecutive local runs.
  - CI run `31427408109` is green after the failed-job rerun, including Backend Functional Coverage and Verification Policy.
 - **Learnings for future iterations:**
   - Keep repeated CI flake evidence scoped to the named test and package; a successful isolated rerun is sufficient to validate recovery when the feature diff is untouched.
---
## 2026-08-10 13:41 -07:00 - thr-deflake-js-durability-001
- What was implemented
  - Reproduced the named CI evidence from Backend Functional Coverage attempt 2 of run 31426292402, job 93582274257: `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` failed at `resume_test.go:208` because the provider did not observe the canceled workflow context.
  - Identified the competing operations: the runtime publishes dispatch `RUNNING` before the worker necessarily enters the injected provider `Infer` edge; the test interrupted immediately after observing `RUNNING`, so cancellation could occur before the blocking provider call began.
  - Replaced the durability provider's counter polling and timing gap with deterministic `inferStarted` and `contextCanceled` channel signals. The interruption helper now waits for provider entry before issuing interrupt-dispatch and waits for provider cancellation through a bounded failure timer select.
- Files changed
  - `tests/functional/orchestration/javascript/durability/resume_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Baseline `go test ./tests/functional/orchestration/javascript/durability -run '^TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault$' -count=20 -v -timeout 900s` — 20/20 passed.
  - Baseline three-sibling durability run with `-count=20` — passed.
  - Post-change focused no-snapshot run with `-count=5` — passed.
  - Post-change three-sibling focused run — passed.
  - `go test -race ./tests/functional/orchestration/javascript/durability -count=1 -v -timeout 900s` — passed.
  - `go vet ./tests/functional/orchestration/javascript/durability` and `git diff --check` — passed.
- **Learnings for future iterations:**
  - A public dispatch `RUNNING` projection is not proof that an external provider call has started; cancellation tests must synchronize on the injected edge's entry signal before issuing lifecycle control.
  - Channel-based edge observations remove both the pre-entry race and the test's counter-polling sleep without changing product durability behavior. Story 002 still needed the no-snapshot filesystem boundary, and story 003 still owned fresh-run and sibling-scope proof.
---
## 2026-08-10 14:14 -07:00 - thr-deflake-js-durability-002
- What was implemented
  - Made `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` wait for the injected blocking provider's `Infer` call to return after cancellation, closing the gap where the public `INTERRUPTED` projection could be observed before the external edge completed.
  - Kept the public Factory Session assertions for in-memory `INTERRUPTED` status and `interruptedAt` lifecycle data, and expanded the negative durability proof to reject both the project-local durable-session snapshot and its parent directory.
  - Confirmed no product durability change is needed: default execution remains in-memory-only, and the correction stays in the JavaScript durability functional test.
- Files changed
  - `tests/functional/orchestration/javascript/durability/resume_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test -p=1 -vet=off ./tests/functional/orchestration/javascript/durability -run '^TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault$' -count=1 -v -timeout 180s` — passed.
  - The same focused test with `-count=5 -v` — five consecutive passes.
  - All three JavaScript durability resume scenarios with `-count=1 -v` — passed.
  - `go vet ./tests/functional/orchestration/javascript/durability` — passed.
  - `make ui-deps` followed by `make typecheck` — passed; dependency setup changed no tracked lockfile.
  - `git diff --check` and `prd.json` JSON validation — passed.
- **Learnings for future iterations:**
  - A cancellation-observed signal is not the same as an external provider call's completion; negative persistence assertions should wait for the injected edge to return before reading the filesystem.
  - When the local UI dependency tree is incomplete, `make ui-deps` restores the pinned Bun workspace without changing tracked files.
  - Story 003 owns the 20 fresh package runs, race-enabled package run, and sibling-scope proof.
---
## 2026-08-10 14:21 -07:00 - thr-deflake-js-durability-003
- What was implemented
  - Completed the sibling-scope review: the durability package contains exactly three scenarios, all using the shared blocking-provider fixture and the same confirmed dispatch-`RUNNING`/provider-entry race. The existing correction covers each sibling through provider entry, cancellation, and completion channel observations while preserving the original public lifecycle, dispatch, checkpoint, result, and no-snapshot assertions.
  - No additional sibling with a different race mechanism was changed, and no product durability code, sleep, retry loop, timeout padding, relaxed assertion, `cmd/functionallane`, Makefile functional target, or shared functional wait helper was modified.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Twenty separate fresh `go test ./tests/functional/orchestration/javascript/durability -count=1 -v -timeout 900s` commands passed. Retained command output records `RUN 1/20 PASS`, `RUN 2/20 PASS`, `RUN 3/20 PASS`, `RUN 4/20 PASS`, `RUN 5/20 PASS`, `RUN 6/20 PASS`, `RUN 7/20 PASS`, `RUN 8/20 PASS`, `RUN 9/20 PASS`, `RUN 10/20 PASS`, `RUN 11/20 PASS`, `RUN 12/20 PASS`, `RUN 13/20 PASS`, `RUN 14/20 PASS`, `RUN 15/20 PASS`, `RUN 16/20 PASS`, `RUN 17/20 PASS`, `RUN 18/20 PASS`, `RUN 19/20 PASS`, and `RUN 20/20 PASS`.
  - `go test -race ./tests/functional/orchestration/javascript/durability -count=1 -v -timeout 900s` passed all three scenarios without a race report.
  - `go vet ./tests/functional/orchestration/javascript/durability`, `make typecheck`, `git diff --check`, and PRD JSON validation passed.
- **Learnings for future iterations:**
  - A shared injected-edge synchronization fixture is sufficient when sibling scenarios demonstrably exercise the same provider-entry race; keep each scenario's public assertions intact rather than adding separate timing controls.
  - The required fresh-run evidence is strongest when each `-count=1` invocation is launched as a separate command and the numbered outcomes are retained alongside the PRD notes.
---
## 2026-08-10 15:10 -07:00 - thr-deflake-js-durability-002 review follow-up
- What was implemented
  - Addressed the blocking PR feedback by replacing the durability test's custom in-process `ProviderOverride` fake with a `ProviderCommandRunner` edge runner that returns sanitized provider-shaped Codex JSONL, blocks its second command until the dispatch context is canceled, and exposes entry/cancellation signals.
  - Made the copied workflow explicitly select `CODEX` for both live children so the public JavaScript worker route reaches the injected command edge.
  - Replaced the premature no-snapshot completion signal with the root-built `FunctionalAPIServer.Stop` shutdown/join boundary. The test still reads the public interrupted session before stopping the process, then rejects both the durable-session snapshot and its parent directory after `Process.Execute` has returned.
- Files changed
  - `tests/functional/orchestration/javascript/durability/resume_test.go`
  - `tests/fixtures/javascript_runtime/resumable-two-step-fake-children.workflow.js`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/orchestration/javascript/durability -count=1 -v -timeout 240s` — all three scenarios passed.
  - CLI resume, MCP resume, sessions restart, and lower-level runtime resume consumers of the shared workflow fixture — all targeted cases passed.
  - `go test -race ./tests/functional/orchestration/javascript/durability -count=1 -v -timeout 900s` — passed without a race report.
  - Twenty separate fresh `go test ./tests/functional/orchestration/javascript/durability -count=1 -timeout 900s` commands — `RUN 1/20 PASS` through `RUN 20/20 PASS`.
  - `go vet ./tests/functional/orchestration/javascript/durability`, `make typecheck`, `git diff --check`, and PRD JSON validation — passed.
- **Learnings for future iterations:**
  - `inferCompleted` was still too early when emitted from a deferred callback inside `Infer`; it only proved the edge function was returning, not that the caller/runtime had processed cancellation and cleanup. An owned process shutdown/join is the correct boundary for the negative persistence assertion.
  - The command-runner edge is wired only after the JavaScript child has a concrete registered provider selection; an empty `agent.run` selection produces a live child with no provider command call rather than exercising the injected runner.
  - PR #1858's blocking comment required both the lifecycle-quiescence correction and removal of the custom provider fake; both are now represented in the source diff and updated PRD notes.
---
## 2026-08-10 13:48 -07:00 - thr-split-cli-root-run-tests-001
- What was implemented
  - Mechanically split `pkg/transports/cli/root_run_test.go` into focused completion, named-factory selection, root composition, input/presentation, run flags, run configuration, and operator-default test files, with shared package-local fixtures in `root_run_test_helpers_test.go`.
  - Preserved all 273 discovered CLI root-package test names and all nonblank test/helper bodies; only declaration placement and per-file imports changed.
  - Removed the obsolete root-file backend-size and maintainability exemptions and updated the intentional `pkg/transports/cli` package file-count baseline from 23 to 30.
  - Marked `thr-split-cli-root-run-tests-001` as passed in `prd.json`; invocation story 002 remains incomplete for the next iteration.
- Files changed
  - `pkg/transports/cli/root_run_test.go` (split/deleted)
  - `pkg/transports/cli/root_command_composition_test.go`
  - `pkg/transports/cli/root_operator_defaults_test.go`
  - `pkg/transports/cli/root_run_completion_test.go`
  - `pkg/transports/cli/root_run_config_test.go`
  - `pkg/transports/cli/root_run_flags_test.go`
  - `pkg/transports/cli/root_run_input_test.go`
  - `pkg/transports/cli/root_run_selection_test.go`
  - `pkg/transports/cli/root_run_test_helpers_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Before/after normalized `go test ./pkg/transports/cli -run '^$' -list '.*'` inventory — 273 names on both sides; diff empty.
  - Before/after `./pkg/transports/cli` coverage — 67.6% on both sides.
  - `go test ./pkg/transports/cli -count=1`, `go vet ./pkg/transports/cli`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - `git diff --check` — passed.
  - `make test` exceeded the 180-second tool ceiling without a failure result; the focused package suite and all changed-surface gates passed.
  - Commit `15ca23ae1` — `test(cli): split root run tests`.
- **Learnings for future iterations:**
  - Keep shared root CLI fixtures in one helper file, but leave behavior-specific assertion helpers beside their owning test group when the move is mechanical.
  - A same-package test split increases the package file count even when the total source body is unchanged; update the package baseline and remove stale file exemptions in the same change.
  - The remaining `pkg/transports/cli/run/run_invocation_test.go` split is independent and should be handled as story 002 without mixing its inventory or coverage evidence into this iteration.
---
## 2026-08-10 14:17 -07:00 - thr-split-cli-root-run-tests-002
- What was implemented
  - Mechanically split `pkg/transports/cli/run/run_invocation_test.go` into lifecycle/bootstrap, input resolution, output/error, CLI/API parity, and no-server integration test files, with shared invocation fixtures in `run_invocation_test_helpers_test.go`.
  - Preserved the package discovery inventory and moved the existing assertions, inputs, expected outputs, fixture semantics, and CLI outcomes without semantic changes.
  - Removed the obsolete run-invocation backend-size and maintainability exemptions and updated the intentional `pkg/transports/cli/run` package file-count baseline from 27 to 32.
  - Marked `thr-split-cli-root-run-tests-002` as passed in `prd.json`; all PRD stories are now complete.
- Files changed
  - `pkg/transports/cli/run/run_invocation_test.go` (split/deleted)
  - `pkg/transports/cli/run/run_invocation_input_test.go`
  - `pkg/transports/cli/run/run_invocation_lifecycle_test.go`
  - `pkg/transports/cli/run/run_invocation_no_server_test.go`
  - `pkg/transports/cli/run/run_invocation_output_test.go`
  - `pkg/transports/cli/run/run_invocation_parity_test.go`
  - `pkg/transports/cli/run/run_invocation_test_helpers_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Isolated before/after `go test ./pkg/transports/cli/run -count=1 -run '^$' -list '.*'` inventories — 146 names on both sides, SHA-256 `8A65F4CD3D63F0384D960E239E4EC434A2AD1F74E92ED8CBC8389B6C33C84E15` on both sides, normalized diff empty.
  - Isolated before/after full package coverage — 72.1% on both sides.
  - `go test ./pkg/transports/cli/run -count=1 -timeout 180s`, focused typecheck via `go test ... -run '^$'`, `go vet ./pkg/transports/cli/run`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - Largest resulting split test file is 513 lines; `git diff --check` passed.
- **Learnings for future iterations:**
  - Keep no-server bootstrap fixtures and their integration assertions together because they share the capturing runner, mock-worker configuration, and API-projection helpers; keep metric recorder helpers with input-resolution observability tests.
  - A full package-level inventory includes tests from every same-package file; compare isolated `HEAD` and current worktrees rather than only the moved file's declarations.
---

## 2026-08-10 16:05 -07:00 - thr-deflake-js-durability current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `e8c0a248a` into the durability branch after the PR base advanced again.
  - Preserved the task-scoped `prd.json` as the PR description source and retained the append-only durability and prior current-main progress history while resolving the shared bookkeeping-file conflict.
  - Retained the unrelated current-main packaged-installation ownership, hard-kill functional regression, and CLI test-split changes in the merge tree without widening this task's implementation scope.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main packaged-installation, system-initialization, CLI, and hard-kill functional test files included by the synchronization merge.
- Verification
  - `prd.json` remains the durability PRD with all three stories marked `passes:true`.
  - Merge conflict markers were removed; the staged merge will be checked with `git diff --cached --check` and JSON validation before push.
- **Learnings for future iterations:**
  - Keep the active task PRD authoritative when current main carries another task's PRD, and record the base SHA in the append-only progress log so PR description parity remains auditable after repeated base advances.
---
## 2026-08-10 14:26 -07:00 - e2-structured-worker-results current-main merge audit
- What was implemented
  - Merged current `origin/main` at `6e01bee689bbddbffbd920632f07e6b09586cf5` into the task branch.
  - Preserved the active E2 task `prd.json` and merged the 25 current-main progress sections plus reusable Codebase Patterns without duplicating dated entries.
- Files changed
  - `progress.txt`
  - Current-main CLI/runtime test and transport files from the merge
- Verification
  - `make test` — passed.
  - `make typecheck` — passed.
  - `make mcp-contract-check` — passed.
  - `make ui-test` — passed (3,161 tests).
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Resolve append-only progress conflicts by selecting unique dated sections from bounded Git chunks, then validate line and header counts before staging.
---

## 2026-08-10 15:00 -07:00 - e2-structured-worker-results review follow-up
- What was implemented
  - Addressed the latest blocking PR feedback by moving recorded Work identity, content, and structured-result projection helpers out of the oversized transitioner and into the existing `invocation_output.go` owner.
  - Reduced `subsystem_transitioner.go` to 959 lines without changing runtime behavior or adding a package file, preserving the exact 19-file subsystem baseline.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/subsystems/invocation_output.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/subsystems/subsystem_transitioner.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/subsystems` — passed.
  - `make test` — passed.
  - `make typecheck` — passed.
  - `make vet` — passed.
  - `make pkg-structure` — passed.
  - `make backend-size`, `make pkg-maint`, and `make pkg-file-count` — passed.
  - `make lint` passed all prerequisite checks and reached only the known 175-entry `pkg-boundary` baseline.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A maintainability fix must satisfy adjacent repository ratchets too: use an existing cohesive owner when adding a file would violate an exact package file-count baseline.
---

## 2026-08-10 15:14 -07:00 - e2-structured-worker-results current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` at `78584efb52dacb6989604f8f7cc6c12ed08da028` into the E2 branch and preserved the task-scoped PRD plus append-only progress history while retaining the current-main CLI split and baseline changes.
  - Revalidated that the live main ref is an ancestor of merge head `19e22198b`; no unresolved shared-file conflicts or generated-file merge conflicts remain.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main CLI test and baseline files from the merge
- Verification
  - `git merge-tree --write-tree HEAD origin/main` — clean.
  - `git diff --check` — passed.
  - GitHub CI run `31436798504` for head `19e22198b` — all required checks passed, including Verification Policy.
- **Learnings for future iterations:**
  - Update the local tracking ref explicitly with `git fetch origin main:refs/remotes/origin/main` before final mergeability checks; a stale tracking ref can hide newer main history even when the PR appears clean.
---
## 2026-08-10 15:44 -07:00 - e2-structured-worker-results current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` at `e8c0a248ae1ba354ccc2fc022fa674feefe52f85` after the PR base advanced through another merge.
  - Preserved the E2 task-scoped `prd.json`, retained both append-only progress histories and reusable patterns, and kept all current-main source/test changes from the synchronization merge.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main packaged-installation, initialization, CLI, and wiring sources/tests from the merge
- Verification
  - `prd.json` parses with all four E2 stories `passes:true`.
  - Conflict-marker scan reports no unresolved markers in `prd.json` or `progress.txt`.
  - `git diff --cached --check` and `git diff --check` — passed.
- **Learnings for future iterations:**
  - When the live base advances during PR verification, resolve shared task bookkeeping by retaining the active PRD and both append-only histories, then rerun the exact generated and repository gates on the new merge head.
---
## 2026-08-10 15:58 -07:00 - e2-structured-worker-results merged-head CI follow-up
- What was implemented
  - Completed the fresh required CI matrix after merging live `origin/main`; no product-code changes were needed for the earlier transient functional-coverage failure.
- Files changed
  - `progress.txt`
- Verification
  - GitHub Actions run `31439657345` for head `9569de807` — all 18 required checks passed, including Backend Functional Coverage, Backend Unit Coverage, and Verification Policy.
  - The PR remains open, cleanly mergeable against base `e8c0a248`, and its body still matches `prd.json` exactly.
- **Learnings for future iterations:**
  - After a current-main synchronization, validate the entire required matrix on that exact merge head; an earlier failed retry on the superseded head is not evidence against the new head.
---

## 2026-08-10 16:19 -07:00 - e2-structured-worker-results final handoff audit
- What was implemented
  - Completed the final one-story iteration review for the E2 structured worker result channel; no additional product-code changes were needed because all four PRD stories are already marked `passes:true`.
  - Audited PR #1855's historical blocking conversation and confirmed the latest requested transitioner extraction, safe schema diagnostics, collision-free watch null handling, current-main synchronization, and generated-contract state are present on the current head.
  - Refreshed the PR description from the active `prd.json`.
- Files changed
  - `progress.txt`
- Verification
  - `prd.json` parses with all four E2 stories `passes:true`.
  - Thread-aware GraphQL audit found 3 conversation comments, 0 reviews, and 0 inline review threads; the latest blocking quality-gate feedback is addressed by the current tree.
  - `subsystem_transitioner.go` is 959 lines; `origin/main` is an ancestor of `HEAD`; `git diff --check` passed.
  - `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `make vet` passed.
  - Isolated `make lint` passed all prerequisite checks and stopped only at the known 175-entry `pkg-boundary` baseline, matching current main with no new violations.
  - PR #1855 is open, cleanly mergeable, and all 18 required checks on head `1d8037901d6e12d7eaecc34fa55ad9237deffec6` are terminal and successful; the PR body matches `prd.json` byte-for-byte.
- **Learnings for future iterations:**
  - Compare a PR body by parsing the full GitHub API response; `gh api --jq .body` appends its own output newline and can produce a false byte-mismatch.
  - A final handoff audit should verify the exact current head against `origin/main`, rerun local ratchets, inspect thread-aware review state, and confirm the PR body source before declaring review readiness.
---
---
## 2026-08-10 04:32 -07:00 - sac-s0-2-pkg-boundary-ignores-001
- What was implemented
  - Centralized repository-root-relative pruning for `.artifacts`, `.artifacts/bootstrap/worktrees`, generated transient worktrees, and the supported root worktree locations.
  - Applied the same skip decision before parsing in every `pkg-boundary` production traversal, including secondary service, transport, provider, edge, test-behavior, Petri, and production-default scans.
  - Preserved the existing Git metadata, dependency/vendor, node-module, and testdata exclusions without matching artifact-like or worktree-like names inside tracked production packages.
  - Expanded the normal `run` regression to use malformed stale `pkg/wire` imports beneath artifact/bootstrap/generated transient layouts and existing ignored roots, proving those files are pruned before parsing.
- Files changed
  - `cmd/pkgboundarycheck/main.go`
  - `cmd/pkgboundarycheck/main_test.go`
  - `cmd/pkgboundarycheck/constructed_service_edges.go`
  - `cmd/pkgboundarycheck/functional_process_edges.go`
  - `cmd/pkgboundarycheck/initializer_behavior.go`
  - `cmd/pkgboundarycheck/provider_effect_ownership.go`
  - `cmd/pkgboundarycheck/production_defaults.go`
  - `cmd/pkgboundarycheck/support_service_imports.go`
  - `cmd/pkgboundarycheck/transport_behavior.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/pkgboundarycheck -run '^TestRunIgnoresRepositoryPolicyNonSourceRoots$' -count=1` — passed.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `make pkg-boundary` — reaches the checker and reports only the unchanged 176-violation baseline on current `main`; no new violation was introduced by this story.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The checker has many independent `WalkDir` callbacks; updating only the primary repository scan leaves secondary scanners able to rediscover ignored files.
  - A malformed ignored fixture is useful behavioral evidence for prune-before-parse: a valid stale import alone may not exercise every parser path.
 - The next story should add the paired production `pkg` failure fixture and verify the `make pkg-boundary` subprocess path while keeping nested artifact-like directory names visible.
 ---
---
---
## 2026-08-10 06:23 -07:00 - pf-stale-scope-diagnostic-001 acquisition-boundary follow-up
- What was implemented
  - Hardened the real-process acquisition observer against a directory-enumeration/atomic-rename race by re-stat-ing the candidate stage and freezing the live Windows predecessor immediately after the resource is observed, before the forced kill.
  - Added the non-Windows no-op companion so the functional test remains portable while retaining the same bounded process choreography.
- Files changed
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_process_windows_test.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_process_nonwindows_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=3 -v -timeout 600s` — passed in all three runs; each successor returned the bounded contention diagnostic and preserved the observed staging path.
- **Learnings for future iterations:**
  - Filesystem enumeration can return a staging entry that has already been atomically renamed by the time the caller acts; re-stat the candidate and suspend the live Windows process at the observation boundary when a hard-kill test must retain the exact resource.
---
---
## 2026-08-10 06:25 -07:00 - pf-stale-scope-diagnostic-001 final verification
- What was implemented
  - Removed the superseded dashboard-readiness helper from the hard-kill harness after the regression moved to bounded successor failure assertions.
- Files changed
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `progress.txt`
- Verification
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - `make pkg-structure` — passed.
  - `go vet ./tests/functional/transport/cli/process` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Keep process-harness helpers aligned with the current observable contract; once startup readiness is intentionally forbidden, retain only bounded-failure and output-drain assertions.
---
---
## 2026-08-10 07:06 -07:00 - pf-stale-scope-diagnostic-002
- What was implemented
  - Added an atomic deterministic `.<factory>.staging-owner` directory for packaged Factory create/replace ownership, with atomically renamed `.owner.json` PID metadata and compare-before-release cleanup.
  - Added conservative owner inspection: active PID evidence returns `active-contention`; missing, malformed, permission-denied, reused/uncertain, and racing metadata preserve the resource and return bounded `indeterminate-contention` or `failed` diagnostics with scope, exact resource, owner evidence, and verify/stop/remove/retry guidance.
  - Kept the normal existing-target skip path free of the write lease, so startup behavior is unchanged when no installation is needed.
  - Added deterministic live-owner, malformed-owner, and acquisition-race tests; strengthened the real hard-kill regression to assert PID evidence in successor stderr.
- Files changed
  - `pkg/platform/filesystem/local.go`
  - `pkg/services/factory_definitions/packaged_installation_ports.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service_test.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/owner_liveness_windows.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/owner_liveness_nonwindows.go`
  - `pkg/wire/factory_definition_packaged_installation_test.go`
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_definitions/internal/services/distribution/packagedinstallation -count=1 -timeout 180s` — passed.
  - Live-owner, malformed-owner, and acquisition-race tests — passed 20 repeated runs.
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=3 -v -timeout 600s` — passed.
  - `go test ./tests/functional/workers/transports/cli/run/lifecycle ./tests/functional/workers/transports/cli/run/modes -count=1 -timeout 300s` — passed after narrowing lease acquisition to writes.
  - `go test ./pkg/wire ./pkg/transports/cli -count=1 -timeout 180s` — passed.
  - `go vet` focused changed packages — passed.
  - `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` — passed.
  - `go test ./... -count=1 -timeout 900s` — completed with unrelated repository baseline/environment failures in migration/ownership inventory fixtures, broad functional metadata/API readiness lanes, and stress/release readiness; changed owner package and focused process/lifecycle lanes passed independently.
- **Learnings for future iterations:**
  - Acquire a cross-process write lease only after proving a create/replace is needed; taking it for an already-valid target turns harmless concurrent startup reads into false contention.
  - Publish ownership metadata through a temp file plus rename, and treat a missing final record while the directory remains as indeterminate rather than guessing whether the writer is live.
  - PID liveness is evidence, not identity: active PIDs must never authorize deletion, and diagnostics should say `owner_identity=unverified` so operators verify the process before clearing the exact path.
---
---
## 2026-08-10 08:02 -07:00 - pf-stale-scope-diagnostic-002 coverage follow-up
- What was implemented
  - Added observable installation tests for ownership acquisition failures, pre-existing stage owner classifications, inspection failures, and compare-before-release errors so the new bounded contention paths retain the package coverage floor.
- Files changed
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/owner_contention_test.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service_test.go`
  - `progress.txt`
- Verification
  - Focused owner/contention tests with `-race` repeated 20 times — passed.
  - Packaged installation coverage — passed at 83.5% locally against the 81.11% package minimum.
  - `go test ./pkg/services/factory_definitions/... -run '^$'` — passed.
  - `go vet` focused changed packages, `make pkg-maint`, `make pkg-file-count`, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - Per-package coverage gates count newly introduced error branches, so each ownership failure mode needs an observable test rather than relying only on the happy-path concurrency tests.
---
---
## 2026-08-10 08:31 -07:00 - pf-stale-scope-diagnostic-002 CI follow-up
- What was implemented
  - Reconciled the updated PR against CI results and retried the failed functional lane twice without changing the scoped implementation.
- Files changed
  - `progress.txt`
- Verification
  - PR #1853 Backend Unit Coverage — passed on commit `390b3a387`.
  - PR #1853 other required build, API, UI, packaged-factory, provider, inference, docs, and README lanes — passed.
  - PR #1853 Backend Functional Coverage — remains blocked by the unrelated checked-in worktree package floor (`45.2381%` actual vs `53.54%` minimum); one retry instead exposed an unrelated flaky JavaScript durability cancellation assertion before the floor check.
  - PR comment/review/thread inspection — no feedback present.
- **Learnings for future iterations:**
  - A green changed-package/unit lane does not clear the PR while the shared functional coverage manifest has an unrelated package-floor regression; record the exact package and measured delta and leave that baseline issue scoped for its owning change.
---
---
## 2026-08-10 08:08 -07:00 - pf-stale-scope-diagnostic-002 PR CI diagnosis follow-up
- What was implemented
  - No source change; inspected PR #1853 with the thread-aware GitHub review workflow and found no conversation comments, reviews, or unresolved inline threads.
  - Retrieved the failed Backend Functional Coverage artifact and identified the exact failure as the parallel-suite assertion in `tests/functional/runtime_api`: the runtime log was missing `factory started`.
  - Confirmed the branch has no diff in that package and reran the named test five times in isolation; all five runs passed, so the failed CI result is a suite-level timing flake rather than a regression from this story.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/runtime_api -run '^TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput$' -count=5 -v -timeout 300s` — passed, 5/5.
  - `python gh-address-comments/scripts/fetch_comments.py` — no PR comments, reviews, or unresolved threads.
  - PR #1853 body remains byte-equivalent to the current `prd.json`.
- **Learnings for future iterations:**
  - When broad functional coverage reports a process-startup/log-observation failure, isolate the named test repeatedly before changing lifecycle code; parallel scheduling can expose a transient missing-log race that is not reproducible alone.
---
---
## 2026-08-10 08:18 -07:00 - pf-stale-scope-diagnostic-002 PR CI rerun follow-up
- What was implemented
  - No source change; inspected the second Backend Functional Coverage failure on the latest PR commit.
  - Confirmed the failure moved to the unrelated `tests/functional/sessions/restart` assertion `TestFactorySessionResumeDoesNotRepeatCompletedDispatch`, where the provider did not observe a canceled workflow context.
  - Confirmed the branch has no diff in that package and reran the named test five times in isolation; all five runs passed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/sessions/restart -run '^TestFactorySessionResumeDoesNotRepeatCompletedDispatch$' -count=5 -v -timeout 300s` — passed, 5/5.
  - Latest PR run: Backend Unit Coverage and all non-functional lanes passed; Backend Functional Coverage failed only at the isolated-test assertion above.
- **Learnings for future iterations:**
  - A changing failure among unrelated cancellation/startup observations across full-suite reruns is evidence of parallel-suite flakiness; preserve the feature scope and isolate each named test before attempting a production change.
---
---
## 2026-08-10 08:45 -07:00 - pf-stale-scope-diagnostic-002 evidence cleanup
- What was implemented
  - Removed the superseded Git `config.lock` contention experiment from the Go worktree preparer and packaged full-flow worktree script after reproduction proved that Git serialization was not the retained pre-runtime resource.
  - Regenerated the packaged Factory JSON/YAML and manifest from the authored script so generated assets no longer carry the unsupported hypothesis.
  - Kept the evidenced packaged Factory staging-owner acquisition, live-owner classification, compare-before-release behavior, and hard-kill regression unchanged.
- Files changed
  - `packages/packaged-factories/factories/full-flow/scripts/setup-task-worktree.py`
  - `packages/packaged-factories/generated/factories/full-flow/factory.json`
  - `packages/packaged-factories/generated/factories/full-flow/factory.yaml`
  - `packages/packaged-factories/generated/manifest.json`
  - `pkg/services/workers/internal/services/workstations/worktree/prepare.go`
  - `pkg/services/workers/internal/services/workstations/worktree/prepare_test.go`
  - `tests/factory/scripts/test_full_flow_setup_task_worktree.py`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/workers/internal/services/workstations/worktree -count=1 -timeout 180s` — passed.
  - `go vet ./pkg/services/workers/internal/services/workstations/worktree` — passed.
  - `python -m unittest discover -s tests/factory/scripts -p 'test_full_flow_setup_task_worktree.py'` — passed, 4 tests.
  - `make packaged-factory-catalog-generate` — passed.
  - `git diff --check` — passed.
  - `make test-functional-coverage` — stopped at the pre-existing parallel failure in `TestFactoryRuntimeMixedDirectAndFactoryChildDispatchesStayIsolatedThroughCLI` (`unexpected provider prompt`) before coverage evaluation; the named test failed identically in five isolated repetitions and this branch has no diff in its package.
  - Focused functional coverage of `pkg/services/workers/internal/services/workstations/worktree` after cleanup measured 52.8% from the Codex worktree flow; the full lane is required to include all contributing functional packages once the unrelated suite flake is resolved.
- **Learnings for future iterations:**
  - Once deterministic reproduction identifies the owning resource, remove speculative ownership mechanisms and their generated/test surfaces instead of preserving competing explanations for coverage or convenience.
  - A full coverage command can fail before package-floor evaluation; isolate the named functional test and report that boundary explicitly rather than lowering a checked-in floor.
---
---
## 2026-08-10 09:12 -07:00 - pf-stale-scope-diagnostic delivery synchronization
- What was implemented
  - Reconciled the branch with current `origin/main` at `59fd57487`, retaining this task’s scoped `prd.json`, combining both append-only progress histories, and preserving all current-main source and test changes.
  - Committed the conflict-free merge as `a147d3fd4`.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main source and test changes were merged as part of the base synchronization.
- Verification
  - Parsed `prd.json` reports zero stories with `passes:false`.
  - `go test ./pkg/services/factory_definitions/internal/services/distribution/packagedinstallation -count=1 -timeout 180s` — passed.
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v -timeout 300s` — passed; the successor reported the retained staging-owner resource and PID without reaching runtime.
  - `go test ./cmd/pkgboundarycheck -count=1` — passed.
  - `make pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A moving `main` can make a completed task PR dirty even when CI is green; resolve shared bookkeeping conflicts by keeping the active task PRD authoritative, combining append-only progress histories, and rerunning the owner and process-boundary evidence after the merge.
---
---
## 2026-08-10 09:28 -07:00 - pf-stale-scope-diagnostic CI verification follow-up
- What was implemented
  - Diagnosed the first post-synchronization Backend Functional Coverage failure as the unchanged `tests/functional/work/root_composition/TestWorkRecoveryActivatesThroughRootBuildProcessAfterLifecycle` parallel-suite race; its CI observation missed the intermediate `New state: init` event while the isolated run passed.
  - Reran the failed GitHub Actions jobs without changing the implementation; the retry completed successfully on the same PR head.
- Files changed
  - No source files; this entry records CI diagnosis and retry evidence.
- Verification
  - `go test ./tests/functional/work/root_composition -run '^TestWorkRecoveryActivatesThroughRootBuildProcessAfterLifecycle$' -count=5 -v -timeout 300s` — passed 5/5.
  - PR #1853 retry Backend Functional Coverage — passed.
  - PR #1853 retry Verification Policy — passed.
  - All required PR checks are terminal and passing on `de46e3c39`.
  - Thread-aware PR inspection — no conversation comments, reviews, or unresolved threads.
- **Learnings for future iterations:**
  - When a full functional suite loses an expected intermediate lifecycle event, isolate the named test repeatedly before changing runtime code; a clean isolated run plus no PR diff in that package supports retrying the CI lane rather than broadening feature scope.
---
---
## 2026-08-10 10:21 -07:00 - pf-stale-scope-diagnostic-002
- What was implemented
  - Threaded the persisted `backendScopeID` from System Initialization through the packaged Factory installer parameters so startup diagnostics identify the actual backend scope instead of mislabeling the Factory root as a scope.
  - Injected the repository logger into packaged installation and emitted structured safe acquisition, success, explicitly revalidated orphan-reclaim, active/indeterminate contention, and failure outcomes with resource paths and owner evidence.
  - Split exclusive staging-directory creation into a narrowly owned `PackagedInstallationDirectoryCreator` edge; removed `Local.Mkdir` so the generic platform filesystem no longer hides the synchronization effect.
  - Preserved conservative live, malformed, permission-denied, and racing ownership behavior; added deterministic logger, scope propagation, owner-reclaim, and edge-override assertions.
- Files changed
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/owner_liveness_*.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/diagnostics_test.go`
  - `pkg/services/factory_definitions/packaged_installation*.go`
  - `pkg/services/system_initialization/internal/workflow/workflow.go` and affected installer fakes/tests
  - `pkg/services/edges/definition.go`, `pkg/wire/*`, and generated `pkg/wire/wire_gen.go`
  - `pkg/platform/filesystem/local.go`
  - `progress.txt` and `prd.json`
- Verification
  - `make test` — passed.
  - `make wire-smoke` — passed, including generated-wire drift check.
  - `make backend-size pkg-maint pkg-file-count vet logging-boundary-check` — passed.
  - Focused packaged-installation, System Initialization, edge, and Wire tests — passed.
  - Built-process hard-kill successor regression — passed on Windows; successor returned bounded contention and preserved the staged resource.
  - `make pkg-boundary` still reports the repository's existing migration backlog; the review-specific `Local.Mkdir` finding is removed and no new packaged-installation finding remains.
- **Learnings for future iterations:**
  - The durable operator config is the source of the backend-scope identity, while the deterministic `.<factory>.staging-owner` directory is the actual serialization resource; keep those concepts separate in errors, logs, and tests.
  - The local OS probe deliberately does not equate a missing PID with safe reclamation because PID reuse and incomplete stage contents are not identity proof; only a stronger injected orphan classification may enter compare-and-reclaim.
---
---
## 2026-08-10 10:29 -07:00 - pf-stale-scope-diagnostic-002 verification follow-up
- What was implemented
  - Kept ordinary payload, validation, and target-format failures on their existing installation-error path while retaining structured `failed` logs; only ownership acquisition/recovery failures use the typed contention diagnostic.
- Files changed
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `progress.txt`
- Verification
  - Focused packaged-installation, System Initialization workflow, and Wire tests — passed.
  - Pushed commit `11a6bacc7` to PR #1853's existing head branch.
- **Learnings for future iterations:**
  - Ownership diagnostics should not broaden typed contention semantics to unrelated invalid payload or customer-target failures; log those failures while preserving their original caller-visible error class.
---
---
## 2026-08-10 10:32 -07:00 - pf-stale-scope-diagnostic-002 review follow-up
- What was implemented
  - Removed the prohibited `--with-mock-workers` flag from the real-process hard-kill regression because the scenario stops before runtime/work execution.
  - Added explicit in-code justification for the bounded filesystem polls that observe the child process's durable operator config and packaged-factory staging reservation; the child exposes neither observation through `BuildProcess` nor an injected edge.
  - Restored the unrelated full-flow setup-task-worktree blank-line change and regenerated its JSON/YAML catalog entries and manifest hashes.
- Files changed
  - `tests/functional/transport/cli/process/hard_kill_successor_test.go`
  - `packages/packaged-factories/factories/full-flow/scripts/setup-task-worktree.py`
  - `packages/packaged-factories/generated/factories/full-flow/factory.json`
  - `packages/packaged-factories/generated/factories/full-flow/factory.yaml`
  - `packages/packaged-factories/generated/manifest.json`
  - `tests/factory/scripts/test_full_flow_setup_task_worktree.py`
  - `prd.json`
  - `progress.txt`
- **Learnings for future iterations:**
  - A built CLI is justified for hard-kill and Windows process-suspension evidence, but functional tests must still avoid worker mocks and explain every filesystem poll used to synchronize an otherwise unobservable child-process checkpoint.
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v -timeout 300s` — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `python tests/factory/scripts/test_full_flow_setup_task_worktree.py` — passed.
  - `make packaged-factory-catalog-generate` and `git diff --check` — passed.
---
---
## 2026-08-10 10:47 -07:00 - pf-stale-scope-diagnostic CI verification follow-up
- What was implemented
  - Diagnosed the failed PR #1853 Backend Functional Coverage lane as the unchanged `tests/functional/factory/packaged/plan_parallel/TestPackagedPlanParallelChildFailureFansInWithoutMerge` test observing two executor calls instead of its bounded-retry expectation; the dependent Verification Policy failure reported the same backend result.
  - Confirmed the failing package has no branch diff relative to `origin/main` and repeated the test 10 times locally with all runs passing. No production or test-code change was warranted.
- Files changed
  - `progress.txt`
- Verification
  - Focused `TestPackagedPlanParallelChildFailureFansInWithoutMerge` — passed 10/10 locally.
  - Focused packaged-installation owner tests — passed.
  - Real-process hard-kill successor regression — passed.
  - `make pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `prd.json` reports zero stories with `passes:false`; `git diff --check` — passed.
- **Learnings for future iterations:**
  - When a full functional coverage lane fails in an unchanged package and the focused test repeats cleanly, preserve the task scope and retry the GitHub Actions lane rather than changing unrelated behavior.
---
---
## 2026-08-09 14:04 - wo-d1-packaged-factories-docs-003
- What was implemented
  - Added distinct operator-ready entries for `@you/factory-builder`, `@you/plan-execute`, `@you/plan-parallel`, and `@you/full-flow` in `docs/reference/packaged-factories.md`.
  - Documented each live help signature, all worker roles and provider/model overrides, relative workflow choice, workspace and Git side effects, concurrency and retry bounds, validation/installation, local merge/replanning, and final versus intermediate output behavior.
  - Added exact current-binary smoke evidence: the builder returned `mock worker accepted`; plan-execute returned `mock worker accepted` and `<COMPLETE>`; plan-parallel returned `mock worker accepted`; full-flow's controlled invalid-bound check returned the documented `maxCycles` choice-validation error.
  - Marked Story 003 as passing in `prd.json` and recorded the generic-mock limitation so protocol-valid planner responses are not confused with end-to-end orchestration evidence.
- Files changed
  - `docs/reference/packaged-factories.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make docs-reference-smoke` — passed.
  - `go test ./tests/functional/factory/packaged/factory_builder ./tests/functional/factory/packaged/plan_execute ./tests/functional/factory/packaged/plan_parallel ./tests/functional/factory/packaged/full_flow -count=1 -timeout 300s` — passed.
  - `make verify-fast` — passed: dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun unit checks, and the short Go suite.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The planning entries are not interchangeable: builder installs a new Factory, plan-execute stays sequential in the current workspace, plan-parallel shares one repository across a bounded DAG, and full-flow owns worktrees, local merges, and replanning.
  - Generic mock workers accept a planner dispatch without producing its required `FACTORY_REQUEST_BATCH`; use protocol-valid fixtures or provider-backed functional tests when verifying generated Work, concurrency, or cycle control.
  - Keep full-flow CLI smoke examples bounded and terminating; use controlled argument validation for the public command example and the isolated functional test for actual worktree/merge behavior.
---

## 2026-08-10 17:10 -07:00 - e2-structured-worker-results current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` at `acbd13fcc` into the E2 branch and preserved the task-scoped `prd.json`, all append-only progress histories, and consolidated Codebase Patterns.
  - No additional E2 product implementation was required because all four PRD stories remain `passes:true`; current-main JavaScript durability changes were retained as part of the required base synchronization.
- Files changed
  - `progress.txt`
  - `tests/fixtures/javascript_runtime/resumable-two-step-fake-children.workflow.js`
  - `tests/functional/orchestration/javascript/durability/resume_test.go`
- Verification
  - `make test` — passed.
  - `make typecheck` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make lint` passed all prerequisite checks and reached only the known `pkg-boundary` baseline of 175 violations; a detached `origin/main` audit reports the same 175.
  - Focused structured-result worker, prompt, Work CLI/watch, and replay-related tests — passed.
  - `go test -tags functionallong ./tests/functional/workstations/execution -run '^TestStructured(ResultFlowsIntoDownstreamPromptThroughRootBuildProcess|StructuredOutputSchemaViolationRoutesFailureWithoutDownstreamDispatch)$' -count=1` — passed.
  - `prd.json` parses with four passing stories; conflict-marker scan and `git diff --check` passed; `origin/main` is an ancestor of `HEAD`.
- **Learnings for future iterations:**
  - When a moving-base merge conflicts in append-only progress, preserve every distinct section and pattern from both clean refs, then validate heading coverage before committing.
  - Compare ratcheted lint counts in a detached current-main worktree; the intentional non-zero `pkg-boundary` result is clean only when the exact baseline matches.

## 2026-08-10 13:58 -07:00 - thr-split-acp-stdio-tests-001
- What was implemented
  - Moved the 22 prompt command and admission tests into `session_prompt_admission_test.go` with their exact names, setup, assertions, and observable outcomes.
  - Consolidated shared ACP stdio fakes, server builders, request payload helpers, identity helpers, and cross-suite stream/control fixtures in `session_test_helpers_test.go` without adding alternate fixture behavior.
  - Kept the change move-dominated and limited to package tests; no production code, public contract, generated artifact, or assertion semantics changed.
- Files changed
  - `pkg/transports/acp/internal/stdio/session_prompt_admission_test.go`
  - `pkg/transports/acp/internal/stdio/session_test_helpers_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_test.go`
  - `pkg/transports/acp/internal/stdio/session_new_test.go`
  - `pkg/transports/acp/internal/stdio/session_set_config_option_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused command/admission tests — passed.
  - `go test -p 1 -vet=off ./pkg/transports/acp/internal/stdio -count=1 -timeout 120s` — passed.
  - Normalized `go test ./pkg/transports/acp/internal/stdio -list '.*'` — 222 tests before and after, zero diff.
  - `go test -cover -count=1` — 94.3% of statements before and after.
  - Package compile/typecheck with `go test -run '^$'` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Package-local Go test fixtures are visible across all files, so shared fakes and constructors can be moved once into a helper while behavioral tests move independently.
  - Normalize and sort `go test -list` output for a stable inventory comparison; moving tests between filenames can otherwise change discovery order without changing the test set.
  - On this shared Windows workspace, use a task-specific `GOCACHE` and serial `-p 1` verification when concurrent worktrees hold the default Go cache; timed-out wrappers can leave child processes that must be identified by their exact command before cleanup.
---
## 2026-08-10 14:23 -07:00 - thr-split-acp-stdio-tests-002
- What was implemented
  - Split the oversized ACP stdio prompt suite into focused dispatch, dispatch-recovery, terminal-lifecycle, and cancellation/connection-failure files while preserving every test name, setup path, assertion, and runtime outcome.
  - Moved shared prompt/control fixtures into `session_test_helpers_test.go` and stream-only event fixtures into `prompt_stream_test_helpers_test.go`, keeping package-local behavior unchanged and every owned file within the enforced backend-size and maintainability limits.
  - Removed stale monolith exemption entries, carried the pre-existing cancellation safety function exemption to its new file, updated the exact package file-count baseline for the intentional suite split, and corrected related source comments.
- Files changed
  - `pkg/transports/acp/internal/stdio/session_prompt_cancellation_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_dispatch_recovery_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_dispatch_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_terminal_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_test_helpers_test.go`
  - `pkg/transports/acp/internal/stdio/session_test_helpers_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_test.go`
  - `pkg/transports/acp/internal/stdio/server_test.go`
  - `tests/functional/chat_sessions/root_composition/acp_prompt_delegation_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
## 2026-08-10 14:47 -07:00 - dwro-s1-remote-placement-flags current-main synchronization follow-up
- What was implemented
  - Merged current `origin/main` into the placement branch, preserved the task-owned `prd.json`, and unioned both branches' append-only progress history and reusable patterns.
  - Reconciled `root_work.go` with current-main centralized CLI diagnostics and moved the shared run-error presentation helpers into the existing `root.go` owner so the package file-line ratchets remain within their recorded limits.
- Files changed
  - `pkg/transports/cli/root.go`
  - `pkg/transports/cli/root_work.go`
  - `prd.json`
  - `progress.txt`
  - Current-main source and generated artifacts included by the synchronization merge.
- Verification
  - `make contracts-validate`, `make cli-manifest-check`, `make docs-reference-smoke`, `make pkg-file-count`, `make pkg-structure`, and `make vet` — passed.
  - `make backend-size` and `make pkg-maint` — passed after the helper move.
  - `go test ./pkg/transports/cli/run` — passed.
  - The exact ACP stdin-cancellation regression passed five consecutive repetitions after one transient failure in a broader CLI package run.
  - `prd.json` parses with all four user stories marked `passes: true`; no conflict markers remain.
- **Learnings for future iterations:**
  - After a current-main merge, run ratchets against the working tree rather than relying on staged or historical counts; moving shared helpers into an existing file can restore deletion-only line limits without adding package-file debt.
---

## 2026-08-10 14:59 -07:00 - dwro-s1-remote-placement-flags CI follow-up
- What was implemented
  - Reconciled the current-main built-server functional assertion with the S1 compatibility contract: the legacy `--server` migration warning is now validated separately from the single structured `SERVER_BIND_FAILED` diagnostic.
  - Kept product code unchanged; the remaining failed coverage cases were unrelated current-main timing-sensitive tests.
- Files changed
  - `tests/functional/transport/server_binding/server_binding_test.go`
  - `progress.txt`
- Verification
  - The sessions partial-result test passed 10 repetitions.
  - The built CLI cancellation test passed 5 repetitions.
  - The corrected built server-bind test passed 5 repetitions.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
- When a synchronized test still encodes the pre-migration stderr shape, preserve the compatibility warning as an explicit first line and decode the structured diagnostic independently rather than suppressing required customer guidance.
---
## 2026-08-10 15:08 -07:00 - dwro-s1-remote-placement-flags current-main test-split reconciliation
- What was implemented
  - Reconciled the branch with current `origin/main` commit `78584efb5`, retaining main's focused root/run test split while preserving the S1 remote adapter and its behavioral coverage.
  - Moved the S1 root placement assertions into `root_run_remote_placement_test.go`, preserved the task-owned S1 PRD, and resolved the intentional package-count baseline to 31 CLI files and 34 run files.
- Files changed
  - `pkg/transports/cli/root_run_remote_placement_test.go`
  - `pkg/transports/cli/root_run_test.go` (removed after its tests were split)
  - `pkg/transports/cli/run/run_invocation_test.go` (removed by current-main split)
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Normalized `go test -p 1 -vet=off ./pkg/transports/acp/internal/stdio -list '.*' -count=1` — 222 tests before and after baseline `a4fc62824`, zero diff.
  - `go test -p 1 -vet=off -cover -count=1 ./pkg/transports/acp/internal/stdio` — 94.3% before and after baseline `a4fc62824`.
  - Full ACP stdio package tests and package compile/typecheck — passed.
  - `go vet ./pkg/transports/acp/internal/stdio` — passed.
  - `make backend-size` — passed.
  - `make pkg-maint` — passed.
  - `make pkg-file-count` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Package-wide Go test symbols allow focused suites to reuse one session/factory/control fixture owner while stream-only event fixtures move to their own small helper when the enforced file-size gate requires it.
  - Exact package file-count baselines must be updated for intentional test-suite additions; the count and rationale must describe the ownership split rather than hiding it behind a broad exemption.
  - Compare both inventory and coverage against the pre-change commit so a move-only split has reviewer-visible equivalence evidence.
---
## [2026-08-10 14:40 -07:00] - thr-split-acp-stdio-tests-003
- What was implemented
  - Split the oversized ACP stdio stream suite into attachment lifecycle, ordered delivery, gap/error recovery, and live-drain behavioral suites while preserving all 43 test names, setup semantics, assertions, and observable outcomes.
  - Consolidated the stream-specific `notificationItemID` and `seedRetentionGap` helpers into `prompt_stream_test_helpers_test.go`; no production code, public contract, generated artifact, or alternate execution path changed.
  - Removed the obsolete `prompt_stream_test.go` file-size exemptions, carried the existing live-drain cyclomatic exemption to its new owner file, and updated the ACP stdio file-count baseline to 31.
- Files changed
  - `pkg/transports/acp/internal/stdio/prompt_stream_attachment_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_delivery_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_gap_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_live_drain_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_test.go` (removed)
  - `pkg/transports/acp/internal/stdio/prompt_stream_test_helpers_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Against baseline revision `2943d7328ed6c11e37e2c138346124e9b3b744bf`, normalized `go test -p 1 -vet=off ./pkg/transports/acp/internal/stdio -list '.*' -count=1` was identical before and after at 222 tests.
  - `go test -p 1 -vet=off -cover -count=1 ./pkg/transports/acp/internal/stdio` — 94.3% before and after.
  - Focused ACP stdio package tests, three-run concurrent-attachment race test, `go vet ./pkg/transports/acp/internal/stdio`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make typecheck`, JSON validation, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - For move-only Go test refactors, use an exact detached baseline worktree to compare normalized test inventory and coverage; current package output alone cannot prove equivalence.
  - Package file-count baselines must be patched with package-specific context: generic `fileCount` hunks can accidentally update an unrelated entry when multiple packages share the same recorded count.
  - When helpers were previously extracted into a shared file, move their adjacent test documentation with the tests so the helper file remains readable and semantically focused.
---
## [2026-08-10 14:54 -07:00] - thr-split-acp-stdio-tests-003 review follow-up
- What was implemented
  - Merged current `origin/main` at `6e01bee689bbddbffbd920632f07e6b09586cf5c` into the ACP split branch, preserving the task-scoped `prd.json` and both branches' append-only `progress.txt` histories.
  - Published merge commit `2292b2696`; the PR has no conversation comments, reviews, or unresolved inline threads.
- Files changed
  - `progress.txt`
- Verification
  - Merged-head normalized ACP stdio test inventory — 222 tests before and after baseline `2943d7328ed6c11e37e2c138346124e9b3b744bf`, zero diff.
  - Merged-head ACP stdio coverage — 94.3% before and after.
  - Focused package tests, three-run concurrent-attachment race test, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, `make typecheck`, PRD/baseline JSON validation, and `git diff --check` — passed.
  - The broad `make test` attempt reached its 300-second wrapper timeout without a result; its exact spawned process tree was stopped after the timeout. This did not affect the focused ACP evidence or structural gates.
- **Learnings for future iterations:**
  - Recheck PR mergeability after pushing a current-main merge; a newly created PR can report conflicts immediately when its branch starts behind the live base.
  - On this Windows checkout, broad `make test` may outlive the wrapper while focused package and repository enforcement gates remain responsive; record the timeout separately from deterministic pass/fail evidence.
---
## [2026-08-10 14:56 -07:00] - thr-split-acp-stdio-tests-003 current-main follow-up
- What was implemented
  - Merged the next live `origin/main` advance at `78584efb52dacb6989604f8f7cc6c12ed08da028` as merge commit `9ee8f4c01`; retained the ACP task PRD and append-only progress history through the second stale-base reconciliation.
- Files changed
  - `progress.txt`
- Verification
  - Focused ACP stdio package tests and the three-run concurrent-attachment race test — passed.
  - `go vet ./pkg/transports/acp/internal/stdio`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, `make typecheck`, PRD validation, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - Re-fetch `origin/main` immediately before the final PR-state check; another merged PR can advance the base between a successful merge and GitHub's mergeability calculation.
---
## [2026-08-10 15:15 -07:00] - thr-split-acp-stdio-tests-003 CI follow-up
- What was implemented
  - Investigated the initial PR CI failure and confirmed it was an unrelated intermittent failure in `tests/functional/workers/mock/TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle`; the ACP stdio split does not touch that package or runtime path.
  - Reran the failed GitHub Actions jobs without source changes. Run `31436174213` completed successfully, including Backend Functional Coverage and Verification Policy.
- Files changed
  - `progress.txt`
- Verification
  - The isolated failing functional test passed locally with `go test -run '^TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples$' -count=1 -v ./tests/functional/workers/mock`.
  - GitHub Actions retry `https://github.com/portpowered/you-agent-factory/actions/runs/31436174213` — passed.
- **Learnings for future iterations:**
  - When a broad functional lane fails on a test outside the changed package, run that exact test locally and retry the failed CI job before introducing unrelated fixes.
---
## [2026-08-10 15:29 -07:00] - thr-split-acp-stdio-tests-003 final CI follow-up
- What was implemented
  - Investigated the final-head CI browser failure and identified an unrelated timing timeout in `integration/maintainer-phantom-worker-graph.integration.test.mjs`; the remaining 33 browser tests and all backend/API/package lanes passed.
  - Reran only the failed Frontend Browser job. The retry passed, and the dependent Verification Policy passed on the same final head.
- Files changed
  - `progress.txt`
- Verification
  - Final-head GitHub Actions run `https://github.com/portpowered/you-agent-factory/actions/runs/31437495633` — passed after the browser retry, including Backend Functional Coverage, API/package lanes, UI Backend Integration, and Verification Policy.
- **Learnings for future iterations:**
  - Browser integration suites can intermittently miss a UI state within their assertion timeout under concurrent CI load; isolate and retry the failed browser job when all changed-surface checks remain green.
---
## [2026-08-10 15:41 -07:00] - thr-split-acp-stdio-tests-003 current-main reconciliation
- What was implemented
  - Merged the latest `origin/main` advance at `e8c0a248ae1ba354ccc2fc022fa674feefe52f85` as merge commit `a0c703bb1` and retained the task-scoped `prd.json` while preserving the append-only progress history.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused ACP stdio package tests and the three-run concurrent-attachment race test — passed.
  - `go vet ./pkg/transports/acp/internal/stdio`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `make typecheck` — passed.
  - Current ACP stdio inventory remained 222 tests with 94.3% coverage; PRD JSON validation and `git diff --check` — passed.
- **Learnings for future iterations:**
  - Fetch `origin/main` immediately before handoff and resolve the task PRD explicitly when the shared progress/PRD files conflict during stale-base reconciliation.
---
## [2026-08-10 16:01 -07:00] - thr-split-acp-stdio-tests-003 current-main CI blocker
- What was implemented
  - Investigated the final reconciled-head CI failure and confirmed it is outside the ACP split: Backend Functional Coverage failed while all other final-head lanes completed successfully.
  - Retried Backend Functional Coverage once. The initial attempt reported `pkg/transports/cli` at 72.8180% against the 72.98% minimum after current-main added CLI lease-contention diagnostic branches; the retry also exited nonzero while producing the functional coverage profile.
- Files changed
  - `progress.txt`
- Verification
  - ACP stdio focused tests, race test, vet, repository gates, typecheck, inventory, and coverage remain green on the reconciled head.
  - The same current-main commit `e8c0a248ae1ba354ccc2fc022fa674feefe52f85` has its own failed Backend Functional Coverage CI run (`31438826294`), confirming a base-branch blocker rather than an ACP split regression.
  - No PR conversation comments, formal reviews, or unresolved inline threads are present.
- **Learnings for future iterations:**
  - When current-main itself fails the same required functional lane, preserve the scoped move-only change and leave the PR on the continue path until the base coverage regression is repaired or the repository lane is green.
---
## [2026-08-10 16:21 -07:00] - thr-split-acp-stdio-tests-003 PR handoff audit
- What was implemented
  - Audited existing PR #1861 against the latest branch: all three PRD stories remain `passes: true`, the branch is synchronized with `origin`, and the thread-aware review audit found no conversation comments, reviews, or unresolved inline threads.
  - Replaced the PR description through the repository-scoped `gh api` fallback so it is exactly the contents of `prd.json`; no production or test source changed.
- Files changed
  - `progress.txt`
- Verification
  - PR body comparison — byte-identical with `prd.json` at 9,931 bytes.
  - Current PR checks — all selected lanes passed except Backend Functional Coverage and its dependent Verification Policy; the failing lane exits on the known `origin/main` coverage regression at base `e8c0a248ae1ba354ccc2fc022fa674feefe52f85`.
  - `python .../fetch_comments.py` — zero conversation comments, reviews, and unresolved inline threads.
  - Worktree status and `git diff --check` — clean before this progress append; no source changes were introduced.
- **Learnings for future iterations:**
  - PR metadata can be corrected without a source commit, but the handoff is not complete while a required check remains red; keep the PR on the continue path until current-main coverage is green or explicitly repaired by its owning change.
---
## [2026-08-10 16:32 -07:00] - thr-split-acp-stdio-tests-003 review-ready handoff
- What was implemented
  - Confirmed the fresh CI run passed all 18 jobs, including Backend Functional Coverage and Verification Policy.
  - Confirmed PR #1861 is mergeable and marked it ready for review after the empty thread-aware review audit; all three PRD stories remain `passes: true`.
- Files changed
  - `progress.txt`
- Verification
  - GitHub Actions run `31442041604` — all required checks passed.
  - PR state — open, ready for review, `MERGEABLE`, `CLEAN`, head `1008faa7440c9406e8968ff855dad7f276c78e61`.
  - PR body — byte-identical to `prd.json` at 9,931 bytes.
  - Conversation/review audit — zero top-level comments, formal reviews, and unresolved inline threads.
- **Learnings for future iterations:**
  - Once the latest head is green and review threads are empty, `gh pr ready` is the final state transition needed to hand a draft PR to reviewers.
---
## [2026-08-10 16:58 -07:00] - thr-split-acp-stdio-tests-003 final coverage blocker
- What was implemented
  - Investigated the latest-head Backend Functional Coverage failure and confirmed it is the same unrelated `pkg/transports/cli` coverage-floor regression on attempts 2 and 3: actual 72.8180% versus the 72.98% manifest minimum.
  - Retried only the failed backend job; the retry remained red, so no unrelated CLI baseline or production-code change was introduced.
- Files changed
  - `progress.txt`
- Verification
  - The prior final-head run `31442041604` passed all 18 jobs, while the current final-head run `31442775623` reproduced only the CLI functional coverage-floor failure; all other jobs passed.
  - The PR remains open, ready for review, and has no conversation comments, formal reviews, or unresolved inline threads.
  - All three PRD stories remain `passes: true`; the PR body remains byte-identical to `prd.json`.
- **Learnings for future iterations:**
  - A repeated coverage-floor result on an unchanged source head should be recorded as a CI/base blocker; do not lower a package minimum or add unrelated tests merely to make a scoped PR green.
---
## [2026-08-10 17:11 -07:00] - thr-split-acp-stdio-tests-003 post-rebase CI blocker
- What was implemented
  - Rebased the PR branch by merging the live `origin/main` head `acbd13fcc960926c6118d2e3167426a71345ace6`, resolved only the shared task bookkeeping conflicts, and preserved the ACP PRD plus both append-only progress histories.
  - Rechecked the required CI on merged head `00c00a511`; Backend Functional Coverage reproduced the same unrelated CLI coverage-floor regression, so no CLI baseline relaxation or unrelated production/test change was made.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files from the merge: `tests/fixtures/javascript_runtime/resumable-two-step-fake-children.workflow.js` and `tests/functional/orchestration/javascript/durability/resume_test.go`
- Verification
  - GitHub Actions run `31444646791`: Backend Unit Coverage, all frontend/API/package/integration lanes, and long local inference passed; Backend Functional Coverage failed only at `pkg/transports/cli` actual 72.8180% versus minimum 72.98%, with dependent Verification Policy failing.
  - Current PR state is mergeable without conflicts; all three ACP PRD stories remain `passes: true`, the PR body remains byte-identical to `prd.json`, and the thread-aware review audit remains empty.
- **Learnings for future iterations:**
  - Resolve stale-base conflicts in task-owned bookkeeping files by keeping the active task PRD authoritative and unioning both append-only progress histories; then rerun CI on the merge commit before claiming review readiness.
  - Focused remote-placement tests, complete `./pkg/transports/cli`, and complete `./pkg/transports/cli/run` suites — passed.
  - `make pkg-file-count` and staged/working-tree `git diff --check` — passed.
- **Learnings for future iterations:**
  - When current main mechanically deletes an oversized test file, carry task-specific tests into a focused same-package file before resolving the deletion; this preserves behavior without resurrecting the old contention hotspot.
---

## 2026-08-10 16:47 -07:00 - dwro-s1-remote-placement-flags current-main synchronization and verification
- What was implemented
  - Merged the live `origin/main` head `acbd13fcc` into the placement branch in merge commit `8916cf53f`.
  - Preserved the task-owned S1 `prd.json` with all four stories marked `passes: true`, and retained the combined append-only `progress.txt` history while resolving the shared bookkeeping conflict.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main packaged-installation, system-initialization, CLI, JavaScript durability, and hard-kill functional-test files included by the synchronization merge.
- Verification
  - `make test` — passed in 125.8 seconds.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make pkg-boundary` on the replacement head and a detached `origin/main` checkout — both report the same intentional 175-entry package-boundary baseline, including 154 exact Petri public-surface migration edges; neither adds a new head-only finding.
  - `prd.json` parses with all four user stories marked `passes: true`; merge conflict markers are absent; `git diff --check` passed before the verification entry was appended.
- **Learnings for future iterations:**
  - When a PR becomes unmergeable because the base advances, fetch the live remote base rather than relying on cached PR metadata, merge it into the task branch, keep the active PRD authoritative, and compare any intentional repository-wide lint baseline against a detached checkout of that exact base.
---

## [2026-08-10 15:40 -07:00] - thr-split-fake-service-runtime-tests-005 current-main synchronization
- What was implemented
  - Merged current `main` at `e8c0a248a` into the split branch after GitHub reported the PR base had advanced beyond the earlier synchronization point.
  - Preserved the task-owned five-story `prd.json` and unioned the append-only progress history and reusable patterns from both branches; no test-split source files conflicted.
- Files changed
  - Current-main files carried forward by the merge, plus `prd.json` and `progress.txt` conflict resolution.
- Verification
  - Merge committed as `0dfcbf754`; all five task stories remain `passes:true`.
  - The previously completed PR workflow was green before this required current-main synchronization; focused gates will be rerun on the new head.
- **Learnings for future iterations:**
  - A PR can report a stale cached base SHA after another main merge; compare both the hosting repository's live main ref and the PR base, then synchronize and rerun CI on the resulting head.
---

## [2026-08-10 15:42 -07:00] - thr-split-fake-service-runtime-tests-005 merged-head gates
- What was implemented
  - No source changes after synchronization; verified the move-only split against current `main` and confirmed both retired giant test files remain absent.
- Files changed
  - No additional source files.
- Verification
  - `go test ./pkg/services/factory_sessions/internal/execution/...`, `go vet ./pkg/services/factory_sessions/internal/execution/...`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `make typecheck` — passed.
  - The final inventory comparison remains 269 baseline entries versus 269 current entries with zero differences.
  - `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` remains 79.6% execution, 80.0% fixtures, 83.1% recordingreplay, and 89.8% runtimepersist; `git diff --check` passed.
- **Learnings for future iterations:**
  - After reconciling current-main changes, rerun both focused behavior checks and the repository ratchets before relying on the prior PR workflow result.
---

## [2026-08-10 15:59 -07:00] - thr-split-fake-service-runtime-tests-005 CI flake diagnosis
- What was implemented
  - Investigated the final-head Backend Functional Coverage failure in untouched `tests/functional/events/response_events/stream_test.go`.
  - Confirmed the failure was the timing-sensitive `TestAPIResponseEventStreamClosesAtDocumentedBoundary` observation that a completed session retained zero Response Events before stream open; no split-file code or assertion changed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/events/response_events -run '^TestAPIResponseEventStreamClosesAtDocumentedBoundary$' -count=10 -v -timeout 300s` — passed all ten runs.
  - The failed PR job is isolated to this existing functional test; the other final-head checks passed.
- **Learnings for future iterations:**
  - When broad functional coverage reports a retained-response timing failure in an untouched stream test, run that exact test repeatedly before changing behavior and rerun the failed CI lane on the latest head.
---

## [2026-08-10 16:11 -07:00] - thr-split-fake-service-runtime-tests final handoff audit
- What was implemented
  - No additional story implementation was needed; all five PRD stories remain marked `passes:true`.
  - Audited PR #1862 and marked the existing draft PR ready for review.
- Files changed
  - `progress.txt`
- Verification
  - PR #1862 is open, mergeable, and ready for review with title and branch `thr-split-fake-service-runtime-tests`.
  - Local `HEAD`, the fork branch, and the PR head all match `431bf9b505e78b436e0bbc636157ac5e4d40efc2`.
  - The PR base matches current `origin/main` at `e8c0a248ae1ba354ccc2fc022fa674feefe52f85`.
  - The PR body matches `prd.json` byte-for-byte; both SHA-256 values are `D75EFC3B16ECA4ED660C02D18D9BDC6B376EAAA426241E6291A31995036FAB45`.
  - Thread-aware GraphQL audit found zero reviews, conversation comments, and inline review threads; all required CI checks are terminal and successful.
- **Learnings for future iterations:**
  - A completed split can be handed off without source changes when the PRD, PR body, head/base SHAs, review-thread state, and required checks are independently synchronized and verified.
---

## [2026-08-10 16:27 -07:00] - thr-split-fake-service-runtime-tests final CI rerun
- What was implemented
  - Investigated the post-handoff Backend Functional Coverage failure and confirmed it was the unrelated `TestFactorySessionHistoryIsNotPersistedByDefault` cancellation-observation flake in `tests/functional/sessions/restart`.
  - Made no production or test-code changes; reran the failed GitHub jobs on the unchanged split head after the isolated test passed ten consecutive local runs.
- Files changed
  - `progress.txt`
- Verification
  - Isolated `go test ./tests/functional/sessions/restart -run '^TestFactorySessionHistoryIsNotPersistedByDefault$' -count=10 -v -timeout 300s` — passed all ten runs.
  - Rerun Backend Functional Coverage — passed in 4m58s.
  - Rerun Verification Policy — passed.
  - All required checks on PR #1862 are terminal and successful.
- **Learnings for future iterations:**
  - A broad functional-coverage failure in an untouched package should be isolated and repeated before changing the task; a clean repeated characterization supports rerunning the failed CI lane while preserving the move-only diff.
---

## [2026-08-10 16:43 -07:00] - thr-split-fake-service-runtime-tests final coverage-ratchet rerun
- What was implemented
  - Investigated the second post-push Backend Functional Coverage failure and confirmed it was an unrelated `pkg/transports/cli` package-coverage measurement of 72.818% against the existing 72.98% minimum; no split source or assertion changed.
  - Reran the failed GitHub jobs on the unchanged head and preserved the existing coverage baseline.
- Files changed
  - `progress.txt`
- Verification
  - Rerun Backend Functional Coverage — passed in 5m23s.
  - Rerun Verification Policy — passed.
  - All required checks on PR #1862 are terminal and successful.
- **Learnings for future iterations:**
  - A small package-coverage-ratchet delta on an unchanged broad suite can be run-to-run variance; rerun the lane before modifying a baseline or unrelated code.
---

## [2026-08-10 16:47 -07:00] - thr-split-fake-service-runtime-tests current-main synchronization
- What was implemented
  - Merged the live `origin/main` head `acbd13fcc` into the split branch after main advanced during final CI monitoring.
  - Preserved this task’s five-story `prd.json`, unioned the append-only progress history and reusable patterns from both branches, and retained main’s unrelated durability source/test changes.
- Files changed
  - `progress.txt`
  - Current-main durability fixture and functional test files carried forward by the merge.
- Verification
  - `prd.json` parses as the split PRD with all five stories marked `passes:true`.
  - `git ls-files -u` is empty, conflict markers are absent, and `git diff --cached --check` passes.
  - The synchronization target is `origin/main` at `acbd13fcc960926c6118d2e3167426a71345ace6`; final focused and required CI gates will be rerun on the merged head.
- **Learnings for future iterations:**
  - If main advances while a PR’s required checks are running, merge the live main ref before handoff and preserve both task metadata histories so the PR base and progress evidence remain auditable.
---

## [2026-08-10 16:55 -07:00] - thr-split-fake-service-runtime-tests final PR handoff
- What was implemented
  - No additional story implementation was needed; all five PRD stories remain marked `passes:true`.
  - Audited the existing PR #1862 and confirmed that the requested branch is open, non-draft, synchronized with the current main base, and has no review, conversation, or inline-thread feedback to address.
- Files changed
  - `progress.txt`
- Verification
  - `gh pr checks 1862 --watch` completed with every required check passing, including Backend Unit Coverage, Backend Functional Coverage, API Contract And Package, Frontend, UI Backend Integration, Local Inference, and Verification Policy.
  - The PR body remains sourced from `prd.json`; local HEAD and the fork branch remain `2560aee673a65b2e60b89a6069b3f60b4b95fdb8`, and the PR base remains current `origin/main` at `acbd13fcc960926c6118d2e3167426a71345ace6`.
  - The thread-aware GraphQL audit reports zero reviews, conversation comments, or inline review threads; the worktree was clean before this append.
- **Learnings for future iterations:**
  - When all story work is complete and an existing PR is already review-ready, preserve the move-only source diff and record the final PR head/base, review-thread, PRD/body, and terminal-CI checks in the append-only progress log.
## 2026-08-10 16:18 -07:00 - thr-worktree-build-cache-002 PR scope follow-up
- What was implemented
  - Reconciled `progress.txt` with the live `origin/main` after PR #1860 review identified unrelated merged-task history as out-of-scope churn.
  - Retained the build-cache PRD and root Makefile change; appended only the task-specific review follow-up instead of restoring unrelated progress entries.
- Files changed
  - `progress.txt`
- Verification
  - `git diff origin/main...HEAD --stat` is now limited to `Makefile`, `prd.json`, and this task-specific progress entry.
  - The PR review's build-cache correctness, parity, throughput, and required-CI criteria remain satisfied; the blocking scope issue was the shared progress-file churn.
  - `prd.json` still parses with both `thr-worktree-build-cache-001` and `thr-worktree-build-cache-002` marked `passes:true`.
- **Learnings for future iterations:**
  - When a shared append-only log is merged across task branches, reconcile it against the live base before handoff and preserve only entries owned by the current PR.
---

## 2026-08-10 16:46 -07:00 - thr-worktree-build-cache-002 merge follow-up
- What was implemented
  - Merged the current `origin/main` (`acbd13fcc`) into the build-cache branch after PR review identified a live `prd.json` conflict.
  - Kept this branch's build-cache PRD, accepted current-main progress history, and retained only the task-scoped progress follow-up in the live-base diff.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main durability changes included by the synchronization merge.
- Verification
  - The merge conflict is resolved and `prd.json` parses with both build-cache stories marked `passes:true`.
  - The live-base diff remains limited to `Makefile`, `prd.json`, and the task-specific `progress.txt` entry; current-main test changes are merge-base synchronization only.
  - Focused final checks and the post-merge CI run remain to be completed before handoff.
- **Learnings for future iterations:**
  - After fetching a moving base, merge it before final review and preserve the active task PRD when the shared `prd.json` is owned by another task on main.
---

## 2026-08-10 16:49 -07:00 - thr-worktree-build-cache-002 verification follow-up
- What was implemented
  - Completed post-merge verification for the corrected cache policy and current-main synchronization.
- Files changed
  - `progress.txt`
- Verification
  - `make test` passed.
  - `make typecheck` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - Dry-run `make build` and `make install` omit `-a`; explicit `GO_BUILD_FLAGS=-a` retains the forced-rebuild control.
  - The working tree remains clean before push, and the PRD still has both stories marked `passes:true`.
- **Learnings for future iterations:**
  - After a base-sync merge, rerun the repository gates locally before pushing so the new PR head has direct evidence in addition to the earlier green CI run.

---
## [2026-08-10 17:24 -07:00] - thr-split-acp-stdio-tests-003 final handoff audit
- What was implemented
  - Re-audited the completed ACP stdio split against the live PR; all three PRD stories remain marked `passes: true`, so no additional story source change was needed in this iteration.
  - Confirmed the existing PR is ready for review and has no actionable review feedback.
- Files changed
  - `progress.txt`
- Verification
  - PR #1861 is open, non-draft, `MERGEABLE`, and `CLEAN` against current `origin/main`.
  - All 18 reported GitHub Actions checks passed, including Backend Functional Coverage and Verification Policy.
  - Thread-aware review audit found zero conversation comments, formal reviews, and unresolved inline threads.
  - The PR body is byte-identical to `prd.json` at 9,955 UTF-8 bytes.
  - Worktree was clean before this append; `git diff --check` passed.
- **Learnings for future iterations:**
  - Once the PRD is complete and the live PR has green required checks with no review threads, the remaining handoff work is limited to preserving the audit evidence and keeping the branch synchronized.

---
## [2026-08-10 17:52 -07:00] - thr-split-acp-stdio-tests-003 CI follow-up
- What was implemented
  - Investigated the final-head Backend Functional Coverage failure and confirmed each failed attempt was an unrelated intermittent functional test outside the ACP stdio split.
  - Retried only the failed GitHub job three times; the fourth attempt completed successfully. No production, test, contract, or baseline changes were introduced.
- Files changed
  - `progress.txt`
- Verification
  - Isolated local reruns passed for `TestProvidersACPRestartsAfterCrashWithoutReplayingUncertainPrompt`, `TestJavaScriptParallelDispatchesChildrenConcurrently`, `TestAgyConductorSuccessThroughRootBuildProcess`, `TestBTRCP0OneShotCancellationCharacterization`, and `TestCodexGoldenDerivesProviderSessionAndResponseEvents`.
  - GitHub Actions run `31445974775`, attempt 4 — all 18 checks passed, including Backend Functional Coverage and Verification Policy.
  - The PR remains open, non-draft, mergeable, and clean; no review feedback or unresolved threads were found.
- **Learnings for future iterations:**
  - For a broad functional lane that fails on changing unrelated tests, isolate each named test locally and retry only the failed job before considering any change to the scoped implementation.
---

## 2026-08-10 17:21 -07:00 - e2-structured-worker-results current-main synchronization follow-up
- What was implemented
  - Merged the newly live `origin/main` at `e6797fd5d` after the base advanced through the build-cache PR, preserving the E2 `prd.json`, all append-only histories, and the current-main `Makefile` change.
  - No additional structured-result product implementation was required; all four E2 stories remain `passes:true`.
- Files changed
  - `Makefile`
  - `progress.txt`
  - `prd.json` conflict resolution retained the E2 task PRD
- Verification
  - `make test` — passed.
  - `make typecheck backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make pkg-boundary` reports the unchanged 175-entry known baseline.
  - `prd.json` parses with four passing stories; conflict-marker scan and `git diff --check` passed.
- **Learnings for future iterations:**
  - Re-fetch `origin/main` immediately before pushing: a PR can be clean against one base and become stale again while waiting for GitHub checks.
---

## 2026-08-10 18:07 -07:00 - e2-structured-worker-results review and CI follow-up
- What was implemented
  - Audited PR #1855's latest blocking review feedback; the reported transitioner size regression is cleared on the current head (`subsystem_transitioner.go` is 959 lines), and no unresolved review threads remain.
  - Confirmed the existing PR body is an exact byte-for-byte match for the task-scoped `prd.json`; all four stories remain `passes:true`, so no PRD change was required.
  - Diagnosed the newest Backend Functional Coverage failure as the unchanged Linux process-boundary test `TestBuiltCLIDeclaredCancellationExitCodes/continuous_run`; the PR has no diff in that test package and the focused test is skipped on this Windows checkout.
- Files changed
  - `progress.txt`
- Verification
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make lint` — all prerequisite checks passed; it reached only the known 175-entry `pkg-boundary` baseline, matching current `origin/main`.
  - Focused cancellation test — passed repeatedly locally (skipped on Windows because child-process `os.Interrupt` is unsupported).
  - `git diff --check` — passed; `origin/main` is the current branch base and the worktree was clean before this log entry.
- **Learnings for future iterations:**
  - When a required CI lane fails in an unchanged process-boundary package, isolate the named test and retry the failed GitHub lanes before altering feature code.
---
## 2026-08-10 18:04 -07:00 - dwro-s1-remote-placement-flags review follow-up
- What was implemented
  - Merged current `origin/main` (`e6797fd5`) while preserving the S1-owned `prd.json` and unioning the append-only progress history.
  - Consolidated the remote placement adapter and its focused behavioral tests into existing `run` package owners, and moved the root placement tests into the existing root run-flags test owner.
  - Restored the exact current-main deletion-only package baselines instead of increasing them for the new files.
- Files changed
  - `docs/internal/baselines/backend-package-file-count.json`
  - `pkg/transports/cli/root_run_flags_test.go`
  - `pkg/transports/cli/run/invocation_error.go`
  - `pkg/transports/cli/run/run_invocation_output_test.go`
  - `pkg/transports/cli/root_run_remote_placement_test.go` (removed after consolidation)
  - `pkg/transports/cli/run/remote.go` and `remote_test.go` (removed after consolidation)
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli ./pkg/transports/cli/run -count=1` — passed.
  - `make pkg-file-count pkg-maint backend-size pkg-structure` and `go vet ./...` — passed.
  - `prd.json` parses with all four stories marked `passes:true`; merge conflict markers are absent.
- **Learnings for future iterations:**
  - Deletion-only package baselines are ratchets, not capacity budgets; keep them at the exact current-main count and consolidate small additions into an existing focused owner.
  - During a moving-base merge, keep the active task PRD authoritative and preserve both branches' append-only progress entries before validating the replacement head.
---

## 2026-08-10 18:37 -07:00 - dwro-s1-remote-placement-flags-002 review follow-up
- What was implemented
  - Made `safeRemoteEndpoint` fail closed after parse-success/base-invalid endpoint inputs by validating the credential-stripped value through the shared HTTP(S) server-base resolver.
  - Added redaction coverage for opaque HTTPS, unsupported-scheme, malformed, and malformed-userinfo endpoint values.
- Files changed
  - `pkg/transports/cli/run/invocation_error.go`
  - `pkg/transports/cli/run/run_invocation_output_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/cli/run -count=1` — passed.
  - `make pkg-maint pkg-file-count backend-size pkg-structure vet` — passed.
  - `make test` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Redacting URL userinfo is not sufficient: parse-success values can still carry sensitive text in opaque or otherwise invalid URL components, so diagnostic labels must be produced only after shared server-base validation and normalization.
## [2026-08-10 17:30 -07:00] - thr-split-fake-service-runtime-tests-005 current-main conflict follow-up
- What was implemented
  - Merged authoritative `upstream/main` at `e6797fd5de547aad6cc8c3ef22e3ca3fc486c713` to address the blocking PR conflict.
  - Preserved this task's five-story `prd.json` and retained both append-only progress histories, including the current-main build-cache entries; current-main's Makefile change remains carried forward.
- Files changed
  - `Makefile` (current-main synchronization)
  - `progress.txt`
- Verification
  - `prd.json` parses as the Factory Sessions split PRD with all five stories marked `passes:true`; merge conflict markers and unmerged index entries are absent, and `git diff --cached --check` passes.
  - `go test ./pkg/services/factory_sessions/internal/execution/...` passes.
  - Before/after `go test -list '.*' ./pkg/services/factory_sessions/internal/execution/...` inventory comparison remains 269 entries on each side with zero differences and matching sorted SHA-256 `09876A7E761EAB43E48DD3729153FCCAE52802F37CC95B424B706AAFA3869B51`.
  - `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` matches the baseline: execution 79.6%, fixtures 80.0%, recordingreplay 83.1%, runtimepersist 89.8%.
  - `make typecheck backend-size pkg-maint pkg-file-count pkg-structure vet` passes.
  - The first `make test` attempt hit the unrelated Windows PTY timing test `TestAgyLegacyPTYCompatibilityThroughProvidersWire/deadline_terminates_native_process`; that exact test passed 10/10 in isolation, and the immediate full `make test` rerun passed.
- **Learnings for future iterations:**
  - After synchronizing a moving main branch, validate both the merge bookkeeping and the task behavior evidence before pushing; when a broad unit lane reports a timing failure in an untouched test, isolate and repeat the named test before changing task code.
---
## [2026-08-10 18:12 -07:00] - thr-split-fake-service-runtime-tests review follow-up
- What was implemented
  - Addressed the two blocking PR findings: restored the exact deletion-only execution package file-count baseline from 39 to 28, and restored the flagged test/fixture declarations byte-for-byte apart from relocation.
  - Mechanically consolidated adjacent same-package test owners while keeping the focused construction, dispatch, lifecycle, replay, runtime start/result/persistence/control/checkpoint/validation-event groups and the external-package boundary tests within the existing 28-file budget.
  - Updated the story notes in `prd.json` so the PR description records the final file budget, inventory hash, coverage, and review-follow-up evidence; all five stories remain `passes:true`.
- Files changed
  - `pkg/services/factory_sessions/internal/execution/` focused test owners and mechanical source-file retirements
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Sorted before/after `go test -list '.*' ./pkg/services/factory_sessions/internal/execution/...` inventory — 269 entries on both sides, SHA-256 `09876A7E761EAB43E48DD3729153FCCAE52802F37CC95B424B706AAFA3869B51` on both sides, diff empty.
  - Exact `go test -covermode=atomic ./pkg/services/factory_sessions/internal/execution/...` — unchanged at 79.6% (execution), 80.0% (fixtures), 83.1% (recordingreplay), and 89.8% (runtimepersist).
  - Focused package suite, full `make test` (311.9s), `go vet`, `make typecheck`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` — passed.
  - On-disk execution package count is exactly 28; every resulting hand-maintained Go file is below the 1,000-line maintainability threshold and the PRD’s 1,200-line cap.
  - Review-fix commit: `5cde8e22b` (`test(factory-sessions): preserve split file budget`).
- **Learnings for future iterations:**
  - Treat the package file-count baseline as a deletion-only ratchet even when a split is mechanically correct; use existing responsibility-aligned test owners and verify both the 1,000-line maintainability gate and the broader 1,200-line task cap.
  - When review identifies whitespace drift in a move-only split, compare the named declarations directly against the unmodified source and restore the exact text before rerunning inventory and coverage.
 ---

## [2026-08-10 18:54 -07:00] - thr-split-acp-stdio-tests-003 current-main merge and CI follow-up
- What was implemented
  - Synchronized the ACP stdio test-split branch with live `origin/main` at `0b9771fe85fd1aedf9968d636de5e72cd193970c` after the base advanced.
  - Preserved this task's authoritative `prd.json` and unioned both append-only progress histories while carrying the current-main factory-session execution test split.
  - No ACP production code, public contract, generated artifact, or test assertion behavior was changed.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main factory-session execution test split and exemption-budget files carried by the merge.
- Verification
  - `prd.json` parses successfully and all three ACP stories remain `passes:true`; merge conflict markers and unmerged index entries are cleared.
  - Isolated local reruns passed for `TestFactoryRunRetriesACPProviderByResumingExactSession`, `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`, and `TestWorkRecoveryActivatesThroughRootBuildProcessAfterLifecycle`.
  - GitHub Actions run `31447539078`, attempt 7 — all 18 checks passed, including Backend Functional Coverage, pinned ACP evidence, and Verification Policy, on the pre-merge branch head.
  - PR #1861 has no top-level comments, reviews, or unresolved inline threads; `git diff --check` passes.
- **Learnings for future iterations:**
  - Fetch the live base immediately before final handoff because a green run can become conflicted when another PR lands.
  - When `prd.json` and an append-only progress log conflict, keep the active task's PRD and concatenate both histories rather than dropping unrelated log entries.
---

## [2026-08-10 19:18 -07:00] - thr-split-acp-stdio-tests post-merge CI completion
- What was implemented
  - Completed the post-merge CI follow-up for PR #1861 on head `45e43abbff81e552b51161f02048d0e00e2cac2c`.
  - Retried only the failed Backend Functional Coverage job after the initial run exposed an unrelated `pkg/transports/cli` coverage-floor fluctuation and its dependent policy failure.
- Files changed
  - `progress.txt`
- Verification
  - Local `go test -p 1 -vet=off ./pkg/transports/acp/internal/stdio` passed after the current-main merge.
  - GitHub Actions run `31450791786`: the initial full matrix encountered the pre-existing CLI coverage floor (72.8180% vs 72.98%); the first targeted retry encountered the unrelated no-dependent-packages coverage-lane failure for `pkg/transports/mcp/discoverygen`; the second targeted retry passed Backend Functional Coverage and Verification Policy.
  - The final post-merge run conclusion is successful, and no ACP-specific job or review feedback failed.
- **Learnings for future iterations:**
  - When a broad coverage job fails outside the changed package, preserve the implementation and retry only that job; the same job can surface different unrelated coverage-lane flakes across attempts.
  - Record the final successful attempt separately from the initial failure so CI history remains auditable.
## 2026-08-10 18:03 -07:00 - wsr-001-opening-record-001
- What was implemented
  - Completed the post-push CI follow-up. The first run exposed a 0.162 percentage-point functional coverage fluctuation in `pkg/transports/cli` (72.8180% versus the 72.98% floor); the green current-main run and prior green WSR run both covered 883/1203 statements, while the failed run covered 876/1203.
  - Re-ran only the failed GitHub Actions jobs after confirming no root CLI source changed in the WSR diff; the fresh Backend Functional Coverage and Verification Policy jobs passed.
- Files changed
  - `progress.txt`
- Verification
  - All 17 required PR checks for head `add05ef4d` are terminal and successful after the failed-job rerun.
  - Downloaded CI coverage artifacts and confirmed the failing result was limited to five `root_factory.go` blocks; no WSR assertion or package test failure was reported.
- **Learnings for future iterations:**
  - For a tiny ratcheted coverage miss, compare the failed profile with current-main and the previous green profile before adding tests or changing a baseline; a failed run may be a measurement fluctuation when the changed source is outside the affected package.
---

## 2026-08-10 17:30 -07:00 - wsr-001-opening-record-001
- What was implemented
  - Completed the latest review follow-up for the opening-first invariant: the existing terminal publication guard requires a committed opening, and the fail-first/succeed-second Events regression verifies that an opening append failure cannot create terminal-only history.
  - Merged the live `origin/main` (`e6797fd5e`) into the WSR branch, retained the WSR PRD while incorporating current-main progress, and resolved the shared `prd.json` conflict.
- Files changed
  - `Makefile` (current-main synchronization)
  - `progress.txt`
  - `prd.json` (merge resolution; WSR content retained)
- Verification
  - `make interfaces-all` — passed with no generated drift.
  - Worker Sessions packages, WSR-FT-001/002/003, and stream-fidelity tests — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `git diff --check` and PRD JSON validation — passed; all three stories remain `passes:true`.
- **Learnings for future iterations:**
  - When a review comment is already fixed on the branch, verify its behavioral regression and then fetch/merge the live base before pushing; a clean earlier merge can become stale while CI and review are in flight.
---

## 2026-08-10 08:50 -0700 - wsr-001-opening-record coverage follow-up
- What was implemented
  - Addressed the first PR CI rerun's Worker Sessions unit coverage ratchet failure by adding focused behavioral coverage for provider binding validation, provider attribution conflicts and duplicates, publication-state failures, append failures/duplicates, dispatch lookup, and missing constructor dependencies.
  - Kept the tests split into focused helpers so the backend maintainability limit remains satisfied.
- Files changed
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
  - `progress.txt`
  - `prd.json` remains unchanged with all three stories marked `passes:true`.
- Verification
  - `make test` — passed.
  - Package-scoped `gocoveragecheck` with the CI floor — passed at 99.1% against the 98.72% package minimum.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` — passed.
  - `go vet ./...` — passed.
  - `git diff --check` — passed.
  - The repository-wide coverage command remains host-limited on Windows because its default all-package process arguments exceed the Windows command-line limit; the equivalent scoped lane passed, and CI will validate the Linux-wide lane after push.
- **Learnings for future iterations:**
  - Coverage regressions can be isolated by reading the package manifest's uncovered functions, then testing their externally visible error/result contracts through the narrowest real service setup.
  - Keep coverage-only edge tests in focused subtests/helpers; the package maintainability gate counts test complexity as well as production complexity.
---

## 2026-08-10 10:53 -0700 - wsr-001-opening-record CI follow-up
- What was implemented
  - Registered the provider-neutral Worker Session-ID REST/SSE operations in the reviewed functional-scenario manifest and evidence registry, and made the generic OpenAPI route-reachability fixture resolve `{worker_session_id}` with a deliberately invalid-but-routable value so the handler returns a structured response instead of a 404.
  - Added focused service and HTTP handler coverage for invalid context/request inputs, missing sessions, public provider-neutral replay, subscription failures, and response-writer/adapter boundaries, restoring both package coverage ratchets.
  - Repaired the current-main CLI dispatch functional test double to read prompts from the runtime’s argument-based Claude command shape and emit Claude-native JSONL; the root-composition functional package now passes.
- Files changed
  - `internal/functionalscenarios/manifest.go`
  - `contracts/functional-scenarios.json`
  - `contracts/functional-scenario-evidence.json`
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
  - `pkg/services/worker_sessions/transports/http/handler_test.go`
  - `tests/functional/workers/inference/opening_record_test.go`
  - `tests/functional/transport/http/server/routing_safe_requests_test.go`
  - `tests/functional/factory_runtime/root_composition/dispatch_worker_sessions_concurrency_activation_test.go`
  - `progress.txt`
- Verification
  - Focused Worker Sessions service and HTTP tests — passed.
  - Root-built WSR-FT-003, Claude full-stream, and Codex partial-stream tests — passed.
  - OpenAPI route reachability, contract smoke, and root-composition functional package — passed.
- **Learnings for future iterations:**
  - The provider command seam may carry prompts in CLI arguments rather than stdin; test doubles should inspect the actual command contract and shape output for the selected provider.
  - A reviewed functional operation with an SSE response may produce separate REST and SSE scenario records, so both must be declared and covered by the same observable test.
---
- Capture the Worker Session start timestamp once at the observation boundary and reuse that exact value in the lifecycle opening payload; calling the injected clock separately for projection and publication breaks live/replay identity.
- Lifecycle metadata uses optional pointers/`omitempty` and exact typed continuation values; unknown provider, Factory, recording, and capability facts must stay absent instead of being inferred from runner or model defaults.
- Authored OpenAPI and CLI contracts generate their Go, TypeScript, published metadata, and family-manifest consumers; update the source contract first, then run the repository generators and drift checks.
- Provider-neutral output contracts belong on the workstation definition and are enforced after a successful worker response; provider process success or transport parsing alone must not satisfy a semantic report contract.
- Stream transport dispatch stays below the package maintainability threshold by delegating lifecycle-specific frame handling to focused helpers.
- Deterministic Worker Session lifecycle tests can combine the controlled workstation boundary with the real in-memory Events service to verify retained replay ordering without sleeps or cancellation.
- Detached Cobra command families should project local-only flag relationships without eagerly merging parent persistent flags; otherwise the detached storage can shadow the production root flag when the family is attached.
- Process-boundary CLI tests should build `./cmd/factory` only when proving OS-level behavior, keep the real server on a root-built process, and assign redirected stdout and diagnostics stderr separately.
- Restore package coverage thresholds with focused behavioral tests for typed edge outcomes; do not lower baseline manifests for new branches.
- Public CLI manifest additions require regenerating family metadata and the published CLI contract, then updating command input/help/tree baselines.
- Reviewed functional CLI coverage requires a sorted scenario declaration, an evidence-registry entry, a manifest mapping, and one literal `functionalevidence.Covers` marker.
- Replay completeness must be derived from a terminal lifecycle record inside the captured Events range, not from a separately synchronized terminal session state.
- When a package has a strict file-count baseline, put a small refactor in an existing focused file before adding a new helper file or changing the baseline.
- When broad functional coverage reports a cancellation-observation failure, rerun the failed lane and isolate the named test before changing an unrelated feature; parallel-suite failures can move between existing durability/restart tests while isolated runs pass.
- When a functional coverage job reports a missing evidence artifact, inspect the earlier test output first; the missing artifact can be a secondary symptom of the primary test failure.
- For cross-repository PRs, resolve review and CI state with an explicit owner/repository and compare local, fork-branch, and PR head SHAs; current-branch discovery may follow a different local origin.
- New functional scenarios must use an allowed product-domain noun plus subsection path such as `tests/functional/factory/batch`; transport names like `cli` and internal implementation names like `factory_runtime` are not durable domain cells.
- Ordered functional provider outcomes can use `support.NewShapedProviderCommandRunner` with queued provider-native `CommandResult` values, including a later non-zero result, while preserving the real command-runner edge and typed provider classification.

## 2026-08-10 11:39 -07:00 - wsr-001-opening-record functional coverage follow-up
- What was implemented
  - Resolved the current-main merge conflict while preserving this WSR PRD and the append-only progress history; the branch now includes the current `origin/main` head.
  - Added an observable functional transport regression for the public Worker Session history route when its optional Worker Sessions owner is unavailable. The test asserts the structured `500 INTERNAL_ERROR` response and covers the API-owned defensive branch without changing the canonical success path.
  - Kept the WSR-003 PRD evidence current with the additional unavailable-owner route coverage.
- Files changed
  - `tests/functional/transport/http/server/routing_safe_requests_test.go`
  - `pkg/services/worker_sessions/transports/http/handler_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make test-functional-coverage` — passed; overall coverage 55.5% and `pkg/transports/http` 68.5%, above the 66.35% package floor.
  - `TestWSRFT001OpeningRecordPrecedesProviderOutput` and `TestWSRFT002LiveAndReplayCorrelationRemainStable` — passed.
  - `TestWSRFT003ProviderNeutralLifecycleWorksWithoutProviderSession` — passed.
  - `TestAPIWorkerSessionHistoryRouteReportsUnavailableWithoutOwnerHandler` — passed.
  - Targeted coverage profile — all three previously uncovered `pkg/transports/http/server.go` unavailable-owner statements executed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet functional-boundary-check api-smoke` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract checks, UI unit tests, and the short Go suite.
  - `git diff --check` and PRD JSON validation — passed.
- **Learnings for future iterations:**
  - When a package coverage floor falls after adding a defensive transport branch, cover the public error response through the generated route and assert its observable status and error code; this preserves meaningful functional evidence instead of changing the threshold.
  - A root-built success-path test can cover transport delegation while leaving an optional-owner failure branch uncovered, so inspect the package profile before choosing the narrowest additional test.
---

## 2026-08-10 12:13 -07:00 - wsr-001-opening-record latest-main synchronization follow-up
- What was implemented
  - Merged the latest `origin/main` head `947152348` after the base advanced during PR verification, resolving the generated API conflicts through `make interfaces-all`.
  - Preserved the WSR implementation and PRD while incorporating the current main artifact-contract changes; the local merge-tree is now conflict-free.
- Files changed
  - `packages/api/generated/manifest.json`
  - `ui/packages/client/src/generated/openapi.ts`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make interfaces-all` — passed and produced no generated drift.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet functional-boundary-check api-smoke` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract checks, UI unit tests, and the short Go suite.
  - `make test-functional-coverage` — passed; overall coverage 55.7% and `pkg/transports/http` 68.5%, above the 66.35% package floor.
  - WSR-FT-001, WSR-FT-002, WSR-FT-003, public unavailable-owner route, and provider-neutral replay transport tests — passed.
  - `git merge-tree --write-tree origin/main HEAD`, `git diff --check`, and PRD JSON validation — passed.
- **Learnings for future iterations:**
  - A PR can become dirty immediately after a successful synchronization when main advances during CI; re-fetch the live base before final handoff and regenerate only the conflicted generated artifacts through the canonical interface workflow.
---

## 2026-08-10 12:29 -07:00 - wsr-001-opening-record final CI retry follow-up
- What was implemented
  - Investigated the only failure on the latest-main PR run and confirmed it was the existing timing-sensitive JavaScript cancellation observation, not a WSR or coverage regression.
  - Reproduced `TestJavaScriptDurabilityDoesNotPersistSnapshotsByDefault` successfully for 10 consecutive local runs and reran only the failed GitHub jobs; no code change was needed.
- Files changed
  - `progress.txt`
- Verification
  - GitHub Actions run `31423097483` — all required checks pass after the failed-job retry, including Backend Functional Coverage and Verification Policy.
  - PR #1849 head `7e0e04ddc` — base `947152348`, mergeable state `clean`, open and non-draft.
  - Thread-aware PR audit — no inline review threads or formal reviews; the historical blocking conversation findings are addressed by the current head.
  - PR description is synchronized with `prd.json`; `git diff --check` and PRD JSON validation pass.
- **Learnings for future iterations:**
  - When a broad coverage lane fails at an unchanged cancellation observation, isolate the exact test, repeat it locally, and retry only the failed CI jobs before modifying stable runtime behavior.
---

## 2026-08-10 13:16 -07:00 - wsr-001-opening-record package-boundary review follow-up
- What was implemented
  - Addressed the latest blocking PR feedback without changing product behavior: replaced the added internal Worker Sessions `eventswire.NewService` constructor helper with a local `EventsAppender` stub, and moved the unavailable-owner Worker Session route assertion from the functional suite into the owning HTTP transport tests.
  - Kept the customer-facing WSR functional scenarios on the required `root.BuildProcess`/`Process.Execute` path while preserving structured `500 INTERNAL_ERROR` coverage for the transport-owned defensive branch.
  - Updated the WSR-003 PRD note to record the boundary-safe test placement and committed the code/test fix as `e07f04edb`.
- Files changed
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
  - `pkg/transports/http/server_models_events_additional_test.go`
  - `tests/functional/transport/http/server/routing_safe_requests_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused Worker Sessions and HTTP transport tests — passed.
  - `make functional-boundary-check` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make test` — passed.
  - `go run ./cmd/pkgboundarycheck -root .` — reports only the known 175-entry migration baseline; the removed functional construction no longer appears and the remaining Events construction is the exact pre-existing baseline entry.
  - `make test-functional-coverage` — exceeded the local 15-minute command wrapper timeout while its Go coverage workers continued running; no result was returned by that invocation and terminal CI remains the authoritative follow-up.
  - `git diff --check` — passed before bookkeeping changes.
- **Learnings for future iterations:**
  - API-owned defensive transport assertions belong in the owning transport package; functional coverage should reserve `root.BuildProcess` for customer-visible composition and behavior.
  - A constructor-validation test that only needs a narrow port should use a local stub, even when a real in-memory peer service is convenient elsewhere in the same package.
---

## 2026-08-10 13:58 -07:00 - wsr-001-opening-record functional coverage follow-up
- What was implemented
  - Added a root-built direct JavaScript server test that sends a real public Worker Session-ID events request through the handler supplied by the process edge, then verifies the intentionally partial durable composition returns structured `500 INTERNAL_ERROR` with the internal-server error family.
  - Kept the defensive owner-unavailable assertion out of the normal functional server's direct transport construction while restoring meaningful coverage for the root HTTP transport's unavailable Worker Sessions branch.
  - Updated the WSR-003 PRD note to document both the owning transport test and the root-built direct-JavaScript exception.
- Files changed
  - `tests/functional/transport/run_scoped_server/run_scoped_server_test.go`
  - `prd.json`
- Verification
  - Focused and full `tests/functional/transport/run_scoped_server` tests — passed.
  - `make functional-boundary-check` — passed.
  - Local `make test-functional-coverage` — exceeded the 20-minute command allowance without returning a result; its coverage process was terminated after the timeout, so GitHub Actions remains authoritative for the package-floor check.
  - `git diff --check` — passed before bookkeeping; code commit `79f0c6ef3` is pushed to PR #1849.
- **Learnings for future iterations:**
  - Direct JavaScript hosting is a legitimate root-built HTTP composition with durable Factory Sessions but no live Worker Sessions owner; public route assertions there can cover composition-specific defensive behavior without importing or constructing the top-level transport in a functional test.
  - Aggregate functional coverage can exceed the local execution budget on this Windows worktree; retain focused behavioral evidence and use the terminal GitHub coverage artifact for the final package-floor result.
---

## 2026-08-10 07:25 -0700 - wsr-001-opening-record current-main verification
- What was implemented
  - Merged the latest `origin/main` at `ccea14bf8` as merge commit `9bc01fdd1`, retaining the WSR task PRD, append-only progress history, current-main runtime changes, and functional coverage.
  - Regenerated bundled, Go, package, and UI interface artifacts after synchronization; generation produced no tracked drift.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main orchestration engine and plain-batch functional changes were retained by the merge.
- Verification
  - `make interfaces-all` — passed with a clean working tree.
  - `make verify-fast` — passed dashboard typecheck, MCP contract boundary, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make pkg-structure` — passed; 573 deletion-only baseline entries remain.
  - `go test ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/... -count=1` — passed.
  - WSR-FT-001/002 and WSR-FT-003 focused functional tests — passed on the merged head.
  - `make lint` passed UI lint/dead-code, vet, backend-size, maintainability, and file-count stages, then stopped at the existing 176-entry `pkg-boundary` migration baseline; no changed WSR file was reported as a new violation.
  - `git diff --check` — passed; `prd.json` parses with all three stories still marked `passes:true`.
- **Learnings for future iterations:**
  - A current-main merge can add unrelated product tests and runtime fixes while leaving the active PRD unchanged; preserve those files and rerun both the shared fast tier and task-specific functional cells.
  - The repository lint aggregate reports the known package-boundary migration baseline after all preceding checks pass; use the focused boundary output to distinguish baseline debt from task regressions.
---

## 2026-08-10 08:28 -0700 - wsr-001-opening-record review follow-up
- What was implemented
  - Merged current `origin/main` at `11b34c073` as merge commit `c8f25ec73`; the active WSR PRD remains authoritative and current-main product/test changes are retained.
  - Corrected the provider-output bridge so canonical and raw provider observations are suppressed when Worker Sessions cannot resolve the dispatch or detects a provider-binding conflict; pre-opening provider output can no longer be forwarded ahead of `SESSION/STARTED`.
  - Split the Worker publisher identity regression into focused helpers so backend-size and maintainability gates remain within their limits.
- Files changed
  - `pkg/services/worker_sessions/publish.go`
  - `pkg/services/worker_sessions/observation_test.go`
  - `progress.txt`
  - `prd.json` remains unchanged with all three stories marked `passes:true`.
- Verification
  - `make interfaces-all` — passed with no generated drift.
  - Focused Worker Sessions, Factory Runtime, and WSR-FT-001/002/003 tests — passed.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract checks, 355 UI files / 2,970 tests, 191 Bun checks, and the short Go suite.
  - `make lint` passed all stages through backend, then stopped at the known 176-entry package-boundary migration baseline; no WSR change was reported as a new violation.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Provider output that cannot be mapped to a supervised Worker Session is not safely recoverable at the response boundary; suppress it so the customer-visible stream cannot outrun the authoritative opening.
  - Keep ordinary publication-failure forwarding behavior separate from typed dispatch-resolution failures: the former preserves downstream resilience, while the latter would violate lifecycle ordering.
  - After current-main synchronization, rerun interface generation before interpreting contract or generated-artifact failures; a clean generator run is stronger evidence than comparing only the authored API fragments.
---

## 2026-08-10 09:40 -0700 - wsr-001-opening-record provider-history review follow-up
- What was implemented
  - Addressed the blocking PR feedback that WSR-FT-003 only checked Factory response events by adding a public canonical Worker Session-ID history route and draining it through the root-built functional support path.
  - Made Worker-ID history projection provider-neutral: retained lifecycle/output records remain inspectable when Provider Sessions transcript projection is unavailable, while any real Provider Session reference is preserved without synthesis.
  - Extended observable provider conformance evidence across Antigravity final-only, Claude full-stream, Codex partial-stream, and Cursor/ACP session-bearing executions. Each history asserts opening position 1, stable Work/Worker correlation, source-event idempotency and sequence, provider binding before output, provider provenance/fidelity, and terminal-last replay.
  - Synchronized the branch with current `origin/main`, regenerated REST, Go, package, and UI contract artifacts, and updated the active PRD story notes and consolidated patterns.
- Files changed
  - `api/openapi-main.yaml`
  - `api/openapi.yaml`
  - `api/components/parameters/WorkerSessionID.yaml`
  - `pkg/services/worker_sessions/`
  - `pkg/transports/http/`
  - `tests/functional/internal/support/`
  - `tests/functional/workers/inference/`
  - `tests/functional/providers/acp/acp_provider_events_test.go`
  - `packages/api/generated/`
  - `ui/src/api/generated/openapi.ts`
  - `ui/packages/client/src/generated/openapi.ts`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` — passed.
  - Root-built WSR-FT-003, Claude full-stream, and Codex partial-stream history tests — passed.
  - Root-built Cursor/ACP Worker Session history conformance test — passed.
  - `make interfaces-all` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A public Worker Session history contract must expose the stable Worker ID directly; requiring a Provider Session transcript projection makes valid provider-neutral histories appear unavailable.
  - Conformance is stronger when the same public history drain validates both no-reference and session-bearing providers, including source idempotency and terminal replay rather than only final response payloads.
---

## 2026-08-10 10:06 -0700 - wsr-001-opening-record final verification
- What was implemented
  - Kept the new conformance assertions within the repository backend-size and maintainability limits by moving focused observation edge helpers alongside the existing internal service coverage and splitting the Antigravity history assertion at its behavioral boundary.
  - Updated existing Worker Sessions service doubles for the expanded observation contract so the full repository short suite continues to compile and run.
- Files changed
  - `pkg/services/worker_sessions/internal/service/internal_test.go`
  - `pkg/services/worker_sessions/internal/service/invoke_observation_test.go`
  - `tests/functional/workers/inference/opening_record_test.go`
  - `pkg/services/factory_runtime/internal/build_test.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/factory_runtime_test_helpers_test.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/dispatch_worker_sessions_idempotency_test.go`
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/dispatch_worker_sessions_terminal_semantics_test.go`
  - `progress.txt`
- Verification
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract checks, 355 Vitest files / 2,970 tests, 191 Bun tests, and the short Go suite.
  - `make api-smoke` — passed OpenAPI validation, generation/drift checks, contract alignment, and provider parity smoke.
  - Focused Worker Sessions, WSR, Claude/Codex, and Cursor/ACP tests — passed.
  - `git diff --check` — passed; worktree clean before publish.
- **Learnings for future iterations:**
  - The repository’s parallel short-suite lane can expose an intermittent 300-second Worker Sessions timeout even when the package passes in isolation; rerunning the complete lane after isolating the named package establishes whether it is a concurrency flake.
  - Public interface additions require updating manually implemented service doubles across Runtime tests; embedding an existing root interface does not help doubles that implement methods explicitly.
---

## 2026-08-10 14:10 -07:00 - wsr-001-opening-record current-main synchronization follow-up
- What was implemented
  - Synchronized the branch with the current `origin/main` at `6e01bee68`, retaining the active WSR PRD and current-main CLI exit-code changes.
  - Resolved the append-only `progress.txt` merge by preserving the complete current-main history, all WSR history entries, and the consolidated reusable patterns.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main CLI and functional test changes retained by the merge.
- Verification
  - `prd.json` parses with all three stories still marked `passes:true`.
  - All current-main and WSR progress headings are present; no conflict markers or malformed escaped blocks remain.
  - `git diff --check` — passed before staging.
- **Learnings for future iterations:**
  - When the default branch advances during review, resolve the task PRD as the active branch’s source of truth while preserving both histories in the append-only progress log.
---

## 2026-08-10 14:53 -07:00 - wsr-001-opening-record final review-state verification
- What was implemented
  - Re-audited PR #1849 with the thread-aware GitHub review workflow after the package-boundary follow-up; the historical blocking findings are covered by the current branch commits, with no unresolved inline threads or formal reviews remaining.
  - Confirmed the current branch remains synchronized with `origin/main` at `6e01bee68`, has a clean merge tree, and retains the intentional 175-entry package-boundary migration baseline without the previously reported new violation.
  - Confirmed the pushed PR head, exact `prd.json` PR description, all three `passes:true` stories, and terminal-success required checks are aligned.
- Files changed
  - `progress.txt`
- Verification
  - Thread-aware PR audit — 5 historical conversation comments, 0 formal reviews, and 0 inline review threads; PR #1849 is open, non-draft, mergeable, and all required checks are successful.
  - PRD/body equality and JSON validation — passed.
  - `git diff --check` and `git merge-tree --write-tree origin/main HEAD` — passed.
  - `go run ./cmd/pkgboundarycheck -root .` — reports only the known 175-entry baseline recorded on current main.
- **Learnings for future iterations:**
  - A remaining service-internal constructor can be an intentional baseline entry; compare the checker’s total against current main and inspect the changed call sites before treating any matching `rg` result as a new violation.
  - After a review-fix commit, verify both the branch’s remote SHA and the PR body/check state so bookkeeping does not leave the handoff stale.
---

## 2026-08-10 14:56 -07:00 - wsr-001-opening-record latest-main synchronization follow-up
- What was implemented
  - Merged the newly advanced `origin/main` head `78584efb5`, retaining its unrelated CLI test-file split and baseline updates while preserving the WSR branch’s task-scoped `prd.json`.
  - Resolved the `prd.json` merge by keeping the WSR PRD as the active branch source of truth and confirmed the append-only progress histories merged without conflict markers.
- Files changed
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `pkg/transports/cli/`
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three stories still marked `passes:true`.
  - The merged progress log has no conflict markers; staged merge content passes `git diff --cached --check`.
- **Learnings for future iterations:**
  - When current main advances during PR recomputation, preserve task-scoped PRD ownership, merge the append-only progress history, and update the synchronization record to the actual fetched base SHA before publishing.
---

## 2026-08-10 15:02 -07:00 - wsr-001-opening-record merged-head verification
- What was implemented
  - Completed the post-merge verification pass without additional product changes; regenerated interfaces remain drift-free and the WSR implementation is ready to publish from the conflict-free merged head.
- Files changed
  - `progress.txt`
- Verification
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet functional-boundary-check api-smoke` — passed.
  - Worker Sessions/Factory Runtime package tests, WSR-FT-001/002/003, provider stream-fidelity, ACP session-bearing history, and unavailable-owner HTTP tests — passed.
  - `make verify-fast` — passed dashboard typecheck, MCP contract checks, 355 UI files / 2,970 tests, 191 Bun tests, and the short Go suite.
  - `make interfaces-all`, `git diff --check`, and `git merge-tree --write-tree origin/main HEAD` — passed with `origin/main` at `78584efb5`.
- **Learnings for future iterations:**
  - After reconciling a moving default branch, rerun both contract generation and the shared fast tier before pushing so unrelated merged test reorganizations are covered by the same handoff evidence.
---

## 2026-08-10 15:23 -07:00 - wsr-001-opening-record story-001 opening-failure review follow-up
- What was implemented
  - Addressed the latest blocking PR feedback by requiring `publishTerminalRecord` to observe an open Worker publication window, which exists only after `SESSION/STARTED` has committed; unknown session publications now return the typed not-found error.
  - Added a fail-first/succeed-second Events regression proving an opening append failure leaves no terminal-only Worker topic, retains the typed `EVENT_PUBLICATION_FAILURE` session result, and never calls Workers.
- Files changed
  - `pkg/services/worker_sessions/internal/service/publish_record.go`
  - `pkg/services/worker_sessions/internal/service/start_side_effects_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused opening/terminal publication failure tests — passed.
  - `go test ./pkg/services/worker_sessions/... -count=1` — passed.
  - `go test ./pkg/services/factory_runtime/... -count=1` — passed.
  - `git diff --check` — passed before bookkeeping changes.
- **Learnings for future iterations:**
  - The publication `open` flag is the authoritative in-process proof that the topic has a valid opening; terminal callers must check it before closing or appending so a failed opening cannot manufacture position 1.
---

## 2026-08-10 15:49 -07:00 - wsr-001-opening-record current-main synchronization follow-up
- What was implemented
  - Merged the newly advanced `origin/main` head `e8c0a248a` into the task branch after the PR base changed during CI.
  - Preserved the WSR task-scoped `prd.json`, retained the append-only progress history, and kept the current-main packaged-installation changes in the merge result.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main packaged-installation and CLI functional-test changes included by the synchronization merge.
- Verification
  - `prd.json` parses with all three stories still marked `passes:true`.
  - No merge stages or conflict markers remain; staged `git diff --check` passed.
- **Learnings for future iterations:**
  - Fetch the live default branch again immediately before the final PR handoff; a clean earlier merge can become stale while CI is running.
---

## 2026-08-10 16:05 -07:00 - wsr-001-opening-record merged-head verification follow-up
- What was implemented
  - Completed the post-synchronization verification without further product changes; generated interfaces remain drift-free on the merged head.
- Files changed
  - `progress.txt`
- Verification
  - `make interfaces-all` — passed with no generated diff.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` — passed.
  - Focused Worker Sessions race regression tests — passed.
  - Scoped `go vet ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/...` — passed.
  - Full `go vet ./...` was attempted but exceeded the local 180-second wrapper while unrelated Go jobs shared the build cache; no vet diagnostic was emitted.
  - `git diff --check`, `git merge-tree --write-tree origin/main HEAD`, and PRD JSON validation — passed; all three stories remain `passes:true`.
- **Learnings for future iterations:**
  - When the shared local Go cache is busy, distinguish a wrapper timeout with no diagnostics from a vet finding; use scoped vet as local evidence and rely on isolated CI for the full repository sweep.
---

## 2026-08-10 16:57 -07:00 - wsr-001-opening-record CI deflake and current-main synchronization
- What was implemented
  - Stabilized the existing cancellation-sensitive functional fixtures by waiting for the injected provider edge to enter its blocking call before sending interrupt controls; production behavior and the WSR PRD remain unchanged.
  - Synchronized with current `origin/main` at `acbd13fcc`; retained main's approved JavaScript durability `ProviderCommandRunner` deflake and its fixture provider selections while resolving the task-scoped PRD/progress conflicts.
- Files changed
  - `tests/functional/cli/mcp_resume/serve_runtime_resume_smoke_test.go`
  - `tests/functional/cli/session_resume/resume_smoke_test.go`
  - `tests/functional/orchestration/javascript/durability/resume_test.go`
  - `tests/functional/sessions/execution/helpers_test.go`
  - `tests/functional/sessions/restart/logical_identity_test.go`
  - `tests/fixtures/javascript_runtime/resumable-two-step-fake-children.workflow.js`
  - `progress.txt`
- Verification
  - Affected functional packages, Worker Sessions/Factory Runtime packages, WSR-FT-001/002/003 — passed.
  - `make interfaces-all` — passed with no generated drift.
  - `make test` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `git diff --check` and `git merge-tree --write-tree origin/main HEAD` — passed.
- **Learnings for future iterations:**
  - Current main can carry a concurrent test-deflake implementation that supersedes a local fixture change; prefer the newer approved edge-based synchronization when resolving overlapping test conflicts.
---

## 2026-08-10 18:41 -07:00 - wsr-001-opening-record final PR audit and CI follow-up
- What was implemented
  - No product-code changes were needed: all three WSR stories remain marked `passes:true`, the branch is clean and conflict-free with `origin/main` at `e6797fd5d`, and PR #1849 still has the exact `prd.json` body.
  - Re-audited the PR with the thread-aware GitHub workflow: six historical conversation comments, zero formal reviews, and zero inline review threads remain; no unresolved review feedback is outstanding.
  - Reran the failed CI jobs. The functional tests completed on retry, but Backend Functional Coverage failed its existing `pkg/transports/cli` ratchet (`72.8180%` actual vs `72.98%` minimum), which also caused Verification Policy to fail.
- Files changed
  - `progress.txt`
- Verification
  - Focused JavaScript parallel-dispatch and Codex golden tests passed five local repetitions each.
  - `gh run rerun 31448113624 --failed` reached a terminal result, with the only retry coverage diagnostic being the unchanged `pkg/transports/cli` minimum.
  - The local `make test-functional-coverage` attempt exceeded its 10-minute bound without a diagnostic; its exact descendant test processes were stopped without touching a concurrent `make test` process.
- **Learnings for future iterations:**
  - A clean PR diff and passing focused tests do not establish the shared functional coverage ratchet; inspect the package-level CI diagnostic before changing unrelated production code.
- Verification Policy is an aggregate failure when Backend Functional Coverage misses its package minimum; address the underlying coverage lane first.
---

## 2026-08-10 18:52 -07:00 - wsr-001-opening-record current-main synchronization after PR push
- What was implemented
  - Merged the newly advanced `origin/main` at `0b9771fe8` (PR #1862) after the progress-only PR update made the default branch conflict visible.
  - Kept the WSR task-scoped `prd.json`, retained current main’s Factory Sessions test split and history, and reassembled the append-only log with all 22 WSR sections preserved.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main Factory Sessions execution test-split files and baseline documentation from the synchronization merge.
- Verification
  - `prd.json` parses with all three stories marked `passes:true`.
  - No conflict markers remain; the WSR-section line/hash audit matches the pre-merge task history, and `git diff --cached --check` passes.
  - The merge is staged for a synchronization commit; generated WSR artifacts were not regenerated because the merge changed only current-main test organization and bookkeeping.
- **Learnings for future iterations:**
  - A progress-only push can race with default-branch advancement; fetch the live base again before handoff, use current main’s progress as the merge base, preserve task sections explicitly, and validate reconstructed history before committing.
---

## 2026-08-10 19:08 -07:00 - wsr-001-opening-record final PR handoff audit
- What was implemented
  - Completed the latest PR follow-up for PR #1849; all three PRD stories remain marked `passes:true` and no additional product-code change was required.
  - Confirmed the opening-publication fix prevents a successful terminal append from creating terminal-only Worker history after a failed `SESSION/STARTED` append, while retaining the typed publication-failure result and skipping Workers handoff.
  - Confirmed the existing PR body still matches `prd.json` byte-for-byte, the branch is clean and conflict-free with current `origin/main`, and the historical blocking findings are addressed by the current commits.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1 -run 'Test.*(Opening|Terminal|Publication|Provider|Record)'` — passed.
  - `gh pr checks 1849` — all listed checks terminal and passing.
  - Thread-aware GitHub review audit — 0 formal reviews and 0 inline review threads; the historical conversation findings are covered by the pushed fixes.
  - `git merge-tree --write-tree origin/main HEAD`, `git diff --check`, PRD validation, and PRD/body byte equality — passed.
- **Learnings for future iterations:**
  - A final PR audit should verify both the latest review finding’s behavioral regression and the remote PR’s body, mergeability, base synchronization, and terminal check state before handoff.
---
## 2026-08-10 18:48 -07:00 - e2-structured-worker-results current-main review follow-up
- What was implemented
  - Merged current `origin/main` at `0b9771fe8` into the E2 branch after PR #1855 reported a stale merge base.
  - Resolved the shared `prd.json` and `progress.txt` conflicts by preserving the E2 task PRD and combining both append-only histories; all four E2 stories remain `passes:true`.
  - No additional structured-result product change was required; the current-main Factory Sessions test split is carried through as base synchronization.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main Factory Sessions split-test files and related baseline artifacts included by the synchronization merge.
- Verification
  - `make test` — passed.
  - `make typecheck` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - Focused structured-result worker, prompt, Work CLI/HTTP, event/replay, and projection packages — passed.
  - Root-built functional structured-result prompt and schema-violation routing tests — passed.
  - `make lint` passed all prerequisite checks and reached only the known `pkg-boundary` baseline of 175 findings; detached `origin/main` reports the same 175 findings.
  - `prd.json` parses with four passing stories; conflict-marker scan and `git diff --check` passed.
- **Learnings for future iterations:**
  - Re-fetch and merge the live PR base immediately before pushing because shared bookkeeping files can conflict again after an otherwise clean review handoff.
  - When a shared append-only log conflicts, preserve the task PRD as authoritative and retain both histories, then validate JSON, conflict markers, and line completeness before committing.
---

## [2026-08-10 18:58 -07:00] - e2-structured-worker-results final review handoff
- What was implemented
  - No additional product implementation was required; all four E2 stories remain marked `passes:true`.
  - Audited PR #1855's full conversation and thread-aware review state. The three earlier code blockers and the later transitioner-size blocker are cleared on the current branch; no unresolved inline review threads remain.
  - Kept `prd.json` unchanged because the existing PR description already reflects the completed stories and review fixes.
- Files changed
  - `progress.txt`
- Verification
  - PR #1855 is open, non-draft, clean, and mergeable. Local HEAD, the fork branch, and the PR head are `c3cacd3d26ec38af6438efcad140c4576bc79156`; the PR base and local `origin/main` are `0b9771fe85fd1aedf9968d636de5e72cd193970c`.
  - All required GitHub checks are terminal and successful, including Backend Unit Coverage, Backend Functional Coverage, Local Inference, UI Backend Integration, and Verification Policy.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed. `make lint` reached only the documented/current-main `pkg-boundary` baseline of 175 findings; all lint prerequisites passed and GitHub Verification Policy passed.
  - `git diff --check` passed. The PR body matches `prd.json` byte-for-byte: 11,666 UTF-8 bytes and SHA-256 `D2C1FC43700B37B0D8F50964607DD2318945ED1F7776AEE0C88BF0A749E9D0A6`.
- **Learnings for future iterations:**
  - A completed review handoff is ready when the latest conversation blockers are cleared, thread state is empty, the branch is clean against current main, the PR body is byte-identical to `prd.json`, and all required checks are terminal-green.
---

## [2026-08-10 19:25 -07:00] - e2-structured-worker-results CI flake follow-up
- What was implemented
  - Made no product or test-code changes. Investigated the final-head CI failure and reran only the failed Backend Functional Coverage job.
  - Confirmed the first failure was an unchanged `pkg/transports/cli` functional-coverage measurement at 72.818% versus the 72.98% manifest floor; the targeted rerun then exposed the existing `TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle` shutdown race in `tests/functional/workers/mock`.
- Files changed
  - `progress.txt`
- Verification
  - The isolated `continuous_idle` subtest passed 20 consecutive local runs; no feature diff touches its package.
  - Failed-job rerun Backend Functional Coverage job `93658010711` passed, including the pinned real ACP evidence artifact; Verification Policy job `93658764710` passed.
  - The branch remains at the pushed head `868990d2b4a07270cce311ec715c98bf10eacb7e`, with all four PRD stories marked `passes:true` and the PR body still byte-identical to `prd.json`.
- **Learnings for future iterations:**
  - When functional coverage first reports a package-floor variance and a retry names an unrelated cancellation/shutdown race, preserve the feature diff, stress the exact subtest locally, and rerun only the failed job after clean characterization.
 ---
## 2026-08-10 18:42 -07:00 - dwro-s1-remote-placement-flags current-main synchronization follow-up
- What was implemented
  - Fetched the live `origin/main` head `0b9771fe8` after the PR base advanced beyond the previously synchronized `e6797fd5`.
  - Merged the current base, preserved the S1-owned `prd.json`, and retained both branches' append-only progress history while resolving the shared bookkeeping conflicts.
- Files changed
  - Current-main merge files.
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all four S1 stories marked `passes:true`.
  - `progress.txt` and `prd.json` contain no merge conflict markers; the merge index is resolved.
  - `git diff --cached --check` passed before the merge commit.
- **Learnings for future iterations:**
 - A clean merge against a stale remote-tracking base is not sufficient; compare the live hosting ref, fetch it, and resolve shared PRD/progress files again before pushing the replacement head.
  - A clean merge against a stale remote-tracking base is not sufficient; compare the live hosting ref, fetch it, and resolve shared PRD/progress files again before pushing the replacement head.
 ---
## [2026-08-10 19:21 -07:00] - thr-split-acp-stdio-tests second current-main synchronization
- What was implemented
  - Fetched live `origin/main` at `72537e7f24125d0d99a152efea65febec62e2141` after another main-branch advancement made the PR conflicting again.
  - Preserved the ACP task `prd.json`, unioned the append-only progress histories, and carried the current-main CLI manifest, generated-artifact, invocation-error, and server-binding changes that are outside this task.
- Files changed
- A clean merge against a stale remote-tracking base is not sufficient; compare the live hosting ref, fetch it, and resolve shared PRD/progress files again before pushing the replacement head.
---
## [2026-08-10 19:05 -07:00] - graph-workstation-semantics-001
- What was implemented
  - Added a package-owned `ValidateFirstPartyWorkStateRoles` semantic validator for parsed first-party Petri Factory definitions.
  - Counted external initial admission, workstation inputs/outputs, continuation, rejection, failure, classification-route outputs, and explicit invocation-return states as implemented roles.
  - Added the named `automations.invocation_schedule.overlap_skip` lifecycle bridge for `scheduled:skipped`, matching the production scheduler contract and its direct fake-clock overlap coverage; JavaScript Factories are excluded from the Petri-only rule.
  - Added focused tests proving valid role coverage, exact Factory-slug plus `workType:state` disconnected diagnostics, lifecycle bridge acceptance, and JavaScript scope exclusion.
- Files changed
  - `internal/packagedfactorycatalog/work_state_roles.go`
  - `internal/packagedfactorycatalog/work_state_roles_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./internal/packagedfactorycatalog -run '^TestValidateFirstPartyWorkStateRoles' -count=1` — passed.
  - `git diff --check` — passed.
  - The full `internal/packagedfactorycatalog` package remains red on pre-existing branch inventory drift: authored `agy-clip-qa`/`agy-cold-watch` entries are not present in the generated catalog/validation ledger, and existing fixture hash expectations are based on the older inventory. No unrelated generated artifacts were changed in this one-story iteration.
- **Learnings for future iterations:**
  - Keep first-party semantic reachability separate from customer Factory validation; the package catalog can enforce publication-specific role completeness without tightening the general Factory validator.
  - Lifecycle-only states need a named production bridge plus direct runtime behavior evidence; a comment-only state allowlist is not sufficient.
  - Enable the new catalog gate only after story 002 removes `goal:execute` and reconciles all authored/generated catalog inventory, otherwise the existing catalog check cannot stay green.
---

## [2026-08-10 20:04 -07:00] - graph-workstation-semantics-002
- What was implemented
  - Removed the unreachable `goal:execute` state from the canonical `@you/goal` Factory while preserving its repeater, loop-breaker, accepted, continue, blocked, and failed paths.
  - Regenerated the goal JSON/YAML artifacts and catalog manifest hashes from the authored source.
  - Updated the canonical authoring reference and CLI docs smoke expectations so the shipped goal state inventory no longer advertises `goal:execute`.
  - Added a published-catalog regression assertion for the exact goal state list and reconciled catalog tests/validation ledger with the current first-party inventory, including the two AGY entries already present on the current base.
  - Audited the remaining candidates and deliberately left `loop-controller:stopped` and `quorum task:complete` unresolved because current runtime code and direct tests do not prove those states are emitted; they remain follow-up lifecycle work rather than guessed catalog exceptions.
- Files changed
  - `packages/packaged-factories/factories/goal/factory.yaml`
  - `packages/packaged-factories/generated/factories/goal/factory.json`
  - `packages/packaged-factories/generated/factories/goal/factory.yaml`
  - `packages/packaged-factories/generated/manifest.json`
  - `internal/packagedfactorycatalog/definition_catalog_test.go` and catalog inventory/integrity tests
  - `docs/reference/authoring-factories.md`
  - `tests/functional/smoke/cli_docs_smoke_test.go`
  - `docs/internal/packaged-factory-validation/ledger.json`
  - `prd.json`
- Verification
  - `go test ./internal/packagedfactorycatalog -count=1` — passed.
  - `make packaged-factory-catalog-check` — passed; generated catalog is current.
  - `go test ./tests/functional/factory/packaged/goal -count=1 -timeout 300s` — passed.
  - `go test ./tests/functional/factory/packaged/loop ./tests/functional/factory/packaged/quorum -count=1 -timeout 300s` — passed serially; the initial parallel run hit shared packaged-installation contention and was rerun successfully in isolation.
  - `make docs-reference-smoke`, `git diff --check`, and `prd.json` parsing — passed.
- **Learnings for future iterations:**
  - A generated-artifact regression test proves the published catalog inventory, but the story still needs a graph-package projection or browser fixture to prove downstream graph consumers omit the removed state.
  - The catalog gate should be enabled only after the unresolved loop-controller and quorum parent lifecycle semantics are either implemented with direct evidence or removed from their canonical definitions.
  - Current-main catalog inventory and validation-ledger expectations must be reconciled together when first-party entries arrive on the base branch, or focused catalog tests fail before feature assertions run.
---

## [2026-08-10 20:06 -07:00] - graph-workstation-semantics-002 verification follow-up
- What was implemented
  - Recorded the final typecheck attempt and its environment prerequisite failure in the task bookkeeping.

## 2026-08-10 19:41 -07:00 - prose-standard-and-terms-001
- What was implemented
  - Added the normative customer technical-writing standard for labels, procedures, descriptions, machine and technical text, technical terms, protected literals, deterministic and human review boundaries, bounded exceptions, ASD-STE100 claim limits, paired examples, and the manual review checklist.
  - Kept the story documentation-only: no prose checker, baseline, CI enforcement, generated artifact, CLI manifest change, or customer-corpus rewrite was added.
- Files changed
  - `docs/internal/standards/writing/customer-technical-writing-standard.md`
  - `prd.json`
  - `progress.txt`
  - Current-main CLI contracts, generated artifacts, runtime CLI mapping, and functional-test changes.
- Verification
  - `prd.json` parses with all three ACP stories marked `passes:true`; conflict markers are absent and the only unresolved paths are awaiting staging for this merge.
  - The prior post-merge run `31450791786` is successful after the targeted functional-coverage retry.
- **Learnings for future iterations:**
  - Main can advance again while documenting CI follow-up; fetch and merge the live base immediately before the final push, including when the previous post-merge run was already green.
---

## [2026-08-10 19:28 -07:00] - thr-split-acp-stdio-tests final current-main handoff synchronization
- What was implemented
  - Fetched live `origin/main` at `aac04248d95de7f5412d58f194e7ae03858a3b39` after the final audit found the PR base had advanced again.
  - Merged the documentation-only current-main changes cleanly, keeping the ACP task PRD and append-only progress history intact.
- Files changed
  - Current-main internal development plan documents.
  - `progress.txt`
- Verification
  - The merge completed without conflicts; the ACP PRD remains valid with all three stories `passes:true`.
  - No review comments, formal reviews, or unresolved inline threads exist on PR #1861.
  - The prior synchronized head `103a76bde` had all 18 CI checks passing; this merge commit is queued for the final replacement-head CI run.
- **Learnings for future iterations:**
  - Even a documentation-only main advancement must be included before handoff when the PR base has moved; recheck CI on the exact pushed head afterward.
---

## [2026-08-10 19:50 -07:00] - thr-split-acp-stdio-tests final CI completion
- What was implemented
  - Completed final CI validation on the latest-base handoff head `3cf2de0f5e9d1d7fed36aae312e5ae4279f08a0c`.
  - Retried only Backend Functional Coverage after the initial and first targeted attempts hit the unrelated no-dependent-packages coverage-lane condition.
- Files changed
  - `progress.txt`
- Verification
  - GitHub Actions run `31452536129`: the final targeted Backend Functional Coverage retry `93661800395` passed, and Verification Policy passed; all 18 required checks are successful.
  - The ACP stdio focused package test passed locally on the synchronized tree.
  - No ACP-specific test failure, review comment, formal review, or unresolved inline thread exists.
- **Learnings for future iterations:**
  - A successful functional-coverage retry must be recorded against the exact handoff head; broad coverage failures here are unrelated runner/package-selection failures rather than evidence against the move-only ACP split.
  - `git diff --cached --check` passed for the implementation commit.
  - `make typecheck` reached TypeScript after the missing `@types/bun` dependency was restored locally, but the gate remains blocked by pre-existing `Assertion.not` errors in untouched UI test helpers: `current-selection-save-toast-test-helpers.ts` and `trace-grid-card-scroll-test-helpers.ts`.
  - No UI or runtime files changed in this story; stories `prose-standard-and-terms-002` and `prose-standard-and-terms-003` remain incomplete.
- **Learnings for future iterations:**
  - Keep the writing standard independent from the terminology data and standards-index wiring so each documentation story remains reviewable; add those companion links only when their target artifacts exist.
  - A docs-only change can still inherit a repository-wide typecheck failure; record the exact untouched files and error class instead of broadening the story into unrelated UI cleanup.
---

## 2026-08-10 19:54 -07:00 - prose-standard-and-terms-002
- What was implemented
  - Added the versioned YAML product terminology register with an explicit record schema for canonical terms, definitions, categories, approved forms, alternatives, examples, valid surfaces, and ownership references.
  - Added 59 consistently shaped records covering the required public nouns, customer-visible relationships and protected relationship literals, live and durable lifecycle names, and the internal-only CPN, place, transition, marking, and token terms.
- Files changed
  - `docs/internal/standards/writing/customer-technical-terms.yaml`
  - `prd.json`
  - `progress.txt`
- Verification
  - PyYAML parsed the complete register and verified version `1`, the documented schema, identical record fields, allowed categories, non-empty required values, alternative-record shape, required terms, and internal-only surfaces.
  - Every owner reference resolves to an existing architecture or public-contract source.
  - `git diff --check` passed before the register commit `5df0598a8` (`docs: add customer terminology register`).
  - `make typecheck` passed on the repository-wide rerun after dependencies were restored; this story changed no UI or runtime files.
- **Learnings for future iterations:**
  - Keep natural-language product terms separate from exact contract literals, and give both records an owner when authors need to know which spelling is protected.
  - Explicit `null` values in `approvedForms` make non-applicable plural and verb forms reviewable instead of relying on omitted keys.
---

## 2026-08-10 20:08 -07:00 - prose-standard-and-terms-003
- What was implemented
  - Connected the normative writing standard, versioned terminology register, and embedded manual review checklist from the standards index.
  - Added a direct terminology-register link in the technical-terms guidance and made the checklist’s sentence-level, contract-meaning, and review-boundary expectations explicit.
  - Kept the story documentation-only: no prose checker, baseline, CI enforcement, generated artifact, CLI manifest change, or customer-corpus rewrite was added.
- Files changed
  - `docs/internal/standards/STANDARDS.md`
  - `docs/internal/standards/writing/customer-technical-writing-standard.md`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make typecheck` passed on 2026-08-10.
  - Markdown links resolve to the writing standard, terminology register, architecture data model, and official ASD-STE100 references; the manual-checklist index link resolves to the standard’s checklist heading.
  - `prd.json` parses with all three stories marked `passes:true`.
  - `git diff --check` passed; no code, generated artifact, or customer-corpus file changed.
- **Learnings for future iterations:**
  - Keep the standards index focused on discoverability: link the normative writing entry point first, then its structured register and embedded manual checklist.
  - When a checklist is embedded in a normative standard, expose its heading anchor from the index so reviewers can reach the manual-review contract directly.
---
## 2026-08-10 19:41 -07:00 - cli-error-classification-and-run-output-001
- What was implemented
  - Classified dynamic Factory invocation missing values and static run-flag parse failures with stable `INVOCATION_ARGUMENT_*` errors, including the case where one dynamic flag is followed by another and must not consume it as a value.
  - Added process-boundary regression coverage for the reported `--planer-model` typo in normal and quiet modes, dynamic/static missing values, positional overflow, and invalid JSON values; usage failures emit one stderr `ErrorResponse`, leave stdout empty, and make zero provider calls.
  - Added the invocation argument error entries to the authored CLI contract and regenerated the CLI family and published contract artifacts.
- Files changed
  - `contracts/cli/commands.json`
  - `packages/api/generated/cli/commands.json`
  - `packages/api/generated/manifest.json`
  - `pkg/services/factory_runtime/transports/cli/invocation_error.go`
  - `pkg/services/work/internal/invocationreturnpolicy/arguments.go`
  - `pkg/services/work/internal/invocationreturnpolicy/preparation.go`
  - `pkg/services/work/internal/invocationreturnpolicy/preparation_test.go`
  - `pkg/services/work/invocation_return_policy_contract.go`
  - `pkg/transports/cli/generated/family_manifests_gen.go`
  - `pkg/transports/cli/root_submit_batch.go`
  - `pkg/transports/cli/run/invocation_error.go`
  - `tests/functional/transport/cli/output/json_result_test.go`
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
- Verification
  - `go test ./pkg/services/work/internal/invocationreturnpolicy ./pkg/services/work ./pkg/services/factory_runtime/transports/cli ./pkg/transports/cli/run ./pkg/transports/cli` passed.
  - Focused functional CLI output/parameter tests passed.
  - `make cli-manifest-check` and `git diff --check` passed.
- **Learnings for future iterations:**
  - Dynamic Factory arguments are parsed by Work after run-level flags are removed; a value-taking argument must reject a following `--flag` token before normalization so it cannot accidentally start runtime execution.
  - `make cli-manifest-generate` refreshes the generated Go family metadata; changes to the published `packages/api/generated/cli/commands.json` projection also require `make contracts-generate` before `make cli-manifest-check` will pass.
---
## 2026-08-10 20:00 -07:00 - cli-error-classification-and-run-output-001 verification follow-up
- What was implemented
  - Restored the locked UI dependency needed by the repository typecheck and verified the frontend typecheck after the story changes.
## 2026-08-10 19:25 -07:00 - pp-merge-context-bug-001
- What was implemented
  - Added a deterministic packaged `@you/plan-parallel` functional regression with 10 independent generated children and unique executor output markers.
  - Captured the actual merger provider request through the injected `ProviderCommandRunner` while executing through `root.BuildProcess` and `Process.Execute`.
  - Asserted the original request exactly once, every unique completed child result exactly once, each result under its generated Work name, and absence of the planner payload in that Work section.
  - Updated the story note in `prd.json`; the story remains `passes:false` because the same test passes on the current `origin/main`, so the required current-main red reproduction is not established yet.
- Files changed
  - `tests/functional/factory/packaged/plan_parallel/invocation_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/plan_parallel -run '^TestPackagedPlanParallelMergerReceivesEveryUniqueCompletedChildResult$' -count=10 -v -timeout 600s` — passed 10/10.
  - `go test ./tests/functional/factory/packaged/plan_parallel -count=1 -v -timeout 300s` — passed.
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/token_transformer ./pkg/services/factory_runtime/internal/services/orchestration/subsystems -count=1 -timeout 300s` — passed.
  - Historical inspection localized the earlier omission to packaged merger prompt assembly: `b0bf1dd597` added the Factory-input rendering block, and the current base already contains the `Payload`/`PreviousOutput`/`Content` fallback.
  - Regression commit: `ab6593506` (`test(plan-parallel): assert unique merger fan-in inputs`).
- **Learnings for future iterations:**
  - The existing three-child test's identical executor output marker can pass without proving child-result identity; fan-in coverage must derive unique result markers from each assigned payload and assert section-level association.
  - A green strengthened regression on the current base is evidence against the recorded omission in this exact scenario; do not mark the story complete or claim a red run until the failing mechanism is reproduced or a distinct current-base gap is proven.
---

## 2026-08-10 19:35 -07:00 - pp-merge-context-bug-001 verification follow-up
- What was implemented
  - Restored the frozen UI dependencies required by the repository typecheck; no tracked dependency files changed.
  - Confirmed the focused Go regression, Go orchestration package tests, backend structural checks, `go vet ./...`, and `make typecheck` all pass.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - A fresh checkout may need `bun install --frozen-lockfile` before `make typecheck`; the configured Bun version and lockfile restore the missing `@types/bun` package.
---

## 2026-08-10 19:49 -07:00 - pp-merge-context-bug-001 historical red localization
- What was implemented
  - Reproduced the exact regression on a disposable worktree at the pre-fix parent `b0bf1dd597^` (`fb7805bd8`): the merger provider request contained only its static instructions, and the original request occurred zero times.
  - Strengthened the current functional regression to reject planner payload markers anywhere in the merger input and to require generated Work sections in deterministic name order, while continuing to assert every unique completed result exactly once under the matching Work identity.
  - Updated `prd.json` to record the red pre-fix output, green current-main output, and the two-part root cause: consume-only scheduler/dispatcher routing plus missing packaged merger `.Inputs` rendering.
- Files changed
  - `tests/functional/factory/packaged/plan_parallel/invocation_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Pre-fix red: `go test ./tests/functional/factory/packaged/plan_parallel -run '^TestPackagedPlanParallelMergerReceivesEveryUniqueCompletedChildResult$' -count=1 -v -timeout 600s` — failed at `invocation_test.go:45` with `merger prompt contains original request 0 times, want exactly once`; the captured prompt had no Factory inputs.
  - Current implementation: the same regression with `-count=10` — passed all ten runs (`ok ... 20.902s`).
  - `go test ./tests/functional/factory/packaged/plan_parallel -count=1 -v -timeout 300s` — passed.
  - `prd.json` parses; `git diff --check` passes.
- **Learnings for future iterations:**
  - When the live base already contains a fix for a recorded production incident, preserve the exact historical pre-fix revision for red evidence and state explicitly that current main is post-fix; do not mislabel a historical failure as a current-main failure.
---

## 2026-08-10 19:56 -07:00 - pp-merge-context-bug-002
- What was implemented
  - Completed the delivery story against the fan-in correction already present on the synchronized main implementation; no duplicate production change was introduced.
  - Confirmed the lower-layer invariant: scheduler decisions retain all observed child input token IDs in deterministic order, the dispatcher forwards those tokens to the merger Work dispatch, and only the consumed parent token produces a consume mutation.
 - Updated `prd.json` to mark `pp-merge-context-bug-002` passed with the exact lower-layer, functional, failure-gating, and root-cause evidence.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make typecheck` — blocked before project compilation because the worktree does not have the Bun type definitions; TypeScript reported `TS2688: Cannot find type definition file for 'bun'`.
- **Learnings for future iterations:**
  - The backend/catalog changes are independently verified, but the UI typecheck requires the repository’s Bun dependency/type-definition installation to be restored before story completion can be claimed.
---

## [2026-08-10 20:54 -07:00] - graph-workstation-semantics-003
- What was implemented
  - Added the public `@you-agent-factory/factory-graph` workstation semantic projection with separate authored runtime type, scheduling behavior, and guarded-control role axes sourced from the generated client taxonomy.
  - Mapped inference, agent, script, poller-run, classifier, logical-move, and legacy model workstation values; normalized lowercase legacy inputs; and kept omitted, incomplete, invalid, or future metadata neutral instead of inferring exhaustion or another special role.
  - Derived `LOOP_BREAKER` only for supported guarded `LOGICAL_MOVE` workstations with a `VISIT_COUNT` target and positive fixed or argument limit; ordinary logical moves remain routers and classifiers remain classifiers.
  - Joined authored semantics and selected-tick activity by durable workstation id, using names only for id-less legacy definitions, and carried the disposable projection into replay node data without allowing runtime overlays to replace authored meaning.
  - Removed the package’s overloaded `semanticKind`/exhaustion presentation path and exported the canonical semantic projection through the package entrypoint.
- Files changed
  - `ui/packages/client/src/contracts.ts`
  - `ui/packages/client/src/index.ts`
  - `ui/packages/factory-graph/README.md`
  - `ui/packages/factory-graph/src/workstation-semantics.ts`
  - `ui/packages/factory-graph/src/workstation-semantics.test.ts`
  - `ui/packages/factory-graph/src/factory-graph-replay-surface.tsx`
  - `ui/packages/factory-graph/src/factory-graph-replay-surface.test.ts`
  - `ui/packages/factory-graph/src/semantic-workstation-presentation.ts`
  - `ui/packages/factory-graph/src/semantic-workstation-node.tsx`
  - `ui/packages/factory-graph/src/index.ts`
  - Regenerated `ui/packages/factory-graph/dist/*` artifacts.
- Verification
  - `make typecheck` — passed.
  - Graph package typecheck and 53 focused graph tests — passed.
  - Client package typecheck and 88 tests — passed.
  - Graph package build, client generated-contract drift check, graph package-boundary check, and graph package pack verification (31 files) — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The graph package’s canonical source is the complete authored `FactoryDefinition`; React Flow node data receives only a disposable semantic projection and selected-tick activity overlay.
  - Treat each semantic axis independently: a known scheduling behavior must remain visible even when runtime type is future, while omitted scheduling metadata must not be guessed as `STANDARD`.
  - Continue story 004 with host-level visible/accessibility coverage for guarded-control cards and browser verification; story 003 intentionally stops at the shared projection and replay-node wiring.
---

## [2026-08-10 23:49 -07:00] - graph-workstation-semantics-004
- What was implemented
  - Completed the shared workstation rendering layer for current activity, Factory-definition editing, selected-tick replay, public Factory graph replay, and trace dispatch hosts.
  - Added visible and accessible runtime-type labels, distinct scheduling treatments, and a selection-independent guarded logical-control card exposing VISIT_COUNT role, target, and limit.
  - Preserved neutral UNKNOWN semantics for missing, future, or lossy metadata and made authored workstation identity joins id-safe; names are used only by id-less legacy definitions.
  - Added mixed semantic Storybook coverage with long names, component/projection/replay/trace assertions, and direct fit-to-view touch and desktop browser verification.
- Files changed
  - `ui/packages/factory-graph/src/semantic-workstation-node.tsx`, `semantic-workstation-presentation.ts`, `workstation-semantics.ts`, and generated `dist/*`
  - `ui/src/features/factory-graph-editor/`, `ui/src/features/workflow-activity/`, `ui/src/features/trace-drilldown/`, and shared flowchart semantic adapters/tests
  - `ui/src/components/dashboard/test-fixtures.ts` and the mixed current-activity Storybook story
  - `ui/scripts/verify-factory-graph-touch-browser.mjs` and the feature-folder allowlist
  - `prd.json`
- Verification
  - `make typecheck` — passed.
  - `make ui-test` — passed: 355 Vitest files/2,970 tests and 191 Bun-native tests.
  - `make ui-lint` — passed.
  - Factory graph package tests (54), build, package-boundary, and pack verification — passed.
  - Storybook build, filtered `Mixed Workstation Semantics` interaction test, and direct touch/desktop browser verification — passed.
- **Learnings for future iterations:**
  - Shared dashboard fixtures are also consumed by Bun-native tests; keep package-only runtime imports out of those fixture modules unless the native lane can resolve their generated declaration dependencies.
  - Storybook browser checks need a fresh static server after rebuilding; an older server can serve stale assets or make readiness timeouts misleading.
  - A host can preserve runtime activity overlays without allowing topology metadata to classify authored workstation semantics; the package projection and shared node renderer should remain the only semantic authority.
---

## [2026-08-11 00:12 -07:00] - graph-workstation-semantics-001
- What was implemented
  - Added focused catalog-role regression coverage for a disconnected terminal Work state alongside the existing orphan processing-state case; diagnostics still require the Factory slug and exact `workType:state` identities.
  - Audited the attempted repository-wide discovery wiring against the canonical Factory validator. The current `loop-controller:stopped` and `quorum task:complete` declarations have no direct runtime route, while removing them causes the existing canonical validator to reject their referenced Work types for lacking a terminal state.
  - Kept the global catalog gate deferred rather than adding a guessed lifecycle allowlist or leaving the package suite red; the lifecycle/topology contract must be decided before the gate can be enabled.
- Files changed
  - `internal/packagedfactorycatalog/work_state_roles_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./internal/packagedfactorycatalog -run '^TestValidateFirstPartyWorkStateRoles' -count=1` — passed.
  - `go test ./internal/packagedfactorycatalog -count=1` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - The semantic catalog rule and the general canonical completion-state rule must be reconciled before strict discovery validation is enabled; do not solve that mismatch with comment-only exceptions or fabricated routes.
  - A disconnected terminal declaration is as important to reject as an orphan processing state because terminal declarations can make speculative topology look operational in generated graph consumers.
---

## [2026-08-11 00:26 -07:00] - graph-workstation-semantics-002
- What was implemented
  - Audited the remaining packaged-Factory cleanup candidates against the canonical source and catalog-generation path.
  - Attempted the definitively unreachable `loop-controller:stopped` removal, then reverted the partial edit when the canonical generator rejected the definition because `loop-controller` is a referenced Petri Work type without another completion state.
  - Preserved the authored/generated catalog and runtime fixtures unchanged; the decision now requires either a real stop-to-terminal runtime bridge or an explicit canonical validation/topology change before cleanup can proceed.
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/scheduler ./pkg/services/factory_runtime/internal/services/orchestration/subsystems/... -count=1 -timeout 300s` — passed, including FIFO, priority, and dispatcher observed-input tests.
 - `make typecheck` passed.
  - The broad `make test` command reached its six-minute command timeout without emitting a test failure; the focused backend and functional CLI suites remained green.
- **Learnings for future iterations:**
  - On this shared Windows host, broad test runs can be delayed by concurrent test trees; use the focused package and process-boundary suites for fast feedback, then record any uncompleted broad-gate result explicitly.
---
## 2026-08-10 20:05 -07:00 - cli-error-classification-and-run-output-002
- What was implemented
  - Extended human accepted-work rendering to use a singular item label for one-item payloads, keep partially populated work items readable with a stable unnamed fallback, and skip relation records with no type instead of presenting an ambiguous edge.
  - Preserved the canonical batch listing of work IDs/names and text descriptions, dependency direction, and provider-payload redaction while adding exact renderer coverage for independent and partially populated items.
- Files changed
  - `pkg/services/factory_visualization/transports/cli/factory_event_support.go`
  - `pkg/services/factory_visualization/transports/cli/service_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_visualization/transports/cli -count=1` passed.
  - `go test ./pkg/services/factory_visualization/... -count=1` passed.
  - `go test ./tests/functional/cli/factory_run/output -count=1` passed.
  - `make typecheck` and `git diff --check` passed.
- **Learnings for future iterations:**
  - Human accepted-work output is owned by the Factory Visualization CLI adapter; exercise it through the response-stream renderer so sequence prefixes and presentation behavior remain part of the assertion.
  - Canonical request validation normally guarantees named relation endpoints, but the renderer should still handle replayed or partially populated payloads without blank work lines, private JSON content, or malformed relation prose.
---
## 2026-08-10 20:44 -07:00 - cli-error-classification-and-run-output-003
- What was implemented
  - Extended human Factory Event output with Worker Session association lines and workstation lifecycle details for queued, started, completed, failed, and interrupted dispatches.
  - Added deterministic dispatch correlation so active TTY progress retains worker IDs, workstation names, and canonical Work IDs across interleaved concurrent workers; terminal dispatch outcomes remove stale active indications.
  - Preserved JSON/NDJSON and redirected human output boundaries, updated the packaged run documentation, and added exact renderer plus functional lifecycle assertions.
- Files changed
  - `docs/reference/run.md`
  - `pkg/services/factory_visualization/transports/cli/factory_event_renderer.go`
  - `pkg/services/factory_visualization/transports/cli/factory_event_support.go`
  - `pkg/services/factory_visualization/transports/cli/human_worker_progress.go`
  - `pkg/services/factory_visualization/transports/cli/service_test.go`
  - `tests/functional/cli/factory_run/output/output_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_visualization/transports/cli -count=1` — passed.
  - `go test -race ./pkg/services/factory_visualization/transports/cli -count=1` — passed.
  - `go test ./pkg/services/factory_visualization/... -count=1` — passed.
  - `go test ./tests/functional/cli/factory_run/output -count=1` — passed.
  - `make typecheck` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Canonical dispatch queue events can contain the only available Work IDs; retain that correlation as pending state until the dispatch request becomes active.
  - The response-stream formatter is serialized by the Visualization presentation service, so stateful dispatch correlation can remain local to one human renderer without a cross-service event store.
 - `go test ./pkg/services/factory_runtime/internal/services/orchestration/scheduler ./pkg/services/factory_runtime/internal/services/orchestration/subsystems/... -count=1 -timeout 300s` — passed, including FIFO, priority, and dispatcher observed-input tests.
  - `go test ./tests/functional/factory/packaged/plan_parallel -run '^TestPackagedPlanParallelMergerReceivesEveryUniqueCompletedChildResult$' -count=10 -v -timeout 600s` — passed all ten N=10 runs.
  - `go test ./tests/functional/factory/packaged/plan_parallel -count=1 -v -timeout 600s` — full packaged plan-parallel suite passed.
  - `go test ./pkg/services/factory_sessions/internal/execution/... -count=1 -timeout 600s` — all Factory Sessions execution packages passed.
- **Learnings for future iterations:**
  - When the owning production correction is already on main, use the existing lower-layer tests plus an observable end-to-end regression to complete the story without duplicating a fix or widening the fan-in surface.
---

## 2026-08-10 20:00 -07:00 - pp-merge-context-bug current-main synchronization
- What was implemented
  - Fetched and merged the live `origin/main` head `aac04248d95de7f5412d58f194e7ae03858a3b39` into the task branch.
  - Preserved this task's `prd.json` and append-only fan-in evidence while carrying the current-main history forward.
- Files changed
  - Current-main merge files are included only as synchronization history.
  - `progress.txt` records the synchronization evidence.
- Verification
  - `prd.json` parses with both `pp-merge-context-bug-001` and `pp-merge-context-bug-002` marked `passes:true`.
  - `git status --short` is clean; `git diff origin/main...HEAD --stat` is limited to `prd.json`, `progress.txt`, and `tests/functional/factory/packaged/plan_parallel/invocation_test.go`.
  - The merge index is resolved and the task branch is based on current `origin/main`.
- **Learnings for future iterations:**
  - Synchronize against the freshly fetched hosting `main` immediately before final gates, then verify the live-base diff so current-main additions do not become accidental task scope.
---

## 2026-08-10 20:01 -07:00 - pp-merge-context-bug final verification
- What was implemented
  - Completed synchronized-head verification without changing the fan-in implementation or broadening the task diff.
- Files changed
  - `prd.json` records the final verification gates for story 002.
  - `progress.txt` records the terminal local gate results.
- Verification
  - `make typecheck` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed; backend file/function limits, maintainability, package count/structure, and `go vet ./...` are green.
  - `make test` — passed; the repository short Go unit suite completed successfully.
  - The focused lower-layer and packaged plan-parallel suites remain green after the current-main merge, as recorded in the preceding story entry.
- **Learnings for future iterations:**
  - Run the repository-wide gates after the final base synchronization; focused evidence from a pre-sync head is useful but not sufficient for PR handoff.
---

## 2026-08-10 20:27 -07:00 - pp-merge-context-bug PR CI rerun
- What was implemented
  - Investigated the first hosted `Backend Functional Coverage` failure and confirmed it was unrelated to the plan-parallel diff: the existing ACP retry test failed with a transient cursor-ACP session/load internal error.
  - Isolated the failed ACP test, then the two unrelated runtime/API tests exposed by the first rerun; each exact test passed 10/10 locally.
  - Reran only the failed hosted jobs twice. The final rerun passed both `Backend Functional Coverage` and downstream `Verification Policy`.
- Files changed
  - `progress.txt`
- Verification
  - Initial backend failure: `TestFactoryRunRetriesACPProviderByResumingExactSession` reported temporary cursor-ACP session/load unavailability; the required real-ACP evidence artifact was absent because the lane stopped on that failure.
  - First rerun failures: `TestAPIEventReplaySmoke_PublicEventsAndSessionProjectionExposeActiveAndCompletedTimeline` and `TestAPIPartialResultIsAvailableBeforeTerminalCompletion`; both passed 10/10 in isolated local runs.
  - Final hosted rerun on PR #1870 head `d165f5a1eb3bc6fbc1f2b8c47cd12e6466639ee8`: `Backend Functional Coverage` passed in 5m13s and `Verification Policy` passed; all 17 reported checks are terminal and successful.
  - No source or PRD change was made in response to unrelated flaky failures.
- **Learnings for future iterations:**
  - For broad functional-coverage flakes, isolate every named failing test and repeat it before changing untouched behavior; rerun only the failed CI jobs after the isolated characterization is clean.

---
## [2026-08-10 20:04 -07:00] - ui-packaged-factories-decoupling-001
- What was implemented
  - Moved the packaged Factory catalog translation from the top-level Models handler into the Factory Definitions HTTP adapter, backed by the injected Definitions root.
  - Preserved deterministic catalog ordering and exposed stable identity, customer-facing description, invocation examples, and backend-provided JSON/YAML artifacts through the existing generated contract.
  - Added safe internal-error mapping and actionable, payload-free catalog diagnostics for load, resolution, identity, and artifact decode failures; empty catalogs return a non-nil empty collection.
  - Wired the opened Factory Definitions root through application HTTP binding and updated the top-level server/test constructors.
- Files changed
  - `pkg/services/factory_definitions/transports/http/handlers_current_factory.go`
  - `pkg/services/factory_definitions/transports/http/handlers_current_factory_test.go`
  - `pkg/transports/http/application/handler.go`
  - `pkg/transports/http/handlers_models.go`
  - `pkg/transports/http/server.go` and HTTP route tests/support
  - `prd.json`
- Verification
  - `make generate-api` and `make api-smoke` passed; authored OpenAPI, bundled OpenAPI, generated Go contracts, and generated UI types had no drift.
  - Focused Factory Definitions, HTTP transport/contract/server, and Wire suites passed.
  - `go test ./... -run '^$' -count=1` passed as a repository-wide compile sweep.
  - `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `go vet ./...` passed.
- **Learnings for future iterations:**
  - The existing catalog route and generated contract can remain stable while ownership moves behind the Factory Definitions root; the top-level server should only compose the service-owned adapter.
  - API generation requires the worktree UI dependencies; `bun install --frozen-lockfile --ignore-scripts` restored the missing generator without running lifecycle hooks.
---

## 2026-08-10 21:39 -07:00 - ui-packaged-factories-decoupling-002
- What was implemented
  - Replaced the dashboard's publication-package source with a typed `ui/src/api/packaged-factories` client built on generated OpenAPI catalog types and a React Query server-state hook.
  - Moved catalog/detail rendering to API-shaped projections, preserving deterministic selection, localized metadata, invocation examples, JSON/YAML content, loading/empty/error/retry/success states, fail-closed selected-artifact handling, accessible keyboard selection, and responsive layout behavior.
  - Converted component and Storybook fixtures to browser/API mocks, added API-boundary tests, expanded the hosted browser scenario for keyboard/recovery/responsive checks, and added the `/packaged-factories` Vite dev/preview proxy path.
  - Removed the remaining dashboard package-data/public-contract readers and source coupling; the UI manifest, lockfile, and lifecycle workflow remain free of `@you-agent-factory/packaged-factories`.
- Files changed
  - `ui/src/api/packaged-factories/`
  - `ui/src/App.tsx`
  - `ui/src/features/packaged-factories/` API consumers, projections, tests, stories, messages, and retired package-boundary modules
  - `ui/vite.config.ts` and `ui/vite.config.test.ts`
  - `ui/integration/packaged-factories-hosted-route.integration.test.mjs`
  - `prd.json` and `progress.txt`
- Verification
  - `bun install --frozen-lockfile --ignore-scripts`, `bun run tsc`, `bun run build`, `bun run lint`, `bun test vite.config.test.ts`, targeted Biome, and `git diff --check` passed.
  - `bun run test:unit` passed on retry with 191 Bun tests and 2,956 Vitest tests.
  - `bun run test:integration` passed on retry with 14 files and 35 tests, including both hosted packaged-factories scenarios; the first run had one unrelated durable-session timing flake, which passed in isolation.
  - Focused API/projection/component suites passed 17 tests; direct browser verification passed catalog success, keyboard selection, recovery, and responsive assertions.
  - The full component suite passed all 147 files/834 tests but exceeded the local 150-second runner budget at 330.59 seconds. Repository-wide `bun run check` still reports 269 pre-existing formatting diagnostics outside the changed files; targeted changed-file checks and `bun run lint` are clean.
- **Learnings for future iterations:**
  - Dashboard API-backed surfaces should consume generated OpenAPI types through a small API module and React Query hook, keeping rendering components focused on query states and pure projections.
  - The shared browser preview uses a separate API port from the real backend; hosted browser scenarios need an explicit API route bridge while production Vite still needs the canonical proxy path.
  - Live catalog ordering can change as built-in Factories evolve, so browser selection checks should use scoped first/nth controls and semantic state rather than hard-coded catalog positions or identities.
---

## [2026-08-10 21:48 -07:00] - ui-packaged-factories-decoupling-003
- What was implemented
  - Strengthened the dashboard packaged-factories boundary guard around explicit UI manifest and source roots.
  - Added coverage for runtime, development, optional, peer, and bundled dependency declarations, plus static, dynamic, re-export, and CommonJS references to the package root or subpaths.
  - Added an isolated CLI regression fixture proving useful non-zero failure output for synthetic manifest/source violations and a compliant fixture pass.
- Files changed
  - `ui/scripts/check-packaged-factories-boundary.mjs`
  - `ui/scripts/check-packaged-factories-boundary.test.mjs`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused Vitest guard suite — 2 tests passed.
  - Live `bun run check:packaged-factories-boundary` — passed.
  - `bun run tsc` — passed.
  - `bun run lint` — passed.
  - Focused Biome and `git diff --check` — passed.
  - `bun run check` remains blocked by 268 existing generated `packages/*/dist` and root `tsconfig.json` formatting diagnostics; no changed-file diagnostics remain.
- **Learnings for future iterations:**
  - Boundary guards should expose an injectable manifest/source-root inspection function so synthetic fixtures can exercise the real CLI path without scanning unrelated repository inventories.
  - TypeScript AST inspection catches side-effect static imports and dynamic subpath imports more reliably than a `from`/`import()` regular expression, while keeping the guard limited to UI source files.
## 2026-08-10 19:12 -07:00 - wsr-001-opening-record latest-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `72537e7f2` into the WSR branch after PR #1849 became conflicting during final handoff.
  - Preserved the WSR task-scoped `prd.json`, unioned both append-only progress histories, retained current-main CLI/generated-artifact changes, and resolved the import-only functional test conflict.
- Files changed
  - Current-main CLI contract, documentation, generated-artifact, and functional-test files carried forward by the synchronization merge.
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three WSR stories marked `passes:true`; no conflict markers remain.
  - `make interfaces-all` — passed with no generated drift.
  - Worker Sessions package tests and `tests/functional/transport/run_scoped_server` — passed.
  - `git diff --cached --check` and `git diff --check` — passed before the merge commit.
- **Learnings for future iterations:**
  - A documentation-only PR update can still become conflicting when current main advances; fetch the live base immediately before handoff and validate the replacement merge head before pushing.
---

## 2026-08-10 19:38 -07:00 - wsr-001-opening-record final CI and handoff follow-up
- What was implemented
  - Re-audited PR #1849 with the thread-aware GitHub workflow; all historical conversation feedback is addressed, with zero formal reviews and zero unresolved or outdated inline threads.
  - Investigated the failed Backend Functional Coverage retry as unrelated current-main functional-test flakiness, verified the WSR-specific focused test locally, and reran only the failed CI jobs until the complete required check set passed.
  - Merged the live `origin/main` head `aac04248d` into the branch before final handoff; the merge contained only current-main development-plan documentation and had no conflicts.
- Files changed
  - `progress.txt`
  - Current-main files under `docs/internal/development/plans/` carried by the synchronization merge.
- Verification
  - `go test ./tests/functional/transport/cli/process -run '^TestCLISuccessorAfterHardKillReportsPreRuntimeStagingContention$' -count=1 -v` — passed.
  - CI run `31451707750` — all required checks, including Backend Functional Coverage and Verification Policy, passed after the failed-lane retries.
  - `prd.json` JSON validation and all three `passes:true` stories — passed; PR #1849 body remains byte-for-byte identical to `prd.json`.
  - Thread-aware PR audit — 7 conversation comments, 0 formal reviews, and 0 unresolved/outdated inline threads.
  - `git merge-tree --write-tree origin/main HEAD` and `git diff --check` — passed with `origin/main` at `aac04248d`.
- **Learnings for future iterations:**
  - When a broad functional lane fails on different timing-sensitive tests across retries, inspect the exact failed test and rerun only the failed jobs before widening an unrelated feature diff; the repository already records this as the preferred flake-handling pattern.
---

## 2026-08-10 20:00 -07:00 - wsr-001-opening-record pushed-head CI confirmation
- What was implemented
  - Recorded the final pushed-head verification after the latest-main synchronization and progress handoff commit.
- Files changed
  - `progress.txt`
- Verification
  - Head `4795b0d1c` — CI run `31453029741` is terminal and all required checks pass, including Backend Functional Coverage and Verification Policy.
  - PR #1849 is open, non-draft, `CLEAN/MERGEABLE`, and targets `origin/main` at `aac04248d`.
  - Thread-aware audit remains at 7 conversation comments, 0 formal reviews, and 0 unresolved/outdated inline threads; all three PRD stories remain `passes:true` and the PR body remains byte-identical to `prd.json`.
- **Learnings for future iterations:**
  - Treat the CI result on the pushed head, rather than the pre-push synchronization head, as the final handoff evidence.
---

## 2026-08-10 20:18 -07:00 - wsr-001-opening-record final iteration audit
- What was implemented
  - No remaining story work was required: all three WSR stories remain marked `passes:true`.
  - Re-audited existing PR #1849 after reading the thread-aware review history; the historical blocking findings have corresponding fixes, with no formal reviews or unresolved inline threads.
- Files changed
  - `progress.txt`
- Verification
  - `prd.json` parses successfully and the PR body remains byte-for-byte equal to it.
  - `gh pr checks 1849` — all listed required checks pass, including Backend Functional Coverage and Verification Policy.
  - `gh pr view 1849` — PR is open, non-draft, `CLEAN/MERGEABLE`, and targets current `origin/main` at `aac04248d`.
  - `git merge-tree --write-tree origin/main HEAD` — passed; the worktree is clean and conflict-free.
- **Learnings for future iterations:**
  - When all stories are complete and a PR already exists, perform the thread-aware review, body/PRD, current-base, mergeability, and terminal-check audit before making only the required append-only progress update.
---
## [2026-08-10 19:35 -07:00] - e2-structured-worker-results current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `aac04248d` into the E2 branch after PR #1855 reported the base had advanced again.
  - Preserved the E2 `prd.json` as authoritative, retained both branches' append-only `progress.txt` history, and regenerated the merged CLI/package artifacts from the combined authored command contract.
  - No additional structured-result product change was required; all four E2 stories remain marked `passes:true`.
- Files changed
  - `contracts/testdata/baseline/cli-commands.json`
  - `packages/api/generated/manifest.json`
  - `pkg/transports/cli/generated/family_manifests_gen.go`
  - `prd.json`
  - `progress.txt`
  - Current-main CLI/server documentation, source, test, and generated-artifact synchronization files.
- Verification
  - `go test ./pkg/transports/cli/commandidentity -run '^TestWriteProductionInventoryBaseline$' -count=1` passed after regenerating the combined CLI baseline.
  - `prd.json` parses with four passing stories; conflict-marker scan is clean.
- **Learnings for future iterations:**
  - After resolving a generated-artifact conflict, regenerate the artifact and refresh any runtime-observation baseline from the merged authored contract before staging the merge.
  - Keep the task PRD authoritative while unioning append-only progress histories when multiple worktrees share the same base.
---
## [2026-08-10 21:12 -07:00] - prose-standard-and-terms PR review follow-up
- What was implemented
  - Replaced the nonexistent `FACTORY_SESSION_OPENED` exception example with the shipped `SESSION_STARTED` Factory Event literal verified in `api/components/schemas/events/FactoryEventType.yaml`.
  - Changed the valid exception example to use `B-PROC-20` only for a genuine simultaneous-action constraint and stated that exact protected literals are not `B-LITERAL` findings.
  - Merged `origin/main` at `ee002339728765f72dab16197588664dfff163fb`, preserved this task's `prd.json`, and unioned the append-only progress patterns and history.
  - Rechecked the terminology definitions and owner references against the current architecture data model and public schemas; the required owner confirmation will be recorded in the PR conversation.
- Files changed
  - `docs/internal/standards/writing/customer-technical-writing-standard.md`
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files carried by the merge commit.
- Verification
  - `make ownership-inventory-check` passed on the synchronized head.
  - `make typecheck` passed after restoring the pinned `@types/bun` workspace dependency with `bun install --frozen-lockfile`.
  - `prd.json` parses with all three stories marked `passes:true`; conflict markers are absent and `git diff --check` passed during merge resolution.
  - The bounded `make -k lint` attempt exceeded its local timeout while several repository verification processes were already active; the focused ownership-inventory target passed, and the full required CI rerun remains the authoritative final gate.
- **Learnings for future iterations:**
  - Protected contract literals should be verified against the owning schema before they appear in normative examples; a literal-preservation case does not justify suppressing a literal-mismatch finding.
  - When a moving main branch changes shared `prd.json` and `progress.txt`, preserve the active task PRD and union the append-only patterns/history before staging the merge.
---
## [2026-08-10 21:16 -07:00] - prose-standard-and-terms owner confirmation
- What was implemented
  - Updated PR #1869's description from the current `prd.json` after the local `gh pr edit` path was blocked by the token's missing `read:project` scope; the authenticated REST fallback succeeded.
  - Posted the required PR conversation confirmation that the architecture/data-model definitions, capitalization, and ownership references were reviewed and that the internal Petri-net terms remain internal-only.
- Files changed
  - `progress.txt`
- Verification
  - Owner-confirmation comment: https://github.com/portpowered/you-agent-factory/pull/1869#issuecomment-5248916345
  - PR #1869 points to head `627b87f2a9a94eb1628211d087eb123eb8e8b9b6` and base `ee002339728765f72dab16197588664dfff163fb`.
  - The PR body contains the current `prd.json`; all three stories remain marked `passes:true`.
  - GitHub had not yet reported checks for the new head and still showed a transient dirty mergeability state; CI and final mergeability remain to be monitored.
- **Learnings for future iterations:**
  - When `gh pr edit` requires an unavailable project scope, the repository-scoped pull-request REST endpoint can update the body from the checked-in PRD, while `gh pr comment` can provide the required conversation evidence.

## [2026-08-10 20:53 -07:00] - pp-merge-context-bug delivery handoff
- What was implemented
  - No additional product change was required; both task stories remain marked `passes:true` and the existing regression/fan-in correction is complete.
  - Audited PR #1870 with the thread-aware GraphQL review workflow; there are no conversation comments, reviews, or unresolved review threads.
  - Confirmed the local branch is synchronized with current `origin/main` and the task diff remains limited to the PRD, append-only progress log, and plan-parallel regression test.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/plan_parallel -run '^TestPackagedPlanParallelMergerReceivesEveryUniqueCompletedChildResult$' -count=3 -v -timeout 600s` — passed all three runs.
  - `prd.json` parses with every story marked `passes:true`; `git diff --check` passes.
  - The existing PR body is semantically identical to the current `prd.json`; it will be refreshed from the checked-in PRD during handoff.
- **Learnings for future iterations:**
  - Before final publication, fetch the live base, inspect the three-way task diff, and audit GraphQL review threads even when the flat PR comment list is empty.
  - A completed task whose production correction is already on current main should retain its behavioral regression and evidence rather than duplicate the production fix.

## 2026-08-10 21:00 -07:00 - wsr-001-opening-record review feedback and current-main synchronization
- What was implemented
  - Addressed the latest PR feedback by removing `--with-mock-workers` from the root-built direct-JavaScript unavailable-owner route probe; the workflow only returns JavaScript and does not invoke a worker.
  - Merged the live `origin/main` head `ee0023397` while preserving the WSR PRD and combining both append-only progress histories, then regenerated public interfaces and package artifacts.
- Files changed
  - `tests/functional/transport/run_scoped_server/run_scoped_server_test.go`
  - `progress.txt`
  - Current-main synchronization and regenerated API/package files.
- Verification
  - `make interfaces-all` — passed.
  - `go test ./tests/functional/transport/run_scoped_server -run '^TestRunScopedRawJavaScriptServerReportsUnavailableWorkerSessionOwner$' -count=1 -v` — passed.
  - `go test ./pkg/services/worker_sessions/... -count=1` — passed.
  - WSR-FT-001, WSR-FT-002, and WSR-FT-003 focused functional tests — passed.
  - `prd.json` parses with all three stories marked `passes:true`; no conflict markers remain; `git diff --check` passed.
- **Learnings for future iterations:**
  - A root-built JavaScript workflow with no worker handoff does not need Mock Workers; removing the flag preserves the transport probe while satisfying the functional-test construction boundary.
  - After merging current main, regenerate interfaces from the merged authored sources before staging generated artifacts.
---
## 2026-08-10 19:25 -07:00 - ci-additive-classifier-001
- What was implemented
  - Added explicit root `frontend-verification`, `backend-verification`, and `ui-backend-integration` Make targets for direct classifier-lane reruns.
  - Composed the frontend lane from typecheck, lint, component/unit coverage, browser, Storybook, and public UI-package checks without backend verification.
  - Composed the backend lane from backend build plus the existing unit and functional coverage checks.
  - Kept UI-backend integration narrow to the real-backend durable-session browser suite, with no Storybook-only checks.
  - Updated classifier-emitted core lane commands to use the new direct targets and marked story 001 complete in `prd.json`.
- Files changed
  - `Makefile`
  - `cmd/ciclassify/main.go`
  - `cmd/ciclassify/main_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make -n frontend-verification`, `make -n backend-verification`, and `make -n ui-backend-integration` show the expected owned command scopes.
  - `go test ./cmd/ciclassify -count=1` passed.
  - `make typecheck` passed after installing the declared UI dependencies with `make ui-deps`.
  - `make ui-lint` passed.
  - `make test` passed.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - Focused Make lanes should compose existing user-facing targets with `$(MAKE)` so their dry-run expansion remains auditable and broad verification targets do not need to change.
  - The real-backend browser proof already has a dedicated `ui-durable-session-real-backend-integration-test` target; use that seam for the focused UI-backend lane instead of the broader UI integration or Storybook targets.
---

## 2026-08-10 19:31 -07:00 - ci-additive-classifier-002
- What was implemented
  - Added the focused `local-inference-verification` root Make target.
  - Composed that lane from the managed-runtime regression and exactly one real-inference approval regression, without invoking the broader specialty sweep.
  - Kept the existing direct docs, README, API package, Packaged Factories package, and Model Providers package targets as the supporting lane commands and updated classifier output to emit the new local-inference command.
  - Marked story `ci-additive-classifier-002` complete in `prd.json`.
- Files changed
  - `Makefile`
  - `cmd/ciclassify/main.go`
  - `cmd/ciclassify/main_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/ciclassify -count=1` passed.
  - `make typecheck` passed.
  - `make docs-reference-smoke`, `make readme-check`, `make api-package-verify`, `make packaged-factory-package-verify`, and `make model-provider-package-verify` passed.
  - `make long-tests-managed-runtime` passed.
  - `make -n docs-reference-smoke readme-check api-package-verify packaged-factory-package-verify model-provider-package-verify local-inference-verification` showed the expected direct scopes; the real-inference executable is Linux/runner-gated and unavailable in this Windows worktree.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - Existing package-family targets can remain the canonical direct rerun commands when they already isolate self-verification; only add a new target where an existing aggregate omits or duplicates required work.
  - The local-inference command should call `long-tests-managed-runtime` and `pr-inference-approval` directly so the single real-inference regression is visible in dry-run output.
---

## 2026-08-10 19:55 -07:00 - ci-additive-classifier-003
- What was implemented
  - Completed additive path ownership in `cmd/ciclassify`, including deterministic normalization, duplicate removal, path-order independence, neutral factory content, exact mixed-lane unions, and conservative full verification for empty, CI/tooling, Go-toolchain, and unknown inputs.
  - Tightened local-inference ownership to managed model/runtime, OMNIVOICE, and dedicated local-inference surfaces so generic provider or UI paths containing “inference” do not select the specialty lane accidentally.
  - Added API ownership for service HTTP adapters and generated UI client contracts, backend ownership for release scripts, and the `run_everything` fallback output while preserving existing lane outputs and commands.
  - Replaced broad classifier fixtures with exact complete-lane assertions for the governing matrix, factory-mixed behavior, duplicate/order invariance, ownership boundaries, and stdout/GitHub-compatible Boolean outputs.
- Files changed
  - `cmd/ciclassify/main.go`
  - `cmd/ciclassify/main_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./cmd/ciclassify -count=1` passed.
  - `go vet ./cmd/ciclassify` passed.
  - `make typecheck` and `make ui-lint` passed.
  - Focused `make -n` expansion passed for frontend, backend, UI-backend integration, local inference, docs, README, and all package-family lanes; `git diff --check` passed.
  - No `.github/workflows/**` files changed.
- **Learnings for future iterations:**
  - Keep the classifier ownership table explicit and narrow: generic filenames such as `inference` are not sufficient to identify the Linux local-inference specialty lane.
  - Assert the complete emitted lane set—not only selected lanes—because an accidental extra lane is the central cost/regression risk this classifier is designed to prevent.
---

## 2026-08-10 20:45 -07:00 - ci-additive-classifier current-main conflict follow-up
- What was implemented
  - Resolved the blocking PR conflict against current `origin/main` while preserving the CI branch’s authoritative `prd.json` and the current-main append-only progress history.
  - Reconciled the shared bookkeeping files without changing the classifier or focused Make implementation; the merge result retains the current-main structured-result changes already staged by the interrupted merge.
- Files changed
  - Current-main merge files.
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three CI stories marked `passes:true`.
  - `progress.txt` contains the current-main and CI histories with no merge conflict markers.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - For a shared task bookkeeping conflict, keep the branch-specific PRD authoritative and append the branch-specific progress entries after the current-main history; validate both parseability and marker-free output before staging.
---
## [2026-08-10 21:18 -07:00] - prose-standard-and-terms current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `4692044216626340364ac8a793881272fc2c1aac` after main advanced during the PR handoff.
  - Preserved the prose-standard task PRD and unioned the newly arrived CI-classifier progress history with the existing append-only log.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main files carried by the merge commit.
- Verification
  - `prd.json` parses with all three prose stories marked `passes:true`.
  - `progress.txt` contains both task histories with no merge conflict markers.
  - `git diff --check` passed before the merge commit.
  - The replacement head must be pushed before GitHub can recompute checks and mergeability.
- **Learnings for future iterations:**
  - Recheck the live remote main immediately before final push; another merged PR can make a previously clean synchronization stale during review handoff.
---
## [2026-08-10 21:27 -07:00] - prose-standard-and-terms final CI handoff
- What was implemented
  - Completed the review follow-up and recorded the final ready-for-review status in PR #1869.
  - Posted the final handoff comment after the corrected head passed the full required CI run.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31458112019` passed all required checks, including Backend Unit Coverage, Backend Functional Coverage, Verification Policy, and every docs, frontend, API/package, packaged-factory, model-provider, UI-integration, and local-inference lane.
  - The thread-aware review audit reports zero submitted reviews and zero inline review threads; the top-level blocking comment is addressed by the correction, owner confirmation, current-main synchronization, and green CI.
  - Final handoff comment: https://github.com/portpowered/you-agent-factory/pull/1869#issuecomment-5248982136
  - At the time of handoff, PR head was `a87f2993e`, base was `469204421`, the PR body matched `prd.json`, and all three stories were marked `passes:true`.
- **Learnings for future iterations:**
  - Treat the final CI run, thread-aware review audit, PR body/PRD comparison, and current-base SHA as one handoff bundle before marking a documentation-only PR ready for review.

## 2026-08-10 21:29 -07:00 - cli-error-classification-and-run-output-004
- What was implemented
  - Wired human progress output through the explicit terminal policy and stderr channel, preserving response-stream progress for ordinary one-shot invocations while suppressing explicit quiet-mode progress.
  - Added deterministic collision-resolved worker colors over the varied ANSI palette and retained cursor-free, ANSI-free behavior for redirected, non-interactive, and structured output.
  - Documented the interactive spinner/color/channel behavior in the packaged run guide and synchronized the branch with `origin/main` at `469204421`.
- Files changed
  - `docs/reference/run.md`
  - `pkg/services/factory_visualization/transports/cli/factory_event_renderer.go`
  - `pkg/services/factory_visualization/transports/cli/factory_event_support.go`
  - `pkg/services/factory_visualization/transports/cli/human_worker_progress.go`
  - `pkg/services/factory_visualization/transports/cli/service_test.go`
  - `pkg/transports/cli/root.go`
  - `pkg/transports/cli/root_run_factory_prompt_test.go`
  - `pkg/transports/cli/root_terminal_policy_test.go`
  - `prd.json`
  - `progress.txt`
  - regenerated CLI/package contract artifacts
- Verification
  - Focused CLI, renderer, functional output, and race tests passed after the final rebase.
  - `make test`, `make typecheck`, `make cli-manifest-check`, `make docs-reference-smoke`, `make backend-size pkg-maint pkg-file-count pkg-structure`, `make vet`, and `git diff --check` passed.
  - All four PRD stories are marked `passes:true`; generated artifacts are current and no merge conflict markers remain.
- **Learnings for future iterations:**
  - When a command has a quiet effective rendering policy but still emits an intentional one-shot response stream, keep progress gating tied to the explicit terminal policy so customer-visible response progress is not accidentally removed.
  - Assign concurrent worker colors with a deterministic collision resolver rather than relying only on a hash modulo palette size; this preserves stable identity while keeping simultaneous workers visually distinct.
---
## 2026-08-10 21:54 -07:00 - cli-error-classification-and-run-output-004 CI correction
- What was implemented
  - Corrected the CI-found stdout contamination by making worker association events progress-only and routing active-worker state through the configured stderr channel.
  - Added deterministic non-TTY progress lines without ANSI or cursor controls, preserving TTY spinner/color behavior and quiet-mode suppression.
  - Updated functional process-boundary assertions so stdout remains canonical lifecycle/primary output while redirected worker progress is verified on stderr.
- Files changed
  - `pkg/services/factory_visualization/transports/cli/factory_event_renderer.go`
  - `pkg/services/factory_visualization/transports/cli/factory_event_support.go`
  - `pkg/services/factory_visualization/transports/cli/human_worker_progress.go`
  - `pkg/services/factory_visualization/transports/cli/service_test.go`
  - `tests/functional/cli/factory_run/output/output_test.go`
  - `tests/functional/transport/cli/output/text_stream_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_visualization/transports/cli -count=1` passed.
  - `go test ./tests/functional/cli/factory_run/output ./tests/functional/transport/cli/output -count=1` passed.
  - `go test -race ./pkg/services/factory_visualization/transports/cli ./pkg/transports/cli/run -count=1` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` passed; the unrelated ACP cancellation test passed on targeted rerun after one broad-package flake.
- **Learnings for future iterations:**
  - Keep worker association/state lines on the dedicated progress channel; response-stream stdout is reserved for canonical lifecycle records and the primary result.
  - Non-TTY progress must render stable snapshots synchronously as events arrive, including intermediate active states before a batch also contains terminal events.
---
## 2026-08-10 21:58 -07:00 - cli-error-classification-and-run-output-004 CI retrigger follow-up
- What was implemented
  - Reopened PR #1872 after pushing corrective head `3b8d30a8a` because GitHub had not created a synchronize check run for the updated branch head.
- Files changed
  - `progress.txt`
- Verification
  - The PR remains open and non-draft, its body matches `prd.json`, and thread-aware review inspection still reports no comments or review threads.
  - Local corrective tests and repository gates remain green; the corrected head is awaiting a fresh GitHub Actions run.
- **Learnings for future iterations:**
  - Verify both the PR head SHA and the commit check-run list after pushing; a successful Git push does not guarantee that the hosted workflow was enqueued.
---
## 2026-08-10 22:03 -07:00 - cli-error-classification-and-run-output-004 final local verification
- What was implemented
  - Re-ran the complete repository `make test` suite after the stderr progress correction; all unit and functional packages passed.
- Files changed
  - `progress.txt`
- Verification
  - `make test` passed on the corrected local head `d8aa73d84`.
  - The hosted PR still has no check suite for the corrected head despite the branch ref being current.
- **Learnings for future iterations:**
  - A complete local test pass can validate a corrected head even when hosted check scheduling is delayed or unavailable; retain the exact head SHA and missing check-run evidence for handoff.
---
## 2026-08-10 21:38 -07:00 - pp-merge-context-bug review follow-up
- What was implemented
  - Merged the freshly fetched current `origin/main` head `469204421` into PR #1870 as merge commit `e9fa81c95`, resolving the blocking `prd.json` and `progress.txt` conflicts.
  - Preserved this task's authoritative PRD and both branches' append-only progress history.
  - Reconciled project acceptance criterion 2 so the required red evidence is tied to the exact pre-fix revision when current main already contains the correction; the PRD now explicitly records why a current-main red run is impossible and retains the historical red, current green, and root-cause evidence.
  - No additional fan-in production change was introduced; the existing behavioral regression and current-main correction remain the scoped implementation.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main merge files: `Makefile`, `cmd/ciclassify/main.go`, and `cmd/ciclassify/main_test.go`.
- Verification
  - `go test ./tests/functional/factory/packaged/plan_parallel -run '^TestPackagedPlanParallelMergerReceivesEveryUniqueCompletedChildResult$' -count=3 -v -timeout 600s` — passed all three runs.
  - Focused scheduler/dispatcher orchestration packages — passed.
  - `go test ./cmd/ciclassify -count=1` — passed.
  - `make typecheck` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make test` — passed.
  - `prd.json` parses, shared files contain no conflict markers, and `git diff --check` passed before the follow-up append.
- **Learnings for future iterations:**
  - When a PR is blocked by a moving base and shared bookkeeping conflicts, merge the live base first, keep the task PRD authoritative, union append-only progress histories, and make any impossible historical acceptance criterion explicit in the authoritative PRD rather than hiding the exception in a story note.
  - The PR body must be refreshed from the resolved checked-in `prd.json` after any criterion reconciliation, then hosted checks and thread state must be re-audited on the new head.
---
## 2026-08-10 21:56 -07:00 - pp-merge-context-bug CI flake follow-up
- What was implemented
  - Made no product or test-code changes; investigated the initial hosted `Backend Functional Coverage` failure after the conflict-resolution push.
  - Isolated the exact failing test, `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput`, which failed because its runtime log did not yet contain `factory started`; the plan-parallel package and remaining functional package inventory passed.
  - Reran that exact test 10 consecutive times locally; all ten passed.
  - Reran only the failed hosted jobs from workflow run `31459255828`; the backend functional lane and downstream Verification Policy both passed.
- Files changed
  - `progress.txt`
- Verification
  - Local isolation: `go test ./tests/functional/runtime_api -run '^TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput$' -count=10 -v -timeout 600s` — passed 10/10.
  - Hosted rerun on head `b57b8c823`: Backend Functional Coverage passed in 5m8s; Verification Policy passed; all reported checks are terminal and successful.
  - No source change was made because the isolated evidence identifies an unrelated transient runtime-log observation failure.
- **Learnings for future iterations:**
  - For a broad functional-coverage failure with one named runtime-log assertion, inspect the diagnostic artifact and timing summary, isolate the exact test repeatedly, and rerun only the failed jobs before touching unrelated product code.

## 2026-08-10 21:23 -07:00 - wsr-001-opening-record final pushed-head audit
- What was implemented
  - Recorded the final pushed-head audit after the live `origin/main` advanced to `469204421` and the current-main CI classifier merge was reconciled into the WSR branch.
  - Confirmed the latest review feedback remains addressed by the root-built JavaScript probe running without `--with-mock-workers`.
- Files changed
  - `progress.txt`
- Verification
  - Head `36686ef79` — CI run `31457898566` has all 18 listed checks terminal and successful, including Backend Functional Coverage, Backend Unit Coverage, and Verification Policy.
  - PR #1849 is open, non-draft, `CLEAN/MERGEABLE`, targets `origin/main` at `469204421`, and its body remains byte-for-byte equal to `prd.json`.
  - Thread-aware review audit reports 8 conversation comments, 0 formal reviews, and 0 unresolved/outdated inline threads.
  - `prd.json` parses with all three stories marked `passes:true`; `git merge-tree --write-tree origin/main HEAD` and `git diff --check` pass; the worktree is clean.
- **Learnings for future iterations:**
- A green PR run becomes stale when `origin/main` advances during local verification; fetch immediately before handoff, merge the new base, and wait for checks on the replacement head.
---

## [2026-08-10 19:12 -07:00] - thr-deflake-model-host-concurrent-reuse-001
- What was implemented
  - Established that the failure is a production EnsureModelHost reuse race, not a test-only choreography race.
  - Reproduced the reported symptom during an uncontrolled concurrent scheduler sweep (`process starts = 2, want 1`) and confirmed the same failure signature in GitHub Actions run `31449874394`, job `93651926054`, at `service_test.go:255`.
  - Traced the exact interleaving in `supervisor.go`: callers A and B can both return `nil` from `awaitExistingLoad` while the slot is `absent`; A then publishes `loading` with `loadDoneA`; B enters the later election lock, does not handle the already-published `loading` state, overwrites it with `loadDoneB`, and starts a second supervised process. The unsafe boundary is the split between the precheck and the loading election.
  - The diagnosis remains inside the Models runtime-host owner, with no public API or generated-contract changes. The atomic election and deterministic regression are left for story 002.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Baseline focused regression: `go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -run '^TestEnsureModelHostConcurrentReuseSharesOneSupervisedProcess$' -count=50 -v` passed once, while the concurrent scheduler sweep also observed the duplicate-start failure.
  - Sequential scheduler evidence: the same race-enabled test with `GOMAXPROCS=1`, `2`, `8`, and `32`, each at `-count=50`, passed with exit code 0.
  - `go test ./pkg/services/models/internal/services/runtime_host/internal/service`, `go vet ./pkg/services/models/internal/services/runtime_host/internal/service`, `make typecheck`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - A passing repeated test does not disprove a race when the state machine has a provable read/election gap; preserve both the probabilistic outcome and the lock-level interleaving before choosing the fix.
  - Keep the one-process assertion strict and fix the shared runtime-host election in story 002; do not add sleeps, retries, or relaxed process-count checks.
---
## [2026-08-10 19:24 -07:00] - thr-deflake-model-host-concurrent-reuse-002
- What was implemented
  - Fixed the Models runtime-host production race by moving the ready/loading/failed observation and absent-to-loading election into one mutex-protected transition in `supervisedRuntime.ensureReady`.
  - Loading followers now capture the elected attempt's completion channel and await its terminal result; the elected caller alone launches, supervises, and publishes the process, preserving the strict one-process invariant.
  - Added a test-only `BeforeLoadElection` seam and a channel/WaitGroup barrier that brings all eight `EnsureModelHost` callers to the diagnosed election boundary before release. The regression asserts every caller receives a ready/loaded host and exactly one launcher start, with no sleeps or retries.
- Files changed
  - `pkg/services/models/internal/services/runtime_host/internal/service/supervisor.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/export_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/service_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/models/internal/services/runtime_host/internal/service -count=1 -v` — passed.
  - `go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -run '^TestEnsureModelHostConcurrentReuseSharesOneSupervisedProcess$' -count=50` — passed all 50 repetitions with no race report or extra start.
  - `go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -count=1` — passed.
  - `go vet ./pkg/services/models/internal/services/runtime_host/internal/service`, `make typecheck`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - Put the single-flight decision and loading publication in the same lock-protected switch; a separate absence precheck creates a stale observation that can overwrite `loadDone` and process ownership.
  - A test-only barrier immediately before the atomic election can force all callers to contend at the diagnosed boundary while keeping production free of sleeps, retries, and timing-dependent coordination.
---
## [2026-08-10 19:41 -07:00] - thr-deflake-model-host-concurrent-reuse-003
- What was implemented
  - Completed scheduler-independent stability evidence for the Models runtime-host fix. The exact full-package command `go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -count=50` passed all 50 repetitions after the focused test was also repeated 50 times.
  - Repeated the strict concurrent-reuse regression at `GOMAXPROCS=1`, `2`, `8`, and `32`, each with `-race -count=50`; every run passed without race reports, deadlocks, failures, or a second launcher start.
  - Replaced the cancellation test's fixed 50ms startup sleep with an observable injected launcher-start channel and a bounded failure guard. The first whole-package stress attempt showed that this separate test choreography race could cancel before process launch and report `ErrAssetCancelled`; after the change the same full-package stress command passed.
  - Marked story 003 `passes:true` in `prd.json` with the commands, scheduler settings, outcomes, original CI root-cause classification, and final quality-gate evidence.
- Files changed
  - `pkg/services/models/internal/services/runtime_host/internal/service/failure_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -count=50` — passed in 22.488s.
  - `go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -run '^TestEnsureModelHostConcurrentReuseSharesOneSupervisedProcess$' -count=50` — passed in 1.411s.
  - GOMAXPROCS 1/2/8/32 named-regression runs — passed in 1.403s/1.353s/1.286s/1.276s respectively.
  - Full runtime-host race suite — passed; `make test` — passed in 380s; `make typecheck backend-size pkg-maint pkg-file-count pkg-structure vet` — passed; `git diff --check` — passed.
- **Learnings for future iterations:**
  - Treat a repeated package failure outside the target test as a separate characterization: isolate its named test, identify whether the failure is caused by a fixed timing assumption, and observe the injected runtime edge directly before changing production behavior.
  - Record both the exact package-wide race command and a low/high GOMAXPROCS matrix so reviewers can distinguish broad-suite stability from targeted scheduler contention evidence.
---
## [2026-08-10 20:04 -07:00] - thr-deflake-model-host-concurrent-reuse-003 final verification
- What was implemented
  - No additional production change; completed the post-edit broad-suite verification and retained the final timing in the append-only evidence log.
- Files changed
  - `progress.txt`
- Verification
  - The first post-edit `make test` attempt exited non-zero without diagnostic output after 518.7s; the immediate captured-output rerun completed with `make test exit code: 0` in 315.9s, with all unit packages passing.
  - The exact post-edit `go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -count=50` remained green in 22.488s.
- **Learnings for future iterations:**
  - When a long-running shell wrapper returns a silent non-zero result, rerun the same command while capturing and reporting its final output before classifying the failure or changing code.
---
## [2026-08-10 20:23 -07:00] - thr-deflake-model-host-concurrent-reuse final CI rerun
- What was implemented
  - Investigated the first PR CI failure and confirmed it was the known unrelated `TestFactorySessionHistoryIsNotPersistedByDefault` cancellation-observation flake, not a Models runtime-host regression.
  - Reran the exact failing functional test locally ten times, then reran only the failed GitHub Actions jobs; no source behavior changed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/sessions/restart -run '^TestFactorySessionHistoryIsNotPersistedByDefault$' -count=10 -v -timeout 300s` — all ten passed in 22.410s.
  - `gh run rerun 31454365225 --failed` — Backend Functional Coverage passed in 5m5s and Verification Policy passed in 4s; all other required checks remained passing.
  - PR #1868 remains open, mergeable, non-draft, and synchronized to head `0447883f3`; thread-aware review audit remains empty.
- **Learnings for future iterations:**
  - When a broad functional lane fails at an untouched cancellation observation, isolate and repeat the named test before changing task code, then rerun only the failed jobs once the characterization is clean.
---
## [2026-08-10 20:53 -07:00] - thr-deflake-model-host-concurrent-reuse review follow-up
- What was implemented
  - Replaced the pre-election arrival barrier with an `AfterLoadStateObservation` test seam that runs after each caller releases the runtime-state mutex.
  - Coordinated all eight callers at that edge; the fixed implementation atomically publishes `loading` before followers observe it, while the historical split observation/election implementation fails deterministically with `process starts = 8, want 1`.
  - Merged current `origin/main` at `ee0023397`, kept this task's `prd.json` authoritative, and retained the unrelated main-line progress history.
- Files changed
  - `pkg/services/models/internal/services/runtime_host/internal/service/supervisor.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/export_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/service_test.go`
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files carried by the merge.
- Verification
  - The corrected focused regression passed; the temporary historical implementation failed at the strict single-start assertion with `process starts = 8, want 1`.
  - `prd.json` parses with all three task stories marked `passes:true`, conflict markers are absent, and the merged worktree has no unmerged entries.
- **Learnings for future iterations:**
  - To prove a single-flight regression against a split read/election implementation, synchronize after the state-read lock is released and before the second election lock; a pre-lock arrival barrier alone does not force the unsafe interleaving.
---
## [2026-08-10 21:08 -07:00] - thr-deflake-model-host-concurrent-reuse-002 review follow-up
- What was implemented
  - Completed the blocking review follow-up for the deterministic concurrent-reuse regression. The `AfterLoadStateObservation` seam coordinates all eight callers after they release the runtime-state mutex; the production path has already published `loading` atomically, while the historical split path can be instrumented at the same post-observation/pre-election edge.
  - Reproduced the former implementation's failure in a detached temporary checkout: the strict launcher assertion reported `process starts = 8, want 1`. The corrected implementation passed the focused race-enabled regression 50 times at `GOMAXPROCS=8`.
  - Confirmed the branch already contains the current `origin/main` merge at `ee0023397`; `git merge-tree --write-tree HEAD origin/main` completed without conflicts.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `GOMAXPROCS=8 go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -run '^TestEnsureModelHostConcurrentReuseSharesOneSupervisedProcess$' -count=50` — passed all 50 repetitions in 1.457s with no race report, deadlock, failure, or extra process start.
  - Instrumented historical split implementation — failed deterministically at `service_test.go:305` with `process starts = 8, want 1 shared supervised process`.
  - `git merge-tree --write-tree HEAD origin/main`, `git diff --check`, and the conflict audit — passed; all three PRD stories remain `passes:true`.
- **Learnings for future iterations:**
  - A deterministic regression for a removed synchronization gap should identify the semantic post-observation/pre-election edge and retain a strict externally visible side-effect assertion; a pre-lock arrival barrier does not distinguish the historical implementation.
---
## [2026-08-10 21:23 -07:00] - thr-deflake-model-host-concurrent-reuse-002 current-main synchronization
- What was implemented
  - Merged the live `origin/main` head `469204421` after it advanced beyond `ee0023397`; preserved this task's authoritative `prd.json` and appended the four current-main `ci-additive-classifier` progress entries without truncating the append-only log.
  - Kept the deterministic `AfterLoadStateObservation` regression and Models runtime-host production fix unchanged while reconciling the shared bookkeeping files.
- Files changed
  - `Makefile`
  - `cmd/ciclassify/main.go`
  - `cmd/ciclassify/main_test.go`
  - `progress.txt`
- Verification
  - `GOMAXPROCS=8 go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -run '^TestEnsureModelHostConcurrentReuseSharesOneSupervisedProcess$' -count=50` — passed all 50 repetitions in 3.411s.
  - `prd.json` parses with three passing stories; `progress.txt` remains 6,872 lines before this entry, has no conflict markers, and retains both branch histories.
  - `git diff --check` and `git merge-tree --write-tree origin/main HEAD` passed with `origin/main=469204421`.
- **Learnings for future iterations:**
  - For long append-only conflict files, resolve from full Git objects or bounded patches; never reconstruct a large file from truncated shell output, and validate line count plus both branch histories before staging.
---
## [2026-08-10 21:43 -07:00] - thr-deflake-model-host-concurrent-reuse-003 CI flake follow-up
- What was implemented
  - Investigated the first post-reconciliation CI failure in run `31458338809`. Backend Functional Coverage failed only at the unrelated `tests/functional/workers/inference/codex:TestCodexGoldenTextAndToolSuccess`, whose fixture transcript omitted the expected tool lifecycle; the required real-ACP evidence artifact was consequently absent and Verification Policy failed downstream.
  - Made no production or test-code changes. Downloaded the functional diagnostics, isolated the named test locally for ten repetitions, and confirmed it passed every run.
  - Reran only the failed GitHub Actions jobs with `gh run rerun 31458338809 --failed`; the backend functional rerun and dependent Verification Policy both passed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/inference/codex -run '^TestCodexGoldenTextAndToolSuccess$' -count=10 -v -timeout 300s` — all ten passed in 9.537s.
  - Backend Functional Coverage rerun job `93678695912` — passed.
  - Verification Policy rerun job `93679483557` — passed.
  - All other required checks on the replacement head remained passing; the Models runtime-host focused race regression was unaffected.
- **Learnings for future iterations:**
- When functional diagnostics name an untouched Codex or worker fixture failure, isolate that exact test before changing task code; rerun only the failed coverage and policy jobs after the characterization passes.
---

## [2026-08-10 21:57 -07:00] - thr-deflake-model-host-concurrent-reuse final current-main reconciliation
- What was implemented
  - Merged the live `origin/main` head `7dbeb472a` after it advanced beyond the previously synchronized `469204421`.
  - Preserved this task's authoritative `prd.json`, retained the complete current-main append-only progress history, and added the task-specific concurrency history and reusable patterns without truncating either branch.
  - Carried the current-main Worker Sessions/API/generated-artifact changes through the merge; no additional Models runtime-host production change was required.
- Files changed
  - Current-main files carried by the merge.
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three task stories marked `passes:true`.
  - `progress.txt` contains 7,490 lines, no conflict markers, and both branch histories.
  - `$env:GOMAXPROCS=8; go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -run '^TestEnsureModelHostConcurrentReuseSharesOneSupervisedProcess$' -count=50` — passed all 50 repetitions in 1.650s.
  - The merge commit is `dd925e6fb`; the staged merge passed `git diff --cached --check` and the committed tree is conflict-free.
- **Learnings for future iterations:**
- Fetch `origin/main` immediately before final handoff because another merged PR can invalidate an otherwise green head; resolve shared PRD/progress files from complete Git objects and rerun the focused regression on the replacement merge head.
---

## [2026-08-10 22:16 -07:00] - thr-deflake-model-host-concurrent-reuse final CI flake follow-up
- What was implemented
  - Investigated the only failure in final-head CI run `31460167917`: Backend Functional Coverage named the unrelated `tests/functional/providers/acp:TestFactoryRunRetriesACPProviderByResumingExactSession`, which observed zero completed work after a transient ACP session-load failure.
  - Made no production or test-code changes. Isolated the exact test locally for ten repetitions; every repetition passed.
  - Reran only the failed Backend Functional Coverage and dependent Verification Policy jobs; both passed on the same pushed head.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/providers/acp -run '^TestFactoryRunRetriesACPProviderByResumingExactSession$' -count=10 -v -timeout 300s` — all ten passed in 39.776s.
  - Backend Functional Coverage rerun in `31460167917` — passed.
  - Verification Policy rerun in `31460167917` — passed.
  - All other final-head jobs remained passing, including Backend Unit Coverage, Local Inference, UI Backend Integration, and the focused Models runtime-host race regression.
- **Learnings for future iterations:**
  - When a functional coverage lane names an untouched ACP session-resume fixture and the exact test passes repeated local characterization, preserve task code and rerun only the failed coverage and policy jobs before recording the flake.
---
## [2026-08-10 22:07 -07:00] - prose-standard-and-terms-002
- What was implemented
  - Addressed the latest PR feedback for the terminology register by replacing the five self-replacing internal alternatives with distinct, actionable Petri-net expansions.
  - Documented the register invariants that alternatives must differ from their replacements and that replacements must resolve to registered canonical or approved forms.
  - Added a focused Go contract test that parses the real YAML register and rejects self-replacements and unregistered replacements.
  - Synchronized the branch with `origin/main` at `7dbeb472a8db18648bc262355f9c96d98930aabe`, preserving this task PRD and both append-only progress histories.
- Files changed
  - `docs/internal/standards/writing/customer-technical-terms.yaml`
  - `cmd/contractsvalidate/customer_terms_test.go`
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files carried by the merge.
- Verification
  - `go test ./cmd/contractsvalidate -count=1` passed.
  - `prd.json` parses with all three prose stories marked `passes:true`; conflict markers are absent.
  - `git diff --check` passed after the merge resolution.
- **Learnings for future iterations:**
  - Structured terminology alternatives need executable validation for non-no-op replacements and registered replacement targets; prose-only schema descriptions are not enough.
  - Internal implementation terms should describe their own spelling variants, not list customer concepts as alternatives when the concepts are not interchangeable.
---
## [2026-08-10 22:21 -07:00] - prose-standard-and-terms-002 verification follow-up
- What was implemented
  - Recorded the post-synchronization verification for the terminology correction and replacement-head handoff.
- Files changed
  - `progress.txt`
- Verification
  - `make typecheck` passed.
  - `make docs-reference-smoke` passed.
  - `make test` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make ownership-inventory-check` and `make ui-lint` passed.
  - `make pkg-boundary` reports the repository's known baseline violations; no `pkg/` or `ui/` files changed relative to `origin/main`.
  - The bounded `make -k lint` attempt reached its 360-second local limit without emitting a failure; the focused gates above passed, and replacement-head CI remains authoritative for the full lint policy.
  - `git diff --check` passed; the synchronized head is based directly on `origin/main` at `7dbeb472a8db18648bc262355f9c96d98930aabe`.
- **Learnings for future iterations:**
  - When the aggregate lint target exceeds the local bound, record the timeout separately and rely on its independently runnable gates plus CI rather than calling the aggregate green.
---

## 2026-08-10 22:30 -07:00 - cli-error-classification-and-run-output latest-main synchronization follow-up
- What was implemented
  - Merged the newer live `origin/main` head `b68e4bf95` after it advanced during the first PR push, preserving the CLI task PRD and the append-only histories from the plan-parallel branch.
  - Re-ran CLI manifest generation/checks against the final reconciled sources; no additional CLI product change was required.
- Files changed
  - Current-main plan-parallel regression and synchronization bookkeeping files.
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/factory/packaged/plan_parallel -count=3 -timeout 600s` passed.
  - `make typecheck`, `make backend-size pkg-maint pkg-file-count pkg-structure vet`, `make cli-manifest-generate`, `make cli-manifest-check`, and `make contracts-check` passed.
  - `prd.json` parses with all four CLI stories marked `passes:true`; no conflict markers remain and `git diff --check` passes.
- **Learnings for future iterations:**
  - A moving default branch can invalidate a clean PR handoff within one push; compare the live base again immediately before the replacement push and preserve every shared progress history while resolving the resulting bookkeeping conflicts.
---
## [2026-08-10 21:56 -07:00] - ui-packaged-factories-decoupling current-main synchronization
- What was implemented
  - Merged live `origin/main` at `7dbeb472a` before handoff after the base advanced during story implementation.
  - Preserved the three-story dashboard PRD as authoritative and retained both branches' append-only progress histories while carrying current-main source and generated-artifact changes forward.
- Files changed
  - Current-main synchronization files.
  - `prd.json`
  - `progress.txt`
- Verification
  - Merge index is resolved, `git ls-files -u` is empty, conflict markers are absent, and `git diff --check` passes.
  - All three PRD stories parse with `passes:true`.
  - Post-merge packaged-factories boundary guard and isolated 2-test suite passed.
  - Post-merge `bun run tsc`, `bun run lint`, and `bun run build` passed.
- **Learnings for future iterations:**
  - Fetch the live base immediately before pushing and resolve shared PRD/progress conflicts before opening a PR; a task branch can be clean yet already stale against the hosting base.
  - Keep the active task PRD authoritative during a shared-bookkeeping merge, while preserving unrelated current-main progress entries for auditability.
---

## [2026-08-10 22:11 -07:00] - ui-packaged-factories-decoupling backend coverage follow-up
- What was implemented
  - Investigated the first PR run's Backend Unit Coverage failure and reproduced the `pkg/services/factory_definitions/transports/http` package shortfall locally at 79.5% against the 79.67% ratchet.
  - Added observable HTTP adapter tests for unavailable roots, resolution and identity mismatches, incomplete identity/artifact/discovery metadata, and typed root-error mapping.
- Files changed
  - `pkg/services/factory_definitions/transports/http/handlers_current_factory_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused Factory Definitions HTTP tests passed.
  - Package coverage increased to 84.3%; the coverage baseline was not lowered.
  - `gofmt`, `git diff --check`, and the exact failed CI job rerun were initiated; the rerun is still pending on GitHub.
- **Learnings for future iterations:**
  - When a new backend adapter causes a small package-coverage ratchet miss, inspect the coverage profile for the adapter's unexercised failure branches and add behavior assertions before changing the manifest threshold.
## 2026-08-10 22:02 -07:00 - pp-merge-context-bug latest-main synchronization follow-up
- What was implemented
  - Fetched and merged the live `origin/main` head `7dbeb472a` into PR #1870 as merge commit `051c7bb4e` after the base advanced again.
  - Preserved the task-scoped `prd.json`, retained both branches' append-only progress history, and carried the current-main Worker Sessions/API/generated changes forward without adding another fan-in production change.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files under API, Factory Runtime, Factory Sessions, Worker Sessions, Workers, HTTP/generated clients, and related functional tests.
- Verification
  - `go test ./tests/functional/factory/packaged/plan_parallel -run '^TestPackagedPlanParallelMergerReceivesEveryUniqueCompletedChildResult$' -count=3 -timeout 600s` — passed all three runs.
  - `go test ./pkg/services/factory_runtime/internal/services/orchestration/scheduler ./pkg/services/factory_runtime/internal/services/orchestration/subsystems/... -count=1 -timeout 300s` — passed.
  - `go test ./pkg/services/worker_sessions/... -count=1 -timeout 600s` — passed.
  - `go test ./pkg/services/operator_settings/transports/cli -count=1 -timeout 300s` — passed.
  - `make typecheck` — passed.
  - `prd.json` parses, no conflict markers remain, and `git diff --check` passed before this append.
- **Learnings for future iterations:**
  - A PR can become conflicting again after a successful retry because `main` advances asynchronously; fetch the hosting ref immediately before each final push and repeat the shared-file merge protocol.
- When parallel Go test processes produce a tool subprocess failure, rerun the named package serially before treating it as a source regression.
---

## [2026-08-10 22:19 -07:00] - thr-deflake-model-host-concurrent-reuse latest-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `b68e4bf95` after it advanced beyond `7dbeb472a`.
  - Preserved this task's authoritative `prd.json`, retained both append-only progress histories, and carried the current-main plan-parallel regression changes forward without adding unrelated task behavior.
- Files changed
  - `pkg/services/factory_runtime/internal/services/orchestration/packaged/plan_parallel/invocation_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three task stories marked `passes:true`; `progress.txt` contains both branch histories and no conflict markers.
  - `$env:GOMAXPROCS=8; go test -race ./pkg/services/models/internal/services/runtime_host/internal/service -run '^TestEnsureModelHostConcurrentReuseSharesOneSupervisedProcess$' -count=50` — passed all 50 repetitions in 2.205s.
  - `git diff --cached --check` passed for the resolved merge, and the merge tree contains no unmerged entries.
- **Learnings for future iterations:**
  - Main can advance again while a targeted rerun is in flight; merge the latest hosting ref before the final push and combine the synchronization evidence with the merge commit so CI runs on the actual handoff head.
---
## [2026-08-10 22:30 -07:00] - prose-standard-and-terms current-main synchronization follow-up
- What was implemented
  - Merged the newly advanced live `origin/main` head `b68e4bf956fc127b346e61f76e033a0223175fc2` into the prose branch.
  - Preserved the prose task PRD, unioned all bounded conflict regions from both append-only progress histories, and retained current-main source and generated changes.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files carried by the merge.
- Verification
  - `prd.json` parses with all three prose stories marked `passes:true`.
  - Conflict-marker scan and `git diff --check` pass after resolution.
  - The replacement head must be pushed before GitHub can recompute checks and mergeability.
- **Learnings for future iterations:**
  - Fetch `origin/main` immediately before each replacement push; a second independent merge can advance the base while hosted checks are still pending.
---

## 2026-08-10 22:50 -07:00 - prose-standard-and-terms-002 review follow-up
- What was implemented
  - Removed `cmd/contractsvalidate/customer_terms_test.go` because it only scanned the authored YAML source and was out of scope for this documentation-only story.
  - Updated the terminology story note so the checked-in PRD describes the shipped register and review evidence accurately.
  - Merged `origin/main` at `ecd1d248731c9108dbbc4dafe2f28c4120043346`, retaining the prose task PRD, unioning the append-only progress history, and carrying the current-main model runtime changes forward.
- Files changed
  - `cmd/contractsvalidate/customer_terms_test.go`
  - `prd.json`
  - `progress.txt`
  - Current-main model runtime files carried by the merge.
- Verification
  - `prd.json` parses with all three prose stories marked `passes:true`.
  - Conflict-marker scan is clean and `git diff --check` passes.
  - The merge resolution preserves the branch-scoped PRD and both task histories; final merge-tree and hosted checks remain to be verified after the merge commit is pushed.
- **Learnings for future iterations:**
  - Documentation-only acceptance criteria can justify parser review evidence, but a test that only reads and validates source-file shape is a prohibited meta test unless a real enforcement consumer makes that behavior user-visible.
  - Resolve a moving-base conflict before final review handoff, then rerun validation on the replacement head rather than relying on checks from the old base.
---

## 2026-08-10 23:16 -07:00 - prose-standard-and-terms final CI follow-up
- What was implemented
  - Recorded the replacement-head CI result after the out-of-scope terminology meta-test removal and current-main synchronization.
  - Isolated the first Backend Unit Coverage failure to a 0.2742 percentage-point fluctuation in `pkg/services/chat_sessions/internal/responsebridge`; the exact current-main commit `ecd1d2487` passed its own Backend Unit Coverage job.
  - Reran only the failed Backend Unit Coverage job `93693324933`, which passed in 5m08s, and the dependent Verification Policy job `93694199719`, which passed.
- Files changed
  - `progress.txt`
- Verification
  - Replacement CI run `31463651328` has all required lanes terminal and successful after the targeted rerun, including Backend Unit Coverage, Backend Functional Coverage, Verification Policy, and every docs, frontend, API/package, packaged-factory, model-provider, UI-integration, and local-inference lane.
  - The branch head remains `4a8bd201d`, the base is `ecd1d2487`, and the PR body matches `prd.json` byte-for-byte.
- **Learnings for future iterations:**
  - When a hosted coverage ratchet misses its package minimum by a small amount, compare the exact base commit, inspect the named package, and rerun only the failed coverage and dependent policy jobs before changing baselines or product code.
---

## 2026-08-10 23:24 -07:00 - prose-standard-and-terms final handoff audit
- What was implemented
  - Corrected the final handoff record to identify the pushed bookkeeping head `82ddeb66d`.
  - Recorded the thread-aware review audit and final mergeability state after the green replacement CI run.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31464508923` is terminal and green across every required lane, including Backend Unit Coverage, Backend Functional Coverage, Verification Policy, and all docs, frontend, API/package, packaged-factory, model-provider, UI-integration, and local-inference lanes.
  - The PR head is `82ddeb66d`, the base is `ecd1d2487`, `git merge-tree --write-tree origin/main HEAD` is clean, the worktree is clean, and GitHub reports the PR mergeable.
  - The PR body matches `prd.json` byte-for-byte. The thread-aware GraphQL audit reports 8 conversation comments, 0 formal reviews, 0 inline review threads, 0 unresolved threads, and 0 outdated threads; the latest blocking feedback is addressed by the scoped test removal, current-main synchronization, targeted coverage rerun, and green final CI.
- **Learnings for future iterations:**
  - Treat the final pushed head, current base, PR body/PRD equality, hosted checks, and thread state as one audit bundle; append a correction if bookkeeping is committed after an earlier handoff record.
## 2026-08-10 22:44 -07:00 - cli-error-classification-and-run-output hosted functional failure correction
- What was implemented
  - Updated the run-scoped server functional assertion to reflect the CLI contract introduced by this story: active dispatch and worker progress is emitted on stderr while the customer invocation result remains on stdout.
  - Investigated the hosted runtime-log failure separately; it was the same timing-sensitive `factory started` observation that passes when isolated and repeated locally.
- Files changed
  - `tests/functional/transport/run_scoped_server/run_scoped_server_test.go`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/run_scoped_server -run TestRunScopedServerAndSiteOwnNamedAndFileInvocationLifecycles -count=1 -timeout 180s` — passed.
  - `go test ./tests/functional/runtime_api -run TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput -count=5 -timeout 300s` — passed all five runs.
  - Hosted run `31461891506` identified the stale stderr assertion as the cause of the two run-scoped failures; the runtime-log failure remains an isolated timing flake.
- **Learnings for future iterations:**
  - When a CLI output contract moves operator progress to stderr, update broader functional invocation tests to assert the progress channel explicitly instead of retaining an empty-stderr expectation.
  - A missing downstream evidence artifact can be a consequence of an earlier functional assertion failure; inspect the first failing test before treating artifact publication as the root cause.
---
## [2026-08-10 23:16 -07:00] - cli-error-classification-and-run-output PR handoff verification
- What was implemented
  - Reproduced the two failing tests from hosted run `31461891506` after the PR’s stderr-output correction; both passed in ten repeated local executions, so no additional product change was justified.
  - Merged the live `origin/main` head `ecd1d2487` into the branch, regenerated the CLI manifest, and preserved the existing PRD with all four stories marked `passes:true`.
  - Audited PR #1872 conversation comments, reviews, and inline review threads; none are present or unresolved.
- Files changed
  - `progress.txt`
- Verification
  - `make cli-manifest-generate` produced no diff.
  - Focused renderer, CLI functional, typecheck, backend-size, pkg-maint, pkg-file-count, pkg-structure, vet, CLI manifest/contract, functional-scenario, and docs-reference gates passed on the reconciled head.
  - `TestFunctionalAPIServer_RuntimeLogDirectoryIsAProcessInput` passed 10/10; `TestRunScopedServerAndSiteOwnNamedAndFileInvocationLifecycles` passed 10/10.
  - The broad `make test` unit lane returned a single `pkg/wire` failure under aggregate parallel execution; the exact `go test ./pkg/wire -short -count=1 -v -timeout 600s` rerun passed, identifying the broad failure as a concurrency/resource-sensitive flake rather than a task regression.
- **Learnings for future iterations:**
  - When an aggregate Go test lane reports only one package failure, rerun that package serially with verbose output before changing source; package-level success can distinguish a scheduler/resource flake from a behavioral regression.
  - For GitHub Actions failures in a multi-job run, fetch the exact failing job log rather than the whole run log so downstream policy failures do not obscure the first failing test.
---
## [2026-08-10 23:25 -07:00] - cli-error-classification-and-run-output hosted CI handoff
- What was implemented
  - Pushed the reconciled branch head `55eebb269` to PR #1872 after the current-main merge and final manifest/check verification.
  - Completed the fresh hosted CI run and confirmed the PR is ready for review with no blocking review feedback.
- Files changed
  - `progress.txt`
- Verification
  - Hosted run `31464542975` passed all required jobs, including Classify Verification, Backend Unit Coverage, Backend Functional Coverage, Verification Policy, typecheck/frontend, API/contract, packaged-factory, local-inference, and UI-backend lanes.
  - PR #1872 is open, non-draft, `MERGEABLE`, points at `55eebb269`, and its description matches `prd.json` byte-for-byte.
  - Thread-aware GraphQL audit reports zero conversation comments, review submissions, and inline review threads.
- **Learnings for future iterations:**
  - After a final bookkeeping-only push, wait for the replacement hosted run instead of relying on the previous head’s green checks; review readiness is a property of the exact pushed SHA.

## [2026-08-10 22:36 -07:00] - ui-packaged-factories-decoupling current-main conflict resolution and CI follow-up
- What was implemented
  - Investigated PR #1873 after `origin/main` advanced from `7dbeb472a` to `b68e4bf95` and GitHub reported the previously green PR as conflicting.
  - Merged the latest `origin/main` into this branch as merge commit `717698495`, kept the dashboard `prd.json` authoritative, and unioned the append-only progress histories.
  - Isolated the unrelated Backend Functional Coverage flakes: the runtime-log assertion passed on retry, a separate packaged-loop scheduling timeout failed on the next retry, and the third exact-job retry passed.
- Files changed
  - `progress.txt`
  - Current-main synchronization changes, including `tests/functional/factory/packaged/plan_parallel/invocation_test.go`
- Verification
  - The CI run for head `8411a2cb7` is terminal and green after the third Backend Functional Coverage attempt, including Verification Policy; Backend Unit Coverage remains green with the 84.3% adapter coverage.
  - PR #1873 has no comments or reviews. The merge conflict is resolved, `git ls-files -u` is empty, conflict markers are absent, `prd.json` parses with all stories passing, and `git diff --check` passes.
- **Learnings for future iterations:**
  - When a required functional job fails on changing timing-sensitive tests, inspect the exact test output and retry only that job; do not alter unrelated product code.
  - A PR can become conflicting after its checks pass when `main` advances; synchronize again immediately before the final push and treat the replacement head as a new CI gate.
---

## [2026-08-10 22:47 -07:00] - ui-packaged-factories-decoupling latest-main reconciliation
- What was implemented
  - Fetched the next live `origin/main` head `ecd1d2487` after it advanced during the final PR audit and made PR #1873 conflicting again.
  - Merged that current main into this branch as merge commit `5e4b645cd`, retained the dashboard `prd.json` as authoritative, and preserved the automatically merged append-only progress history.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main model-host synchronization files under `pkg/services/models/internal/services/runtime_host/internal/service/`
- Verification
  - The merge is resolved with no unmerged entries or conflict markers; `prd.json` remains valid and all three dashboard stories remain marked `passes:true`; `git diff --check` passes.
  - The prior synchronized head `ec87a80c9` had terminal green required CI, but this replacement head needs its own CI run after push.
- **Learnings for future iterations:**
  - Main can advance while the final PR audit or CI run is in progress; fetch immediately before each handoff push and regard every replacement head as requiring fresh checks.
---

## [2026-08-10 22:58 -07:00] - ui-packaged-factories-decoupling final handoff audit
- What was implemented
  - Recorded the terminal PR handoff after the latest-main reconciliation and refreshed PR #1873's body from the canonical `prd.json`.
- Files changed
  - `progress.txt`
- Verification
  - Head `be7038ef5` matches the pushed branch, targets current `origin/main` `ecd1d2487`, and CI run `31462842504` is terminal and fully green, including Verification Policy.
  - PR #1873 is `CLEAN/MERGEABLE`; issue comments, review comments, and formal reviews are all zero.
  - The PR body matches `prd.json` after newline normalization, all three stories parse with `passes:true`, the worktree is clean, no unmerged entries or conflict markers remain, and `git diff --check` passes.
- **Learnings for future iterations:**
  - If `gh pr edit --body-file` is blocked by a missing read-only scope, update only the pull-request body through the authenticated GitHub API using the canonical file content, then verify the returned body and head.
## [2026-08-10 22:52 -07:00] - provider-capability-contract current-main conflict resolution
- What was implemented
  - Merged the newly advanced live `origin/main` at `ecd1d248a` into the provider capability contract branch after PR #1874 became conflicting during final CI observation.
  - Preserved the provider task's authoritative `prd.json`, unioned the current-main and provider append-only `progress.txt` histories and reusable patterns, and carried the four current-main Models runtime-host files forward.
  - Reran `make interfaces-all`; the canonical generated tree remained unchanged.
- Files changed
  - `pkg/services/models/internal/services/runtime_host/internal/service/export_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/failure_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/service_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/supervisor.go`
  - `progress.txt`
- Verification
  - `make interfaces-all` — passed with no generated diff.
  - `go test ./pkg/services/models/internal/services/runtime_host/internal/service -count=1 -timeout 180s` — passed.
  - `go test ./pkg/services/providers/... -count=1 -timeout 300s` — passed.
  - `prd.json` parses with all three stories marked `passes:true`; conflict markers are absent; `git diff --check` passes.
- **Learnings for future iterations:**
  - A green PR can become conflicting while its checks are running; fetch `origin/main` again immediately before push, resolve shared PRD/progress files from complete Git objects, and rerun generation plus focused owner tests on the replacement merge state.
---

## [2026-08-10 21:03 -07:00] - provider-capability-contract-001
- What was implemented
  - Added the truthful provider capability vocabulary to authored OpenAPI components: native CLI/ACP harness metadata, typed ACP resource delivery, model-catalog posture, four-state support, directional modality routes, modality transports and media constraints, tool availability/default enablement/tool outputs, and bounded evidence records.
  - Added offline catalog semantic validation for duplicate facts, evidence-reference closure, qualified conditional claims, non-unknown evidence requirements, route/support compatibility, ACP resource routing, unknown tool availability defaults, and direct versus tool-mediated output contradictions.
  - Preserved legacy manifests during the additive migration by requiring evidence as soon as a manifest opts into expanded capability metadata; native manifest migration is intentionally deferred to Story 002.
  - Kept generated conditional requirements visible in the published TypeScript provider types and split the focused invalid-fact cases to satisfy backend-size and pkg-maint ratchets.
- Files changed
  - Authored provider OpenAPI schema components and `api/openapi-main.yaml`.
  - `internal/providercatalog/capabilities.go`, `internal/providercatalog/capabilities_test.go`, and `internal/providercatalog/validation.go`.
  - Regenerated bundled OpenAPI, Go/TypeScript HTTP clients, UI package client, provider catalog schemas, provider package types/metadata, and package generator tests.
  - `prd.json` marks Story 001 passed; Stories 002 and 003 remain pending.
- Verification
  - `go test ./internal/providercatalog ./packages/model-providers ./pkg/services/providers/internal/services/catalog/internal/service -count=1` passed.
  - `make test` passed; `make typecheck` passed; `make api-smoke` passed; `make provider-catalog-check`, `make model-provider-package-smoke`, and `make contracts-check` passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure` and `make vet` passed.
  - Commits: `b89ad8392` capability contract implementation and `b6c640724` focused test-ratchet fix.
- **Learnings for future iterations:**
  - Expanded capability metadata must activate strict evidence validation without breaking the pre-migration manifests; the next story should add evidence to all three native manifests before turning that migration lane green.
  - One-of conditional requirements need both authored schema requirements and generator support for parent properties so published TypeScript types retain the required condition field.
---
## [2026-08-10 21:23 -07:00] - provider-capability-contract-002
- What was implemented
  - Migrated the antigravity, claude, and codex authored manifests to the new native capability vocabulary with native_cli harness metadata, exact model-catalog posture, complete directional harness routes, evidence-qualified model facts, and tool availability/output metadata.
  - Preserved existing provider identity, aliases, model IDs, efforts, limits, discovery prerequisites, execution capabilities, response-fidelity capabilities, and established discovery projection behavior.
  - Corrected Codex direct image output to unsupported/none and represented AGY image generation as supported tool-mediated output; unknown AGY output routes remain explicit rather than inferred.
  - Added golden generated-catalog assertions for all four support states, evidence-reference closure, tool availability/default enablement, and direct versus tool-mediated image output.
- Files changed
  - `packages/model-providers/providers/antigravity/provider.yaml`
  - `packages/model-providers/providers/claude/provider.yaml`
  - `packages/model-providers/providers/codex/provider.yaml`
  - `packages/model-providers/generated/catalog.json`
  - `packages/model-providers/metadata/manifest.json`
  - `packages/model-providers/catalog_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./internal/providercatalog ./packages/model-providers ./pkg/services/providers/... -count=1` — passed.
  - `go run ./cmd/providercatalogcheck -root .` — passed.
  - `make model-provider-package-check` and `make typecheck` — passed.
  - `git diff --check` — passed.
  - Implementation commit: `cd33d4205` (`feat(providers): publish native capability manifests`).
- **Learnings for future iterations:**
  - The existing provider projection accepts the established supported/unsupported tool states; keep additive conditional and unknown route facts in the published manifest without changing that projection boundary until the dedicated discovery-parity story updates its public contract.
  - Quote ISO evidence dates in authored YAML so yaml.v3 keeps them as strings for calendar-date validation.
---
## [2026-08-10 21:55 -07:00] - provider-capability-contract-003
- What was implemented
  - Preserved discovery parity while accepting the additive capability vocabulary: conditional and unknown routes/tools are validated but omitted from the legacy two-state projection instead of being downgraded into false claims.
  - Added observable provider projection, CLI output, generated-schema/declaration, and representative contract-fixture coverage for deterministic ordering, identity, models, efforts, limits, prerequisites, execution capabilities, response fidelity, nullability, evidence references, and additive metadata.
  - Confirmed static provider discovery remains offline and inert through the root-build functional test.
- Files changed
  - `pkg/services/providers/internal/services/catalog/internal/service/projection.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/projection_test.go`
  - `pkg/services/providers/internal/services/catalog/internal/service/service_test.go`
  - `pkg/services/providers/transports/cli/list_capability_output_test.go`
  - `scripts/model-provider-package.test.mjs`
  - `api/testdata/provider-catalog-contract.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make interfaces-all`, `make provider-catalog-generate`, and `make model-provider-package-generate` — passed with no generated diff.
  - `make provider-catalog-check`, `make model-provider-package-check`, `make contracts-check`, `make typecheck`, and `make api-smoke` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` and `make test` — passed.
  - Focused provider/catalog/CLI/package/contract tests and `go test ./tests/functional/providers/discovery/... -run '^TestProvidersListThroughRootBuildProcess$' -count=1` — passed.
  - `git diff --check` and clean generated-state checks — passed.
- **Learnings for future iterations:**
  - When a richer provider manifest is consumed by a legacy projection, preserve the richer facts at the contract boundary and make the projection conservative; additive unknowns must not become legacy assertions.
  - Run generation and drift checks serially in a shared worktree when repository-wide Go builds are competing for resources; parallel lanes can time out without indicating a code failure.
---
## [2026-08-10 22:04 -07:00] - provider-capability-contract final pre-push audit
- What was implemented
  - Merged the live `origin/main` at `7dbeb472a` into the completed provider capability contract branch.
  - Preserved this branch's authoritative `prd.json`, combined both reusable Codebase Patterns sections, retained append-only progress history, and regenerated the API package manifest through `make interfaces-all`.
- Files changed
  - `prd.json` and `progress.txt` conflict resolution.
  - Canonical generated artifacts regenerated by `make interfaces-all`.
  - Merge commit `f29ee5447`.
- Verification
  - `make api-smoke`, `make provider-catalog-check`, `make model-provider-package-check`, `make contracts-check`, and `make typecheck` — passed.
  - `go test ./pkg/services/providers/... -count=1` and the offline root-built discovery test — passed.
  - `prd.json` parses with all three stories marked `passes:true`; all merge markers are absent; `git diff --check` passes.
- **Learnings for future iterations:**
  - Resolve generated conflicts by rerunning their canonical generator after the base merge; only hand-resolve human-authored task bookkeeping and preserve the branch-specific PRD as the PR description source.
---
## [2026-08-10 22:24 -07:00] - provider-capability-contract latest-base refresh
- What was implemented
  - Merged the newly advanced live `origin/main` at `b68e4bf95` into the provider capability contract branch after the first pushed PR head became stale against main.
  - Preserved the provider-specific PRD and append-only progress history, then regenerated all canonical API, contract, catalog, and model-provider package artifacts.
- Files changed
  - `prd.json` and `progress.txt` conflict resolution.
  - Merge commit `2323dc0de`.
- Verification
  - `make interfaces-all`, `make provider-catalog-generate`, and `make model-provider-package-generate` — passed with a clean generated tree.
  - `prd.json` parses with all three stories marked `passes:true`; no conflict markers remain; `git diff --check` passes.
- **Learnings for future iterations:**
  - Fetch the hosting `main` ref immediately before each PR push; concurrent mainline merges can invalidate an otherwise clean merge and require a fresh CI attempt.
---
## [2026-08-10 22:37 -07:00] - provider-capability-contract final handoff audit
- What was implemented
  - No additional product change was required; all three provider-capability-contract stories remain marked `passes:true`.
  - Audited PR #1874 with the thread-aware GraphQL workflow: there are zero conversation comments, formal reviews, or unresolved/outdated inline review threads.
  - Confirmed the existing non-draft PR is mergeable, its body matches `prd.json`, its head is the latest pushed branch commit, and the live `origin/main` base has not advanced.
- Files changed
  - `progress.txt`
- Verification
  - PR #1874 hosted checks are terminal and successful, including Verification Policy.
  - `prd.json` parses with all three stories marked `passes:true`.
  - `git merge-tree --write-tree origin/main HEAD` and `git diff --check` pass; the worktree was clean before this append.
- **Learnings for future iterations:**
  - A completed provider-contract task can be handed off without new product edits when the existing PR already has a byte-equivalent PRD body, a clean current-main merge state, terminal green checks, and an empty thread-aware review audit.
---
## 2026-08-10 19:28 -07:00 - bun-compat-batch-migration-001
- What was implemented
  - Completed the API-01 cohort handoff using the five existing native Bun owners on current `main`: bundled-file preservation, layout preservation, lifecycle controls, generated session-factory OpenAPI types, and import discovery.
  - Confirmed the retired Vitest `.test.ts` paths are absent and no product behavior, generated artifact, coverage membership, or browser-test ownership changed.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `make packaged-factory-catalog-generate` — correctly failed at canonical validation with `work type "loop-controller" must declare a completion state`; no generated files were changed.
  - `git diff --check` — passed after restoring the partial cleanup.
  - `prd.json` parses successfully with both remaining stories still marked `passes:false`.
- **Learnings for future iterations:**
  - Catalog semantic reachability cannot be solved by deleting an unused terminal declaration in isolation; the general validator runs first and requires a real completion path for referenced Work types.
  - Keep `loop-controller:stopped` and `quorum:task:complete` as explicit lifecycle/topology decisions until direct runtime behavior or a narrow, tested validator contract exists; do not add projection-only exceptions.
---

## [2026-08-11 01:03 -07:00] - graph-workstation-semantics-002
- What was implemented
  - Removed `loop-controller:stopped` and `quorum:task:complete` from the canonical first-party Factory definitions, regenerated their flattened artifacts and manifest hashes, and kept the earlier `goal:execute` cleanup intact.
  - Added route-proven canonical validation for recurring controller self-loops and composite terminal outcomes, while continuing to reject ordinary nonterminal Work types without a completion role.
  - Enabled the first-party semantic role validator on the real `BuildCatalog` path and added a synthetic catalog regression proving the Factory slug and exact `workType:state` diagnostic; synthetic generator fixtures and JavaScript/customer-authored scope remain unaffected.
  - Added exact published state-inventory regression coverage, a graph projection test proving `work-state:goal:execute` is absent, and aligned the lifecycle diagram and automation fixture with the shipped topology.
- Files changed
  - `docs/internal/development/plans/packaged-factory-families.md`
  - `internal/packagedfactorycatalog/catalog.go`
  - `internal/packagedfactorycatalog/definition_catalog_test.go`
  - `internal/packagedfactorycatalog/inventory_test.go`
  - `packages/packaged-factories/factories/loop/factory.yaml`
  - `packages/packaged-factories/factories/quorum/factory.yaml`
  - `packages/packaged-factories/generated/factories/loop/`
  - `packages/packaged-factories/generated/factories/quorum/`
  - `packages/packaged-factories/generated/manifest.json`
  - `pkg/services/automations/internal/invocation_schedule_test.go`
  - `pkg/services/factory_definitions/internal/services/validation/impl/validate_structural.go`
  - `pkg/services/factory_definitions/internal/services/validation/impl/topology_validator_test.go`
  - `ui/src/features/workflow-activity/lib/current-activity-factory-graph-layout.test.ts`
  - `prd.json`
- Verification
  - `make packaged-factory-catalog-generate` and `make packaged-factory-catalog-check` — passed.
  - Catalog, canonical-validator, automation, and packaged loop/quorum functional tests — passed.
  - Focused UI graph test, UI typecheck, UI lint, package-structure, focused Go vet, and `git diff --check` — passed.
  - Full `make vet` was bounded at 360 seconds while unrelated repository-wide Go verification was already running in the shared worktree; the focused vet lane passed.
- **Learnings for future iterations:**
  - Enforce publication-only semantic rules at `BuildCatalog`, not `Discover` or `GenerateArtifacts`, because unit fixtures intentionally use canonical but not first-party-complete topology.
  - A Work type may omit a terminal state only when route topology proves a recurring self-loop or a composite terminal outcome; ordinary nonterminal loops remain rejected.
  - Removing speculative states requires a coordinated canonical-validator decision before regeneration; regenerate published manifest hashes from authored sources.
---

## [2026-08-11 01:08 -07:00] - graph-workstation-semantics-001 acceptance audit
- What was implemented
  - Completed the one-story acceptance audit for the first-party packaged Factory disconnected-state rule.
  - Confirmed the parsed-definition validator is enforced by `BuildCatalog`, accounts for initial admission, workstation routes and lifecycle outputs, classification outputs, explicit invocation returns, and the named scheduler overlap bridge, and remains out of scope for JavaScript/customer-authored Factories.
  - Confirmed focused regression coverage rejects synthetic processing and terminal disconnected states with the Factory slug and exact `workType:state` identities.
  - `bun test src/api/factory-definition/preserve-bundled-files.bun.unit.test.ts src/api/factory-definition/preserve-layout.bun.unit.test.ts src/api/factory-sessions/lifecycle-controls.bun.unit.test.ts src/api/generated/openapi.session-factory.bun.unit.test.ts src/api/session-factory/import-activation.discover.bun.unit.test.ts` — 5 files / 17 tests passed, 35 expect calls.
  - Dashboard-unit Vitest selection of all five migrated paths and all five retired paths — `No test files found` in each selection, confirming zero Vitest execution.
  - `bun run check:test-lanes` — passed.
  - `bun run typecheck` — passed.
- **Learnings for future iterations:**
  - A cohort already merged to current `main` still needs direct focused Bun execution, retired-path absence, Vitest exclusion, lane-contract, and typecheck evidence before its batch PRD story can be marked complete.
  - A fresh worktree may contain only Bun’s temporary UI install directory; `bun install --frozen-lockfile` restores the pinned Vitest and TypeScript tools without tracked dependency changes.
---

## 2026-08-10 19:43 -07:00 - bun-compat-batch-migration-002
- What was implemented
  - Verified the API-02 migration already present in the branch history: session routing, session scope, and prompt-template API contracts execute exclusively in the native Bun unit lane.
  - Confirmed the three-file cohort preserves all 20 baseline cases, including default/selected session routing, pause-state scoping, typed prompt-template errors, network failures, and invalid-response handling.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./internal/packagedfactorycatalog -count=1` — passed.
  - `make packaged-factory-catalog-check` — passed; generated catalog is current.
  - `go test ./pkg/services/factory_definitions/internal/services/validation/impl ./pkg/services/automations/internal -count=1` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - A story can be marked complete only after its focused behavior, publication integration, scope exclusions, and generated-output checks are all re-run in the same acceptance audit.
---

## [2026-08-11 01:31 -07:00] - graph-workstation-semantics-004 review follow-up
- What was implemented
  - Normalized trimmed blank workstation IDs to absent in the Factory graph editor’s authored-semantics join, restoring the name fallback for legacy topology nodes whose ID field is empty.
  - Added an editor-flow regression that renders a legacy blank-ID topology with authored agent-run and repeater semantics and asserts both visible labels survive the join.
  - Updated the existing missing-metadata editor assertion to require the documented neutral unknown presentation rather than the removed generic workstation label.
- Files changed
  - `ui/src/features/factory-graph-editor/lib/projection/factory-graph-react-flow-projection.ts`
  - `ui/src/features/factory-graph-editor/components/flow/factory-graph-editor-flow.test.tsx`
  - `progress.txt`
  - `prd.json`
- Verification
  - Focused Biome check for the projection and editor-flow test — passed.
  - Focused editor component lane — passed: 1 file, 11 tests.
  - Focused React Flow projection unit lane — passed: 1 file, 25 tests.
  - `git diff --check` — passed.
  - Committed as `6498ef3c2` (`fix(graph-editor): normalize blank workstation ids`).
- **Learnings for future iterations:**
  - Legacy graph topology can preserve an empty ID even when the authored definition omits it; normalize both join inputs before deciding whether durable identity or name fallback applies.
  - Component tests that render React Flow nodes catch stale neutral-fallback expectations that pure projection tests do not expose.
  - `bun test src/api/session-factory/prompt-template/api.bun.unit.test.ts src/api/session-routing.bun.unit.test.ts src/api/session-scope.bun.unit.test.ts` — 3 files / 20 tests passed, 41 expect calls.
  - `vitest run --config vitest.lanes.config.ts --project=dashboard-unit --maxWorkers=4 --retry=0` with all three `.bun.unit.test.ts` owners — `No test files found`.
  - The same `dashboard-unit` selection with all three retired `.test.ts` paths — `No test files found`; the legacy paths are absent.
  - `bun run check:test-lanes` — passed.
  - `bun run typecheck` — passed.
- **Learnings for future iterations:**
  - The next API cohort was already migrated in the branch ancestry, so the required work was to reproduce the frozen focused/lane/typecheck evidence and record it rather than duplicate test owners.
  - For Bun migrations, Vitest’s expected no-file exit is code 1; capture the output and treat `No test files found` as the zero-selection assertion.
---

## 2026-08-10 19:48 -07:00 - bun-compat-batch-migration-003
- What was implemented
  - Completed the dashboard components-01 cohort handoff using the four existing native Bun owners for fixture catalog exports, runtime overlay projections, dashboard test-fixture exports, and typography-role mappings.
  - Preserved all 11 baseline named/executable cases and confirmed the retired Vitest owners are absent; no production or public contract behavior changed.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `bun test src/components/dashboard/fixtures/public-api.bun.unit.test.ts src/components/dashboard/fixtures/runtime.bun.unit.test.ts src/components/dashboard/test-fixtures.bun.unit.test.ts src/components/dashboard/typography.bun.unit.test.ts` — 4 files / 11 tests passed, 46 expect calls.
  - Pinned `vitest.exe` dashboard-unit selection of all four migrated paths and all four retired `.test.ts` paths — `No test files found` in each selection, confirming zero Vitest execution and no fallback copies.
  - `bun run check:test-lanes` — passed.
  - `bun run typecheck` — passed.
- **Learnings for future iterations:**
  - When a cohort is already migrated in branch ancestry, reproduce its exact focused Bun count, both Vitest exclusion selections, lane-contract, and typecheck evidence before marking the batch story complete.
  - On Windows PowerShell, use the repository-pinned `ui\\node_modules\\.bin\\vitest.exe` for direct lane checks when the bare `vitest` command is not on PATH.
---

## 2026-08-10 19:52 -07:00 - bun-compat-batch-migration-004
- What was implemented
  - Completed the current-factory-definition-01 cohort handoff using the seven existing native Bun owners for runner selection, default handling, worker/workstation taxonomy, editable document values, workstation guards, Go-duration parsing, and cron validation.
  - Preserved all 39 source-level named cases and the cron descriptor parameterization's seven expanded cases for the exact 46-case executable total; the retired Vitest paths are absent.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused `bun test` — 7 files / 46 tests passed, 101 expect calls.
  - Pinned `vitest.exe` `dashboard-unit` selection of all seven migrated paths and all seven retired `.test.ts` paths — `No test files found` in each selection, confirming zero Vitest execution and no fallback copies.
  - `bun run check:test-lanes` — passed.
  - `bun run typecheck` — passed.
- **Learnings for future iterations:**
  - A cohort already migrated in branch ancestry still needs direct focused Bun execution, exact source/expanded-case reconciliation, retired-path absence, both Vitest exclusion selections, lane-contract, and typecheck evidence before its batch story can be marked complete.
---

## 2026-08-10 19:58 -07:00 - bun-compat-batch-migration-005
- What was implemented
  - Verified the current-factory-definition-02 workstation-helper cohort already present in branch ancestry: progress/failure outcome routes, worker assignment, editable workstation resolution, model-invoke bindings, and workstation type conversion execute exclusively in the native Bun unit lane.
  - Preserved all 41 frozen source-level named and executable cases; no legacy Vitest owner or fallback copy remains.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused `bun test` — 5 files / 41 tests passed, 76 expect calls.
  - Pinned `vitest.exe` `dashboard-unit` selection of all five migrated `.bun.unit.test.ts` paths — `No test files found` with captured exit code 1.
  - Pinned `vitest.exe` selection of all five retired `.test.ts` paths — `No test files found` with captured exit code 1; all legacy paths are absent.
  - `bun run check:test-lanes` — passed.
  - `bun run typecheck` — passed.
- **Learnings for future iterations:**
  - When a cohort’s source migration is already in branch ancestry, complete the story with direct focused execution, exact baseline reconciliation, both Vitest exclusion selections, lane-contract, and typecheck evidence rather than creating duplicate owners.
  - The workstation-helper cohort has no parameterized cases, so its source-level and executable totals must remain identical at 41.
---

## 2026-08-10 20:03 -07:00 - bun-compat-batch-migration-006
- What was implemented
  - Verified the current-selection-04 request-helper cohort already present in branch ancestry: explicit-map/runtime-snapshot projection, provider-session filtering, request ordering, dispatch-attempt merging, and request-owned work-item behavior remain covered by the native Bun owner.
  - Preserved all five frozen source-level named and executable cases; the retired Vitest owner is absent and no fallback copy remains.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `bun test src/features/current-selection/hooks/helpers/useCurrentSelection.request-helpers.bun.unit.test.ts` — 1 file / 5 tests passed, 19 expect calls.
  - Pinned `vitest` `dashboard-unit` selection of the migrated `.bun.unit.test.ts` path — `No test files found` with exit code 1.
  - Pinned `vitest` selection of the retired `.test.ts` path — `No test files found` with exit code 1; the legacy path is absent.
  - `bun run check:test-lanes` — passed.
  - `bun run typecheck` — passed.
- **Learnings for future iterations:**
  - A migration that predates the current batch still needs exact baseline-case reconciliation, focused Bun execution, both Vitest exclusion selections, lane-contract, and typecheck evidence before its story is marked complete.
  - For this cohort, the source-level and executable totals are both five because no parameterized cases expand the baseline.
---

## 2026-08-10 20:08 -07:00 - bun-compat-batch-migration-007
- What was implemented
  - Verified the current-selection-06 selected-work relationship cohort already present in branch ancestry: graph construction and dashboard relation projection execute exclusively through four native Bun owners.
  - Preserved the frozen connected, empty, unavailable, direct-relation, fallback-edge, and repeated `DEPENDS_ON` contracts without changing product code or test behavior.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused `bun test` passed 9/9 across 4 files with 26 expect calls, matching the nine baseline named cases.
  - Pinned `vitest.exe` `dashboard-unit` selection of all four migrated `.bun.unit.test.ts` paths returned `No test files found` with expected exit code 1.
  - The four retired `.test.ts` paths are absent; selecting them in `dashboard-unit` also returned `No test files found` with expected exit code 1.
  - `bun run check:test-lanes` and `bun run typecheck` passed.
- **Learnings for future iterations:**
  - When a frozen cohort is already migrated in branch ancestry, complete the story by reconciling the baseline names/counts and reproducing focused Bun, both Vitest exclusion, lane-contract, and typecheck evidence before changing task metadata.
  - The relationship cohort's nine source-level cases remain nine executable cases because it has no parameterized tests; the existing baseline report already contains the matched latency evidence for the later aggregate audit.
---

## 2026-08-10 20:13 -07:00 - bun-compat-batch-migration-008
- What was implemented
  - Completed the current-selection-08 workstation-selection cohort handoff using the four existing native Bun owners for workstation summary values, editable session-draft mutators, cron-draft transitions, and overwrite-field projection.
  - Preserved all 34 frozen named/executable cases and confirmed the retired Vitest owners are absent; no product behavior or public contract behavior changed.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `bun test` passed 34/34 across 4 files with 46 expect calls.
  - Pinned `vitest.exe` `dashboard-unit` selection of all four migrated `.bun.unit.test.ts` paths returned `No test files found` with exit code 1.
  - The four retired `.test.ts` paths are absent; selecting them in `dashboard-unit` also returned `No test files found` with exit code 1.
  - `bun run check:test-lanes` and `bun run typecheck` passed.
- **Learnings for future iterations:**
  - The final frozen cohort was already migrated in branch ancestry, so story completion required reproducing exact focused Bun counts, both Vitest exclusion selections, retired-path absence, lane-contract, and typecheck evidence before changing task metadata.
  - The workstation-selection-08 cohort has no parameterized cases; its source-level and executable totals remain identical at 34.
---

## 2026-08-10 20:45 -07:00 - bun-compat-batch-migration-009
- What was implemented
  - Completed the final batch audit: all eight frozen cohorts reconcile to 33 unique native Bun files, 176 source-level named cases, and 183 executable cases; all retired Vitest owners are absent.
  - Ran one focused Bun command over all 33 migrated files (183/183 passing, 390 expect calls) and pinned Vitest `dashboard-unit` selections for both migrated and retired paths; both selections returned `No test files found`.
  - Recorded the frozen per-cohort warm focused medians and a matched aggregate `bun run test:unit -- --logHeapUsage --no-color` comparison in `prd.json`; the full workload remains 390 files / 3,161 tests, with current after median 82.935s versus 95.89s before (12.955s / 13.5% lower).
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `bun test <all 33 migrated cohort paths>` — passed 183/183 across 33 files with 390 expect calls.
  - Pinned `vitest.exe` `dashboard-unit` selections of all migrated `.bun.unit.test.ts` paths and all retired `.test.ts` paths — both returned `No test files found` with exit code 1.
  - `bun run test:unit -- --logHeapUsage --no-color` — three aggregate samples passed; current output was 35 Bun files / 191 tests plus 355 Vitest files / 2,970 tests, preserving 390 files / 3,161 tests overall; wrapper samples were 90.657s, 82.935s, and 55.186s.
  - `bun run check:test-lanes`, `bun run typecheck`, and `bun run lint` — passed.
  - Browserless component sublanes — 860 Bun tests and 829 Vitest tests passed with no assertion failures; the Windows wrapper measured 323.43s against the CI-configured 300s duration guard, so the host-only performance-budget overrun is recorded separately from test correctness.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
- **Learnings for future iterations:**
  - The full unit wrapper's unchanged workload is the strongest aggregate parity check: sum Bun and remaining Vitest outputs before comparing wall time, because the leased cohort is only part of the native Bun inventory.
  - `test:component` can report a nonzero result solely for its wall-clock budget after every component assertion passes; preserve the sublane pass counts and host/budget context in review evidence.
## 2026-08-10 20:50 -07:00 - bun-compat-batch-migration-009 current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `ee0023397` into the Bun migration branch after PR #1871 reported a dirty merge state.
  - Preserved the Bun migration `prd.json` as authoritative and unioned the unrelated E2 synchronization entry into the append-only `progress.txt` history.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main merge files.
- Verification
  - The merge conflict set was limited to the shared `prd.json` and `progress.txt` bookkeeping files; no product or generated-file conflict required a task-side resolution.
  - `prd.json` parses with all nine Bun migration stories marked `passes:true`.
  - Conflict-marker scan and `git diff --check` passed.
- **Learnings for future iterations:**
  - When a task branch is behind current main, keep the active task PRD authoritative and merge unrelated progress entries into the append-only log before refreshing the PR body.
---

## 2026-08-10 21:27:58 -07:00 - bun-compat-batch-migration-009 current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `469204421` into the Bun migration branch to clear PR #1871's current-base conflict.
  - Preserved the Bun migration `prd.json` as authoritative and unioned the current-main CI-classifier history into the append-only `progress.txt` log.
- Files changed
  - `Makefile`
  - `cmd/ciclassify/main.go`
  - `cmd/ciclassify/main_test.go`
  - `progress.txt`
- Verification
  - `bun run check:test-lanes` passed from `ui/`.
  - `bun run typecheck` and `bun run lint` passed from `ui/`.
  - `go test ./cmd/ciclassify -count=1` and `go vet ./cmd/ciclassify` passed.
  - `prd.json` parses with all nine Bun migration stories marked `passes:true`; conflict-marker and whitespace checks passed.
- **Learnings for future iterations:**
  - Bun scripts are owned by `ui/package.json`; run lane, typecheck, and lint commands with `ui/` as the working directory.
---
## 2026-08-10 22:22:17 -07:00 - bun-compat-batch-migration-009 current-main synchronization follow-up
- What was implemented
  - Merged the live `origin/main` head `7dbeb472a` into the Bun migration branch after PR #1871 reported a dirty base.
  - Preserved the Bun migration `prd.json` as authoritative and unioned the complete current-main history with all Bun migration entries in the append-only `progress.txt` log.
  - No product or generated-file conflict required task-side changes; current-main implementation files remain the synchronized merge result.
- Files changed
  - Current-main synchronization files.
  - `prd.json`
  - `progress.txt`
- Verification
  - Exact focused Bun cohort — 33 files / 183 tests / 390 expect calls passed.
  - `bun run check:test-lanes` and `bun run typecheck` passed from `ui/`.
  - `bun run lint` passed from `ui/`.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `prd.json` parses with all nine stories marked `passes:true`; conflict-marker and whitespace checks passed.
- **Learnings for future iterations:**
  - When a current-main merge conflicts only in shared bookkeeping, reconstruct the append-only log from complete merge stages in bounded chunks, validate object counts, and keep the active task PRD authoritative before staging.
---
## 2026-08-10 22:27:32 -07:00 - bun-compat-batch-migration-009 current-main synchronization follow-up
- What was implemented
  - Merged the newly advanced live `origin/main` head `b68e4bf956` into the Bun migration branch after the first replacement head became stale during verification.
  - Preserved the Bun migration `prd.json` as authoritative and retained the complete append-only progress history, including the prior 7dbeb472a synchronization entry.
  - Carried the unrelated current-main packaged-factory test change without adding task-specific product changes.
- Files changed
  - Current-main synchronization files.
  - `prd.json`
  - `progress.txt`
- Verification
  - Exact focused Bun cohort — 33 files / 183 tests / 390 expect calls passed on the second synchronized tree.
  - `bun run check:test-lanes` and `bun run typecheck` passed from `ui/`.
  - `prd.json` parses with all nine stories marked `passes:true`; conflict-marker and whitespace checks passed.
- **Learnings for future iterations:**
- Fetch the live base immediately before pushing and again after local verification; another merged main commit can make a freshly synchronized PR dirty before its checks begin.
---
## 2026-08-10 22:49:55 -07:00 - bun-compat-batch-migration-009 current-main synchronization follow-up
- What was implemented
  - Fetched and merged the live `origin/main` head `ecd1d248731c9108dbbc4dafe2f28c4120043346` into the Bun migration branch as `c86a0c190` after the PR became dirty again.
  - Preserved the Bun migration `prd.json` as authoritative and retained the append-only `progress.txt` history; the incoming implementation changes are the current-main runtime-host reuse/cancellation fixes.
- Files changed
  - `pkg/services/models/internal/services/runtime_host/internal/service/export_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/failure_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/service_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/supervisor.go`
  - `progress.txt`
- Verification
  - Exact focused Bun cohort — 33 files / 183 tests / 390 expect calls passed under Bun 1.3.12.
  - `go test ./pkg/services/models/internal/services/runtime_host/internal/service -count=1 -timeout 300s` — passed.
  - `bun run check:test-lanes`, `bun run typecheck`, and `bun run lint` — passed from `ui/`.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `prd.json` parses with all nine stories marked `passes:true`; conflict-marker and whitespace checks passed.
- **Learnings for future iterations:**
- A replacement PR head must be validated against the exact live base after merging; the required CI run and mergeability state belong to that replacement head, not the prior reviewed commit.
---

## 2026-08-10 23:48 -07:00 - provider-capability-contract current-main review follow-up
- What was implemented
  - Addressed PR #1874's blocking review feedback by merging the latest fetched `origin/main` head `85619c087`.
  - Kept the provider-specific `prd.json` authoritative and reconstructed the append-only `progress.txt` from the complete merge stages, preserving both branch histories and reusable Codebase Patterns.
  - No product or generated-file conflict required a source change; the merge conflict was limited to task bookkeeping.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three `provider-capability-contract` stories marked `passes:true`.
  - `progress.txt` contains no merge markers and retains both provider and incoming mainline histories.
  - `git diff --check` passes.
- **Learnings for future iterations:**
  - For a moving-base conflict in long append-only logs, resolve from complete bounded Git-stage slices, keep the active task PRD authoritative, and validate both histories before staging.
 ---
 ---

## 2026-08-11 -07:00 - thr-split-acp-stdio-tests review follow-up
- What was implemented
  - Addressed PR #1861's blocking feedback by consolidating all cross-suite ACP stdio fixtures into `session_test_helpers_test.go` and removing the alternate stream helper file.
  - Reworked the move-only split against current `origin/main` so the package remains at the exact 22-file baseline; cohesive declarations were consolidated into existing test owners rather than increasing the deletion-only file-count debt.
  - Routed chat-session store construction through the shared helper so the package-boundary report has no new finding, and moved the affected maintainability exemption targets with their test declarations.
- Files changed
  - `pkg/transports/acp/internal/stdio/*_test.go`
  - `docs/internal/baselines/backend-exemption-budget.json`
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - Against `origin/main` `3a0b571b2`, normalized package test inventory was 222 before and after with zero diff and SHA-256 `da83686cb68f0988570ca0ea0f429eabf40beb2bb47dba967874a33d46481bf1`; coverage remained 94.3% of statements.
  - Focused ACP stdio tests, the concurrent-attachment race test (three runs), package vet, backend-size, pkg-maint, pkg-file-count, typecheck, boundary checks, and `git diff --check` passed.
- **Learnings for future iterations:**
  - Same-package deletion-only splits must be planned around the existing package-file baseline; moving declarations into existing cohesive owners keeps the file-count ratchet stable.
  - Cross-suite fixtures are easiest to audit when one helper file owns all shared fakes and setup, including fixtures that originated in a stream-specific suite.
  - A boundary-sensitive wire constructor can remain a single known finding when its construction is centralized in the shared test helper instead of repeated in behavioral files.
---
## 2026-08-10 23:49 -07:00 - prose-standard-and-terms latest-main synchronization follow-up
- What was implemented
  - Recorded that the prose branch was synchronized with live `origin/main` at `85619c087` after the prior final CI run.
  - Preserved this task's authoritative `prd.json` and retained the complete append-only progress history while carrying current-main changes forward.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files carried by the merge.
- Verification
  - The pre-synchronization head `55dd2a815` passed CI run `31466033222` across every required lane, including Backend Unit Coverage, Backend Functional Coverage, and Verification Policy.
  - `prd.json` parses with all three prose stories marked `passes:true`; conflict-marker scan, `git diff --check`, and `git merge-tree --write-tree origin/main HEAD` are clean after the merge.
  - The replacement head created by this bookkeeping append must pass its own hosted checks before final handoff.
- **Learnings for future iterations:**
  - Main can advance during the final audit; fetch immediately before pushing the final progress record and attach CI evidence to the exact head it verifies.
---
## [2026-08-11 00:25 -07:00] - cli-error-classification-and-run-output review follow-up
- What was implemented
  - Reconciled the branch with live `origin/main` at `85619c087089e3ed6a04edad8b794de62785ee30`; the only merge conflicts were the shared `prd.json` and append-only `progress.txt` files, which were resolved by preserving this task PRD and both histories.
  - Addressed the blocking functional-test review finding by converting the changed CLI output scenarios in the factory-run, text-stream, and run-scoped-server suites from Mock Workers to `serviceedges.Edges.ProviderCommandRunner` with protocol-valid Codex decision-envelope responses. Existing unrelated Mock Workers feature tests remain unchanged.
- Files changed
  - `prd.json`
  - `progress.txt`
  - `tests/functional/cli/factory_run/output/output_test.go`
  - `tests/functional/transport/cli/output/text_stream_test.go`
  - `tests/functional/transport/run_scoped_server/run_scoped_server_test.go`
- Verification
  - Focused functional suites passed: `go test ./tests/functional/cli/factory_run/output ./tests/functional/transport/cli/output ./tests/functional/transport/run_scoped_server -count=1 -timeout 600s`.
  - Visualization package suite passed: `go test ./pkg/services/factory_visualization/... -count=1 -timeout 600s`.
  - `make typecheck`, `make backend-size pkg-maint pkg-file-count pkg-structure vet`, `make cli-manifest-generate`, `make cli-manifest-check`, `make cli-contract-smoke`, `make docs-reference-smoke`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - Functional coverage that changes CLI behavior must keep the provider-edge construction preference visible in the changed scenario itself; a pre-existing Mock Workers helper can still violate review expectations when the assertion begins proving provider-backed output.
  - After resolving shared bookkeeping conflicts, regenerate the authored CLI manifest and validate the PRD before staging so generated artifacts and the task body remain aligned.
---
## [2026-08-11 00:32 -07:00] - cli-error-classification-and-run-output latest-main synchronization
- What was implemented
  - Merged the newer live `origin/main` head `3a0b571b234f70d68908e36669100d0ac9f66a9f` after it advanced during the provider-edge review verification window; the incoming API/model-provider changes were carried through the merge and the task PRD remained authoritative.
  - Regenerated the CLI manifest after the merge and retained the provider-edge functional-test corrections unchanged.
- Files changed
  - Current-main API/model-provider files carried by the merge.
  - `prd.json`
  - `progress.txt`
  - `packages/api/generated/manifest.json`
- Verification
  - Focused functional suites passed on the replacement head: `go test ./tests/functional/cli/factory_run/output ./tests/functional/transport/cli/output ./tests/functional/transport/run_scoped_server -count=1 -timeout 600s`.
  - `go test ./pkg/services/factory_visualization/... -count=1 -timeout 600s` passed.
  - `make typecheck`, `make backend-size pkg-maint pkg-file-count pkg-structure vet`, `make cli-manifest-generate`, `make cli-manifest-check`, `make cli-contract-smoke`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - Fetch and merge the live base immediately before the replacement push; main advanced again while the prior local gate set was running, and the affected tests must be rerun on the actual pushed ancestry.
---
## [2026-08-11 00:40 -07:00] - cli-error-classification-and-run-output hosted CI handoff
- What was implemented
  - Completed the replacement-head review follow-up for PR #1872. The provider-edge test conversion and current-main reconciliation are now pushed; this entry records the green hosted run immediately before the final bookkeeping push.
  - Audited the PR with the thread-aware GitHub workflow after the push: there are no review submissions or inline review threads; the existing top-level blocker comment is superseded by the reconciled head, provider-edge corrections, and passing replacement checks.
- Files changed
  - `progress.txt`
- Verification
  - Hosted run `31469400154` for head `7aa3b26a01ea010fa4383822c89ea229d9e8d5fd` passed every required check: Classify Verification, Backend Unit Coverage, Backend Functional Coverage, API Contract And Package, frontend/UI lanes, Local Inference, packaged/model-provider packages, and Verification Policy.
  - PR #1872 remains open and non-draft with title `cli-error-classification-and-run-output`; its base is `3a0b571b234f70d68908e36669100d0ac9f66a9f` and it is mergeable pending repository review/merge policy.
- **Learnings for future iterations:**
  - When a bookkeeping-only progress commit follows a green hosted run, trigger and wait for the replacement run on the new head before declaring review readiness; the prior run is evidence for the preceding implementation head only.
## [2026-08-10 23:55 -07:00] - ui-packaged-factories-decoupling PR review follow-up
- What was implemented
  - Split the reported 122-line `TestListPackagedFactories_CoversBoundaryFailures` into focused missing-root, resolve-failure, identity-mismatch, incomplete-definition, and typed-root-failure tests, using a shared request helper without reducing behavioral assertions.
  - Merged live `origin/main` at `85619c087089e3ed6a04edad8b794de62785ee30`; kept this task's `prd.json` authoritative and retained both branches' append-only progress histories.
- Files changed
  - `pkg/services/factory_definitions/transports/http/handlers_current_factory_test.go`
  - `prd.json`
  - `progress.txt`
  - Current-main merge files.
- Verification
  - `make backend-size` passed with all owned Go backend functions within the 100-line limit.
  - `go test ./pkg/services/factory_definitions/transports/http -count=1` and `go test ./pkg/services/factory_definitions/... -count=1` passed.
  - `prd.json` parses with all three stories marked `passes:true`; conflict-marker, unmerged-entry, and `git diff --check` audits passed.
- **Learnings for future iterations:**
  - Resolve shared `prd.json`/`progress.txt` merge conflicts by preserving the active task PRD and unioning both complete append-only histories; validate JSON, marker absence, and index state before committing.
---
## [2026-08-11 00:21 -07:00] - ui-packaged-factories-decoupling CI flake follow-up
- What was implemented
  - Investigated the first replacement-head Backend Functional Coverage failure, which named the untouched `TestProvidersACPSerializesConcurrentPromptsOnOneStdioConnection` ACP fixture and observed a transient failed session after a peer connection close.
  - Made no product or test-code changes. Isolated the exact test locally for ten repetitions; all ten passed.
  - Reran only the failed Backend Functional Coverage and dependent Verification Policy jobs on the same pushed head; both passed.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/providers/acp -run '^TestProvidersACPSerializesConcurrentPromptsOnOneStdioConnection$' -count=10 -v -timeout 600s` — all ten passed.
  - Backend Functional Coverage rerun job `93705509715` — passed.
  - Verification Policy rerun job `93706601140` — passed.
  - All other replacement-head checks remained green; no task file was named by the failure.
- **Learnings for future iterations:**
  - For an untouched functional fixture that passes repeated local characterization, preserve task code and rerun only the failed coverage/policy jobs before widening the diff.
---
## [2026-08-11 00:22 -07:00] - ui-packaged-factories-decoupling latest-main synchronization
- What was implemented
  - Merged the newly advanced live `origin/main` head `3a0b571b234f70d68908e36669100d0ac9f66a9f` after the prior CI rerun became stale against the moving base.
  - Preserved this task's authoritative `prd.json` and unioned the incoming provider-catalog API/generated-artifact history into the append-only `progress.txt` log.
- Files changed
  - Current-main provider catalog API, generated-contract, and package files.
  - `prd.json`
  - `progress.txt`
- Verification
  - The merge conflict set was limited to shared bookkeeping; no task-side product conflict required additional behavior changes.
  - `prd.json` parses with all three stories marked `passes:true`; conflict-marker, unmerged-entry, and `git diff --check` audits passed.
  - The previous replacement head's required CI rerun was terminal-green; this latest-base replacement needs a fresh run after push.
- **Learnings for future iterations:**
  - Fetch `origin/main` immediately before each final push because a moving base can invalidate a green replacement head; resolve shared PRD/progress files before starting the next CI gate.
---
## 2026-08-11 00:34:30 -07:00 - ui-packaged-factories-decoupling final PR handoff audit
- What was implemented
  - Audited PR #1873's only blocking conversation comment against the replacement head: the 122-line backend-size test was split, live `origin/main` is included, and no unresolved review threads remain.
  - Confirmed all three PRD stories remain marked `passes:true`; no additional product or generated-contract changes were needed.
  - Refreshed the PRD review metadata for the mergeable, non-draft replacement head.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses and all three story pass flags are true.
  - Live `origin/main` `3a0b571b2` is an ancestor of HEAD `37142ba1a`.
  - PR #1873 is open, non-draft, mergeable, and all 18 reported checks are terminal-success.
  - Thread-aware PR audit found one historical conversation comment and zero review threads; its requested test split/base synchronization are present on the current head.
  - The PR body matched the pre-update `prd.json` byte-for-byte before this metadata refresh.
- **Learnings for future iterations:**
  - A prior blocking conversation comment can be treated as addressed only after the requested diff is present, the live base is an ancestor, and the replacement-head checks are terminal-success; verify all three together before final handoff.
---

## [2026-08-11 01:45 -07:00] - graph-workstation-semantics current-main synchronization
- What was implemented
  - Merged the fetched live `origin/main` head `1b871e3a8` into the graph-workstation-semantics branch after PR #1877 reported the stale base and shared-file conflicts.
  - Preserved this task’s authoritative `prd.json`, retained all four graph-workstation story pass flags and the editor review follow-up, and unioned incoming mainline history into the append-only `progress.txt` log.
- Files changed
  - Current-main implementation and generated contract files from `origin/main`.
  - `prd.json`
  - `progress.txt`
- Verification
  - Conflict resolution was limited to shared `prd.json`/`progress.txt` bookkeeping; no graph product file conflicted with current main.
  - `prd.json` parses with all four `graph-workstation-semantics` stories marked `passes:true`.
  - `progress.txt` and `prd.json` contain no merge markers; `git diff --check` passed.
  - The merge remains unpushed and requires focused post-merge gates plus a fresh PR check run.
- **Learnings for future iterations:**
  - After a long moving-base merge, validate the active task PRD and append-only history independently of the incoming task’s bookkeeping before staging the merge commit.
---

## [2026-08-11 01:50 -07:00] - graph-workstation-semantics post-merge gate characterization
- What was implemented
  - Re-ran the reviewer-requested repository gates on the current-main merge head without changing the remaining backend validation files.
- Files changed
  - `progress.txt`
- Verification
  - `make test` — passed; the full short Go unit lane completed successfully on the synchronized head, so the earlier unit-lane `Error -1` did not reproduce.
  - `make backend-size` — blocked by `pkg/services/factory_definitions/internal/services/validation/impl/topology_validator_test.go` at 1107 lines and `validate_structural.go` at 1037 lines, both over the 1000-line limit.
  - `make pkg-maint` — failed on the same two file-line violations.
- **Learnings for future iterations:**
  - The remaining quality-gate work is an isolated backend file split; preserve the validator behavior and its existing test evidence while moving cohesive helpers/tests under the established package boundaries.
---

## [2026-08-11 02:19 -07:00] - graph-workstation-semantics review follow-up
- What was implemented
  - Restored the shared customer Factory validator to require completion states without inferring exceptions from CRON self-loops or composite topology shape.
  - Kept the two lifecycle exceptions in `internal/packagedfactorycatalog`, tied them to exact `builtin-loop`/`builtin-quorum` identities and named workstation bridges, and routed packaged installation through a catalog-owned preparation hook only.
  - Split the oversized topology validator test/helpers so the backend-size and package-maintainability gates pass, while preserving observable missing-completion assertions.
  - Preserved the blank workstation-ID editor join fix from the prior review iteration.
- Files changed
  - `internal/packagedfactorycatalog/{artifacts.go,definition_catalog_validation.go,factory_validation.go,inventory.go,work_state_roles.go,work_state_roles_test.go}`
  - `pkg/services/factory_definitions/internal/services/catalog/persistence/service.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `pkg/services/factory_definitions/internal/services/validation/impl/{topology_validator_test.go,validate_structural.go,work_type_outcome_test.go}`
  - `progress.txt`
- Verification
  - `go test ./internal/packagedfactorycatalog ./pkg/services/factory_definitions/... -count=1` — passed.
  - `make backend-size pkg-maint` — passed.
  - `make test` — passed.
- **Learnings for future iterations:**
  - Publication-specific lifecycle allowances must be applied at the packaged-installation boundary; filtering the shared customer validator would make topology guesses valid for ordinary authored Factories.
  - The packaged installer can preserve strict generic persistence by detecting a narrow optional preparation capability on the catalog persistence implementation, with test doubles continuing to use the ordinary path.
---
## 2026-08-11 01:23:11 -07:00 - prose-standard-and-terms-001 latest normative review follow-up
- What was implemented
  - Updated the normative customer-writing standard to enumerate the complete governing blocking and advisory rule sets, including semicolons, contractions, public-term replacements, canonical term casing, suppression directives, baseline lifecycle, and parse failures.
  - Marked suppression, baseline, and parse lifecycle rules as future-checker blocking rules that are not manually applicable in this documentation-only story, while keeping ordering and sequence in human review.
  - Clarified protected-literal repair: align a stale documentation copy to a correct owning source; escalate an incorrect source to its owner and update dependents together; never use an exception to hide a contract mismatch.
  - Preserved the documentation-only scope; no checker, source-scanning test, runtime, API, UI, generated artifact, or customer-corpus rewrite was added.
- Files changed
  - `docs/internal/standards/writing/customer-technical-writing-standard.md`
  - `prd.json`
  - `progress.txt`
  - Current-main synchronization files carried by the merge.
- Verification
  - PRD JSON and terminology YAML parsed successfully; all 3 stories remain `passes:true` and all 59 term records retain the documented shape.
  - `git diff --check`, `make docs-reference-smoke`, and `make typecheck` passed.
  - `make ownership-inventory-check`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `make vet` passed individually on the synchronized head.
  - Head `make -k lint` passed UI lint and structural checks; its known baseline failures were 175 `pkg-boundary` findings, the packaged-factory consumption migration finding, and deadcode drift from 350 baseline findings to 543 current findings. The same three failures and counts were reproduced on the detached `origin/main` audit worktree at `1b871e3a8`, so no new lint violation is attributable to this story. The aggregate vet subprocess returned `-1` under host contention, while standalone `make vet` passed.
- **Learnings for future iterations:**
  - Mirror the governing plan's exact rule inventory in a normative standard so future checker and reviewer work share stable IDs and boundaries.
  - On Windows, run repository-wide Go gates sequentially; concurrent invocations can exhaust the host and leave timeout evidence that differs from standalone gate results.
  - Fetch the live base immediately before the final merge and push; `origin/main` advanced again during this audit.
## [2026-08-11 01:43:17 -07:00] - cli-error-classification-and-run-output review follow-up
- What was implemented
  - Merged the live `origin/main` head `1b871e3a84125400ca3f04b2bdf9fd2658ac5c45`, preserved this task's authoritative PRD, and unioned both append-only progress histories without conflict markers.
  - Expanded the authored `you run` command contract to describe accepted Work/dependency output, stderr-only worker/workstation progress, TTY color/spinner policy, deterministic redirected output, quiet suppression, and structured-output isolation; regenerated the CLI family and published contract artifacts through the repository targets.
  - Migrated a dispatch's assigned progress color to its associated Worker Session ID when association arrives in a later event batch, and added a deterministic 12-worker split-batch regression covering palette capacity, terminal cleanup, and replacement workers.
- Files changed
  - `contracts/cli/commands.json`
  - `packages/api/generated/cli/commands.json`
  - `packages/api/generated/manifest.json`
  - `pkg/services/factory_visualization/transports/cli/human_worker_progress.go`
  - `pkg/services/factory_visualization/transports/cli/service_test.go`
  - `pkg/transports/cli/generated/family_manifests_gen.go`
  - `prd.json`
  - `progress.txt`
  - Current-main packaged-factories files carried by the synchronization merge.
- Verification
  - `go test ./pkg/services/factory_visualization/... -count=1 -timeout 180s` — passed.
  - `go test -race ./pkg/services/factory_visualization/transports/cli -count=1 -timeout 180s` — passed.
  - `make typecheck`, `make cli-manifest-generate`, `make cli-manifest-check`, and `make cli-contract-smoke` — passed.
  - `make backend-size`, `make pkg-maint`, `make pkg-file-count`, and `make pkg-structure` — passed.
  - The combined static-gate command and the standalone `make vet` invocation each reached the six-minute host command bound while unrelated verification processes were competing for the Windows host; the exact timed-out process trees were inspected and terminated, with no source failure emitted. `git diff --check` passed.
- **Learnings for future iterations:**
  - The published CLI copy is refreshed by `make contracts-generate` after the authored contract changes; `make cli-manifest-generate` refreshes the family/runtime projections and the smoke check catches published drift.
  - When broad Go verification is run concurrently across worktrees on Windows, isolate the exact timed-out process tree before retrying a gate so other task verification remains undisturbed.
---

## [2026-08-11 02:38 -07:00] - graph-workstation-semantics current-main replacement verification
- What was implemented
  - Merged live `origin/main` at `0e7df069f` into the branch, preserved this task's authoritative `prd.json`, and unioned the incoming CLI progress history with the append-only task log.
  - Recorded the intentional validation test split in the backend package file-count baseline.
- Files changed
  - `docs/internal/baselines/backend-package-file-count.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./internal/packagedfactorycatalog -count=1` — passed.
  - `go test ./pkg/services/factory_definitions/... -count=1 -timeout 900s` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make test` — passed.
  - `origin/main` is an ancestor of the replacement head.
- **Learnings for future iterations:**
  - A focused test-file split is a maintainability improvement but still needs an explicit deletion-only package file-count baseline update when the package is already at its audited limit.
  - After a moving-base merge, rerun the complete short lane and all ratcheted static gates on the replacement head, not only on the pre-merge feature commit.
---

## [2026-08-11 03:33 -07:00] - graph-workstation-semantics final-main verification
- What was implemented
  - Reconfirmed the latest fetched `origin/main` head `dd87fd04d` is an ancestor of the synchronized branch; no newer mainline changes required another merge.
  - Captured the complete short Go suite output so the final unit-lane result is explicit rather than relying on an output-truncated run.
- Files changed
  - `progress.txt`
- Verification
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed under the current lint-checker path.
  - `make test` — passed with exit code 0; the full short Go unit lane completed successfully.
  - Thread-aware PR audit — one historical blocking conversation comment, zero review threads; requested validator scope, editor join, file split, current-main synchronization, and test-gate fixes are present locally.
- **Learnings for future iterations:**
  - For long unit lanes that can exceed tool output limits, capture output to a temporary log and report the terminal exit code plus a bounded tail before treating the gate as verified.
---
## [2026-08-11 04:08 -07:00] - graph-workstation-semantics PR browser-gate follow-up
- What was implemented
  - Reproduced the hosted `Frontend Storybook` failure twice: the customer emulator's replay graph placed the `Review` workstation button at x=-82 on the 390px viewport after the new authored runtime/scheduling labels widened semantic nodes.
  - Lowered the canonical Factory graph replay surface minimum fit-to-view zoom from React Flow's default to `0.25`, allowing narrow graphs to keep all semantic workstation controls inside the viewport without changing authored topology or interaction behavior.
  - Regenerated the checked-in Factory graph distribution and recorded the completed Story 004 review follow-up in the PRD.
- Files changed
  - `ui/packages/factory-graph/src/factory-graph-replay-surface.tsx`
  - `ui/packages/factory-graph/dist/factory-graph-replay-surface.js`
  - `prd.json`
  - `progress.txt`
- Verification
  - `ui/packages/factory-graph`: typecheck passed; 54 tests passed.
  - `bun run storybook:customer-factory-emulator-demos-check` passed at the 390px and 1440px viewports.
  - `bun run storybook:factory-graph-touch-check` passed for touch pan and desktop selection-drag.
  - The in-app browser was unavailable (`agent.browsers.list()` returned no browser surfaces); the repository's Playwright Storybook check provided the available browser evidence.
- **Learnings for future iterations:**
  - Fit-to-view can stop at React Flow's default `minZoom` before all authored nodes fit on touch-sized replay surfaces; tune the package-owned surface rather than reconstructing topology or weakening the browser assertion.
## [2026-08-11 02:11:21 -07:00] - cli-error-classification-and-run-output review follow-up
## [2026-08-11 02:07:36 -07:00] - thr-split-acp-stdio-tests current-main synchronization follow-up
## [2026-08-11 02:07:05 -07:00] - prose-standard-and-terms-003 current-main synchronization follow-up
- What was implemented
  - Fetched live `origin/main` at `0e7df069f80518287a7905558a525ac196b11bc0` and merged its current source and generated changes into the prose branch.
  - Preserved this task's authoritative `prd.json` and unioned both append-only progress histories without dropping either branch's entries.
  - Addressed the latest PR #1869 blocker by resolving the shared-file conflicts and preparing the replacement head for current-main verification.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main CLI, work, packaged-contract, and functional-test files carried by the synchronization merge.
- Verification
  - `git ls-files -u` is empty, conflict-marker scan is clean, and the prose PRD parses with all three stories marked `passes:true`.
  - The replacement-head documentation, typecheck, ratchet, lint-comparison, and hosted-CI checks remain to be run after this bookkeeping update.
- **Learnings for future iterations:**
  - Resolve moving-base conflicts by keeping the active task PRD authoritative and unioning append-only progress histories; validate the index, markers, and parsed PRD before starting replacement-head gates.
---
## [2026-08-11 02:31:48 -07:00] - prose-standard-and-terms-003 final current-main audit
- What was implemented
  - Completed the replacement-head audit after synchronizing with `origin/main` at `0e7df069f80518287a7905558a525ac196b11bc0`; no additional prose or terminology changes were required.
  - Recorded the exact head-versus-base lint comparison and post-merge verification evidence for PR #1869.
- Files changed
  - `progress.txt`
- Verification
  - `make typecheck`, `make docs-reference-smoke`, `make ownership-inventory-check`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `make vet` passed sequentially.
  - `make -k lint` on replacement head `7a52a0e6b` and exact current `origin/main` both exited with the same known baseline findings: 175 `pkg-boundary` violations, the existing packaged-factory consumption violation, deadcode baseline 350 versus current 546, and passing ownership inventory.
  - `git merge-base --is-ancestor origin/main HEAD`, `git merge-tree --write-tree origin/main HEAD`, conflict-marker scan, `git diff --check`, and the parsed PRD/YAML validation all pass; all three stories remain `passes:true` and the worktree is clean.
  - The temporary exact-base audit worktree was removed after comparison; the branch's tracked UI package files were verified restored from `HEAD` after Windows workspace-link cleanup.
  - Replacement-head hosted CI and final PR conversation audit remain pending after the push.
- **Learnings for future iterations:**
  - A no-new-lint claim is strongest when the identical lint target is run on the exact fetched base and replacement head, with baseline counts compared rather than treating aggregate baseline failures as task regressions.
---
## [2026-08-11 02:43:23 -07:00] - prose-standard-and-terms-003 latest-main synchronization follow-up
- What was implemented
  - Fetched the newly advanced live `origin/main` at `dd87fd04db20d755f9b44c369aa79b8c3e600f5f` after the first green replacement head became conflicted during final audit.
  - Merged the current-main lint-lane and baseline-maintenance changes, preserved this task's authoritative `prd.json`, and retained the append-only progress history.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main lint driver, package-boundary baseline, Makefile, and baseline cleanup files carried by the synchronization merge.
- Verification
  - The shared PRD conflict is resolved with all three prose stories still marked `passes:true`; no conflict markers or unmerged index entries remain.
  - The previous replacement head `ff958b2f9` had all hosted checks terminal-success, but its final PR audit detected this newer base before review handoff; replacement-head checks must be rerun on the new merge commit.
- **Learnings for future iterations:**
  - A green hosted run is not final evidence while `origin/main` moves; fetch and compare the live base again immediately before declaring review readiness, even after the PR becomes mergeable.
---
## [2026-08-11 03:36:15 -07:00] - prose-standard-and-terms-003 latest-main lint comparison
- What was implemented
  - Completed the exact-base audit against `origin/main` `dd87fd04db20d755f9b44c369aa79b8c3e600f5f` after the latest synchronization merge `ca0b5ea7e`.
  - Confirmed that no documentation or terminology source changes are needed for the lint-lane mainline update.
- Files changed
  - `progress.txt`
- Verification
  - Direct checks on exact `origin/main` and the replacement tree agree: `pkg-boundary` passes its package scan while `ownership-boundary` reports the same two existing findings in `pkg/initializer/application/process.go` and `pkg/services/edges/definition.go`; packaged-factory consumption fails on the same migration-ledger import; deadcode reports baseline 350 versus current 546; ownership inventory passes.
  - The replacement merge's sequential `make typecheck`, `make docs-reference-smoke`, `make ownership-inventory-check`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `make vet` gates pass.
  - The exact-base audit worktree was removed without cross-worktree links; the active worktree remains clean and retains all three passing prose stories.
  - Replacement-head hosted CI and final PR state must be rerun after pushing `ca0b5ea7e`.
- **Learnings for future iterations:**
  - When aggregate lint is host-bound, compare the individual owning checkers directly on the exact base and replacement tree; this preserves rule-level no-new evidence without mistaking process timeout for a source regression.
---
## [2026-08-11 03:46:05 -07:00] - prose-standard-and-terms-003 final PR handoff audit
- What was implemented
  - Completed the final PR #1869 handoff audit on head `518ddbbd258f32133e5f3edfebd9435df1d9ef92` after current-main synchronization and replacement CI.
  - Recorded the terminal hosted-check, mergeability, PR-body, PRD, and thread-aware review evidence for the documentation-only story.
- Files changed
  - `progress.txt`
- Verification
  - All 18 required CI lanes plus Verification Policy pass on the exact head; the current base is `dd87fd04db20d755f9b44c369aa79b8c3e600f5f`, is an ancestor, and GitHub reports `MERGEABLE/CLEAN`.
  - The thread-aware audit reports 12 historical conversation comments, 0 formal reviews, 0 inline review threads, 0 unresolved threads, and 0 outdated threads; the latest blocking feedback is addressed by the synchronized head and recorded verification.
  - `prd.json` has all three stories marked `passes:true`, the complete YAML register parses with version 1 and 59 terms, and the PR body matches `prd.json` byte-for-byte.
  - The pushed branch and local worktree are clean at the audited head.
- **Learnings for future iterations:**
  - Treat final handoff as one exact-head bundle: fetch the live base, verify ancestry and mergeability, wait for replacement-head CI, audit thread state, and compare the PR body to `prd.json` before reporting review readiness.
---
## [2026-08-11 04:19:49 -07:00] - prose-standard-and-terms-002 latest terminology-contract feedback
- What was implemented
  - Removed the duplicate spaced `Factory Response Event` product-term record so the register has one canonical `FactoryResponseEvent` record matching the architecture data model and response-event schema.
  - Updated the protected-literal replacement and definition to reference the exact registered `FactoryResponseEvent` term.
  - Replaced the unsupported `Parent-Child` lifecycle-ownership example with the contract-backed child-set and parent-aware fan-in behavior; the other relationship examples were checked against `RelationType.yaml` and the relationship reference.
- Files changed
  - `docs/internal/standards/writing/customer-technical-terms.yaml`
  - `prd.json`
  - `progress.txt`
- Verification
  - PyYAML loaded version 1 and all 58 term records; required fields, allowed categories, registered replacements, unique `FactoryResponseEvent` spelling, and the revised Parent-Child example validated.
  - `git diff --check` passed, and the focused correction was committed as `0d2c5f4c0`.
- **Learnings for future iterations:**
  - When deleting a duplicate vocabulary record, revalidate every replacement reference so no remaining record points to the removed canonical form.
  - Public relationship examples should state the observable contract effect, not infer ownership or lifecycle behavior from a relationship name.
---
## [2026-08-11 02:11:21 -07:00] - cli-error-classification-and-run-output review follow-up
- What was implemented
  - Addressed the post-merge blocking review comment by removing `--with-mock-workers` from the three changed invalid-input functional scenarios: invalid JSON, malformed key=value, and positional overflow.
  - Kept each scenario on the canonical `root.BuildProcess` / `Process.Execute` path with an injected `edges.Edges.ProviderCommandRunner`, making the zero-provider-call assertions authoritative for the BAD_REQUEST boundary.
- Files changed
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/parameters -count=1 -timeout 600s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Functional validation scenarios that assert absence of provider side effects must not configure Mock Workers; inject the exact provider command-runner edge and assert its observations instead.
---
## [2026-08-11 02:56:56 -07:00] - cli-error-classification-and-run-output review follow-up
- What was implemented
  - Reconciled PR #1879 with live `origin/main` at `dd87fd04db20d755f9b44c369aa79b8c3e600f5f` after the blocking review comment reported a real merge conflict.
  - Preserved this task's authoritative `prd.json`, including the `reviewFollowUp` note, while accepting the incoming mainline lint-tooling and functional-test changes; `progress.txt` merged cleanly.
  - Confirmed that the merge did not change authored CLI contract inputs, so generated CLI artifacts were not hand-merged or regenerated unnecessarily.
- Files changed
  - Current-main lint checker/lane and maintenance-gate files carried by the synchronization merge.
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/parameters -count=1 -timeout 600s` — passed.
  - `go test ./pkg/services/factory_visualization/... -count=1 -timeout 600s` — passed.
  - `go test ./tests/functional/cli/factory_run/output ./tests/functional/transport/cli/output ./tests/functional/transport/run_scoped_server -count=1 -timeout 600s` — passed.
  - `make typecheck` and `make cli-manifest-check` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - PRD all-pass, merge-marker, and `git diff --check` audits — passed.
- **Learnings for future iterations:**
  - A shared PRD conflict can contain a different task's metadata after main advances; resolve it by retaining the active branch's complete PRD and follow-up note, then verify story flags and JSON before committing.
  - A fresh replacement head needs new hosted CI even when the pre-conflict head was green; local focused gates establish the merge result while GitHub validates the pushed ancestry.
---
## [2026-08-11 03:16:31 -07:00] - cli-error-classification-and-run-output hosted CI follow-up
- What was implemented
  - Investigated the replacement-head Backend Functional Coverage failure and identified the untouched `tests/functional/workers/mock` scenario `TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle`; the failure was a post-shutdown timing/ordering characterization, not a file in this PR's diff.
  - Repeated the exact failing test locally ten times with no failures and reran only the failed hosted backend job; the dependent Verification Policy rerun then passed without changing product or test code.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run '^TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle$' -count=10 -v -timeout 600s` — passed all ten repetitions.
  - Hosted run `31480072598` replacement Backend Functional Coverage job `93745975060` — passed.
  - Hosted Verification Policy job `93747061691` — passed.
  - All other replacement-head checks remained passing; the PR head remains `f259db6c4a91814b57d427d1bf889c76835d93a4`.
- **Learnings for future iterations:**
  - When hosted functional coverage names an untouched fixture, repeat that exact test locally before widening the diff; a clean characterization supports rerunning the failed lane and dependent policy job without unrelated fixes.
---
## [2026-08-11 03:28:29 -07:00] - cli-error-classification-and-run-output final PR handoff audit
- What was implemented
  - Audited PR #1879 after the four implementation stories reached `passes:true`.
  - Confirmed the historical blocking feedback is addressed: the branch contains current `origin/main`, the PR is mergeable and clean, and the replacement-head checks are terminal and passing.
  - Confirmed the PR body matches the current `prd.json` byte-for-byte and the thread-aware review audit reports zero inline review threads and zero unresolved threads.
- Files changed
  - `progress.txt`
- Verification
  - PRD parses with all four stories marked `passes:true`; no conflict markers remain.
  - `origin/main` is an ancestor of `HEAD`.
  - PR #1879 is open, non-draft, `MERGEABLE`/`CLEAN`, with all 18 reported checks passing on the reconciled head.
  - `gh pr checks 1879`, the thread-aware `fetch_comments.py` audit, the PR-body comparison, and `git status` all passed.
- **Learnings for future iterations:**
  - Treat a prior blocking comment as addressed only after its requested base synchronization is present, the PR is cleanly mergeable, and the replacement-head checks are terminal-success.
  - Keep the PR body byte-for-byte synchronized with `prd.json` when the PR description is defined by the task contract.
---
## [2026-08-11 04:35:43 -07:00] - prose-standard-and-terms-002 replacement-head verification
- What was implemented
  - Merged live `origin/main` at `012e98e0fbd1b6fcac106d90b1f3d335b8a3dce6` into the prose branch, retained the authoritative prose PRD, and unioned the incoming append-only progress entries.
  - Kept current-main functional-test changes in the merge; the task diff relative to current `main` remains limited to the writing standard, terminology register, standards index, PRD, and progress history.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main functional-test files carried by the synchronization merge.
- Verification
  - `make typecheck`, `make docs-reference-smoke`, `make ownership-inventory-check`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `make vet` passed sequentially.
  - `git merge-base --is-ancestor origin/main HEAD`, `git merge-tree --write-tree origin/main HEAD`, conflict-marker scanning, PRD/YAML parsing, and `git diff --check` passed; all three stories remain `passes:true` and the register has 58 records.
- **Learnings for future iterations:**
  - Resolve moving-main conflicts by retaining the active task PRD and unioning both progress histories, then rerun the complete replacement-head gates before refreshing the PR body.
  - On this Windows host, sequential Go gates avoid the contention that can make aggregate verification appear hung.
---
## [2026-08-11 04:54:05 -07:00] - prose-standard-and-terms-002 CI retry and PR audit
- What was implemented
  - Characterized the first replacement-head Backend Functional Coverage failure as the unchanged Linux-only process-boundary test `TestBuiltCLIDeclaredCancellationExitCodes/continuous_run`; no task or current-main source change was implicated.
  - Reran the failed hosted coverage and dependent Verification Policy jobs after the focused local characterization; the replacement head remained unchanged.
- Files changed
  - `progress.txt`
- Verification
  - The focused Windows run passed all ten repetitions as expected skips because `os.Interrupt` is unsupported for child processes on Windows.
  - Initial run `31487373457` failed only Backend Functional Coverage on the named process-boundary test; rerun coverage job `93768201391` and Verification Policy job `93769473623` passed, with all 18 required lanes terminal-success.
  - The thread-aware PR audit reports 13 conversation comments, 0 formal reviews, 0 inline review threads, 0 unresolved threads, and 0 outdated threads. The latest historical blocker is addressed by the exact `FactoryResponseEvent` register correction, contract-backed Parent-Child example, current-main merge, and green retry.
  - `origin/main` remains an ancestor of `HEAD` `2e530e12d`; the PR body is sourced from the current `prd.json`, and the worktree was clean before this progress append.
- **Learnings for future iterations:**
  - For an unchanged Linux process-boundary test that is intentionally skipped on Windows, preserve task code, document the characterization, and rerun only the failed hosted lane plus its dependent policy check.
  - Treat the thread-aware audit and terminal replacement checks as separate final evidence: a clean thread list does not replace rerunning failed hosted jobs.
---
## [2026-08-11 05:17:44 -07:00] - prose-standard-and-terms-002 latest-main synchronization
- What was implemented
  - Resolved the replacement-head merge against `origin/main` at `3521f0b0a2f313b22e2c79edcf90339a01b3bce7` by retaining the task-scoped PRD and unioning the append-only progress histories.
  - Preserved the incoming Worker Session start contract, generated API artifacts, runtime changes, and functional coverage additions from current main.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Current-main Worker Session, OpenAPI, generated-client, runtime, and functional-test files carried by the synchronization merge.
- Verification
  - `prd.json` parses with all three stories at `passes:true`; the register remains version 1 with 58 consistently shaped records and one exact `FactoryResponseEvent` record.
  - The previous replacement head was green after the failed-lane retry; this synchronized head requires a fresh hosted CI run before final handoff.
- **Learnings for future iterations:**
  - When append-only logs contain changed entries with the same heading on both sides, preserve both contents and mark the incoming copy as mainline history rather than silently discarding either audit trail.
---
## [2026-08-11 05:31:00 -07:00] - prose-standard-and-terms-002 final PR handoff audit
- What was implemented
  - Completed the replacement-head handoff after synchronizing with `origin/main` at `3521f0b0a2f313b22e2c79edcf90339a01b3bce7` and resolving the shared PRD/progress merge.
  - Confirmed the latest terminology-contract corrections remain on the pushed head: one exact `FactoryResponseEvent` record, no spaced duplicate, and a contract-backed Parent-Child example.
- Files changed
  - `progress.txt`
- Verification
  - CI run `31490644011` passed all 18 required lanes on exact head `8817b101016548ec4af23bfd965593d7dc09a66a`, including Backend Unit Coverage, Backend Functional Coverage, and Verification Policy.
  - `git merge-base --is-ancestor origin/main HEAD` and `git merge-tree --write-tree origin/main HEAD` passed; GitHub reports PR #1869 `CLEAN`/`MERGEABLE`.
  - The PR body is byte-for-byte equal to `prd.json`; all three stories remain `passes:true`.
  - The thread-aware PR audit reports 13 conversation comments, 0 formal reviews, 0 inline review threads, 0 unresolved threads, and 0 outdated threads.
- **Learnings for future iterations:**
  - Final handoff evidence must be recorded on the exact pushed head after CI and mergeability are checked; a progress-only follow-up commit requires its own replacement-head CI run.
---
## [2026-08-11 06:13:54 -07:00] - prose-standard-and-terms-002 hosted functional retry characterization
- What was implemented
  - Made no source or PRD changes in response to rotating failures from the untouched Backend Functional Coverage suite; isolated each named fixture and reran only the failed lane with its dependent policy check.
- Files changed
  - `progress.txt`
- Verification
  - Focused local characterization passed the JavaScript parallel-dispatch test 10/10, CLI writer-cancellation test 10/10, hard-kill staging-contention test 5/5, and mock-worker continuous-idle counterexample 5/5; the Windows-only context-cancellation fixture produced its expected 10/10 skips.
  - The ACP retry-resume fixture showed transient session-load failures under repetition (9/10, then 4/5); the hosted retry eventually passed.
  - CI run `31491644438`, attempt 5, passed all 18 required lanes on exact head `5a071aecb967fbecc221b265063fa0b6a32139b3`, including Backend Functional Coverage and Verification Policy.
- **Learnings for future iterations:**
  - A broad functional lane can rotate among existing timing-sensitive fixtures across retries; isolate every named failure, preserve the task scope when focused runs pass or characterize expected skips, and rerun only the failed coverage/policy jobs until the exact head is green.
---
## [2026-08-10 19:25 -07:00] - e2-structured-worker-results CI flake follow-up (mainline history)
- What was implemented
  - Made no product or test-code changes. Investigated the final-head CI failure and reran only the failed Backend Functional Coverage job.
  - Confirmed the first failure was an unchanged `pkg/transports/cli` functional-coverage measurement at 72.818% versus the 72.98% manifest floor; the targeted rerun then exposed the existing `TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle` shutdown race in `tests/functional/workers/mock`.
- Files changed
  - `progress.txt`
- Verification
  - The isolated `continuous_idle` subtest passed 20 consecutive local runs; no feature diff touches its package.
  - Failed-job rerun Backend Functional Coverage job `93658010711` passed, including the pinned real ACP evidence artifact; Verification Policy job `93658764710` passed.
  - The branch remains at the pushed head `868990d2b4a07270cce311ec715c98bf10eacb7e`, with all four PRD stories marked `passes:true` and the PR body still byte-identical to `prd.json`.
- **Learnings for future iterations:**
  - When functional coverage first reports a package-floor variance and a retry names an unrelated cancellation/shutdown race, preserve the feature diff, stress the exact subtest locally, and rerun only the failed job after clean characterization.
---
## 2026-08-10 18:42 -07:00 - dwro-s1-remote-placement-flags current-main synchronization follow-up
- What was implemented
  - Fetched the live `origin/main` head `0b9771fe8` after the PR base advanced beyond the previously synchronized `e6797fd5`.
  - Merged the current base, preserved the S1-owned `prd.json`, and retained both branches' append-only progress history while resolving the shared bookkeeping conflicts.
- Files changed
  - Current-main merge files.
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all four S1 stories marked `passes:true`.
  - `progress.txt` and `prd.json` contain no merge conflict markers; the merge index is resolved.
  - `git diff --cached --check` passed before the merge commit.
- **Learnings for future iterations:**
  - A clean merge against a stale remote-tracking base is not sufficient; compare the live hosting ref, fetch it, and resolve shared PRD/progress files again before pushing the replacement head.
---

## 2026-08-10 22:27 -07:00 - dwro-s2-server-owned-start-001
- What was implemented
  - Added the Worker Sessions `Start` operation for already-resolved Workers executions.
  - Gated successful return on the opening SESSION/STARTED record being retained-readable and subscribable, followed by the Workers admission callback.
  - Reused the existing retry, control, provider-association, and exactly-once terminal supervision path; asynchronous attempts run under a detached server-owned context while synchronous `InvokeSession` remains unchanged at its caller-facing boundary.
  - Recorded pre-admission cancellation intent so a racing Cancel cannot be acknowledged as accepted, and added stable start/readiness/admission error classifications plus safe structured outcome logs.
- Files changed
  - `pkg/services/worker_sessions/contracts.go`
  - `pkg/services/worker_sessions/doc.go`
  - `pkg/services/worker_sessions/internal/service/service.go`
  - `pkg/services/worker_sessions/internal/service/control.go`
  - `pkg/services/worker_sessions/internal/service/invoke_session.go`
  - `pkg/services/worker_sessions/internal/service/start_test.go`
  - `pkg/services/worker_sessions/internal/service/start_side_effects_test.go`
  - `pkg/services/worker_sessions/internal/service/fakes_test.go`
  - Factory Runtime test service doubles updated for the expanded Worker Sessions contract.
  - `prd.json`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` passed.
  - `go test -race ./pkg/services/worker_sessions/... -count=1` passed before the final test relocation; focused race tests passed after relocation.
  - Affected Factory Runtime package tests and compile-only coverage passed; `go vet` passed.
  - `make backend-size` and `make pkg-maint` passed after keeping touched files within repository limits; `make pkg-file-count` passed.
  - `make lint` remains blocked by the environment's missing `knip` binary in the UI deadcode target; the repository-wide package-boundary target reports pre-existing baseline violations unrelated to this change.
- **Learnings for future iterations:**
  - Keep the async start acceptance signal distinct from publication and terminal completion, and reserve the admission signal for starts with no queued pre-admission control.
  - The Worker Sessions package is already at its file-count budget; move focused implementation or tests into existing responsibility-aligned files and rerun backend-size/pkg-maint after additions.
---
## [2026-08-10 23:06 -07:00] - dwro-s2-server-owned-start-002
- What was implemented
  - Made asynchronous `StartRequest` distinct from synchronous `InvokeSessionRequest` and required a non-empty caller `RequestID`.
  - Added an atomic Worker Sessions replay registry that stores a detached normalized start tuple and original acceptance/failure outcome before any opening publication or Workers admission.
  - Same-key identical starts now converge before, during, and after execution; tuple conflicts return a typed idempotency error; invalid requests do not consume keys; deterministic pre-admission failures replay without replacement work.
  - Added deterministic replay, conflict, failure-replay, and concurrent-submission tests, plus the Factory Runtime test-double conversions required by the distinct contract.
- Files changed
  - `pkg/services/worker_sessions/contracts.go`
  - `pkg/services/worker_sessions/internal/service/control.go`
  - `pkg/services/worker_sessions/internal/service/service.go`
  - `pkg/services/worker_sessions/internal/service/fakes_test.go`
  - `pkg/services/worker_sessions/internal/service/start_test.go`
  - `pkg/services/worker_sessions/start_test.go`
  - Factory Runtime test doubles under `pkg/services/factory_runtime/`
  - `prd.json`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` passed.
  - `go test -race ./pkg/services/worker_sessions/internal/service -count=1` passed.
  - Affected Factory Runtime tests, full `make test`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `go vet ./...` passed.
  - `make typecheck` remains environment-blocked by missing UI Bun type definitions; no UI files changed.
- **Learnings for future iterations:**
  - Keep idempotency replay state in the Worker Sessions owner and compare cloned canonical values; reserve the caller key and session identity in one mutex transaction before downstream effects.
  - When a strict package file budget prevents a focused helper file, place small lifecycle-coordination helpers in an existing responsibility-aligned owner and rerun both backend-size and package-file-count gates.
---
## 2026-08-10 23:54 -07:00 - dwro-s2-server-owned-start-003
- What was implemented
  - Exposed the Worker Sessions asynchronous start contract through typed `POST /worker-sessions`.
  - Added authored request/response/retry/resolved-execution OpenAPI schemas, stable conflict/unavailable responses, and safe error codes; regenerated the bundled OpenAPI, Go server/client contracts, and TypeScript client.
  - Added one strict HTTP decode-and-map boundary that delegates only to Worker Sessions `Start`, returns `202 Accepted` after the reservation, Events readiness, and Workers admission barrier, and returns the stable Worker Session identity plus deterministic Events topic without waiting for terminal output.
  - Added stable 400/409/503 mapping for malformed input, bounded retry violations, idempotency/session conflicts, opening/event readiness failures, and admission failures. Same-key replay and duplicate suppression remain owned by the Worker Sessions service.
- Files changed
  - `api/openapi-main.yaml` and authored Worker Session start schemas/responses under `api/components/`.
  - `api/openapi.yaml`, `packages/api/generated/manifest.json`, `packages/api/generated/openapi/openapi.yaml`, `pkg/transports/http/generated/server.gen.go`, `pkg/transports/http/client/client.gen.go`, `ui/src/api/generated/openapi.ts`, and `ui/packages/client/src/generated/openapi.ts`.
  - `pkg/services/worker_sessions/transports/http/adapter.go`, `mapping.go`, `handler.go`, and `handler_test.go`.
  - `pkg/transports/http/server.go` and `pkg/transports/http/application/handler.go`.
  - `prd.json`.
- Verification
  - `make generate-api` passed.
  - `make interfaces-all` passed, including packaged API and UI client artifacts.
  - `make api-smoke` passed, including generated-artifact drift, HTTP contract alignment, and provider parity.
  - `make test` passed the full short unit lane after the packaged contract artifacts were regenerated; `TestMockWorkersSchema_StaleStagingDetectedByContractCheck` now passes with the complete generated artifact set.
  - Affected Worker Sessions, HTTP, contract, mapping, and CLI Go tests passed; focused `go vet` passed.
  - `git diff --check` passed. Repository `make typecheck` remains blocked by pre-existing UI test-helper assertion typings; a full unscoped Go test run exceeded the local five-minute command budget, while all affected suites passed.
- **Learnings for future iterations:**
  - Add `x-enum-varnames` to new OpenAPI enums when their values overlap existing generated enum constants; otherwise code generation can rename unrelated public identifiers.
  - Keep typed transport schemas aligned with the Workers request shape, but preserve Worker Sessions ownership of admission, idempotency, and stable failure classification in the service boundary.
---
## [2026-08-11 00:45 -07:00] - dwro-s2-server-owned-start-004
- What was implemented
  - Replaced request-derived asynchronous execution lifetime with a Worker Sessions-owned process lifecycle context and made the caller context cancel only the admission wait; a canceled caller leaves the request-ID replay authoritative until the server-owned attempt accepts or fails.
  - Added a server-owned supervisor driver completion barrier, shutdown admission gate, idempotent lifecycle stop, and explicit shutdown termination through the existing Workers cancellation boundary. Terminal publication remains live until callbacks and driver tails have joined, then the lifecycle context is canceled.
  - Wired Factory Runtime shutdown to stop server-owned asynchronous Worker Sessions before the dispatch planner and Workers pool, while leaving synchronous InvokeSession supervisors on their existing control path. Added stable HTTP handling for caller cancellation and server-stopping rejection.
  - Added deterministic disconnect-before/after-admission, replay, exact terminal-event, shutdown-join, and runtime shutdown-order assertions.
- Files changed
  - `pkg/services/worker_sessions/internal/service/service.go`, `publish_record.go`, `control.go`, and `invoke_session.go`
  - `pkg/services/worker_sessions/contracts.go`, `doc.go`, and HTTP handler tests/mapping
  - `pkg/services/factory_runtime/internal/services/orchestration/runtime/worker_pool.go` and `factory_modes_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `make test` passed.
  - `make typecheck` passed.
  - `go test -race ./pkg/services/worker_sessions/...` passed.
  - Affected Factory Runtime tests, `go vet ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/...`, `make backend-size pkg-maint pkg-file-count pkg-structure`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - Keep optional lifecycle capabilities structural when the service root is constrained to one named interface; composition can type-assert the narrow `Stop(context.Context) error` method without widening every fake.
  - A canceled admission waiter must not finalize the request replay; let a detached owner finish the state machine so a retry sees the authoritative accepted or failed result.
  - Runtime shutdown must target only server-owned asynchronous supervisors; synchronous InvokeSession work is still joined by its existing Factory Runtime/Workers control path.
---
## [2026-08-11 01:25 -07:00] - dwro-s2-server-owned-start-004
- What was implemented
  - Added root-built functional HTTP coverage for an admitted direct Worker surviving cancellation of the submitting request and for server shutdown canceling and joining the admitted Worker.
  - The process-boundary tests use a deterministic injected command gate, assert same-requestId replay does not duplicate the Worker command, and verify shutdown reaches the command context without arbitrary sleeps.
- Files changed
  - `tests/functional/workers/transports/http/worker_sessions_lifecycle_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/runtime_api -run 'TestWorkerSessionHTTP(DisconnectKeepsAdmittedWorkerAlive|ShutdownJoinsAdmittedWorker)$' -count=1 -v` passed.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - The root functional harness routes mock model-worker script execution through either the provider or script command edge depending on the assembled worker; inject a deterministic runner into both when the test needs to observe the actual process boundary.
---
## [2026-08-11 02:05 -07:00] - dwro-s2-server-owned-start-004
- What was implemented
  - Moved the process-boundary Worker Session regression from the deprecated `runtime_api` test owner to `tests/functional/workers/transports/http`, the durable Worker/HTTP domain required by the package-structure gate.
- Files changed
  - `tests/functional/workers/transports/http/worker_sessions_lifecycle_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/transports/http -run 'TestWorkerSessionHTTP(DisconnectKeepsAdmittedWorkerAlive|ShutdownJoinsAdmittedWorker)$' -count=1 -v` passed.
  - The same focused tests passed under `go test -race`.
  - `go vet ./pkg/services/worker_sessions/... ./pkg/services/factory_runtime/... ./tests/functional/workers/transports/http` passed.
  - `make typecheck`, `make ui-lint`, `make backend-size pkg-maint pkg-file-count pkg-structure`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - New functional scenarios must live under `tests/functional/<durable-domain>/<subsection>/...`; the package-structure checker rejects new files under the historical `runtime_api` aggregate even when their assertions are valid.
---
## [2026-08-11 01:45 -07:00] - dwro-s2-server-owned-start-005
- What was implemented
  - Consolidated synchronous `InvokeSession` and asynchronous `Start` onto one Worker Sessions preparation path covering reservation, STARTING transition, observation capture, opening publication, supervision registration, retry budget, admission callback, control races, terminal classification, and terminal publication.
  - Preserved synchronous callers' request-ID-free session conflict rules and terminal return behavior while keeping asynchronous Starts server-owned and admission-gated.
  - Added deterministic compatibility tests proving synchronous terminal waiting versus asynchronous admission return and matching pre-admission failure classification and event history.
- Files changed
  - `pkg/services/worker_sessions/contracts.go`
  - `pkg/services/worker_sessions/internal/service/invoke_session.go`
  - `pkg/services/worker_sessions/internal/service/publish_record.go`
  - `pkg/services/worker_sessions/internal/service/service.go`
  - `pkg/services/worker_sessions/internal/service/start_test.go`
- Verification
  - `go test ./pkg/services/worker_sessions/... -count=1` passed.
  - `go test -race ./pkg/services/worker_sessions/internal/service -count=1` passed.
  - Affected Factory Runtime and HTTP transport suites, affected `go vet`, `make typecheck`, `make ui-lint`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` passed.
  - The aggregate `make lint` command was bounded at five minutes while unrelated concurrent unit-lane processes were active; focused lint/static targets and affected vet checks passed.
- **Learnings for future iterations:**
  - Shared lifecycle behavior should be factored below the public entry points, with mode options limited to lifecycle ownership, readiness verification, and caller-facing wait/error semantics.
  - Keep the Worker Sessions package at its 15-file limit by placing small lifecycle coordination beside the existing publication/lifecycle implementation rather than adding another package file.
## 2026-08-10 19:41 -07:00 - cli-error-classification-and-run-output-001
- What was implemented
  - Classified dynamic Factory invocation missing values and static run-flag parse failures with stable `INVOCATION_ARGUMENT_*` errors, including the case where one dynamic flag is followed by another and must not consume it as a value.
  - Added process-boundary regression coverage for the reported `--planer-model` typo in normal and quiet modes, dynamic/static missing values, positional overflow, and invalid JSON values; usage failures emit one stderr `ErrorResponse`, leave stdout empty, and make zero provider calls.
  - Added the invocation argument error entries to the authored CLI contract and regenerated the CLI family and published contract artifacts.
- Files changed
  - `contracts/cli/commands.json`
  - `packages/api/generated/cli/commands.json`
  - `packages/api/generated/manifest.json`
  - `pkg/services/factory_runtime/transports/cli/invocation_error.go`
  - `pkg/services/work/internal/invocationreturnpolicy/arguments.go`
  - `pkg/services/work/internal/invocationreturnpolicy/preparation.go`
  - `pkg/services/work/internal/invocationreturnpolicy/preparation_test.go`
  - `pkg/services/work/invocation_return_policy_contract.go`
  - `pkg/transports/cli/generated/family_manifests_gen.go`
  - `pkg/transports/cli/root_submit_batch.go`
  - `pkg/transports/cli/run/invocation_error.go`
  - `tests/functional/transport/cli/output/json_result_test.go`
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
- Verification
  - `go test ./pkg/services/work/internal/invocationreturnpolicy ./pkg/services/work ./pkg/services/factory_runtime/transports/cli ./pkg/transports/cli/run ./pkg/transports/cli` passed.
  - Focused functional CLI output/parameter tests passed.
  - `make cli-manifest-check` and `git diff --check` passed.
- **Learnings for future iterations:**
  - Dynamic Factory arguments are parsed by Work after run-level flags are removed; a value-taking argument must reject a following `--flag` token before normalization so it cannot accidentally start runtime execution.
  - `make cli-manifest-generate` refreshes the generated Go family metadata; changes to the published `packages/api/generated/cli/commands.json` projection also require `make contracts-generate` before `make cli-manifest-check` will pass.
---
## 2026-08-10 20:00 -07:00 - cli-error-classification-and-run-output-001 verification follow-up
- What was implemented
  - Restored the locked UI dependency needed by the repository typecheck and verified the frontend typecheck after the story changes.
## 2026-08-10 19:25 -07:00 - pp-merge-context-bug-001
- What was implemented
  - Added a deterministic packaged `@you/plan-parallel` functional regression with 10 independent generated children and unique executor output markers.
  - Captu…14684 tokens truncated… `passes:true`; conflict-marker and whitespace checks passed.
- **Learnings for future iterations:**
  - When a current-main merge conflicts only in shared bookkeeping, reconstruct the append-only log from complete merge stages in bounded chunks, validate object counts, and keep the active task PRD authoritative before staging.
---
## 2026-08-10 22:27:32 -07:00 - bun-compat-batch-migration-009 current-main synchronization follow-up
- What was implemented
  - Merged the newly advanced live `origin/main` head `b68e4bf956` into the Bun migration branch after the first replacement head became stale during verification.
  - Preserved the Bun migration `prd.json` as authoritative and retained the complete append-only progress history, including the prior 7dbeb472a synchronization entry.
  - Carried the unrelated current-main packaged-factory test change without adding task-specific product changes.
- Files changed
  - Current-main synchronization files.
  - `prd.json`
  - `progress.txt`
- Verification
  - Exact focused Bun cohort — 33 files / 183 tests / 390 expect calls passed on the second synchronized tree.
  - `bun run check:test-lanes` and `bun run typecheck` passed from `ui/`.
  - `prd.json` parses with all nine stories marked `passes:true`; conflict-marker and whitespace checks passed.
- **Learnings for future iterations:**
- Fetch the live base immediately before pushing and again after local verification; another merged main commit can make a freshly synchronized PR dirty before its checks begin.
---
## 2026-08-10 22:49:55 -07:00 - bun-compat-batch-migration-009 current-main synchronization follow-up
- What was implemented
  - Fetched and merged the live `origin/main` head `ecd1d248731c9108dbbc4dafe2f28c4120043346` into the Bun migration branch as `c86a0c190` after the PR became dirty again.
  - Preserved the Bun migration `prd.json` as authoritative and retained the append-only `progress.txt` history; the incoming implementation changes are the current-main runtime-host reuse/cancellation fixes.
- Files changed
  - `pkg/services/models/internal/services/runtime_host/internal/service/export_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/failure_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/service_test.go`
  - `pkg/services/models/internal/services/runtime_host/internal/service/supervisor.go`
  - `progress.txt`
- Verification
  - Exact focused Bun cohort — 33 files / 183 tests / 390 expect calls passed under Bun 1.3.12.
  - `go test ./pkg/services/models/internal/services/runtime_host/internal/service -count=1 -timeout 300s` — passed.
  - `bun run check:test-lanes`, `bun run typecheck`, and `bun run lint` — passed from `ui/`.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `prd.json` parses with all nine stories marked `passes:true`; conflict-marker and whitespace checks passed.
- **Learnings for future iterations:**
- A replacement PR head must be validated against the exact live base after merging; the required CI run and mergeability state belong to that replacement head, not the prior reviewed commit.
---

## 2026-08-10 23:48 -07:00 - provider-capability-contract current-main review follow-up
- What was implemented
  - Addressed PR #1874's blocking review feedback by merging the latest fetched `origin/main` head `85619c087`.
  - Kept the provider-specific `prd.json` authoritative and reconstructed the append-only `progress.txt` from the complete merge stages, preserving both branch histories and reusable Codebase Patterns.
  - No product or generated-file conflict required a source change; the merge conflict was limited to task bookkeeping.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all three `provider-capability-contract` stories marked `passes:true`.
  - `progress.txt` contains no merge markers and retains both provider and incoming mainline histories.
  - `git diff --check` passes.
- **Learnings for future iterations:**
  - For a moving-base conflict in long append-only logs, resolve from complete bounded Git-stage slices, keep the active task PRD authoritative, and validate both histories before staging.
---
---
## [2026-08-11 00:22 -07:00] - ui-packaged-factories-decoupling latest-main synchronization (mainline history)
- What was implemented
  - Merged the newly advanced live `origin/main` head `3a0b571b234f70d68908e36669100d0ac9f66a9f` after the prior CI rerun became stale against the moving base.
  - Preserved this task's authoritative `prd.json` and unioned the incoming provider-catalog API/generated-artifact history into the append-only `progress.txt` log.
- Files changed
  - Current-main provider catalog API, generated-contract, and package files.
  - `prd.json`
  - `progress.txt`
- Verification
  - The merge conflict set was limited to shared bookkeeping; no task-side product conflict required additional behavior changes.
  - `prd.json` parses with all three stories marked `passes:true`; conflict-marker, unmerged-entry, and `git diff --check` audits passed.
  - The previous replacement head's required CI rerun was terminal-green; this latest-base replacement needs a fresh run after push.
- **Learnings for future iterations:**
  - Fetch `origin/main` immediately before each final push because a moving base can invalidate a green replacement head; resolve shared PRD/progress files before starting the next CI gate.
---
## 2026-08-11 00:34:30 -07:00 - ui-packaged-factories-decoupling final PR handoff audit
- What was implemented
  - Audited PR #1873's only blocking conversation comment against the replacement head: the 122-line backend-size test was split, live `origin/main` is included, and no unresolved review threads remain.
  - Confirmed all three PRD stories remain marked `passes:true`; no additional product or generated-contract changes were needed.
  - Refreshed the PRD review metadata for the mergeable, non-draft replacement head.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses and all three story pass flags are true.
  - Live `origin/main` `3a0b571b2` is an ancestor of HEAD `37142ba1a`.
  - PR #1873 is open, non-draft, mergeable, and all 18 reported checks are terminal-success.
  - Thread-aware PR audit found one historical conversation comment and zero review threads; its requested test split/base synchronization are present on the current head.
  - The PR body matched the pre-update `prd.json` byte-for-byte before this metadata refresh.
- **Learnings for future iterations:**
  - A prior blocking conversation comment can be treated as addressed only after the requested diff is present, the live base is an ancestor, and the replacement-head checks are terminal-success; verify all three together before final handoff.
---
## [2026-08-11 02:21 -07:00] - dwro-s2-server-owned-start-005 CI follow-up
- What was implemented
  - Merged the live `origin/main` head `0e7df069f80518287a7905558a525ac196b11bc0` to clear PR #1861's dirty base.
  - Preserved the ACP task's authoritative `prd.json` and unioned both complete append-only progress histories; no ACP source or test-split changes were introduced by the synchronization merge.
- Files changed
  - Current-main CLI, generated-contract, documentation, and functional-test files carried by the synchronization merge.
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make typecheck` — passed.
  - `make pkg-boundary` reports the known 175-finding current-main baseline, including the one pre-existing ACP stdio `chat_sessions/wire` finding; its nonzero exit is the repository's intentional baseline state, with no new ACP finding.
  - `prd.json` parses with all three stories marked `passes:true`; conflict-marker, unmerged-entry, and `git diff --cached --check` audits passed.
- **Learnings for future iterations:**
  - When a review branch becomes dirty, merge the freshly fetched live base, keep the active task PRD authoritative, union append-only progress histories, and rerun the exact task gates on the replacement head.
  - Ratcheted boundary output must be compared with the exact current-main baseline; do not rewrite a deletion-only baseline to make an intentional nonzero baseline appear green.
---
## [2026-08-11 02:19:28 -07:00] - thr-split-acp-stdio-tests CI characterization follow-up
- What was implemented
  - Made no ACP production or test-split changes after the replacement-head CI failure.
  - Characterized the failed Backend Functional Coverage lane as the existing persistent-ACP functional test `TestPackagedSpawnRunsPlannerChildrenAndMergerThroughPersistentACPStdio`, not an ACP stdio split test.
- Files changed
  - `progress.txt`
- Verification
  - Replacement-head CI run `31476405488` failed only in Backend Functional Coverage at the named persistent-ACP test; its terminal `unknown`/FAILED outcome prevented the pinned ACP evidence artifact from being produced.
  - `go test ./tests/functional/providers/acp -run '^TestPackagedSpawnRunsPlannerChildrenAndMergerThroughPersistentACPStdio$' -count=10 -v -timeout 600s` — passed all 10/10 local repetitions.
  - The isolated ACP stdio package test and all named static/typecheck gates were already passing on the same head.
- **Learnings for future iterations:**
  - For a coverage-lane failure naming an untouched persistent-ACP fixture, repeat the exact test locally and rerun the failed coverage/policy jobs on the same head before changing the move-only task diff.
---
## [2026-08-11 02:28:05 -07:00] - thr-split-acp-stdio-tests PR handoff audit
- What was implemented
  - Confirmed the ACP task remains complete with no additional story or source changes required after the current-main reconciliation and targeted CI-flake characterization.
- Files changed
  - `progress.txt`
- Verification
  - On evidence head `ce5a8b3392aaff103d40ca34c67e655fbff4d441`, CI run `31477235397` completed with all 18 required checks terminal-successful, including Backend Functional Coverage and Verification Policy.
  - PR #1861 is open, non-draft, `CLEAN/MERGEABLE`, targets live `origin/main` `0e7df069f80518287a7905558a525ac196b11bc0`, and its PR body is byte-identical to `prd.json` (SHA-256 `722f2ad470e1ff54916fa507485f91a325fe949c704085d09c7eaed0794c8c9c`).
  - Thread-aware review audit found 3 historical conversation comments, 0 formal reviews, and 0 inline review threads; the current-main conflict request and earlier implementation blockers are addressed on the pushed tree.
- **Learnings for future iterations:**
  - Final handoff requires checking the exact pushed head, live-base ancestry, terminal CI, PRD/body identity, and thread-aware review state together; a clean local merge alone is insufficient.
---
## [2026-08-11 04:11:12 -07:00] - thr-split-acp-stdio-tests current-main review follow-up
- What was implemented
  - Merged live `origin/main` `dd87fd04db20d755f9b44c369aa79b8c3e600f5f` into the ACP branch as `5c56aeb4f`, preserving this task's authoritative `prd.json` and the complete append-only progress history.
  - Reconciled the current-main boundary checker without changing production code or test assertions: the terminal lifecycle suite remains at the pre-existing `session_prompt_test.go` path, while the shared helper no longer imports `chat_sessions/wire`; the five pre-existing constructor selections remain represented by that path's reviewed migration edge.
- Files changed
  - `pkg/transports/acp/internal/stdio/session_prompt_test.go`
  - `pkg/transports/acp/internal/stdio/session_test_helpers_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` and `go test -race ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` — passed.
  - `make typecheck`, `make backend-size pkg-maint pkg-file-count pkg-structure` — passed.
  - `make pkg-boundary` reports no blocking package-boundary findings on the ACP split; its aggregate exit remains nonzero only for the same two ownership-boundary findings reported on live `origin/main`. `make vet` reached the 15-minute Windows host bound with no diagnostic; only its exact process descendants were terminated.
  - Normalized inventory comparison against the detached live-base worktree: 222 tests on both sides, `DIFF_COUNT=0`, SHA-256 `d613f4f6cdfdc310b42ce01e543b2942b968ab8b3079e27b125af2db576bd031` on both sides. `go test -p 1 -vet=off -cover -count=1 ./pkg/transports/acp/internal/stdio` reports 94.3% statement coverage on both sides.
- **Learnings for future iterations:**
  - The current package-boundary presentation fingerprint includes the source line of a service-construction finding; a move-only split must preserve the reviewed edge deliberately or the checker treats line-only relocation as new debt.
  - Compare package-boundary and ownership-boundary outputs against the exact live base separately; the current main has two unchanged ownership findings even when the package-boundary portion passes.
  - Under concurrent Windows Go workloads, broad vet can exceed the host ceiling without a source failure; inspect the exact invocation tree before terminating anything.
## [2026-08-11 02:11:21 -07:00] - cli-error-classification-and-run-output review follow-up
- What was implemented
  - Addressed the post-merge blocking review comment by removing `--with-mock-workers` from the three changed invalid-input functional scenarios: invalid JSON, malformed key=value, and positional overflow.
  - Kept each scenario on the canonical `root.BuildProcess` / `Process.Execute` path with an injected `edges.Edges.ProviderCommandRunner`, making the zero-provider-call assertions authoritative for the BAD_REQUEST boundary.
- Files changed
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/parameters -count=1 -timeout 600s` — passed.
  - `git diff --check` — passed.
- **Learnings for future iterations:**
  - Functional validation scenarios that assert absence of provider side effects must not configure Mock Workers; inject the exact provider command-runner edge and assert its observations instead.
---
## [2026-08-11 02:56:56 -07:00] - cli-error-classification-and-run-output review follow-up
- What was implemented
  - Reconciled PR #1879 with live `origin/main` at `dd87fd04db20d755f9b44c369aa79b8c3e600f5f` after the blocking review comment reported a real merge conflict.
  - Preserved this task's authoritative `prd.json`, including the `reviewFollowUp` note, while accepting the incoming mainline lint-tooling and functional-test changes; `progress.txt` merged cleanly.
  - Confirmed that the merge did not change authored CLI contract inputs, so generated CLI artifacts were not hand-merged or regenerated unnecessarily.
- Files changed
  - Current-main lint checker/lane and maintenance-gate files carried by the synchronization merge.
  - `tests/functional/transport/cli/parameters/json_values_test.go`
  - `tests/functional/transport/cli/parameters/key_value_test.go`
  - `tests/functional/transport/cli/parameters/positional_values_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./tests/functional/transport/cli/parameters -count=1 -timeout 600s` — passed.
  - `go test ./pkg/services/factory_visualization/... -count=1 -timeout 600s` — passed.
  - `go test ./tests/functional/cli/factory_run/output ./tests/functional/transport/cli/output ./tests/functional/transport/run_scoped_server -count=1 -timeout 600s` — passed.
  - `make typecheck` and `make cli-manifest-check` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - PRD all-pass, merge-marker, and `git diff --check` audits — passed.
- **Learnings for future iterations:**
  - A shared PRD conflict can contain a different task's metadata after main advances; resolve it by retaining the active branch's complete PRD and follow-up note, then verify story flags and JSON before committing.
  - A fresh replacement head needs new hosted CI even when the pre-conflict head was green; local focused gates establish the merge result while GitHub validates the pushed ancestry.
---
## [2026-08-11 03:16:31 -07:00] - cli-error-classification-and-run-output hosted CI follow-up
- What was implemented
  - Investigated the replacement-head Backend Functional Coverage failure and identified the untouched `tests/functional/workers/mock` scenario `TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle`; the failure was a post-shutdown timing/ordering characterization, not a file in this PR's diff.
  - Repeated the exact failing test locally ten times with no failures and reran only the failed hosted backend job; the dependent Verification Policy rerun then passed without changing product or test code.
- Files changed
  - `progress.txt`
- Verification
  - `go test ./tests/functional/workers/mock -run '^TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle$' -count=10 -v -timeout 600s` — passed all ten repetitions.
  - Hosted run `31480072598` replacement Backend Functional Coverage job `93745975060` — passed.
  - Hosted Verification Policy job `93747061691` — passed.
  - All other replacement-head checks remained passing; the PR head remains `f259db6c4a91814b57d427d1bf889c76835d93a4`.
- **Learnings for future iterations:**
  - When hosted functional coverage names an untouched fixture, repeat that exact test locally before widening the diff; a clean characterization supports rerunning the failed lane and dependent policy job without unrelated fixes.
---
## [2026-08-11 03:28:29 -07:00] - cli-error-classification-and-run-output final PR handoff audit
- What was implemented
  - Audited PR #1879 after the four implementation stories reached `passes:true`.
  - Confirmed the historical blocking feedback is addressed: the branch contains current `origin/main`, the PR is mergeable and clean, and the replacement-head checks are terminal and passing.
  - Confirmed the PR body matches the current `prd.json` byte-for-byte and the thread-aware review audit reports zero inline review threads and zero unresolved threads.
- Files changed
  - `progress.txt`
- Verification
  - PRD parses with all four stories marked `passes:true`; no conflict markers remain.
  - `origin/main` is an ancestor of `HEAD`.
  - PR #1879 is open, non-draft, `MERGEABLE`/`CLEAN`, with all 18 reported checks passing on the reconciled head.
  - `gh pr checks 1879`, the thread-aware `fetch_comments.py` audit, the PR-body comparison, and `git status` all passed.
- **Learnings for future iterations:**
  - Treat a prior blocking comment as addressed only after its requested base synchronization is present, the PR is cleanly mergeable, and the replacement-head checks are terminal-success.
  - Keep the PR body byte-for-byte synchronized with `prd.json` when the PR description is defined by the task contract.
---
## [2026-08-11 04:25:29 -07:00] - thr-split-acp-stdio-tests latest-main synchronization and verification
- What was implemented
  - Merged the freshly fetched live `origin/main` head `012e98e0fbd1b6fcac106d90b1f3d335b8a3dce6` into this branch as `350638889` and preserved the ACP task's authoritative `prd.json`.
  - Resolved the shared `progress.txt` conflict by retaining both append-only histories; the current-main CLI changes remain in the merge without changing the ACP split.
- Files changed
  - `progress.txt`
  - Current-main CLI functional-test files carried by the synchronization merge.
  - `prd.json` remained authoritative for this task.
- Verification
  - `go test ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` and the race-enabled package test — passed.
  - `make typecheck`, `make backend-size pkg-maint pkg-file-count pkg-structure`, and `go vet ./pkg/transports/acp/internal/stdio` — passed.
  - `make pkg-boundary` reports no ACP package-boundary findings; its aggregate nonzero result remains exactly the two ownership-boundary findings also present on the checked live-base worktree. No boundary-owned source changed between the prior base check and `origin/main` `012e98e0f`.
  - Normalized ACP stdio inventory is 222/222 with zero diff and SHA-256 `da83686cb68f0988570ca0ea0f429eabf40beb2bb47dba967874a33d46481bf1` on both live-base and task heads; coverage is 94.3% on both.
  - `prd.json` parses with all three stories marked `passes:true`, no conflict markers remain, and `git diff --check` passes.
- **Learnings for future iterations:**
  - A shared append-only progress log can have divergent ordering after repeated task merges; resolve its conflict by retaining both complete conflict blocks, then append the current task entry at EOF.
  - When a ratcheted aggregate boundary gate is nonzero, compare its ownership-specific output with a detached worktree at the exact fetched base; unchanged base findings are not ACP regressions.
---
## 2026-08-11 06:07:54 -07:00 - thr-split-acp-stdio-tests review follow-up
- What was implemented
  - Moved the complete shared Chat Sessions fixture chain (`newChatSessionsStore`, `sequentialIDGenerator`, and `fixedClock`) into the single `session_test_helpers_test.go` owner; all cross-suite active-control setup now enters through that helper.
  - Preserved the exact current-main boundary migration fingerprint by retaining only the constructor callback at `session_prompt_test.go`; no production code, public contract, generated artifact, or test assertion changed.
  - Merged live `origin/main` `3521f0b0a` and unioned the incoming append-only progress history while keeping this task's authoritative `prd.json`.
- Files changed
  - `pkg/transports/acp/internal/stdio/session_prompt_test.go`
  - `pkg/transports/acp/internal/stdio/session_test_helpers_test.go`
  - `prd.json`
  - `progress.txt`
  - Current-main Worker Sessions/API/generated files carried by the synchronization merge.
- Verification
  - `go test ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` — passed.
  - `go test -race ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` — passed.
  - `make typecheck`, `make backend-size pkg-maint pkg-file-count pkg-structure`, ACP `go vet`, and `git diff --check` — passed.
  - Against exact live `origin/main` `3521f0b0a`, normalized ACP inventory was 222/222 with zero diff and SHA-256 `da83686cb68f0988570ca0ea0f429eabf40beb2bb47dba967874a33d46481bf1`; coverage was 94.3% before and after.
  - Current-main package-boundary comparison reported no new ACP finding; the pre-existing `chat_sessions/wire` import and five-construction selection fingerprint remained at `session_prompt_test.go`.
- **Learnings for future iterations:**
  - When a shared fixture must move but a deletion-only boundary ratchet keys an existing constructor by source path and selection count, keep the helper entry point in the shared owner and isolate the reviewed constructor callback at that exact path rather than adding a new migration baseline.
---
## 2026-08-11 06:16:00 -07:00 - thr-split-acp-stdio-tests verification follow-up
- What was implemented
  - Completed the requested full repository `make test` gate after synchronizing with live `origin/main`.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - Full `make test` includes the broader functional package set and can take several minutes even when most package results are cached; retain the final exit status when collecting bounded output.
---
## [2026-08-11 07:14:00 -07:00] - thr-split-acp-stdio-tests review-fix
- What was implemented
  - Removed the `newBoundarySensitiveChatSessionsStore` shim and both `//line` directives.
  - Moved the complete real Chat Sessions constructor into `session_test_helpers_test.go` and routed every recovery/control setup through `newChatSessionsStore`.
  - Added exact deletion-only `service-construction` and `test-service-import` baseline entries for the relocated pre-existing `chat_sessions/wire` test edge.
  - Formatted all nine Go files identified by review and updated the PRD evidence note.
- Files changed
  - `docs/internal/baselines/service-construction-baseline.json`
  - `docs/internal/baselines/test-service-import-baseline.json`
  - `pkg/transports/acp/internal/stdio/prompt_stream_attachment_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_delivery_test.go`
  - `pkg/transports/acp/internal/stdio/prompt_stream_gap_test.go`
  - `pkg/transports/acp/internal/stdio/server_test.go`
  - `pkg/transports/acp/internal/stdio/session_load_test.go`
  - `pkg/transports/acp/internal/stdio/session_new_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_admission_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_dispatch_test.go`
  - `pkg/transports/acp/internal/stdio/session_prompt_test.go`
  - `pkg/transports/acp/internal/stdio/session_test_helpers_test.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused ACP stdio tests, `make typecheck`, `make backend-size pkg-maint pkg-file-count pkg-structure`, `make vet`, and `git diff --check` passed.
  - Against `origin/main` `b99c8b942`, normalized inventory was 222/222 with zero diff and SHA-256 `adbe5493c77fd04c100112584add6454b690d8d6ec7aaed486cc65914976cddc`; coverage was 94.3% before and after.
  - Package-boundary checker passed with no blocking package-boundary violations; the aggregate `make pkg-boundary` result remains nonzero only for the two unchanged ownership-boundary findings also present on current main.
- **Learnings for future iterations:**
  - Exact service-construction and test-service-import baselines are the repository-supported way to carry reviewed migration debt when a move changes the finding path; preserving a source line with `//line` is not acceptable.
---
## [2026-08-11 04:32:41 -07:00] - graph-workstation-semantics hosted CI and final handoff
- What was implemented
  - Completed the replacement-head validation after the Storybook viewport fix; hosted run `31486520825` passed all checks, including Frontend Storybook, Backend Unit Coverage, Backend Functional Coverage, and Local Inference.
  - Confirmed the branch remains synchronized with live `origin/main` at `012e98e0fbd1b6fcac106d90b1f3d335b8a3dce6`, with `origin/main` an ancestor of `HEAD`.
  - Confirmed the PR body remains synchronized with the authoritative `prd.json`, and the blocking review feedback has a documented response with no inline review threads or unresolved review threads.
- Files changed
  - `progress.txt`
- Verification
  - Hosted run `31486520825` — terminal success.
  - `git merge-base --is-ancestor origin/main HEAD` — passed.
  - PRD story audit — four stories, all `passes:true`.
  - Review audit — zero inline threads and zero unresolved threads.
- **Learnings for future iterations:**
  - A green replacement run must be recorded against the exact pushed head; any subsequent progress-only push requires another hosted replacement run before declaring the PR ready.
---
## [2026-08-11 05:20:03 -07:00] - graph-workstation-semantics packaged preparation dependency review follow-up
- What was implemented
  - Addressed the blocking PR feedback by introducing the explicit `factorydefinitions.PackagedFactoryPersistence` capability for first-party packaged installation.
  - Removed the packaged installer’s optional runtime type assertion and strict fallback; create and replace now call `PreparePackagedFactoryLayout` directly through the required capability.
  - Updated catalog persistence, Factory Definitions Wire, application Wire bindings, generated `pkg/wire/wire_gen.go`, and affected composition fixtures so the catalog-owned capability is injected explicitly while ordinary named-Factory persistence remains strict.
  - Added a recorder-backed create/replace regression that fails if either path calls ordinary `PrepareFactoryLayout`.
- Files changed
  - `pkg/services/factory_definitions/persistence_contract.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service_test.go`
  - `pkg/services/factory_definitions/internal/services/catalog/persistence/service.go`
  - `pkg/services/factory_definitions/internal/services/catalog/wire/wire.go`
  - `pkg/services/factory_definitions/wire/{catalog_ports.go,root_wire_persistence.go,packaged_catalog_ports.go}`
  - `pkg/services/factory_definitions/internal/packaged_installer.go`
  - `pkg/services/factory_definitions/internal/testcomposition/composition.go`
  - packaged/runtime/Wire composition test helpers and generated `pkg/wire/wire_gen.go`
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_definitions/internal/services/distribution/packagedinstallation -count=1 -timeout 180s` — passed.
  - `go test ./pkg/services/factory_definitions/... ./pkg/wire/... -count=1 -timeout 600s` — passed.
  - `go generate ./pkg/wire` — passed and refreshed generated Wire output.
  - `git diff --check` — passed before commit.
  - Commit `a14b3d9f9` records the implementation checkpoint.
- **Learnings for future iterations:**
  - When one implementation exposes a stricter policy variant of a shared persistence operation, model that variant as a named composite capability and bind it explicitly in Wire; do not recover it from the concrete value at operation time.
  - A create/replace recorder that makes the ordinary operation fail is stronger evidence than checking only call counts because it proves the fallback cannot silently return.
---
## [2026-08-11 05:38:15 -07:00] - graph-workstation-semantics packaged dependency gate cleanup
- What was implemented
  - Split the new create/replace recorder into `packaged_preparation_test.go` so the existing packaged-installation test file stays within the repository’s 1,000-line backend limit.
  - Moved the named packaged persistence interface into the internal contract layer and kept the service root exposure as a type alias, satisfying the exact-one-root-interface structure rule.
- Files changed
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/service_test.go`
  - `pkg/services/factory_definitions/internal/services/distribution/packagedinstallation/packaged_preparation_test.go`
  - `pkg/services/factory_definitions/internal/contracts/application.go`
  - `pkg/services/factory_definitions/persistence_contract.go`
  - `progress.txt`
- Verification
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make test` — passed; the full short Go unit lane completed successfully.
  - Focused create/replace packaged-preparation tests — passed.
  - Commits `00592ad5d` and `7dd95fea2` record the test split and contract placement correction.
- **Learnings for future iterations:**
  - New root-facing contracts should be declared in the internal contracts package and exposed through aliases when the package-structure gate reserves the service root for its singular public service interface.
  - Run ratcheted gates after test-file moves and contract-placement fixes; passing focused tests alone does not prove file-count or root-structure compliance.
---
## [2026-08-11 05:45:42 -07:00] - graph-workstation-semantics latest-main reconciliation
- What was implemented
  - Merged live `origin/main` at `3521f0b0a2f313b22e2c79edcf90339a01b3bce7` into the branch after the PR reported a moving-base conflict.
  - Preserved this task's authoritative four-story `prd.json`, retained both sides of the append-only progress history, and accepted the incoming Worker Sessions API/runtime/generated-artifact changes from main.
- Files changed
  - Current-main Worker Sessions API, runtime, transport, functional-test, and generated contract files carried by the synchronization merge.
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses with all four stories marked `passes:true`.
  - `prd.json` and `progress.txt` contain no merge conflict markers; staged merge output passes `git diff --cached --check`.
  - The merged source remains pending the task-specific post-merge gates before the replacement PR head is pushed.
- **Learnings for future iterations:**
  - Resolve shared metadata conflicts by keeping the active task PRD authoritative and preserving both append-only progress branches; source conflicts from a moving base should be accepted when they are outside the task's ownership.
---
## [2026-08-11 06:01:51 -07:00] - graph-workstation-semantics merged-head verification
- What was implemented
  - Completed the post-merge validation on `6bfb8279e`, including the regenerated Wire output required after accepting the current-main Worker Sessions composition changes.
  - Confirmed the authored REST/OpenAPI sources and generated clients are already current; `make generate-api` produced no additional API drift.
- Files changed
  - `pkg/wire/wire_gen.go`
  - `progress.txt`
- Verification
  - `go test ./pkg/services/factory_definitions/... ./pkg/wire/... -count=1 -timeout 600s` — passed.
  - `make backend-size pkg-maint pkg-file-count pkg-structure vet` — passed.
  - `make test` — passed; the full short Go unit lane completed successfully.
  - `go generate ./pkg/wire`, `make generate-api`, and `git diff --check` — passed; only the expected regenerated Wire file changed.
- **Learnings for future iterations:**
  - After a moving-base merge changes the dependency graph, rerun the generator even when the feature's Wire source is unchanged; generated variable numbering can legitimately change when mainline adds providers.
---
## [2026-08-11 06:16:34 -07:00] - graph-workstation-semantics hosted functional characterization
- What was implemented
  - Investigated the replacement-head hosted failure in `Backend Functional Coverage`; the only failing product scenario was the untouched `TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle` case.
  - Characterized the failure as transient packaged-installation staging-owner contention during system bootstrap, not an assertion or compile regression in the packaged-preparation change.
- Files changed
  - `progress.txt`
- Verification
  - The hosted failure log shows the `@you/tournament` staging-owner path reported an active owner during the isolated functional process, and the dependent pinned ACP artifact step failed only because the functional lane stopped early.
  - `go test ./tests/functional/workers/mock -run '^TestPlainBatchDrainPreservesFiniteAndContinuousCounterexamples/continuous_idle$' -count=10 -timeout 600s` — passed all ten repetitions locally.
  - No product or test source was changed for this unrelated hosted contention; the replacement CI lane must be rerun on the next pushed bookkeeping head.
- **Learnings for future iterations:**
  - When hosted functional coverage fails in an untouched fixture with an active packaged-installation owner, repeat that exact test locally before changing runtime behavior; a clean repetition supports rerunning the failed lane and its dependent policy check.
---
## [2026-08-11 06:28:54 -07:00] - graph-workstation-semantics second hosted functional characterization
- What was implemented
  - Investigated the second replacement-run failure; the earlier plain-batch fixture passed, while an unrelated CLI process scenario `TestCLIFailureWritesDiagnosticToStderr` failed with an incomplete terminal diagnostic and `Workers workstation dispatch is unknown`.
  - Confirmed the failure is not deterministic in the feature branch by rerunning the exact scenario locally ten times without failure.
- Files changed
  - `progress.txt`
- Verification
  - Hosted run `31495390070`, job `93791902103`, was green for all other lanes and failed only on that functional test; the dependent Verification Policy job failed as a consequence.
  - `go test ./tests/functional/transport/cli/process -run '^TestCLIFailureWritesDiagnosticToStderr$' -count=10 -timeout 600s` — passed all ten repetitions locally.
  - No product source was changed for this unrelated hosted-only functional race; the next replacement head requires another hosted rerun.
- **Learnings for future iterations:**
  - When a rerun moves to a different untouched functional fixture, preserve the exact failing stderr and repeat only that test locally; do not widen the task diff to mask a nondeterministic hosted failure.
---
## [2026-08-11 07:12:17 -07:00] - graph-workstation-semantics-004
- What was implemented
  - Preserved the complete package-owned workstation semantic projection in the public Factory replay node data, including guarded `VISIT_COUNT` control details.
  - Added a public replay-host render regression that proves a guarded logical move exposes its accessible Loop breaker card, target workstation, visit limit, and guard type.
- Files changed
  - `ui/packages/factory-graph/src/factory-graph-replay-surface.tsx`
  - `ui/packages/factory-graph/dist/factory-graph-replay-surface.js`
  - `ui/packages/factory-visualizers/src/factory-topology-replay.semantics.test.tsx`
  - `prd.json`
  - `progress.txt`
- Verification
  - Factory graph typecheck and 54 package tests — passed.
  - Factory visualizers typecheck, 31 package tests, and lint — passed.
  - Factory graph build, package-boundary, package-pack verification, Biome, and `git diff --check` — passed.
- **Learnings for future iterations:**
  - A host boundary that copies only required-looking semantic axes can erase optional behavior; pass the package-owned projection through or explicitly preserve every semantic field used by the shared renderer.
---
## [2026-08-11 06:20 -07:00] - prose-standard-and-terms-002
- What was implemented
  - Completed the latest terminology-review follow-up on the current PR head. The register contains one exact `FactoryResponseEvent` record, and the `Parent-Child` example describes child-set membership and parent-aware fan-in rather than unsupported lifecycle ownership.
  - Preserved the documentation-only scope; no source-scanning test or runtime implementation was added.
- Files changed
  - `progress.txt`
- Verification
  - PyYAML loaded version 1 and all 58 term records; the required fields, allowed categories, registered replacements, owner paths, internal-only Petri-term surfaces, exact `FactoryResponseEvent` spelling, and revised `Parent-Child` example passed review validation.
  - `make typecheck` and `git diff --check` passed.
  - `origin/main` is an ancestor of the current head, and `git merge-tree --write-tree origin/main HEAD` reported no conflicts.
- **Learnings for future iterations:**
  - Public identifier spellings from the owning architecture or contract source take precedence over natural-language variants when a terminology register defines one canonical record.
  - Relationship examples should describe observable contract effects, such as membership or fan-in, instead of inferring lifecycle ownership from relationship names.
---
## [2026-08-11 07:19:50 -07:00] - graph-workstation-semantics latest-main replay verification
- What was implemented
  - Merged live `origin/main` at `b99c8b942` and retained the graph task's authoritative four-story PRD while unioning both append-only progress histories without conflict markers.
  - Revalidated the guarded-control replay fix and regenerated package output on the conflict-free merged head.
- Files changed
  - Current-main writing-standard files carried by the merge.
  - `prd.json`
  - `progress.txt`
- Verification
  - `origin/main` is an ancestor of `HEAD`; `git merge-tree --write-tree origin/main HEAD` reports no conflicts.
  - Factory graph typecheck, 54 tests, build, package-boundary, and package-pack verification — passed.
  - Factory visualizers typecheck, 31 tests, and lint — passed.
  - `prd.json` parses with all four stories marked `passes:true`; `git diff --check` — passed.
- **Learnings for future iterations:**
  - Re-run package generation and focused host tests after a moving-main merge even when source conflicts are absent; generated consumers and dependency aliases can change independently.
---
## [2026-08-10 22:06 -07:00] - dashboard-fixture-and-quick-wins-001
- What was implemented
  - Added the reusable `dashboard-regression` fixture catalog with immutable canonical Factory Session UUIDs, the `~default` selector, initial/stale/refreshed lists, chart states, session-scoped delayed submission outcomes, and Open/New validation, cancellation, success, and failure outcomes.
  - Added a controlled fixture controller with observable promises, stale-response generation ordering, typed API fetch adapters, canonical session selection, and deterministic mutation/factory completions.
  - Added typed API/session-provider component coverage, a Storybook interaction story, viewport-registry coverage, and fixture documentation for Stories 002–005 to reuse.
- Files changed
  - `ui/src/components/dashboard/fixtures/dashboard-regression.ts`
  - `ui/src/components/dashboard/fixtures/dashboard-regression-controller.ts`
  - `ui/src/components/dashboard/fixtures/dashboard-regression.bun.unit.test.ts`
  - `ui/src/components/dashboard/fixtures/dashboard-regression.component.test.tsx`
  - `ui/src/components/dashboard/fixtures/index.ts`
  - `ui/src/components/dashboard/fixtures/README.md`
  - `ui/src/App.dashboard-regression-fixture.stories.tsx`
  - `ui/scripts/verify-dashboard-viewport-browser.mjs`
  - `prd.json`
  - `progress.txt`
- Verification
  - `bunx --no-install tsc -b --noEmit --pretty false` passed.
  - `bun test src/components/dashboard/fixtures/dashboard-regression.bun.unit.test.ts` passed (5 tests, 23 expectations).
  - Focused Vitest component test passed (1 test).
  - `bun run lint` passed, including localized-copy, semantic-color, boundary, and UI test-lane guards.
  - `bun run build-storybook` passed; the dashboard viewport browser check passed at 320, 375, 768, and 1280 pixels.
  - Focused Storybook interaction run with `-t Canonical` passed (2 tests, 274 unrelated tests skipped). The broader Storybook project still reports 25 unrelated pre-existing failing files, so it is recorded as non-clean rather than used as Story 001 evidence.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - The fixture catalog/controller split lets later chart, submit, reconciliation, and Open/New stories share immutable records and deterministic controls while staying on the real typed query, session-store, and API boundaries.
  - The existing Storybook viewport verifier is the browser regression registry; adding a story ID there is required for its mobile/desktop coverage.
---
## [2026-08-10 22:31 -07:00] - dashboard-fixture-and-quick-wins-002
- What was implemented
  - Removed the duplicate outcomes `CartesianGrid` and duplicate Recharts Y-axis title so the ready chart has one dashed grid treatment and one overlay-owned Y-axis title through remounts and Strict Mode rerenders.
  - Added a localized `aria-describedby` data description for visible chart values; legend labels and distinct dashed line patterns preserve non-color series distinction.
  - Added Story 0 fixture-backed chart-state/remount component coverage, localized message assertions, Storybook one-treatment assertions, and a Playwright browser check wired into Storybook CI for 320px/1280px viewports and a live resize.
- Files changed
  - `ui/src/features/work-outcome/components/work-chart/work-chart.tsx`
  - `ui/src/features/work-outcome/components/work-chart/contracts/work-chart-dashboard-regression.bun.component.test.tsx`
  - `ui/src/features/work-outcome/components/work-chart/work-chart.stories.tsx`
  - `ui/src/features/work-outcome/messages/work-outcome.ts`
  - `ui/src/features/work-outcome/messages/work-outcome.test.ts`
  - `ui/scripts/verify-work-chart-browser.mjs`
  - `ui/scripts/run-storybook-ci.mjs`
  - `ui/package.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `bunx --no-install tsc -b --noEmit --pretty false` passed.
  - `bun run lint` and the focused Biome check passed.
  - Focused Bun chart component suite passed: 20 tests across four WorkChart contract files; the new fixture file passed 2 tests with 27 expectations.
  - Focused Vitest chart/hook component suite passed: 6 tests; localized message unit suite passed: 2 tests.
  - `bun run build-storybook` passed.
  - `node scripts/verify-work-chart-browser.mjs` passed against the generated Storybook at 320px and 1280px, including the in-page resize transition.
  - The exact WorkChart Storybook file passed 5 of 6 stories; its pre-existing Constrained Width legend-height ratio assertion still fails at `0.648 < 0.4`, while the new direct browser gate passes that story at both viewports. The broader `Work Chart` filter also matched the known unrelated empty-model timeline story, so neither failure was used as Story 002 behavior evidence.
  - `prd.json` parses with Stories 001 and 002 marked `passes:true`; Stories 003–005 remain open.
- **Learnings for future iterations:**
  - `ChartContainer` passes chart children through Recharts `ResponsiveContainer`, so an accessible value description belongs beside the chart root and should be referenced with `aria-describedby`, not inserted as a non-chart child.
  - Vitest Storybook `-t` filters match full generated test names unpredictably; filtering by the concrete story file path isolates the intended chart stories, while a broad phrase can select unrelated stories.
  - The direct viewport browser check is useful evidence for resize containment even when an older Storybook geometry assertion fails independently.
---
## [2026-08-10 23:45 -07:00] - dashboard-fixture-and-quick-wins-003
- What was implemented
  - Keyed rich Submit Work drafts, validation, file staging, pending mutations, and completion receipts by the exact canonical Factory Session ID, with request guards that ignore late results outside their owning session.
  - Keyed the default-work/simple composer independently, including a child-level guard so a late session A rejection cannot surface in session B; preserved the owning session's success state and draft reset behavior.
  - Added visible canonical destination labels to both submit paths, localized success/error copy with programmatic associations, an explicit accessible rich-form name, and full-width wrapping status/receipt regions.
  - Added a deterministic two-session Storybook fixture harness and a mobile/desktop Playwright gate covering A-to-B draft isolation, delayed A success/failure, B feedback, canonical destinations, full-width feedback, and page-overflow checks.
- Files changed
  - `ui/src/features/submit-work/hooks/use-submit-work-widget.ts`
  - `ui/src/features/submit-work/hooks/use-session-scoped-simple-submission.ts`
  - `ui/src/features/submit-work/components/submit-work-widget.tsx`
  - `ui/src/features/submit-work/components/composer/factory-simple-submission-composer.tsx`
  - `ui/src/features/submit-work/components/submit-work-card.tsx`
  - `ui/src/features/submit-work/components/submit-work-status-panel.tsx`
  - `ui/src/features/submit-work/components/regression/submit-work-dashboard-regression.stories.tsx`
  - `ui/src/features/submit-work/components/submit-work-widget.isolated.bun.component.test.tsx`
  - `ui/src/features/submit-work/components/submit-work-card.bun.component.test.tsx`
  - `ui/src/features/submit-work/components/submit-work-status-panel.test.tsx`
  - `ui/src/features/submit-work/messages/submit-work.ts`
  - `ui/scripts/verify-submit-work-session-scoping-browser.mjs`
  - `ui/scripts/run-storybook-ci.mjs`
  - `ui/scripts/run-storybook-ci.test.mjs`
  - `ui/package.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `bunx --no-install tsc -b --noEmit --pretty false` passed.
  - `bun run lint` passed, including localized-copy, semantic-color, feature-boundary, file-count, test-lane, and public-package checks.
  - Focused simple-composer/status Vitest suite passed (12 tests); the fixture-backed rich and simple session regressions passed in isolated focused Vitest runs.
  - Native Bun `SubmitWorkCard` component suite passed (13 tests); Storybook CI scheduling tests passed (10 tests).
  - `bun run build-storybook` passed; the generated Storybook contains the moved regression story.
  - `bun run storybook:submit-work-session-check` passed at 390x844 and 1440x900, including full-width feedback and no horizontal overflow.
  - `git diff --check` passed.
- **Learnings for future iterations:**
  - The direct browser lane must own the delayed fixture interaction when a Storybook story is used as a passive harness; an in-story `play` function races the standalone Playwright flow against the same controlled promises.
  - The isolated Submit Work widget suite is currently classified as a Vitest compatibility exception while the normal Vitest project excludes `.bun.component` files; focused runs need the DOM matcher setup explicitly until that existing lane mismatch is reconciled.
  - Moving a regression story into a focused subdirectory keeps the feature folder-count guard green without weakening the structural boundary.
---
## [2026-08-11 00:59 -07:00] - dashboard-fixture-and-quick-wins-004
- What was implemented
  - Made Factory Session list mappings canonical at the Go transport boundary: selector aliases and blank IDs are omitted, duplicate canonical IDs collapse to one row, and the canonical UUID row remains the public identity.
  - Added typed UI list normalization, abort-aware list requests, and authoritative dashboard-store reconciliation for selected session, tab order, paused state, stale membership, and resolved default selection.
  - Preserved successful rows during background refreshes, labeled refresh failures with localized retry UI, and kept the tab navigation busy state/accessibility semantics truthful.
  - Extended the shared dashboard regression fixture, controller, component tests, Storybook story, and browser verifier for alias-plus-UUID lists, refreshed membership, failed retry, keyboard Home/End ordering, and mobile/desktop overflow.
- Files changed
  - `pkg/transports/mapping/factorysession/factory_session_listing.go`
  - `pkg/transports/mapping/factorysession/factory_session_listing_test.go`
  - `pkg/transports/http/server_factory_sessions_test.go`
  - `pkg/transports/http/servertests/server_durable_session_read_test.go`
  - `ui/src/api/factory-sessions/api.ts`
  - `ui/src/api/factory-sessions/normalize-session-list.ts`
  - `ui/src/components/dashboard/fixtures/dashboard-regression.ts`
  - `ui/src/components/dashboard/fixtures/dashboard-regression-controller.ts`
  - `ui/src/features/dashboard/session/dashboard-session-provider.tsx`
  - `ui/src/features/dashboard/state/dashboardSessionStore.ts`
  - `ui/src/features/header/hooks/use-dashboard-session-tabs-state.ts`
  - `ui/src/features/header/components/dashboard-session-tabs.tsx`
  - `ui/src/features/header/components/dashboard-session-tabs.stories.tsx`
  - `ui/scripts/verify-dashboard-session-reconciliation-browser.mjs`
  - `ui/scripts/run-storybook-ci.mjs`
  - `ui/package.json`
  - `prd.json`
  - `progress.txt`
- Verification
  - `bunx --no-install tsc -b --noEmit --pretty false` passed.
  - Focused Bun unit/component suites passed: 21 unit tests across the normalizer, dashboard session store, and fixture; 2 canonical reconciliation component tests; and 22 existing session-tab component tests.
  - Focused Go mapping/HTTP suites passed: `go test ./pkg/transports/mapping/factorysession ./pkg/transports/http/...`.
  - `bun run lint` passed, including localized copy, semantic colors, feature boundaries, file counts, test lanes, and package boundaries.
  - Storybook rebuilt; focused Storybook interaction for the canonical reconciliation story passed (1 test, 2 unrelated stories skipped).
  - `node scripts/verify-dashboard-session-reconciliation-browser.mjs` passed at 390x844 and 1440x900, including canonical rendering, stale removal, retained failed refresh/retry, keyboard ordering, and overflow checks.
  - `bun test scripts/run-storybook-ci.test.mjs` and `git diff --check` passed.
- **Learnings for future iterations:**
  - API-backed Storybook fixtures must return canonical UUID rows for durable sessions; alias-only mocks now correctly represent an empty canonical list and should be reserved for selector-resolution tests that bypass the typed list API.
  - A focused Storybook story-file filter is the reliable interaction evidence when the broader Storybook interaction project contains unrelated browser stories and startup overhead; the direct Playwright lane remains the responsive regression gate.
---
## [2026-08-11 02:52 -07:00] - dashboard-fixture-and-quick-wins-005
- What was implemented
  - Replaced the ambiguous single session action with separately named Open Factory and New Factory controls, distinct localized dialog journeys, accessible loading/error/success states, and focus restoration to the invoking action.
  - Kept validation and New preview non-mutating; Open now requires explicit target confirmation, while New previews the normalized `/factory` target and requires explicit creation confirmation.
  - Added existing-target and broken-target guards, cancel/dismissal/failure coverage, canonical list invalidation and reconciliation after success, deterministic resulting-session selection, and protection against reintroducing a `~default` tab.
  - Extended the shared fixture/controller, isolated and reconciliation component coverage, Storybook fixture story, Storybook CI schedule, and direct mobile/desktop browser verifier for the complete Open/New journeys.
- Files changed
  - `prd.json`, `progress.txt`
  - `ui/package.json`, `ui/scripts/button-usage-allowlist.mjs`, `ui/scripts/run-storybook-ci.mjs`, `ui/scripts/verify-dashboard-open-new-factory-browser.mjs`
  - `ui/src/components/dashboard/fixtures/dashboard-regression.ts`
  - `ui/src/features/header/components/dashboard-session-tab.tsx`, `dashboard-session-tabs-open-dialog.tsx`, `dashboard-session-tabs.tsx`, `components/stories/dashboard-session-tabs-open-new-factory.stories.tsx`
  - `ui/src/features/header/components/dashboard-session-tabs.isolated.bun.component.test.tsx`, `dashboard-session-tabs.launch-target-selection.test.tsx`, `dashboard-session-tabs.reconciliation.bun.component.test.tsx`, `dashboard-session-tabs.stories.tsx`
  - `ui/src/features/header/hooks/use-dashboard-session-tabs-state.ts`, `ui/src/features/header/lib/dashboard-session-tabs-utils.ts`, `ui/src/features/header/messages/header-controls.ts`, `ui/src/features/header/messages/factory-journeys.ts`
- Verification
  - `bun run typecheck` passed.
  - `bun run lint` passed, including localized-copy, semantic-color, feature-boundary, folder-count, test-lane, package-boundary, button-usage, and form-control guards.
  - Focused Bun component suites passed: 22 isolated session-tab tests with 178 expectations and 5 reconciliation tests with 45 expectations. Focused Vitest target-selection, fixture, and Storybook scheduling suites passed.
  - `bun run build-storybook` passed, and `bun run storybook:dashboard-open-new-factory-check` passed at 390x844 and 1440x900.
  - `git diff --check` passed and all five PRD user stories are now marked `passes: true`.
- **Learnings for future iterations:**
  - When a shared Button replaces a raw action in a semantic exception file, update the narrow raw-button fingerprint allowlist so it describes only the remaining tab semantics.
  - Keep journey-specific locale catalogs separate from broad header controls to stay below the feature's file-size guard while preserving one resolved public message type.
  - Storybook fixture operation matchers use request payload identity, so paths for overlapping validation outcomes must remain unique; otherwise a broken-target scenario can resolve the earlier matching operation.
---
## [2026-08-11 04:17 -07:00] - dashboard-fixture-and-quick-wins-review-follow-up
- What was implemented
  - Rebased the seven dashboard commits onto current `origin/main` at `012e98e0f` and resolved the shared `prd.json`/`progress.txt` conflicts while preserving the dashboard PRD and append-only history.
  - Removed the prohibited exact Storybook command/registration-inventory assertions from `ui/scripts/run-storybook-ci.test.mjs` and replaced them with behavioral readiness, completion, failure-propagation, and server-cleanup coverage.
  - Recorded the review-fix quality evidence and current-main baseline in `prd.json`.
- Files changed
  - `ui/scripts/run-storybook-ci.test.mjs`
  - `prd.json`
  - `progress.txt`
- Verification
  - `bun test scripts/run-storybook-ci.test.mjs` — 11 tests passed.
  - `bun run typecheck`, `bun run lint`, focused Go transport/HTTP tests, `make test`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` — passed.
  - Detached exact-`origin/main` baseline at `dd87fd04d` passed `backend-size`, `pkg-maint`, `pkg-file-count`, `pkg-structure`, and the same UI lint gate; the final live base moved to `012e98e0f` during rebase and no new lint findings were observed against either fetched baseline.
- **Learnings for future iterations:**
  - Runner tests remain useful when they prove externally meaningful readiness, completion, failure, and cleanup behavior; exact command lists are brittle registration inventories and violate the behavioral-test rule.
  - A detached worktree is the reliable way to compare repository-wide lint baselines when the active worktree contains task changes and many sibling worktrees are moving independently.
---
## [2026-08-11 04:39 -07:00] - dashboard-fixture-and-quick-wins-final-main-sync
- What was implemented
  - Rebased the replacement head onto the live `origin/main` commit `012e98e0f` after `main` advanced during the first push, preserving the dashboard PRD and task commits.
  - Refreshed the PRD review evidence to name the actual final base commit.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Final replacement head `ba5d9cb3c` passed `bun test scripts/run-storybook-ci.test.mjs`, `bun run typecheck`, `bun run lint`, focused Go transport/HTTP tests, `make test`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check`.
  - Detached `origin/main` at `012e98e0f` passed `backend-size`, `pkg-maint`, `pkg-file-count`, and `pkg-structure`. The seven commits added to `main` after the earlier UI baseline touched only shared bookkeeping and CLI parameter tests, so the previously captured exact-main UI lint result remains applicable.
- **Learnings for future iterations:**
  - Fetch the hosting `main` ref immediately before the final push; a clean local rebase can become conflicted again when another merge lands between verification and publication.
---
## [2026-08-11 05:04 -07:00] - dashboard-fixture-and-quick-wins-coverage-race
- What was implemented
  - Added an explicit `sessionLists.waitUntilPending` readiness signal to the dashboard regression fixture controller.
  - Updated the typed Factory Sessions API fixture test to await that signal before resolving the controlled initial list response, removing the coverage-only scheduling race reported by PR CI.
- Files changed
  - `ui/src/components/dashboard/fixtures/dashboard-regression-controller.ts`
  - `ui/src/components/dashboard/fixtures/dashboard-regression.bun.unit.test.ts`
  - `prd.json`
  - `progress.txt`
- Verification
  - Focused dashboard regression Bun suite passed: 5 tests, 23 expectations.
  - The same focused suite passed with Bun coverage instrumentation.
  - `bun run typecheck` passed.
  - The broad local `bun run test:coverage` wrapper exceeded its 15-minute bound without producing a result; its orphaned coverage subprocesses were stopped, and this timeout is recorded rather than counted as a pass.
- **Learnings for future iterations:**
  - Controlled async fixtures need a first-class pending/ready signal so tests do not depend on incidental scheduling, especially under coverage instrumentation.
  - A focused instrumented rerun is useful evidence for a coverage race, but final confidence still depends on the hosted full coverage job.
---
## [2026-08-11 05:13 -07:00] - dashboard-fixture-and-quick-wins-latest-main-reconciliation
- What was implemented
  - Rebased the dashboard replacement head onto live `origin/main` at `3521f0b0a` after the hosting branch advanced again.
  - Unioned the complete current-main append-only `progress.txt` history with the dashboard task entries and retained the dashboard PRD as authoritative.
  - Updated the PRD review evidence to name the new live base and require replacement-head verification.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Rebase completed without source conflicts after resolving only the shared bookkeeping files.
  - `prd.json` parses; the five dashboard stories remain marked `passes: true`.
  - The fixture readiness fix and focused coverage evidence are retained in the rebased source tree.
- **Learnings for future iterations:**
  - Moving-base resolution for append-only logs must union full Git-object histories; choosing one side can silently drop unrelated mainline progress even when the source rebase is clean.
  - Rerun task-specific gates after the final mainline reconciliation because a replacement head is a new verification surface.
---
## [2026-08-11 05:16 -07:00] - dashboard-fixture-and-quick-wins-rebased-verification
- What was implemented
  - Revalidated the fixture readiness fix and dashboard transport surface after the final rebase onto `3521f0b0a`.
  - Kept the fixture controller lint-clean by moving the reusable readiness/waiter helpers outside the construction function and using a block-bodied waiter callback.
- Files changed
  - `ui/src/components/dashboard/fixtures/dashboard-regression-controller.ts`
  - `prd.json`
  - `progress.txt`
- Verification
  - `bun test src/components/dashboard/fixtures/dashboard-regression.bun.unit.test.ts` passed: 5 tests, 23 expectations.
  - The same focused suite passed with Bun coverage instrumentation.
  - `bun run typecheck`, `bun run lint`, focused Go transport/HTTP tests, and `git diff --check` passed on the rebased head.
- **Learnings for future iterations:**
  - A small fixture synchronization helper can trip both callback-return and function-size lint rules; extract reusable control helpers before adding further fixture orchestration.
---
## [2026-08-11 05:33 -07:00] - dashboard-fixture-and-quick-wins-bun-mock-isolation
- What was implemented
  - Restored Bun module mocks after each `discoverSessionNamedFactoryNames` unit test so its mocked Factory Sessions barrel cannot leak into the dashboard regression fixture in the shared coverage process.
  - Kept the dashboard fixture's explicit pending readiness synchronization in place; the CI failure was caused by the leaked module mock replacing the typed API, not by the fixture controller.
- Files changed
  - `ui/src/api/session-factory/import-activation.discover.bun.unit.test.ts`
  - `prd.json`
  - `progress.txt`
- Verification
  - The mock-owning unit file passed: 3 tests, 5 expectations.
  - The dashboard regression unit file passed: 5 tests, 23 expectations.
  - The affected dashboard test passed with direct Bun coverage instrumentation.
  - The exact hosted Frontend Coverage job had reproduced the leak as a 5-second dashboard test timeout; a fresh hosted run is required after this fix.
- **Learnings for future iterations:**
  - Bun's unit coverage lane runs multiple files in one process, so module mocks are process-scoped even when each test file is logically independent; always clean up module mocks at the owning test boundary.
---
## [2026-08-11 06:02 -07:00] - dashboard-fixture-and-quick-wins-ci-follow-up
- What was implemented
  - Replaced the session-factory discovery Bun test's process-global `mock.module` with a queued `fetch` boundary against the real typed Factory Sessions API, eliminating the shared coverage-process race.
  - Made default-selector discovery resolve the canonical `isDefault` session row after selector-only `~default` rows are removed by list normalization.
  - Updated the two existing browser integration journeys for the renamed `Open Factory` dialog and explicit `Open selected target` confirmation.
- Files changed
  - `ui/src/api/session-factory/import-activation.ts`
  - `ui/src/api/session-factory/import-activation.discover.bun.unit.test.ts`
  - `ui/integration/dashboard-session-tabs.integration.test.mjs`
  - `ui/integration/factory-import-second-session.integration.test.mjs`
  - `prd.json`
  - `progress.txt`
- Verification
  - `bun run typecheck` passed.
  - Full Bun unit lane passed: 196 tests across 36 files; the lcov coverage variant also passed with 196 tests.
  - `bun run lint` passed.
  - Focused browser integration for both affected files passed: 2 test files, 3 tests.
  - `make test`, `make vet`, `make backend-size`, `make pkg-maint`, `make pkg-file-count`, `make pkg-structure`, and `git diff --check` passed.
- **Learnings for future iterations:**
  - Updating a visible action label is not enough for browser coverage: integration scenarios must follow the complete semantic journey, including renamed dialog titles and newly explicit confirmation controls.
  - Canonical session-list normalization requires selector resolution to consult the row's `isDefault` marker when callers still use the `~default` selector.
---
## [2026-08-11 06:25 -07:00] - dashboard-fixture-and-quick-wins-functional-coverage-follow-up
- What was implemented
  - Investigated the hosted Backend Functional Coverage failure and isolated it to the untouched `TestJavaScriptParallelDispatchesChildrenConcurrently` test, which observed one active child call instead of two under the parallel suite scheduler.
  - Made no unrelated Go product or test changes; the named test passed once and across ten local repetitions.
  - Reran only the failed Backend Functional Coverage job and its dependent Verification Policy gate on the pushed dashboard head; both passed.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Hosted run `31494337558` is terminal-green with all 18 PR checks passing, including Backend Functional Coverage and Verification Policy.
  - The PR remains based on `3521f0b0a2f313b22e2c79edcf90339a01b3bce7`; the local worktree was clean before this bookkeeping update.
- **Learnings for future iterations:**
  - When broad functional coverage names an untouched concurrency-sensitive fixture, repeat that exact test locally before widening the feature diff; a clean repeated characterization supports rerunning only the failed coverage and policy jobs.
---
## [2026-08-11 06:45 -07:00] - dashboard-fixture-and-quick-wins-final-review-audit
- What was implemented
  - Rechecked PR #1882 against the latest review feedback and confirmed that the stale pre-fix blocking comments are addressed by the rebased, green head `1023eefe6`.
  - Recorded the thread-aware review audit, clean mergeability, exact `prd.json` PR-body match, and latest hosted verification run in the PRD.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - Five PRD stories remain marked `passes: true`.
  - The current PR head is cleanly based on `main` at `3521f0b0a2f313b22e2c79edcf90339a01b3bce7`; GitHub reports `MERGEABLE` / `CLEAN`.
  - Thread-aware GraphQL audit found no unresolved inline review threads; the two top-level blocking comments refer to the superseded pre-fix head.
  - Hosted run `31496150336` is terminal-green with all 18 PR checks passing, including Frontend, Frontend Browser, Backend Functional Coverage, and Verification Policy.
  - The PR body matches `prd.json` byte-for-byte; `git diff --check` passes.
- **Learnings for future iterations:**
  - When a review comment names an old head, verify the current PR head, mergeability, thread state, checks, and PR-body parity before making another source change; stale gate comments can remain in the conversation after their required fixes are already on the branch.
---
## [2026-08-11 07:06 -07:00] - dashboard-fixture-and-quick-wins-current-main-reconciliation
- What was implemented
  - Rebased the dashboard branch onto the newer live `origin/main` commit `b99c8b942` after GitHub reported a merge conflict caused by mainline advancement.
  - Preserved the dashboard PRD as authoritative and rebuilt the append-only progress log from the complete current-main history plus all 14 dashboard-only entries.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `prd.json` parses and all five dashboard stories remain marked `passes: true`.
  - The rebased worktree has no conflict markers; progress contains all 538 current-main headings plus the 14 dashboard-only headings.
  - Fresh hosted verification is intentionally pending for the new rebased head.
- **Learnings for future iterations:**
  - When main advances through another bookkeeping-heavy merge, retain the active task PRD, use current main as the progress-log base, append the task-only history, and rerun all hosted checks on the replacement ancestry.
---
## [2026-08-11 07:50 -07:00] - graph-workstation-semantics current-main reconciliation
- What was implemented
  - Merged the live `origin/main` commit `635a43870` after the PR became dirty again, preserving the graph PRD as authoritative and unioning the dashboard mainline progress history with the graph task history.
  - Revalidated the complete public replay semantic projection and guarded-control regression on the new merge head.
- Files changed
  - `prd.json`
  - `progress.txt`
  - Mainline dashboard and documentation files carried by the merge.
- Verification
  - `prd.json` parses with four user stories and zero incomplete `passes` values; no conflict markers remain.
  - Factory graph typecheck, 54 tests, build, package-boundary, and 31-file package-pack verification passed.
  - Factory visualizers typecheck, 31 tests, and lint passed; `git diff --check` passed.
- **Learnings for future iterations:**
  - Package build/typecheck scripts mutate shared dependency `dist` directories, so dependent package verification must run sequentially; parallel build jobs can race and temporarily remove generated declarations.
  - Fetch `origin/main` immediately before final publication and record the exact live hash because mainline bookkeeping merges can make an otherwise green PR dirty again.
---
## [2026-08-11 08:17 -07:00] - graph-workstation-semantics-004 review follow-up audit
- What was implemented
  - Rechecked the existing PR #1877 and verified that the remaining top-level public-replay blocker was posted before commit `06e3e8fa9`, which already forwards the complete package-owned workstation semantic projection instead of manually dropping `guardedControl`.
  - Confirmed the guarded public-replay regression is present and that the branch contains no unresolved inline review threads; no additional source change was needed in this iteration.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `ui/packages/factory-graph`: 54 tests passed.
  - `ui/packages/factory-visualizers/src/factory-topology-replay.semantics.test.tsx`: 1 test passed.
  - Thread-aware PR audit: 0 review threads and 0 unresolved inline threads.
  - Existing hosted PR head `9ff965503` reported all required checks terminal-successful and `MERGEABLE`/`CLEAN`.
- **Learnings for future iterations:**
  - Compare review-comment timestamps and the current source head before changing code; a top-level blocker can be stale when the requested fix and behavioral regression landed in a later commit.
## [2026-08-11 07:40:54 -07:00] - thr-split-acp-stdio-tests current-main-reconciliation
- What was implemented
  - Synchronized the task branch with the newer live `origin/main` commit `635a43870` and retained the ACP-focused split as the task-side resolution for the overlapping test moves.
  - Refreshed all three PRD evidence notes to the current-main baseline and recorded the replacement-head verification.
- Files changed
  - `prd.json`
  - `progress.txt`
- Verification
  - `go test ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` passed.
  - `go test -race ./pkg/transports/acp/internal/stdio -count=1 -timeout 300s` passed; `gofmt -l` reported no files.
  - Against detached `origin/main` `635a43870`, normalized inventory remained 222/222 with zero diff and SHA-256 `da83686cb68f0988570ca0ea0f429eabf40beb2bb47dba967874a33d46481bf1`; coverage remained 94.3% before and after.
  - `make typecheck backend-size pkg-maint pkg-file-count pkg-structure vet` passed.
  - `make pkg-boundary` reports no blocking package-boundary violations; its two ownership findings match the exact detached current-main baseline with zero diff.
  - `prd.json` parses with all three stories marked `passes: true`, conflict markers are absent, and `git diff --check` passes.
- **Learnings for future iterations:**
  - The current-main ACP baseline already contains a different partial test split, so the task branch must preserve the complete focused-suite layout while proving the normalized inventory and coverage remain identical.
  - Compare boundary-gate output against the detached current-main checkout when the aggregate gate reports existing ownership debt; identical findings are not new task regressions.
---
