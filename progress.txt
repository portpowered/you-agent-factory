## Codebase Patterns
- Inline single-use workflow-activity layout class strings directly on JSX in `workflow-activity-bento-card.tsx`; assert the graph panel shell via the viewport region’s `section[aria-labelledby]` parent in `workflow-activity-bento-card.test.tsx` (contains `relative`, `h-full`, `min-h-0`, `min-w-0`).
- Parse worker/workstation `openCodeAgent` through `pkg/config/agents_config.go` with `validateOpenCodeAgentInFrontmatter` (rejects explicit empty or whitespace-only values), merge via `applyWorkerRuntimeExecution` / `applyWorkstationRuntimeIdentity`, and resolve dispatch precedence with `interfaces.ResolveOpenCodeAgent(workstation, worker)`.
- Forward resolved OpenCode agent on `interfaces.ProviderInferenceRequest.OpenCodeAgent` from `pkg/workers/executor/agent.go` `inferenceRequestForExecutionRequest` only when `ModelProvider` is `opencode`; lookup workstation via `runtimeConfig.Workstation(inferenceWorkstationType(request))` and leave the field empty for other providers.
- Append `opencode run --agent <name>` in `openCodeProviderBehavior.BuildArgs` when `req.OpenCodeAgent` is non-empty, placing `--agent` after `--model` and before `--session`; empty agent leaves today's `opencode run` arg shape unchanged.
- Record configured OpenCode agents as `opencode_agent` in provider request metadata from `workDiagnosticsForInferenceRequest` in `pkg/workers/executor/diagnostics.go` and `pkg/workers/provider/helpers.go` only when `OpenCodeAgent` is non-empty; allowlist via `isSafeProviderMetadataKey` in `pkg/interfaces/safe_diagnostics.go`.
- Reject non-empty resolved `openCodeAgent` when `interfaces.ResolveRunnerSelection(...)` is not `opencode` via `interfaces.ValidateOpenCodeAgentForRunnerSelection(...)` from `pkg/service/factory_build.go` `validateConfiguredWorkstationRunners` before worker load; error text includes `openCodeAgent`, agent name, required runner `opencode`, and resolved runner ID.
- Header-only structured submit-work (`name`, `workTypeName`, `items: []`) is accepted in `pkg/api/submit_work_items.go` via empty-array early returns in `validateSubmitWorkItemsField` and `submitWorkItemsToContent`; API proof lives in `TestSubmitWork_AcceptsHeaderOnlyStructuredSubmitWork` and `TestGeneratedAPIIntegrationSmoke_SubmitWorkItemsAcceptHeaderOnlyStructuredSubmission`.
- Dashboard header-only submit proof belongs in `submit-work-widget.test.tsx` (`completes header-only submission when the default text item is blank`) and the empty-payload segment of `App.follow-up-submit.test.tsx`; assert POST body `items: []`, success trace status copy, and absence of submit error fallback.
- Hide internal `__system_time` factory graph nodes with `filterFactoryGraphTopologyForCustomerDisplay` in `ui/src/features/factory-graph-editor/lib/factory-graph-customer-display.ts`; reuse `systemTime.ts` constants/predicates and apply the filter at graph render boundaries (layout/projection) before seeding nodes or edges.
- Apply customer-display filtering in `projectFactoryGraphToReactFlow` only; keep `buildFactoryGraphTopologyFromDefinition` and draft-validation topology unfiltered so canonical factory data and editor mutations stay complete.
- Register new packaged `you docs` topics in the single `topicDocuments` table in `pkg/cli/docs/docs.go` with canonical name, description, `displayOrder`, path, and optional aliases together so `SupportedTopics()`, index output, and unsupported-topic errors stay aligned.
- Keep `docs/reference/*.md` and `pkg/cli/docs/reference/*.md` synchronized when adding customer-facing packaged topics; embedded markdown is loaded from `pkg/cli/docs/reference/` via `//go:embed`.
- When a new canonical topic is added, update hardcoded topic-order assertions in `pkg/cli/docs/docs_test.go`, `pkg/cli/root_docs_test.go`, and `tests/functional/smoke/cli_docs_smoke_test.go` unsupported-topic error expectations.
- When trimming `authoring-factories` after new topics land, point readers at `you docs mock-workers` and `you docs record-replay` and update `TestMarkdown_AuthoringFactoriesReturnsRawAuthoredMarkdown` markers away from removed deep-dive strings.

## 2026-05-29T14:50:44Z - docs-extensions-mock-workers-and-record-001
- What was implemented: Added canonical `mock-workers` packaged docs topic with synchronized reference markdown, `docs.go` registration at display order 25, and test expectation updates for topic order and unsupported-topic errors.
- Files changed: `docs/reference/mock-workers.md`, `pkg/cli/docs/reference/mock-workers.md`, `pkg/cli/docs/docs.go`, `pkg/cli/docs/docs_test.go`, `pkg/cli/root_docs_test.go`, `tests/functional/smoke/cli_docs_smoke_test.go`
- **Learnings for future iterations:**
  - Patterns discovered: New topics slot between existing `displayOrder` values (used 25 between config 20 and work 30); `TestMarkdown_ReturnsRawPackagedMarkdownForEachSupportedTopic` automatically covers new topics via `topicRegistry.ordered`.
  - Gotchas encountered: `TestSupportedTopicCommands` and smoke unsupported-topic strings must include new canonical topics in deterministic order.
  - Useful context: Mock-worker JSON contract and examples are consolidated from `authoring-factories.md` § Test Workflows With Mock Workers; story 003 will trim duplication there.
---

## 2026-05-29T15:12:00Z - docs-extensions-mock-workers-and-record-002
- What was implemented: Added canonical `record-replay` packaged docs topic with synchronized reference markdown, `docs.go` registration at display order 26, focused `TestMarkdown_RecordReplayReturnsRawAuthoredMarkdown`, and topic-order expectation updates across docs and smoke tests.
- Files changed: `docs/reference/record-replay.md`, `pkg/cli/docs/reference/record-replay.md`, `pkg/cli/docs/docs.go`, `pkg/cli/docs/docs_test.go`, `pkg/cli/root_docs_test.go`, `tests/functional/smoke/cli_docs_smoke_test.go`
- **Learnings for future iterations:**
  - Patterns discovered: Record/replay customer content mirrors `authoring-factories.md` § Start the factory recording subsection; use `displayOrder` 26 immediately after `mock-workers` (25) before `work` (30).
  - Gotchas encountered: Incompatible-flag bullets use markdown backticks (`--record` with `--replay`); test markers must include those backticks when asserting exact phrases.
  - Useful context: `sample-run.replay.json` is referenced as an example path in docs but is not checked in; story 003 will add cross-links from config and trim authoring-factories duplication.
---

## 2026-05-29T16:05:00Z - docs-extensions-mock-workers-and-record-003
- What was implemented: Trimmed duplicated mock-worker and record/replay depth from `authoring-factories`, added `Run Controls` brief mentions with canonical links in `config`, updated `docs/reference/README.md` packaged-topic table, and aligned `docs_test` authoring-factories markers with cross-links.
- Files changed: `docs/reference/README.md`, `docs/reference/authoring-factories.md`, `docs/reference/config.md`, `pkg/cli/docs/reference/authoring-factories.md`, `pkg/cli/docs/reference/config.md`, `pkg/cli/docs/docs_test.go`
- **Learnings for future iterations:**
  - Patterns discovered: After extracting topics, replace deep sections with `you docs <topic>` pointers and keep only quick-start `you run` commands needed for the walkthrough; sync `docs/reference/authoring-factories.md` to `pkg/cli/docs/reference/` with `cp` when they must stay identical.
  - Gotchas encountered: `TestMarkdown_AuthoringFactoriesReturnsRawAuthoredMarkdown` still asserted record/replay example commands removed from authoring-factories; update markers to `you docs mock-workers` / `you docs record-replay` when trimming.
  - Useful context: Packaged `config` (`pkg/cli/docs/reference/config.md`) and layout `docs/reference/config.md` differ; both need run-control cross-links for story 003.
---

## 2026-05-30T01:12:00Z - cron-workstation-ui-hide-001
- What was implemented: Added shared `factory-graph-customer-display` module with system-time graph predicates and `filterFactoryGraphTopologyForCustomerDisplay`, plus unit tests for pure system-time, mixed, and customer-only factory fixtures.
- Files changed: `ui/src/features/factory-graph-editor/lib/factory-graph-customer-display.ts`, `ui/src/features/factory-graph-editor/lib/factory-graph-customer-display.test.ts`
- **Learnings for future iterations:**
  - Patterns discovered: Graph node IDs follow `nodeKeyId` prefixes (`work-type:`, `work-state:<workType>:<state>`, `workstation:<name>`); filter by hidden node ID set and drop incident edges, matching resource-availability hiding in layout tests.
  - Gotchas encountered: Empty `worker: ""` on utility workstations still materializes a `worker:` node after filtering system-time nodes; tests should assert absence of `__system_time` IDs, not zero nodes.
  - Useful context: Mixed fixture from `factoryTimelineSnapshotFactoryGraph.test.ts` is a good source for cron/system-time topology tests; stories 002–003 wire the filter into `buildCurrentActivityGraphLayoutFromFactory` and React Flow projection.
---

## 2026-05-30T01:15:00Z - cron-workstation-ui-hide-002
- What was implemented: Wired `filterFactoryGraphTopologyForCustomerDisplay` into `buildCurrentActivityGraphLayoutFromFactory` before resource-availability skipping; added layout test asserting raw topology still contains system-time nodes while rendered layout output does not.
- Files changed: `ui/src/features/workflow-activity/lib/current-activity-factory-graph-layout.ts`, `ui/src/features/workflow-activity/lib/react-flow-current-activity-card-graph.test.ts`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: Apply customer-display filtering on the topology object immediately after `buildFactoryGraphTopologyFromDefinition`; resource-availability collapsing still runs on the filtered topology during node/edge seeding.
  - Gotchas encountered: Biome expects `factory-graph-customer-display` imports before `factory-graph-draft.test-helpers` in graph test files.
  - Useful context: Mirror resource-availability layout tests—assert `buildFactoryGraphTopologyFromDefinition` still exposes internal nodes, then assert `buildCurrentActivityGraphLayoutFromFactory` node IDs and edge endpoints exclude `__system_time`.
---

## 2026-05-30T01:18:00Z - cron-workstation-ui-hide-003
- What was implemented: Applied `filterFactoryGraphTopologyForCustomerDisplay` inside `projectFactoryGraphToReactFlow` at the React Flow display boundary; added projection test proving raw topology retains system-time nodes while projected nodes and edges omit them.
- Files changed: `ui/src/features/factory-graph-editor/lib/factory-graph-react-flow-projection.ts`, `ui/src/features/factory-graph-editor/lib/factory-graph-react-flow-projection.test.ts`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: Draft validation and graph operations should keep full topology from `buildFactoryGraphTopologyFromDefinition`; only `projectFactoryGraphToReactFlow` (and current-activity layout) should filter for customer display.
  - Gotchas encountered: Connection-anchor building must use the filtered `displayTopology` so hidden nodes are not referenced when resolving connection targets.
  - Useful context: Reuse the mixed `mixed-public-system-time` fixture from customer-display or layout tests for raw-vs-projected assertions.
---

## 2026-05-30T01:20:00Z - cron-workstation-ui-hide-004
- What was implemented: Added `cronSystemTimeFactoryDefinition` / `dashboardSnapshotWithCronSystemTimeFactory` and a React Flow current-activity card DOM test that asserts layout and rendered controls omit `__system_time` work type, pending state, and expire utility nodes while the customer cron workstation stays selectable.
- Files changed: `ui/src/features/workflow-activity/components/react-flow-current-activity-card.test.tsx`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: Card tests should assert both `buildCurrentActivityGraphLayoutFromFactory` node IDs and DOM queries (`work-type:__system_time` label, workstation/state select buttons) because the card mounts async layout from `snapshot.factory`.
  - Gotchas encountered: Workstation graph node IDs use authored `name` (`workstation:Nightly Cron`) while `onSelectWorkstation` callbacks use `id || name` (`nightly-cron`); keep both in mind when wiring fixtures.
  - Useful context: Browser verification via dev-browser skill was not run this iteration; the Vitest card test is the automated E2E proof for story 004.
---

## 2026-05-30T12:00:00Z - submit-work-defaults-001
- What was implemented: Renamed and strengthened the API regression test to `TestSubmitWork_RejectsHeaderOnlyStructuredSubmitWork`, documenting that POST `/work` with only `name`, `workTypeName`, and `items: []` returns 400 with structured-items validation before backend relaxation.
- Files changed: `pkg/api/server_submit_work_test.go`
- **Learnings for future iterations:**
  - Patterns discovered: Dashboard blank-text path sends explicit `items: []`; distinguish from omitted `items` (legacy payload/content path) when changing validation in `submit_work_items.go`.
  - Gotchas encountered: `TestSubmitWork_RejectsEmptyStructuredItems` name did not satisfy PRD wording for header/type-only documentation; prefer explicit test naming tied to dashboard operator flow.
  - Useful context: Functional smoke `TestGeneratedAPIIntegrationSmoke_SubmitWorkItemsRejectEmptyStructuredSubmission` covers whitespace-only items, not `items: []`; story 002 adds acceptance coverage there.
---

## 2026-05-30T12:30:00Z - submit-work-defaults-002
- What was implemented: Relaxed structured submit-work validation and conversion to accept explicit `items: []`, flipped API test to `TestSubmitWork_AcceptsHeaderOnlyStructuredSubmitWork`, and added `TestGeneratedAPIIntegrationSmoke_SubmitWorkItemsAcceptHeaderOnlyStructuredSubmission` for runtime acceptance.
- Files changed: `pkg/api/submit_work_items.go`, `pkg/api/server_submit_work_test.go`, `tests/functional/runtime_api/api_generated_smoke_test.go`
- **Learnings for future iterations:**
  - Patterns discovered: Empty `items: []` is valid header-only structured submit-work; return `[]interfaces.WorkContentPart{}` from `submitWorkItemsToContent` and skip meaningful-item checks in `validateSubmitWorkItemsField` when the unmarshaled array length is zero.
  - Gotchas encountered: `waitForGeneratedWorkComplete` returns `ListWorkResponse`, not a single `Work`; assert on `workList.Results[0]` like other generated smoke tests.
  - Useful context: Whitespace-only non-empty `items` arrays still fail with `items must contain at least one non-empty item`; story 003 verifies dashboard widget end-to-end.
---

## 2026-05-30T02:05:00Z - submit-work-defaults-003
- What was implemented: Strengthened dashboard header-only submit regressions to assert blank default text item, POST body with `items: []`, 201 success trace status copy, and no submit error fallback in the widget test and app-shell follow-up submit flow.
- Files changed: `ui/src/features/submit-work/components/submit-work-widget.test.tsx`, `ui/src/App.follow-up-submit.test.tsx`, `docs/internal/processes/development-guide-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: `buildStructuredSubmitItems` omits blank text items so the default empty `Text item 1` yields `items: []` without UI changes; mirror the full happy-path assertions from the text submission test (trace success + no `errorFallback` copy).
  - Gotchas encountered: `App.follow-up-submit.test.tsx` already asserted empty-payload POST bodies but not post-submit success messaging for the header-only segment.
  - Useful context: Rename widget tests to describe operator flow (`header-only`, `default text item is blank`) so PRD story wording maps directly to test discovery.
---

## 2026-05-30T18:30:00Z - opencode-agent-support-001
- What was implemented: Added `openCodeAgent` to worker and workstation config structs, AGENTS.md frontmatter parsing with non-empty validation, runtime merge through factory build, and `interfaces.ResolveOpenCodeAgent` for workstation-over-worker precedence.
- Files changed: `pkg/interfaces/factory_config.go`, `pkg/interfaces/work_payload_lineage.go`, `pkg/config/agents_config.go`, `pkg/config/layout.go`, `pkg/config/runtime_config.go`, `pkg/config/agenttests/agents_config_test.go`, `pkg/config/runtimetests/runtime_config_test.go`, `pkg/interfaces/interfaces_contract_test.go`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: Use `validateOpenCodeAgentInFrontmatter` on the raw frontmatter map so explicit `openCodeAgent: ""` fails while omitted keys stay optional; workstation override precedence lives in `ResolveOpenCodeAgent` for later dispatch stories.
  - Gotchas encountered: Struct-only validation cannot distinguish omitted vs empty YAML strings; check the raw frontmatter map when empty explicit values must fail.
  - Useful context: Story 002 should validate `openCodeAgent` against resolved runner; story 003 should populate `ProviderInferenceRequest.OpenCodeAgent` from `ResolveOpenCodeAgent`.
---

## 2026-05-30T20:15:00Z - opencode-agent-support-002
- What was implemented: Added `interfaces.ValidateOpenCodeAgentForRunnerSelection` and wired it into `validateConfiguredWorkstationRunners` so non-empty `openCodeAgent` fails when resolved runner is not `opencode`; added contract and service-layer tests including `loadWorkersFromConfig` integration.
- Files changed: `pkg/interfaces/work_payload_lineage.go`, `pkg/interfaces/interfaces_contract_test.go`, `pkg/service/factory_build.go`, `pkg/service/factory_test_helpers_test.go`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: Validate OpenCode agent against the same `ResolveRunnerSelection` result used for runner preflight; wrap errors as `workstations[i](name).openCodeAgent` for operator-scoped paths.
  - Gotchas encountered: Default codex runner applies when only `openCodeAgent` is set on a claude worker, so mismatches surface even without explicit runner overrides.
  - Useful context: Story 003 should populate `ProviderInferenceRequest.OpenCodeAgent` from `ResolveOpenCodeAgent` after this guard passes.
---

## 2026-05-30T21:00:00Z - opencode-agent-support-003
- What was implemented: Added `OpenCodeAgent` to `ProviderInferenceRequest`, populated it in `inferenceRequestForExecutionRequest` for OpenCode model dispatches via `ResolveOpenCodeAgent`, and added executor plus clone contract tests.
- Files changed: `pkg/interfaces/work_dispatch.go`, `pkg/interfaces/work_runtime_test.go`, `pkg/workers/executor/agent.go`, `pkg/workers/executor/agent_test.go`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: Gate `OpenCodeAgent` on effective `ModelProvider` (`opencode`), not raw runner fields alone; workstation lookup uses `inferenceWorkstationType(request)` to match dispatch naming.
  - Gotchas encountered: `inferenceRequestForExecutionRequest` needed an optional `*FactoryWorkstationConfig` parameter—pass `nil` in unit tests that only exercise worker fields.
  - Useful context: Story 004 should read `req.OpenCodeAgent` in `openCodeProviderBehavior.BuildArgs`; story 005 should add `opencode_agent` to provider request metadata in `workDiagnosticsForInferenceRequest`.
---

## 2026-05-30T22:00:00Z - opencode-agent-support-004
- What was implemented: Appended `--agent <name>` to `openCodeProviderBehavior.BuildArgs` when `ProviderInferenceRequest.OpenCodeAgent` is set, ordered after `--model` and before `--session`; added BuildArgs tests for agent-only and full-flag shapes.
- Files changed: `pkg/workers/provider/provider_behavior.go`, `pkg/workers/provider/provider_behavior_test.go`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: PRD flag order for OpenCode is `run`, `--model`, `--agent`, `--session`, `--dir`, `--dangerously-skip-permissions`, then the user prompt; keep `TestOpenCodeProviderBehavior_BuildArgs` cases aligned when adding metadata in story 005.
  - Gotchas encountered: `inference_provider_test.go` OpenCode integration case omits `OpenCodeAgent` by design—empty agent must preserve existing `wantArgs` without `--agent`.
  - Useful context: Story 005 should add `opencode_agent` to provider request metadata in `workDiagnosticsForInferenceRequest` without changing BuildArgs ordering.
---

## 2026-05-30T23:30:00Z - opencode-agent-support-005
- What was implemented: Added `opencode_agent` to provider request metadata in executor and provider `workDiagnosticsForInferenceRequest` when `OpenCodeAgent` is set, allowlisted the key in safe diagnostics projection, and added provider plus executor regression tests.
- Files changed: `pkg/workers/executor/diagnostics.go`, `pkg/workers/executor/agent_test.go`, `pkg/workers/provider/helpers.go`, `pkg/workers/provider/diagnostics_test.go`, `pkg/interfaces/safe_diagnostics.go`, `docs/internal/processes/factory-workstation-relevant-files.md`
- **Learnings for future iterations:**
  - Patterns discovered: Omit `opencode_agent` from request metadata entirely when unset rather than emitting an empty string; mirror the conditional in both `pkg/workers/executor/diagnostics.go` and `pkg/workers/provider/helpers.go`.
  - Gotchas encountered: Safe projection drops unknown metadata keys—add new provider request metadata keys to `isSafeProviderMetadataKey` or inference events will lose them downstream.
  - Useful context: Story 006 extends OpenAPI factory schemas; story 007 documents `openCodeAgent` for factory authors.
---

## 2026-05-30T08:57:00Z - inline-workflow-activity-bento-card-class-001
- What was implemented: Removed `GRAPH_PANEL_SHELL_CLASS` from `workflow-activity-bento-card.tsx`, inlined `relative h-full min-h-0 min-w-0` on the graph panel shell `<section>`, and added a rendering test that asserts those classes on the shell wrapping the viewport region.
- Files changed: `ui/src/features/workflow-activity/components/workflow-activity-bento-card.tsx`, `ui/src/features/workflow-activity/components/workflow-activity-bento-card.test.tsx`
- **Learnings for future iterations:**
  - Patterns discovered: Graph panel shell is the parent of the workflow graph `section[aria-labelledby]`; locate it from `screen.getByRole("region", { name: messages.viewportLabel })` via `closest("section[aria-labelledby]")?.parentElement`.
  - Gotchas encountered: Story 002 must remove the stale `GRAPH_PANEL_SHELL_CLASS` allowlist entry; inline-class guard will fail until then.
  - Useful context: `ReactFlowCurrentActivityCardView` keeps its own `CURRENT_ACTIVITY_CARD_CLASS` on the inner workflow section—do not conflate with the bento card outer shell.
---

## 2026-05-30T02:16:00Z - inline-workflow-activity-bento-card-class-002
- What was implemented: Removed stale `workflow-activity-bento-card.tsx#GRAPH_PANEL_SHELL_CLASS` entry from `inline-component-class-usage-allowlist.mjs` after story 001 inlined the shell classes; verified `bun run check:inline-component-class-usage`, allowlist guard tests, and `workflow-activity-bento-card.test.tsx`.
- Files changed: `ui/scripts/inline-component-class-usage-allowlist.mjs`
- **Learnings for future iterations:**
  - Patterns discovered: After inlining a single-use class constant, remove the matching `file#CONSTANT` allowlist entry in the follow-up story so `check:inline-component-class-usage` reports no stale exceptions.
  - Gotchas encountered: The allowlist guard test file is `ui/scripts/check-inline-component-class-usage.test.mjs`, not `.ts`.
  - Useful context: No PR conversation feedback existed this iteration; PR creation follows when all PRD stories pass.
---
