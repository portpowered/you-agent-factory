## Codebase Patterns
- Deterministic fake durable sessions live in `pkg/factorysessionexecution/` (`FakeService`, `NewFakeServiceFromContractFixtures`, `WithFakeScenarios`); scenarios are keyed by execution `requestId` and loaded from `pkg/api/testdata/durable-session-contract-fixtures.json` plus built-in `BuiltinInterruptedRecoverableScenario()`.
- Prove fake projections at the service layer in `pkg/factorysessionexecution/fake_service_test.go` and through apisurface mappers in `pkg/apisurface/factorysession/factory_session_fake_consumer_test.go`.
- Start idempotency replays on `requestId` via `startReplay` map; conflicting tuples return `ErrExecutionRequestIDConflict`. Concurrent starts are safe under `FakeService.mu`.
- Fixture `asyncResponse.links` may be partial; stability on replay matters more than requiring every inspection link field to be populated.
- Use `workflowsource.KindFactoryID` (not string literals) when constructing `factorysessionexecution.StartRequest` in tests.
- Fixture `session` rows may omit `progress`/`artifactCount` while `listSummary` carries them; call `enrichSessionReadProjection` when loading scenarios so `GetSession` detail matches scoped list rows and persisted seeds.
- Use `ValidateSessionDetailMatchesListSummary` to prove list/detail coherence for phase, progress, result summary, artifact count, actions, links, and recoverability.
- Final result mode returns `NOT_READY` for running and interrupted sessions that only have partial progress; `FAILED_WITH_PARTIAL` remains the terminal final read for failed-with-partial scenarios.
- Use `ValidateDispatchDetailMatchesListSummary` and `ValidateArtifactDetailMatchesListSummary` to prove dispatch/artifact list/detail coherence; artifact detail may carry retrieval refs on `ContentRef` while list rows use `RetrievalRef`.
- `enrichArtifactDetailFromListSummary` copies list retrieval refs onto sparse artifact detail fixtures during scenario load.

## 2026-06-11T11:25:55Z - fake-session-service-001
- Added story-focused tests proving deterministic fake async starts for success, running, failed-with-partial, and interrupted scenarios; unknown `requestId` validation errors; stable replay of session id, status, and inspection links; and concurrent idempotent starts producing one session with stable dispatch/event projections.
- Added apisurface consumer test mapping the four core start scenarios through `StartRequestFromAPI` / `AsyncStartResponseToAPI`.
- Removed stray comment fragment in `fake_service.go`.
- Files changed: `pkg/factorysessionexecution/fake_service.go`, `pkg/factorysessionexecution/fake_service_test.go`, `pkg/apisurface/factorysession/factory_session_fake_consumer_test.go`
- **Learnings for future iterations:**
  - The fake service implementation was already present on the branch from prior contract-kernel work; story 001 mainly needed explicit acceptance tests rather than new service logic.
  - Running scenario `req-petri-run-001` has dispatch id `disp-petri-001`, not `disp-petri-run-001`.
  - Interrupted recoverable scenario is built-in (`req-js-interrupted-001`) and is not present in the contract fixtures JSON; start it by request id directly in consumer tests.
  - `make verify-fast` may fail locally when `tsc` is not on PATH; run focused `go test` for backend-only story work.
---

## 2026-06-11T12:10:00Z - fake-session-service-002
- Added `ValidateSessionDetailMatchesListSummary` and fixture `enrichSessionReadProjection` so session detail reads include progress/artifact metadata aligned with durable list summaries.
- Added service tests for scoped live/persisted/all listing determinism, lifecycle scope membership, detail/list consistency, and not-found reads that do not mutate state.
- Added apisurface consumer test mapping scoped list and detail reads through `ListSessionsRequestFromAPI`, `ListSessionsResponseToAPI`, and `SessionReadResponseToAPI`.
- Files changed: `pkg/factorysessionexecution/projection_consistency.go`, `pkg/factorysessionexecution/fake_fixture.go`, `pkg/factorysessionexecution/fake_service_test.go`, `pkg/factorysessionexecution/projection_test.go`, `pkg/apisurface/factorysession/factory_session_fake_consumer_test.go`
- **Learnings for future iterations:**
  - Persisted list rows for started terminal sessions come from preloaded `persistedSeeds` (fixture `listSummary`), while live rows always include every in-memory session; scope=all dedupes terminal ids from live via `DeduplicateLiveSessionsForAllScope`.
  - Contract fixture `session` documents can be sparser than `listSummary`; enrich on scenario load instead of expecting clients to reconcile the gap.
  - `SessionReadResult` exposes action availability indirectly via `DeriveSessionActionAvailability(status)`; durable list rows materialize `Actions` explicitly.
---

## 2026-06-11T13:05:00Z - fake-session-service-003
- Tightened final-mode result shaping so interrupted sessions with only partial progress report `NOT_READY` instead of exposing partial data as a final result.
- Added dispatch/artifact list-detail validators and artifact detail enrichment from list retrieval refs for sparse fixtures.
- Added service tests for success/running/failed-partial/interrupted result reads, dispatch failure/interruption metadata, artifact retrieval linkage, and not-found dispatch/artifact reads.
- Added apisurface consumer test mapping coherent result, dispatch, artifact, and interrupted-final reads through projection mappers.
- Files changed: `pkg/factorysessionexecution/fake_service.go`, `pkg/factorysessionexecution/projection_consistency.go`, `pkg/factorysessionexecution/fake_fixture.go`, `pkg/factorysessionexecution/fake_service_test.go`, `pkg/factorysessionexecution/projection_test.go`, `pkg/apisurface/factorysession/factory_session_fake_consumer_test.go`
- **Learnings for future iterations:**
  - Contract `artifactDetail` fixtures can omit `retrievalRef` while list rows include it; enrich detail from list summaries instead of expecting fixture parity.
  - Failed dispatch detail for `javascript-failed-with-partial` is `disp-js-fail-002` with `VERIFY_ASSERTION_FAILED` and JavaScript `VERIFY` task metadata.
  - Interrupted builtin scenario exposes canceled dispatch `disp-js-interrupted-002` for interruption inspection reads.
---
