## Codebase Patterns
- Public CLI commands are authored in `contracts/cli/commands.json`; update the matching family ID list, constructor bindings, handler registry, and generated CLI artifacts together.
- Run `go run ./cmd/climanifestgen -root .` for CLI family artifacts and `go run ./cmd/contractsgenerate -root .` for published contract staging; `climanifest-check`, `contracts-check`, and CLI contract smoke catch drift.
- CLI identity and input baselines are rewritten through the `UPDATE_CLI_BASELINES=1` test helpers; run writers one package at a time because command and input baselines are shared files.
- Generated Work leaves without authored positional arguments preserve the existing inventory behavior; apply `cobra.NoArgs` only when a new command explicitly requires rejecting positional input.
- Long-lived CLI event streams use a dedicated no-timeout `clihttp.Protocol` and consume the canonical SSE route through generated Factory Event payload types.

## [2026-08-08 14:07 -07:00] - wo-c2-work-watch-event-stream-001
- What was implemented
  - Added the public generated `you work watch [--session <session>] [--follow]` Work-family command and stable handler/config bindings.
  - Added `you.work.watch.v1` NDJSON rendering with required transition fields, optional-field omission, JSON escaping, one-write line emission, validation, and write-error reporting.
  - Added parsing, validation, rendering, command-tree, registry, stdout/stderr separation, and generated-contract coverage.
  - Regenerated CLI family metadata, published CLI contract artifacts, command/input identity baselines, and command-tree baseline.
- Files changed
  - `contracts/cli/commands.json`, `packages/api/generated/cli/commands.json`, `packages/api/generated/manifest.json`
  - `contracts/testdata/baseline/cli-command-inputs.json`, `contracts/testdata/baseline/cli-commands.json`, `pkg/transports/cli/baseline/testdata/command_tree.txt`
  - `pkg/services/work/transports/cli/watch.go`, `pkg/services/work/transports/cli/watch_test.go`, `pkg/transports/cli/work/watch.go`
  - Work CLI constructor, manifest generator, registry, generated metadata, root bindings, and focused tests
- **Learnings for future iterations:**
  - The event-stream operation is not wired yet; the compatibility registry intentionally reports `work watch service is required` until story 002 supplies the canonical stream operation.
  - Keep the watch renderer transport-neutral so later stream/reducer code can emit complete lines without importing Cobra.
  - The repository’s public CLI contract includes both generated package staging and committed identity/help baselines; updating only the authored manifest is incomplete.
---

## [2026-08-08 14:28 -07:00] - wo-c2-work-watch-event-stream-002
- What was implemented
  - Wired the Work watch operation to the selected Factory Session’s canonical Factory Event SSE stream, with retained events delivered before live events and no Work list/snapshot polling.
  - Added a sequence-integrity reducer that projects Factory/Work metadata, emits only `WORK_STATE_CHANGE` transitions, suppresses exact replay duplicates, rejects conflicting IDs and non-monotonic canonical sequences, and derives terminal state from authoritative WorkState metadata.
  - Added finite-mode terminal stopping, final-line-before-exit behavior, already-terminal retained-cohort handling, and an injected no-timeout HTTP protocol for long-lived streams.
  - Regenerated Wire output and added reducer, finite fake-stream, and real HTTP SSE route/parser coverage.
- Files changed
  - `pkg/services/work/transports/cli/watch_reducer.go`, `pkg/services/work/transports/cli/watch_stream.go`, `pkg/services/work/transports/cli/watch.go`, `pkg/services/work/transports/cli/watch_test.go`
  - `pkg/transports/cli/root.go`, `pkg/transports/cli/root_work.go`, `pkg/transports/cli/sessionpath/sessionpath.go`, `pkg/transports/cli/work/watch.go`
  - `pkg/wire/cli_commands.go`, `pkg/wire/wire.go`, `pkg/wire/wire_gen.go`
  - `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - The session event endpoint already provides retained-then-live ordering; the CLI needs only one long-lived GET and a reducer, not a client-side polling or snapshot path.
  - Factory definition WorkState types should be preferred when classifying terminal transitions; Work payload state types are useful only as a fallback when definition metadata has not arrived.
  - Follow mode, cancellation, and reconnect cursor/backoff behavior are intentionally still isolated for Story 003; finite completion currently closes immediately after rendering the terminal transition.
---

## [2026-08-08 14:32 -07:00] - wo-c2-work-watch-event-stream-002 verification follow-up
- What was implemented
  - No additional story code; completed broader verification for the Story 002 implementation.
- Files changed
  - `progress.txt`
- **Learnings for future iterations:**
  - `go test ./pkg/wire/... ./pkg/transports/cli/... ./pkg/services/work/... -count=1` passes after Wire generation.
  - `make verify-fast` is currently blocked by the environment’s UI dependency/toolchain state: TypeScript cannot find the `bun` type definitions and rejects the existing deprecated `baseUrl` option.
  - `make test` reaches the full short Go suite but fails an unrelated Windows permission-mode assertion in `pkg/platform/runtimeartifact` (`0666` observed versus `0600` expected); the Work/CLI/Wire packages remain green.
---
