## Codebase Patterns
- Keep `FakeService` and `RuntimeService` as parallel `factorysessionexecution.Service` implementations: mock-provider transports inject `FakeService` via `NewFakeService`, `NewFakeServiceFromContractFixtures`, `WithFakeScenarios`, and `WithPersistedSessionSeeds` without JavaScript runtime setup; real simple-workflow execution uses `NewRuntimeService` behind the same contract. Prove coexistence in `fake_service_coexistence_test.go` alongside `fake_service_test.go` and runtime-backed tests.
- Use `factorysessionexecution.PrepareStart` to normalize durable JavaScript session starts: it chains `NormalizeStartRequest`, `resolveStartSourceWithResolution`, wrapped-source validation (IIFE wrap matching `workflowruntime.Run`), args JSON checks, `workflowpolicy.Resolve`, and wait-option validation before runtime execution.
- Runtime simple-workflow fixtures live in `pkg/orchestrators/javascript/runtime/testdata/`; factorysessionexecution tests can read them via `../orchestrators/javascript/runtime/testdata/<name>`.
- Durable start source resolution is shared through `resolveStartSourceWithResolution`; `ResolveStartSource` is a thin wrapper returning only `ResolvedSource`.
- `RuntimeService` request-id replay stores the first observed async/sync start responses in `runtimeStartReplayRecord` (`asyncStart` / `syncStart`); sync timeout replays must return the stored `TIMED_OUT` shape even after the session eventually completes. Idempotency tuple hashing excludes `wait` options—use args/source/policy changes to prove conflicts in tests.
- `factorysessionexecution.RuntimeService` implements async/sync simple-JS starts via `PrepareStart` + background `workflowruntime.Run` (`runSession` closes per-session `runtimeDone` when execution finishes). `StartAsync` returns immediate `RUNNING`; `StartSync` waits for terminal completion or `wait.timeoutMillis`, returns `COMPLETED` with projected result JSON or `TIMED_OUT` with `SYNC_WAIT_TIMED_OUT` availability while keeping the session running unless `wait.cancelOnTimeout` is true. In-memory `GetSession`/`GetResult`/`ReadEvents` read `runtimeSessionState`; canonical events use `BuildCanonicalRuntimeSessionEvents` (`runtime-service` source). Use `busy-loop.workflow.js` for non-terminating sync-timeout tests; use `simple-final.workflow.js` for completion; use a temp-dir spin-loop workflow when proving eventual completion after sync timeout.
- Terminal runtime session results normalize through `projectRuntimeSuccessResult` (`workflowresult.ProjectPrimaryResult` + `workcontent.GeneratedPtrFromParts`); durable `PrimaryResult` is WorkContent JSON (e.g. `[{"type":"JSON","json":{...}}]`), not raw workflow return JSON. Prove API-shaped parity with `factorysession.SessionReadResponseToAPI` / `ResultResponseToAPI` plus `ValidateResultMatchesSessionRead` in `runtime_projection_test.go`.
- Canonical runtime session lifecycle events replay through `ReplaySessionProjection` (`session_event_projection.go`) into durable `SessionReadResult`/`ResultReadResult` projections; `BuildCanonicalRuntimeSessionEvents` may include bounded `resultSummary` text on `SESSION_RESULT_UPDATED`, and sync-timeout paths rebuild events after `SYNC_WAIT_TIMED_OUT` availability changes. Prove live-vs-replayed parity and idempotent replay in `runtime_event_replay_test.go`.
- CI `Build, Lint, and API` runs `make backend-size` on owned Go packages (100-line function cap). Split large test functions with `t.Helper()` assertion helpers (see `runtime_projection_test.go`) rather than inline assertion blocks.
- Route durable lifecycle-control HTTP for `dur-sess-*` IDs through `apisurface.DurableSessionLifecycleAPI` (`handlers_factory_durable.go`); map typed `ControlError` outcomes to `409` lifecycle-control responses, missing sessions and dispatches to `404` via `factory_session_lifecycle_errors.go`. Inject `factorysessionexecution.Service` through `testutil.MockFactory.DurableExecutionService` for API server tests without full `FactoryService` wiring.
- Lifecycle-control `requestId` replay uses `ControlIdempotencyTupleHash` + shared `lookupControlReplay`/`storeControlReplay` in `control.go` on both `FakeService` and `RuntimeService`; tuple conflicts return `ControlError` with `CONFLICT` outcome without mutating session state, and accepted/terminal/invalid control errors replay the stored result or error for exact replays.
- `JavaScriptRuntimeService` lifecycle controls live in `pkg/factorysessionexecution/control.go` (`applyRuntimeExtendedLifecycleControl`, `applyRuntimeAcceptedLifecycleControl`) with `controlReplay` on the service struct; port runtime lifecycle tests to `pkg/factorysessionexecution/fixtures/runtime_lifecycle_control_test.go` using `newJavaScriptRuntimeServiceWithFixture`.
- Real-backend API slice contract tests use `assertRealBackendSessionAPISliceRoutes` for in-scope lifecycle/read routes and `assertDeferredRealBackendSessionRouteFamilies` with an empty deferred map once lifecycle controls ship; `server_durable_session_deferred_routes_test.go` proves implemented lifecycle routes return typed responses instead of `501`.
- `RuntimeService` lifecycle controls live in `pkg/factorysessionexecution/control.go` (`applyRuntimeExtendedLifecycleControl`, `applyRuntimeAcceptedLifecycleControl`) to stay within package file-count and backend-size limits; fixture-backed `FakeService` scenarios cover approve/retry-dispatch at the HTTP boundary when runtime-backed guarded approvals or retryable dispatches are unavailable.
- Lifecycle-control read-parity regression tests live in `pkg/api/servertests/server_durable_session_lifecycle_read_parity_test.go`: exercise pause/resume/cancel on runtime-backed sessions, then prove list/get/result/event reconnect, dispatch/artifact reads, and inspection links (`FactorySessionExecutionLinks` on session reads; `FactorySessionLifecycleControlLinks` on control responses) remain stable.

## 2026-06-11T12:49:03Z - dynamic-workflows-cell-session-backend-runtime-001
- Implemented `PrepareStart`, `StartPrepareContext`, and `PreparedStart` in `pkg/factorysessionexecution/prepare_start.go` to normalize real JavaScript session starts through orchestrator-owned source resolution, validation, policy projection, args checks, and wait validation before runtime execution.
- Refactored `ResolveStartSource` to delegate to shared `resolveStartSourceWithResolution`.
- Added focused tests for successful preparation and invalid source/args/policy/wait rejection paths using runtime `simple-final` and `syntax-error` fixtures.
- Updated `docs/internal/processes/api-relevant-files.md` with the `PrepareStart` wiring pattern.
- **Learnings for future iterations:**
  - Pre-execution source validation must wrap executable source in an IIFE like `workflowruntime.Run`; validating raw top-level `return` fixtures fails incorrectly.
  - Policy denial surfaces as `ValidationError` with field `requestedPolicy`; source issues use field `source`.
  - `PreparedStart` carries `SourceContent` (executable), `EffectivePolicy`, `PolicyProjection`, and `TupleHash` for downstream runtime-backed `StartAsync`/`StartSync` work in story 002+.
---

## 2026-06-11T13:15:00Z - dynamic-workflows-cell-session-backend-runtime-002
- Implemented `RuntimeService` with `StartAsync`, in-memory session projection (`GetSession`, `GetResult`, dispatch/artifact stubs, `ReadEvents`), and background `workflowruntime.Run` execution for simple JavaScript workflows.
- Added `BuildCanonicalRuntimeSessionEvents` for runtime-backed canonical `SESSION_STARTED` / `SESSION_RESULT_UPDATED` / `SESSION_COMPLETED` facts.
- Added focused tests for async start response shape, running-session inspection (`busy-loop`), event modeling, and `simple-final` background completion.
- Updated `docs/internal/processes/api-relevant-files.md` with the runtime service wiring pattern.
- **Learnings for future iterations:**
  - `StartAsync` must build and return the async response before launching the background goroutine so callers always observe `RUNNING` even for fast fixtures like `simple-final`.
  - Simple runtime sessions have zero dispatch/artifact stubs; progress counts stay at zero until fanout work lands in later cells.
  - `StartSync`, lifecycle controls, and richer result projection remain for stories 003–004; `RuntimeService.StartSync` currently returns a not-implemented error.
---

## 2026-06-11T12:59:59Z - dynamic-workflows-cell-session-backend-runtime-003
- Implemented `RuntimeService.StartSync` with sync wait budgeting, `COMPLETED` responses carrying projected `ResultReadResult` JSON, and `TIMED_OUT` responses that keep sessions `RUNNING`, set `SYNC_WAIT_TIMED_OUT` availability, and allow background execution to finish unless `wait.cancelOnTimeout` cancels the runtime context.
- Refactored runtime execution through shared `runSession` with per-session `runtimeDone` signaling used by both async and sync paths.
- Added focused tests for sync completion (`simple-final`), sync timeout shape (`busy-loop`), and eventual result preservation after timeout (temp-dir `slow-final` spin loop).
- Updated `docs/internal/processes/api-relevant-files.md` with sync runtime service wiring.
- **Learnings for future iterations:**
  - Sync completed `Result` JSON comes from `ProjectResultRead` + `json.Marshal`; runtime `PrimaryResult` is still the raw workflow JSON object until story 004 normalization lands.
  - `busy-loop.workflow.js` never terminates, so use it only for timeout-shape assertions; prove eventual completion after timeout with a finite spin-loop fixture in a temp project root.
  - Request-id replay for sync timeout responses that must reproduce `TIMED_OUT` while still `RUNNING` is story 005; current replay derives from live session state via `syncStartFromStateLocked`.
---

## 2026-06-11T14:30:00Z - dynamic-workflows-cell-session-backend-runtime-004
- Added `projectRuntimeSuccessResult` to normalize terminal runtime results through `workflowresult.ProjectPrimaryResult` and `workcontent.GeneratedPtrFromParts`, updating session `ResultSummary`, artifact refs/counts, and canonical result state on success.
- Added `runtime_projection_test.go` proving runtime-backed `GetSession`/`GetResult` parity with API-shaped mappers, list-summary action/recoverability metadata, and projection consistency validators for completed, running, and sync-timeout sessions.
- Updated sync completion assertions to expect WorkContent `JSON` primary-result parts instead of raw workflow JSON.
- Updated `docs/internal/processes/api-relevant-files.md` with runtime result projection wiring.
- **Learnings for future iterations:**
  - Durable `PrimaryResult` stores marshaled `factoryapi.WorkContent` arrays; tests must assert `type:"JSON"` parts, not raw workflow objects.
  - `DurableListSummaryFromSessionRead` is the right helper for recoverability/action availability checks on runtime session reads.
  - `ValidateResultMatchesEventProjection` only compares latest `SESSION_RESULT_UPDATED` payload `resultStatus`; keep session/result status aligned when rebuilding events after normalization.
---

## 2026-06-11T15:10:00Z - dynamic-workflows-cell-session-backend-runtime-005
- Stored first observed async/sync start responses in `runtimeStartReplayRecord` so exact `requestId` replays return the original response shape; sync timeout replays stay `TIMED_OUT`/`RUNNING` even after background completion.
- Added `runtime_idempotency_test.go` covering async replay after completion, sync completed replay, sync timeout immediate/after-completion replay, and tuple conflict rejection without new sessions.
- Updated `docs/internal/processes/api-relevant-files.md` with runtime idempotency wiring.
- **Learnings for future iterations:**
  - Idempotency tuple hashing intentionally excludes `wait` options; conflict tests must change source/args/policy, not timeout millis alone.
  - `busy-loop` never completes—use `slow-final` temp fixture when proving replay shape after eventual runtime success following sync timeout.
  - Async replay must return stored `RUNNING` response, not live terminal session status from `asyncStartFromState`.
---

## 2026-06-11T16:20:00Z - dynamic-workflows-cell-session-backend-runtime-006
- Added `ReplaySessionProjection` to reconstruct durable session/result read models from canonical `SESSION_STARTED` / `SESSION_RESULT_UPDATED` / `SESSION_COMPLETED` events without duplicating summaries, links, or artifact stubs on repeated replay.
- Sync-timeout sessions now rebuild canonical runtime events after `SYNC_WAIT_TIMED_OUT` availability is recorded; `SESSION_RESULT_UPDATED` payloads may carry bounded `resultSummary` text when present on the live projection.
- Added focused replay tests for completed, running, sync-timeout, and async-completed runtime sessions plus pure reducer/idempotency coverage.
- Increased `TestRuntimeService_StartSync_TimeoutPreservesEventualRuntimeResult` wait budget to 15s so background completion stays reliable under parallel package test load.
- Updated `docs/internal/processes/api-relevant-files.md` with runtime event replay wiring.
- **Learnings for future iterations:**
  - Event replay reconstructs lifecycle status, result summary status, source hash, policy hash, and links—not `PrimaryResult` JSON, which remains a live runtime projection concern.
  - `applySyncWaitTimeout` must rebuild `state.events` or replayed running-timeout sessions only show `SESSION_STARTED`.
  - `resultSummary` WorkContent parts use lowercase `text` type identifiers when embedded in canonical event payloads.
---

## 2026-06-11T17:00:00Z - dynamic-workflows-cell-session-backend-runtime-007
- Added `fake_service_coexistence_test.go` proving `FakeService` and `RuntimeService` both satisfy `factorysessionexecution.Service`, constructor/scenario-loading APIs (`NewFakeService`, `WithFakeScenarios`, `WithPersistedSessionSeeds`, `LoadFakeScenariosFromContractFixtures`) remain available, fixture event replay reconstructs live fake projections, and idempotency works on both paths without interference.
- Updated `docs/internal/processes/api-relevant-files.md` to document parallel fake/runtime service injection.
- **Learnings for future iterations:**
  - Fixture-backed terminal sessions may omit `Links.Events` on live reads while `ReplaySessionProjection` always projects event links—compare lifecycle/source/policy/result summary for fake replay parity, not every link field.
  - Contract idempotent-replay scenarios use `req-idempotent-replay-001` from `durable-session-contract-fixtures.json`; custom request ids need explicit `WithFakeScenarios` registration.
  - `BuiltinInterruptedRecoverableScenario()` is the built-in recoverable fake scenario for `WithFakeScenarios` smoke tests without contract fixture files.
---

## 2026-06-11T18:30:00Z - mergeability
- Merged `origin/main` into the branch and resolved `docs/internal/processes/api-relevant-files.md` by combining main's JavaScript host-runtime and fixtures-catalog documentation with this cell's `PrepareStart` / `RuntimeService` / fake-runtime coexistence notes.
- Fixed flaky `TestRuntimeService_StartSync_IdempotentReplayTimeout` slow-final subtest by aligning its background-completion wait budget to 15s (matching `runtime_service_test.go`).
- Pushed merge commit `a32cefd73`; posted PR conversation follow-up on #803.
- **Learnings for future iterations:**
  - Under parallel `pkg/factorysessionexecution` test load, temp-dir `slow-final` spin-loop fixtures need 15s completion waits, not 5s.
  - Main's `api-relevant-files.md` may reference `canonical_events.go` while the package still uses `listing.go` for `BuildCanonicalSessionEvents` / `BuildCanonicalRuntimeSessionEvents`—prefer the actual file names when merging docs.
  - `go vet` rejects `StartSync` holding a `context.WithCancel` func without calling it on every return path—only create a cancellable runtime context when `wait.cancelOnTimeout` is true and `defer cancel()` inside the background goroutine.
---

## 2026-06-11T18:45:00Z - mergeability (CI)
- Fixed `go vet` context-leak failure in `RuntimeService.StartSync` by scoping cancellable runtime contexts to `cancelOnTimeout` sessions and deferring `cancel()` inside the background goroutine.
- Pushed fix commit `408399daf`.
---

## 2026-06-11T19:10:00Z - mergeability (CI)
- Fixed `make backend-size` failure: `TestRuntimeService_ReadProjections_MatchAPIShapedExpectations` exceeded the 100-line function limit (112 lines). Extracted `assertCompletedRuntimeSessionRead`, `assertAPIShapedCompletedSessionRead`, and `assertCompletedRuntimeResultProjection` helpers in `runtime_projection_test.go`.
- Pushed fix commit `96b699d42`.
- **Learnings for future iterations:**
  - CI `Build, Lint, and API` runs `make backend-size` after `make vet`; large table-driven or assertion-heavy tests should use `t.Helper()` assertion helpers early to stay under the 100-line function cap.
  - `go vet` passing locally does not imply the full `Build, Lint, and API` job is green—run `make backend-size` when adding substantial test functions in owned backend packages.
---

## 2026-06-16T12:00:00Z - dynamic-workflows-cell-real-backend-api-lifecycle-control-001
- Implemented runtime-backed `Cancel` and `Terminate` in `RuntimeService` via `applyRuntimeLifecycleControl`, per-session `runCancel`, and canceled-runtime projection to `CANCELED`/`SESSION_CANCELED` availability.
- Routed `POST /factory-sessions/{session_id}/cancel` and `/terminate` for `dur-sess-*` session IDs through `DurableSessionLifecycleAPI` handlers with typed success (`200`/`202`) and conflict/not-found mapping in `factory_session_lifecycle_errors.go`.
- Extended `testutil.MockFactory` with durable execution/lifecycle delegation for focused API server tests.
- Added runtime and API tests in `runtime_lifecycle_control_test.go` and `server_durable_session_lifecycle_control_test.go`.
- Updated `docs/internal/processes/api-relevant-files.md` with runtime lifecycle-control and handler wiring notes.
- **Learnings for future iterations:**
  - `RuntimeService` must store `runCancel` on `runtimeSessionState` for both async and sync starts so lifecycle controls can interrupt `workflowruntime.Run`.
  - Accepted cancel returns `202` with `CANCELING`; accepted terminate returns `200` with `TERMINATED`.
  - Non-`dur-sess-*` session IDs should keep the existing `501` live-session stub until live lifecycle parity lands.
  - API lifecycle tests can seed sessions through `DurableExecutionService.StartAsync` while proving HTTP cancel/terminate behavior through `MockFactory`.
---


## 2026-06-16T20:15:00Z - dynamic-workflows-cell-real-backend-api-lifecycle-control-003
- Routed `POST /factory-sessions/{session_id}/approve` and `/retry-dispatch` for `dur-sess-*` session IDs through `handleDurableApproveControl` / `handleDurableRetryDispatchControl`, `MockFactory` durable lifecycle delegation, and `RuntimeService.applyRuntimeExtendedLifecycleControl` in `control.go`.
- Added fixture-backed API tests for approve (`AWAITING_APPROVAL` → `RUNNING`), retry-dispatch on failed sessions, terminal-session retry conflict, and non-durable `501` stubs; runtime tests for approve invalid-state and missing-dispatch not-found.
- Mapped `ErrDispatchNotFound` to `404` in `factory_session_lifecycle_errors.go`.
- Updated `docs/internal/processes/api-relevant-files.md` with approve/retry handler and runtime lifecycle-control wiring.
- **Learnings for future iterations:**
  - Approve/retry use distinct OpenAPI request bodies (`FactorySessionApproveRequest`, `FactorySessionRetryDispatchRequest`); share decode via generic `decodeOptionalJSONRequest` but keep separate handler entrypoints.
  - `pkg/factorysessionexecution` is at the 15-file cap—extend `control.go` for runtime lifecycle mutations instead of adding `runtime_lifecycle_control.go`.
  - Fixture-backed `FakeService` is the right API-boundary proof path for approve/retry when runtime sessions lack `AWAITING_APPROVAL` or dispatch fanout.
---

## 2026-06-16T21:30:00Z - dynamic-workflows-cell-real-backend-api-lifecycle-control-004
- Added shared lifecycle-control `requestId` replay via `lookupControlReplay`/`storeControlReplay` in `control.go`, wired `controlReplay` on `RuntimeService`, and refactored `FakeService` to use the same helpers.
- Extended `ControlErrorToAPI` to include post-control inspection links on typed conflict/terminal/invalid responses.
- Added runtime and API tests for idempotent control replay, request-id tuple conflicts without mutation, validation `400`, and dispatch-not-found `404`; extended mapper/error unit tests.
- Updated `docs/internal/processes/api-relevant-files.md` with control replay wiring notes.
- **Learnings for future iterations:**
  - Control replay must run before dispatch existence checks so retry-dispatch replays match `FakeService` ordering.
  - Terminal/invalid control errors should be stored in `controlReplay` so exact `requestId` replays return the same typed `409` shape without re-evaluating live session state.
  - `ControlErrorToAPI` can derive inspection links from `sessionId` even when the service `ControlError` omits a links field.
---

## 2026-06-16T18:30:00Z - dynamic-workflows-cell-real-backend-api-lifecycle-control-002
- Implemented runtime-backed `Pause` and `Resume` in `RuntimeService` through `applyRuntimeLifecycleControl` with projection-only state transitions (`PAUSED`/`RUNNING`, `pausedAt`/`resumedAt`) that do not invoke `runCancel`.
- Routed `POST /factory-sessions/{session_id}/pause` and `/resume` for `dur-sess-*` session IDs through `DurableSessionLifecycleAPI` handlers; extended `testutil.MockFactory` pause/resume delegation to match cancel/terminate.
- Added runtime and API tests for accepted pause/resume, terminal-session `409` conflict vocabulary, and non-durable `501` live stubs in `runtime_lifecycle_control_test.go` and `server_durable_session_lifecycle_control_test.go`.
- Updated `docs/internal/processes/api-relevant-files.md` with pause/resume runtime and handler wiring notes.
- **Learnings for future iterations:**
  - `applyRuntimeLifecycleControl` must gate `runCancel` behind an `interruptRuntime` flag so pause/resume do not kill the background `workflowruntime.Run` goroutine.
  - Terminal-session pause/resume map to `TERMINAL_SESSION` `ControlError` and HTTP `409` lifecycle-control responses at the API boundary—the same pattern as cancel on succeeded sessions.
  - Fixture-backed `FakeService` pause/resume scenarios already exist in `fixtures/lifecycle_test.go`; runtime-backed API tests can prove the HTTP path with `busy-loop` sessions.
---

## 2026-06-16T22:30:00Z - dynamic-workflows-cell-real-backend-api-lifecycle-control-005
- Merged `origin/main` (session-parity and dispatch-artifact lanes) and ported lifecycle controls from `RuntimeService` to `JavaScriptRuntimeService` with `controlReplay` on the service struct.
- Moved lifecycle-control routes into `assertRealBackendSessionAPISliceRoutes`, cleared lifecycle entries from `assertDeferredRealBackendSessionRouteFamilies`, and replaced deferred-route `501` regression with `TestRealBackendFactorySessionRoutes_LifecycleControlsAreImplemented`.
- Added lifecycle request-normalization mapper tests (`ControlRequestFromAPI`, `ApproveRequestFromAPI`, `RetryDispatchRequestFromAPI`) and updated `api-relevant-files.md` for `JavaScriptRuntimeService` lifecycle wiring.
- **Learnings for future iterations:**
  - Main renamed `RuntimeService` to `JavaScriptRuntimeService` with `NewExecutionService(ExecutionProviderJavaScriptRuntime)`; lifecycle methods belong on `JavaScriptRuntimeService` in `control.go`, not stubbed in `runtime_service.go`.
  - `runtime_lifecycle_control_test.go` lives in `pkg/factorysessionexecution/fixtures/` as `fixtures_test` and reuses `newJavaScriptRuntimeServiceWithFixture`.
  - Merge from main can duplicate `MockFactory` durable methods and `isDurableExecutionSessionID`; keep main's full execution/read implementations and our lifecycle delegation.
---

## 2026-06-16T23:45:00Z - dynamic-workflows-cell-real-backend-api-lifecycle-control-006
- Added `server_durable_session_lifecycle_read_parity_test.go` with focused API regression tests proving pause/resume/cancel lifecycle controls preserve durable list/get/result/event reconnect reads, dispatch/artifact list/detail parity, and stable inspection links on session and lifecycle-control responses.
- Updated `docs/internal/processes/api-relevant-files.md` with lifecycle read-parity test wiring notes.
- **Learnings for future iterations:**
  - `FactorySessionExecutionLinks` on session reads exposes session/results/events/status only; dispatch/artifact links appear on `FactorySessionLifecycleControlLinks` after controls—prove dispatch/artifact read reachability via direct GET paths in parity tests.
  - Running durable sessions appear in `scope=all` live `sessions`, not `scope=persisted` (`IsPersistedListCandidate` excludes non-terminal/non-recoverable rows).
  - Runtime-backed pause/resume return HTTP `200` (synchronous projection), not `202` like cancel.
---
