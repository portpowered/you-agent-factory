## Codebase Patterns
- Run-scoped Worker selections must be forwarded through both the hosted runtime opening request and the detached Factory Session invocation target before reaching Worker execution.
- Public CLI behavior should be proven through `root.BuildProcess` and `Process.Execute`, replacing external effects only with injected `edges.Edges` ports.
- File-backed invocation text must be read through Work's injected `SubmittedFileReader` and carried as prepared input; transports must not re-tokenize prompt bytes.

## [2026-08-09 08:40 -07:00] - wo-c3-run-parity-long-prompt-input-001
- What was implemented
  - Added the typed `--worker-reasoning-effort` run input with shared canonical effort normalization and actionable invalid-value errors before runtime/provider dispatch.
  - Forwarded the normalized override through hosted and detached invocation runtime construction, where it overrides authored Worker effort while preserving authored defaults when omitted.
  - Regenerated the CLI contract/help artifacts and added focused CLI, runtime-boundary, and end-to-end Codex command-edge regressions.
- Files changed
  - CLI run configuration, manifest/source consumers, generated contract/help baselines, Factory Session invocation/runtime-opening boundaries, Worker runtime assembly/executor, focused tests, and `prd.json`.
- **Learnings for future iterations:**
  - The clean ad-hoc invocation path uses `factorysessions.InvocationTarget` and does not reuse the hosted `RuntimeOpeningRequest`; every run-scoped Worker selection must be mapped into both paths.
  - The functional harness can observe the exact provider command while preserving architecture by using `support.BuildProcess(t, edges).Execute(...)` with `serviceedges.Edges`.
  - Focused verification passed with `go test ./pkg/transports/cli/run ./pkg/transports/cli ./pkg/services/factory_sessions/internal/runtimeopening/... ./pkg/services/workers/... ./pkg/wire/... -count=1` and the two new lifecycle tests.
---
## [2026-08-09 09:15 -07:00] - wo-c3-run-parity-long-prompt-input-002
- What was implemented
  - Added the authored `you.run.flag.to-file` CLI input and regenerated the manifest, published contract, CLI baselines, and wire composition.
  - Added Work-owned file-backed invocation preparation with injected reading, valid UTF-8 and logical non-empty checks, exact content preservation, file/named/positional/stdin conflict classification, signature primary-parameter routing, and source metadata.
  - Added early rejection for file prompts on `--work`, continuous, replay, and JavaScript workflow run shapes while preserving existing non-file input behavior.
  - Added focused policy/CLI tests and an end-to-end lifecycle regression using a real 30-line UTF-8 prompt at a path containing spaces, asserting exact Codex `ProviderCommandRunner` stdin and zero dispatch for conflicts/unreadable input.
- Files changed
  - Work invocation input contracts, compatibility/signature normalization and preparation, CLI run configuration/parsing/error mapping, authored/generated CLI contracts and baselines, wire generation, focused tests, and functional lifecycle coverage.
- **Learnings for future iterations:**
  - The default Work submitted-file reader is the existing `serviceedges.Edges.WorkSubmittedFileReader` seam; production wiring must pass it into invocation preparation rather than reading files in the CLI transport.
  - A workstation prompt template that renders the first input payload provides an observable exact provider-command assertion without exposing or bypassing the runtime boundary.
  - Verification passed with `go test ./pkg/services/work/... ./pkg/transports/cli/... ./pkg/wire/... -count=1`, the full CLI lifecycle package, and the focused file-backed lifecycle test.
---
