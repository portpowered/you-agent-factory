// Package logging provides policy-free terminal and runtime log sinks for
// composition roots to inject into Factory, Factory Session, worker, model,
// and transport owners. Domain packages remain responsible for deciding what
// to log and how logging failures affect their own behavior.
package logging
