## Codebase Patterns

- Validate repo-truth claims about `factory/inputs/**` with `git ls-files`
  before trusting visible local files; the repo tracks sentinels while live
  work items are ignored.
- Read `factory/logs/meta/asks.md` before submitting cleanup ideas; it is the
  canonical maintainer-routing surface and can explicitly forbid queue work.
- Refresh the meta model from live git state, not stale summaries; merge state,
  ignore rules, and local dirtiness can change within hours.
- Check `factory/factory.json` directly when summarizing the workflow because
  the maintainer loop includes `to-complete`, `consume`, per-worktree
  execution, shared resource limits, cron cleanup, and loop breakers that are
  easy to omit in compact prose.

## Current Summary

- As of `2026-05-02T22:01:36.2540358-07:00`, `HEAD` and `origin/main` both
  point to `bda83e1`; branch divergence is `0/0`.
- The canonical ask surface remains `factory/logs/meta/asks.md`, and the
  current local ask text scopes this pass to compact meta-log cleanup only.
- The checked-in maintainer workflow is still
  `thoughts -> idea -> plan -> task`, with `process`/`review` repeaters in
  `.claude/worktrees/{{name}}`, `to-complete` holding states, a same-name
  `consume` join, an hourly cleaner, a shared `executor-slot` capacity of `10`,
  and loop breakers.
- The tracked input surface remains sentinel-only; visible files under
  `factory/inputs/**` are ignored local residue.
- Recent merged cleanup PRs remain `#61`, `#65`, `#64`, `#63`, and `#62`.
- The worktree is dirty outside this pass because of local UI edits, a local
  edit to `factory/logs/meta/asks.md`, and untracked helper files.

## 2026-05-02T22:01:36.2540358-07:00 - meta state refresh

- Ran `git pull`; the branch was already up to date with `origin/main`.
- Re-read `factory/logs/meta/view.md`, `factory/logs/meta/progress.txt`,
  `factory/logs/meta/asks.md`, `factory/logs/agent-fails.json`,
  `factory/logs/agent-fails.replay.json`, `docs/standards/STANDARDS.md`,
  `factory/README.md`, `docs/guides/batch-inputs.md`, `factory/factory.json`,
  and `.gitignore`.
- Re-checked tracked versus ignored workflow inputs with `git ls-files`,
  `Get-ChildItem`, and `git check-ignore`.
- Re-checked recent merge history and merged PR metadata for `#61`, `#65`,
  `#64`, `#63`, `#62`, and `#60`.
- Confirmed the checked-in ask still forbids new task submissions, so this
  iteration only refreshed the meta world model and did not queue cleanup work.
